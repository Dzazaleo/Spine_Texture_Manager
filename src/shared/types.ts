/**
 * Phase 1 — Shared IPC types (placeholder seeded in Plan 01-01 Task 2).
 *
 * This module will house SkeletonSummary, PeakRecordSerializable, LoadResponse,
 * SerializableError, and the Api interface — the shapes that cross the
 * main ↔ preload ↔ renderer boundary via structuredClone-safe IPC.
 *
 * Seeded empty by Plan 01-01 Task 2 to satisfy TypeScript 6.x's TS18003 gate
 * (tsconfig.web.json requires at least one input file). The real interfaces
 * land in Plan 01-02 Task 1 per Wave 0 ordering.
 *
 * These interfaces erase at compile time. No runtime code lives in this file.
 */
export {};
