---
status: complete
phase: 22.1-close-7-gaps-from-phase-22-human-uat-dims-02-badge-mode-awar
source:
  - 22.1-01-loader-unified-actualsource-and-rotation-rejection-SUMMARY.md
  - 22.1-03-export-partition-restructure-SUMMARY.md
  - 22.1-04-dims-badge-tooltip-primitive-SUMMARY.md
  - 22.1-05-optimize-dialog-passthrough-rows-SUMMARY.md
started: 2026-05-03T00:00:00Z
updated: 2026-05-03T00:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 7
name: Export panel dims — override accuracy (post-CR-001 fix)
expected: |
  With the drift fixture loaded, set an override on SQUARE (e.g. 80%).
  The Peak W×H column in GlobalMaxRenderPanel updates to reflect the override
  relative to source dims (670×670), not canonical dims (1000×1000).
  Open OptimizeDialog — the output dims shown there match the panel.
awaiting: complete

## Tests

### 1. DimsBadge tooltip fires on hover
expected: In the GlobalMaxRenderPanel, hover over a dims-mismatch ! badge. A styled React tooltip appears (not a browser native tooltip) with text about the source/atlas dims. Hover out — tooltip disappears. Repeat 3 times — fires reliably each cycle.
result: pass

### 2. DimsBadge tooltip wording — atlas-source mode
expected: Still hovering a dims-mismatch badge in a normal project (atlas-source mode). The tooltip text starts with "Atlas region declares X×Y" (not "Source PNG"). The actual dims and canonical dims are mentioned.
result: pass

### 3. DimsBadge in AnimationBreakdownPanel
expected: In the AnimationBreakdownPanel (lower panel), expand an animation card that has a dims-mismatch attachment. Hover the ! badge there — same styled tooltip fires. Confirms sibling symmetry (both panels use the same component).
result: pass

### 4. DimsBadge cap suffix — with drift fixture
expected: |
  Run: npm run fixture:atlas-source-drift
  Load the output path it prints into the app.
  Find the SQUARE attachment (dims-mismatch badge visible — drifted atlas).
  Set its override to 100%. Hover the ! badge.
  Tooltip includes a cap suffix line like "Capped at source resolution" or similar.
  Set override back to 50% — cap suffix disappears.
result: pass

### 5. OptimizeDialog passthrough row dim shape
expected: Open a project. Click Optimize. In the pre-flight dialog, passthrough rows (COPY chip) show a dim label in the format "833×759 → 833×759" (source → target). No "(already optimized)" text anywhere in the dialog.
result: pass

### 6. OptimizeDialog (capped) suffix on drifted row
expected: With the atlas-source-drift fixture loaded, open OptimizeDialog. The SQUARE attachment row (drifted, cap fires) shows a muted "(capped)" label after the COPY chip. Other rows do not show it.
result: pass

### 7. Export panel dims — override accuracy (post-CR-001 fix)
expected: With the atlas-source-drift fixture, set an override on the SQUARE attachment (e.g. 80%). The Peak W×H column in the GlobalMaxRenderPanel updates to reflect the override relative to source dims (not canonical dims). The OptimizeDialog shows the same output dims as the panel.
result: issue
reported: "panel showed 360×360 instead of 536×536 — fix applied during UAT (computeExportDims using sourceW instead of canonicalW as outW base)"
severity: major

## Summary

total: 7
passed: 6
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

