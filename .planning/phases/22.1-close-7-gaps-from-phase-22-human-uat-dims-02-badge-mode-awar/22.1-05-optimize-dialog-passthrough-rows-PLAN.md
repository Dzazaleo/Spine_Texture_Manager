---
phase: 22.1
plan: 05
type: execute
wave: 2
depends_on:
  - 22.1-03
files_modified:
  - src/renderer/src/modals/OptimizeDialog.tsx
  - tests/renderer/optimize-dialog-passthrough-rows.spec.tsx
autonomous: false
requirements:
  - DIMS-04
gap_closure: false
must_haves:
  truths:
    - "G-05: '(already optimized)' parenthetical is removed from the OptimizeDialog passthrough row label. The COPY chip alone communicates the byte-copy semantic."
    - "G-06 D-08: Passthrough rows render the source→target dim shape ('670×670 → 670×670' when no override; '670×670 → 335×335' when 50% override pushes the row out of passthrough via plan 22.1-03's G-07 partition). Resize-row dim shape is preserved (no regression)."
    - "G-07 D-07: Cap-binding signal is surfaced in the OptimizeDialog passthrough row when ExportRow.isCapped === true. Rendering style is Claude's discretion: a small muted '(capped)' suffix is the recommended choice (consistent with existing row chrome; revisitable during HUMAN-UAT)."
    - "Phase 22 CHECKER FIX preserved: `row.actualSourceW ?? row.sourceW` fallback at the source-dim computation site. Still correct under the unified actualSource model since both resolve to the same value in atlas-source mode."
    - "Phase 6 D-109 muted-row UX preserved: passthrough rows continue to use opacity-60 / text-fg-muted styling parallel to excludedUnused rows."
    - "Per D-07, D-08 (locked in 22.1-CONTEXT.md): cap-binding signal in OptimizeDialog row + always-render source→target shape on passthrough rows."
  artifacts:
    - path: "src/renderer/src/modals/OptimizeDialog.tsx"
      provides: "Passthrough row JSX updated: '(already optimized)' removed; source→target dim shape (mirrors resize-row pattern); optional '(capped)' muted suffix when row.isCapped"
    - path: "tests/renderer/optimize-dialog-passthrough-rows.spec.tsx"
      provides: "G-05 (no '(already optimized)' string); G-06 D-08 (source→target dim shape rendered including the no-override case where source = target); G-07 D-07 (cap suffix when isCapped, no suffix otherwise)"
  key_links:
    - from: "src/renderer/src/modals/OptimizeDialog.tsx passthroughCopies block"
      to: "ExportRow.isCapped (added by plan 22.1-03)"
      via: "row.isCapped conditional rendering of '(capped)' muted suffix"
      pattern: "row\\.isCapped"
    - from: "src/renderer/src/modals/OptimizeDialog.tsx passthroughCopies block"
      to: "resize-row source→target dim shape (existing pattern at lines 459-475)"
      via: "{sourceDimW}×{sourceDimH} → {row.outW}×{row.outH} interpolation"
      pattern: "→.*\\{row\\.outW\\}"
---

<objective>
Update the OptimizeDialog passthrough row JSX (currently src/renderer/src/modals/OptimizeDialog.tsx:476-505) to: (a) drop the "(already optimized)" parenthetical (G-05); (b) render the source→target dim shape on every passthrough row, mirroring the resize-row shape (G-06 D-08); (c) surface a small muted "(capped)" suffix when ExportRow.isCapped is true (G-07 D-07). The Phase 22 CHECKER FIX `row.actualSourceW ?? row.sourceW` fallback is preserved.

Purpose: Pre-flight is the user's last sanity check before clicking Start. Today the passthrough rows show only `670×670` while resize rows show `833×759 → 833×759` — inconsistent. Worse, when a user applies a 50% override on a passthrough row (now correctly re-routed to resize by plan 22.1-03), the row leaves the passthroughCopies bucket and re-appears as a resize row with `670×670 → 335×335`. The user can verify their override decision before pulling the trigger. The cap-binding signal (G-07 D-07) gives the user a final hint when the cap clamped their requested scale — surfaced in the row label so it's right where they're looking when reviewing the plan.

Output:
- src/renderer/src/modals/OptimizeDialog.tsx: passthroughCopies row JSX restructured per the spec above.
- tests/renderer/optimize-dialog-passthrough-rows.spec.tsx: covers G-05, G-06, G-07 D-07 row rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22.1-close-7-gaps-from-phase-22-human-uat-dims-02-badge-mode-awar/22.1-CONTEXT.md
@.planning/phases/22.1-close-7-gaps-from-phase-22-human-uat-dims-02-badge-mode-awar/22.1-PATTERNS.md
@.planning/phases/22.1-close-7-gaps-from-phase-22-human-uat-dims-02-badge-mode-awar/22.1-03-export-partition-restructure-SUMMARY.md
@CLAUDE.md

<interfaces>
ExportRow type (post-plan-22.1-03 update in src/shared/types.ts) has fields: sourcePath, outPath, sourceW, sourceH, outW, outH, effectiveScale, attachmentNames, optional atlasSource, optional actualSourceW, optional actualSourceH, optional **isCapped: boolean** (NEW from plan 22.1-03).

Existing resize-row pattern at src/renderer/src/modals/OptimizeDialog.tsx:459-475 — renders `{row.sourceW}×{row.sourceH} → {row.outW}×{row.outH}` plus a `~Xx smaller` ratio chip. This is the shape to mirror onto passthrough rows.

Existing passthrough-row pattern at src/renderer/src/modals/OptimizeDialog.tsx:492-505 — currently renders `{row.actualSourceW ?? row.sourceW}×{row.actualSourceH ?? row.sourceH} (already optimized)` plus a `COPY` chip. The CHECKER FIX 2026-05-02 fallback `row.actualSourceW ?? row.sourceW` is locked-in and must be preserved.

Phase 6 D-109 muted-row UX — passthrough rows already use `opacity-60` and `text-fg-muted` parallel to excludedUnused rows. Preserve.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update OptimizeDialog passthrough row JSX (G-05 + G-06 D-08 + G-07 D-07)</name>
  <files>src/renderer/src/modals/OptimizeDialog.tsx, tests/renderer/optimize-dialog-passthrough-rows.spec.tsx</files>
  <read_first>
    - src/renderer/src/modals/OptimizeDialog.tsx (full file — confirm line numbers for the passthroughCopies block at PATTERNS.md-cited lines 476-505 + the resize-row pattern at lines 459-475 used as the dim-shape analog)
    - src/shared/types.ts (read ExportRow interface — confirm `isCapped?: boolean` was added by plan 22.1-03)
    - tests/renderer/optimize-dialog-passthrough.spec.tsx (full file — existing passthrough test patterns; particularly the makeRow/makePlan synthetic builders if present)
    - .planning/phases/22.1-close-7-gaps-from-phase-22-human-uat-dims-02-badge-mode-awar/22.1-CONTEXT.md §"### Dim Label Shape" (D-08) AND §"### G-07 Override semantics + partition" (D-07)
    - .planning/phases/22.1-close-7-gaps-from-phase-22-human-uat-dims-02-badge-mode-awar/22.1-PATTERNS.md §"### `src/renderer/src/modals/OptimizeDialog.tsx`" (canonical replacement code with comments)
    - .planning/phases/22.1-close-7-gaps-from-phase-22-human-uat-dims-02-badge-mode-awar/22.1-03-export-partition-restructure-SUMMARY.md (confirm ExportRow.isCapped emission semantics so the dialog can rely on the field)
  </read_first>
  <behavior>
    - Test 1 (G-05): the rendered passthrough row markup does NOT contain the substring `'(already optimized)'`.
    - Test 2 (G-06 D-08, no-override case): for a passthrough row with `sourceW=670, sourceH=670, outW=670, outH=670, actualSourceW=670, actualSourceH=670`, the rendered markup contains `'670×670 → 670×670'` (the source dim repeats — intentional per D-08 "even when source = target").
    - Test 3 (G-06 D-08, override case verifying the row left passthroughCopies): given a plan where SQUARE has a 50% override applied, that row is in `plan.rows` (NOT `plan.passthroughCopies`) and renders with the resize-row shape `'670×670 → 335×335'`. (The partition decision lives in plan 22.1-03; this test confirms the dialog correctly renders the resize variant.)
    - Test 4 (G-07 D-07, capped row): for a passthrough row with `isCapped: true`, the rendered markup contains the muted `(capped)` suffix.
    - Test 5 (G-07 D-07, uncapped row): for a passthrough row with `isCapped: false` (or undefined), the rendered markup does NOT contain `(capped)`.
    - Test 6 (CHECKER FIX preserved): for a passthrough row where `actualSourceW === undefined` (defensive case), the row renders `{row.sourceW}×{row.sourceH} → {row.outW}×{row.outH}` (fallback to sourceW) — proves the `??` fallback still works.
    - Test 7 (Phase 6 D-109 muted style preserved): the row's container className still contains `'opacity-60'` AND the dim-label span still contains `'text-fg-muted'`.
    - Test 8 (COPY chip preserved): the row contains the `COPY` chip element with the existing className shape (`uppercase`, border, etc.).
  </behavior>
  <action>
1. **Read src/renderer/src/modals/OptimizeDialog.tsx in full** — confirm current line numbers for the passthroughCopies block (PATTERNS.md cites lines 476-505) and the analog resize-row pattern (lines 459-475). Adjust line references if shifted.

2. **Replace the passthroughCopies row JSX** with the canonical PATTERNS.md shape:

```tsx
{plan.passthroughCopies.map((row) => {
  // G-06 D-08 (Phase 22.1) — source→target shape parity with resize rows.
  // For passthrough, outW === sourceW so the "→ X×Y" repeats the source dims;
  // this is intentional (preflight shows the user the row WILL become outW×outH
  // at their current override setting; if they set a 50% override, the row
  // leaves passthrough via plan 22.1-03's G-07 partition and reappears in
  // plan.rows above with the resize dim shape).
  // CHECKER FIX 2026-05-02 fallback `row.actualSourceW ?? row.sourceW`
  // preserved — still correct under the 22.1-01 unified model.
  const sourceDimW = row.actualSourceW ?? row.sourceW;
  const sourceDimH = row.actualSourceH ?? row.sourceH;
  return (
    <li key={row.outPath} className="py-1 border-b border-border last:border-0 opacity-60">
      <span className="text-fg-muted">{row.outPath}</span>
      <span className="ml-2 text-fg-muted">
        {sourceDimW}×{sourceDimH} → {row.outW}×{row.outH}
      </span>
      <span className="ml-2 inline-block border border-border rounded-sm px-1 text-[10px] uppercase">
        COPY
      </span>
      {/* G-07 D-07 (Phase 22.1) — cap-binding signal. Rendered as a small
          muted "(capped)" suffix when ExportRow.isCapped (set by plan 22.1-03's
          buildExportPlan emit loop when downscaleClampedScale > sourceRatio).
          Per CONTEXT D-07: "If it's too noisy we can remove it later." */}
      {row.isCapped && (
        <span className="ml-2 text-fg-muted text-[10px]">(capped)</span>
      )}
    </li>
  );
})}
```

**PRESERVE EVERY ADJACENT ELEMENT** that's outside this `.map(...)` block (the surrounding `<ul>` / section header / etc.). Do NOT touch the resize-row block at lines 459-475 — leave it byte-for-byte identical (its dim-shape pattern is the analog, and the partition fix from plan 22.1-03 ensures rows that need a resize render now appear there with override-resolved outW/outH).

**Tailwind v4 literal-class discipline:** all className strings are literal. No template-string interpolation.

3. **Add tests in tests/renderer/optimize-dialog-passthrough-rows.spec.tsx** (NEW file, or extend the existing tests/renderer/optimize-dialog-passthrough.spec.tsx if the file naming convention prefers extension over a new file — read the existing file's name first; PATTERNS.md cites both names so confirm by `ls tests/renderer/optimize-dialog-passthrough*.spec.tsx`):

   Use the existing makeRow/makePlan synthetic builders from optimize-dialog-passthrough.spec.tsx (or copy them into the new file). Cover behaviors 1-8 above. Test name examples:
   - `it('G-05: passthrough row label does NOT contain "(already optimized)"', ...)`
   - `it('G-06 D-08: passthrough row renders source→target dim shape even when source = target', ...)`
   - `it('G-06 D-08 (override path): row with 50% override renders in plan.rows with resize dim shape', ...)`
   - `it('G-07 D-07: passthrough row with isCapped=true renders the (capped) suffix', ...)`
   - `it('G-07 D-07: passthrough row with isCapped=false does NOT render (capped)', ...)`
   - `it('CHECKER FIX preserved: undefined actualSourceW falls back to sourceW', ...)`
   - `it('Phase 6 D-109 muted style preserved: opacity-60 + text-fg-muted', ...)`
   - `it('COPY chip preserved on passthrough rows', ...)`

   Use `not.toBeNull()` / `toContain(...)` per project convention. Render the dialog (or the passthroughCopies sub-block if it's extracted) using @testing-library/react.

4. Confirm via grep that no other usage of `'(already optimized)'` exists anywhere in src/renderer (to defend against the user surface accidentally re-introducing it elsewhere).
  </action>
  <verify>
    <automated>npm test -- --run optimize-dialog-passthrough &amp;&amp; npx tsc --noEmit -p tsconfig.web.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "\\(already optimized\\)" src/renderer/src/modals/OptimizeDialog.tsx` returns 0 matches.
    - `grep -rnE "\\(already optimized\\)" src/renderer/` returns 0 matches (defends against re-introduction in any related file).
    - `grep -nE "row\\.actualSourceW \\?\\? row\\.sourceW" src/renderer/src/modals/OptimizeDialog.tsx` returns at least 1 match (CHECKER FIX preserved).
    - `grep -nE "→ \\{row\\.outW\\}" src/renderer/src/modals/OptimizeDialog.tsx | wc -l` is at least 2 (one in resize-row block, one in passthrough-row block — both render source→target).
    - `grep -nE "row\\.isCapped" src/renderer/src/modals/OptimizeDialog.tsx` returns at least 1 match (G-07 D-07 conditional render).
    - `grep -nE "\\(capped\\)" src/renderer/src/modals/OptimizeDialog.tsx` returns at least 1 match (the muted suffix string literal).
    - `grep -nE "opacity-60" src/renderer/src/modals/OptimizeDialog.tsx` returns at least 1 match in the passthrough row container (Phase 6 D-109 muted style preserved).
    - `grep -nE ">COPY<" src/renderer/src/modals/OptimizeDialog.tsx` returns at least 1 match (chip preserved verbatim).
    - tests/renderer/optimize-dialog-passthrough-rows.spec.tsx (or the extended optimize-dialog-passthrough.spec.tsx) contains test names matching ALL of: `/G-05.*already optimized|already optimized.*not/i`, `/G-06.*source.*target|→.*dim/i`, `/G-07.*isCapped|capped.*suffix/i`, `/CHECKER FIX|fallback|undefined actualSourceW/i`.
    - `npm test -- --run optimize-dialog-passthrough` exits 0.
    - `npx tsc --noEmit -p tsconfig.web.json` exits 0.
  </acceptance_criteria>
  <done>
    Passthrough row JSX in OptimizeDialog renders: source→target dim shape (D-08), no '(already optimized)' (G-05), optional muted '(capped)' suffix when row.isCapped (D-07). CHECKER FIX preserved. Phase 6 D-109 muted style preserved. COPY chip preserved. All 8 specified tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — OptimizeDialog passthrough rows visual review</name>
  <files>(human-verification only — no files modified by this task; verification covers src/renderer/src/modals/OptimizeDialog.tsx changes produced by Task 1)</files>
  <action>HUMAN-ONLY verification step. Claude does not write code in this task — Claude pauses and presents the how-to-verify checklist below to the user, then awaits the resume-signal before continuing the phase. The checklist exercises the visual layout, the reactive override-flip between passthrough and resize sections (which depends on plan 22.1-03's partition fix), and the cap-suffix appearance behaviors that the integration tests cannot fully verify (because they assert markup, not visual fidelity / parity with excludedUnused styling).</action>
  <verify><automated>(no automated verification — human checkpoint; Task 1 already enforces all greppable invariants via its own automated verify gate)</automated></verify>
  <done>User responds with "approved" via the resume-signal. If the user reports issues (e.g. "(capped) suffix visually noisy"), the phase pauses for plan revision and a follow-up plan re-renders the cap signal differently per CONTEXT D-07's revisitable clause.</done>
  <what-built>
    OptimizeDialog passthrough rows now render: source→target dim shape (mirrors resize-row pattern); no '(already optimized)' parenthetical; optional muted '(capped)' suffix when the source-ratio cap clamped the effective scale; COPY chip preserved; Phase 6 D-109 muted styling preserved.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` to start Electron in dev mode.
2. Drag-drop `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` (atlas-source, no drift after 22.1-01 unified model).
3. Click "Optimize Assets" to open the OptimizeDialog.
4. Inspect the pre-flight rows:
   - **TRIANGLE row** — should now appear in the passthrough section (G-04 fix from plan 22.1-03), NOT in the resize section. Should render `833×759 → 833×759` with the COPY chip and NO '(already optimized)' parenthetical.
   - Other rows — verify the source→target shape is consistent.
5. Now use the override dialog (or however overrides are applied in this app surface) to set a 50% override on SQUARE.
6. Re-open OptimizeDialog. Verify SQUARE has MOVED from the passthrough section to the resize section (G-07 BLOCKER fix from plan 22.1-03), and renders with the resize-row dim shape (e.g. `670×670 → 335×335`).
7. Reset the override. Re-open OptimizeDialog. Verify SQUARE is back in passthrough.
8. Now load a fixture with drift (e.g. tmpDir copy of SIMPLE_PROJECT_NO_ATLAS_MESH, or programmatically-mutated atlas with TexturePacker scale<1). Open OptimizeDialog.
9. The drifted row should appear in the passthrough section with the source→target dim shape AND the '(capped)' muted suffix (because the cap binds when no override is applied to a drifted row).
10. Apply an override to that row that drops effective scale BELOW the source-ratio cap. Re-open OptimizeDialog. The row should now be in the resize section AND should NOT have the (capped) suffix (cap is no longer binding because the user-supplied scale is smaller than what the cap would have clamped to).
11. Verify the muted styling looks visually consistent with excludedUnused rows (Phase 6 D-109 parity).
12. If any of the above behaviors fail or the (capped) suffix looks visually noisy / wrong, type the specific issue. Otherwise type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExportPlan → OptimizeDialog | plan.passthroughCopies and plan.rows are produced by buildExportPlan in src/core/export.ts (or its renderer mirror); the dialog trusts the partition |
| user override input → preflight render | overrides applied upstream of buildExportPlan; partition decision fires post-override (plan 22.1-03 lock) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.1-13 | Information Disclosure | ExportRow.outPath rendered in row label may expose deep filesystem path | accept | Same path the user already sees in the file browser; no new exposure surface |
| T-22.1-14 | Tampering | ExportRow.isCapped boolean trusted from plan 22.1-03 emit loop | accept | Single source of truth (buildExportPlan emit loop); no user-supplied path to tamper |
</threat_model>

<verification>
- `npm test -- --run optimize-dialog-passthrough` exits 0 (all 8 row tests pass).
- `npx tsc --noEmit -p tsconfig.web.json` exits 0.
- All grep gates pass.
- Human verification checkpoint (Task 2) returns "approved" — covers the visual layout + reactive override flip + cap-suffix appearance behaviors.
</verification>

<success_criteria>
- G-05 (ROADMAP success criterion #7): "(already optimized)" parenthetical removed from passthrough row label. COPY chip alone communicates byte-copy semantic. No regression to chip placement / muted styling parity with excludedUnused (Phase 6 D-109).
- G-06 (ROADMAP success criterion #6): passthrough rows render source→target dim shape (`670×670 → 670×670` when no override; `670×670 → 335×335` when 50% override applied — the latter happens automatically because plan 22.1-03's partition re-routes the row to plan.rows in that case). Resize-row shape preserved.
- G-07 D-07: cap-binding signal surfaced in OptimizeDialog row label as a small muted '(capped)' suffix when ExportRow.isCapped is true.
- CHECKER FIX 2026-05-02 (`row.actualSourceW ?? row.sourceW` fallback) preserved.
- Phase 6 D-109 muted-row UX preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/22.1-close-7-gaps-from-phase-22-human-uat-dims-02-badge-mode-awar/22.1-05-optimize-dialog-passthrough-rows-SUMMARY.md` documenting:
- Final line numbers of the modified passthroughCopies block in OptimizeDialog.tsx.
- Whether the test file was new (optimize-dialog-passthrough-rows.spec.tsx) or extended the existing optimize-dialog-passthrough.spec.tsx.
- Test count and case names.
- Cap-suffix rendering style chosen (muted '(capped)' suffix per recommendation, OR alternative if planner picked differently per the Claude's-discretion clause in CONTEXT D-07).
- Confirmation that the human verification checkpoint passed.
- Any UX feedback from the checkpoint that should feed into a follow-up plan (e.g. "(capped) suffix visually noisy → consider icon").
</output>
