---
phase: 38
plan: 03
type: execute
wave: 3
depends_on: [38-01, 38-02]
files_modified:
  - .planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md
  - .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md
autonomous: true
requirements: [POLISH-03]
must_haves:
  truths:
    - "`.planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md` no longer exists in `pending/`."
    - "`.planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md` exists in `resolved/` with the same date-prefixed filename."
    - "The resolved file appends a `## Resolved` section below the existing content (separated by `---`) referencing Phase 38 and the per-finding outcomes (1 applies, 5 no-ops, 1 skip)."
    - "The pre-existing problem-statement content is byte-identical to the pre-move state — no rewrite of historical record."
    - "Single atomic commit captures both the `git mv` and the close-out append."
  artifacts:
    - path: ".planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md"
      provides: "POLISH-03 deliverable: long-lived todo retired with audit-trail-intact close-out"
      contains: "## Resolved"
  key_links:
    - from: ".planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md"
      to: ".planning/phases/38-phase-4-code-review-polish-pass/38-POLISH-AUDIT.md"
      via: "close-out note path reference"
      pattern: "38-POLISH-AUDIT.md"
    - from: ".planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md"
      to: "Phase 38 outcome (1 applies + 5 no-ops + 1 skip)"
      via: "close-out narrative paragraph"
      pattern: "Phase 38"
---

<objective>
Move `.planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md` to `.planning/todos/resolved/` and append a `## Resolved` close-out section referencing Phase 38 + the per-finding outcomes recorded in `38-POLISH-AUDIT.md`.

Purpose: Retire the long-lived v1.0-era pending todo. The todo has been carried forward across 7 milestones (v1.0 → v1.4) waiting for a "natural polish window". Phase 38 IS that window; the audit in 38-01 confirmed most items were already swept by intervening phases, and 38-02 landed the single still-applicable fix (IN-02).

Output: One file move (`git mv`), one in-place edit (append close-out section), one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-phase-4-code-review-polish-pass/38-RESEARCH.md
@.planning/phases/38-phase-4-code-review-polish-pass/38-PATTERNS.md
@.planning/phases/38-phase-4-code-review-polish-pass/38-POLISH-AUDIT.md
@.planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md
@.planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv todo to resolved/ and append close-out section</name>
  <read_first>
    - .planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md (full file — the problem statement that stays byte-identical above the new separator)
    - .planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md (lines 75-79 — the exact close-out shape: `---` separator + `## Resolved` h2 + date-prefixed paragraph)
    - .planning/phases/38-phase-4-code-review-polish-pass/38-PATTERNS.md (lines 215-258 — close-out narrative shape with verbatim recommended body; git mv semantics for git-beginner user)
    - .planning/phases/38-phase-4-code-review-polish-pass/38-RESEARCH.md (lines 419-432 — recommended close-out body verbatim)
  </read_first>
  <action>
This task involves three operations in one atomic commit: (1) `git mv` the file, (2) Edit-append the close-out, (3) `git add` + commit. Steps below are written for a git-beginner workflow per memory `user_git_experience` + `feedback_explain_git`.

**Step 1 — move the file with `git mv`.**

```bash
git mv .planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md
```

What this does (plain English): tells git to record this as a single rename rather than a delete + create. Git tracks the content as the same file, just at a new path. The basename stays `2026-04-24-phase-4-code-review-follow-up.md` — date prefix preserved per project convention.

After this step, `git status` should show:
```
renamed:    .planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md -> .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md
```

**Step 2 — append the close-out section using the Edit tool (do NOT re-Write the whole file).**

Read the resolved file first to know its current end-of-file state. Then use the Edit tool to append the close-out below the existing content. The pre-existing content above the new section MUST stay byte-identical — no rewrite of historical record.

Append exactly this block to the END of `.planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md`:

```markdown

---

## Resolved

2026-05-13 — Phase 38 (POLISH-01..03): audit at `.planning/phases/38-phase-4-code-review-polish-pass/38-POLISH-AUDIT.md` enumerated all 7 deferred findings against current code. Net outcome:
  - **1 applies** (IN-02 drag-to-cancel overlay guard) — fixed in `src/renderer/src/modals/OverrideDialog.tsx` via `onMouseDown` + `e.target === e.currentTarget` guard; regression spec at `tests/renderer/override-dialog-drag-to-cancel.spec.tsx`.
  - **5 no-ops** — IN-01 (Phase 6 Gap-Fix R6, commit `5551073` — focus trap via shared `useFocusTrap` hook), IN-03 (Phase 27 QA-02, commit `fb3fedc` — empty-input guard), IN-05 (Phase 27 QA-03, commit `01468e4` — natural-order localeCompare), IN-06 (Phase 27 QA-04, commit `cf098e0` — dead `open` prop removed), WR-03 (Phase 27 QA-01, commit `f7668c4` — functional `setSelected` updater).
  - **1 skip** — IN-04 (`highlightMatch` duplication between `GlobalMaxRenderPanel.tsx` and `AnimationBreakdownPanel.tsx`) intentional per Phase 2/3 self-contained-panel pattern; no third consumer triggers the documented extraction threshold. Note: the original todo line 42-45 phrased the duplication as "from SearchBar.tsx" — that was a slip; the actual duplication sites are the two panels, as the 38-01 audit records.
```

Use the Edit tool with `old_string` being the last line of the existing file content (e.g. the final line `WR-02 (the only user-visible items) are already fixed.`) and `new_string` being that same line followed by the full close-out block above. This appends without rewriting.

**Step 3 — stage and commit atomically.**

```bash
git add .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md
git commit -m "$(cat <<'EOF'
chore(38): POLISH-03 close v1.0 Phase 4 code-review follow-up todo

Move .planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md
to .planning/todos/resolved/ and append a Resolved section referencing
Phase 38's audit + per-finding outcomes:

  - 1 applies (IN-02 — landed in plan 38-02 via onMouseDown + target-
    equality guard in OverrideDialog.tsx)
  - 5 no-ops (IN-01 swept by Phase 6 Gap-Fix R6; IN-03/IN-05/IN-06/WR-03
    swept by Phase 27 QA-01..04)
  - 1 skip (IN-04 highlightMatch DRY — intentional duplication per
    Phase 2/3 self-contained-panel pattern; no third consumer)

The todo has carried forward across 7 milestones since 2026-04-24.
Audit at .planning/phases/38-phase-4-code-review-polish-pass/
38-POLISH-AUDIT.md is the verification artifact for the no-op +
skip verdicts.

Closes: 2026-04-24-phase-4-code-review-follow-up
EOF
)"
```

What this does (plain English): `git add` registers the appended content as part of the commit. `git mv` already staged the rename. The commit captures BOTH the rename and the close-out content as one atomic change — anyone running `git log --follow` on the resolved file sees the full history including pre-move edits, and the close-out append is the same commit as the move.

**Plain-English note on safety:** `git mv` is non-destructive — the file content is preserved bit-for-bit during the rename. The Edit-tool append adds new content below the existing content; it does not modify or rewrite any pre-existing line. No `--force`, no `--amend`, no `git reset` involved. If anything goes wrong before the final commit, `git status` reveals the staged state and `git restore --staged <file>` un-stages without data loss.
  </action>
  <verify>
    <automated>! test -e .planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md && test -s .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md && grep -q '^## Resolved' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md && grep -q 'Phase 38 (POLISH-01..03)' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md && grep -q '38-POLISH-AUDIT.md' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md && grep -q 'IN-02' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md && grep -q '5551073\|fb3fedc\|01468e4\|cf098e0\|f7668c4' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md && git log -1 --pretty=%s | grep -q '^chore(38): POLISH-03' && git log -1 --name-status | grep -qE 'R[0-9]+\s+\.planning/todos/pending/2026-04-24-phase-4-code-review-follow-up\.md\s+\.planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up\.md'</automated>
  </verify>
  <acceptance_criteria>
    - File NOT present in `pending/`: `! test -e .planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md`
    - File present in `resolved/` and non-empty: `test -s .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md`
    - Filename preserved (date prefix intact): `ls .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md`
    - Close-out heading present: `grep -q '^## Resolved' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md`
    - Close-out references Phase 38: `grep -q 'Phase 38 (POLISH-01..03)' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md`
    - Close-out references audit doc: `grep -q '38-POLISH-AUDIT.md' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md`
    - Close-out references IN-02 fix: `grep -q 'IN-02' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md`
    - Close-out cites at least one no-op commit SHA: `grep -qE '5551073|fb3fedc|01468e4|cf098e0|f7668c4' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md`
    - All five no-op commit SHAs present: `for sha in 5551073 fb3fedc 01468e4 cf098e0 f7668c4; do grep -q "$sha" .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md || exit 1; done`
    - Pre-existing content preserved (problem statement intact): `grep -q 'Deferred findings:' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md && grep -q '## Problem' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md`
    - Commit message starts with `chore(38): POLISH-03`: `git log -1 --pretty=%s | grep -q '^chore(38): POLISH-03'`
    - Commit captures the rename (R status, with `-M` similarity threshold): `git log -1 --name-status -M | grep -qE 'R[0-9]+\s+\.planning/todos/pending/2026-04-24-phase-4-code-review-follow-up\.md\s+\.planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up\.md'`
  </acceptance_criteria>
  <done>
    The pending todo is moved to `resolved/` with the same date-prefixed filename. The close-out section is appended below the existing content (separator + `## Resolved` h2 + dated paragraph referencing Phase 38, the audit doc, IN-02 fix, all 5 no-op commit SHAs, and the IN-04 skip rationale). The pre-existing problem statement is byte-identical to its pre-move content. One atomic commit `chore(38): POLISH-03 close v1.0 Phase 4 code-review follow-up todo` captures both the rename and the append.
  </done>
</task>

</tasks>

<verification>
After the commit:

```bash
# File present in resolved/, absent from pending/
test -s .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md
! test -e .planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md

# Single commit captures rename + content append (R status indicates rename)
git log -1 --name-status -M

# All 5 no-op commit SHAs cited in close-out for traceability
for sha in 5551073 fb3fedc 01468e4 cf098e0 f7668c4; do
  grep -q "$sha" .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md
done
```

No source files modified by this plan — `git diff HEAD~1..HEAD -- src/ tests/` should be empty.

Sanity check the close-out narrative cites both the audit-doc and the fix-spec paths:
```bash
grep -q '38-POLISH-AUDIT.md' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md
grep -q 'tests/renderer/override-dialog-drag-to-cancel.spec.tsx' .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md
```
</verification>

<success_criteria>
- `.planning/todos/pending/2026-04-24-phase-4-code-review-follow-up.md` no longer exists.
- `.planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md` exists with the same filename, with the original content preserved AND a `## Resolved` section appended.
- The close-out section references: Phase 38 (POLISH-01..03), the audit doc, the IN-02 fix, all 5 no-op commit SHAs (`5551073`, `fb3fedc`, `01468e4`, `cf098e0`, `f7668c4`), and the IN-04 skip rationale citing the two panels.
- One atomic commit `chore(38): POLISH-03 close v1.0 Phase 4 code-review follow-up todo` in git log.
- No source files in `src/` or `tests/` modified by this plan.
- POLISH-03 satisfied; phase-wide success criterion 4 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/38-phase-4-code-review-polish-pass/38-03-SUMMARY.md`.
</output>
