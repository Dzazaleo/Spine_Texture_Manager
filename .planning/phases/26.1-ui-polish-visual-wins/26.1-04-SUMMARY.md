---
phase: 26.1-ui-polish-visual-wins
plan: "04"
subsystem: ui
tags: [react, tailwind, panels, danger-theme, svg-icons, layout-shift]

# Dependency graph
requires:
  - phase: 26.1-01
    provides: Color system + Tailwind token foundation (danger/10, danger/40 tokens)
  - phase: 26.1-02
    provides: Full-width panel layout
  - phase: 26.1-03
    provides: Zebra rows + toolbar height harmonization
provides:
  - Danger-themed headers (bg-danger/10 + border-danger/40 + triangle warning icon) on MissingAttachmentsPanel and UnusedAssetsPanel
  - AnimationBreakdownPanel header with bar-chart SVG icon + text-sm title + animation count chip
  - GlobalMaxRenderPanel count cell min-w-[6rem] text-right preventing layout shift
affects: [ui-polish, panel-headers, danger-zone-visibility]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Danger-themed panel headers: bg-danger/10 + border-danger/40 + 16px SVG triangle before left-accent bar"
    - "Inline SVG icon + text-sm title + count chip ml-auto pattern for panel section headers (mirrors GlobalMaxRenderPanel)"

key-files:
  created: []
  modified:
    - src/renderer/src/panels/MissingAttachmentsPanel.tsx
    - src/renderer/src/panels/UnusedAssetsPanel.tsx
    - src/renderer/src/panels/AnimationBreakdownPanel.tsx
    - src/renderer/src/panels/GlobalMaxRenderPanel.tsx

key-decisions:
  - "Left-accent bar (inline-block w-1 h-4 bg-danger) preserved after warning icon — icon precedes bar for layered visual signal"
  - "AnimationBreakdownPanel count chip uses text-xs to match text-sm count chip pattern on GlobalMaxRenderPanel"
  - "Pre-existing viewBox=0 0 20 20 in AnimationBreakdownPanel (AnimationCard toggle icon) did not conflict — new header icon is at a structurally distinct location"

patterns-established:
  - "Danger panel header pattern: border-b border-danger/40 bg-danger/10 + SVG triangle icon + left-accent bar"
  - "Section header pattern: inline SVG icon (w-5 h-5) + text-sm font-semibold text-fg + count chip (ml-auto font-mono text-xs text-fg-muted)"

requirements-completed: [UI-06, UI-07, UI-10]

# Metrics
duration: 10min
completed: 2026-05-04
---

# Phase 26.1 Plan 04: Panel Header Polish Summary

**Danger-themed headers (bg-danger/10 + border-danger/40 + triangle SVG) on problem-zone panels; bar-chart icon + animation count chip added to AnimationBreakdownPanel; GlobalMaxRenderPanel count cell min-w-[6rem] prevents layout shift**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-05-04T15:54:00Z
- **Completed:** 2026-05-04T15:58:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- MissingAttachmentsPanel and UnusedAssetsPanel now use bg-danger/10 background + border-danger/40 border, making "problem zone" panels visually distinct even when collapsed
- Both danger panels gain a 16px SVG triangle warning icon inserted before the existing left-accent bar — two-layer visual signal (icon + accent strip)
- AnimationBreakdownPanel header upgraded from plain `text-lg font-semibold` h2 to a full icon+title+count pattern matching GlobalMaxRenderPanel: bar-chart SVG (M3 17 V10 M8 17 V6 M13 17 V12 M18 17 V4), text-sm font-semibold text-fg title, and right-aligned `{filteredCards.length} animations` chip
- GlobalMaxRenderPanel count cell gains `min-w-[6rem] text-right` to prevent layout jitter when selected/total counts change digit width

## Task Commits

1. **Task 1: Danger-themed headers for MissingAttachmentsPanel + UnusedAssetsPanel** - `6dd3eed` (feat)
2. **Task 2: Stronger AnimationBreakdownPanel header + GlobalMaxRenderPanel count cell** - `60a8a46` (feat)

## Files Created/Modified

- `src/renderer/src/panels/MissingAttachmentsPanel.tsx` - header div: border-border bg-panel -> border-danger/40 bg-danger/10; SVG triangle icon added before left-accent bar
- `src/renderer/src/panels/UnusedAssetsPanel.tsx` - identical header transformation to MissingAttachmentsPanel
- `src/renderer/src/panels/AnimationBreakdownPanel.tsx` - header replaced with bar-chart SVG icon + text-sm h2 + filteredCards.length count chip; gap-4 -> gap-2
- `src/renderer/src/panels/GlobalMaxRenderPanel.tsx` - count chip span: added min-w-[6rem] text-right

## Decisions Made

- Preserved the existing left-accent bar (`inline-block w-1 h-4 bg-danger`) after the new warning icon — both elements provide distinct signals (icon = immediate recognition, accent strip = left-margin alignment marker)
- AnimationBreakdownPanel count chip uses `text-xs` (vs GlobalMaxRenderPanel's `text-sm`) to align with the smaller panel context
- No changes to `role="alert"` or `aria-label` attributes on the outer divs — accessibility annotations unchanged

## Deviations from Plan

### Minor Acceptance Criterion Clarification

The plan acceptance criterion `grep -c "viewBox=\"0 0 20 20\"" src/renderer/src/panels/AnimationBreakdownPanel.tsx` returns 1 — but the baseline already had 1 instance of this viewBox in an AnimationCard sub-component (line 514). The new header icon adds a second instance (line 406). The criterion intent was "1 new header icon exists," which is satisfied. Both instances are distinct SVG elements at different locations (header vs. card toggle). This is not a code defect.

**Total deviations:** 0 auto-fixed  
**Impact on plan:** None — plan executed exactly as written; one acceptance criterion had a baseline count assumption that was off-by-one but the implementation is correct.

## Issues Encountered

Three pre-existing test failures exist in the baseline (not introduced by this plan):
1. `tests/integration/build-scripts.spec.ts` — checks `package.json version is 1.1.3` (stale version assertion)
2. `tests/main/sampler-worker-girl.spec.ts` — wall-time gate (environment-dependent timing)
3. `tests/renderer/atlas-preview-modal.spec.tsx` — dblclick handler test (pre-existing failure)

All 3 failures confirmed pre-existing via `git stash` baseline check. This plan introduced 0 new test failures.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

All 9 component contracts (C-01 through C-09) are now complete across plans 26.1-01 through 26.1-04. Phase 26.1 UI polish visual wins fully implemented:
- C-01..C-04 from plan 26.1-01 (color scheme, tokens)
- C-05 from plan 26.1-02 (full-width panels)
- C-06 (partial), C-07 from plan 26.1-03 (zebra rows, toolbar heights)
- C-08, C-09, C-06 (count cell min-width) from this plan 26.1-04

Ready for phase verification.

---
*Phase: 26.1-ui-polish-visual-wins*
*Completed: 2026-05-04*
