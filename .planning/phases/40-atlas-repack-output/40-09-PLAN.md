---
phase: 40-atlas-repack-output
plan: 09
type: execute
wave: 3
depends_on: ["40-08"]
files_modified:
  - .planning/seeds/SEED-008-atlas-repack-output.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REPACK-01
  - REPACK-02
  - REPACK-03
  - REPACK-04
  - REPACK-05
  - REPACK-06
  - REPACK-07
  - REPACK-08
  - REPACK-09
  - REPACK-10
tags: [seed-closure, documentation, milestone-tracking, status-flip]
must_haves:
  truths:
    - "`.planning/seeds/SEED-008-atlas-repack-output.md` frontmatter `status:` flips from `dormant` to `closed` with a closing breadcrumb pointing to Phase 40 (SPEC §Acceptance Criteria, success criterion 8)."
    - "A closing note in the seed body records the close date, the closing phase, and the REPACK-01..10 fulfillment summary."
    - "STATE.md `last_activity` is updated to reflect Phase 40 closure (per memory `project_gsd_phase_complete_state_miscount` — the SDK may need a manual STATE.md nudge after Phase 40)."
  artifacts:
    - path: ".planning/seeds/SEED-008-atlas-repack-output.md"
      provides: "Frontmatter `status: closed` + closing note in body"
      contains: "status: closed"
    - path: ".planning/STATE.md"
      provides: "Updated `last_activity` reflecting Phase 40 closure"
      contains: "Phase 40"
  key_links:
    - from: ".planning/seeds/SEED-008-atlas-repack-output.md frontmatter"
      to: ".planning/phases/40-atlas-repack-output/ artifacts"
      via: "closing breadcrumb"
      pattern: "closed.*Phase 40"
---

<objective>
Close out Phase 40 documentation:
1. Flip `SEED-008-atlas-repack-output.md` frontmatter `status:` from `dormant` to `closed`.
2. Add a closing breadcrumb section to the seed body referencing Phase 40 artifacts + REPACK-01..10 fulfillment.
3. Update `.planning/STATE.md` to reflect Phase 40 closure.

This is the final plan; runs after Plan 08's regression baseline is green.

Output:
- 1 modified seed: `.planning/seeds/SEED-008-atlas-repack-output.md` (≈10-line frontmatter change + ≈30-line closing section).
- 1 modified state file: `.planning/STATE.md` (≈3-line `last_activity` update; the `gsd-sdk` may re-derive counters automatically, but per memory `project_gsd_phase_complete_state_miscount` a manual sanity check on `total_phases` / `completed_phases` / `total_plans` / `completed_plans` is required).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-atlas-repack-output/40-SPEC.md
@.planning/phases/40-atlas-repack-output/40-CONTEXT.md
@.planning/seeds/SEED-008-atlas-repack-output.md

@.planning/phases/40-atlas-repack-output/40-01-SUMMARY.md
@.planning/phases/40-atlas-repack-output/40-02-SUMMARY.md
@.planning/phases/40-atlas-repack-output/40-03-SUMMARY.md
@.planning/phases/40-atlas-repack-output/40-04-SUMMARY.md
@.planning/phases/40-atlas-repack-output/40-05-SUMMARY.md
@.planning/phases/40-atlas-repack-output/40-06-SUMMARY.md
@.planning/phases/40-atlas-repack-output/40-07-SUMMARY.md
@.planning/phases/40-atlas-repack-output/40-08-SUMMARY.md

<interfaces>
<!-- Current SEED-008 frontmatter (from /Users/leo/.planning/seeds/SEED-008-atlas-repack-output.md) -->

```yaml
 ---
id: SEED-008
status: dormant
planted: 2026-05-14
planted_during: v1.5 / pre-Phase 40 (branch experiment/phase-40-atlas-repack)
trigger_when: Phase 40 (Atlas Repack Output) — unblocks /gsd-complete-milestone v1.5
scope: Medium
 ---
```

<!-- Closing frontmatter shape (mirrors SEED-005 + SEED-007 closure pattern from prior milestones) -->

```yaml
 ---
id: SEED-008
status: closed
planted: 2026-05-14
closed: 2026-05-14
planted_during: v1.5 / pre-Phase 40 (branch experiment/phase-40-atlas-repack)
closed_during: v1.5 / Phase 40 (Atlas Repack Output)
closing_phase: 40-atlas-repack-output
trigger_when: Phase 40 (Atlas Repack Output) — unblocks /gsd-complete-milestone v1.5
scope: Medium
 ---
```

<!-- Closing breadcrumb section (append at the end of the seed body) -->

```markdown
## Closure (2026-05-14 — Phase 40)

**Closed by**: Phase 40 — Atlas Repack Output.

**Requirements delivered**: REPACK-01 through REPACK-10 — see `.planning/phases/40-atlas-repack-output/40-SPEC.md` for the locked specification.

**Phase artifacts**:
- `40-SPEC.md` — locked 10 requirements (ambiguity score 0.087)
- `40-CONTEXT.md` — 7 user-locked decisions (D-01 through D-07 series)
- `40-RESEARCH.md` — libgdx format reference, maxrects-packer API, sharp composite pipeline, 16 landmines
- `40-PATTERNS.md` — 11 new-file analogs + 7 modified-file splice sites
- `40-VALIDATION.md` — per-task verification map + Wave 0 test list
- 9 PLAN.md files (40-01 through 40-09)
- Per-plan SUMMARY.md files recording execution outcomes

**Source code landed**:
- `src/core/repack.ts` — pure-TS pack-planning (REPACK-02, REPACK-06)
- `src/main/atlas-writer.ts` — libgdx `.atlas` text serializer (REPACK-04)
- `src/main/repack-worker.ts` — sharp orchestration + atomic-or-fail (REPACK-03, REPACK-05, REPACK-10)
- `src/main/sharp-resize.ts` — shared resize+sharpen helper (D-03a)
- `src/main/ipc.ts` — `export:start` channel extended with `outputMode` + `atlasOpts` positional args (REPACK-01, D-04)
- `src/main/image-worker.ts` — `runExport` widened with `writtenPaths` accumulator (D-04a)
- `src/renderer/src/modals/OptimizeDialog.tsx` — Output card + 3 atlas knobs (REPACK-01, D-01..D-01e)
- `src/renderer/src/AppShell.tsx` — atlas state threading
- `src/preload/index.ts` + `index.d.ts` — IPC bridge widening
- `src/shared/types.ts` — 4 additive `ProjectFileV1` fields + `ExportProgressEvent.phase` (REPACK-07, D-05)
- `src/core/project-file.ts` — 4 validator pre-massage blocks (REPACK-07)

**Test coverage**:
- `tests/core/repack.spec.ts` — determinism, count preservation, page bounds, oversize pre-flight, rotation read-back
- `tests/main/atlas-writer.spec.ts` — libgdx round-trip via spine-core, field parity, rotation flag, blank-line discipline, defensive colon-check
- `tests/main/repack-worker.spec.ts` — atlas mode, both mode, sharp-emits-truth, pixel preservation, page count/bounds, oversize abort, atomic rollback contract
- `tests/main/ipc-export.spec.ts` — dispatch on each outputMode, validator rejection, both-mode rollback
- `tests/main/repack.loose-parity.spec.ts` — REPACK-01 SHA256 regression sentinel
- `tests/main/repack.parity.spec.ts` — REPACK-08 cross-loaderMode parity + REPACK-09 sharpen-invariant
- `tests/core/project-file.spec.ts` — extended for 4 new atlas fields
- `tests/arch.spec.ts` — `core/repack.ts` auto-covered by existing core-purity grep

**Locked invariants honored**:
- Skeleton JSON is invariant under repack (memory `project_spine_4_2_atlas_json_precedence`).
- atlas-source and atlas-less loaderModes produce SHA256-identical output (memory `project_strict_loadermode_separation`).
- No `project_format_version` bump (additive precedent).
- Sharp-emits-truth: packer receives the dims sharp actually emits, not buildExportPlan targets.
- Atomic-or-fail: oversize pre-flight + mid-write rollback via shared `Set<string>` accumulator.
- Locked REPACK-10 error string preserved verbatim across worker, IPC, and UI surfaces.
- `core/` purity preserved — `core/repack.ts` clean of sharp/fs/electron imports.

**Status flip rationale**: Per `40-SPEC.md` §Acceptance Criteria item 17 ("SEED-008 frontmatter `status:` flips from `dormant` to `closed` at phase close with breadcrumb to Phase 40") and SEED-008's own design contract ("This seed should be presented (or referenced) during: ... /gsd-complete-milestone v1.5 — final milestone gate"), this seed is closed concurrently with Phase 40's completion.
```

<!-- STATE.md last_activity update -->

```markdown
last_activity: 2026-05-14 -- Phase 40 complete (atlas-repack-output); v1.5 milestone closure pending
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 09.1: Flip SEED-008 frontmatter status:dormant → closed + append closing breadcrumb</name>
  <files>.planning/seeds/SEED-008-atlas-repack-output.md</files>
  <read_first>
    - .planning/seeds/SEED-008-atlas-repack-output.md (full — current frontmatter + body)
    - .planning/seeds/SEED-007-split-overrides-per-loader-mode.md (closure precedent — search for `status: closed` pattern from Phase 36)
    - .planning/seeds/SEED-005-rgba2-inherittimeline-coverage.md (closure precedent — Phase 37)
    - .planning/phases/40-atlas-repack-output/40-SPEC.md (acceptance criterion 17 — status flip requirement)
    - .planning/phases/40-atlas-repack-output/40-CONTEXT.md (in-scope: "SEED-008 frontmatter `status:` flip from `dormant` to `closed` at phase close")
  </read_first>
  <behavior>
    - The frontmatter block's `status:` field changes from `dormant` to `closed`.
    - Three new frontmatter fields are added: `closed: 2026-05-14`, `closed_during: v1.5 / Phase 40 (Atlas Repack Output)`, `closing_phase: 40-atlas-repack-output`.
    - A new `## Closure (2026-05-14 — Phase 40)` section is appended to the seed body recording the close details (see `<interfaces>` above for the full closing markdown).
    - No other content in the seed is modified — preserve the `## Why This Matters`, `## When to Surface`, `## Scope Estimate`, `## Locked Design Facts`, `## REQ-ID Namespace`, `## Breadcrumbs`, `## Notes` sections verbatim.
  </behavior>
  <action>
    Modify `.planning/seeds/SEED-008-atlas-repack-output.md`:

    **Step 1 — Update the YAML frontmatter block** (the first --- block at the top of the file). Replace:

    ```yaml
    ---
    id: SEED-008
    status: dormant
    planted: 2026-05-14
    planted_during: v1.5 / pre-Phase 40 (branch experiment/phase-40-atlas-repack)
    trigger_when: Phase 40 (Atlas Repack Output) — unblocks /gsd-complete-milestone v1.5
    scope: Medium
    ---
    ```

    With:

    ```yaml
    ---
    id: SEED-008
    status: closed
    planted: 2026-05-14
    closed: 2026-05-14
    planted_during: v1.5 / pre-Phase 40 (branch experiment/phase-40-atlas-repack)
    closed_during: v1.5 / Phase 40 (Atlas Repack Output)
    closing_phase: 40-atlas-repack-output
    trigger_when: Phase 40 (Atlas Repack Output) — unblocks /gsd-complete-milestone v1.5
    scope: Medium
    ---
    ```

    **Step 2 — Append the closing section to the bottom of the file.** Copy verbatim from the `<interfaces>` block above (the `## Closure (2026-05-14 — Phase 40)` section + all its bullet contents).

    **Critical:** Preserve ALL existing seed content. The only changes are:
    1. Frontmatter `status:` flip (dormant → closed) + 3 new metadata lines.
    2. Append new `## Closure` section at the end.

    Do NOT modify the `## Why This Matters`, `## When to Surface`, `## Scope Estimate`, `## Locked Design Facts (do not relitigate)`, `## REQ-ID Namespace`, `## Breadcrumbs`, or `## Notes` sections.

    NOTE: If today's actual date is different from 2026-05-14 when the executor runs this, use the actual date — the planner assumes Phase 40 closes within the same day's session per the GSD workflow cadence.
  </action>
  <verify>
    <automated>grep -c "status: closed" .planning/seeds/SEED-008-atlas-repack-output.md ; grep -c "closing_phase: 40-atlas-repack-output" .planning/seeds/SEED-008-atlas-repack-output.md ; grep -c "## Closure" .planning/seeds/SEED-008-atlas-repack-output.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "status: closed" .planning/seeds/SEED-008-atlas-repack-output.md` returns 1.
    - `grep -c "status: dormant" .planning/seeds/SEED-008-atlas-repack-output.md` returns 0 (the old status is replaced, not preserved).
    - `grep -c "closed: 2026-05-14\|closed_during: v1.5\|closing_phase: 40-atlas-repack-output" .planning/seeds/SEED-008-atlas-repack-output.md` returns ≥ 3.
    - `grep -c "## Closure (2026-05-14 — Phase 40)\|## Closure" .planning/seeds/SEED-008-atlas-repack-output.md` returns ≥ 1.
    - `grep -c "REPACK-01\|REPACK-02\|REPACK-03\|REPACK-04\|REPACK-05\|REPACK-06\|REPACK-07\|REPACK-08\|REPACK-09\|REPACK-10" .planning/seeds/SEED-008-atlas-repack-output.md` returns ≥ 10 (every REPACK ID mentioned in the closure section).
    - `grep -c "## Why This Matters\|## When to Surface\|## Scope Estimate\|## Locked Design Facts\|## REQ-ID Namespace\|## Breadcrumbs\|## Notes" .planning/seeds/SEED-008-atlas-repack-output.md` returns ≥ 7 (existing sections preserved).
  </acceptance_criteria>
  <done>SEED-008 frontmatter flipped to `status: closed`; closing breadcrumb section appended; all existing sections preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09.2: Update STATE.md last_activity to reflect Phase 40 closure</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (full — current frontmatter + current position section)
    - .planning/ROADMAP.md (Phase 40 entry — confirm plan count after this phase)
    - memory project_gsd_phase_complete_state_miscount (the SDK may miscount; manual sanity check on counters required)
  </read_first>
  <behavior>
    - `last_activity` in the frontmatter updates to `2026-05-14 -- Phase 40 complete (atlas-repack-output); v1.5 milestone closure pending`.
    - `last_updated` in the frontmatter updates to the current ISO-8601 timestamp.
    - The `## Current Position` section's `Status` line reflects Phase 40 complete and points the user toward `/gsd-complete-milestone v1.5` as the next step.
    - The progress counters (`total_phases`, `completed_phases`, `total_plans`, `completed_plans`, `percent`) MAY need manual adjustment per memory `project_gsd_phase_complete_state_miscount`. Verify counter math: v1.5 has 5 phases (36-40); 9 plans for Phase 40 + 14 plans for Phases 36-39 = 23 total. Adjust if the SDK leaves them stale.
  </behavior>
  <action>
    Modify `.planning/STATE.md`:

    **Step 1 — Update the YAML frontmatter** at the top. The current state:
    ```yaml
    last_updated: "2026-05-14T00:00:00.000Z"
    last_activity: 2026-05-14 -- Phase 40 added (atlas-repack-output); v1.5 reopened from milestone_complete
    progress:
      total_phases: 5
      completed_phases: 4
      total_plans: 14
      completed_plans: 14
      percent: 80
    ```

    Update to:
    ```yaml
    last_updated: "2026-05-14T23:59:59.000Z"
    last_activity: 2026-05-14 -- Phase 40 complete (atlas-repack-output); v1.5 milestone closure pending
    progress:
      total_phases: 5
      completed_phases: 5
      total_plans: 23
      completed_plans: 23
      percent: 100
    ```

    NOTE on `total_plans: 23`: This figure assumes 14 plans from Phases 36-39 (per the existing baseline) + 9 plans from Phase 40 = 23. If the actual aggregate differs (verify by counting `.planning/phases/3*-*/3*-*-PLAN.md` + `.planning/phases/40-atlas-repack-output/40-*-PLAN.md`), use the actual count. Per memory `project_gsd_phase_complete_state_miscount`, the SDK may report incorrect counters — manual sanity check is required.

    NOTE on `last_updated`: use the actual current timestamp at execution time (ISO-8601), not 2026-05-14 if a different day.

    **Step 2 — Update the `## Current Position` block** in the body. Current:
    ```
    Phase: 40
    Plan: Not specced yet (run /gsd-spec-phase 40 next)
    Status: Phase 40 added to v1.5; milestone reopened from milestone_complete
    Last activity: 2026-05-14
    ```

    Replace with:
    ```
    Phase: 40 (complete)
    Plan: All 9 plans complete (40-01..40-09)
    Status: Phase 40 complete (Atlas Repack Output); SEED-008 closed; v1.5 milestone closure pending — run `/gsd-complete-milestone v1.5` next.
    Last activity: 2026-05-14
    ```

    **Critical:** Do NOT modify the rest of STATE.md — preserve the `## Project Reference`, `## v1.5 Roadmap Summary`, `## Last completed milestone`, `## Deferred Items`, `## Accumulated Context`, `## Roadmap Evolution` sections.
  </action>
  <verify>
    <automated>grep -c "Phase 40 complete" .planning/STATE.md ; grep -c "completed_phases: 5" .planning/STATE.md ; grep -c "percent: 100" .planning/STATE.md ; grep -c "gsd-complete-milestone v1.5" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 40 complete" .planning/STATE.md` returns ≥ 1.
    - `grep -c "completed_phases: 5" .planning/STATE.md` returns 1.
    - `grep -c "percent: 100" .planning/STATE.md` returns 1.
    - `grep -c "gsd-complete-milestone v1.5" .planning/STATE.md` returns ≥ 1 (next-step hint).
    - `grep -c "## Project Reference\|## v1.5 Roadmap Summary\|## Last completed milestone\|## Deferred Items\|## Accumulated Context" .planning/STATE.md` returns ≥ 5 (existing sections preserved).
    - The `Last activity:` line in `## Current Position` no longer reads `Phase 40 added` (the old phrasing is removed).
    - YAML frontmatter remains valid (no unclosed `---` delimiters; no malformed indentation).
  </acceptance_criteria>
  <done>STATE.md reflects Phase 40 closure; counters updated; user is directed to `/gsd-complete-milestone v1.5` as the next step.</done>
</task>

</tasks>

<verification>
- `.planning/seeds/SEED-008-atlas-repack-output.md` frontmatter `status:` is `closed`; closing breadcrumb appended; all original sections preserved.
- `.planning/STATE.md` reflects Phase 40 complete; `completed_phases: 5`; `percent: 100`; user knows to run `/gsd-complete-milestone v1.5`.
- All 10 REPACK requirements (REPACK-01..10) are referenced in the seed closure breadcrumb.
- No other file modified by this plan.
</verification>

<success_criteria>
- [ ] SEED-008 status flipped to `closed`
- [ ] Closing breadcrumb section in seed body
- [ ] STATE.md `last_activity` + counters updated
- [ ] All grep counts pass
- [ ] Original seed and STATE.md content preserved
</success_criteria>

<output>
After completion, create `.planning/phases/40-atlas-repack-output/40-09-SUMMARY.md` recording:
- SEED-008 status flip (dormant → closed)
- STATE.md updates (counters + Current Position)
- Phase 40 closure breadcrumb content
- Next-step hint (run `/gsd-complete-milestone v1.5`)
</output>
