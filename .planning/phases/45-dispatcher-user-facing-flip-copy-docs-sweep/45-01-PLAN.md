---
phase: 45-dispatcher-user-facing-flip-copy-docs-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/App.tsx
  - src/renderer/src/modals/HelpDialog.tsx
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "For a 4.3 file the drop-zone copy presents 4.3 as supported — it no longer instructs 're-export as Version 4.2' (UX-01 / D-01/D-02/D-03)"
    - "Every stale 'Spine 4.2+' / 'version 4.2 only' / '4.2 or later' surface in renderer + docs is swept to the affirmative 'Spine 4.2 and 4.3' band (UX-02 / D-04/D-05)"
    - "git grep for stale '4.2-only / re-export-as-4.2 / 4.2+ / 4.2 or later' over renderer + docs (D-08 allowlist applied) returns no stale promise (UX-02 / D-08)"
    - "A repo-root CHANGELOG.md exists with a v1.6 entry naming 'Spine 4.3 skeleton support (dual-runtime)' (UX-02 / D-06)"
    - "The Documentation Builder HTML (src/main/doc-export.ts renderHero/renderChipStrip) is proven version-agnostic with grep evidence recorded — explicit confirm-and-no-op, not a silent skip (UX-02 / D-09)"
    - "src/core/errors.ts, src/core/loader.ts and the <4.2 / >=4.4 guard are NOT modified — the Phase-44-D-10 correct-by-construction strings stay byte-identical (D-08 scope-leak guard)"
  artifacts:
    - path: "src/renderer/src/App.tsx"
      provides: "Drop-zone else-branch copy: 'Drop a Spine v4.2 or v4.3 JSON file anywhere in this window' with both version tokens in font-bold text-danger, no .spine token"
      contains: 'or <span className="font-bold text-danger">v4.3</span> JSON file anywhere in this window'
    - path: "src/renderer/src/modals/HelpDialog.tsx"
      provides: "Section-1 product sentence stating 'Spine 4.2 and 4.3' (was 'Spine 4.2+')"
      contains: "Spine 4.2 and 4.3"
    - path: "README.md"
      provides: "Lines 3/15 affirmative 'Spine 4.2 and 4.3'; line 29 D-05 exact reject band (both <4.2 and >=4.4 hard-rejected with a typed error)"
      contains: "Spine 4.2 and 4.3"
    - path: "CHANGELOG.md"
      provides: "Net-new repo-root release-history file with a v1.6 'Spine 4.3 skeleton support (dual-runtime)' entry"
      contains: "Spine 4.3 skeleton support (dual-runtime)"
  key_links:
    - from: "src/renderer/src/App.tsx drop-zone else-branch"
      to: "user-visible 4.3-supported promise"
      via: "inline-TSX string edit (no i18n layer)"
      pattern: 'Drop a Spine .*v4.2.* or .*v4.3.* JSON file anywhere in this window'
    - from: "README.md:29 reject band"
      to: "src/core/errors.ts:130-141 (reference-only) typed-error truth"
      via: "doc wording mirrors the errors.ts band: supported = 4.2 and 4.3; <4.2 and >=4.4 hard-rejected"
      pattern: "4\\.2 and 4\\.3"
---

<objective>
Sweep every user-facing 4.2-only surface in the renderer + docs into first-class
"Spine 4.2 and 4.3" support, now that the 4.3 dispatch path works and the
cross-runtime oracle proves it (Phase 44, complete). Delivers the COPY half of
UX-01 + UX-02:

- UX-01: the App.tsx drop-zone copy presents 4.3 as supported (D-01/D-02/D-03).
- UX-02 (copy portion): HelpDialog + README swept to the affirmative band
  (D-04/D-05); a net-new repo-root CHANGELOG.md announces 4.3 (D-06); the
  exhaustive renderer/docs grep is clean under the D-08 allowlist; the
  Documentation Builder HTML no-op is explicitly dispositioned with grep
  evidence (D-09).

Purpose: the app's user-visible promise must match its proven capability — a
4.3 file is now supported, so no surface may still say "Spine v4.2 only" or
"re-export as Version 4.2".

Output: 3 modified files (App.tsx, HelpDialog.tsx, README.md) + 1 net-new file
(CHANGELOG.md) + the D-09 confirm-and-no-op disposition recorded in this plan's
SUMMARY.

ZERO runtime/loader/dispatch behavior changes. src/core/loader.ts,
src/core/runtime/*, src/core/errors.ts, dispatch — NOT touched (scope-leak
guard; the dispatch flip was Phase 44's job and is done).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-dispatcher-user-facing-flip-copy-docs-sweep/45-CONTEXT.md
@.planning/phases/45-dispatcher-user-facing-flip-copy-docs-sweep/45-PATTERNS.md

<interfaces>
Reference-only anchors. The executor uses these to SOURCE wording and the
D-08 allowlist. It MUST NOT modify these files (D-08 scope-leak guard).

src/core/errors.ts:130-141 — the Phase-44-D-10 correct-by-construction
SpineVersionUnsupportedError strings (D-08 "legit, NOT stale" anchor; the
README:29 D-05 band wording is SOURCED from here — DO NOT EDIT errors.ts):

  const message =
    kind === 'ge44'
      ? `This file is from Spine ${detectedVersion}. This app supports Spine 4.2 and 4.3. Re-export as Version 4.3 (or 4.2) and try again.`
      : kind === 'contradiction'
        ? `This file is stamped Spine 4.2 but contains a Spine 4.3 constraints[] schema. Re-export it cleanly as Version 4.2 or Version 4.3 and try again.`
        : `This file was exported from Spine ${detectedVersion}. ` +
          `Spine Texture Manager requires Spine 4.2 or later. ` +
          `Re-export from Spine 4.2 or later in the editor.`;

The <4.2 reject arm `(major === 4 && minor >= 4) || major >= 5` (errors.ts
~117-128) is ALSO legit, NOT stale — never sweep it.

src/main/doc-export.ts:262-267 — `renderHero`, version-agnostic by design
(D-09 grep-clean evidence; reference-only — DO NOT EDIT):

  function renderHero(skeletonName: string): string {
    return `<div class="hero">
      ${GLYPH.doc}
      <div class="hero-title">Spine Documentation / <span class="hero-name">${escapeHtml(skeletonName)}</span></div>
    </div>`;
  }

`renderChipStrip` (doc-export.ts ~280+) emits date/counts only — no version
string.

.planning/MILESTONES.md — the structural analog for the net-new CHANGELOG.md
(per-version `## vX.Y <title> (Shipped: YYYY-MM-DD)` headings, newest-on-top,
`---` separators, bulleted accomplishments).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the App.tsx drop-zone copy (UX-01 — D-01/D-02/D-03)</name>
  <files>src/renderer/src/App.tsx</files>
  <read_first>
    - src/renderer/src/App.tsx:650-680 — the FULL `state.status === 'idle'`
      `isElevated` ternary. Confirm the EXACT current `else`-branch line and
      that the `isElevated === true` arm (the `<div role="status">` admin
      advisory, lines ~657-673) is a SEPARATE LOCKED UI-SPEC block. Do NOT
      span the conditional; do NOT touch the admin arm.
    - 45-PATTERNS.md section "src/renderer/src/App.tsx (renderer-copy,
      drop-zone else branch) — D-01/D-02/D-03" (exact current-state excerpt +
      exact target diff).
    - 45-CONTEXT.md Decisions D-01/D-02/D-03 (D-02: red on BOTH tokens is the
      LOCKED user-overridden choice — do NOT relitigate to neutral; D-03:
      drop the .spine token).
  </read_first>
  <action>
    In src/renderer/src/App.tsx, replace ONLY the drop-zone `else`-branch
    text line. Current line (verbatim):

        Drop a Spine <span className="font-bold text-danger">v4.2</span> <code>.spine</code> JSON file anywhere in this window

    New line (verbatim — D-01 ROADMAP wording + D-02 dual red spans + D-03
    no .spine token):

        Drop a Spine <span className="font-bold text-danger">v4.2</span> or <span className="font-bold text-danger">v4.3</span> JSON file anywhere in this window

    Constraints (LOCKED):
    - Keep the wrapping `<p className="text-fg-muted font-mono text-sm">`
      element UNCHANGED — only its inner text content changes.
    - Keep `font-bold text-danger` on BOTH version `<span>`s — D-02 is a
      user-overridden aesthetic choice (the user explicitly rejected the
      neutral-styling recommendation). Do NOT change to neutral, remove
      emphasis, or move the red.
    - The word `or` and the remaining text stay in the existing muted
      `text-fg-muted font-mono text-sm` paragraph style (no span around `or`).
    - Drop the `<code>.spine</code>` token entirely (D-03 — the app loads
      exported `.json`/`.stmproj`; `.spine` is the editor project file per
      CLAUDE.md folder conventions).
    - Single-line edit. Do NOT touch the sibling `isElevated === true`
      `<div role="status">` admin-DnD advisory (lines ~657-673) — it has its
      own LOCKED Phase-31 UI-SPEC copy and is out of scope.
  </action>
  <verify>
    <automated>grep -F 'or <span className="font-bold text-danger">v4.3</span> JSON file anywhere in this window' src/renderer/src/App.tsx && ! grep -nE 'Drop a Spine .*<code>\.spine</code>' src/renderer/src/App.tsx && grep -F 'Drag-and-drop is unavailable while running as administrator' src/renderer/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'or <span className="font-bold text-danger">v4.3</span> JSON file anywhere in this window' src/renderer/src/App.tsx` matches (the new D-01/D-02 line landed with red on both tokens).
    - `grep -nE 'Drop a Spine .*<code>\.spine</code>' src/renderer/src/App.tsx` returns NOTHING (D-03 — the .spine token was removed from the drop-zone line).
    - `grep -F 'Drag-and-drop is unavailable while running as administrator' src/renderer/src/App.tsx` STILL matches verbatim (the LOCKED admin advisory was NOT touched).
    - `grep -n 'text-fg-muted font-mono text-sm' src/renderer/src/App.tsx` still shows the wrapping `<p>` immediately above the edited drop-zone text.
    - `git diff --stat src/renderer/src/App.tsx` shows a minimal change (the drop-zone text line only; surrounding JSX unchanged).
  </acceptance_criteria>
  <done>The drop-zone else-branch reads "Drop a Spine v4.2 or v4.3 JSON file anywhere in this window" with both version tokens in font-bold text-danger and no .spine token; the admin advisory arm is byte-untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep HelpDialog + README to the affirmative band; verify the D-08-allowlisted grep is clean (UX-02 — D-04/D-05/D-08)</name>
  <files>src/renderer/src/modals/HelpDialog.tsx, README.md</files>
  <read_first>
    - src/renderer/src/modals/HelpDialog.tsx:118-160 — the section-1 `<p>`
      (line ~128, "reads Spine 4.2+ skeleton JSON") + the adjacent
      install-instructions `<section>` analog (lines ~135-151).
    - README.md:1-35 — lines 3 (product sentence; mirrors CLAUDE.md:5 +
      HelpDialog:128), 5 (the "Latest release" GitHub pointer — adjacent
      CHANGELOG context; NOTE the stale `v1.3.6` literal is OUT of D-04/D-05
      scope, do not change it here), 15 ("Reads a Spine 4.2+ …"), 29
      (lower-bound-only reject framing).
    - src/core/errors.ts:130-141 — reference-ONLY: SOURCE the README:29 D-05
      band wording from here. DO NOT EDIT errors.ts.
    - 45-PATTERNS.md section "README.md (docs prose) — D-04 / D-05 / D-06
      context" + section "D-08 allowlist anchor".
    - 42-CONTEXT.md D-09 (the <4.2 + >=4.4 reject band the README:29 wording
      must match: reject <4.2 (Phase-12 F3) AND a NEW >=4.4 arm —
      `major=4 AND minor>=4` OR `major>=5`).
  </read_first>
  <action>
    Three copy edits, then the exhaustive grep gate.

    (a) src/renderer/src/modals/HelpDialog.tsx section-1 `<p>` (line ~128):
    replace the phrase "Spine 4.2+ skeleton JSON" with "Spine 4.2 and 4.3
    skeleton JSON". Keep the rest of the `<p>` body and ALL wrapper
    classes/structure unchanged. Resulting sentence:
    "Spine Texture Manager reads Spine 4.2 and 4.3 skeleton JSON and computes
    the peak world-space render scale for every attachment, across every
    animation and skin. Use it to right-size textures per-asset before atlas
    export."

    (b) README.md line 3: replace "reads Spine 4.2+ skeleton JSON" with
    "reads Spine 4.2 and 4.3 skeleton JSON". Keep the rest of line 3 and its
    markdown structure identical (this sentence must stay phrase-consistent
    with HelpDialog:128 + CLAUDE.md:5 — the same product sentence in 3 places).

    (c) README.md line 15: replace
    "- Reads a Spine 4.2+ skeleton JSON in either supported delivery shape:"
    with
    "- Reads a Spine 4.2 and 4.3 skeleton JSON in either supported delivery shape:"

    (d) README.md line 29: replace the current lower-bound-only line
    "- Spine editor 4.2 or later for the input skeleton JSON. (3.x and earlier are hard-rejected at load time with a typed error.)"
    with the D-05 EXACT band (sourced from errors.ts:130-141 + 42-CONTEXT.md
    D-09 — supported = 4.2 and 4.3; BOTH the lower <4.2 AND the new upper
    >=4.4 arms hard-reject with a typed error). Write EXACTLY:
    "- Spine editor 4.2 or 4.3 for the input skeleton JSON. Spine 4.1 and earlier, and Spine 4.4 and later, are hard-rejected at load time with a typed error."
    ("4.2 or later" was technically wrong — it implied 4.4+ works.)

    (e) Run the exhaustive UX-02 grep over renderer + docs (the grep below
    excludes src/core by path, per D-08 scope). After edits (a)-(d) it MUST
    return NOTHING:
    `git grep -inE "spine 4\.2\+|4\.2 or later|spine v?4\.2 only|re-export as (version )?4\.2" -- src/renderer README.md INSTALL.md`
    If it returns a line that is a GENUINE stale promise NOT in the
    D-08-legit set below, sweep that line too (D-04 explicitly says do NOT
    assume the 3/15/29 list is complete) and record it in the SUMMARY.

    D-08-LEGIT set — these are allowed to remain and MUST NOT be swept
    (sweeping them is a defect, not progress):
    - unrelated code comments containing "re-export"/"re-exported" (e.g.
      AppShell.tsx "the user picks up changes re-exported from Spine",
      DocumentationBuilderDialog.tsx "re-exported through shared/types.ts")
      — code-comment prose about React/type re-export, NOT a version promise;
      the grep regex above is narrow enough that it will NOT match these
      ("re-export as 4.2" specifically, not bare "re-export").
    - core/ code comments in errors.ts/loader.ts (out of grep scope by
      design — the grep path-excludes src/core).
    - the < 4.2 guard, runtime-42, fixture/token literals (skeleton2_42,
      4.2-from-4.3.01).
    - CHANGELOG/MILESTONES release-history entries naming "4.2".
    - the Phase-44-D-10 correct errors.ts strings.

    Optional consistency-only (NOT a UX-02 gate item): CLAUDE.md:5 carries
    the same "reads Spine 4.2+ skeleton JSON" product sentence. CLAUDE.md is
    project-instructions, out of the renderer/docs UX-02 grep scope. Leave
    CLAUDE.md UNTOUCHED in this task to keep the change set minimal and
    avoid risk to project-instruction prose; note in the SUMMARY that the
    3rd mirrored copy (CLAUDE.md:5) is intentionally deferred as out-of-grep
    -scope consistency-only (not a gate item, not stale per D-08).
  </action>
  <verify>
    <automated>grep -F 'reads Spine 4.2 and 4.3 skeleton JSON' src/renderer/src/modals/HelpDialog.tsx && grep -F 'reads Spine 4.2 and 4.3 skeleton JSON' README.md && grep -F 'Reads a Spine 4.2 and 4.3 skeleton JSON in either supported delivery shape:' README.md && grep -F 'Spine editor 4.2 or 4.3 for the input skeleton JSON. Spine 4.1 and earlier, and Spine 4.4 and later, are hard-rejected at load time with a typed error.' README.md && test -z "$(git grep -inE 'spine 4\.2\+|4\.2 or later|spine v?4\.2 only|re-export as (version )?4\.2' -- src/renderer README.md INSTALL.md)" && git diff --quiet src/core/errors.ts src/core/loader.ts && echo SCOPE_GUARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -F 'reads Spine 4.2 and 4.3 skeleton JSON' src/renderer/src/modals/HelpDialog.tsx matches (D-04 HelpDialog swept).
    - grep -F 'reads Spine 4.2 and 4.3 skeleton JSON' README.md matches (D-04 README:3 swept, phrase-consistent with HelpDialog:128).
    - grep -F 'Reads a Spine 4.2 and 4.3 skeleton JSON in either supported delivery shape:' README.md matches (D-04 README:15 swept).
    - grep -F 'Spine editor 4.2 or 4.3 for the input skeleton JSON. Spine 4.1 and earlier, and Spine 4.4 and later, are hard-rejected at load time with a typed error.' README.md matches (D-05 README:29 exact band landed, both reject arms stated).
    - The UX-02 grep is EMPTY: running `git grep -inE 'spine 4\.2\+|4\.2 or later|spine v?4\.2 only|re-export as (version )?4\.2' -- src/renderer README.md INSTALL.md` returns no lines.
    - SCOPE-LEAK GUARD: `git diff --quiet src/core/errors.ts src/core/loader.ts` exits 0 (errors.ts + loader.ts byte-untouched — the Phase-44-D-10 correct strings + the <4.2/>=4.4 guard were NOT swept).
    - No new external attack surface: `git diff --stat` shows only src/renderer/src/modals/HelpDialog.tsx + README.md changed by this task (plus App.tsx from Task 1, CHANGELOG.md from Task 3).
  </acceptance_criteria>
  <done>HelpDialog:128 + README:3/15 read "Spine 4.2 and 4.3"; README:29 states the exact D-05 band (both <4.2 and >=4.4 hard-rejected with a typed error); the renderer/docs UX-02 grep is clean under the D-08 allowlist; errors.ts/loader.ts are byte-untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Create the net-new repo-root CHANGELOG.md + record the D-09 doc-export confirm-and-no-op disposition (UX-02 — D-06/D-09)</name>
  <files>CHANGELOG.md (net-new); reference-only (DO NOT EDIT): src/main/doc-export.ts</files>
  <read_first>
    - .planning/MILESTONES.md (whole file, or at least the top ~40 lines) —
      the only existing structured release-history markdown; the structural
      analog for CHANGELOG.md (per-version `## vX.Y <title> (Shipped:
      YYYY-MM-DD)` headings, newest-on-top, `---` separators, bulleted
      accomplishments).
    - README.md:5 — the current "Latest release" GitHub-Releases pointer;
      CHANGELOG.md is its new in-repo counterpart (D-06). NOTE: D-07 keeps
      external GitHub surfaces deferred — do NOT touch GitHub or the
      README:5 link from here.
    - src/main/doc-export.ts:258-290 — renderHero (line ~262, version-
      agnostic `Spine Documentation / {skeletonName}`) + renderChipStrip
      (line ~280, date/counts only, no version string). This is the D-09
      grep-clean evidence to RECORD (reference-only — DO NOT EDIT
      doc-export.ts).
    - 45-CONTEXT.md Decisions D-06 (CHANGELOG mandatory tokens) + D-09
      (confirm-and-no-op disposition must be recorded WITH grep evidence —
      a silently-skipped surface reads as unswept).
  </read_first>
  <action>
    (a) Create `CHANGELOG.md` at the REPO ROOT (confirmed absent —
    `ls CHANGELOG.md` returns no such file). Format = Claude's discretion
    (D-06) — use the MILESTONES.md idiom (newest-on-top `## vX.Y` headings
    with `---` separators) OR keep-a-changelog; either is acceptable. HARD
    requirements:
    - A top entry for `v1.6` whose title/body explicitly contains the exact
      phrase `Spine 4.3 skeleton support (dual-runtime)`.
    - The entry summarizes the v1.6 user-visible change: the app now loads
      and correctly samples Spine 4.3 skeleton JSON (routed by detected
      version) alongside the existing Spine 4.2 support; 4.2 behavior is
      byte-frozen and 4.3 correctness is cross-runtime oracle-proven.
    - A short header note that this is the in-repo release history; future
      milestones append newest-on-top (the README:5 GitHub-Releases pointer
      remains the external counterpart — do NOT modify it here, D-07).

    Concrete CHANGELOG.md skeleton to write (fill the v1.6 bullets from this
    milestone's user-facing reality; keep the mandatory phrase verbatim):

      # Changelog

      In-repo release history for Spine Texture Manager. Newest on top.
      Per-release downloads + first-launch notes are on
      [GitHub Releases](https://github.com/Dzazaleo/Spine_Texture_Manager/releases).

      ## v1.6 — Spine 4.3 Runtime Port (Dual-Runtime)

      - **Spine 4.3 skeleton support (dual-runtime).** The app now loads and
        correctly samples Spine 4.3 skeleton JSON in addition to Spine 4.2,
        routed by the detected skeleton version. A 4.3 file is no longer
        rejected with "re-export as Version 4.2" — it is first-class
        supported.
      - Spine 4.2 behavior is byte-frozen (regression-gated); 4.3 sampling
        correctness is independently proven by a same-rig cross-runtime
        equivalence oracle.
      - Unsupported versions still fail loudly with a typed error: Spine 4.1
        and earlier, and Spine 4.4 and later, are hard-rejected at load time.

    (b) Record the D-09 confirm-and-no-op disposition in this plan's SUMMARY
    (NOT a code edit). The disposition text MUST state, with the grep
    evidence, that the Documentation Builder exported HTML carries NO stale
    "4.2"/"re-export"/version promise and is therefore an intentional NO-OP
    (no edit, no `detectedVersion` threading — deferred per D-09/Deferred):
    - Evidence command: `grep -niE "4\.2|re-export|version 4|spine v?4" src/main/doc-export.ts`
      — the only matches are unrelated identifiers/comments, NOT a rendered
      user-facing version string. Specifically `renderHero` (doc-export.ts
      ~262) renders `Spine Documentation / {skeletonName}` (version-agnostic)
      and `renderChipStrip` (~280) emits date/counts only.
    - Conclusion to record verbatim in SUMMARY: "D-09: Documentation Builder
      HTML (src/main/doc-export.ts) is version-agnostic by design (renderHero
      / renderChipStrip carry no version string). The ROADMAP SC#2
      'Documentation Builder HTML template' surface was listed speculatively;
      it carries no stale promise. Disposition = explicit confirm-and-no-op:
      NO edit, NO detectedVersion threading (deferred). Grep evidence:
      [paste the grep output]."
    Do NOT edit src/main/doc-export.ts.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -F 'Spine 4.3 skeleton support (dual-runtime)' CHANGELOG.md && grep -F 'v1.6' CHANGELOG.md && git diff --quiet src/main/doc-export.ts && echo CHANGELOG_OK_DOCEXPORT_UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - `test -f CHANGELOG.md` succeeds (the net-new repo-root file exists).
    - `grep -F 'Spine 4.3 skeleton support (dual-runtime)' CHANGELOG.md` matches the mandatory D-06 phrase verbatim.
    - `grep -F 'v1.6' CHANGELOG.md` matches (the v1.6 entry exists).
    - `git status --porcelain CHANGELOG.md` shows it as a new untracked/added file (`??` or `A`).
    - SCOPE-LEAK GUARD: `git diff --quiet src/main/doc-export.ts` exits 0 (doc-export.ts byte-untouched — D-09 is a no-op, not an edit).
    - The plan SUMMARY contains the D-09 confirm-and-no-op disposition block WITH the pasted `grep -niE ... src/main/doc-export.ts` evidence (the no-op is proven, not silently skipped).
  </acceptance_criteria>
  <done>CHANGELOG.md exists at repo root with a v1.6 entry naming "Spine 4.3 skeleton support (dual-runtime)"; doc-export.ts is byte-untouched and its version-agnostic no-op is dispositioned with grep evidence in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 45 introduces NO new inputs, network surface, auth, data handling, IPC
channel, or untrusted parsing. It is renderer/docs string edits + a net-new
static markdown file. No trust boundary is created, moved, or weakened by this
plan.

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds no boundary. The only adjacent security-relevant consideration is ensuring the copy sweep does not weaken the existing security-relevant typed-error path (the `<4.2`/`>=4.4` version guard + the Phase-44-D-10 `SpineVersionUnsupportedError` strings in src/core/errors.ts). That path is OUT of scope and protected by the D-08 allowlist + the explicit scope-leak grep guard in Task 2/3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | src/core/errors.ts version-guard / typed-error strings (a copy sweep that "cleans" the legit `<4.2`/`>=4.4` reject path would silently weaken a fail-loud security-relevant boundary) | mitigate | Task 2 + Task 3 each assert `git diff --quiet src/core/errors.ts src/core/loader.ts` / `src/main/doc-export.ts` exits 0 (byte-untouched scope-leak guard); D-08 allowlist + the narrow grep regex (matches "re-export as 4.2" specifically, never bare "re-export" or the `<4.2` guard) prevent over-sweeping the legit occurrences. |
| T-45-02 | Information Disclosure | CHANGELOG.md (net-new public release-history file) | accept | Content is intentional public release-note prose (milestone summary) authored from the ROADMAP; no secrets, paths, tokens, or PII. Mirrors the already-public .planning/MILESTONES.md content posture. Low-value target, no sensitive data. |
| T-45-03 | Spoofing/Repudiation | n/a | accept | No identity, auth, or audited action is in scope. No applicable threat. |
| T-45-04 | Denial of Service / Elevation of Privilege | n/a | accept | No executable code path, input parser, privilege, or resource allocation is added or modified (pure static copy/markdown). No applicable threat. |
</threat_model>

<verification>
- Task 1 grep: the App.tsx drop-zone reads the D-01/D-02/D-03 line; the admin
  advisory is byte-untouched.
- Task 2 grep: HelpDialog:128 + README:3/15/29 swept; the renderer/docs UX-02
  grep `git grep -inE 'spine 4\.2\+|4\.2 or later|spine v?4\.2 only|re-export as (version )?4\.2' -- src/renderer README.md INSTALL.md` returns NOTHING.
- Task 3: CHANGELOG.md exists with the mandatory v1.6 + "Spine 4.3 skeleton
  support (dual-runtime)" tokens; the D-09 no-op is recorded with grep
  evidence in the SUMMARY.
- SCOPE-LEAK GUARD (whole plan): `git diff --quiet src/core/errors.ts src/core/loader.ts src/main/doc-export.ts` exits 0 — runtime/loader/dispatch/error code is byte-untouched (ZERO behavior change; the dispatch flip was Phase 44).
- Targeted regression (NOT raw suite count — per project_renderer_mixblend_preexisting_failure): `npm run test -- tests/core/errors-version.spec.ts tests/core/loader-version-guard.spec.ts` exits 0 (the copy sweep did not collaterally break the typed-error / version-guard assertions; the ~11 tests/renderer/* MixBlend IMPORT failures are pre-existing + Phase-47-owned + NOT a Phase-45 regression).
</verification>

<success_criteria>
- UX-01 (copy): the App.tsx drop-zone no longer says "v4.2 [.spine]"; it reads
  "Drop a Spine v4.2 or v4.3 JSON file anywhere in this window" with both
  version tokens in font-bold text-danger (D-01/D-02/D-03 satisfied).
- UX-02 (copy portion): HelpDialog:128 + README:3/15 read the affirmative
  "Spine 4.2 and 4.3"; README:29 states the D-05 exact reject band; the
  renderer/docs UX-02 grep is clean under the D-08 allowlist; CHANGELOG.md
  exists at repo root with the mandatory v1.6 / "Spine 4.3 skeleton support
  (dual-runtime)" tokens; the doc-export D-09 no-op is explicitly
  dispositioned with grep evidence in the SUMMARY.
- ZERO scope leak: src/core/errors.ts, src/core/loader.ts, src/core/runtime/*,
  src/main/doc-export.ts are byte-untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/45-dispatcher-user-facing-flip-copy-docs-sweep/45-01-SUMMARY.md`.

The SUMMARY MUST include:
- The D-09 confirm-and-no-op disposition block WITH the pasted
  `grep -niE "4\.2|re-export|version 4|spine v?4" src/main/doc-export.ts`
  evidence (proven, not silently skipped).
- Any line swept beyond the README:3/15/29 + HelpDialog:128 set found by the
  exhaustive UX-02 grep (D-04 says do NOT assume that list is complete).
- The intentional-deferral note for CLAUDE.md:5 (the 3rd mirrored product
  sentence, out-of-grep-scope consistency-only, not a UX-02 gate item).
</output>
