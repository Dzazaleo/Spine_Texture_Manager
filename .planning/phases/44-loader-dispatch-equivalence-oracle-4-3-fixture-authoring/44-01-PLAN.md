---
phase: 44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/SIMPLE_PROJECT_43/skeleton2_42.json
  - fixtures/SIMPLE_PROJECT_43/skeleton2_42.atlas
  - fixtures/SIMPLE_PROJECT_43/skeleton2_42.png
  - fixtures/SLIDER_4_3/
  - fixtures/XTRA01_4_3/
  - fixtures/XTRA02_4_3/
  - tests/runtime43/baseline-driver.ts
  - tests/safe01/phase-gate.ts
autonomous: true
requirements: [ORCL-01]
nyquist_compliant: true
wave_0_complete: false

must_haves:
  truths:
    - "D-05: the already-on-disk-uncommitted ORCL-02 4.2 leg (fixtures/SIMPLE_PROJECT_43/skeleton2_42.{json,atlas,png}) is staged+committed as part of this phase's fixture-authoring plan with plain-English git narration (user does not run git) — git-tracked, not just on-disk"
    - "D-01: the 3 owner-exported 4.3 rigs (SLIDER_4_3/, XTRA01_4_3/, XTRA02_4_3/), exported by the owner before Phase 44 executes, are git-tracked and redistributable; phase stays fully scoped (no roadmap/REQUIREMENTS re-map)"
    - "tests/runtime43/baseline-driver.ts exports buildLoadXtra01() and buildLoadXtra02() that drive their rigs through runtime-43 via the sampler (clones of buildLoad43)"
    - "CURRENT_PHASE = 44 arms tests/safe01/phase44-fixture-guard.spec.ts and it passes (the guarded dirs exist)"
    - "The D-15 PASS is preserved: skeleton2_42.atlas (new libgdx format) is committed byte-untouched — NO .atlas normalization step"
  artifacts:
    - path: "fixtures/SIMPLE_PROJECT_43/skeleton2_42.json"
      provides: "ORCL-02 4.2 leg skeleton (spine:'4.2-from-4.3.01', legacy transform/path, NO top-level constraints[])"
      contains: "4.2-from-4.3.01"
    - path: "tests/runtime43/baseline-driver.ts"
      provides: "buildLoadXtra01 + buildLoadXtra02 cross-runtime sampler drivers for the XTRA rigs"
      exports: ["buildLoadXtra01", "buildLoadXtra02"]
    - path: "tests/safe01/phase-gate.ts"
      provides: "CURRENT_PHASE bumped 42 -> 44 (committed-constant phase marker)"
      contains: "CURRENT_PHASE = 44"
  key_links:
    - from: "tests/runtime43/baseline-driver.ts buildLoadXtra01/02"
      to: "fixtures/XTRA01_4_3/ + fixtures/XTRA02_4_3/"
      via: "readFileSync + pickRuntime('4.3') + buildSourceDims (clone of buildLoad43 lines 128-157)"
      pattern: "fixtures/XTRA0(1|2)_4_3"
    - from: "tests/safe01/phase-gate.ts CURRENT_PHASE=44"
      to: "tests/safe01/phase44-fixture-guard.spec.ts"
      via: "it.skipIf(CURRENT_PHASE < 44) flips skip -> hard pass (dirs present)"
      pattern: "CURRENT_PHASE\\s*<\\s*44"
---

<objective>
Acquire and commit every owner-blocked in-repo 4.3 fixture (the ORCL-01
deliverable), wire the cross-runtime sampler drivers the XTRA/ORCL specs
consume, and arm the Phase-44 fixture guard — all BEFORE the dispatch flip and
the oracle specs that depend on them.

This is Wave 1: a fixture/infrastructure foundation plan. It commits the
on-disk-but-uncommitted 4.2 sibling (`skeleton2_42.*` — the ORCL-02 4.2 leg)
plus the 3 owner-exported rigs (`SLIDER_4_3/`, `XTRA01_4_3/`, `XTRA02_4_3/`),
adds `buildLoadXtra01()`/`buildLoadXtra02()` to the existing
`baseline-driver.ts` (clones of the proven `buildLoad43`), and bumps
`CURRENT_PHASE` 42 → 44.

Purpose: ORCL-01 (in-repo redistributable 4.3 + 4.2-sibling rig) + the driver
infra ORCL-02/XTRA specs require. Sequenced first so the dispatch flip (Plan
02) and the oracle specs (Plan 04) have their fixtures + drivers in place.
Output: 5+ committed fixture artifacts, 2 new driver functions, the armed
phase guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-CONTEXT.md
@.planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-RESEARCH.md
@.planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-PATTERNS.md

<interfaces>
<!-- Extracted from the live codebase 2026-05-17. Use these directly — do NOT explore. -->

The driver-clone source — tests/runtime43/baseline-driver.ts (LIVE-VERIFIED anchors):
  - `buildLoad43(): { load: LoadResult; rt: SpineRuntime } | null` at lines 128-157.
    Reads `fixtures/SIMPLE_PROJECT_43/skeleton2.atlas` (line 132) + `.json` (line
    138), calls `buildSourceDims(atlasText)` (helper at lines 66-118 — replicates
    loader.ts sourceDims derivation), `pickRuntime('4.3')`, `rt.makeAtlas`,
    `rt.parseSkeleton`, `rt.applyRotatedRegionFix`, returns a LoadResult.
  - `buildLoadSibling42(): {...} | null` at lines 192-211 (the ORCL-02 4.2 leg —
    already wired; reads `skeleton2_42.json`/`.atlas` via `pickRuntime('4.2')`).
  - `buildSourceDims(atlasText: string): Map<string, SourceDims>` at lines 66-118.
  - `sample(load: LoadResult): SamplerOutput` at lines 259-261.
  - `REPO_ROOT` constant + `fixtures/SIMPLE_PROJECT_43/skeleton2_42.json` path at
    lines 38-41.

The loud-or-skip presence-guard idiom — tests/runtime43/load43.ts:47-72:
  `const rt = pickRuntime('4.3'); if (rt == null) throw new Error('... NOT a
  Wave-0 skip.'); try { json = JSON.parse(readFileSync(...)); ... } catch (err)
  { if (isFileAbsent(err)) return null; throw err; }`
  → a broken pickRuntime PROPAGATES; only genuine ENOENT returns null (skip).

The phase-gate constant — tests/safe01/phase-gate.ts:23:
  `export const CURRENT_PHASE = 42 as const;`  (committed-constant by design —
  Q2-RESOLVED, NOT a tracking-file parse)

The guard it arms — tests/safe01/phase44-fixture-guard.spec.ts:
  `it.skipIf(CURRENT_PHASE < 44)` checking `existsSync(fixtures/SIMPLE_PROJECT_43)`
  AND `existsSync(fixtures/SLIDER_4_3)` — flips skip → HARD-FAIL if the dirs are
  absent once CURRENT_PHASE >= 44.

The fixture-set already on disk (VERIFIED via git ls-files 2026-05-17):
  - `fixtures/SIMPLE_PROJECT_43/skeleton2.{json,atlas,png}` — COMMITTED (Phase 43).
  - `fixtures/SIMPLE_PROJECT_43/skeleton2_42.{json,atlas,png}` — ON DISK,
    UNCOMMITTED (git status shows them untracked). spine:"4.2-from-4.3.01".
  - `fixtures/SLIDER_4_3/`, `fixtures/XTRA01_4_3/`, `fixtures/XTRA02_4_3/` —
    owner-exported pre-execution per D-01 (verify presence at task start).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildLoadXtra01() + buildLoadXtra02() to baseline-driver.ts</name>
  <files>tests/runtime43/baseline-driver.ts</files>
  <read_first>
    - tests/runtime43/baseline-driver.ts (FULL — you are modifying it; study
      buildLoad43 lines 128-157, buildSourceDims 66-118, buildLoadSibling42
      192-211, sample 259-261, the REPO_ROOT + path constants 38-41, and the
      import block so the clone is byte-faithful)
    - tests/runtime43/load43.ts:34-72 (the isFileAbsent + loud-or-skip contract
      — replicate the "broken pickRuntime PROPAGATES, ENOENT returns null"
      semantics in the new builders)
    - .planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-PATTERNS.md
      (§ "tests/runtime43/xtra01-baseline.spec.ts ..." — the "Delta the planner
      must specify" paragraph: clone buildLoad43 verbatim, swap fixture paths)
  </read_first>
  <action>
    Add two exported functions to `tests/runtime43/baseline-driver.ts`,
    immediately after `buildLoadSibling42()` (after line ~211). Each is a
    VERBATIM clone of `buildLoad43()` (lines 128-157) with ONLY the fixture
    paths swapped — do NOT re-derive the LoadResult shape (RESEARCH "Don't
    Hand-Roll": re-deriving sourceDims risks a faithfulness drift the existing
    driver already solved):

    ```typescript
    /**
     * XTRA-01 (multi-map TransformConstraint rig) through runtime-43 via the
     * sampler. Clone of buildLoad43 — only the fixture dir differs. Loud-or-skip:
     * pickRuntime('4.3') failure PROPAGATES (verification-integrity, NOT a skip);
     * only genuine ENOENT on the rig returns null (legit Wave-0 fixture-absence).
     */
    export function buildLoadXtra01(): { load: LoadResult; rt: SpineRuntime } | null {
      // body = buildLoad43 body, with:
      //   'fixtures/SIMPLE_PROJECT_43/skeleton2.atlas' -> resolve the .atlas in
      //       fixtures/XTRA01_4_3/  (use the SAME filename the owner exported;
      //       discover it via the directory — see note below)
      //   'fixtures/SIMPLE_PROJECT_43/skeleton2.json'  -> the .json in
      //       fixtures/XTRA01_4_3/
      // pickRuntime('4.3'), buildSourceDims, rt.makeAtlas, rt.parseSkeleton,
      // rt.applyRotatedRegionFix — IDENTICAL to buildLoad43.
    }

    export function buildLoadXtra02(): { load: LoadResult; rt: SpineRuntime } | null {
      // identical, fixtures/XTRA02_4_3/
    }
    ```

    Owner-rig internal filenames are NOT locked (only the dir names are — D-01
    / CONTEXT Claude's Discretion). Resolve the `.json` and `.atlas` inside each
    `fixtures/XTRA0{1,2}_4_3/` directory by reading the directory contents
    (`readdirSync`) and picking the single `*.json` and single `*.atlas` — do
    NOT hardcode an assumed filename. If the directory is absent or empty,
    return `null` (legit Wave-0 skip via the `isFileAbsent`/ENOENT idiom from
    load43.ts:34-72). A `pickRuntime('4.3')` returning null MUST throw (NOT
    return null) — that is a verification-integrity failure, not fixture
    absence (the exact 43-03 contract in load43.ts).

    Preserve every import, the `REPO_ROOT` resolution, and the return-shape
    exactly as `buildLoad43` uses them. Do NOT touch `buildLoad43`,
    `buildLoadSibling42`, `loadSibling42`, `sample`, `squarePeakScale`, or
    `buildSourceDims`.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime43/ --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function buildLoadXtra01" tests/runtime43/baseline-driver.ts` returns a match
    - `grep -n "export function buildLoadXtra02" tests/runtime43/baseline-driver.ts` returns a match
    - `grep -n "fixtures/XTRA01_4_3" tests/runtime43/baseline-driver.ts` returns a match (path swapped, not SIMPLE_PROJECT_43)
    - `grep -n "fixtures/XTRA02_4_3" tests/runtime43/baseline-driver.ts` returns a match
    - `grep -c "export function buildLoad43" tests/runtime43/baseline-driver.ts` still returns 1 (original untouched)
    - `npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "baseline-driver" | grep -v '^#'` is empty (no type errors in the modified file)
    - `npx vitest run tests/runtime43/ --reporter=dot` exits 0 (existing runtime43 specs still green; new builders compile and the existing harness is unbroken — XTRA specs do not exist yet so nothing consumes the builders this wave)
  </acceptance_criteria>
  <done>
    baseline-driver.ts exports buildLoadXtra01 + buildLoadXtra02 as faithful
    buildLoad43 clones pointing at fixtures/XTRA0{1,2}_4_3/, resolving the
    rig filenames by directory scan, with the loud-or-skip presence-guard
    contract preserved; the original drivers are byte-untouched; the runtime43
    suite stays green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stage + commit the 5 fixture artifacts (D-05, plain-English git narration)</name>
  <files>fixtures/SIMPLE_PROJECT_43/skeleton2_42.json, fixtures/SIMPLE_PROJECT_43/skeleton2_42.atlas, fixtures/SIMPLE_PROJECT_43/skeleton2_42.png, fixtures/SLIDER_4_3/, fixtures/XTRA01_4_3/, fixtures/XTRA02_4_3/</files>
  <read_first>
    - .planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-CONTEXT.md
      (D-05 — fixture commits folded into Phase 44 execution with plain-English
      git narration; the user does NOT run git; D-15 RESOLVED HARD PASS — NO
      .atlas normalization)
    - .planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-PATTERNS.md
      (§ "Fixture-commit task (D-05)" — stage the uncommitted skeleton2_42.* +
      the 3 owner rigs; D-15 PASS so skeleton2_42.atlas is byte-untouched)
    - fixtures/SIMPLE_PROJECT_43/skeleton2_42.json (CONFIRM spine:"4.2-from-4.3.01",
      NO top-level "constraints" array, HAS legacy "transform"/"path" — this is
      the ORCL-02 4.2 leg; routing correctness depends on this shape per D-07/D-08)
  </read_first>
  <action>
    The user has very little hands-on git experience and does NOT run git
    (memory `user_git_experience` / `feedback_explain_git`). Narrate EVERY git
    step in plain English BEFORE running it (what the command does and why),
    then run it.

    Step 1 — Presence check (fail loud if D-01 unmet): verify all owner rigs
    exist:
      `ls -la fixtures/SLIDER_4_3/ fixtures/XTRA01_4_3/ fixtures/XTRA02_4_3/ fixtures/SIMPLE_PROJECT_43/skeleton2_42.json fixtures/SIMPLE_PROJECT_43/skeleton2_42.atlas fixtures/SIMPLE_PROJECT_43/skeleton2_42.png`
    If ANY of `fixtures/SLIDER_4_3/`, `fixtures/XTRA01_4_3/`, `fixtures/XTRA02_4_3/`
    is missing or empty: STOP and report "D-01 precondition unmet: owner has not
    exported {dir}. Per `42-OWNER-EXPORT-SPEC.md` §3-§5 the owner must export
    these before Phase 44 executes. The phase44-fixture-guard.spec.ts is the
    designed enforcement — it will hard-fail at CURRENT_PHASE>=44." Do NOT
    fabricate or stub fixtures. (Per CONTEXT D-05 the commit task "stages
    whatever is on disk" — but the 3 owner dirs are a hard D-01 precondition;
    `skeleton2_42.*` being absent is also a stop.)

    Step 2 — D-15 verification (NO normalization): confirm
    `fixtures/SIMPLE_PROJECT_43/skeleton2_42.atlas` is committed BYTE-UNTOUCHED.
    D-15 is RESOLVED to a HARD PASS (RESEARCH § D-15 Verdict: spine-core@4.2.111
    TextureAtlas.parse handles the new libgdx format natively; the identical
    format is the in-production LOCKED SIMPLE_TEST.atlas golden). Do NOT
    normalize, rewrite, or edit the .atlas text. Do NOT amend
    42-OWNER-EXPORT-SPEC.md. The skeleton JSON is JSON-invariant since v1.0 —
    also byte-untouched.

    Step 3 — Stage. Explain: "git add tells git which files to include in the
    next snapshot. We're adding the 4.2-sibling oracle fixture and the three
    owner-exported 4.3 rigs." Run:
      `git add fixtures/SIMPLE_PROJECT_43/skeleton2_42.json fixtures/SIMPLE_PROJECT_43/skeleton2_42.atlas fixtures/SIMPLE_PROJECT_43/skeleton2_42.png fixtures/SLIDER_4_3/ fixtures/XTRA01_4_3/ fixtures/XTRA02_4_3/`
    Then `git status --short` and narrate exactly which files are now staged.

    Step 4 — Verify the .gitignore does NOT exclude these dirs (the fixture
    convention requires in-repo redistributable rigs — ORCL-01/§7):
      `git check-ignore fixtures/SLIDER_4_3 fixtures/XTRA01_4_3 fixtures/XTRA02_4_3 fixtures/SIMPLE_PROJECT_43/skeleton2_42.json 2>&1 || echo "NOT IGNORED — good"`
    If any IS ignored, STOP and report (the owner rig must be redistributable;
    a .gitignore rule would silently drop it — memory
    `feedback_gitignore_fixtures_check_test_refs`).

    Step 5 — Commit. Explain: "git commit saves the staged snapshot with a
    message." Run:
      `git commit -m "test(44-01): commit ORCL-01 4.2-sibling + owner-exported 4.3 rigs (SLIDER/XTRA01/XTRA02)" -m "skeleton2_42.* is the ORCL-02 4.2 leg (spine 4.2-from-4.3.01, legacy transform/path). D-15 PASS: new-format atlas committed byte-untouched (spine-core@4.2.111 parses it natively). 3 owner rigs per 42-OWNER-EXPORT-SPEC §3-§5." -m "Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"`
    Then `git log --oneline -1` and narrate that the commit landed and what it
    contains.

    Do NOT push (memory `feedback_dont_push_release_tags` — and never push
    without explicit ship-decision).
  </action>
  <verify>
    <automated>git ls-files fixtures/SIMPLE_PROJECT_43/skeleton2_42.json fixtures/SLIDER_4_3 fixtures/XTRA01_4_3 fixtures/XTRA02_4_3 | sort</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files fixtures/SIMPLE_PROJECT_43/skeleton2_42.json` is non-empty (the 4.2 leg is git-tracked)
    - `git ls-files fixtures/SIMPLE_PROJECT_43/skeleton2_42.atlas` is non-empty
    - `git ls-files fixtures/SIMPLE_PROJECT_43/skeleton2_42.png` is non-empty
    - `git ls-files fixtures/SLIDER_4_3/` is non-empty
    - `git ls-files fixtures/XTRA01_4_3/` is non-empty
    - `git ls-files fixtures/XTRA02_4_3/` is non-empty
    - `grep -l '"spine"' fixtures/SIMPLE_PROJECT_43/skeleton2_42.json && grep -c '4.2-from-4.3.01' fixtures/SIMPLE_PROJECT_43/skeleton2_42.json` returns >=1 (the ORCL-02 4.2-leg token is intact / byte-untouched)
    - `python3 -c "import json,sys; d=json.load(open('fixtures/SIMPLE_PROJECT_43/skeleton2_42.json')); print('constraints' in d)"` prints `False` (NO top-level constraints[] — confirms D-08 routes it to 4.2 with no contradiction)
    - `git log --oneline -1 | grep -i "44-01"` returns a match (commit landed)
    - `git status --short fixtures/SIMPLE_PROJECT_43/skeleton2_42.*` is empty (nothing left uncommitted)
  </acceptance_criteria>
  <done>
    The ORCL-02 4.2 leg (skeleton2_42.{json,atlas,png}) and the 3 owner rigs
    (SLIDER_4_3/, XTRA01_4_3/, XTRA02_4_3/) are git-tracked and committed under
    44-01; skeleton2_42.atlas is byte-untouched (D-15 PASS, no normalization);
    skeleton2_42.json retains spine:"4.2-from-4.3.01" with no top-level
    constraints[]; every git step was narrated in plain English; nothing was
    pushed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bump CURRENT_PHASE 42 -> 44 (arm the fixture guard)</name>
  <files>tests/safe01/phase-gate.ts</files>
  <read_first>
    - tests/safe01/phase-gate.ts (FULL — you are modifying line 23; the
      file-header comment explains the skip→hard-fail semantics)
    - tests/safe01/phase44-fixture-guard.spec.ts (the guard this arms —
      it.skipIf(CURRENT_PHASE < 44) checks SIMPLE_PROJECT_43/ + SLIDER_4_3/
      existence; both now exist post-Task-2)
    - .planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-PATTERNS.md
      (§ "tests/safe01/phase-gate.ts (MOD — 1-line const bump)")
  </read_first>
  <action>
    Edit `tests/safe01/phase-gate.ts` line 23 ONLY:
      `export const CURRENT_PHASE = 42 as const;`
      →
      `export const CURRENT_PHASE = 44 as const;`

    Change NOTHING else in the file. This is the Q2-RESOLVED committed-constant
    phase marker (deliberately NOT a brittle milestone-state-file parse). It
    arms `tests/safe01/phase44-fixture-guard.spec.ts:55` (`it.skipIf(CURRENT_PHASE
    < 44)`): the guard flips from skip-with-reason to a HARD assertion that
    `fixtures/SIMPLE_PROJECT_43/` AND `fixtures/SLIDER_4_3/` exist. Task 2
    committed both, so the guard now PASSES (not skips, not fails).

    Sequencing note (do not reorder): this bump is safe in Wave 1 BEFORE the
    Plan-02 dispatch flip — the bump only arms phase44-fixture-guard; it does
    NOT affect `safe01-enumeration`/`safe01-baseline` (those break only from
    the dispatch flip removing the 4.3-reject, which is Plan 02's D-04 denylist
    concern, not this constant).
  </action>
  <verify>
    <automated>npx vitest run tests/safe01/phase44-fixture-guard.spec.ts tests/safe01/safe01-enumeration.spec.ts tests/safe01/safe01-baseline.spec.ts --reporter=dot 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CURRENT_PHASE = 44 as const" tests/safe01/phase-gate.ts` returns exactly one match (line ~23)
    - `grep -c "CURRENT_PHASE = 42" tests/safe01/phase-gate.ts` returns 0 (old value fully replaced)
    - `npx vitest run tests/safe01/phase44-fixture-guard.spec.ts --reporter=dot` exits 0 AND the guard test is NOT skipped (it now asserts dir existence and passes — Task 2 committed the dirs)
    - `npx vitest run tests/safe01/safe01-enumeration.spec.ts tests/safe01/safe01-baseline.spec.ts --reporter=dot` exits 0 (the phase-gate bump alone does NOT break SAFE-01 — the dispatch flip has not landed yet; this confirms the Wave-1 sequencing is safe)
  </acceptance_criteria>
  <done>
    tests/safe01/phase-gate.ts has CURRENT_PHASE = 44; phase44-fixture-guard.spec.ts
    runs (not skipped) and passes because the guarded fixture dirs are committed;
    SAFE-01 enumeration + baseline remain green (Wave-1 ordering verified safe
    pre-flip).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner export → in-repo fixture | Owner-exported Spine JSON/atlas/PNG enters the repo as committed test data. NOT runtime user input — these are in-repo test fixtures consumed only by vitest, never by the shipped app's loader at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | committed fixture (skeleton2_42.atlas) | accept | New-format atlas committed byte-untouched per D-15 PASS (no normalization that could introduce a transcription error). In-repo test data, not a runtime trust surface — no new attack surface (RESEARCH § Security Domain: "fixtures are in-repo test data, not runtime input"). |
| T-44-02 | Information Disclosure | owner rigs redistributable | accept | ORCL-01 requires the rigs be redistributable (§7); they are owner-authored synthetic test rigs (SIMPLE_TEST-equivalent), no PII/secrets. Verified non-ignored so they are intentionally in-repo. |

No new network, IPC, filesystem-trust, or auth boundary. This plan only commits
in-repo test fixtures and adds test-harness driver functions.
</threat_model>

<verification>
- `npx vitest run tests/runtime43/ tests/safe01/phase44-fixture-guard.spec.ts tests/safe01/safe01-enumeration.spec.ts tests/safe01/safe01-baseline.spec.ts --reporter=dot` exits 0.
- `git ls-files fixtures/SLIDER_4_3 fixtures/XTRA01_4_3 fixtures/XTRA02_4_3 fixtures/SIMPLE_PROJECT_43/skeleton2_42.json` lists all four (the ORCL-01 in-repo deliverable).
- `grep -n "CURRENT_PHASE = 44" tests/safe01/phase-gate.ts` matches.
- `grep -n "export function buildLoadXtra0[12]" tests/runtime43/baseline-driver.ts` matches both.
- Pre-existing noise reminder: the ~11 `tests/renderer/*` MixBlend IMPORT
  failures are Phase-47-owned, NOT a Phase-44 regression (memory
  `project_renderer_mixblend_preexisting_failure`). Trust the targeted gates.
</verification>

<success_criteria>
- ORCL-01: `fixtures/SIMPLE_PROJECT_43/skeleton2_42.{json,atlas,png}` + the 3
  owner rigs (`SLIDER_4_3/`, `XTRA01_4_3/`, `XTRA02_4_3/`) are git-tracked,
  redistributable (non-ignored), and committed under 44-01.
- `skeleton2_42.atlas` byte-untouched (D-15 PASS — no normalization step).
- `skeleton2_42.json` retains `spine:"4.2-from-4.3.01"` with NO top-level
  `constraints[]` (the D-07/D-08 routing-correctness precondition for the
  ORCL-02 4.2 leg).
- `tests/runtime43/baseline-driver.ts` exports `buildLoadXtra01` +
  `buildLoadXtra02` (faithful `buildLoad43` clones).
- `tests/safe01/phase-gate.ts` has `CURRENT_PHASE = 44`;
  `phase44-fixture-guard.spec.ts` passes (not skipped).
- SAFE-01 enumeration + baseline remain green (Wave-1 pre-flip sequencing
  verified safe).
</success_criteria>

<output>
After completion, create `.planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-01-SUMMARY.md`
</output>
