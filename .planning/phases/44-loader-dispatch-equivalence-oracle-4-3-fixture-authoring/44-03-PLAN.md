---
phase: 44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring
plan: 03
type: execute
wave: 3
depends_on: [44-02]
files_modified:
  - tests/core/loader-version-guard.spec.ts
  - tests/core/errors-version.spec.ts
  - tests/runtime/d13-43-load-smoke.spec.ts
  - tests/core/loader.spec.ts
  - tests/core/ipc.spec.ts
  - tests/main/ipc.spec.ts
  - tests/main/viewer-asset-feed-ipc.spec.ts
autonomous: true
requirements: [DISP-01, DISP-03]
nyquist_compliant: true
wave_0_complete: false

must_haves:
  truths:
    - "Every 4.3-input test arm asserts ROUTING (dispatch target / no throw), not the OLD 4.3-reject — CI is green at Phase-44 exit (D-11)"
    - "The <4.2 and >=4.4 throw-cases stay EXPLICITLY asserted in the test suite (preserved, not deleted — a passing test still asserting the OLD 4.3-reject is a false-green; deleting the <4.2/>=4.4 asserts is a silent descope)"
    - "d13-43-load-smoke.spec.ts is 'made real' — it now ALSO exercises the gated loader routing a 4.3 fixture (D-11)"
    - "The dispatch routes a 4.3 input correctly through ALL 3 entrypoints: vitest, the BUILT out/main CJS worker, and the tsx/ESM CLI (not npm test alone)"
    - "ORCL-03 disposition recorded: v1.6 NO-OP by design — ik absent in both skeleton2.json + skeleton2_42.json (Phase 42 D-03); no human gate"
    - "The ROADMAP Phase-45 SC#3 split is documented (Phase 44 owns 4.3-routing assertions + preserving <4.2/>=4.4 throws; Phase 45 owns the user-facing copy/docs/drop-zone sweep + final reject-test inversion) — NOT silently descoped"
  artifacts:
    - path: "tests/core/loader-version-guard.spec.ts"
      provides: "FIXTURE_43 (SPINE_4_3_TEST 4.3.91-beta) arm flipped reject->route; FIXTURE_38 (3.8.99) <4.2 throw PRESERVED"
      contains: "loadSkeleton(FIXTURE_43)"
    - path: "tests/runtime/d13-43-load-smoke.spec.ts"
      provides: "a gated-loader-routes arm (D-11 'makes it real')"
      contains: "loadSkeleton"
  key_links:
    - from: "the dispatch flip (loader.ts resolveRuntimeTag)"
      to: "out/main built CJS worker + npm run cli (tsx/ESM)"
      via: "3-entrypoint verification: npm run build + sample-4.3-through-built-worker + npm run cli -- fixtures/SIMPLE_PROJECT_43/skeleton2.json"
      pattern: "runtime-43\\.cjs|register-esm-adapter-resolver"
---

<objective>
Reconcile the full D-11 test suite to the new dispatch behavior — flipping
every 4.3-input arm from reject to routing while EXPLICITLY preserving the
`<4.2`/`≥4.4` throw-cases — and verify the dispatch resolves correctly across
ALL THREE entrypoints (vitest, the built CJS worker, the tsx/ESM CLI).

Plan 02 landed the flip and the two Wave-0 predicate specs. This plan finishes
the remaining 8 D-11 files so CI is green at Phase-44 exit (D-11's hard
requirement), and runs the load-bearing Multi-Runtime Entrypoint verification:
`pickRuntime`'s `'4.3'` `require('../runtime-43.cjs')` arm is exercised in
PRODUCTION for the first time by this flip — `npm test` alone green-washes it
(Phase 43's GAP-43-CLI-SEAM was a 5/5-verified phase with a fully-broken CLI;
memory `feedback_verify_all_entrypoint_runtimes_of_a_perruntime_seam`).

Purpose: DISP-01/03 test-completeness + the 3-entrypoint integrity gate.
Output: 7 reconciled D-11 test files, the made-real smoke spec, the verified
3-entrypoint matrix, the ORCL-03 + Phase-45-split dispositions recorded.

The Task-3 multi-runtime verification is fully CLI-scriptable (`npm run test` /
`npm run build` / `npm run cli`), so it is an `auto` task with a HARD-STOP-on-
failure embedded — a build/CLI failure on the 4.3 production arm is a hard stop
(report + scope the fix locus), NOT a soft skip or a silent pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-CONTEXT.md
@.planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-RESEARCH.md
@.planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-PATTERNS.md
@.planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-02-PLAN.md

<interfaces>
<!-- D-11 file classification — RESOLVED at plan time via grep (Open-Q2). Each
     file's exact reclassification rule. Use directly. -->

ALREADY DONE in Plan 02 (do NOT re-touch — they are green):
  - tests/core/loader-version-guard-predicate.spec.ts
  - tests/core/loader-43-schema-guard-predicate.spec.ts

HEAVY — must flip 4.3 arms reject→route, PRESERVE <4.2/≥4.4 throws:
  - tests/core/loader-version-guard.spec.ts (40 refs). LIVE-VERIFIED:
    FIXTURE_38 = fixtures/SPINE_3_8_TEST/SPINE_3_8_TEST.json (spine 3.8.99 — a
      <4.2 reject → PRESERVE the `loadSkeleton(FIXTURE_38)` toThrow asserts at
      :44/:50/:61).
    FIXTURE_42 = fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json (:97/:157 not.toThrow
      — UNCHANGED).
    FIXTURE_43 = fixtures/SPINE_4_3_TEST/SPINE_4_3_TEST.json (spine 4.3.91-beta).
      :102-103 `loadSkeleton(FIXTURE_43)` toThrow(SpineVersionUnsupportedError)
      + :125 message toContain('Re-export from your 4.3 editor as Version 4.2')
      → FLIP: 4.3 now ROUTES (no throw). Replace the reject assertions with a
      routing assertion (loadSkeleton(FIXTURE_43) does NOT throw; it returns a
      LoadResult — and per the SAFE-01 D-04 denylist this fixture is excluded
      from SAFE-01 discovery so loading it directly here is the routing proof).
  - tests/core/errors-version.spec.ts (31 refs). Plan 02 Task 2 ALREADY added
    the ≥4.4/contradiction cases. THIS plan: verify the EXISTING
    `'4.3-schema'`-sentinel (:61-64) + `'4.3.91-beta'`-semver (:69-70) cases
    that asserted the OLD "Re-export from your 4.3 editor as Version 4.2"
    wording are RECONCILED — '4.3.91-beta' no longer rejects via this class on
    the routing path; the '4.3-schema' sentinel now carries the contradiction
    wording. PRESERVE the <4.2 case (:85 'requires Spine 4.2 or later'). (If
    Plan 02 fully covered these, this is a verify-only no-op for this file —
    confirm, don't re-edit redundantly.)

ALREADY WANTS THE NEW BEHAVIOR — "make it real" (D-11):
  - tests/runtime/d13-43-load-smoke.spec.ts (48 refs). LIVE-VERIFIED: today it
    drives the 4.3 runtime DIRECTLY (resolves the canonical 4.3.0 spine-core,
    parses fixtures/SPINE_4_3_TEST/SPINE_4_3_TEST.json, asserts
    bones[]/skins[]). It does NOT currently go through `loadSkeleton`. D-11
    "makes it real" → ADD an arm that calls `loadSkeleton(fixtures/
    SIMPLE_PROJECT_43/skeleton2.json)` (the committed 4.3 leg, spine "4.3.01",
    top-level constraints[]) and asserts it routes (no throw; returns a
    LoadResult). Keep the existing direct-runtime arms.

UNRELATED / MOCKED — verify-only, do NOT change assertions (Open-Q2 grep):
  - tests/core/loader.spec.ts (10 refs — all are atlas "Re-export from the
    Spine editor" / SkeletonJsonNotFoundError at :77/:148/:291; NOT
    version-reject). No version change. Run it to confirm no regression.
  - tests/core/ipc.spec.ts (3 refs — `spine:"4.2.43"` happy-path context at
    :82-85; a <4.2-guard precondition, not a 4.3-reject). No change; verify.
  - tests/main/ipc.spec.ts (3 refs — `checkSpineVersion` mocked as a no-op at
    :120; the :232 not.toThrow is unrelated). No change; verify.
  - tests/main/viewer-asset-feed-ipc.spec.ts (3 refs — `checkSpineVersion:
    vi.fn()` mock + `spine:'4.2'` at :104/:149/:185). No change; verify.

Multi-Runtime resolver — src/core/runtime/runtime.ts:174-217 (READ-ONLY):
  - vitest arm: globalThis `__GSD_ESM_ADAPTER_RESOLVER__` (:178-188)
  - built CJS worker arm: `tag === '4.2' ? require('../runtime-42.cjs') :
    require('../runtime-43.cjs')` (:204) — the '4.3' arm is exercised in
    PRODUCTION for the FIRST time by this flip (A3 / Pitfall 5)
  - CLI tsx/ESM arm: scripts/register-esm-adapter-resolver.ts (43-07
    GAP-43-CLI-SEAM fix)

ORCL-03 disposition (Phase 42 D-03 — v1.6 NO-OP): `ik` is absent in BOTH
fixtures/SIMPLE_PROJECT_43/skeleton2.json AND skeleton2_42.json (VERIFIED:
constraint types = transform/path only). ORCL-01 is TransformConstraint-only →
spine-editor#891-immune → no human gate. Record the disposition in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the HEAVY D-11 4.3 arms reject->route; PRESERVE <4.2/>=4.4 throws</name>
  <files>tests/core/loader-version-guard.spec.ts, tests/core/errors-version.spec.ts</files>
  <read_first>
    - tests/core/loader-version-guard.spec.ts (FULL — you are modifying it;
      study FIXTURE_38/42/43 at :38-40, the FIXTURE_43 reject asserts at
      :102-103/:125, the FIXTURE_38 <4.2 asserts at :44/:50/:61, the FIXTURE_42
      not.toThrow at :97/:157)
    - tests/core/errors-version.spec.ts (FULL — verify Plan 02 Task 2's
      ≥4.4/contradiction cases are present; check the OLD-wording cases at
      :61-70 are reconciled; PRESERVE :85)
    - src/core/loader.ts resolveRuntimeTag + src/core/errors.ts (the live
      post-Plan-02 source — so the new assertions match actual behavior, not
      assumptions)
    - .planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-PATTERNS.md
      (§ "D-11 test files (10× MOD)" — the D-11 false-green-guard rule)
    - .planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-CONTEXT.md
      (D-11 — the 44/45 split is documented, NOT silently descoped; memory
      `feedback_replan_can_silently_descope_roadmap_contract`)
  </read_first>
  <action>
    Apply the D-11 false-green-guard rule to the HEAVY files. Flip ONLY the
    4.3-input arms to assert ROUTING; PRESERVE the `<4.2`/`≥4.4` throw-cases as
    EXPLICIT assertions (a passing test still asserting the OLD 4.3-reject is a
    false-green; deleting the `<4.2`/`≥4.4` throw asserts is a silent descope —
    memory `feedback_replan_can_silently_descope_roadmap_contract`).

    **tests/core/loader-version-guard.spec.ts:**
    - FIXTURE_43 arm (:102-103, :125): currently
      `expect(() => loadSkeleton(FIXTURE_43)).toThrow(SpineVersionUnsupportedError)`
      + `expect((caught as Error).message).toContain('Re-export from your 4.3
      editor as Version 4.2')`. REPLACE with a routing assertion:
      `expect(() => loadSkeleton(FIXTURE_43)).not.toThrow();` and assert the
      result is a LoadResult routed to the 4.3 runtime (e.g.
      `const load = loadSkeleton(FIXTURE_43); expect(load).toBeTruthy();` — and
      if the LoadResult carries a runtime tag/handle, assert it is the 4.3
      runtime; otherwise assert `load.skeletonData`/equivalent is populated,
      proving it parsed rather than rejected). Update the `it(...)` description
      from "rejects Spine 4.3.91-beta fixture" to "routes Spine 4.3.91-beta
      fixture to the 4.3 runtime (D-11: dispatch, not reject)".
    - FIXTURE_38 arms (:44, :50, :61): `loadSkeleton(FIXTURE_38)` is spine
      3.8.99 — a `<4.2` reject. PRESERVE these `toThrow(SpineVersionUnsupportedError)`
      + the `.message` `toContain('Re-export')` asserts EXACTLY (do NOT flip —
      <4.2 still rejects; this is the explicitly-preserved throw-case).
    - FIXTURE_42 arms (:97, :157): `not.toThrow` — UNCHANGED.
    - If the file's header/describe prose says "rejects ... 4.3" generically,
      update it to reflect routing for 4.3 while keeping the <4.2/≥4.4 reject
      language.

    **tests/core/errors-version.spec.ts:** Plan 02 Task 2 already added the
    `4.4.0`/`5.0.0` LOCKED-≥4.4-wording cases + the `4.3-schema` contradiction
    case + verified the `<4.2` case. THIS task: confirm the pre-existing
    OLD-wording cases at :61-64 (`'4.3-schema'` → old "Re-export from your 4.3
    editor as Version 4.2") and :69-70 (`'4.3.91-beta'` semver → old wording)
    are RECONCILED to the new behavior (the '4.3-schema' sentinel now asserts
    the contradiction wording; '4.3.91-beta' is a routing token, so a
    `new SpineVersionUnsupportedError('4.3.91-beta', P)` constructed directly
    must NOT emit the old "re-export as 4.2" string — assert it falls to the
    defensive 'unsupported' branch and does NOT contain that string). PRESERVE
    the `<4.2` `'requires Spine 4.2 or later'` assertion at :85 verbatim. If
    Plan 02 already fully reconciled this file, make NO redundant edits — just
    record "errors-version.spec.ts verified reconciled by Plan 02 Task 2" in
    the changes.

    Do NOT delete any `<4.2`/`≥4.4` throw assertion anywhere. Do NOT touch the
    user-facing drop-zone copy / docs / README — that is Phase 45 (UX-01/02);
    the ROADMAP Phase-45 SC#3 split is documented in CONTEXT D-11 and MUST be
    re-stated in this plan's SUMMARY (NOT silently descoped).
  </action>
  <verify>
    <automated>npx vitest run tests/core/loader-version-guard.spec.ts tests/core/errors-version.spec.ts --reporter=dot 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "loadSkeleton(FIXTURE_43)).toThrow" tests/core/loader-version-guard.spec.ts` returns 0 (the 4.3-reject assertion is flipped — no longer asserts throw)
    - `grep -cE "loadSkeleton\(FIXTURE_43\)\)\.not\.toThrow|loadSkeleton\(FIXTURE_43\)" tests/core/loader-version-guard.spec.ts` returns >=1 (a routing assertion replaced it)
    - `grep -c "loadSkeleton(FIXTURE_38)).toThrow" tests/core/loader-version-guard.spec.ts` returns >=1 (the <4.2 throw-case is PRESERVED as an explicit assertion — NOT deleted)
    - `grep -c "Re-export from your 4.3 editor as Version 4.2" tests/core/errors-version.spec.ts` returns 0 (the OLD wrong-for-4.3 assertion is removed/reconciled)
    - `grep -c "requires Spine 4.2 or later" tests/core/errors-version.spec.ts` returns >=1 (the <4.2 LOCKED-wording assertion is PRESERVED)
    - `grep -c "This app supports Spine 4.2 and 4.3" tests/core/errors-version.spec.ts` returns >=1 (the LOCKED ≥4.4-wording assertion is present — added Plan 02 Task 2, confirmed here)
    - `npx vitest run tests/core/loader-version-guard.spec.ts tests/core/errors-version.spec.ts --reporter=dot` exits 0
  </acceptance_criteria>
  <done>
    loader-version-guard.spec.ts asserts FIXTURE_43 (4.3.91-beta) ROUTES (not
    rejects) while PRESERVING the FIXTURE_38 (3.8.99) <4.2 throw asserts;
    errors-version.spec.ts is reconciled (no OLD "re-export as 4.2" assertion,
    LOCKED ≥4.4 + <4.2 wordings asserted); no <4.2/≥4.4 throw assertion was
    deleted; both green.
  </done>
</task>

<task type="auto">
  <name>Task 2: "Make real" d13-43-load-smoke.spec.ts + verify the unrelated/mocked D-11 files</name>
  <files>tests/runtime/d13-43-load-smoke.spec.ts, tests/core/loader.spec.ts, tests/core/ipc.spec.ts, tests/main/ipc.spec.ts, tests/main/viewer-asset-feed-ipc.spec.ts</files>
  <read_first>
    - tests/runtime/d13-43-load-smoke.spec.ts (FULL — you are modifying it;
      study the existing direct-4.3.0-runtime arms at :50-208, what it asserts
      today bones[]/skins[] at :189-192, and the file-header comment 5-40
      explaining the beta-vs-stable parse fallback)
    - src/core/loader.ts (the live post-Plan-02 loadSkeleton + resolveRuntimeTag
      — the new arm calls loadSkeleton and must match actual routing behavior)
    - fixtures/SIMPLE_PROJECT_43/skeleton2.json (the committed 4.3 leg —
      spine "4.3.01", top-level constraints[]; the gated-loader arm loads THIS,
      not SPINE_4_3_TEST which is the existing direct-runtime arm's fixture)
    - tests/core/loader.spec.ts, tests/core/ipc.spec.ts, tests/main/ipc.spec.ts,
      tests/main/viewer-asset-feed-ipc.spec.ts (the 4 verify-only files — read
      enough to CONFIRM their refs are unrelated/mocked per the <interfaces>
      Open-Q2 classification; do NOT change their assertions)
    - .planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-PATTERNS.md
      (§ "D-11 test files" — `d13-43-load-smoke.spec.ts` "already wants the new
      behavior — Phase 44 makes it real")
  </read_first>
  <action>
    **tests/runtime/d13-43-load-smoke.spec.ts — make it real (D-11):** ADD a
    new `it(...)` arm (do NOT remove the existing direct-4.3.0-runtime arms —
    they cover the beta-vs-stable parse fallback). The new arm proves the GATED
    LOADER routes a 4.3 file:
    ```typescript
    it('D-11: loadSkeleton routes the committed 4.3 leg (skeleton2.json) to the 4.3 runtime (dispatch made real)', () => {
      const FIXTURE = path.resolve('fixtures/SIMPLE_PROJECT_43/skeleton2.json');
      // Pre-flip this threw SpineVersionUnsupportedError; post-flip (Plan 02
      // resolveRuntimeTag) it routes-and-loads. spine "4.3.01" + top-level
      // constraints[] → D-07 token 4.3, D-08 no contradiction → runtime-43.
      const load = loadSkeleton(FIXTURE);
      expect(load).toBeTruthy();
      // Assert it actually parsed via the 4.3 runtime (not rejected): a
      // populated SkeletonData-equivalent. If LoadResult exposes a runtime
      // tag/handle, assert it is the 4.3 runtime; else assert the parsed
      // skeleton structure is non-empty (bones/skins) — proving route+parse,
      // not reject.
    });
    ```
    Import `loadSkeleton` from `../../src/core/loader.js` (match the project's
    existing import style in sibling specs). Use the SAME structural assertions
    the file already uses for the direct arm (bones[]/skins[] non-empty) so the
    "parsed, not rejected" proof is consistent.

    **The 4 verify-only files** (loader.spec.ts, ipc.spec.ts core, main/ipc.spec.ts,
    viewer-asset-feed-ipc.spec.ts): per the Open-Q2 grep classification their
    refs are atlas-not-found / `<4.2`-precondition / mocked-`checkSpineVersion`
    — NONE are 4.3-reject assertions. Make NO assertion changes. Run them to
    CONFIRM no regression from the Plan-02 flip. If any unexpectedly fails,
    STOP and report (it would mean the Open-Q2 classification missed a
    4.3-reject ref — investigate, do not blindly edit).
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/d13-43-load-smoke.spec.ts tests/core/loader.spec.ts tests/core/ipc.spec.ts tests/main/ipc.spec.ts tests/main/viewer-asset-feed-ipc.spec.ts --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "loadSkeleton" tests/runtime/d13-43-load-smoke.spec.ts` returns >=1 (the gated-loader arm was added — previously it drove the runtime directly with 0 loadSkeleton calls)
    - `grep -c "SIMPLE_PROJECT_43/skeleton2.json" tests/runtime/d13-43-load-smoke.spec.ts` returns >=1 (the made-real arm loads the committed 4.3 leg)
    - `npx vitest run tests/runtime/d13-43-load-smoke.spec.ts --reporter=dot` exits 0 (the new gated-loader-routes arm + the existing direct-runtime arms both green)
    - `npx vitest run tests/core/loader.spec.ts tests/core/ipc.spec.ts tests/main/ipc.spec.ts tests/main/viewer-asset-feed-ipc.spec.ts --reporter=dot` exits 0 (the 4 verify-only files have NO regression from the Plan-02 flip — Open-Q2 classification confirmed correct)
    - `git diff --stat tests/core/loader.spec.ts tests/core/ipc.spec.ts tests/main/ipc.spec.ts tests/main/viewer-asset-feed-ipc.spec.ts` shows 0 changed lines (verify-only — no assertion edits)
  </acceptance_criteria>
  <done>
    d13-43-load-smoke.spec.ts now ALSO exercises `loadSkeleton` routing the
    committed 4.3 leg (skeleton2.json) to the 4.3 runtime while keeping its
    existing direct-runtime arms; the 4 unrelated/mocked D-11 files are
    unchanged and green (Open-Q2 classification confirmed); CI is green at
    Phase-44 exit.
  </done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Multi-Runtime Entrypoint verification (vitest + built CJS worker + tsx CLI) + ORCL-03/Phase-45-split dispositions</name>
  <files>(verification + documentation only — no source files modified; produces the 3-entrypoint evidence + the two dispositions recorded in 44-03-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-VALIDATION.md
      (§ Multi-Runtime Entrypoint Matrix + § Manual-Only Verifications — the
      3-entrypoint table is the contract)
    - .planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-PATTERNS.md
      (§ "Multi-runtime entrypoint verification (NOT a vitest spec — a
      verification task)" — the 3 exact commands)
    - src/core/runtime/runtime.ts:174-217 (READ-ONLY — the 3-env split; the
      '4.3' require('../runtime-43.cjs') arm at :204 is production-exercised for
      the FIRST time by this flip — A3)
    - .planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-CONTEXT.md
      (memory `project_phase43_pickruntime_esm_split`: "Phase 44 must re-verify
      lazy-single-copy after the dispatch flip changes the worker's bundled
      graph"; memory `feedback_verify_all_entrypoint_runtimes_of_a_perruntime_seam`)
  </read_first>
  <action>
    Plans 01-02 + Tasks 1-2 landed: the dispatch flip (resolveRuntimeTag), the
    D-04 denylist, the 3-branch error, the committed fixtures + drivers, and
    the full D-11 test reconciliation. This task is the load-bearing integrity
    gate: `pickRuntime`'s `'4.3'` `require('../runtime-43.cjs')` arm is
    exercised in PRODUCTION for the first time by this flip. `npm test` (the
    vitest globalThis-resolver arm) alone green-washes a per-runtime seam gap —
    Phase 43's GAP-43-CLI-SEAM was a 5/5-verified phase with a fully-broken
    CLI. All THREE entrypoints must be verified independently. These are CLI
    commands — RUN them (automation-first; do NOT ask the user to run them):

    1. **vitest arm (globalThis resolver):**
       `npm run test 2>&1 | tail -40`
       Expect: the DISP/D-11 specs green. The ~11 `tests/renderer/*` MixBlend
       IMPORT failures are PRE-EXISTING / Phase-47-owned (memory
       `project_renderer_mixblend_preexisting_failure`) — trust the targeted
       gates + the per-spec Tests line, NOT the raw suite count. Confirm 0 NEW
       failures beyond that documented set.

    2. **built CJS worker arm (ambient require('../runtime-43.cjs') — A3, the
       4.3 production path exercised for the FIRST time):**
       `npm run build 2>&1 | tail -20`
       Then sample the committed 4.3 fixture through the BUILT worker (NOT
       src/): use the project's built-worker harness/entrypoint to sample
       `fixtures/SIMPLE_PROJECT_43/skeleton2.json` via `out/main` (the same
       mechanism Phase 43's GAP-43-PROD-SEAM falsifier used —
       `grep -rl "out/main" tests/ scripts/` to find it; or the production
       sampler-worker spawn-smoke). Expect: the built worker ROUTES the 4.3
       input to runtime-43 and returns a SamplerOutput (no "Cannot find module
       './runtime-43.cjs'", no reject). Confirm lazy-single-copy is preserved
       (the unmatched adapter is not required — re-verify per memory
       `project_phase43_pickruntime_esm_split`: the dispatch flip changed the
       worker's bundled graph).

    3. **CLI tsx/ESM arm (scripts/register-esm-adapter-resolver.ts — the
       GAP-43-CLI-SEAM fix):**
       `npm run cli -- fixtures/SIMPLE_PROJECT_43/skeleton2.json 2>&1 | tail -30`
       Expect: a printed table, exit 0. NOT a `pickRuntime` loud-throw, NOT a
       SpineVersionUnsupportedError reject. (This is the EXACT GAP-43-CLI-SEAM
       signature — a green `npm test` with a broken `npm run cli` — that the
       memory mandates be checked.)

    HARD STOP on any failure: if ANY arm fails this is the runtime-43 analog of
    GAP-43-PROD-SEAM/CLI-SEAM. Diagnose which entrypoint, what the
    require/resolve error is, whether it is the emitted-path/specifier for the
    '4.3' arm. Report the failing command + output + the likely fix locus
    (runtime.ts:204 require literal / electron.vite.config.ts input entries /
    scripts/register-esm-adapter-resolver.ts) and do NOT mark the phase
    verifiable. Fix → re-run all 3 → only proceed when all 3 are green (an
    authentication-gate-style retry: the gate does not soften).

    Then record two dispositions in 44-03-SUMMARY.md (documentation, no code):
    - **ORCL-03 (v1.6 NO-OP):** confirm `ik` is absent in BOTH
      `fixtures/SIMPLE_PROJECT_43/skeleton2.json` and `skeleton2_42.json`
      (parse the JSON / grep the constraint types — they are `transform`/`path`
      only; ORCL-01 is TransformConstraint-only → spine-editor#891-immune → no
      human gate, Phase 42 D-03). Record the disposition + the evidence.
    - **ROADMAP Phase-45 SC#3 split:** explicitly re-state in the SUMMARY that
      Phase 44 owns ONLY the 4.3-input-now-asserts-routing reconciliation +
      preserving the `<4.2`/`≥4.4` throw-cases; Phase 45 retains the
      user-facing copy/docs/drop-zone sweep (UX-01/02) + the final reject-test
      inversion. This is a documented split, NOT a silent descope (memory
      `feedback_replan_can_silently_descope_roadmap_contract`).
  </action>
  <verify>
    <automated>npm run cli -- fixtures/SIMPLE_PROJECT_43/skeleton2.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test 2>&1 | tail -40` shows 0 NEW failures beyond the documented ~11 `tests/renderer/*` MixBlend IMPORT failures (the vitest entrypoint routes 4.3 correctly)
    - `npm run build` exits 0 (the built `out/main` worker compiles with the post-flip bundled graph)
    - Sampling `fixtures/SIMPLE_PROJECT_43/skeleton2.json` through the BUILT `out/main` worker returns a SamplerOutput with NO "Cannot find module './runtime-43.cjs'" and NO SpineVersionUnsupportedError reject (the 4.3 production require arm resolves — the runtime-43 analog of GAP-43-PROD-SEAM is verified absent; lazy-single-copy preserved)
    - `npm run cli -- fixtures/SIMPLE_PROJECT_43/skeleton2.json 2>&1` exits 0 AND prints a table (NOT a `pickRuntime` loud-throw, NOT a reject — the GAP-43-CLI-SEAM signature is verified absent)
    - 44-03-SUMMARY.md records: (i) all 3 entrypoints GREEN with the commands+evidence; (ii) the ORCL-03 v1.6-NO-OP disposition with the `ik`-absent evidence for both JSONs; (iii) the explicit ROADMAP Phase-45 SC#3 split statement
  </acceptance_criteria>
  <done>
    All 3 Multi-Runtime entrypoints (vitest + built out/main CJS worker + tsx
    CLI) independently verified routing a 4.3 input to runtime-43 with
    lazy-single-copy preserved (any failure was a hard stop with a scoped fix
    locus, not a soft pass); the ORCL-03 v1.6-NO-OP disposition and the ROADMAP
    Phase-45 SC#3 split are recorded in 44-03-SUMMARY.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch tag → 3 distinct runtime-resolution mechanisms | `pickRuntime` resolves via vitest globalThis / built CJS `require('../runtime-43.cjs')` / tsx-ESM resolver. The flip changes the tag passed; a subtly-wrong `'4.3'` require path surfaces ONLY on the entrypoint that uses it (the GAP-43-CLI-SEAM class). Not a new boundary — an existing per-runtime seam now exercised on its 4.3 arm for the first time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07 | Tampering (integrity) | the `'4.3'` `require('../runtime-43.cjs')` arm in the BUILT worker / CLI — a 4.3 input mis-resolving in production while vitest is green | mitigate | Task 3 verifies ALL 3 entrypoints independently (vitest + built out/main worker + tsx CLI) — NOT npm test alone. A failure is a HARD STOP with a scoped fix locus, not a soft skip (the GAP-43-CLI-SEAM lesson: a 5/5-verified phase had a fully-broken CLI). Lazy-single-copy re-verified post-flip per memory `project_phase43_pickruntime_esm_split`. |
| T-44-08 | Repudiation | test suite green-washing a residual 4.3-reject (false-green) or dropping a `<4.2`/`≥4.4` throw assertion (silent descope) | mitigate | Task 1 D-11 false-green-guard rule: flip ONLY 4.3 arms to routing; PRESERVE `<4.2`/`≥4.4` throw-cases as explicit assertions (grep-verified the FIXTURE_38 throw asserts survive + the old "re-export as 4.2" assertion is gone). The Phase-45 SC#3 split is documented in the SUMMARY (memory `feedback_replan_can_silently_descope_roadmap_contract`). |

This plan modifies only test files + runs verification — it introduces no new
runtime trust surface. Fixtures are in-repo test data; no network/IPC/filesystem
boundary added.
</threat_model>

<verification>
- `npx vitest run tests/core/loader-version-guard.spec.ts tests/core/errors-version.spec.ts tests/runtime/d13-43-load-smoke.spec.ts tests/core/loader.spec.ts tests/core/ipc.spec.ts tests/main/ipc.spec.ts tests/main/viewer-asset-feed-ipc.spec.ts --reporter=dot` exits 0.
- All 3 Multi-Runtime entrypoints verified GREEN (Task 3): `npm run test`,
  built `out/main` worker on `skeleton2.json`, `npm run cli --
  fixtures/SIMPLE_PROJECT_43/skeleton2.json`.
- No `<4.2`/`≥4.4` throw assertion deleted (grep-confirmed FIXTURE_38 throw +
  the LOCKED <4.2/≥4.4 wordings survive).
- ORCL-03 v1.6-NO-OP disposition + the ROADMAP Phase-45 SC#3 split recorded in
  44-03-SUMMARY.md (not silently descoped).
- Pre-existing noise: the ~11 `tests/renderer/*` MixBlend IMPORT failures are
  Phase-47-owned (memory `project_renderer_mixblend_preexisting_failure`) —
  trust the targeted gates + the per-spec Tests line, not the raw suite count.
</verification>

<success_criteria>
- D-11: all 10 enumerated files reconciled — 4.3 arms assert routing, the
  `<4.2`/`≥4.4` throw-cases preserved as explicit assertions, CI green at
  Phase-44 exit.
- `d13-43-load-smoke.spec.ts` "made real" (a gated-loader-routes arm added,
  direct-runtime arms preserved).
- The 4 verify-only D-11 files unchanged + green (Open-Q2 classification
  confirmed by zero-diff + green).
- All 3 Multi-Runtime entrypoints verified (the runtime-43 production arm + the
  CLI arm proven, not just vitest).
- ORCL-03 (v1.6 no-op, non-IK confirmed) + the ROADMAP Phase-45 SC#3 split
  documented in 44-03-SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/44-loader-dispatch-equivalence-oracle-4-3-fixture-authoring/44-03-SUMMARY.md`
</output>
