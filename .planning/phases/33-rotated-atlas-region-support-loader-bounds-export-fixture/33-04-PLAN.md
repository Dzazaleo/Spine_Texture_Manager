---
phase: 33
plan: 04
type: execute
wave: 3
depends_on: [33-03]
files_modified:
  - src/core/loader.ts
  - tests/core/loader-rotation-accept.spec.ts
  - tests/core/bounds-rotation-aabb.spec.ts
autonomous: true
requirements:
  - ATLAS-01
  - ATLAS-02
must_haves:
  truths:
    - "src/core/loader.ts imports RegionAttachment from @esotericsoftware/spine-core"
    - "src/core/loader.ts contains a post-readSkeletonData walk over skeletonData.skins that, for each rotated RegionAttachment, directly writes the 8 canonical-corner floats into attachment.offset[0..7]"
    - "loadSkeleton(fixtures/spine_rotated/EXPORT/skeleton.json) returns successfully"
    - "loadSkeleton result has at least one atlasSources entry with rotated=true"
    - "attachmentWorldAABB on a synthetic rotated RegionAttachment (canonical 100x500, packed 500x100) produces width/height equal (within 1e-6 tolerance) to attachmentWorldAABB on an unrotated reference, across 16 bone-state x attachment.rotation cases"
    - "tests/core/loader-rotation-accept.spec.ts is no longer describe.skip — active passing block"
    - "tests/core/bounds-rotation-aabb.spec.ts is no longer describe.skip — active passing block, all 16 it() cases pass"
    - "npm test exits 0"
    - "npx tsc --noEmit exits 0"
    - "Layer 3 invariant preserved: src/core/loader.ts still does not import DOM, Electron, or sharp"
  artifacts:
    - path: "src/core/loader.ts"
      provides: "Loader with D-01 walk that pre-cooks canonical offsets for rotated RegionAttachments"
      contains: "RegionAttachment"
    - path: "tests/core/loader-rotation-accept.spec.ts"
      provides: "ATLAS-01 fixture-driven smoke test"
      contains: "loadSkeleton"
    - path: "tests/core/bounds-rotation-aabb.spec.ts"
      provides: "ATLAS-02 16-case bone-rotation matrix"
      contains: "attachmentWorldAABB"
  key_links:
    - from: "src/core/loader.ts D-01 walk"
      to: "src/core/bounds.ts attachmentWorldAABB"
      via: "pre-cooked attachment.offset[] consumed by computeWorldVertices on every sampler tick"
      pattern: "attachment.offset"
    - from: "tests/core/bounds-rotation-aabb.spec.ts"
      to: "src/core/bounds.ts"
      via: "attachmentWorldAABB import + assertion across 16 cases"
      pattern: "attachmentWorldAABB"
    - from: "tests/core/loader-rotation-accept.spec.ts"
      to: "fixtures/spine_rotated/EXPORT/skeleton.json"
      via: "loadSkeleton(ROTATED_FIXTURE) call"
      pattern: "loadSkeleton"
---

<objective>
Implement the D-01 loader walk in src/core/loader.ts AND fill in the ATLAS-01 + ATLAS-02 test bodies (un-skipping the Wave 1 scaffolds). After this plan, ATLAS-01 (loader accepts rotated atlases) and ATLAS-02 (world AABB matches unrotated reference across the bone-rotation matrix) are both met.

D-01 is the mathematical core of Phase 33: bypasses spine-core's RegionAttachment.updateRegion() packed-dim layout for rotated regions only by directly writing the 8 canonical-corner floats into attachment.offset. The bounds.ts hot loop reads pre-cooked offsets verbatim — no per-tick branching, no math change downstream.

Critical research correction: Use the region.width <-> region.height SWAP form (NOT the originalWidth/Height substitution sketched in CONTEXT). RESEARCH §"D-01 Formula Derivation" proved the SWAP preserves SW trim semantics; the substitution variant breaks SW + rotation. The recommended implementation form is the direct-offset-write variant (no region-mutation needed) per RESEARCH §"D-01 Loader walk (recommended direct-offset variant)" lines 461-512.

Purpose: ATLAS-01 + ATLAS-02 acceptance criteria. After this plan, fixture loads cleanly + AABBs are correct.

Output: D-01 walk inserted into loader.ts; both new test specs un-skipped + passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-CONTEXT.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-PATTERNS.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-03-SUMMARY.md
@CLAUDE.md
@src/core/loader.ts
@src/core/bounds.ts
@tests/core/loader-rotation-accept.spec.ts
@tests/core/bounds-rotation-aabb.spec.ts
@tests/core/loader-strip-whitespace.spec.ts
@tests/core/bounds.spec.ts

<interfaces>
D-01 walk verbatim form (RESEARCH §"Code Examples → D-01 Loader walk" lines 461-512).
Insert AFTER readSkeletonData returns. The post-Plan-03 loader.ts no longer has
the throw block; the new walk replaces that vacant region.

D-01 direct-offset-write variant (recommended — touches no region state):

```
// Phase 33 — D-01: per-RegionAttachment offset override for rotated regions.
// Bypasses spine-core RegionAttachment.updateRegion()'s packed-dim layout
// (which produces wrong world-quad geometry for rotate:true regions —
// verified at node_modules/@esotericsoftware/spine-core/dist/attachments/RegionAttachment.js:82-87).
// Direct-write variant — touches no region state, so MeshAttachment.updateRegion
// downstream still sees PACKED dims as before (D-02 untouched).
//
// Walks ALL skins (memory: project_sampler_visibility_invariant — sampler
// measures all skin-declared attachments, not just default skin).
//
// ASSUMES region.degrees === 90 only. Spine editor never emits 180/270 in
// 4.2; if a future export surfaces non-90° rotation, this path silently
// misbehaves (deferred per CONTEXT Out-of-Scope).
//
// Direction VERIFIED EMPIRICALLY in 33-RESEARCH.md (CONTEXT D-03 hypothesis
// of -90 was falsified): the SWAP form (region.width <-> region.height substituted
// into spine-core's offset formula) preserves SW trim semantics, while the
// originalWidth/Height substitution variant breaks SW+rotation.
if (!isAtlasLess) {
  for (const skin of skeletonData.skins) {
    for (const slotEntry of skin.attachments) {
      for (const [, attachment] of slotEntry) {
        if (!(attachment instanceof RegionAttachment)) continue;
        const region = attachment.region;
        if (!region || region.degrees === 0) continue;

        const w = attachment.width;
        const h = attachment.height;
        const x = attachment.x;
        const y = attachment.y;
        const scaleX = attachment.scaleX;
        const scaleY = attachment.scaleY;
        const rotation = attachment.rotation;

        const radians = (rotation * Math.PI) / 180;
        const cos = Math.cos(radians);
        const sin = Math.sin(radians);

        // Canonical (post-swap) regionScaleX/Y. originalWidth/originalHeight
        // are canonical per libgdx convention (verified: TextureAtlas.js:87-93).
        const rsX = (w / region.originalWidth) * scaleX;
        const rsY = (h / region.originalHeight) * scaleY;

        const localX = (-w / 2) * scaleX + region.offsetX * rsX;
        const localY = (-h / 2) * scaleY + region.offsetY * rsY;

        // SWAP — verified at 33-RESEARCH.md §"D-01 Formula Derivation" probe:
        //   localX2 uses region.height (PACKED H = post-rot canonical W);
        //   localY2 uses region.width  (PACKED W = post-rot canonical H).
        const localX2 = localX + region.height * rsX;
        const localY2 = localY + region.width * rsY;

        const lXc = localX * cos + x;
        const lXs = localX * sin;
        const lYc = localY * cos + y;
        const lYs = localY * sin;
        const lX2c = localX2 * cos + x;
        const lX2s = localX2 * sin;
        const lY2c = localY2 * cos + y;
        const lY2s = localY2 * sin;

        const off = attachment.offset;
        off[0] = lXc - lYs;   off[1] = lYc + lXs;
        off[2] = lXc - lY2s;  off[3] = lY2c + lXs;
        off[4] = lX2c - lY2s; off[5] = lY2c + lX2s;
        off[6] = lX2c - lYs;  off[7] = lYc + lX2s;
        // Note: do NOT touch attachment.uvs — spine-core wrote correct
        // rotated-UVs at parse time (RegionAttachment.js:109-117).
      }
    }
  }
}
```

Spine-core types referenced (already available via the existing @esotericsoftware/spine-core import in loader.ts — Plan 03 confirmed it remains installed):

```
import { RegionAttachment } from '@esotericsoftware/spine-core';
// RegionAttachment exposes:
//   width, height, x, y, scaleX, scaleY, rotation (degrees),
//   region: TextureAtlasRegion | null,
//   offset: number[] (length 8).
```

Bounds verification path (no edit — pre-cooked offsets flow through unchanged):

```
// src/core/bounds.ts:attachmentWorldAABB
//   --> att.computeWorldVertices(slot, wv, 0, 2)
//      uses pre-cooked attachment.offset[] --> correct AABB
```

Test analog patterns (per 33-PATTERNS.md):
- tests/core/loader-rotation-accept.spec.ts mirrors tests/core/loader-strip-whitespace.spec.ts:24-61
- tests/core/bounds-rotation-aabb.spec.ts mirrors tests/core/bounds.spec.ts synthetic-AABB pattern + RESEARCH §"Bone-Rotation Matrix Specifics" full 16-case matrix
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add D-01 walk to src/core/loader.ts</name>
  <files>src/core/loader.ts</files>
  <read_first>
    src/core/loader.ts (read lines 30-50 for current import state — Plan 03 left RegionAttachment NOT imported; this task adds it),
    src/core/loader.ts (read lines 500-560 for the post-Plan-03 region around the deleted throw block — D-01 walk goes after readSkeletonData at ~line 546),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md (§"Code Examples → D-01 Loader walk (recommended direct-offset variant)" lines 461-512 — verbatim form to insert),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md (§"D-01 Formula Derivation" lines 102-200 — proof that SWAP is correct vs originalWidth/Height substitution),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-PATTERNS.md (§"src/core/loader.ts - D-01 walk" — adapt-vs-mirror notes),
    node_modules/@esotericsoftware/spine-core/dist/attachments/RegionAttachment.js (read lines 60-130 — confirms updateRegion's packed-dim layout that D-01 bypasses)
  </read_first>
  <behavior>
    - Test 1 (covered by Task 2): loadSkeleton(fixtures/spine_rotated/EXPORT/skeleton.json) resolves without throwing
    - Test 2 (covered by Task 2): result has at least one atlasSources entry with rotated=true
    - Test 3 (covered by Task 3): attachmentWorldAABB on rotated synthetic RegionAttachment with canonical 100x500 / packed 500x100 returns identical width/height to unrotated reference at bone identity
    - Test 4 (covered by Task 3): same equality holds at bone rotations 0/45/90/180/-45/scale combinations
    - Test 5 (covered by Task 3): same equality holds at attachment.rotation = 0 AND 30
  </behavior>
  <action>
Step 1 — Add RegionAttachment to the spine-core import block (loader.ts ~lines 32-39, currently after Plan 03):

Read loader.ts lines 30-50, locate the existing braced import from @esotericsoftware/spine-core. Add RegionAttachment to the imported names. Post-edit form:

```
import {
  AtlasAttachmentLoader,
  RegionAttachment,
  SkeletonJson,
  TextureAtlas,
  Texture,
  TextureFilter,
  TextureWrap,
} from '@esotericsoftware/spine-core';
```

(Alphabetical order is conventional; if the existing block uses a different order, append RegionAttachment at the position that matches the existing convention.)

Step 2 — Insert the D-01 walk after readSkeletonData (currently around line 546 in the post-Plan-03 file):

Locate the line:
```
const skeletonData = skeletonJson.readSkeletonData(parsedJson);
```
and insert the D-01 walk immediately after it. Use the verbatim form from the <interfaces> block above (RESEARCH lines 461-512). The walk:
- Iterates skeletonData.skins (NOT a Skeleton instance — none has been constructed yet)
- For each skin, iterates skin.attachments (the spine-core data-shape Map)
- Filters to RegionAttachment instances only (skips MeshAttachment, BoundingBox, Path, Clipping per D-02 invariant)
- Filters to rotated regions only (region.degrees !== 0 — guards against 180/270 silently per A1 assumption)
- Computes 8 canonical-corner floats from the attachment width/height/x/y/scaleX/scaleY/rotation and the region originalWidth/originalHeight/offsetX/offsetY PLUS the SWAP form region.height for localX2, region.width for localY2
- Writes to attachment.offset[0..7] directly, leaving attachment.uvs untouched
- Wrapped in if (!isAtlasLess) { ... } so atlas-less mode is not entered

Step 3 — Confirm Layer 3 invariant:

The D-01 walk is pure TypeScript object manipulation. No new imports beyond RegionAttachment. Run tests/arch.spec.ts (already exists, line 148-178 enforces Layer 3) to confirm:

```
npm test -- tests/arch.spec.ts
```

Step 4 — Confirm full suite still green:

Run npm test and npx tsc --noEmit to confirm no regression. Note: tests/core/loader-rotation-accept.spec.ts and tests/core/bounds-rotation-aabb.spec.ts are still describe.skip at this point (Tasks 2 + 3 unskip them).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- tests/arch.spec.ts &amp;&amp; grep -q "Phase 33 . D-01" src/core/loader.ts &amp;&amp; grep -q "RegionAttachment" src/core/loader.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "import" src/core/loader.ts and grep -q "RegionAttachment" src/core/loader.ts both exit 0
    - grep -c "off\[0\] = lXc - lYs" src/core/loader.ts outputs at least 1 (the offset-write block landed)
    - grep -q "Phase 33" src/core/loader.ts exits 0 (header comment landed)
    - grep -q "if (!isAtlasLess)" src/core/loader.ts exits 0 (walk wrapped in atlas-less guard)
    - grep -q "instanceof RegionAttachment" src/core/loader.ts exits 0 (filter present)
    - grep -q "region.degrees === 0" src/core/loader.ts exits 0 (rotation guard present)
    - grep -q "region.height \* rsX" src/core/loader.ts exits 0 (SWAP form: localX2 uses region.height — verified at 33-RESEARCH.md)
    - grep -q "region.width \* rsY" src/core/loader.ts exits 0 (SWAP form: localY2 uses region.width)
    - npx tsc --noEmit exits 0
    - npm test -- tests/arch.spec.ts exits 0 (Layer 3 invariant preserved)
    - npm test exits 0 (no regression in existing suite — new specs still describe.skip)
    - The D-01 walk runs only in atlas-source mode (skipped for atlas-less per project_strict_loadermode_separation)
  </acceptance_criteria>
  <done>D-01 walk inserted; RegionAttachment imported; arch tests green; full suite green; new specs still skipped (Tasks 2 + 3 will unskip them).</done>
</task>

<task type="auto">
  <name>Task 2: Fill in tests/core/loader-rotation-accept.spec.ts (ATLAS-01)</name>
  <files>tests/core/loader-rotation-accept.spec.ts</files>
  <read_first>
    tests/core/loader-rotation-accept.spec.ts (current Wave 1 RED scaffold from Plan 02 — describe.skip + it.todo placeholders),
    tests/core/loader-strip-whitespace.spec.ts (full file — analog for fixture-driven loadSkeleton + atlasSources Map assert pattern),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-PATTERNS.md (§"tests/core/loader-rotation-accept.spec.ts (CREATE — ATLAS-01)" — verbatim adapt sketch),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-01-SUMMARY.md (Fixture Shape table — region name + rotated count for the lock assertion),
    src/core/loader.ts (post-Task-1 with D-01 walk applied — read for return-type shape of LoadResult)
  </read_first>
  <action>
Replace the contents of tests/core/loader-rotation-accept.spec.ts with the active test (un-skip describe.skip, replace it.todo with real assertions). Use the analog from tests/core/loader-strip-whitespace.spec.ts:24-61 as the structural pattern.

Use this exact body, substituting the rotated-region count from 33-01-SUMMARY.md's Fixture Shape table for {EXPECTED_ROTATED_COUNT}:

```
import { describe, expect, it } from 'vitest';
import * as path from 'node:path';
import { loadSkeleton } from '../../src/core/loader.js';

const ROTATED_FIXTURE = path.resolve('fixtures/spine_rotated/EXPORT/skeleton.json');

describe('loader — accepts rotated atlas regions (ATLAS-01)', () => {
  it('loadSkeleton resolves without throwing on rotate:true regions', () => {
    const r = loadSkeleton(ROTATED_FIXTURE);
    expect(r.skeletonData).toBeDefined();
    expect(r.atlas).toBeDefined();
    expect(r.atlasPath).not.toBeNull();
  });

  it('at least one atlasSources entry has rotated=true (fixture has a packer-rotated region)', () => {
    const r = loadSkeleton(ROTATED_FIXTURE);
    const rotatedRegions = [...r.atlasSources.entries()].filter(([, s]) => s.rotated);
    expect(
      rotatedRegions.length,
      'fixture must contain at least one rotated region (packer threshold met)',
    ).toBeGreaterThanOrEqual(1);
    // Lock the exact count to the fixture shape (per CONTEXT §Claude's Discretion).
    // Reference: 33-01-SUMMARY.md "Fixture Shape" table.
    expect(rotatedRegions.length).toBe({EXPECTED_ROTATED_COUNT});
  });
});
```

If the Fixture Shape table records a single rotated region (most common case for the recommended 100x500 source rectangle), substitute `{EXPECTED_ROTATED_COUNT}` with `1`. If the user's actual fixture has more, substitute the actual count.

After the substitution, run:
- npm test -- tests/core/loader-rotation-accept.spec.ts (must exit 0)

If the test fails because the count assertion is wrong, the fixture shape may have multiple rotated regions; adjust the literal to match the SUMMARY's count.
  </action>
  <verify>
    <automated>npm test -- tests/core/loader-rotation-accept.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "describe.skip" tests/core/loader-rotation-accept.spec.ts exits 1 (no longer skipped)
    - grep -q "describe('loader — accepts rotated atlas regions (ATLAS-01)'" tests/core/loader-rotation-accept.spec.ts exits 0
    - grep -q "import { loadSkeleton }" tests/core/loader-rotation-accept.spec.ts exits 0
    - grep -q "ROTATED_FIXTURE" tests/core/loader-rotation-accept.spec.ts exits 0
    - grep -q "rotatedRegions.length" tests/core/loader-rotation-accept.spec.ts exits 0
    - grep -E "toBe\\([0-9]+\\)" tests/core/loader-rotation-accept.spec.ts | grep -q "toBe" (the exact-count lock is present, not just toBeGreaterThanOrEqual)
    - npm test -- tests/core/loader-rotation-accept.spec.ts exits 0
    - npm test exits 0 (full suite still green)
  </acceptance_criteria>
  <done>ATLAS-01 test passes against the fixture; describe.skip removed; rotated-count lock matches fixture shape.</done>
</task>

<task type="auto">
  <name>Task 3: Fill in tests/core/bounds-rotation-aabb.spec.ts (ATLAS-02 16-case matrix)</name>
  <files>tests/core/bounds-rotation-aabb.spec.ts</files>
  <read_first>
    tests/core/bounds-rotation-aabb.spec.ts (current Wave 1 RED scaffold — describe.skip + 16 it.todo placeholders),
    tests/core/bounds.spec.ts (analog file — read full contents; the synthetic-attachment AABB pattern is the structural template),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md (§"Bone-Rotation Matrix Specifics (ATLAS-02)" lines 700-735 — the verbatim 8-case bone matrix definition),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md (§"Code Examples → ATLAS-02 synthetic test pattern" lines 575-621 — verbatim code template),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-PATTERNS.md (§"tests/core/bounds-rotation-aabb.spec.ts (CREATE — ATLAS-02 16-case matrix)" — full pattern),
    src/core/bounds.ts (read attachmentWorldAABB signature — first 100 lines),
    node_modules/@esotericsoftware/spine-core/dist/attachments/RegionAttachment.d.ts (read TypeScript declarations for RegionAttachment class signature),
    node_modules/@esotericsoftware/spine-core/dist/TextureAtlas.d.ts (read TextureAtlasRegion type signature)
  </read_first>
  <action>
Replace tests/core/bounds-rotation-aabb.spec.ts with an active 16-case matrix test. Use RESEARCH lines 575-621 + 700-735 as the verbatim template.

The test matrix (16 cases = 8 bone-states x 2 attachment-rotations) is:

Bone states (8):
1. identity:           { a: 1, b: 0, c: 0, d: 1 }
2. rot 45:             { a: cos(pi/4), b: sin(pi/4), c: -sin(pi/4), d: cos(pi/4) }
3. rot 90:             { a: 0, b: 1, c: -1, d: 0 }
4. rot 180:            { a: -1, b: 0, c: 0, d: -1 }
5. rot -45:            { a: cos(-pi/4), b: sin(-pi/4), c: -sin(-pi/4), d: cos(-pi/4) }
6. scale 2x0.5:        { a: 2, b: 0, c: 0, d: 0.5 }
7. scale 0.5x2:        { a: 0.5, b: 0, c: 0, d: 2 }
8. rot 30 + scale 2x0.5: { a: 2*cos(pi/6), b: 2*sin(pi/6), c: -0.5*sin(pi/6), d: 0.5*cos(pi/6) }

Attachment rotations (2): 0, 30

Tolerance: AABB width and height equality with absolute tolerance < 1e-6.

Verbatim test body:

```
import { describe, expect, it } from 'vitest';
import {
  RegionAttachment,
  TextureAtlasRegion,
  Skeleton,
  SkeletonData,
  Slot,
  SlotData,
  Bone,
  BoneData,
  Skin,
} from '@esotericsoftware/spine-core';
import { attachmentWorldAABB } from '../../src/core/bounds.js';

// Phase 33 — ATLAS-02: 16-case bone-rotation x attachment.rotation matrix.
// Synthetic — no fixture dependency. Bone matrix (a, b, c, d, x=0, y=0)
// is set directly on a Slot's bone after construction. Each iteration builds
// a rotated attachment (canonical 100x500, packed 500x100, region.degrees=90)
// and an unrotated reference (canonical 100x500, region.degrees=0), applies
// the D-01 cooking inline (test-local copy of the loader formula), then asserts
// AABB width/height equality.

// Helper: build a TextureAtlasRegion-shaped object with packed/canonical dims.
function buildRegion(opts: {
  w: number;
  h: number;
  origW: number;
  origH: number;
  degrees: 0 | 90;
}): TextureAtlasRegion {
  const r = new TextureAtlasRegion();
  r.width = opts.w;
  r.height = opts.h;
  r.originalWidth = opts.origW;
  r.originalHeight = opts.origH;
  r.offsetX = 0;
  r.offsetY = 0;
  r.degrees = opts.degrees;
  return r;
}

// Helper: build a RegionAttachment with the given dims/rotation/region.
function buildRegionAttachment(opts: {
  width: number;
  height: number;
  x: number;
  y: number;
  scaleX: number;
  scaleY: number;
  rotation: number;
  region: TextureAtlasRegion;
}): RegionAttachment {
  const att = new RegionAttachment('test');
  att.width = opts.width;
  att.height = opts.height;
  att.x = opts.x;
  att.y = opts.y;
  att.scaleX = opts.scaleX;
  att.scaleY = opts.scaleY;
  att.rotation = opts.rotation;
  att.region = opts.region;
  att.updateRegion(); // populate offset[] and uvs[]
  return att;
}

// Apply the D-01 direct-offset-write cooking (test-local copy of the loader formula
// at src/core/loader.ts §"Phase 33 — D-01"). Mutates attachment.offset[] in place.
function cookD01Offsets(attachment: RegionAttachment): void {
  const region = attachment.region;
  if (!region || region.degrees === 0) return;
  const w = attachment.width;
  const h = attachment.height;
  const x = attachment.x;
  const y = attachment.y;
  const scaleX = attachment.scaleX;
  const scaleY = attachment.scaleY;
  const rotation = attachment.rotation;
  const radians = (rotation * Math.PI) / 180;
  const cos = Math.cos(radians);
  const sin = Math.sin(radians);
  const rsX = (w / region.originalWidth) * scaleX;
  const rsY = (h / region.originalHeight) * scaleY;
  const localX = (-w / 2) * scaleX + region.offsetX * rsX;
  const localY = (-h / 2) * scaleY + region.offsetY * rsY;
  const localX2 = localX + region.height * rsX; // SWAP
  const localY2 = localY + region.width * rsY;  // SWAP
  const lXc = localX * cos + x;
  const lXs = localX * sin;
  const lYc = localY * cos + y;
  const lYs = localY * sin;
  const lX2c = localX2 * cos + x;
  const lX2s = localX2 * sin;
  const lY2c = localY2 * cos + y;
  const lY2s = localY2 * sin;
  const off = attachment.offset;
  off[0] = lXc - lYs;   off[1] = lYc + lXs;
  off[2] = lXc - lY2s;  off[3] = lY2c + lXs;
  off[4] = lX2c - lY2s; off[5] = lY2c + lX2s;
  off[6] = lX2c - lYs;  off[7] = lYc + lX2s;
}

// Helper: build a minimal Slot whose bone has the given world-matrix (a, b, c, d, x=0, y=0).
function buildSlotWithBoneMatrix(opts: {
  a: number;
  b: number;
  c: number;
  d: number;
}): Slot {
  const skelData = new SkeletonData();
  const boneData = new BoneData(0, 'root', null);
  skelData.bones.push(boneData);
  const slotData = new SlotData(0, 'slot', boneData);
  skelData.slots.push(slotData);
  skelData.defaultSkin = new Skin('default');
  const skeleton = new Skeleton(skelData);
  const bone = skeleton.bones[0];
  bone.a = opts.a;
  bone.b = opts.b;
  bone.c = opts.c;
  bone.d = opts.d;
  bone.worldX = 0;
  bone.worldY = 0;
  return skeleton.slots[0];
}

const c45 = Math.cos(Math.PI / 4);
const s45 = Math.sin(Math.PI / 4);
const c30 = Math.cos(Math.PI / 6);
const s30 = Math.sin(Math.PI / 6);

const matrixCases: { name: string; a: number; b: number; c: number; d: number }[] = [
  { name: 'identity',                  a: 1,    b: 0,    c: 0,     d: 1    },
  { name: 'rot 45deg',                 a: c45,  b: s45,  c: -s45,  d: c45  },
  { name: 'rot 90deg',                 a: 0,    b: 1,    c: -1,    d: 0    },
  { name: 'rot 180deg',                a: -1,   b: 0,    c: 0,     d: -1   },
  { name: 'rot -45deg',                a: c45,  b: -s45, c: s45,   d: c45  },
  { name: 'scale 2x0.5',               a: 2,    b: 0,    c: 0,     d: 0.5  },
  { name: 'scale 0.5x2',               a: 0.5,  b: 0,    c: 0,     d: 2    },
  { name: 'rot 30deg + scale 2x0.5',   a: 2*c30, b: 2*s30, c: -0.5*s30, d: 0.5*c30 },
];

const attRotations = [0, 30];

describe('attachmentWorldAABB — rotated RegionAttachment matrix (ATLAS-02)', () => {
  for (const tc of matrixCases) {
    for (const attRot of attRotations) {
      it(`bone=${tc.name}, attRot=${attRot}deg: rotated AABB width/height matches unrotated reference`, () => {
        const rotatedAtt = buildRegionAttachment({
          width: 100, height: 500, x: 0, y: 0, scaleX: 1, scaleY: 1, rotation: attRot,
          region: buildRegion({ w: 500, h: 100, origW: 100, origH: 500, degrees: 90 }),
        });
        cookD01Offsets(rotatedAtt);

        const unrotatedRef = buildRegionAttachment({
          width: 100, height: 500, x: 0, y: 0, scaleX: 1, scaleY: 1, rotation: attRot,
          region: buildRegion({ w: 100, h: 500, origW: 100, origH: 500, degrees: 0 }),
        });

        const slotRot = buildSlotWithBoneMatrix(tc);
        const slotUnrot = buildSlotWithBoneMatrix(tc);

        const aabbRot = attachmentWorldAABB(slotRot, rotatedAtt);
        const aabbUnrot = attachmentWorldAABB(slotUnrot, unrotatedRef);

        expect(aabbRot, 'rotated AABB must not be null').not.toBeNull();
        expect(aabbUnrot, 'unrotated AABB must not be null').not.toBeNull();

        const wRot = aabbRot!.maxX - aabbRot!.minX;
        const hRot = aabbRot!.maxY - aabbRot!.minY;
        const wUnrot = aabbUnrot!.maxX - aabbUnrot!.minX;
        const hUnrot = aabbUnrot!.maxY - aabbUnrot!.minY;

        expect(Math.abs(wRot - wUnrot), `width mismatch at bone=${tc.name}, attRot=${attRot}`).toBeLessThan(1e-6);
        expect(Math.abs(hRot - hUnrot), `height mismatch at bone=${tc.name}, attRot=${attRot}`).toBeLessThan(1e-6);
      });
    }
  }
});
```

After write:
- Run npm test -- tests/core/bounds-rotation-aabb.spec.ts — all 16 it() cases must pass.
- If any case fails, the D-01 walk in loader.ts has a bug — DO NOT relax the tolerance; instead investigate and fix the loader walk.

Note on helpers: if buildSlotWithBoneMatrix's SkeletonData/BoneData/Skin construction needs different constructor args (spine-core's TypeScript declarations may require positional vs object init), adjust based on the .d.ts files read in <read_first>. The matrix iteration logic does NOT change.

Note: the cookD01Offsets test-local helper duplicates the loader formula. This is intentional — it isolates the test from any future loader-walk refactor that might extract the formula into an exported helper. If the developer prefers, they can export a cookRotatedRegionOffset(att) function from src/core/loader.ts and reuse it here; that is acceptable refactoring during this task.
  </action>
  <verify>
    <automated>npm test -- tests/core/bounds-rotation-aabb.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "describe.skip" tests/core/bounds-rotation-aabb.spec.ts exits 1 (no longer skipped)
    - grep -q "describe('attachmentWorldAABB . rotated RegionAttachment matrix" tests/core/bounds-rotation-aabb.spec.ts exits 0
    - grep -c "matrixCases" tests/core/bounds-rotation-aabb.spec.ts outputs at least 2 (declared + iterated)
    - grep -c "attRotations" tests/core/bounds-rotation-aabb.spec.ts outputs at least 2 (declared + iterated)
    - grep -c "buildRegionAttachment" tests/core/bounds-rotation-aabb.spec.ts outputs at least 3 (helper + 2 calls per iteration)
    - grep -q "cookD01Offsets" tests/core/bounds-rotation-aabb.spec.ts exits 0 (D-01 cooking helper present)
    - grep -q "region.height \* rsX" tests/core/bounds-rotation-aabb.spec.ts exits 0 (SWAP form replicated in test helper)
    - grep -q "region.width \* rsY" tests/core/bounds-rotation-aabb.spec.ts exits 0
    - grep -q "toBeLessThan(1e-6)" tests/core/bounds-rotation-aabb.spec.ts exits 0 (tolerance match)
    - npm test -- tests/core/bounds-rotation-aabb.spec.ts exits 0 (all 16 cases pass)
    - npm test exits 0 (full suite green)
  </acceptance_criteria>
  <done>16 it() cases all pass; describe.skip removed; D-01 cooking replicated in test helper.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit exits 0
- npm test exits 0
- npm test -- tests/core/loader-rotation-accept.spec.ts exits 0 (ATLAS-01 met)
- npm test -- tests/core/bounds-rotation-aabb.spec.ts exits 0 (ATLAS-02 met)
- src/core/loader.ts contains the D-01 walk in the post-readSkeletonData region
- Layer 3 invariant preserved (npm test -- tests/arch.spec.ts exits 0)
- Loading fixtures/spine_rotated/EXPORT/skeleton.json no longer throws AND produces correct AABBs
</verification>

<success_criteria>
1. D-01 walk inserted into src/core/loader.ts after readSkeletonData
2. RegionAttachment imported in loader.ts
3. Both new spec files un-skipped + passing (2 it cases in loader-rotation-accept, 16 it cases in bounds-rotation-aabb)
4. npm test full suite exits 0
5. Layer 3 invariant preserved
6. ATLAS-01 acceptance text met (loader accepts rotated atlases, rotated regions propagate, atlas-less unaffected)
7. ATLAS-02 acceptance text met (attachmentWorldAABB world AABB matches runtime at identity scale, verified across the 16-case matrix)
</success_criteria>

<output>
After completion, create .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-04-SUMMARY.md documenting:
- Confirmation D-01 walk landed in src/core/loader.ts (line range, post-readSkeletonData)
- Output of npm test -- tests/core/loader-rotation-accept.spec.ts and npm test -- tests/core/bounds-rotation-aabb.spec.ts (both green)
- Confirmation Layer 3 tests (tests/arch.spec.ts) pass
- Note for Plan 05: src/core/loader.ts now correct; Plan 05 implements the image-worker side (sharp.rotate(+90)) for ATLAS-03's PNG-output-dim requirement.
</output>
</content>
