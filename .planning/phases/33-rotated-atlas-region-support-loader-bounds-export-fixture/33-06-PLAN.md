---
phase: 33
plan: 06
type: execute
wave: 4
depends_on: [33-04, 33-05]
files_modified:
  - .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-06-SUMMARY.md
autonomous: false
requirements:
  - ATLAS-01
  - ATLAS-02
  - ATLAS-03
  - ATLAS-04
must_haves:
  truths:
    - "src/core/export.ts:325 outW formula is documented as canonical-relative AND no math change was needed in Phase 33 (cascade flows through D-01)"
    - "tests/core/no-stale-rotation-error.spec.ts is GREEN (was activated in Plan 03; this plan re-confirms post-Plans-04+05)"
    - "All 5 new test specs from Wave 1 are now ACTIVE (no describe.skip, no it.todo placeholders) and passing"
    - "User confirms via HUMAN-UAT that loading fixtures/spine_rotated/EXPORT/skeleton.json populates the Global + Animation Breakdown panels with no error banner"
    - "User confirms via HUMAN-UAT that running Optimize Assets on the rotated fixture produces output PNGs with canonical (unrotated) dimensions matching the source rectangle"
    - "ATLAS-01..04 are all met (4 REQs covered across Plans 03/04/05; Plan 06 is the verification roll-up)"
    - "npm test exits 0; npx tsc --noEmit exits 0 (final full-suite gate)"
  artifacts:
    - path: ".planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-06-SUMMARY.md"
      provides: "Final phase verification roll-up + HUMAN-UAT sign-off"
  key_links:
    - from: "src/core/export.ts:325"
      to: "src/core/loader.ts D-01 walk + src/main/image-worker.ts sharp.rotate(+90)"
      via: "canonical W/H flows from D-01-cooked offsets through buildExportPlan to image-worker without math change"
      pattern: "canonicalW"
---

<objective>
Final verification roll-up for Phase 33. Two tasks:

1. **Task 1 (auto):** Source-read confirmation that `src/core/export.ts:325` `outW = ceil((canonicalW ?? sourceW) × effScale)` is canonical-relative AND no math change was needed in Phase 33 (the cascade flows through D-01 from Plan 04). Document this finding in the SUMMARY as a no-op verification (per phase guidance "If math change needed, add as a task here. Otherwise document the verification as a no-op task.").

2. **Task 2 (checkpoint:human-verify):** End-to-end HUMAN-UAT — user drops the `fixtures/spine_rotated/EXPORT/skeleton.json` fixture into a dev build, verifies panels populate, then clicks Optimize Assets and inspects output PNG dimensions. This is the ATLAS-01..04 live confidence check that automated tests cannot replicate (the panels render in Electron, not jsdom).

Purpose: Close Phase 33 with full verification — automated tests + ExportPlan canonical-flow confirmation + live UAT. ATLAS-01..04 acceptance text met end-to-end.

Output: 33-06-SUMMARY.md with verification + UAT sign-off; phase ready for `/gsd-verify-work 33`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-CONTEXT.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-PATTERNS.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-VALIDATION.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-01-SUMMARY.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-04-SUMMARY.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-05-SUMMARY.md
@CLAUDE.md
@src/core/export.ts

<interfaces>
ExportPlan canonical-dim verification target (src/core/export.ts:325):
The formula `outW = ceil((canonicalW ?? sourceW) * effScale)` is canonical-relative
because canonicalW is the unrotated source W (set by analyzer.ts from the loader's
post-D-01 atlasSources.w field, which is already the unrotated dim per
loader.ts:699-744). For rotated regions:
- D-01 (Plan 04) cooks canonical-corner offsets in attachment.offset[]
- bounds.ts:attachmentWorldAABB reads pre-cooked offsets unchanged
- analyzer.ts feeds peakScale into the SkeletonSummary; canonicalW = atlasSources.w
- buildExportPlan emits outW = ceil(canonicalW * effScale) = ceil(unrotated_W * effScale)

No math change was needed in Phase 33's export.ts. The cascade flows through D-01.

Confirmation evidence (from completed plans):
- Plan 04 SUMMARY: D-01 walk landed; bounds-rotation-aabb.spec.ts 16/16 pass
- Plan 05 SUMMARY: image-worker rotation landed; export-rotation-dims.spec.ts asserts outW=100, outH=500 for synthetic canonicalW=100, canonicalH=500 row → ExportPlan canonical-flow confirmed by test

UAT scenarios from 33-VALIDATION.md "Manual-Only Verifications":
| Behavior | Requirement | Test Instructions |
| Real rotated atlas loads end-to-end | ATLAS-01 | Drop fixtures/spine_rotated/EXPORT/skeleton.json into a dev build; verify no error banner, panels populate with at least one rotated region present |
| Optimize Assets produces canonical-dim PNGs | ATLAS-03 | After loading rotated fixture, click Optimize → choose folder → inspect produced PNGs; verify per-region dims match canonical (unrotated) source W×H |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source-read confirmation that ExportPlan canonical-flow needs no math change</name>
  <files>.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-06-SUMMARY.md (create)</files>
  <read_first>
    src/core/export.ts (read full file — focus on buildExportPlan + the line ~325 outW formula),
    src/core/analyzer.ts (read to confirm canonicalW = atlasSources.w propagation; just enough lines to verify the data-flow),
    src/core/loader.ts:699-744 (read — atlasSources Map construction; confirm w/h are canonical (unrotated) dims AND the rotated flag is set independently),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-04-SUMMARY.md (Plan 04 outcome — D-01 walk complete),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-05-SUMMARY.md (Plan 05 outcome — image-worker rotation + ATLAS-03 test green)
  </read_first>
  <action>
Read `src/core/export.ts` and locate the `outW` formula. Verify:

1. The formula uses `canonicalW` (or falls back to `sourceW` if canonicalW is undefined). The form is:
   `const outW = Math.ceil((canonicalW ?? sourceW) * effScale);`
   (or equivalent — the exact line may have shifted; verify present in buildExportPlan).

2. `canonicalW` is sourced from `atlasSources.get(...).w`, which is the unrotated dim (verified at loader.ts:699-744 — for atlas-source mode, `w = region.originalWidth` if present, else fallback to packed width post-correction).

3. NO math change was needed in src/core/export.ts during Phase 33. The Plan 05 export-rotation-dims.spec.ts already proves this (outW=100, outH=500 for a synthetic rotated row with canonicalW=100).

4. The rotation cascade is:
   - Loader: D-01 cooks canonical-corner offsets in attachment.offset[] (Plan 04)
   - atlasSources Map: w/h carry canonical dims unchanged; `rotated: bool` carries the flag (no change)
   - bounds.ts: reads pre-cooked offsets via computeWorldVertices → correct world AABB (no change)
   - analyzer.ts: aggregates peaks → SkeletonSummary.peaks[].canonicalW = unrotated W (no change)
   - export.ts:325: outW = ceil(canonicalW * effScale) (no change — already canonical-relative)
   - image-worker: receives ExportRow with outW=canonical and atlasSource.rotated, applies sharp.rotate(+90) (Plan 05)

Write the verification confirmation into the SUMMARY (see <output> block). The SUMMARY documents:
- Exact line in export.ts where the canonical-dim formula lives
- The verbatim formula text
- The data-flow chain proving the cascade
- Cross-reference to Plan 05's export-rotation-dims.spec.ts as the test-level lock
- Statement: "Phase 33 required no math change to src/core/export.ts. ATLAS-03 satisfied via D-01 (Plan 04) + image-worker rotation (Plan 05) + the existing canonical-relative outW formula."

This task is documentary; no source code is modified.
  </action>
  <verify>
    <automated>grep -q "ceil.*canonicalW.*effScale\|ceil.*canonicalW.*effectiveScale\|Math.ceil.*canonical" src/core/export.ts &amp;&amp; npm test -- tests/core/export-rotation-dims.spec.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "canonicalW" src/core/export.ts exits 0 (formula uses canonical-relative dim)
    - grep -q "Math.ceil" src/core/export.ts exits 0 (rounding present)
    - npm test -- tests/core/export-rotation-dims.spec.ts exits 0 (Plan 05's test still green)
    - npm test exits 0 (full suite still green)
    - SUMMARY contains a "ExportPlan canonical-flow verification" section with the exact line of the outW formula AND the data-flow chain
    - SUMMARY contains the statement: "Phase 33 required no math change to src/core/export.ts" (or equivalent — the canonical-flow was already correct pre-phase)
    - No source code was modified by this task (only the SUMMARY .md was written)
    - git diff --stat shows only the SUMMARY file changed (no src/ changes)
  </acceptance_criteria>
  <done>ExportPlan canonical-flow confirmed via source-read + Plan 05 test cross-reference; finding documented in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HUMAN-UAT — drop rotated fixture + Optimize Assets round-trip</name>
  <files>(no files modified — UAT is a runtime verification step)</files>
  <read_first>
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-VALIDATION.md (§"Manual-Only Verifications" — exact UAT scenarios),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-01-SUMMARY.md (Fixture Shape table — expected dims for the assertion in step 4)
  </read_first>
  <what-built>
    The full Phase 33 implementation (Plans 03-05):
    - src/core/loader.ts D-01 walk for rotated RegionAttachments
    - src/main/image-worker.ts sharp.rotate(+90) in passthrough + resize paths
    - RotatedRegionUnsupportedError + 'rotated-region-unsupported' kind fully removed (lockstep)
    - 5 new test specs covering ATLAS-01..04 all passing
    - fixtures/spine_rotated/ committed real-Spine-packer fixture

    What's left to verify by hand: the Electron renderer + sharp pipeline behavior end-to-end, against a live dev build, since unit tests cannot exercise the renderer's panel rendering or the file-picker-driven Optimize flow.
  </what-built>
  <how-to-verify>
    Run a dev build and verify ATLAS-01 + ATLAS-03 end-to-end:

    **Step 1 — Start dev build:**
    ```bash
    npm run dev
    ```
    Wait for the Electron window to open with the drop zone visible.

    **Step 2 — Drop the rotated fixture:**
    Drag `fixtures/spine_rotated/EXPORT/skeleton.json` (or the entire `fixtures/spine_rotated/EXPORT/` folder) onto the drop zone.

    **Expected:**
    - NO error banner appears (specifically: NO "Rotated atlas regions are not supported" message — this is the v1.0-era error that should be gone post-Phase-33).
    - The Global panel populates with at least one row showing the rotated region's name + dims.
    - The Animation Breakdown panel populates (may be empty if the fixture has no animations — that's OK; the Global panel population is the key check).
    - Inspect the row for the rotated region: its source dims should be the **unrotated** (canonical) dims — for the recommended 100×500 source rectangle, the row shows 100×500 (NOT 500×100).

    **If error banner appears OR panels don't populate:** Phase 33 implementation has a bug. Capture a screenshot, note which panel failed to populate, and resume-signal with "ISSUE: <description>".

    **Step 3 — Run Optimize Assets:**
    Click the Optimize Assets button (or equivalent — File menu / toolbar). Choose an output folder (`/tmp/spine_rotated_test_output` or similar).

    Wait for the Optimize flow to complete. If a confirmation dialog appears for safety buffer / sharpen / etc., accept defaults.

    **Step 4 — Inspect output PNG dimensions:**
    For the rotated region's output PNG (named after the region name from 33-01-SUMMARY.md "Fixture Shape" table), check the dimensions:
    ```bash
    cd /tmp/spine_rotated_test_output  # or wherever you exported
    node -e "require('sharp')('images/{REGION_NAME}.png').metadata().then(m => console.log(m.format, m.width, m.height))"
    # Or: macOS Finder → Get Info → Dimensions
    # Or: file images/{REGION_NAME}.png
    ```

    **Expected:**
    - Output PNG dims match the **canonical** (unrotated) source dims from 33-01-SUMMARY.md (e.g., 100×500 for the recommended rig).
    - NOT the packed-rotated dims (500×100 — that would mean rotation handling is broken end-to-end).

    **Step 5 — (Optional) Visual sanity check:**
    Open the output PNG in an image viewer. The pixel content should be in the original (unrotated) orientation — the source rectangle should look the same as before packing. If it looks rotated 90°, mirrored, or upside-down, the sharp.rotate direction in image-worker.ts is wrong (revisit Plan 05 Task 1+2; the +90 vs -90 direction was empirically verified, but a regression could have introduced a sign flip).

    **Step 6 — Confirm no regression:**
    Drop a NON-rotated fixture (e.g., `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` or `fixtures/spine_stripWS/EXPORT/skeleton.json`) into the same dev build. Confirm:
    - It still loads correctly (panels populate).
    - Optimize Assets still produces correct PNG dims for non-rotated regions.

    This regression check confirms Phase 33 didn't break the existing non-rotated atlas-source path.
  </how-to-verify>
  <resume-signal>
    After completing all 6 verification steps, type "UAT approved" or paste the output of step 4's metadata check. If any step fails, type "ISSUE: <step-N> <description>" and Claude will diagnose.

    Example pass signal:
    ```
    UAT approved. Step 2: panels populated, no banner. Step 4: output 100x500 (canonical match). Step 5: visually correct orientation. Step 6: SIMPLE_TEST + spine_stripWS still load + export correctly.
    ```
  </resume-signal>
  <action>See &lt;what-built&gt; and &lt;how-to-verify&gt; above — this is a checkpoint:human-verify task. The user runs the dev build and follows the 6 verification steps; Claude pauses for the resume signal. After approval, Claude updates the SUMMARY with the user's reported observations.</action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - User explicitly types a resume signal indicating all 6 steps passed (or describes which failed)
    - Step 2 verification: dev build accepts the rotated fixture (no error banner, panels populate)
    - Step 4 verification: output PNG dims match canonical (unrotated) source dims from 33-01-SUMMARY.md (NOT packed-swapped)
    - Step 6 regression: non-rotated fixtures (SIMPLE_TEST, spine_stripWS) still load and export correctly
    - SUMMARY records the user's UAT sign-off + the specific dims observed in step 4
  </acceptance_criteria>
  <done>User signs off on the live UAT; output PNG dims confirmed canonical; no regression in non-rotated path.</done>
</task>

</tasks>

<verification>
- npm test exits 0 (final full-suite gate)
- npx tsc --noEmit exits 0
- All 5 Phase 33 specs are active (no describe.skip):
  - tests/core/loader-rotation-accept.spec.ts
  - tests/core/bounds-rotation-aabb.spec.ts
  - tests/core/export-rotation-dims.spec.ts
  - tests/main/image-worker-rotation.spec.ts
  - tests/core/no-stale-rotation-error.spec.ts
- All 4 ATLAS REQs (ATLAS-01..04) have at least one active passing automated test
- HUMAN-UAT sign-off recorded in SUMMARY
- ExportPlan canonical-flow confirmed by source-read + Plan 05 test
</verification>

<success_criteria>
1. ExportPlan canonical-flow source-read confirmation documented in SUMMARY (no src/ changes — verification only)
2. HUMAN-UAT sign-off recorded with specific output dims observed
3. All Wave 1 RED scaffolds are now active GREEN tests (5/5)
4. Full automated suite green
5. Non-rotated regression scenarios still pass (regression check via Step 6)
6. Phase ready for `/gsd-verify-work 33` followed by milestone close (if Phase 33 is the v1.4 closing phase)
</success_criteria>

<output>
After completion, create `.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-06-SUMMARY.md` documenting:

**Section 1 — ExportPlan canonical-flow verification (Task 1):**
- Exact line in src/core/export.ts where the outW = ceil(canonicalW × effScale) formula lives
- Verbatim formula text (copy-paste from src/core/export.ts)
- Data-flow chain from D-01 (loader.ts) → bounds.ts → analyzer.ts → export.ts → image-worker.ts
- Cross-reference to tests/core/export-rotation-dims.spec.ts (Plan 05) as the test-level lock
- Statement: "Phase 33 required no math change to src/core/export.ts; the canonical-relative formula already in place handles rotated regions correctly via the D-01 cascade."

**Section 2 — HUMAN-UAT sign-off (Task 2):**
- The user's resume signal (verbatim — "UAT approved" or the issue description if applicable)
- Step 4 observed output PNG dimensions (vs expected canonical dims)
- Step 6 regression confirmation (non-rotated fixtures still work)
- Date/time of UAT

**Section 3 — Phase 33 close-of-phase summary:**
- All 4 ATLAS REQs covered: ATLAS-01 (Plans 03+04), ATLAS-02 (Plan 04), ATLAS-03 (Plan 05), ATLAS-04 (Plans 01+04+05)
- All 6 plans complete: 33-01 (fixture), 33-02 (scaffolds), 33-03 (lockstep removal), 33-04 (D-01 + ATLAS-01/02), 33-05 (image-worker + ATLAS-03), 33-06 (verification + UAT)
- 5 new test specs all passing; 2 deleted; 1 sub-test deleted; 1 rephrased
- D-158/D-171 lockstep precedent honored (atomic commit in Plan 03)
- Memory invariants preserved: project_strict_loadermode_separation, project_phase6_default_scaling, project_sampler_visibility_invariant, project_peak_anchored_invariants, project_compute_export_dims_canonical_base, project_atlas_pack_options_atlas_source_only, feedback_gitignore_fixtures_check_test_refs, feedback_narrow_before_fixing
- Note: Phase 33 is the v1.4 milestone close (after Phase 32 already complete). Suggest `/gsd-verify-work 33` next, then milestone-archive + tag if all REQs verified.
</output>
</content>
