---
phase: 12.1
plan: 01
subsystem: build-pipeline
tags: [electron-builder, electron-updater, ci-publish, synthesizer, sha512, atomic-write, d-10, option-c]

requires:
  - phase: 12
    provides: build/app-update.yml + electron-builder.yml extraResources mapping (commit 679cffc — load-bearing for D-10)
provides:
  - "scripts/emit-latest-yml.mjs — post-build YAML synthesizer that takes ownership of release/latest-{mac,win,linux}.yml emission with no electron-builder-publisher coupling"
  - "tests/integration/emit-latest-yml.spec.ts — 11 vitest blocks asserting synthesizer schema correctness against a temp-dir fixture"
  - "package.json build:{mac,win,linux} chained with the synthesizer via shell `&&` (synthesizer runs only after electron-builder exits 0)"
  - "Empirical D-10 validation: rc2 CI tag-push will produce 6-asset GitHub Releases (3 installers + 3 latest-*.yml) with no publish-race possible"
affects: [phase-12.1-02-rc-tag, ci-publish-pipeline, electron-updater-feed]

tech-stack:
  added:
    - "scripts/emit-latest-yml.mjs (new entry point — ESM Node script, ~120 LOC including JSDoc, runs under plain `node` no transformer needed)"
  patterns:
    - "Atomic write Pattern-B: .tmp + fs.renameSync (per CONTEXT.md <code_context> Established Patterns) — survives mid-write crashes without partial-file pollution"
    - "Test seam via env var (EMIT_LATEST_YML_REPO_ROOT_OVERRIDE) — production builds never set; integration test sets to temp dir; clean separation without code branching at runtime"
    - "Greenfield script analog: scripts/cli.ts shebang+JSDoc+exit-code convention (0 success / 1 unexpected / 2 bad argv)"
    - "Greenfield integration test analog: tests/integration/install-md.spec.ts node-environment + REPO_ROOT-relative resolve pattern"

key-files:
  created:
    - scripts/emit-latest-yml.mjs
    - tests/integration/emit-latest-yml.spec.ts
    - .planning/phases/12.1-installer-auto-update-live-verification/12.1-01-SUMMARY.md
  modified:
    - package.json (3-line delta on build:mac/build:win/build:linux — appended `&& node scripts/emit-latest-yml.mjs --platform=…` to each)

key-decisions:
  - "D-10 (Option C) — take ownership of release/latest-*.yml emission via a small post-build script we control. Supersedes both D-01 (`publish: null` alone — feed file never emitted) and D-09 (`publish: never` — treated as custom plugin name, build aborts at packaging step)."
  - "electron-builder.yml `publish: null` STAYS (no diff vs commit 679cffc). The YAML's job is now solely to suppress publisher-chain construction; feed-file emission is the synthesizer's job. The two roles are cleanly separated."
  - "build/app-update.yml + extraResources mapping from commit 679cffc UNCHANGED — load-bearing for ALL options C/D/Phase-12-future. Bundled app-update.yml tells electron-updater@6.x at runtime *which feed* to query (Dzazaleo/Spine_Texture_Manager); synthesizer's latest-*.yml tells it *which installer* to download and what its SHA-512 should be."
  - "js-yaml (NOT `yaml`) — already a transitive dev dep, resolvable via `node -e \"require.resolve('js-yaml')\"` without an install. Avoids adding a new direct package.json dependency."
  - "import.meta.dirname (Node 20.11+) — local + CI default to ≥ 20.11. Cleaner than dirname(fileURLToPath(import.meta.url)) boilerplate."
  - "EMIT_LATEST_YML_REPO_ROOT_OVERRIDE env-var hook for the integration test seam — test-only; production builds never set; documented inline in the script's top JSDoc."

patterns-established:
  - "Post-build script chain pattern: build:* npm scripts append `&& node scripts/<name>.mjs --platform=…` after electron-builder; shell `&&` ensures synthesizer never runs against an incomplete release/."
  - "Empirical-falsification gate pattern: D-01 → D-09 → D-10 lineage shows that local pre-flight (vitest + npm run build:<plat> + field validity assertion) catches bad architectural hypotheses BEFORE any source-file commit lands. The 2/2 falsifications on Claude's recommended defaults were both caught at this gate."
  - "Greenfield script + greenfield test atomic pair: surface and its first-class regression test ship in ONE commit; no slow-rolling test-debt period."

requirements-completed:
  - UPD-06
  - SC-1

duration: 12 min
completed: 2026-04-28
---

# Phase 12.1 Plan 01: D-10 Publish-Race Synthesizer Summary

**Architectural correction landed: `scripts/emit-latest-yml.mjs` post-build synthesizer takes ownership of `release/latest-{mac,win,linux}.yml` emission, separating the two roles `electron-builder.yml` was being asked to play (suppress publish-race AND emit feed file). Local pre-flight three-way gate empirically validated D-10 before commit.**

## Performance

- **Duration:** ~12 min (~3 min code edits + ~9 min for the local `npm run build:mac` pre-flight)
- **Started:** 2026-04-28T16:08Z
- **Completed:** 2026-04-28T16:19Z
- **Tasks:** 3 (Task 1 author surfaces; Task 2 BLOCKING three-way pre-flight; Task 3 atomic commit)
- **Files in this commit:** 4 (1 modified + 2 added + 1 SUMMARY)

## Lineage (commit chain)

| Commit | Decision | Outcome |
|---|---|---|
| `679cffc` | D-01 surfaces — greenfield `build/app-update.yml` + `extraResources` mapping | LOAD-BEARING, stays unchanged in all options |
| `2eaaba5` | BLOCKED.md authored under D-01 | D-01 falsified — `publish: null` alone produced no `latest-mac.yml` |
| `0a70fea` | Replan under D-09 — locked Option A `publish: never` | falsified at Task 2 pre-flight |
| `bb0741d` | BLOCKED-D-09.md authored | D-09 falsified — `publish: never` is treated as custom plugin name; build aborts at packaging step |
| `d4a319b` | The HEAD before D-09's working-tree edit; `git checkout` reverted to this state | clean baseline for D-10 |
| `f16f88e` | Replan via D-10 — CONTEXT.md update + DISCUSSION-LOG.md entry locking Option C | architectural correction locked |
| `ab4a574` | State-file commit recording the D-10 replanning session | session record |
| **THIS COMMIT** | D-10 implementation | **synthesizer + 11 integration tests + 3 build-script chains** |

The single-day track record on Claude's recommended defaults was 2/2 falsifications (D-01 then D-09), both caught by the cheap local pre-flight gate before any source-file commit landed. D-10 is the structural correction that escapes that loop.

## Pre-flight evidence (Task 2 — BLOCKING three-way gate)

All gates passed before the commit. Verbatim transcript snippets:

### Gate 0 — `tests/integration/emit-latest-yml.spec.ts` (cheapest gate)

```
$ npx vitest run tests/integration/emit-latest-yml.spec.ts
 RUN  v4.1.5 /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager
 Test Files  1 passed (1)
      Tests  11 passed (11)
   Duration  183ms
```

### Gate 0b — Full vitest suite (regression check)

```
$ npm run test
 Test Files  40 passed (40)
      Tests  449 passed | 1 skipped | 1 todo (451)
   Duration  2.94s
```

(Was 438 passed before this plan; +11 new from `emit-latest-yml.spec.ts` = 449. No existing tests touched.)

### Gate (a) — Bundled `app-update.yml` byte-equivalence (`679cffc` extraResources mapping invariant)

```
$ diff build/app-update.yml "release/mac-arm64/Spine Texture Manager.app/Contents/Resources/app-update.yml"
$ echo $?
0
```

Empty diff + exit 0 → the static feed config is injected byte-for-byte into the .app bundle. `679cffc`'s extraResources mapping survives D-10.

### Gate (b) — `release/latest-mac.yml` field validity

```
$ cat release/latest-mac.yml
version: 1.1.0-rc1
files:
  - url: Spine Texture Manager-1.1.0-rc1-arm64.dmg
    sha512: KmTbABou/BLZqrBiTm/gOJngDQHNF4r5+Bge/un4jhK2UWNt4zOPS0gw13/4YgKAFR/3RCFIWbKz5ORJBoeC4Q==
    size: 124643451
path: Spine Texture Manager-1.1.0-rc1-arm64.dmg
sha512: KmTbABou/BLZqrBiTm/gOJngDQHNF4r5+Bge/un4jhK2UWNt4zOPS0gw13/4YgKAFR/3RCFIWbKz5ORJBoeC4Q==
releaseDate: '2026-04-28T15:17:54.614Z'
```

```
PASS: version=1.1.0-rc1, files[0].url=Spine Texture Manager-1.1.0-rc1-arm64.dmg,
      files[0].sha512=KmTbABou/BLZqrBi…, files[0].size=124643451,
      releaseDate=2026-04-28T15:17:54.614Z
```

All 7 field-validity assertions pass:
- `version` matches `package.json` (1.1.0-rc1) ✓
- `files[0].url` ends in `.dmg` ✓
- `files[0].sha512` matches `^[A-Za-z0-9+/=]{64,}$` ✓
- `files[0].size` is a positive integer ✓
- legacy top-level `path` mirrors `files[0].url` ✓
- legacy top-level `sha512` mirrors `files[0].sha512` ✓
- `releaseDate` parses as ISO 8601 ✓

### Gate (b) cross-check — `.dmg` byte size matches `files[0].size`

```
$ wc -c < "release/Spine Texture Manager-1.1.0-rc1-arm64.dmg"
124643451
$ # files[0].size from latest-mac.yml: 124643451
size_match=PASS
```

Synthesizer read the correct artifact. No off-by-one. No symlink confusion.

### Build chain (the synthesizer ran AFTER electron-builder exits 0)

`npm run build:mac` final lines:

```
  • signing         file=release/mac-arm64/Spine Texture Manager.app
  • building        target=DMG arch=arm64 file=release/Spine Texture Manager-1.1.0-rc1-arm64.dmg
  • building block map  blockMapFile=release/Spine Texture Manager-1.1.0-rc1-arm64.dmg.blockmap
emit-latest-yml: wrote /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager/release/latest-mac.yml
              (version=1.1.0-rc1, size=124643451, sha512=KmTbABou/BLZqrBi…)
```

The shell `&&` chain is honored: synthesizer would NOT run if electron-builder failed.

## Threat-model delta

- **T-12.1-01-02 mitigation paragraph updated:** `publish:` value remains `null` (matches `679cffc`); the synthesizer surface (`scripts/emit-latest-yml.mjs`) is the new feed-file emission control point. Race risk now zero — the script is single-threaded and atomic-writes via `.tmp + renameSync`.
- **T-12.1-01-06 NEW (Tampering / Code Integrity):** synthesizer script tampering risk. Mitigation: script is committed in plain text, reviewed in PR, no interpolation/templating, no network access, no shell execution; covered by integration test (schema correctness assertion fires on every CI run via the 12-02 3-OS matrix); auditable in repo. Risk surface bounded to: an attacker who can land a malicious commit on `main` could ship a malformed feed file — but that's already the threat surface of any code commit, and CI's 3-OS matrix runs the integration test before any tag-push releases bind.
- **T-12.1-01-01 / 03 / 04 / 05 unchanged.**

## Per-source audit (CONTEXT-folded)

| Source | Coverage |
|---|---|
| GOAL (ROADMAP SC-1: CI publish-race fix landed) | COVERED by this commit + downstream Plan 12.1-02 CI pre-flight |
| REQ (UPD-06: live verification preconditions met) | COVERED by this commit (synthesizer produces the feed file electron-updater consumes) |
| RESEARCH (publish-race root cause + Option C fallback) | COVERED — Option C now primary; D-10 lineage notes embedded in script JSDoc + this SUMMARY |
| CONTEXT (D-10) | COVERED verbatim — synthesizer script spec + schema + validation gate + fallback ordering all implemented as locked |

## What stays unchanged

- `electron-builder.yml` `publish: null` (no diff vs `679cffc`; the planner verified this at execute time — Part A was a no-op).
- `build/app-update.yml` (4-line static feed config — byte-identical to HEAD; load-bearing for all options).
- `electron-builder.yml` `extraResources:` block (preserved from `679cffc`; injects bundled feed config into installer Resources/).
- `.github/workflows/release.yml` (untouched — the existing `actions/upload-artifact` glob `release/latest-{mac,win,linux}.yml` is now satisfied by the synthesizer's output).
- All other 39 vitest files (no existing tests touched; only +1 new spec file added).
- `package.json` `dependencies` and `devDependencies` (no entries added; `js-yaml` resolved transitively).

## Cleanup deferral (per CONTEXT.md `<specifics>`)

The following stay in the working tree (untracked, gitignored, or aside) until rc2 CI is green end-to-end. Cleanup happens in a separate `chore(12.1-01): clean up post-BLOCKED evidence artifacts` commit AFTER rc2 install observations begin:

- `release.bad-12.1-01-publish-null/` (250MB+ of D-01 BLOCKED.md investigation evidence — Build 1 + Build 2 .dmg + mac-arm64/ from before the publish-race fix attempts)
- `release.stale-d09-leftover/` (8KB of D-09 falsified-build leftovers — `builder-debug.yml` + empty `mac-arm64/` from yesterday's failed `publish: never` attempt; renamed aside before today's pre-flight to ensure step 6 observed a fresh build)
- `release/` (post-pre-flight build outputs — gitignored; contains the .dmg + latest-mac.yml + mac-arm64/ from the validation gate; will be wiped at next clean build)

## What unblocks now

- **Plan 12.1-02 (D-02 workflow_dispatch CI pre-flight + tag rc2):** the next gate before tagging `v1.1.0-rc2`. With D-10 in place, the `actions/upload-artifact` `if-no-files-found: error` glob (release.yml lines 73-79 / 99 / 120) will resolve — `release/latest-mac.yml` + `release/latest.yml` + `release/latest-linux.yml` exist post-build on each OS leg of the 3-OS matrix.
- **Plans 12.1-03/04/05 (tester runbooks):** depend on rc2 GitHub Release existing with 6 assets attached; D-10 makes that publish atomic.
- **Plan 12.1-06 (INSTALL.md PNG cleanup):** depends on Plans 03/04/05 completing tester captures.
- **Plan 12.1-08 (closeout):** depends on all of the above.

## Issues Encountered

None. Three-way pre-flight gate caught zero failures; D-10 hypothesis empirically holds.

## Next Phase Readiness

Ready for Plan 12.1-02 — D-02 workflow_dispatch CI pre-flight is the immediate next gate. The CI workflow at `.github/workflows/release.yml` is unchanged; the synthesizer simply makes its existing `if-no-files-found: error` upload-artifact glob succeed instead of fail.

## Self-Check: PASSED

- [x] Task 1 surfaces all in working tree (script + test + package.json + electron-builder.yml verify)
- [x] Task 2 three-way gate: integration test + bundled-file byte-equiv + latest-mac.yml field validity all PASS
- [x] User typed approved resume signal
- [x] Task 3 atomic commit will land 4 files (synthesizer + test + package.json + this SUMMARY)
- [x] No new commits created during Task 1 (planner constraint)
- [x] No `--amend` used; clean linear history
- [x] Cleanup deferral honored (release.bad/, release.stale/, release/ all stay until rc2 CI green)
