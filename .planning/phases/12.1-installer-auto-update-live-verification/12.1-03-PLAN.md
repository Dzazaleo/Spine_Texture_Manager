---
phase: 12.1
plan: 03
type: execute
wave: 3
depends_on: [12.1-02]
files_modified:
  - .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md
  - docs/install-images/macos-gatekeeper-open-anyway.png
autonomous: false
requirements:
  - UPD-06
  - REL-03
  - SC-2  # ROADMAP success criterion 2: macOS auto-update happy path verified live
  - SC-5  # ROADMAP success criterion 5: Offline + "no newer version" + Gatekeeper dialogs eyeballed
must_haves:
  truths:
    - "Tester downloaded fresh rc2 .dmg from GitHub Releases (NOT from local release/ — per RESEARCH §Pitfall 2)"
    - "Tester right-click → Open triggered Gatekeeper dialog; screenshot captured to docs/install-images/macos-gatekeeper-open-anyway.png (binary swap; ≥ 5KB)"
    - "Tester confirmed Help → About reads `1.1.0-rc2` after install (rc2 is the installed version BEFORE rc3 tag)"
    - "After Plan 12.1-02 Task 3 publishes rc3, tester relaunched rc2 and observed UpdateDialog appear within ~10s with rc3 in the version label"
    - "Tester clicked Download + Restart; app downloaded rc3, relaunched, Help → About now reads `1.1.0-rc3` (UPD-01 + UPD-03 + UPD-04 verified live on macOS)"
    - "Tester verified Later semantics: dismissed prompt on a fresh launch, relaunched, prompt did NOT re-appear for the same version (UPD-04 dismissedUpdateVersion behavior)"
    - "Tester verified offline graceful: disabled Wi-Fi, relaunched, no error dialog, no crash, normal startup (UPD-05)"
    - "Tester verified Help → Check for Updates on rc3 shows 'You're up to date' message (UPD-02)"
    - "Tester verified Help → Installation Guide opens INSTALL.md externally in default browser (D-16 surface 3)"
    - "12.1-HUMAN-UAT.md Tests 1, 3, 4, 6, 9 (surface 3) flipped from [pending] to passed (or [partial] / [failed] with evidence) with transcript lines appended below each test entry"
  artifacts:
    - path: "docs/install-images/macos-gatekeeper-open-anyway.png"
      provides: "Real Gatekeeper 'Open Anyway' dialog screenshot from fresh rc2 download"
      min_lines: 0  # binary file; size assertion below
    - path: ".planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md"
      provides: "Updated runbook transcript: Tests 1, 3, 4, 6, 9 (surface 3) flipped with evidence"
      contains: "passed"
  key_links:
    - from: "rc2 installed instance"
      to: "GitHub Releases feed"
      via: "electron-updater reads bundled app-update.yml → queries https://github.com/Dzazaleo/Spine_Texture_Manager/releases/latest.json"
      pattern: "background HTTP GET 3.5s after app.whenReady"
    - from: "UpdateDialog 'Download + Restart' click"
      to: "rc3 installed (replacing rc2)"
      via: "autoUpdater.downloadUpdate() → autoUpdater.quitAndInstall(false, true)"
      pattern: "IPC update:download → IPC update:quit-and-install → quitAndInstall"
---

<objective>
Execute the macOS HUMAN-UAT runbook: install rc2 from GitHub Releases (capturing Gatekeeper screenshot during first launch), confirm Help → About reads rc2, observe rc2 detect rc3 once Plan 12.1-02 Task 3 publishes it, click through Download + Restart, confirm app relaunches into rc3, and verify offline + "Later" + "up to date" + Help → Installation Guide behaviors. Append per-step transcript evidence to 12.1-HUMAN-UAT.md.

Purpose: Satisfy ROADMAP SC-2 (macOS auto-update happy path live) + SC-5 (Gatekeeper + offline + "up to date" eyeballed) + REL-03 capture #1 (Gatekeeper PNG) + the macOS slice of UPD-06 / UPD-01..UPD-05 live verification.

Output: 1 binary PNG (overwrites 67-byte placeholder), transcript edits to 12.1-HUMAN-UAT.md flipping Tests 1, 3, 4, 6, and surface 3 of Test 9 with paste-back evidence per BLOCKING checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-RESEARCH.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-PATTERNS.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-02-SUMMARY.md
@INSTALL.md
@src/main/auto-update.ts

<interfaces>
<!-- Key wiring the tester is observing. Lines for grep-fast reference only. -->

src/main/auto-update.ts (DO NOT MODIFY in this plan):
- line 92: `const SPIKE_PASSED = process.platform !== 'win32';`  (macOS = true → full auto-update path)
- line 153-155: `setTimeout(checkUpdate(false), 3500);`  (UPD-01 startup check, 3.5s after whenReady)
- line 168-184: `Promise.race([checkForUpdates(), 10s timeout])`  (UPD-05 graceful timeout)
- line 372-375: variant routing — macOS gets `'auto-update'` variant (NOT windows-fallback)

INSTALL.md macOS section (lines 15-41) — the wording the captured screenshot's dialog text must match (per RESEARCH §Pitfall 4):
- line 25: italicized `_"App cannot be opened because the developer cannot be verified."_`
  - If the captured Gatekeeper dialog wording differs (e.g. macOS Sequoia changed it), edit INSTALL.md:25 in the same commit as the binary swap (per RESEARCH §Pitfall 4 atomic-commit rule).

Screenshot path (binary-only swap; markdown image reference at INSTALL.md:27 unchanged):
- docs/install-images/macos-gatekeeper-open-anyway.png (currently 67-byte 1×1 placeholder; replace with real capture)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Install rc2 .dmg from GitHub Releases on macOS test host; capture Gatekeeper screenshot</name>
  <what-built>
    - Plan 12.1-01 publish-race fix
    - Plan 12.1-02 Task 2 published rc2 with 6 assets
  </what-built>
  <how-to-verify>
This is the live install observation — tester runs each step on the macOS test host, captures one screenshot, pastes evidence below.

**CRITICAL (per RESEARCH §Pitfall 2):** Download the .dmg fresh from GitHub Releases. DO NOT use a local `release/*.dmg` — macOS trusts locally-built apps even with quarantine attribute set; the Gatekeeper dialog state on a local build does NOT match what real testers see.

Sequence (numbered steps; each is BLOCKING; paste evidence after each STOP):

1. STOP — confirm v1.1.0-rc2 is published (non-draft):
   ```bash
   gh release view v1.1.0-rc2 --json isDraft -q .isDraft
   ```
   Expected output: `false`. If `true`, return to Plan 12.1-02 Task 2 step 8 (`gh release edit v1.1.0-rc2 --draft=false`).

2. Download the macOS .dmg from the release page. In a browser, visit:
   `https://github.com/Dzazaleo/Spine_Texture_Manager/releases/tag/v1.1.0-rc2`
   Click `Spine-Texture-Manager-1.1.0-rc2-arm64.dmg` to download.
   Confirm the file size matches `gh release view v1.1.0-rc2 --json assets -q '.assets[] | select(.name | endswith(".dmg")) | .size'` (typically 100-150MB).

3. Double-click the downloaded `.dmg` in Finder. macOS mounts the disk image and shows a Finder window with the app icon next to an Applications shortcut.

4. Drag **Spine Texture Manager.app** into **Applications**.

5. Open Applications in Finder. **Right-click** (or Control-click) the app icon. Choose **Open** from the context menu. macOS displays the Gatekeeper dialog.

6. CAPTURE — use Cmd+Shift+4 → Space → click the Gatekeeper dialog (captures only that window):
   - Default capture path: `~/Desktop/Screenshot 20XX-XX-XX at HH.MM.SS.png`
   - Move + rename:
     ```bash
     mv ~/Desktop/Screenshot*.png /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager/docs/install-images/macos-gatekeeper-open-anyway.png
     ```
   - Verify the captured file:
     ```bash
     stat -f "%z" /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager/docs/install-images/macos-gatekeeper-open-anyway.png
     ```
     Expected: a number ≥ 5000 (real captures are 50KB-200KB; the 67-byte placeholder is gone).

7. Click **Open Anyway** in the Gatekeeper dialog (or **Open** depending on macOS version). The app launches.

8. Verify Help → About shows `1.1.0-rc2` (this gates Plan 12.1-02 Task 3 from firing rc3).

9. STOP — paste below: (a) screenshot file size from step 6 (`stat -f "%z"` output), (b) verbatim transcription of the Gatekeeper dialog title + body text (so executor can compare against INSTALL.md:25 italicized text per RESEARCH §Pitfall 4), (c) confirmation "Help → About reads 1.1.0-rc2".

10. If the Gatekeeper dialog wording in step 9(b) DIFFERS from INSTALL.md:25 (`_"App cannot be opened because the developer cannot be verified."_`), record the EXACT new wording. Plan 12.1-06 will edit INSTALL.md to match in the same commit as the binary swap (per RESEARCH §Pitfall 4 atomic-commit rule).
  </how-to-verify>
  <resume-signal>Type "captured — rc2 installed; Help→About reads rc2" with the 3 evidence items pasted below. Type "wording-mismatch — see paste" if step 9(b) reveals INSTALL.md needs a markdown text edit (Plan 12.1-06 will pick it up). Type "rework — <reason>" if any step failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: After rc3 publishes, observe rc2 detect rc3 + click through full auto-update lifecycle</name>
  <what-built>
    - Task 1: rc2 installed on macOS test host, Help → About reads rc2
    - Plan 12.1-02 Task 3 must publish rc3 BEFORE this task fires
  </what-built>
  <how-to-verify>
Pre-condition: Plan 12.1-02 Task 3 has published v1.1.0-rc3 (non-draft). The rc2 install from Task 1 is still on the macOS test host.

Sequence (BLOCKING per step):

1. STOP — confirm v1.1.0-rc3 is published:
   ```bash
   gh release view v1.1.0-rc3 --json isDraft -q .isDraft
   ```
   Expected: `false`.

2. Quit the rc2 app if running (so the next launch fires the startup check freshly): `Cmd+Q`.

3. Relaunch rc2 from Applications (double-click; the app is now Gatekeeper-trusted from Task 1).

4. Wait ~5-10 seconds (UPD-01 startup check fires at 3.5s post `whenReady` per src/main/auto-update.ts:153-155; HTTP fetch to GitHub adds 1-3s).

5. Observe: a non-blocking modal (UpdateDialog) should appear with:
   - Headline: "Update available" (or similar from UpdateDialog state='available')
   - Version label: `1.1.0-rc3`
   - Release-notes Summary excerpt (D-09 plain-text from rc3's release body Summary section)
   - Buttons: **Download + Restart** (primary) and **Later** (secondary)

6. STOP — paste below: (a) timing — how many seconds from launch to dialog appearing; (b) verbatim screenshot OR transcription of the dialog content (headline + version + Summary first line + button labels).

7. Click **Download + Restart**. Observe state transitions:
   - Dialog updates to state='downloading' with progress (per UpdateDialog.tsx state machine)
   - When download completes, app quits and relaunches (autoUpdater.quitAndInstall(false, true) per auto-update.ts:217-221)

8. After relaunch, verify Help → About shows `1.1.0-rc3` (replaces the rc2 install in place).

9. STOP — paste below: (a) total elapsed wall time from "click Download + Restart" to "rc3 launched"; (b) confirmation "Help → About reads 1.1.0-rc3".

If step 5 NEVER shows a dialog within 30s, check the developer console for the silent-swallow log (per UPD-05 Promise.race timeout at 10s); the network may be unreachable. Re-attempt with a known-good network. If the dialog appears but Download + Restart hangs, that's an electron-updater issue — capture the failure to feed back into RESEARCH (out of 12.1 scope to fix).
  </how-to-verify>
  <resume-signal>Type "rc3 detected + applied — UPD-01..UPD-04 verified macOS" with paste-back evidence. Type "partial — detected but apply failed" if step 5 succeeded but step 7 hung (capture details). Type "not detected — see paste" with timing + console evidence if step 5 never fired.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify "Later" persistence + offline graceful + "up to date" + Help → Installation Guide on macOS</name>
  <what-built>
    - Task 1 + Task 2: rc2 → rc3 lifecycle observed; macOS host now runs rc3
  </what-built>
  <how-to-verify>
This task verifies the remaining 4 macOS slices in one session: UPD-04 "Later" persistence, UPD-05 offline graceful, UPD-02 "up to date", and D-16 surface 3 (Help → Installation Guide).

Sequence (BLOCKING):

**Sub-test A — "Later" persistence (UPD-04 dismissedUpdateVersion):**

This requires a SECOND newer version to test against. Workaround per RESEARCH: fake the test by editing `app.getPath('userData')/<spine-texture-manager-updater>/update-state.json` to set `dismissedUpdateVersion: "1.1.0-rc999"`, then trigger Help → Check for Updates and confirm no prompt fires (because rc3 < rc999 by compareSemver). OR if a real rc4 ships at any point, use the natural path: install rc4 detection from rc3, click Later, relaunch rc3, confirm prompt does NOT re-fire.

For 12.1, the simpler path: skip A-real-newer-version setup; instead verify the `dismissedUpdateVersion` field is written when Later is clicked.

1. With rc3 running, trigger Help → Check for Updates manually:
   ```
   Help menu → Check for Updates...
   ```
   Expected: UpdateDialog appears with state='none' showing "You're up to date — running 1.1.0-rc3" or similar (per UPD-02 + D-05 — no native alert).
   
2. Verify no JSON state was written for "rc3 = up to date":
   ```bash
   cat ~/Library/Application\ Support/spine-texture-manager-updater/update-state.json 2>/dev/null || echo "no state file yet"
   ```
   (state file only writes on Later click, so this should be empty/missing UNLESS user previously clicked Later — note current state.)

3. STOP — paste below: (a) "up to date" dialog screenshot OR verbatim text (UPD-02 evidence), (b) the update-state.json contents (or "missing — no Later click yet").

**Sub-test B — Offline graceful (UPD-05):**

4. Disable Wi-Fi: System Settings → Wi-Fi → Off.

5. Quit rc3 (`Cmd+Q`). Relaunch rc3 from Applications.

6. Observe: app should launch normally to the load screen with NO error dialog, NO crash. The startup check fires, hits Promise.race timeout (10s) silently, log goes to console only.

7. Open DevTools console (View → Toggle Developer Tools → Console). Look for a log line like `[auto-update] startup check timed out` or similar swallow log per src/main/auto-update.ts:184.

8. STOP — paste below: (a) confirmation "no error dialog, app launched normally", (b) the console swallow log line OR confirmation that no error log appeared (depends on logger verbosity).

9. Re-enable Wi-Fi.

**Sub-test C — Help → Installation Guide (D-16 surface 3):**

10. With rc3 running and Wi-Fi back on, click **Help → Installation Guide…** in the menu (3rd item under Help per src/main/index.ts:301-303).

11. Observe: default browser opens; URL is `https://github.com/Dzazaleo/Spine_Texture_Manager/blob/main/INSTALL.md`. The INSTALL.md page renders (with placeholder PNGs today; Plan 12.1-06 swaps them for real captures next).

12. STOP — paste below: (a) browser URL bar contents (must match INSTALL.md URL byte-for-byte per the URL-consistency test gate), (b) confirmation "INSTALL.md page rendered".

**Sub-test D — flip Tests 1, 3, 4, 6, and surface 3 of Test 9 in 12.1-HUMAN-UAT.md:**

13. After all sub-tests pass, edit `.planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md` to flip the relevant test entries:
    - Test 1 (macOS auto-update happy path) → result: passed; append transcript line with evidence from Task 2 step 9
    - Test 3 (Help → Check for Updates "up to date") → result: passed (macOS slice); append transcript from sub-test A
    - Test 4 (Offline graceful) → result: passed (macOS slice); append transcript from sub-test B
    - Test 6 (Gatekeeper "Open Anyway" capture) → result: passed; append transcript from Task 1 step 9 + reference docs/install-images/macos-gatekeeper-open-anyway.png
    - Test 9 (INSTALL.md 4 link surfaces) — surface 3 (Help → Installation Guide) → mark partial-passed (macOS slice only); append transcript from sub-test C

14. Update Summary tally: increment `passed` by N (N = how many tests fully flipped to passed; partial flips on Test 9 stay in `pending` until Plans 04 + 05 also confirm surface 3).

15. Bump `updated:` field in frontmatter to current ISO-8601.

16. Commit the HUMAN-UAT.md edits (and the screenshot if not already committed in Task 1):
    ```bash
    git add .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md docs/install-images/macos-gatekeeper-open-anyway.png
    git commit -m "test(12.1-03): macOS HUMAN-UAT — Tests 1,3,4,6,9 (surface 3) passed; Gatekeeper screenshot captured"
    ```

Paste below: confirmation that all 4 sub-tests passed + the commit SHA.
  </how-to-verify>
  <resume-signal>Type "macOS runbook complete — Tests 1,3,4,6,9.3 flipped" with the commit SHA. Type "partial — <which sub-tests failed>" if any sub-test had unexpected behavior. macOS slice of Phase 12.1 is now complete; Plan 12.1-04 (Linux) + 12.1-05 (Windows) execute in parallel from here.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Releases → tester macOS host | Tester downloads rc2 .dmg over TLS; integrity is via GitHub TLS + (eventual) SHA-512 comparison from latest-mac.yml during auto-update step |
| rc2 installed app → GitHub Releases feed | electron-updater HTTPS fetch to https://github.com/Dzazaleo/Spine_Texture_Manager/releases.atom (RSS feed); strict-`>` semver compare in compareSemver gates upgrade prompt |
| Help → Installation Guide click | window.api.openExternalUrl(INSTALL_DOC_URL) → ipc.ts:174 allow-list check → shell.openExternal → user's default browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-03-01 | Spoofing | Gatekeeper screenshot capture | accept | Captured on the developer's macOS host; provenance recorded in HUMAN-UAT transcript (OS version + capture date); no PII in dialog content |
| T-12.1-03-02 | Tampering | docs/install-images/*.png | mitigate | PNG magic-bytes + ≥5KB size both gated by tests/integration/install-md.spec.ts (existing test + new test added in Plan 12.1-06); CI enforces on every push |
| T-12.1-03-03 | Information Disclosure | DevTools console logs (UPD-05 silent swallow) | accept | Logs are local to the test host; no telemetry exfiltration; log content is generic (timeout / network unreachable) — no user file paths, no PII |
| T-12.1-03-04 | Tampering | Help → Installation Guide URL drift | mitigate | URL consistency gated by tests/integration/install-md.spec.ts (existing 4-surface byte-equivalence assertion); CI fails if any surface drifts |
| T-12.1-03-05 | Elevation | Auto-update quitAndInstall | mitigate | quitAndInstall(false, true) is the documented electron-updater API; runs at user's existing privilege level (no elevation prompt on macOS for app-bundle replacement in user's Applications folder) |
</threat_model>

<verification>
- Tests 1, 3 (macOS slice), 4 (macOS slice), 6 in 12.1-HUMAN-UAT.md flipped from [pending] to passed (or partial with evidence).
- Test 9 surface 3 (Help → Installation Guide) flipped to partial-passed (macOS slice; awaiting Plans 04 + 05 for full surface 3 confirmation).
- docs/install-images/macos-gatekeeper-open-anyway.png is no longer 67-byte placeholder (size ≥ 5KB).
- 12.1-HUMAN-UAT.md `updated:` ISO-8601 bumped; Summary tally incremented.
- One atomic commit with both file changes.
- (Optional) If Gatekeeper wording mismatched INSTALL.md:25, the wording mismatch is recorded in the transcript for Plan 12.1-06 to address.
</verification>

<success_criteria>
- ROADMAP SC-2 verified live on macOS: rc2 detects rc3, downloads, relaunches into rc3.
- ROADMAP SC-5 partial verification: Gatekeeper dialog matches INSTALL.md (or wording-mismatch is documented for 12.1-06); offline + "up to date" verified live.
- REL-03 capture #1 of 4 lands: Gatekeeper PNG.
- 12.1-HUMAN-UAT.md transcript surface populated with macOS evidence; ready for Plans 04 + 05 to append Linux + Windows evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-installer-auto-update-live-verification/12.1-03-SUMMARY.md` documenting:
- The Gatekeeper PNG capture (file size + provenance: macOS version + capture date + capture-tool)
- The rc2 → rc3 detect-download-relaunch transcript (timing + dialog content)
- The "Later" / offline / "up to date" / Help→Installation Guide sub-test results
- Any wording mismatch flagged for Plan 12.1-06 to handle
- The commit SHA(s) for the HUMAN-UAT.md flip + binary swap
</output>
