---
phase: 12.1
plan: 02
type: execute
wave: 2
depends_on: [12.1-01]
files_modified:
  - .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md
  - package.json
autonomous: false
requirements:
  - UPD-06
  - SC-1  # ROADMAP success criterion 1: 6-asset GitHub Release atomically
must_haves:
  truths:
    - "v1.1.0-rc2 tag pushed to origin; CI release.yml workflow run completed successfully (all 3 build legs green + publish job green)"
    - "v1.1.0-rc2 GitHub Release exists with EXACTLY 6 assets attached: 1 .dmg + 1 .exe + 1 .AppImage + latest-mac.yml + latest.yml + latest-linux.yml"
    - "v1.1.0-rc3 tag pushed to origin; CI release.yml workflow run completed successfully"
    - "v1.1.0-rc3 GitHub Release exists with EXACTLY 6 assets attached"
    - "Workflow run logs for both rc2 + rc3 contain ZERO occurrences of 'Personal Access Token is not set' or 'asset_already_exists' (publish-race did not recur)"
    - "Greenfield 12.1-HUMAN-UAT.md scaffold authored at this phase directory; frontmatter + section headings match the Phase 12 analog; 9 numbered runbook entries; tests sequence correctly per RESEARCH §Pitfall 3"
  artifacts:
    - path: ".planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md"
      provides: "Greenfield runbook scaffold for Plans 03/04/05 to append transcripts to"
      contains: "## Tests"
    - path: "package.json"
      provides: "Version bump 1.1.0-rc1 → 1.1.0-rc2 → 1.1.0-rc3 (two atomic version commits)"
      contains: "1.1.0-rc"
    - path: "GitHub Release v1.1.0-rc2 (remote, not in repo)"
      provides: "First publishable rc with publish-race fix; load-bearing for Plan 12.1-03/04/05 install observation"
      exports: ["installer-mac/Spine-Texture-Manager-1.1.0-rc2-arm64.dmg", "installer-win/Spine-Texture-Manager-1.1.0-rc2-x64.exe", "installer-linux/Spine-Texture-Manager-1.1.0-rc2-x86_64.AppImage", "latest-mac.yml", "latest.yml", "latest-linux.yml"]
    - path: "GitHub Release v1.1.0-rc3 (remote, not in repo)"
      provides: "Second publishable rc; the rc2-installed apps must detect this as a newer version (UPD-01..UPD-04 happy path observation)"
      exports: ["installer-mac/Spine-Texture-Manager-1.1.0-rc3-arm64.dmg", "installer-win/Spine-Texture-Manager-1.1.0-rc3-x64.exe", "installer-linux/Spine-Texture-Manager-1.1.0-rc3-x86_64.AppImage", "latest-mac.yml", "latest.yml", "latest-linux.yml"]
  key_links:
    - from: "git tag v1.1.0-rc2"
      to: "GitHub Actions workflow run"
      via: "release.yml `on: push: tags: 'v*.*.*'` trigger"
      pattern: "tag-trigger → workflow run"
    - from: "GitHub Actions workflow publish job"
      to: "GitHub Releases v1.1.0-rc2"
      via: "softprops/action-gh-release@v2.6.2 with files: glob (6 paths)"
      pattern: "softprops upload"
---

<objective>
Tag v1.1.0-rc2 and v1.1.0-rc3 in sequence; supervise both CI runs; assert each produces a complete 6-asset GitHub Release atomically (no missing-asset / partial-publish state, no publish-race log lines). Author the greenfield 12.1-HUMAN-UAT.md scaffold so Plans 03/04/05 can append transcripts. Sequence is locked per RESEARCH §Pitfall 3: tag rc2 → CI green → install rc2 EVERYWHERE → tag rc3.

Purpose: Produce the two publishable releases that Wave 3 (Plans 03/04/05) needs to observe the auto-update detect → download → relaunch lifecycle on real packaged installs.

Output: Two version-bump commits + 2 git tags pushed + 1 greenfield HUMAN-UAT scaffold committed; 2 successful GitHub Releases (each with 6 assets); transcript of `gh release view --json assets -q '.assets | length'` returning 6 for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-RESEARCH.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-PATTERNS.md
@.planning/phases/12-auto-update-tester-install-docs/12-HUMAN-UAT.md
@.github/workflows/release.yml
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-01-SUMMARY.md

<interfaces>
<!-- Greenfield 12.1-HUMAN-UAT.md frontmatter + section pattern (mirrors 12-HUMAN-UAT.md). -->

Frontmatter shape (exactly mirroring 12-HUMAN-UAT.md lines 1-7):
```yaml
---
status: partial
phase: 12.1-installer-auto-update-live-verification
source: [12-VERIFICATION.md, 12.1-VERIFICATION.md]
started: <ISO-8601 timestamp>
updated: <ISO-8601 timestamp>
---
```

Body section structure (exactly mirroring 12-HUMAN-UAT.md):
- ## Current Test (free-text status banner — single paragraph)
- ## Tests (numbered ### N. <title> blocks with `expected:` + `result:` lines)
- ## Summary (key:value tally — total/passed/issues/pending/skipped/blocked)
- ## Gaps (free-text follow-ups paragraph)

The 9 test entries are the same 9 from 12-VERIFICATION.md `human_verification` array — they map 1:1 to runbook step content that Plans 03/04/05 will append.

GitHub Release asset glob (per release.yml:159-165, MUST yield 6 assets per release):
```
assets/*.dmg            → Spine-Texture-Manager-1.1.0-rcN-arm64.dmg
assets/*.exe            → Spine-Texture-Manager-1.1.0-rcN-x64.exe
assets/*.AppImage       → Spine-Texture-Manager-1.1.0-rcN-x86_64.AppImage
assets/latest.yml       → Windows feed
assets/latest-mac.yml   → macOS feed
assets/latest-linux.yml → Linux feed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author greenfield 12.1-HUMAN-UAT.md scaffold</name>
  <read_first>
    - .planning/phases/12-auto-update-tester-install-docs/12-HUMAN-UAT.md (FULL file — 62 lines; the analog being mirrored)
    - .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md (lines 8-36 — the 9 human_verification array entries that map 1:1 to Tests)
    - .planning/phases/12.1-installer-auto-update-live-verification/12.1-PATTERNS.md (§12.1-HUMAN-UAT.md — Body section pattern + What 12.1's runbook should add beyond the analog)
  </read_first>
  <files>.planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md</files>
  <action>
Create the file `.planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md` with the following exact content (frontmatter + 4 sections, mirroring 12-HUMAN-UAT.md byte-for-byte except for phase id + sequencing prose):

```markdown
---
status: partial
phase: 12.1-installer-auto-update-live-verification
source: [12-VERIFICATION.md, 12.1-VERIFICATION.md]
started: <FILL: current ISO-8601 timestamp at task execution time, e.g. 2026-04-28T13:00:00Z>
updated: <SAME timestamp as started>
---

## Current Test

[awaiting tester rounds — Plans 12.1-03 (macOS), 12.1-04 (Linux), 12.1-05 (Windows) append per-OS runbook step transcripts here as they execute against the rc2 + rc3 published artifacts. Sequencing per RESEARCH §Pitfall 3: rc2 must be installed on ALL 3 OSes (Help → About reads rc2 on each) BEFORE rc3 is tagged — this gates the auto-update detect-from-rc2 → land-on-rc3 observation.]

## Tests

### 1. macOS auto-update happy path (UPD-01..UPD-04, ROADMAP SC #2)
expected: Install v1.1.0-rc2 .dmg → confirm Help → About reads rc2 → wait for rc3 published → relaunch rc2 → UpdateDialog appears within 10s with rc3 release-notes Summary → click Download+Restart → app relaunches into rc3; declining (Later) does not re-prompt on next startup
result: [pending — Plan 12.1-03]

### 2. Linux AppImage auto-update happy path (UPD-01..UPD-04, ROADMAP SC #3)
expected: Install v1.1.0-rc2 AppImage on Ubuntu (22.04 OR 24.04 with libfuse2t64 installed) → confirm Help → About reads rc2 → wait for rc3 published → relaunch rc2 → UpdateDialog appears with rc3 → Download+Restart → relaunches into rc3
result: [pending — Plan 12.1-04]

### 3. Help → Check for Updates with no newer version (UPD-02, ROADMAP SC #5)
expected: With latest installed version (rc3 after Test 1/2 complete) → Help → Check for Updates → "You're up to date" message via UpdateDialog state='none' (D-05 — no native alert())
result: [pending — Plans 12.1-03 / 04 / 05]

### 4. Offline graceful (UPD-05, ROADMAP SC #5)
expected: Disconnect network → launch app → no error dialog, no crash, normal startup to load screen; DevTools console shows the swallow log only
result: [pending — Plans 12.1-03 / 04 / 05]

### 5. Windows manual-fallback notice (UPD-06, ROADMAP SC #4 — manual-fallback path)
expected: On Windows install (SPIKE_PASSED=false on win32, src/main/auto-update.ts:92), when the GitHub feed serves rc3 to an installed rc2: UpdateDialog opens in 'windows-fallback' variant — version label + 'Open Release Page' button → click opens https://github.com/Dzazaleo/Spine_Texture_Manager/releases externally; non-blocking, no nag loop, no modal interruption
result: [pending — Plan 12.1-05]

### 6. Gatekeeper "Open Anyway" first-launch on macOS (REL-03, ROADMAP SC #5)
expected: Download fresh rc2 .dmg from GitHub Releases on macOS → double-click → Gatekeeper dialog → right-click → Open → "Open Anyway" click → app launches; capture screenshot for INSTALL.md (saved to docs/install-images/macos-gatekeeper-open-anyway.png)
result: [pending — Plan 12.1-03]

### 7. SmartScreen "More info → Run anyway" on Windows (REL-03, ROADMAP SC #5)
expected: Download fresh rc2 .exe from GitHub Releases on Windows 10/11 → run → SmartScreen dialog → CAPTURE PNG #1 (saved to docs/install-images/windows-smartscreen-more-info.png) → More info → CAPTURE PNG #2 (saved to docs/install-images/windows-smartscreen-run-anyway.png) → Run anyway → NSIS installer launches
result: [pending — Plan 12.1-05]

### 8. libfuse2t64 error on Ubuntu 24.04 (REL-03, ROADMAP SC #5)
expected: Run rc2 AppImage on fresh Ubuntu 24.04 (no libfuse2t64 installed; Docker fallback acceptable per RESEARCH §Pitfall 6 + §Environment Availability) → observe "dlopen(): error loading libfuse.so.2"; INSTALL.md paragraph wording matches actual error; capture screenshot (saved to docs/install-images/linux-libfuse2-error.png)
result: [pending — Plan 12.1-04]

### 9. INSTALL.md 4 link surfaces — eyeballed verification (REL-03 / D-16)
expected: (1) v1.1.0-rc2 GitHub Release body shows `See [INSTALL.md](https://github.com/Dzazaleo/Spine_Texture_Manager/blob/main/INSTALL.md)` (verifiable via `gh release view v1.1.0-rc2 --json body`); (2) README "Installing" section visible on github.com repo page; (3) In-app Help → Installation Guide… opens INSTALL.md externally via system browser; (4) HelpDialog "Install instructions" section link opens same URL
result: [pending — Plans 12.1-03 / 04 / 05 surfaces 3+4; Plan 12.1-02 surface 1 (gh release view); Plan 12.1-08 surface 2 (manual eyeball)]

## Summary

total: 9
passed: 0
issues: 0
pending: 9
skipped: 0
blocked: 0

## Gaps

(none yet — Plans 12.1-03 / 04 / 05 will execute the runbook steps and append transcripts; Plan 12.1-08 will fold passed results back into 12-VERIFICATION.md `human_verification` array per the chosen flip pattern from RESEARCH §Open Question 1.)
```

Replace the two `<FILL: ...>` markers with the actual ISO-8601 timestamp at execution time (use `date -u +"%Y-%m-%dT%H:%M:%SZ"`).

Anti-pattern guard (per RESEARCH §Pitfall 3): The Tests section MUST express sequencing — Test 1 + Test 2 reference "wait for rc3 published" as a STOP gate, NOT "tag rc3 first." Plans 03/04/05 will further expand each test with explicit numbered sub-steps when they execute. The scaffold here is the contract, not the playbook.

DO NOT touch the analog file `.planning/phases/12-auto-update-tester-install-docs/12-HUMAN-UAT.md` — it is the Phase 12 closure record, frozen.
  </action>
  <verify>
    <automated>test -f .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md && grep -q "^phase: 12.1-installer-auto-update-live-verification$" .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md && grep -q "^status: partial$" .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md && grep -c "^### [1-9]\." .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md | grep -q "^9$" && grep -q "^## Current Test$" .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md && grep -q "^## Tests$" .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md && grep -q "^## Summary$" .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md && grep -q "^## Gaps$" .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md && grep -q "^pending: 9$" .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `.planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md`
    - Frontmatter has `phase: 12.1-installer-auto-update-live-verification` and `status: partial`
    - Body has 4 H2 sections: `## Current Test`, `## Tests`, `## Summary`, `## Gaps` (in that order)
    - Body has exactly 9 `### N.` test entries
    - Summary tally has `total: 9` and `pending: 9`
    - All 9 test entries reference the source plan in `[pending — Plan 12.1-XX]` format
    - Sequencing is correct: Tests 1 + 2 reference rc3 as a "wait" gate, not a "tag now" action
  </acceptance_criteria>
  <done>Greenfield 12.1-HUMAN-UAT.md committed; ready for Plans 03/04/05 to append step-by-step transcripts and flip per-test `result:` lines from `[pending]` to `passed` / `failed` / `partial`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bump package.json to 1.1.0-rc2; tag and push v1.1.0-rc2; supervise CI; assert 6-asset Release</name>
  <what-built>
    - Plan 12.1-01 publish-race fix (electron-builder.yml + build/app-update.yml)
    - Task 1 12.1-HUMAN-UAT.md scaffold
  </what-built>
  <how-to-verify>
This task pushes a real git tag that fires the production CI pipeline. The user supervises CI in real-time and validates the resulting GitHub Release. Pre-condition: Plan 12.1-01 Task 3 manual checkpoint is "approved — ready to tag rc2".

Sequence (executor runs commands; user observes; user pastes evidence below at each STOP):

1. Bump package.json version from 1.1.0-rc1 → 1.1.0-rc2 in a dedicated commit:
   ```bash
   npm version 1.1.0-rc2 --no-git-tag-version
   git add package.json package-lock.json
   git commit -m "chore(12.1): bump version 1.1.0-rc1 → 1.1.0-rc2"
   ```
   Why no-git-tag-version: we tag separately so the tag commit message is explicit.

2. Confirm release.yml's tag-version-guard will pass (per release.yml:43-54):
   ```bash
   node -p "require('./package.json').version"
   ```
   Expected output: `1.1.0-rc2` (must match the tag name minus the leading `v`).

3. Tag and push (this fires the CI workflow):
   ```bash
   git tag v1.1.0-rc2
   git push origin main
   git push origin v1.1.0-rc2
   ```

4. Supervise CI run in real-time:
   ```bash
   gh run list --workflow=release.yml --limit=1
   gh run watch  # streams logs until the run completes
   ```
   Expected: ~10-15 minutes wall time; 5 jobs (test×3 OS + build-mac + build-win + build-linux + publish); all green.

5. STOP — paste below the final `gh run view <run-id>` output (job statuses).
   Expected: `test (ubuntu-latest)`, `test (windows-2022)`, `test (macos-14)`, `build-mac`, `build-win`, `build-linux`, `publish` all show `completed - success`.

6. Atomicity assertion — confirm the publish job actually attached 6 assets:
   ```bash
   gh release view v1.1.0-rc2 --json isDraft,assets -q '{isDraft, count: (.assets | length), names: [.assets[].name]}'
   ```
   Expected:
   ```json
   {
     "isDraft": true,
     "count": 6,
     "names": [
       "Spine-Texture-Manager-1.1.0-rc2-arm64.dmg",
       "Spine-Texture-Manager-1.1.0-rc2-x64.exe",
       "Spine-Texture-Manager-1.1.0-rc2-x86_64.AppImage",
       "latest-mac.yml",
       "latest.yml",
       "latest-linux.yml"
     ]
   }
   ```
   `count: 6` is the load-bearing assertion (per RESEARCH §Validation Architecture). Anything less = publish-race recurred OR softprops upload partial.

7. Publish-race regression check — grep the workflow log for the smoking-gun strings:
   ```bash
   gh run view <run-id> --log | grep -iE "personal access token is not set|asset_already_exists|HTTP 422" || echo "CLEAN — no publish-race log lines"
   ```
   Expected: outputs `CLEAN — no publish-race log lines`. If ANY match, the publish-race fix is incomplete; STOP and revisit Plan 12.1-01.

8. Promote draft → published:
   ```bash
   gh release edit v1.1.0-rc2 --draft=false
   gh release view v1.1.0-rc2 --json isDraft -q '.isDraft'
   ```
   Expected: `false` (release is now visible to non-collaborators; needed so installed rc2 instances can detect rc3 in the next round).

Paste below: (a) Step 5 final `gh run view` job-status output, (b) Step 6 JSON output (must show count: 6), (c) Step 7 grep result (must say CLEAN), (d) Step 8 confirmation that isDraft=false.

If any step fails, STOP — do not proceed to Task 3 (rc3 tag) until rc2 is fully clean. Re-attempt root-causes against RESEARCH §Pitfall 1 / §Assumption A2.
  </how-to-verify>
  <resume-signal>Type "rc2 published — 6 assets clean" if all 4 evidence items confirm. Type "rework — <reason>" if any check failed. The next task (rc3 tag) is GATED on this — do not skip ahead.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Bump to 1.1.0-rc3; tag and push v1.1.0-rc3; supervise CI; assert 6-asset Release</name>
  <what-built>
    - rc2 published (Task 2)
    - rc2 must be installed on macOS + Linux + Windows test hosts BEFORE this task fires (per RESEARCH §Pitfall 3 — Plans 12.1-03/04/05 Step 1 each)
  </what-built>
  <how-to-verify>
**CRITICAL PRE-CONDITION (per RESEARCH §Pitfall 3):** Before pushing rc3, the user MUST have already executed Step 1 of Plans 12.1-03 (macOS install rc2), 12.1-04 (Linux install rc2), and 12.1-05 (Windows install rc2). The runbooks for those plans include "confirm Help → About reads rc2" — that confirmation MUST be paste-back-confirmed in the respective HUMAN-UAT transcripts before this task runs.

If the rc2 install on each OS hasn't happened yet, return to Plans 03/04/05 Step 1 first — installing rc3 directly defeats the entire UPD-01..UPD-04 observation goal (rc2 detects rc3, downloads, relaunches into rc3).

Sequence:

1. Confirm rc2 is installed on all 3 OSes (per the 3 runbook transcripts):
   - macOS test host: `Help → About` shows `1.1.0-rc2` (transcript line in 12.1-HUMAN-UAT.md Test 1 partial-pending)
   - Linux test host: `Help → About` shows `1.1.0-rc2`
   - Windows test host: `Help → About` shows `1.1.0-rc2`
   Paste below: confirmation line for each OS.

2. Bump package.json to 1.1.0-rc3:
   ```bash
   npm version 1.1.0-rc3 --no-git-tag-version
   git add package.json package-lock.json
   git commit -m "chore(12.1): bump version 1.1.0-rc2 → 1.1.0-rc3"
   ```

3. Tag and push:
   ```bash
   git tag v1.1.0-rc3
   git push origin main
   git push origin v1.1.0-rc3
   ```

4. Supervise CI:
   ```bash
   gh run list --workflow=release.yml --limit=1
   gh run watch
   ```

5. Atomicity assertion (same as Task 2 step 6, with rc3):
   ```bash
   gh release view v1.1.0-rc3 --json isDraft,assets -q '{isDraft, count: (.assets | length), names: [.assets[].name]}'
   ```
   Expected: `count: 6` with rc3 in all installer filenames.

6. Publish-race regression check:
   ```bash
   gh run view <run-id> --log | grep -iE "personal access token is not set|asset_already_exists|HTTP 422" || echo "CLEAN — no publish-race log lines"
   ```

7. Promote draft → published:
   ```bash
   gh release edit v1.1.0-rc3 --draft=false
   ```

8. Hand off to Plans 12.1-03/04/05 Step 2-onwards: each rc2 install should now detect rc3 within 10s of relaunch (UPD-01 startup check + 3.5s delay).

Paste below: (a) Step 1 the 3 "Help → About reads rc2" confirmations, (b) Step 5 JSON (count: 6), (c) Step 6 CLEAN result, (d) Step 7 isDraft=false confirmation.
  </how-to-verify>
  <resume-signal>Type "rc3 published — runbooks resume from Step 2" once both releases are clean and all 3 OSes have confirmed rc2 install. Then trigger Plans 12.1-03/04/05 to continue from their Step 2 (observe rc2 detects rc3).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev box → GitHub | git push of tag fires CI; tag controls which package.json version is built; tampering with tag-version mismatch is gated by release.yml:43-54 |
| GitHub Actions → GitHub Releases | publish job uploads 6 assets via softprops/action-gh-release@v2.6.2 (SHA-pinned to 3bb12739c298aeb8a4eeaf626c5b8d85266b0e65) |
| draft → published Release | gh release edit --draft=false makes assets visible to non-collaborators (needed so installed rc2 detects rc3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-02-01 | Tampering | git tag → workflow trigger | mitigate | release.yml:43-54 tag-version-guard rejects mismatched tag/package.json (catches accidental tag-without-bump); only repo collaborators can push tags |
| T-12.1-02-02 | Tampering | softprops upload action | mitigate | SHA-pinned to 3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 (supply-chain hygiene preserved from Phase 11 D-22) |
| T-12.1-02-03 | Spoofing | draft Release | accept | Drafts visible only to repo collaborators; promotion to published is explicit `gh release edit --draft=false`; auditable |
| T-12.1-02-04 | Information Disclosure | CI workflow logs | accept | Workflow runs in repo-default `permissions: contents: write` only; no extra secrets exposed; logs are public for public repos but contain no token plaintext |
| T-12.1-02-05 | Denial of Service | GitHub Actions queue | accept | Each rc tag costs ~10-15min CI wall time; concurrency group `release-${{ github.ref }}` (release.yml:21) prevents same-tag double-trigger; cost is bounded |

**Polluted-rc-cleanup capability preserved:** If either tag push produces a partial/broken Release, recovery procedure is the same as the 2026-04-27 incident (commit 44bd03b): `gh release delete v1.1.0-rcN -y` + `git tag -d v1.1.0-rcN` + `git push origin :refs/tags/v1.1.0-rcN` + `npm version <prior> --no-git-tag-version`. No special permissions required.
</threat_model>

<verification>
- 12.1-HUMAN-UAT.md greenfield scaffold exists with all 4 sections + 9 test entries + correct sequencing language.
- v1.1.0-rc2 published as non-draft Release with `count: 6` assets verified via `gh release view`.
- v1.1.0-rc3 published as non-draft Release with `count: 6` assets verified via `gh release view`.
- Both workflow run logs are CLEAN of "Personal Access Token is not set" / "asset_already_exists" / "HTTP 422".
- Pre-rc3 sanity: all 3 OS test hosts have rc2 installed (Help → About transcript) before rc3 tag fires.
</verification>

<success_criteria>
- ROADMAP SC-1 verified: rc2 produces a complete GitHub Release with all 6 assets attached atomically (no missing-asset / partial-publish state).
- rc3 published successfully — Wave 3 plans now have a live "newer version" feed to observe.
- 12.1-HUMAN-UAT.md is the live transcript surface for Wave 3.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-installer-auto-update-live-verification/12.1-02-SUMMARY.md` documenting:
- The 2 version-bump commits (rc2 + rc3) and the 2 git tags pushed
- The 2 GitHub Release URLs (rc2 + rc3) with `gh release view --json assets` JSON evidence
- The CLEAN log-grep evidence for both workflow runs
- The greenfield 12.1-HUMAN-UAT.md scaffold path + line count
- Confirmation that Plans 12.1-03 / 04 / 05 are unblocked
</output>
