# Phase 12.1: Installer + auto-update live verification - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-28
**Phase:** 12.1-installer-auto-update-live-verification
**Mode:** `/gsd-discuss-phase 12.1 --gaps` (retroactive gap-finding — 8 plans + RESEARCH + PATTERNS + VALIDATION already existed without a CONTEXT.md; the workflow surfaced 8 implicit decisions the planner had picked without explicit user input)
**Areas discussed:** Publish-race fix architecture, Tag-push safety + rc2/rc3 sequencing, Live-verification scope (Linux + screenshots), Documentation surfaces, Verification doc structure

---

## Initial Question

| Option | Description | Selected |
|--------|-------------|----------|
| Publish-race fix architecture | Gap 1: Research picked Option (a) flip publish: null + extraResources. Options (b) CLI override and (c) drop GH_TOKEN considered/rejected. Worth ratifying. | (delegated) |
| Tag/release sequencing + safety | Gaps 3+8: Pre-flight dry-run vs tag rc2 directly. Plus rc2→rc3 cadence (atomic vs incremental per-OS). | (delegated) |
| Live-verification scope (Linux + screenshots) | Gaps 2+6: Linux test host UNKNOWN — Docker terminal / real Ubuntu desktop / defer to tester. libfuse2 capture method same. | (delegated) |
| Documentation surfaces | Gaps 5+7: winCodeSign location (README vs CONTRIBUTING). INSTALL.md text-drift policy if Gatekeeper/SmartScreen wording differs. | (delegated) |

**User's choice:** "Other" — free text "i'll let you decide what's best"

**Notes:** User explicitly delegated all 4 areas (8 underlying gaps) to Claude's recommended defaults. Per `<answer_validation>` workflow rules, "Other" with empty text means freeform input; here the user provided substantive freeform input (delegation). Claude proceeded by selecting the HIGH-confidence research recommendations for each gap, cross-referenced against the existing plans to confirm the plans already implement those defaults, then ratified them in CONTEXT.md as locked decisions D-01 through D-08.

---

## Resolutions (delegated → Claude's recommended defaults)

### Publish-race fix architecture (Gap 1 → D-01)

| Option | Pros | Cons | Selected |
|--------|------|------|----------|
| (a) `publish: null` + static `build/app-update.yml` via `extraResources` | One source of truth; restores Phase 11 era working state; HIGH research confidence | Requires hand-authored static YAML file | ✓ |
| (b) `--config.publish=null` CLI override on build:* scripts | More surgical (file untouched) | Split source-of-truth (YAML says github, CLI overrides null) — confusing for future maintainers | |
| (c) Keep `publish: github` + drop `GH_TOKEN` from build env | Minimal YAML change | Rebuilds failure mode (i): publisher constructor throws on missing token; risk of build abort | |

**Rationale:** Plan 12.1-01 already implements Option (a). Research §Summary explicitly recommends (a). Restores the architecture that was working through Phase 11 Plan 11-02 close.

### Tag-push safety + rc2/rc3 sequencing (Gap 3+8 → D-02 + D-03)

| Option | Pros | Cons | Selected |
|--------|------|------|----------|
| Pre-flight workflow_dispatch dry-run BEFORE tagging rc2 | Catches publish-race regression in non-polluting context (no draft Release, no tag pollution); ~10 min CI wait is cheap insurance | Slight delay before rc2 push | ✓ (D-02) |
| Tag rc2 directly without pre-flight | Faster path to live verification | 3 prior tag-push attempts already polluted the release surface; risk of 4th polluted tag | |
| rc2 → install on ALL 3 OSes → THEN rc3 (atomic) | Single observation window for rc2→rc3 detection across all hosts; per RESEARCH Pitfall 3 | Requires coordination across OSes | ✓ (D-03) |
| rc2 → install on macOS first → fix issues → Linux → Windows → tag rc3 (incremental per-OS) | Lower coordination cost | Could miss rc2→rc3 detection on later OSes if rc3 tagged before rc2 installed there | |

**Rationale:** D-02 — User has 3 documented polluted-tag incidents (run IDs 25017095851 / 25017351602 / 25017624868); risk-aversion warranted. D-03 — RESEARCH §Pitfall 3 explicitly warns against per-OS incremental sequencing. Plan 12.1-02 Task 2 enforces atomic gating.

### Live-verification scope — Linux strategy (Gap 2+6 → D-04 + D-05)

| Option | Pros | Cons | Selected |
|--------|------|------|----------|
| Hybrid: Docker for libfuse2 PNG capture + real desktop for AppImage UPD-* (defer to v1.2 if no desktop) | Unblocks v1.1 close on screenshot deliverable; honest about UPD-* coverage gaps | Carry-forward item to v1.2 | ✓ |
| Docker-only for everything | Simplest; no host dependency | Cannot observe AppImage UPD-* lifecycle (no window manager) — would skip a contract test | |
| Real Ubuntu desktop required for both | Full UPD-* lifecycle coverage | Blocks v1.1 close on tester recruitment; unknown availability per RESEARCH Open Question 3 | |
| Defer all Linux verification to v1.2 | Simplest scope | Drops the libfuse2 PNG which IS in scope (REL-03 capture #4) | |

**Rationale:** Plan 12.1-04 Task 1 already supports both paths (a) and (b); Plan 12.1-04 Task 2 already has the deferral path documented. CONTEXT.md formalizes this as the ratified strategy.

### Documentation surfaces (Gap 5+7 → D-06 + D-07)

| Option | Pros | Cons | Selected |
|--------|------|------|----------|
| README.md `## Building on Windows` section between `## Commands (developer)` and `## Reporting issues` | Discoverability — devs already on README; one less file to maintain; project doesn't have CONTRIBUTING.md yet | Mixes tester-facing (## Installing) with dev-facing material in one file | ✓ (D-06) |
| New CONTRIBUTING.md for Windows build notes | Cleaner separation of dev vs user docs | Requires creating + maintaining a new file just for one section | |
| INSTALL.md text-drift policy: edit markdown text in same commit as binary swap when capture reveals wording mismatch | Accuracy — INSTALL.md text matches what testers actually see | Widens Plan 12-06's "binary-only swap" scope (explicitly scoped exception) | ✓ (D-07) |
| Strict binary-only swap; flag wording mismatches but don't fix in 12.1 | Preserves Plan 12-06 scope purity | INSTALL.md ships with stale wording until next phase | |

**Rationale:** Plan 12.1-07 already targets README.md (anti-pattern note explicitly rejects creating CONTRIBUTING.md). Plan 12.1-06 Sub-task B already implements the text-drift policy as a flag-gated exception (no flag = no markdown edit).

### Verification doc structure (Gap 4 → D-08)

| Option | Pros | Cons | Selected |
|--------|------|------|----------|
| Both: flip 12-VERIFICATION.md in-place AND author 12.1-VERIFICATION.md | Cleanest milestone archival; 12-VERIFICATION reflects final phase 12 state; 12.1-VERIFICATION owns 12.1's own success criteria | Two surfaces to maintain | ✓ |
| Only flip 12-VERIFICATION.md in-place | Single source of truth | 12.1's own SC-1 (publish-race fix) + SC-7 (winCodeSign doc) have nowhere to live | |
| Only author 12.1-VERIFICATION.md (don't touch 12-VERIFICATION.md) | Phase isolation | 12-VERIFICATION.md ships v1.1 with stale `human_needed` rows | |

**Rationale:** Plan 12.1-08 already does both. RESEARCH §Open Question 1 explicitly recommends this dual-surface approach.

---

## Claude's Discretion

User invoked `/gsd-discuss-phase 12.1 --gaps` and answered "i'll let you decide what's best" to the gap-area selection question. All 8 implicit decisions (D-01 through D-08) were locked using Claude's recommended defaults aligned with the HIGH-confidence research and the existing plan implementations. Within those locked decisions, executor still has discretion on:

- Exact wording of CI workflow log assertions in Plan 12.1-02 transcript
- Order of OS installs within step (3) of D-03
- Whether to ship Plan 12.1-08's carry-forward entries as a new section in REQUIREMENTS.md or as a paragraph in 12.1-VERIFICATION.md
- Exact phrasing of any `deferred_v1.2` rationales

## Deferred Ideas

(See CONTEXT.md `<deferred>` section for the full list — captured during research/planning, ratified during this discussion.)

- AppImage UPD-* live lifecycle observation on real Linux desktop (carry-forward to v1.2 if no host)
- Flipping `SPIKE_PASSED` to true on Windows (out of scope; manual-fallback ships LIVE per Phase 12 D-04)
- Windows EV cert / Apple Developer ID notarization (out of scope per v1.1 milestone)
- Polish: exact Windows manual-fallback dialog wording (file as `.planning/todos/` for v1.2 if discovered during runbook)

---

# Session 2 — Post-BLOCKED replanning (2026-04-28)

**Date:** 2026-04-28
**Mode:** `/gsd-discuss-phase 12.1` (re-invocation; no flags)
**Trigger:** Plan 12.1-01 BLOCKED at Task 3 pre-flight — RESEARCH §Assumption A6 empirically falsified. `12.1-01-BLOCKED.md` (commit `2eaaba5`) enumerates rework Options A/B/C/D and recommends this re-discuss.
**Areas discussed:** Rework architecture (interactive); Validation strategy + Cleanup + Decision-recording (delegated to Claude)

---

## Initial Question

| Option | Description | Selected |
|--------|-------------|----------|
| Rework architecture | Pick from BLOCKED.md Options A/B/C/D for the publish-race fix. Locks new D-01 (or D-09 superseding D-01). | ✓ |
| Validation strategy | Empirical pre-flight vs spike vs direct re-attempt before re-running Plan 12.1-01. | (delegated) |
| Cleanup of `release.bad-12.1-01-publish-null/` | Now / after rework verified / keep as evidence. | (delegated) |
| How to record the decision | Append D-09 superseding / amend D-01 in place. | (delegated) |

**User's choice:** "Rework architecture (Recommended), you decide" — picked rework as the area to discuss interactively; delegated the other 3 to Claude's recommended defaults.

---

## Resolutions

### Rework architecture (Gap 1 → D-09 supersedes D-01)

| Option | Pros | Cons | Complexity | Selected |
|--------|------|------|------------|----------|
| A: `publish: never` | 1-character edit; preserves `679cffc` static `app-update.yml` + `extraResources` architecture; aligned with electron-builder's documented "construct publishers but don't auto-upload" semantics | Documented behavior is fuzzy on whether publisher constructor still throws on missing `GH_TOKEN`; needs empirical pre-flight | LOW | ✓ |
| B: Restore `publish: github` | None — BLOCKED.md framing as "Phase-11-working" is incorrect (Phase 11 didn't ship `latest-*.yml`; Phase 12 added the requirement, hit the per-artifact race) | This is exactly the config that burned 3 prior CI runs (25017095851 / 25017351602 / 25017624868). No clean way to suppress per-artifact publisher construction. | LOW | |
| C: `publish: null` + post-build synthesizer script | Fully decoupled from electron-builder publisher logic; auditable; no risk of electron-builder version drift breaking it | New ~30 LOC script; schema drift risk if electron-updater adds required fields | MEDIUM | (fallback if A fails) |
| D: `provider: generic` + hosted feed URL | Generates files; no GH auth surface | Heaviest rework; new GitHub Pages surface; changes runtime feed semantics; bundled `app-update.yml` also has to switch | HIGH | (fallback if C fails) |

**User's choice:** "Other" — free text "you decide"

**Notes:** User explicitly delegated the 4-way pick to Claude. Claude locked Option A as D-09 superseding D-01, with documented fallback ordering A → C → D (skipping B). Rationale: A is the lowest-cost path (1-char change), preserves the additive improvements from commit `679cffc`, and BLOCKED.md's recommendation already pointed at A as "the lowest-cost path if it works." Validation gate is mandatory — no commit until local pre-flight confirms `release/latest-mac.yml` is generated under `publish: never` with valid sha512/size/version fields.

### Validation strategy (delegated → Claude's recommended default)

| Option | Pros | Cons | Selected |
|--------|------|------|----------|
| Local pre-flight (Task 3 re-run) gates code commit | Cheap (~2 min); falsifies fast; same protocol that caught the original A6 failure | Local-only — doesn't catch CI-environment regressions | ✓ |
| Workflow_dispatch CI dry-run gates code commit | Catches CI-environment regressions (env vars, runner specifics) | Slow (~10 min CI); harder to iterate on | (kept as D-02 second gate, not first gate) |
| Direct re-attempt without pre-flight | Fastest path | Risks 4th polluted tag; user explicitly didn't want this | |
| Small spike on a feature branch before any commit | Most rigorous | Heavy ceremony for a 1-char change | |

**Selected:** Local Task 3 pre-flight as primary gate; existing D-02 workflow_dispatch CI dry-run preserved as secondary gate before tagging rc2. Defense in depth: local catches generation failure, CI catches upload-race regression.

### Cleanup of `release.bad-12.1-01-publish-null/` (delegated → Claude's recommended default)

| Option | Pros | Cons | Selected |
|--------|------|------|----------|
| Defer until rc2 CI green, then atomic cleanup commit | Preserves evidence trail; no destructive action while replanning | Untracked dir lingers in working tree for ~1 day | ✓ |
| Delete now | Cleanest working tree | Loses Build 1/Build 2 evidence pointed to from BLOCKED.md | |
| Keep permanently as committed evidence | Audit trail in repo | Pollutes repo with build artifacts (.dmg, .blockmap) — gitignored anyway | |

**Selected:** Defer until rc2 CI green, then `chore(12.1-01): clean up post-BLOCKED evidence artifacts` atomic commit during rc2 install observations.

### How to record the decision (delegated → Claude's recommended default)

| Option | Pros | Cons | Selected |
|--------|------|------|----------|
| Append D-09 superseding D-01 + status footnote on D-01 | Preserves audit trail of BLOCKED → unblocked transition; D-01's rationale stays intact for future reference | Two decision IDs about the same thing — minor duplication | ✓ |
| Amend D-01 in place (rewrite history) | Cleanest single decision | Loses the "we tried publish: null and it failed" context; future readers can't tell why publish: never was chosen | |

**Selected:** Append D-09 with cross-reference to D-01's status block; updated CONTEXT.md trailing date with "Updated 2026-04-28 (post-BLOCKED replanning)".

---

## Claude's Discretion

User invoked `/gsd-discuss-phase 12.1` (no flags) and answered "you decide" for both the substantive rework architecture and the 3 procedural sub-decisions. Per workflow rules, this is freeform input → delegation to recommended defaults. Claude proceeded with:

- D-09 = Option A (`publish: never`) + documented A → C → D fallback ordering
- Validation = local pre-flight as primary gate, D-02 workflow_dispatch as secondary gate
- Cleanup = deferred until rc2 CI green
- Recording = append D-09 + status block on D-01 (no history rewrite)

Within these locked decisions, planner replan retains discretion on:

- Exact format of the local pre-flight assertion (npm script / vitest test / inline shell in Task 3 resume-signal)
- Which atomic commit lands the `publish: null` → `publish: never` 1-char edit (likely a single new commit on top of `679cffc`)
- Whether to update `12.1-01-PLAN.md` Task 3 success-criterion text in the same commit or as a separate `docs(12.1-01)` commit before the code change

## Deferred / Escalation

- **Option C (post-build synthesizer script)** — escalation path #1 if D-09 (`publish: never`) fails local pre-flight. ~30 LOC new script `scripts/write-update-feed.mjs`. Would replan as new Plan 12.1-01-v3.
- **Option D (`provider: generic` + GitHub Pages feed URL)** — escalation path #2 if both A and C fail. Heaviest rework; new GH Pages surface; runtime feed URL changes.
- **Option B (restore `publish: github`)** — explicitly skipped. This is the falsified config that burned 3 prior CI runs.
