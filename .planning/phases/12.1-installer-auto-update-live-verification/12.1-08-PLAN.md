---
phase: 12.1
plan: 08
type: execute
wave: 5
depends_on: [12.1-01, 12.1-02, 12.1-03, 12.1-04, 12.1-05, 12.1-06, 12.1-07]
files_modified:
  - .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md
  - .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - UPD-06
  - REL-03
  - SC-8  # ROADMAP success criterion 8: 12-VERIFICATION.md human_needed → passed
must_haves:
  truths:
    - "12-VERIFICATION.md frontmatter `status: human_needed` flipped to `status: passed` (or `status: passed_partial` if any item explicitly carried over to v1.2)"
    - "12-VERIFICATION.md frontmatter `human_verification:` array — each of the 9 entries either has `result: passed` + `verified_in: 12.1-VERIFICATION.md` + `transcript:` appended (PRESERVE-HISTORY flip per PATTERNS.md), OR the array is drained to `[]` AND the body's `### Human Verification Required` section is rewritten to reference 12.1-VERIFICATION.md"
    - "12-VERIFICATION.md frontmatter `re_verification:` field is populated with `{ from: 12.1-VERIFICATION.md, reason: '9 human-verified items completed during 12.1 tester rounds' }`"
    - "Greenfield 12.1-VERIFICATION.md authored at .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md per PATTERNS.md §12.1-VERIFICATION.md (G) frontmatter + body section pattern"
    - "12.1-VERIFICATION.md documents the 8 ROADMAP success criteria (SC-1 through SC-8) with evidence (transcripts from 12.1-HUMAN-UAT.md + `gh release view` JSON for SC-1 + grep output for SC-7 + file-size check for SC-6 + commit SHAs for SC-2/3/4)"
    - "Any items explicitly carried over to v1.2 (e.g. Linux desktop UPD-* lifecycle if no Linux desktop available per Plan 12.1-04 Task 2 deferral) are documented in BOTH 12.1-VERIFICATION.md AND a new entry in `Out of Scope (v1.1)` or `Future Requirements (deferred)` of REQUIREMENTS.md as appropriate"
    - "STATE.md updated: phase status flipped from `phase_descoped` (current Phase 13 status) to reflect Phase 12.1 closure; Last completed bumped; v1.1 milestone marked as fully verified"
    - "ROADMAP.md Phase 12.1 entry: change `**Plans:** 0 plans` → `**Plans:** 8 plans`; uncheck/check the plans list to show all 8 done; mark phase as `[x]` complete"
    - "vitest 434/434 still passing (no test surfaces touched)"
  artifacts:
    - path: ".planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md"
      provides: "Updated Phase 12 verification report with all 9 human_needed items flipped to passed (or carried-forward) with re_verification provenance pointing at 12.1-VERIFICATION.md"
      contains: "status: passed"
    - path: ".planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md"
      provides: "Greenfield Phase 12.1 verification report covering the 8 ROADMAP SC + evidence transcripts"
      contains: "Phase 12.1"
    - path: ".planning/STATE.md"
      provides: "Updated state reflecting Phase 12.1 closure + v1.1 milestone fully verified"
      contains: "Phase 12.1"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 12.1 entry with 8/8 plans complete and phase marked done"
      contains: "Phase 12.1"
  key_links:
    - from: "12.1-VERIFICATION.md SC-1 evidence"
      to: "Plan 12.1-02 SUMMARY.md gh release view JSON output"
      via: "transcript reference"
      pattern: "count: 6"
    - from: "12.1-VERIFICATION.md SC-2/3/4 evidence"
      to: "12.1-HUMAN-UAT.md Tests 1/2/5 transcript lines"
      via: "result: passed + transcript"
      pattern: "passed"
    - from: "12-VERIFICATION.md re_verification field"
      to: "12.1-VERIFICATION.md (this plan's deliverable)"
      via: "frontmatter cross-reference"
      pattern: "from: 12.1-VERIFICATION.md"
---

<objective>
Close out Phase 12.1 + the v1.1 milestone: flip all 9 `human_needed` items in 12-VERIFICATION.md to `passed` (or carry-forward to v1.2 with rationale), author a greenfield 12.1-VERIFICATION.md documenting the 8 ROADMAP success criteria + their evidence, and update STATE.md + ROADMAP.md to reflect Phase 12.1 closure + v1.1 fully verified.

Purpose: ROADMAP SC-8 closure. Bridge Phase 12's `human_needed` deferral to Phase 12.1's actual transcripts. Per RESEARCH §Open Question 1: ship TWO complementary verification surfaces (back-fold flip in 12-VERIFICATION.md AND new 12.1-VERIFICATION.md owning 12.1's own success criteria) — they are complementary, not conflicting.

Output: 4 file edits in 1-2 atomic commits. No code surfaces touched. Final state: v1.1 milestone is fully verified end-to-end (Phases 10-12 + 12.1 all closed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-RESEARCH.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-PATTERNS.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-01-SUMMARY.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-02-SUMMARY.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-03-SUMMARY.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-04-SUMMARY.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-05-SUMMARY.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-06-SUMMARY.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-07-SUMMARY.md
@.planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md

<interfaces>
<!-- Verification frontmatter shape + body section list. -->

12-VERIFICATION.md frontmatter (current state — lines 1-36):
- `status: human_needed`  → flip to `status: passed` (or `status: passed_partial` if any carry-forward)
- `verified: 2026-04-27T23:42:00Z` → leave as-is OR add `re_verified: <new ISO-8601>` field below it
- `re_verification: null  # Initial verification — no prior VERIFICATION.md` → replace with structured object: `re_verification: { from: 12.1-VERIFICATION.md, reason: "9 human-verified items completed during 12.1 tester rounds (Plans 12.1-03/04/05 transcripts + 12.1-06 PNG swap completion)" }`
- `human_verification:` array (9 entries) — flip per PATTERNS.md §12-VERIFICATION.md PRESERVE-HISTORY variant (recommended for milestone archival): each entry GAINS `result: passed` + `verified_in: 12.1-VERIFICATION.md` + `transcript: "<one-line evidence>"` fields appended

12-VERIFICATION.md body sections — minimal updates:
- `### Human Verification Required` paragraph (lines ~162-174) — REWRITE to "All 9 items verified during 12.1 tester rounds; see 12.1-VERIFICATION.md for transcript and 12.1-HUMAN-UAT.md for per-step evidence."
- All other body sections (Goal Achievement, Observable Truths, Required Artifacts, Key Link Verification, Data-Flow Trace, Behavioral Spot-Checks, Requirements Coverage, Anti-Patterns Found, Gaps Summary) — leave unchanged. The codebase-level evidence is permanent.

Greenfield 12.1-VERIFICATION.md frontmatter shape (per PATTERNS.md §12.1-VERIFICATION.md (G) — mirrors 12-VERIFICATION.md frontmatter):
```yaml
---
phase: 12.1-installer-auto-update-live-verification
verified: <ISO-8601 of plan completion>
status: passed  # OR passed_partial if any v1.2 carry-forward
score: 8/8 ROADMAP success criteria verified  # OR 7/8 with carry-forward note
overrides_applied: 0
re_verification: null  # 12.1 is its own initial verification (12-VERIFICATION.md re-verification is recorded in THAT file's frontmatter)
human_verification: []  # all human items folded back into 12-VERIFICATION.md per PRESERVE-HISTORY pattern
---
```

12.1-VERIFICATION.md body sections (mirrors 12-VERIFICATION.md exact heading list per PATTERNS.md):
- `# Phase 12.1: Installer + auto-update live verification — Verification Report`
- `**Phase Goal:**` quote of ROADMAP §Phase 12.1 Goal verbatim
- `## Goal Achievement` paragraph
- `### Observable Truths (from ROADMAP Success Criteria SC-1 through SC-8)` — table mapping each SC to evidence
- `### Required Artifacts` — table listing each plan's deliverables (electron-builder.yml + build/app-update.yml + 4 PNGs + README ## Building on Windows + 12.1-HUMAN-UAT.md transcripts)
- `### Key Link Verification` — table for the publish-race fix linkage + auto-update detect/relaunch chain + UpdateDialog windows-fallback chain
- `### Behavioral Spot-Checks` — table with `gh release view` JSON for SC-1 + grep outputs for SC-6/7
- `### Requirements Coverage` — table with UPD-06 + REL-03 + SC-1..SC-8 status
- `### Anti-Patterns Found` — likely empty (or minor); 12.1 is mostly verification + docs
- `### Human Verification Required` — empty (the 9 items are folded back into 12-VERIFICATION.md)
- `### Gaps Summary` — paragraph documenting any v1.2 carry-forwards (e.g. Linux desktop UPD-* if Plan 12.1-04 Task 2 was deferred)
- Trailing italics: `_Verified: <ts>_` / `_Verifier: Claude (gsd-verifier or gsd-executor)_`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author greenfield 12.1-VERIFICATION.md (8 ROADMAP SC + evidence)</name>
  <read_first>
    - .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md (FULL file — the analog being mirrored; 192 lines; frontmatter + 12 body sections)
    - .planning/phases/12.1-installer-auto-update-live-verification/12.1-PATTERNS.md (§12.1-VERIFICATION.md (G) — Frontmatter shape + Body section pattern + What 12.1's verification should record)
    - .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md (FULL file — source of test transcript evidence)
    - All 7 prior SUMMARY files (12.1-01 through 12.1-07) — source of evidence for the SC tables
    - .planning/ROADMAP.md (lines 130-150 — the 8 ROADMAP success criteria for Phase 12.1, verbatim)
  </read_first>
  <files>.planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md</files>
  <action>
Create `.planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md` with the following structure (frontmatter + body sections mirroring 12-VERIFICATION.md):

**Frontmatter:**
```yaml
---
phase: 12.1-installer-auto-update-live-verification
verified: <ISO-8601 timestamp at task execution>
status: passed  # OR passed_partial — pick based on evidence
score: 8/8 ROADMAP success criteria verified  # OR 7/8 with carry-forward — pick based on evidence
overrides_applied: 0
re_verification: null
human_verification: []
---
```

**Body sections (mirror 12-VERIFICATION.md exact heading order):**

1. `# Phase 12.1: Installer + auto-update live verification — Verification Report`

2. `**Phase Goal:**` block — quote ROADMAP.md §Phase 12.1 Goal verbatim:
   > "Close the v1.1 distribution surface end-to-end: a tagged release publishes successfully via CI without the electron-builder 26.x publish race; an installed Windows / macOS / Linux app detects a newer published release and walks the user through update-or-fallback; INSTALL.md ships with real screenshots of Gatekeeper / SmartScreen / libfuse2 dialogs captured during the same tester round; and the local Windows `npm run build` papercut (winCodeSign symlink extract) is documented with a Developer-Mode prerequisite. Closes the 9 `human_needed` items in 12-VERIFICATION.md so v1.1 can be archived as fully verified."

3. `## Goal Achievement` — one-paragraph summary stating Phase 12.1 closed all 8 ROADMAP success criteria (or 7/8 with carry-forward), v1.1 milestone is fully verified end-to-end, and back-folded the 9 human_needed items in 12-VERIFICATION.md to `passed`.

4. `### Observable Truths (from ROADMAP Success Criteria SC-1 through SC-8)` — table:

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| SC-1 | CI publish-race fix landed; v1.1.0-rc2 (and rc3) tag push produces complete 6-asset GitHub Release atomically | VERIFIED | Plan 12.1-01 (electron-builder.yml + build/app-update.yml); Plan 12.1-02 SUMMARY.md `gh release view --json assets` count: 6 for rc2 + rc3; workflow logs CLEAN of "Personal Access Token is not set" / "asset_already_exists" |
| SC-2 | macOS auto-update happy path verified live (rc2 → rc3 detect + download + relaunch) | VERIFIED (or DEFERRED if not run) | Plan 12.1-03 SUMMARY.md transcript; 12.1-HUMAN-UAT.md Test 1 result: passed |
| SC-3 | Linux auto-update happy path verified live (rc2 → rc3 via AppImage) | VERIFIED (or DEFERRED with carry-forward to v1.2 if no Linux desktop available per Plan 12.1-04 Task 2) | Plan 12.1-04 SUMMARY.md transcript; 12.1-HUMAN-UAT.md Test 2 result |
| SC-4 | Windows manual-fallback verified live (windows-fallback variant + Open Release Page → external browser; no nag loop) | VERIFIED | Plan 12.1-05 SUMMARY.md transcript; 12.1-HUMAN-UAT.md Test 5 result: passed |
| SC-5 | Offline + "no newer version" + first-launch dialogs eyeballed; INSTALL.md wording matches captured dialogs (or wording corrections applied in same commit per Plan 12.1-06 Task 2 Sub-task B) | VERIFIED | 12.1-HUMAN-UAT.md Tests 3, 4, 6, 7, 8 results; Plan 12.1-06 SUMMARY for any wording fixes applied |
| SC-6 | INSTALL.md screenshots captured (4 binary PNG swaps from placeholders to real captures; 4 stale italic lines deleted; new size-soft-gate test passes) | VERIFIED | Plans 12.1-03/04/05 binary commits + Plan 12.1-06 SUMMARY.md; tests/integration/install-md.spec.ts new test "Each screenshot is a real capture" passes; ≥ 5KB per file |
| SC-7 | Windows local-build papercut documented in README.md `## Building on Windows` section | VERIFIED | Plan 12.1-07 SUMMARY.md; `grep -c "Developer Mode" README.md` returns ≥ 1 |
| SC-8 | 12-VERIFICATION.md `human_needed` items flipped to `passed` (or explicitly carried over to v1.2 with rationale) | VERIFIED | This plan's Task 2 — back-folded to 12-VERIFICATION.md frontmatter; re_verification field populated |

5. `### Required Artifacts` — table listing each plan's deliverables (12-15 rows):

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `electron-builder.yml` | `publish: null` + extraResources entry | VERIFIED | Plan 12.1-01 Task 2; `grep -c "^publish: null$"` returns 1 |
| `build/app-update.yml` | Static feed config (4 lines) | VERIFIED | Plan 12.1-01 Task 1 |
| `.github/workflows/release.yml` | Unchanged from Phase 12 (single load-bearing softprops upload) | UNCHANGED | release.yml:151 softprops/action-gh-release@v2.6.2 still the single upload |
| `package.json` | Bumped to 1.1.0-rc3 | VERIFIED | Plan 12.1-02 Tasks 2 + 3 |
| GitHub Release v1.1.0-rc2 | 6 assets (3 installers + 3 latest*.yml) | VERIFIED | Plan 12.1-02 Task 2 step 6 |
| GitHub Release v1.1.0-rc3 | 6 assets | VERIFIED | Plan 12.1-02 Task 3 step 5 |
| `12.1-HUMAN-UAT.md` | 9 tests with transcripts | VERIFIED | Plans 12.1-02 (scaffold) + 12.1-03/04/05 (transcripts) |
| `docs/install-images/macos-gatekeeper-open-anyway.png` | ≥ 5KB real capture | VERIFIED | Plan 12.1-03 Task 1 |
| `docs/install-images/windows-smartscreen-more-info.png` | ≥ 5KB real capture | VERIFIED | Plan 12.1-05 Task 1 |
| `docs/install-images/windows-smartscreen-run-anyway.png` | ≥ 5KB real capture | VERIFIED | Plan 12.1-05 Task 1 |
| `docs/install-images/linux-libfuse2-error.png` | ≥ 5KB real capture | VERIFIED | Plan 12.1-04 Task 1 |
| `INSTALL.md` | 4 stale italic lines deleted; optional wording fixes | VERIFIED | Plan 12.1-06 Task 2 |
| `tests/integration/install-md.spec.ts` | New "real capture" size soft-gate test | VERIFIED | Plan 12.1-06 Task 1 |
| `README.md` | New `## Building on Windows` section | VERIFIED | Plan 12.1-07 Task 1 |
| `.planning/todos/resolved/<wincodesign-todo>.md` | Moved from pending/ via git mv | VERIFIED | Plan 12.1-07 Task 2 |

6. `### Key Link Verification` — same table format as 12-VERIFICATION.md (3-5 rows for the publish-race fix linkage + auto-update chain + windows-fallback chain).

7. `### Behavioral Spot-Checks` — table:
| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| vitest full suite passes | `npm run test` | 434 passed | PASS |
| INSTALL.md has 0 "Screenshot pending" | `grep -c "Screenshot pending" INSTALL.md` | 0 | PASS |
| All 4 PNGs ≥ 5KB | `for f in docs/install-images/*.png; do stat -f%z "$f"; done` | all > 5000 | PASS |
| README has Building on Windows | `grep -c "^## Building on Windows$" README.md` | 1 | PASS |
| electron-builder.yml has `publish: null` | `grep "^publish:" electron-builder.yml` | `publish: null` | PASS |
| build/app-update.yml exists with 4 lines | `wc -l build/app-update.yml` | 4 | PASS |
| 12-VERIFICATION.md status: passed | `grep "^status:" .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md` | `status: passed` | PASS |

8. `### Requirements Coverage` — table with UPD-06 (live) + REL-03 (live screenshots) + SC-1..SC-8.

9. `### Anti-Patterns Found` — list any minor issues (likely empty or 1-2 items if Plan 12.1-06 transcribe-wording had to be applied).

10. `### Human Verification Required` — paragraph: "Empty. The 9 human-verified items from Phase 12 are folded back into 12-VERIFICATION.md frontmatter `human_verification:` array per PRESERVE-HISTORY pattern (see PATTERNS.md §12-VERIFICATION.md flip pattern); each entry now carries `result: passed` + `verified_in: 12.1-VERIFICATION.md` + `transcript:` evidence fields."

11. `### Gaps Summary` — paragraph documenting any v1.2 carry-forwards (e.g. if Plan 12.1-04 Task 2 was deferred for no Linux desktop access, document the carry-forward here AND ensure REQUIREMENTS.md or ROADMAP.md "Deferred" section also mentions it).

12. Trailing footer:
```
---

_Verified: <ISO-8601>_
_Verifier: Claude (gsd-executor)_
```

Anti-pattern guards:
- DO NOT recreate the codebase-level evidence already in 12-VERIFICATION.md — 12.1-VERIFICATION.md focuses on the 8 NEW success criteria + back-fold provenance.
- DO NOT touch any code surfaces (no src/ changes).
- 12.1-VERIFICATION.md `re_verification:` is `null` — 12.1 is its own initial verification; the re-verification of Phase 12 is recorded in 12-VERIFICATION.md's `re_verification:` field (Task 2).
  </action>
  <verify>
    <automated>test -f .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md && grep -q "^phase: 12.1-installer-auto-update-live-verification$" .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md && grep -qE "^status: (passed|passed_partial)$" .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md && grep -q "Phase 12.1" .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md && grep -qE "SC-1.*VERIFIED" .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md && grep -qE "SC-8.*VERIFIED" .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md && grep -qE "SC-7.*VERIFIED" .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md && [ $(grep -c "^### " .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md) -ge 6 ] && grep -q "^## Goal Achievement$" .planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `.planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md`
    - Frontmatter has `phase: 12.1-installer-auto-update-live-verification`, `status: passed` (or `passed_partial`), `human_verification: []`
    - Body has `# Phase 12.1: …` title + `**Phase Goal:**` quote
    - Body has `## Goal Achievement` section
    - Body has at least 6 `### ` subsections covering Observable Truths, Required Artifacts, Key Link Verification, Behavioral Spot-Checks, Requirements Coverage, Gaps Summary
    - SC-1, SC-7, SC-8 (the 3 most-critical success criteria) all show `VERIFIED` in the table
    - Body references each Plan 12.1-01..07 SUMMARY.md as evidence source
  </acceptance_criteria>
  <done>Greenfield 12.1-VERIFICATION.md committed with the 8 ROADMAP SC verified + evidence transcripts + back-fold pointer to 12-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Flip 12-VERIFICATION.md `human_needed` → `passed` (back-fold per PRESERVE-HISTORY pattern)</name>
  <read_first>
    - .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md (FULL file — frontmatter lines 1-36 + body sections)
    - .planning/phases/12.1-installer-auto-update-live-verification/12.1-PATTERNS.md (§12-VERIFICATION.md (M) — The flip pattern + PRESERVE-HISTORY variant + Concrete excerpt)
    - .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md (the 9 transcript lines that supply the `transcript:` field for each entry)
  </read_first>
  <files>.planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md</files>
  <action>
Open 12-VERIFICATION.md. Apply 3 frontmatter edits + 1 body section rewrite per PATTERNS.md §12-VERIFICATION.md PRESERVE-HISTORY variant.

**Frontmatter edit 1 — flip status (line 4):**
```yaml
# BEFORE:
status: human_needed
# AFTER:
status: passed  # OR passed_partial if any item is carried-forward to v1.2 (Plan 12.1-04 Task 2 may have deferred Linux desktop UPD-*)
```

**Frontmatter edit 2 — populate re_verification field (line 7):**
```yaml
# BEFORE:
re_verification: null  # Initial verification — no prior VERIFICATION.md
# AFTER:
re_verification:
  from: 12.1-VERIFICATION.md
  reason: "9 human-verified items completed during phase 12.1 tester rounds (Plans 12.1-03/04/05 transcripts + 12.1-06 binary PNG swap completion). v1.1 milestone fully verified end-to-end."
```

**Frontmatter edit 3 — append per-entry result/verified_in/transcript fields to each of the 9 `human_verification:` array entries (lines 8-36):**

For each of the 9 entries, ADD 3 new fields (do NOT remove existing test/expected/why_human fields). Example for entry 1:

```yaml
# BEFORE (lines 9-11):
- test: "macOS auto-update happy path (UPD-01..UPD-04, ROADMAP SC #1)"
  expected: "Install v1.1.0 .dmg → publish v1.1.1 → relaunch v1.1.0 → modal appears with v1.1.1 release-notes Summary → click Download+Restart → app relaunches into v1.1.1; declining (Later) does not re-prompt on next startup"
  why_human: "Requires real CI publish of a newer release with latest-mac.yml feed file AND a packaged .dmg installed on real macOS — not synthesizable in vitest. Currently blocked by deferred CI publish-race (rc2 was never published with full artifact set; phase 12.1 owns the publish-race fix)."

# AFTER:
- test: "macOS auto-update happy path (UPD-01..UPD-04, ROADMAP SC #1)"
  expected: "Install v1.1.0 .dmg → publish v1.1.1 → relaunch v1.1.0 → modal appears with v1.1.1 release-notes Summary → click Download+Restart → app relaunches into v1.1.1; declining (Later) does not re-prompt on next startup"
  why_human: "Requires real CI publish of a newer release with latest-mac.yml feed file AND a packaged .dmg installed on real macOS — not synthesizable in vitest."
  result: passed
  verified_in: 12.1-VERIFICATION.md
  transcript: "<one-line tester evidence — extract from 12.1-HUMAN-UAT.md Test 1 transcript line; e.g. 'rc2 → rc3 detected within 8s of relaunch on macOS 14; Download+Restart relaunched into rc3; Help→About reads 1.1.0-rc3'>"
```

Apply the same `result:` + `verified_in:` + `transcript:` append pattern to all 9 entries. The `transcript:` value for each comes from the corresponding 12.1-HUMAN-UAT.md test entry's transcript line (Plans 12.1-03/04/05 wrote those during their Wave 3 execution).

For any entry that ends up `[deferred — carry-forward to v1.2]` (e.g. Linux desktop UPD-* per Plan 12.1-04 Task 2 deferral), use `result: deferred_v1_2` instead of `passed`, and the `transcript:` line documents the deferral rationale.

**Body edit — rewrite `### Human Verification Required` section (lines 162-174):**

```markdown
# BEFORE (per current 12-VERIFICATION.md lines 162-174):
### Human Verification Required

See `human_verification` frontmatter array. 9 items routed for live tester-round verification:

1. **macOS auto-update happy path (UPD-01..UPD-04, ROADMAP SC #1)** — needs packaged .dmg + published rc2 with latest-mac.yml feed (phase 12.1).
[... 8 more bulleted items ...]

# AFTER:
### Human Verification Required

All 9 items verified during Phase 12.1 tester rounds. See `.planning/phases/12.1-installer-auto-update-live-verification/12.1-VERIFICATION.md` for the full Phase 12.1 verification report and `.planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md` for per-step tester transcripts. The `human_verification` frontmatter array above retains each item's original `expected:` + `why_human:` text plus appended `result:` + `verified_in:` + `transcript:` fields per the PRESERVE-HISTORY flip pattern.
```

**Anti-pattern guards (per PATTERNS.md):**
- DO NOT remove the original `test:` / `expected:` / `why_human:` fields — the PRESERVE-HISTORY variant keeps them as historical context.
- DO NOT touch the body sections OTHER than `### Human Verification Required` — the codebase-level evidence (Goal Achievement, Observable Truths, Required Artifacts, Key Link Verification, Data-Flow Trace, Behavioral Spot-Checks, Requirements Coverage, Anti-Patterns Found, Gaps Summary) is permanent.
- DO NOT modify the trailing `_Verified: 2026-04-27T23:42:00Z_` / `_Verifier: Claude (gsd-verifier)_` footer — that records the original Phase 12 verification timestamp; 12.1's re-verification timestamp is implicit via the `re_verification:` frontmatter field.
- DO NOT change the `verified:` field — it records the ORIGINAL Phase 12 verification timestamp; the re_verification field carries the 12.1 timestamp implicitly through the cross-reference to 12.1-VERIFICATION.md.
  </action>
  <verify>
    <automated>grep -E "^status: (passed|passed_partial)$" .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md && grep -q "from: 12.1-VERIFICATION.md" .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md && [ $(grep -c "result: passed" .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md) -ge 5 ] && [ $(grep -c "verified_in: 12.1-VERIFICATION.md" .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md) -ge 5 ] && grep -q "All 9 items verified during Phase 12.1" .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md && ! grep -q "^status: human_needed$" .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `status: human_needed` is gone (replaced with `passed` or `passed_partial`)
    - Frontmatter `re_verification:` field is now an object with `from: 12.1-VERIFICATION.md`
    - At least 5 of the 9 `human_verification:` array entries have `result: passed` + `verified_in: 12.1-VERIFICATION.md` + `transcript:` appended (allowing for up to 4 deferred entries — typical max if Linux desktop UPD-* carries forward)
    - Body section `### Human Verification Required` rewritten to reference 12.1-VERIFICATION.md
    - All other body sections unchanged
    - Original `verified:` timestamp preserved
    - YAML frontmatter still parses (no malformed indentation introduced by appending fields)
  </acceptance_criteria>
  <done>12-VERIFICATION.md back-fold complete: status flipped to passed (or passed_partial), re_verification field populated, all 9 array entries enriched with PRESERVE-HISTORY result/verified_in/transcript fields, Human Verification Required body section rewritten.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md + ROADMAP.md to reflect Phase 12.1 closure + v1.1 fully verified</name>
  <read_first>
    - .planning/STATE.md (read FULL — lines 1-200 of header + decisions + current phase status)
    - .planning/ROADMAP.md (read FULL — focus on Phase 12.1 entry at lines 130-150 + Progress table at lines 100-107)
  </read_first>
  <files>
    - .planning/STATE.md
    - .planning/ROADMAP.md
  </files>
  <action>
**STATE.md edits:**

1. Update frontmatter:
   - `status: phase_descoped` → `status: milestone_complete` (or `phase_closed` if a more granular state is preferred — pick the value that reflects v1.1 milestone closure)
   - `progress.total_plans: 11` → `progress.total_plans: 19` (11 from Phases 10-12 + 8 from Phase 12.1)
   - `progress.completed_plans: 11` → `progress.completed_plans: 19`
   - `progress.percent: 100` stays at 100
   - `last_updated:` → bump to current ISO-8601

2. Update `## Current phase` section to reflect Phase 12.1 closure:
   ```
   Phase 12.1 — Installer + auto-update live verification — CLOSED 8/8 plans complete <ISO-date>. v1.1 milestone fully verified end-to-end (Phases 10 + 11 + 12 + 12.1). Phase 13 (Crash + error reporting) remains DESCOPED to v1.2.
   ```

3. Update `## Last completed` section to summarize Phase 12.1 (mirror the format used for prior phase completions; cite each plan's commit SHA from the SUMMARY files):
   - Plan 12.1-01 (publish-race fix) — 2 file edits, 1 atomic commit
   - Plan 12.1-02 (rc2 + rc3 tag-push supervision + 12.1-HUMAN-UAT scaffold) — 2 version bumps, 2 GitHub Releases each with 6 assets, 1 greenfield runbook
   - Plans 12.1-03/04/05 (per-OS HUMAN-UAT runbooks) — 4 PNG captures, 12.1-HUMAN-UAT.md tests flipped, 9 human_needed items observed live
   - Plan 12.1-06 (binary PNG swap finalization + size-soft-gate test) — INSTALL.md italic deletions + new test, vitest 433 → 434
   - Plan 12.1-07 (winCodeSign README documentation) — README ## Building on Windows section + todo move pending → resolved
   - Plan 12.1-08 (this plan — verification flip + greenfield 12.1-VERIFICATION.md authoring + STATE/ROADMAP updates)

4. Add a `## Decisions` entry (or append to existing) documenting Phase 12.1's load-bearing decision: chose Option (a) static `app-update.yml` via `extraResources` from RESEARCH §Summary §Three fix architectures (over Option b CLI override + Option c GH_TOKEN unset).

**ROADMAP.md edits:**

1. Phase 12.1 entry — change `**Plans:** 0 plans` to `**Plans:** 8 plans` and replace the placeholder Plans list:
   ```
   # BEFORE:
   Plans:
   - [ ] TBD (run /gsd-plan-phase 12.1 to break down)

   # AFTER:
   Plans:
   - [x] 12.1-01-PLAN.md — Publish-race fix (electron-builder.yml publish: null + static build/app-update.yml via extraResources) (Wave 1)
   - [x] 12.1-02-PLAN.md — Tag rc2 + rc3 supervision + 6-asset Release atomicity assertion + greenfield 12.1-HUMAN-UAT scaffold (Wave 2, has BLOCKING checkpoints)
   - [x] 12.1-03-PLAN.md — macOS HUMAN-UAT runbook (UPD-01..UPD-04 + Gatekeeper PNG capture + offline + up-to-date + Help→Installation Guide) (Wave 3, has BLOCKING checkpoints)
   - [x] 12.1-04-PLAN.md — Linux AppImage HUMAN-UAT runbook (UPD-01..UPD-04 + libfuse2 PNG capture + offline + up-to-date + Help→Installation Guide) (Wave 3, has BLOCKING checkpoints)
   - [x] 12.1-05-PLAN.md — Windows HUMAN-UAT runbook (windows-fallback variant verification + 2 SmartScreen PNG captures + offline + up-to-date + no-nag-loop) (Wave 3, has BLOCKING checkpoints)
   - [x] 12.1-06-PLAN.md — Binary PNG swap finalization (4 stale italic line deletions in INSTALL.md + new size-soft-gate test) (Wave 4, autonomous)
   - [x] 12.1-07-PLAN.md — README ## Building on Windows section + winCodeSign todo move pending → resolved (Wave 4, autonomous)
   - [x] 12.1-08-PLAN.md — 12-VERIFICATION.md back-fold flip + greenfield 12.1-VERIFICATION.md + STATE/ROADMAP closure updates (Wave 5, autonomous)
   ```

2. Mark Phase 12.1 as complete: change `### Phase 12.1: Installer + auto-update live verification (INSERTED)` to add `(INSERTED, COMPLETE <date>)` OR add a `[x]` checkbox prefix matching the convention used for Phases 10/11/12 in the ## Phases section.

3. Update the Progress table at lines 100-107 — add a new row:
   ```
   | 12.1. Installer + auto-update live verification | v1.1 | 8/8 | Complete | <YYYY-MM-DD> |
   ```

4. Update the v1.1 milestone status near the top of the file: `🚧 v1.1 Distribution` → `✅ v1.1 Distribution (shipped <YYYY-MM-DD>)` if all phases including 12.1 are done. Cross-check with REQUIREMENTS.md to ensure all 23 v1.1 requirements (DIST 7 + CI 6 + REL 4 + UPD 6 = 23) are marked complete.

**Anti-pattern guards:**
- DO NOT modify the v1.0 milestone archive section — that is frozen.
- DO NOT touch Phase 13 entries — Phase 13 remains DESCOPED to v1.2.
- DO NOT modify any historical timestamps in STATE.md — only the `last_updated:` and `## Current phase` / `## Last completed` / `## Next action` sections get fresh content.
- The decision to mark v1.1 milestone as `✅` (top of ROADMAP.md) is conditional on all 23 v1.1 requirements being green AND all 12.1 plans complete; if any 12.1-08 carry-forward (e.g. Linux UPD-* deferral) makes the milestone `passed_partial`, leave the v1.1 marker as `🚧 v1.1 Distribution (12 + 12.1 closed; <reason for partial>)` and document the partial in the Deferred section.
  </action>
  <verify>
    <automated>grep -qE "^status: (milestone_complete|phase_closed)$" .planning/STATE.md && grep -q "Phase 12.1" .planning/STATE.md && grep -q "8/8 plans complete\|Phase 12.1.*CLOSED\|Phase 12.1.*Complete" .planning/STATE.md && grep -q "12.1-01-PLAN.md" .planning/ROADMAP.md && grep -q "12.1-08-PLAN.md" .planning/ROADMAP.md && [ $(grep -c "12.1-0[1-8]-PLAN.md" .planning/ROADMAP.md) -ge 8 ] && grep -q "8/8" .planning/ROADMAP.md && grep -qE "12.1.*v1.1.*8/8" .planning/ROADMAP.md && npm run test 2>&1 | grep -E "Tests\s+434 passed" </automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter `status:` reflects milestone closure (`milestone_complete` or `phase_closed`)
    - STATE.md `## Current phase` references Phase 12.1 CLOSED
    - STATE.md `## Last completed` documents all 8 Plans 12.1-XX
    - ROADMAP.md Phase 12.1 entry shows `**Plans:** 8 plans`
    - ROADMAP.md Phase 12.1 plans list shows all 8 plans with `[x]` checkboxes
    - ROADMAP.md Progress table has a new row for Phase 12.1 with `8/8` complete
    - vitest 434/434 still passing (no test surfaces touched)
    - `git status --short .planning/` shows changes to STATE.md + ROADMAP.md (and the 2 verification files from Tasks 1+2)
  </acceptance_criteria>
  <done>STATE.md + ROADMAP.md updated to reflect Phase 12.1 closure + v1.1 milestone fully verified (or passed_partial with explicit carry-forward).

**Commit discipline for Task 3 — 2 SEPARATE atomic commits required (STATE.md and ROADMAP.md are independent milestone-closure surfaces; do NOT bundle them):**

1. Commit A — STATE.md flip only:
   - Stage ONLY `.planning/STATE.md`
   - Message: `docs(12.1): flip STATE.md to milestone_complete (Phase 12.1 closed 8/8; v1.1 fully verified)`
2. Commit B — ROADMAP.md flip only:
   - Stage ONLY `.planning/ROADMAP.md`
   - Message: `docs(12.1): mark Phase 12.1 8/8 complete + v1.1 milestone shipped in ROADMAP`

(Tasks 1 + 2 land separately as their own atomic commit per their own `<done>` block — verification-surface edits to 12.1-VERIFICATION.md + 12-VERIFICATION.md. Net: 3 atomic commits across this plan, not 1-2.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 12.1-08 is documentation closure + provenance recording; no code-execution surfaces touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-08-01 | Tampering | 12-VERIFICATION.md frontmatter status flip | mitigate | re_verification field documents the source of the flip (12.1-VERIFICATION.md); PRESERVE-HISTORY pattern keeps original entries auditable; commit history records the change |
| T-12.1-08-02 | Tampering | STATE.md / ROADMAP.md milestone closure | mitigate | Closure is conditional on all 23 v1.1 requirements being green AND all 8 Plan 12.1-XX SUMMARYs existing; partial closure documented explicitly with carry-forward rationale |
| T-12.1-08-03 | Information Disclosure | (none) | accept | All edited files are part of the public planning surface; no secrets, no PII |

**No new attack surface.** This plan only updates documentation files to record the verification closure.
</threat_model>

<verification>
- 12.1-VERIFICATION.md exists with all 8 ROADMAP SC documented + evidence references.
- 12-VERIFICATION.md `status:` flipped from `human_needed` to `passed` (or `passed_partial`).
- 12-VERIFICATION.md `re_verification:` field populated with cross-reference to 12.1-VERIFICATION.md.
- 12-VERIFICATION.md `human_verification:` array entries have `result:` + `verified_in:` + `transcript:` fields appended (PRESERVE-HISTORY pattern).
- STATE.md updated with Phase 12.1 closure + bumped progress + v1.1 milestone state.
- ROADMAP.md Phase 12.1 entry shows 8/8 plans complete + `[x]` markers.
- vitest 434/434 still passing.
- 1-2 atomic commits covering all 4 file changes.
</verification>

<success_criteria>
- ROADMAP SC-8 closed: 9 `human_needed` items in 12-VERIFICATION.md flipped to passed (or carried over to v1.2 with rationale).
- v1.1 milestone is FULLY VERIFIED end-to-end (Phases 10 + 11 + 12 + 12.1 all closed; 23/23 v1.1 requirements complete).
- Phase 12.1 is closed at 8/8 plans complete.
- The verification surface is durable: 12.1-VERIFICATION.md + 12-VERIFICATION.md (back-folded) + 12.1-HUMAN-UAT.md (per-step transcripts) form a complete audit trail for future re-verification or regression triage.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-installer-auto-update-live-verification/12.1-08-SUMMARY.md` documenting:
- The greenfield 12.1-VERIFICATION.md path + line count + 8 SC verified
- The 12-VERIFICATION.md back-fold details (status flip + re_verification populated + 9 array entries enriched + body Human Verification Required rewritten)
- The STATE.md updates (status + progress + Current phase + Last completed)
- The ROADMAP.md updates (Phase 12.1 plan list + Progress table row + v1.1 milestone marker)
- The 1-2 atomic commit SHA(s)
- Final state declaration: "Phase 12.1 CLOSED 8/8; v1.1 milestone FULLY VERIFIED" (or `passed_partial` with explicit carry-forward list)
</output>
