---
phase: 36-split-overrides-per-loader-mode
plan: 04
type: execute
wave: 2
depends_on:
  - "36-01"
files_modified:
  - tests/main/override-migration.spec.ts
  - tests/core/project-file.spec.ts
autonomous: true
requirements:
  - OVR-06
must_haves:
  truths:
    - "`tests/core/project-file.spec.ts` proves missing `overridesAtlasLess` field pre-massages to `{}` (forward-compat for v1.3.x/v1.4.x files)."
    - "`tests/core/project-file.spec.ts` proves serialize → materialize round-trips both buckets losslessly (atlas-source bucket AND atlas-less bucket independently populated with different values)."
    - "All pre-existing `AppSessionState` literal-shape tests in `tests/core/project-file.spec.ts` still compile after Plan 36-01 added `overridesAtlasLess` to the type (literal sites updated to include `overridesAtlasLess: {}`)."
    - "`tests/main/override-migration.spec.ts` proves per-bucket migration runs independently against shared `summary.regions` (mode-invariant)."
    - "`tests/main/override-migration.spec.ts` proves stale keys union across buckets (Case C orphans in both)."
    - "Existing override-migration tests (Tests 1..9 including the falsifying Test 6) still pass unchanged — `migrateOverrides` helper body unchanged."
    - "All tests in both spec files pass: `npm test -- tests/core/project-file.spec.ts tests/main/override-migration.spec.ts` exits 0."
  artifacts:
    - path: tests/core/project-file.spec.ts
      provides: "Forward-compat pre-massage test + round-trip test for overridesAtlasLess; updated literal-shape AppSessionState fixtures"
      contains: "Phase 36 — overridesAtlasLess"
    - path: tests/main/override-migration.spec.ts
      provides: "Per-bucket migration test (Test 10) + stale-union test (Test 11) for OVR-04"
      contains: "Phase 36 OVR-04"
  key_links:
    - from: tests/core/project-file.spec.ts
      to: src/core/project-file.ts
      via: "validateProjectFile / serializeProjectFile / materializeProjectFile exports"
      pattern: "validateProjectFile|serializeProjectFile|materializeProjectFile"
    - from: tests/main/override-migration.spec.ts
      to: src/main/override-migration.ts
      via: "migrateOverrides + OverrideMigrationResult"
      pattern: "migrateOverrides\\("
---

<objective>
Lock the OVR-06 round-trip + forward-compat + per-bucket migration contracts via vitest unit tests. Two test files updated:

1. `tests/core/project-file.spec.ts` — add a new `describe` block proving (a) missing `overridesAtlasLess` field pre-massages to `{}` and (b) serialize → materialize round-trips both buckets losslessly. Also update every existing `AppSessionState` literal in this file to include `overridesAtlasLess: {}` (otherwise TypeScript widens the literal and fails to compile after Plan 36-01's type change).

2. `tests/main/override-migration.spec.ts` — add Test 10 (per-bucket migration independence against shared summary.regions) and Test 11 (stale keys union across buckets). All pre-existing tests stay unchanged (`migrateOverrides` body is unmodified — Plan 36-02 only changes the call count from project-io.ts, which is irrelevant to this file's per-helper tests).

Purpose: Satisfy REQ OVR-06 with grep-verifiable + test-runnable proofs. The renderer-level mode-switch divergence test (OVR-07) is owned by Plan 36-05.
Output: `npm test -- tests/core/project-file.spec.ts tests/main/override-migration.spec.ts` exits 0; both new tests in project-file.spec.ts assert pre-massage + round-trip; both new tests in override-migration.spec.ts assert per-bucket independence + stale union.

Dependency rationale (revision 2026-05-13): this plan only exercises (a) `project-file.ts` (Plan 36-01 domain) and (b) the `migrateOverrides` helper body which is explicitly UNCHANGED by Plan 36-02 (Plan 36-02 only changes the call-count from project-io.ts upstream — irrelevant to per-helper tests). Therefore `depends_on: ["36-01"]` only; no 36-02 dependency. This lets 36-04 run as soon as 36-01 lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-split-overrides-per-loader-mode/36-CONTEXT.md
@.planning/phases/36-split-overrides-per-loader-mode/36-PATTERNS.md
@.planning/phases/36-split-overrides-per-loader-mode/36-01-PLAN.md
@.planning/seeds/SEED-007-split-overrides-per-loader-mode.md
@CLAUDE.md

<interfaces>
Post-36-01 type shape (already in scope at test compile time):

The `ProjectFileV1` and `AppSessionState` types both gained `overridesAtlasLess: Record<string, number>` as a required field — every literal-shape test fixture in this file must include it. The `MaterializedProject` type gained `restoredOverridesAtlasLess: Record<string, number>`.

Existing test pattern in `tests/core/project-file.spec.ts:322-385` (mirror for the new describe block): full excerpt in 36-PATTERNS.md §9. Existing test pattern in `tests/main/override-migration.spec.ts:36-100` (helpers) + lines 266-293 (Test 6 falsifying regression): full excerpt in 36-PATTERNS.md §8.

Unchanged helper shape from `src/main/override-migration.ts`:

The `migrateOverrides(savedOverrides, summary)` function returns `OverrideMigrationResult` with `restored`, `stale`, `migratedKeyCount`. Body unchanged in this phase (Plan 36-02 only changes the call count from project-io.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update tests/core/project-file.spec.ts — fix existing AppSessionState literal shapes + add Phase 36 describe block (pre-massage test + round-trip test)</name>
  <files>tests/core/project-file.spec.ts</files>
  <read_first>
    - tests/core/project-file.spec.ts (lines 100-200 + 270-410 — pre-existing AppSessionState literal sites; existing Phase 21 loaderMode describe block at 322-385; sharpenOnExport pre-massage test at 388-409)
    - .planning/phases/36-split-overrides-per-loader-mode/36-PATTERNS.md §9 (verbatim mirror for the new describe block; departure note about updating every existing AppSessionState literal)
    - src/core/project-file.ts (post-36-01 — reference only; confirm validator + serializer + materializer handle the new field)
    - src/shared/types.ts (post-36-01 — reference only; confirm ProjectFileV1 + AppSessionState have the new field)
  </read_first>
  <behavior>
    - Test 1 (pre-massage): a legacy file shape WITHOUT `overridesAtlasLess` passes `validateProjectFile` and yields `result.project.overridesAtlasLess` equal to `{}`.
    - Test 2 (round-trip): an `AppSessionState` with `overrides: { CIRCLE: 75 }` AND `overridesAtlasLess: { SQUARE: 50 }` is serialized; both keys appear in the serialized JSON; `materializeProjectFile` reads both back unchanged with the original values.
    - All pre-existing tests in this spec file continue to pass after literal-shape updates.
    - `npm test -- tests/core/project-file.spec.ts` exits 0.
  </behavior>
  <action>
    Make two kinds of edits.

    Edit A — Update every existing AppSessionState literal site (lines 113-180, 200-208, 270-280, 308-316, 366-380 per 36-PATTERNS.md §9 departure note). For each literal, add `overridesAtlasLess: {},` immediately after the existing `overrides: ...,` field. Otherwise TypeScript widens the literal to a value that doesn't satisfy `AppSessionState` (post-Plan 36-01 the type requires the new field), and existing tests fail to compile. Audit grep for finding all sites: `grep -n 'overrides: ' tests/core/project-file.spec.ts | grep -v 'overridesAtlasLess'`. Every line returned needs the sibling literal added (unless the line is intentionally a legacy-fixture proving forward-compat — only the new Phase 36 Test 1 fixture has that exemption). If a site is a `ProjectFileV1` literal (not `AppSessionState`), apply the same fix.

    Edit B — Add the new describe block at the end of the file per 36-PATTERNS.md §9 verbatim mirror. The describe is titled `Phase 36 — overridesAtlasLess (SEED-007 L-01)`. It contains two `it(...)` blocks:

    1. `it('validateProjectFile pre-massages missing overridesAtlasLess to {} (forward-compat for v1.3.x/v1.4.x files)', ...)` — builds a `legacy: Record<string, unknown>` shape with all v1.3.x fields present BUT `overridesAtlasLess` intentionally absent (mark the absence with a comment `// overridesAtlasLess INTENTIONALLY ABSENT (v1.3.x/v1.4.x shape)`). Calls `validateProjectFile(legacy)`. Asserts `result.ok` is `true` AND `(result.project as ProjectFileV1).overridesAtlasLess` equals `{}`.

    2. `it('serialize → materialize round-trips both buckets losslessly', ...)` — builds an `AppSessionState` literal with `overrides: { CIRCLE: 75 }` AND `overridesAtlasLess: { SQUARE: 50 }`, calls `serializeProjectFile(session, '/abs/project.stmproj')`, asserts both `serialized.overrides` and `serialized.overridesAtlasLess` equal their inputs, then calls `materializeProjectFile(serialized, '/abs/project.stmproj')` and asserts both `materialized.overrides` and `materialized.overridesAtlasLess` equal the original inputs. The fixture deliberately uses DIFFERENT values in the two buckets (CIRCLE in atlas-source, SQUARE in atlas-less) so any copy/swap bug is caught.

    Full mirror block to paste — quote verbatim from 36-PATTERNS.md §9:

    The exact code is `describe('Phase 36 — overridesAtlasLess (SEED-007 L-01)', () => { ... });` containing the two `it(...)` blocks described above. Sample assertions: `expect(result.ok).toBe(true);`, `expect((result.project as ProjectFileV1).overridesAtlasLess).toEqual({});`, `expect(serialized.overrides).toEqual({ CIRCLE: 75 });`, `expect(serialized.overridesAtlasLess).toEqual({ SQUARE: 50 });`, `expect(materialized.overrides).toEqual({ CIRCLE: 75 });`, `expect(materialized.overridesAtlasLess).toEqual({ SQUARE: 50 });`.

    Add any missing imports (`DEFAULT_DOCUMENTATION`, `ProjectFileV1`, `AppSessionState`, `materializeProjectFile`, `serializeProjectFile`, `validateProjectFile`) — verify against existing imports near the top of the file and add only what's missing.

    Constraints:
    - Test descriptions MUST contain the literal strings used in acceptance criteria: `Phase 36 — overridesAtlasLess`, `pre-massages missing overridesAtlasLess`, `round-trips both buckets losslessly`.
    - The legacy fixture's `overridesAtlasLess` field is INTENTIONALLY absent (proving pre-massage); do NOT add it.
    - The round-trip fixture has DIFFERENT values in the two buckets (CIRCLE in atlas-source, SQUARE in atlas-less) so a copy/swap bug would be caught.
  </action>
  <verify>
    <automated>npm test -- tests/core/project-file.spec.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/core/project-file.spec.ts` exits 0 (all tests pass — existing + new).
    - `grep -c "Phase 36 — overridesAtlasLess" tests/core/project-file.spec.ts` returns 1 (describe block landed).
    - `grep -c "pre-massages missing overridesAtlasLess" tests/core/project-file.spec.ts` returns 1 (Test 1 landed).
    - `grep -c "round-trips both buckets losslessly" tests/core/project-file.spec.ts` returns 1 (Test 2 landed).
    - `grep -c "INTENTIONALLY ABSENT" tests/core/project-file.spec.ts` returns 1 (legacy fixture comment present).
    - `npm run typecheck` clean across tests/core/project-file.spec.ts.
    - After audit `grep -n 'overrides: ' tests/core/project-file.spec.ts | grep -v 'overridesAtlasLess'`: any remaining hits must be (a) the intentional legacy fixture inside the new Phase 36 describe block, or (b) lines referring to a deserialized result (not a literal AppSessionState/ProjectFileV1 fixture). Visually confirm zero remaining literal AppSessionState fixtures missing the field.
  </acceptance_criteria>
  <done>All existing AppSessionState literal sites updated to include `overridesAtlasLess: {}`. New describe block with both tests landed. `npm test -- tests/core/project-file.spec.ts` exits 0. Test descriptions match acceptance criteria grep strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Test 10 (per-bucket migration independence) + Test 11 (stale keys union) to tests/main/override-migration.spec.ts</name>
  <files>tests/main/override-migration.spec.ts</files>
  <read_first>
    - tests/main/override-migration.spec.ts (lines 30-100 — helpers makePeak / makeSummary / deriveRegionsFromPeaks; lines 266-293 — Test 6 falsifying regression as the assertion-shape analog)
    - .planning/phases/36-split-overrides-per-loader-mode/36-PATTERNS.md §8 (verbatim mirror for Test 10 + Test 11)
    - src/main/override-migration.ts (lines 92-176 — helper signature + return type; body is UNMODIFIED in Plan 36-02)
  </read_first>
  <behavior>
    - Test 10: per-bucket migration runs independently — atlas-source bucket `{ CIRCLE: 75 }` and atlas-less bucket `{ SQUARE: 50 }` both against a summary containing both regions. Both restored unchanged; both stale arrays empty; both migratedKeyCount zero.
    - Test 11: stale keys union — atlas-source bucket `{ ORPHAN_A: 25 }` and atlas-less bucket `{ ORPHAN_B: 50 }` against a summary containing only CIRCLE. Both stale arrays contain their respective orphans; the union `[...new Set([...aSrc.stale, ...aLess.stale])]` sorted equals `['ORPHAN_A', 'ORPHAN_B']`.
    - Tests 1..9 (pre-existing) continue to pass unchanged.
    - `npm test -- tests/main/override-migration.spec.ts` exits 0.
  </behavior>
  <action>
    Add two `it(...)` blocks to the existing top-level describe in `tests/main/override-migration.spec.ts` per 36-PATTERNS.md §8 verbatim mirror.

    Test 10 name: `Test 10 (Phase 36 OVR-04): per-bucket migration runs independently against shared summary.regions`. Body:
    1. Build `summary = makeSummary([makePeak('CIRCLE'), makePeak('SQUARE')])`.
    2. Call `aSrc = migrateOverrides({ CIRCLE: 75 }, summary)` and `aLess = migrateOverrides({ SQUARE: 50 }, summary)`.
    3. Assert `aSrc.restored` equals `{ CIRCLE: 75 }`, `aSrc.stale` equals `[]`, `aLess.restored` equals `{ SQUARE: 50 }`, `aLess.stale` equals `[]`.
    4. Assert OVR-04 sum semantics: `aSrc.migratedKeyCount + aLess.migratedKeyCount` equals `0`.
    5. Assert OVR-04 union semantics: `[...new Set([...aSrc.stale, ...aLess.stale])]` equals `[]`.

    Test 11 name: `Test 11 (Phase 36 OVR-04): stale keys union across buckets (Case C orphans in both)`. Body:
    1. Build `summary = makeSummary([makePeak('CIRCLE')])`.
    2. Call `aSrc = migrateOverrides({ ORPHAN_A: 25 }, summary)` and `aLess = migrateOverrides({ ORPHAN_B: 50 }, summary)`.
    3. Assert `aSrc.stale` equals `['ORPHAN_A']` and `aLess.stale` equals `['ORPHAN_B']`.
    4. Build `union = [...new Set([...aSrc.stale, ...aLess.stale])]` and assert `union.sort()` equals `['ORPHAN_A', 'ORPHAN_B']`.

    Place both blocks at the end of the existing top-level describe (after Test 9). Reuse the existing `makePeak` / `makeSummary` helpers from lines 30-100 unchanged.

    Constraints:
    - Test names MUST contain the literal strings `Test 10 (Phase 36 OVR-04)` and `Test 11 (Phase 36 OVR-04)` for grep verification.
    - Each test asserts per-call (no shared state between calls — the helper is stateless per 36-PATTERNS.md §4 "Departure notes").
    - `migrateOverrides` body is NOT modified (verify via `git diff --stat src/main/override-migration.ts` returns empty).
  </action>
  <verify>
    <automated>npm test -- tests/main/override-migration.spec.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/main/override-migration.spec.ts` exits 0 (all 11 tests pass — Tests 1..9 unchanged + Tests 10 + 11 new).
    - `grep -c "Test 10 (Phase 36 OVR-04)" tests/main/override-migration.spec.ts` returns 1.
    - `grep -c "Test 11 (Phase 36 OVR-04)" tests/main/override-migration.spec.ts` returns 1.
    - `git diff --stat src/main/override-migration.ts` returns empty output (helper body unchanged).
    - The Test 6 falsifying-regression test (pre-existing) still passes (visible in `npm test` output — its name contains `Test 6` or its existing description).
  </acceptance_criteria>
  <done>Two new it() blocks landed at the end of the existing top-level describe. Test names contain the literal grep strings. `npm test -- tests/main/override-migration.spec.ts` exits 0. override-migration.ts source is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → production validator | The new pre-massage test feeds a hostile-shaped input (`Record<string, unknown>` with INTENTIONALLY missing field) to `validateProjectFile`. This is the FIRST line of defense for hostile `.stmproj` files in production — the test confirms the validator's pre-massage behavior is correct under the precise pre-condition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-12 | Tampering | Test fixtures that ALSO fix the test (e.g., a fixture that uses `overridesAtlasLess: {}` instead of being legacy-shaped) would silently pass without proving forward-compat | mitigate | Acceptance criterion `grep -c "INTENTIONALLY ABSENT" tests/core/project-file.spec.ts` returns 1 confirms the comment is present, and the comment is *adjacent to the absent field*. Test 1's fixture is a `Record<string, unknown>` (not a typed `ProjectFileV1`) — the TypeScript checker cannot reject the absence at the type level; the test relies on the validator's pre-massage to behave correctly. |
| T-36-13 | Repudiation / coverage gap | A round-trip test that uses the SAME value in both buckets would silently pass a copy/swap bug (atlas-source bucket overwriting atlas-less and vice versa during serialize/materialize) | mitigate | Acceptance criterion explicitly mandates `CIRCLE: 75` vs `SQUARE: 50` (different keys + different values) so any swap or single-bucket-write bug is caught. |
| T-36-14 | DoS via large fixtures | Tests use minimal fixtures (1 key per bucket) — no DoS surface | accept | Pre-existing pattern. |

No `high` severity threats. Security gate not triggered.
</threat_model>

<verification>
- `npm test -- tests/core/project-file.spec.ts tests/main/override-migration.spec.ts` exits 0.
- `grep -c "Phase 36 — overridesAtlasLess" tests/core/project-file.spec.ts` returns 1.
- `grep -c "pre-massages missing overridesAtlasLess" tests/core/project-file.spec.ts` returns 1.
- `grep -c "round-trips both buckets losslessly" tests/core/project-file.spec.ts` returns 1.
- `grep -c "Test 10 (Phase 36 OVR-04)" tests/main/override-migration.spec.ts` returns 1.
- `grep -c "Test 11 (Phase 36 OVR-04)" tests/main/override-migration.spec.ts` returns 1.
- `git diff --stat src/main/override-migration.ts` empty.
</verification>

<success_criteria>
OVR-06 satisfied (file-level + helper-level): round-trip both buckets serialize + deserialize losslessly through `project-file.ts`; legacy pre-massage path (missing `overridesAtlasLess` → `{}`); per-bucket migration in `override-migration.spec.ts` (atlas-source bucket migrated independently of atlas-less bucket). The renderer-level mode-switch divergence + legacy-routing-at-Open-seam integration test is owned by Plan 36-05.
</success_criteria>

<output>
After completion, create `.planning/phases/36-split-overrides-per-loader-mode/36-04-SUMMARY.md` recording: which AppSessionState literal sites in project-file.spec.ts were updated (line numbers), `npm test` pass count delta (before vs after), and confirmation that override-migration.ts is unmodified.
</output>
</content>
</invoke>