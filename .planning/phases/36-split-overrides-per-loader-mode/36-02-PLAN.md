---
phase: 36-split-overrides-per-loader-mode
plan: 02
type: execute
wave: 2
depends_on:
  - "36-01"
files_modified:
  - src/main/project-io.ts
autonomous: true
requirements:
  - OVR-02
  - OVR-04
must_haves:
  truths:
    - "Loading a legacy v1.3.x/v1.4.x `.stmproj` saved with `loaderMode === 'atlas-less'` routes the entire single-map `overrides` into the `overridesAtlasLess` bucket; the `overrides` bucket starts empty (SEED-007 L-02 / OVR-02 / D-02)."
    - "Loading a legacy v1.3.x/v1.4.x `.stmproj` saved with `loaderMode === 'auto'` (or undefined) routes the legacy map into `overrides`; the `overridesAtlasLess` bucket starts empty."
    - "Routing detection: legacy map present (`overrides.size > 0 && overridesAtlasLess.size === 0`) AND saved `loaderMode === 'atlas-less'` → route to atlas-less bucket; otherwise no-route. Applying the rule to a v1.5 file with the same shape is a no-op (idempotent)."
    - "`migrateOverrides` is called twice at each of the three seams (Open ~line 579, locate-skeleton recovery ~line 873, resample ~line 1074): once per bucket, against the shared mode-invariant `summary.regions`."
    - "`migratedKeyCount` is the sum across buckets (D-07); `staleOverrideKeys` is the union across buckets (D-06) — both produced as single scalars/lists for the renderer banner with zero IPC contract change."
    - "Migration banner aggregates the summed `migratedKeyCount` into a single `Updated {N1+N2} override(s) to per-region keys.` line (D-05) — renderer-side formatter at `overrideMigrationNotice` is UNTOUCHED; the existing format string consumes the pre-summed scalar."
    - "`MaterializedProject.restoredOverridesAtlasLess` is populated at all three seams alongside the existing `restoredOverrides`."
    - "`SerializableError['SkeletonNotFoundOnLoadError'].mergedOverridesBuckets` is populated at both failed-Open rescue sites carrying both buckets."
    - "`handleProjectReloadWithSkeleton` recovery validator (around line 726) checks `mergedOverridesBuckets` carries both buckets (replacing the old single-`mergedOverrides` Record check)."
    - "`src/main/override-migration.ts` body is UNCHANGED — only the call count from `project-io.ts` changes (1 → 2)."
  artifacts:
    - path: src/main/project-io.ts
      provides: "Per-bucket migration + legacy-routing at the Open seam + recovery payload assembly using mergedOverridesBuckets"
      contains: "mergedOverridesBuckets"
  key_links:
    - from: src/main/project-io.ts
      to: src/main/override-migration.ts
      via: "migrateOverrides called twice per seam (per bucket)"
      pattern: "migrateOverrides\\("
    - from: src/main/project-io.ts
      to: src/shared/types.ts
      via: "MaterializedProject + SerializableError type contract from Plan 36-01"
      pattern: "restoredOverridesAtlasLess|mergedOverridesBuckets"
---

<objective>
Implement the legacy-routing decision (SEED-007 Decision 2-A / D-02) at the Open seam in `src/main/project-io.ts`, and convert all three `migrateOverrides` call sites (Open ~579, locate-skeleton recovery ~873, resample ~1074) to per-bucket calls with summed `migratedKeyCount` (D-07) and unioned `staleOverrideKeys` (D-06). Rename `mergedOverrides` → `mergedOverridesBuckets` carrying both buckets at the two `SkeletonNotFoundOnLoadError` payload assembly sites (~lines 479-491 and ~794-807) and the recovery validator (~lines 726-731).

Purpose: Satisfy OVR-02 (legacy file routing) and OVR-04 (per-bucket migration with sum/union IPC contract). `src/main/override-migration.ts` body stays unchanged (mode-invariant helper); only the call count from project-io.ts changes.
Output: `src/main/project-io.ts` compiles cleanly with `npm run typecheck`; all three seams populate both `restoredOverrides` and `restoredOverridesAtlasLess`; both `SkeletonNotFoundOnLoadError` assembly sites carry `mergedOverridesBuckets: { overrides, overridesAtlasLess }`; legacy-routing decision is enforced ONLY at the Open seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-split-overrides-per-loader-mode/36-CONTEXT.md
@.planning/phases/36-split-overrides-per-loader-mode/36-PATTERNS.md
@.planning/phases/36-split-overrides-per-loader-mode/36-01-PLAN.md
@.planning/seeds/SEED-007-split-overrides-per-loader-mode.md
@CLAUDE.md

<interfaces>
Post-36-01 type shape (already in `src/shared/types.ts`):

```typescript
interface MaterializedProject {
  // ...
  restoredOverrides: Record<string, number>;
  restoredOverridesAtlasLess: Record<string, number>;  // NEW
  staleOverrideKeys: string[];
  migratedKeyCount: number;
  // ...
}

type SerializableError = {
  kind: 'SkeletonNotFoundOnLoadError';
  // ...
  mergedOverridesBuckets: {                            // RENAMED from mergedOverrides
    overrides: Record<string, number>;
    overridesAtlasLess: Record<string, number>;
  };
  // ...
};
```

Unchanged helper signature (`src/main/override-migration.ts:92-95`):
```typescript
export function migrateOverrides(
  savedOverrides: Record<string, unknown>,
  summary: SkeletonSummary,
): OverrideMigrationResult;

interface OverrideMigrationResult {
  restored: Record<string, number>;
  stale: string[];
  migratedKeyCount: number;
}
```

Existing call pattern at Open seam (`src/main/project-io.ts:572-588`) — to be REPLACED:
```typescript
  const { restored, stale, migratedKeyCount } = migrateOverrides(
    materialized.overrides as Record<string, unknown>,
    summary,
  );
```

Existing rescue payload at `src/main/project-io.ts:479-491` — to be RENAMED:
```typescript
      return {
        ok: false,
        error: {
          kind: 'SkeletonNotFoundOnLoadError',
          // ...
          mergedOverrides: materialized.overrides,
          // ...
        },
      };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert the Open seam (~line 579) to per-bucket migration with legacy-routing decision (D-02 / L-02)</name>
  <files>src/main/project-io.ts</files>
  <read_first>
    - src/main/project-io.ts (lines 560-620 — the full Open-seam migrateOverrides call block + MaterializedProject assembly)
    - .planning/phases/36-split-overrides-per-loader-mode/36-PATTERNS.md §3-A (legacy-routing block + per-bucket call pattern)
    - .planning/phases/36-split-overrides-per-loader-mode/36-CONTEXT.md (L-02, D-05, D-06, D-07)
    - src/main/override-migration.ts (lines 92-176 — helper signature + return type, for reference only; body NOT modified)
  </read_first>
  <behavior>
    - Test (Plan 36-04 will assert): legacy file with saved `loaderMode === 'atlas-less'` + non-empty `overrides` map + empty `overridesAtlasLess` → entire legacy map routed to `restoredOverridesAtlasLess`; `restoredOverrides` is `{}`.
    - Test: legacy file with saved `loaderMode === 'auto'` or undefined + non-empty `overrides` → legacy map stays in `restoredOverrides`; `restoredOverridesAtlasLess` is `{}`.
    - Test: v1.5 file with both buckets populated → no routing applied; both buckets pass through to per-bucket migration unchanged.
    - Test: per-bucket migration sums `migratedKeyCount` and unions `staleOverrideKeys`.
    - Test: `grep -c 'migrateOverrides(' src/main/project-io.ts` returns >= 6 (3 seams × 2 calls each).
  </behavior>
  <action>
    Replace the existing single `migrateOverrides` call at the Open seam (lines ~572-588) with the full block from `36-PATTERNS.md` §3-A. Quote the patterns block verbatim — including its doc comment. The structure is:

    1. **Detection block** (legacy single-map presence): compute `legacyMapPresent = Object.keys(materialized.overrides).length > 0 && Object.keys(materialized.overridesAtlasLess).length === 0`. Compute `routeToAtlasLess = legacyMapPresent && materialized.loaderMode === 'atlas-less'`. (Per L-02: the saved `loaderMode` IS the user's last expressed intent. Per the conservative-rule comment in PATTERNS.md: applying the routing rule to a v1.5 file with the same shape is a no-op — that's the documented behavior, not a bug.)

    2. **Bucket-input selection**: 
       - `atlasSourceBucketInput = routeToAtlasLess ? {} : materialized.overrides`
       - `atlasLessBucketInput = routeToAtlasLess ? materialized.overrides : materialized.overridesAtlasLess`

    3. **Per-bucket migration** (D-07 sum + D-06 union):
       - `const aSrc = migrateOverrides(atlasSourceBucketInput as Record&lt;string, unknown&gt;, summary);`
       - `const aLess = migrateOverrides(atlasLessBucketInput as Record&lt;string, unknown&gt;, summary);`
       - `const restoredAtlasSource = aSrc.restored;`
       - `const restoredAtlasLess = aLess.restored;`
       - `const stale = [...new Set([...aSrc.stale, ...aLess.stale])];`
       - `const migratedKeyCount = aSrc.migratedKeyCount + aLess.migratedKeyCount;`

    4. **`MaterializedProject` assembly** (after the call block, around line 584+): the existing `restoredOverrides:` field now sources from `restoredAtlasSource`; add a sibling `restoredOverridesAtlasLess: restoredAtlasLess,` immediately below.

    Keep all surrounding comments and the existing variable destructure name `restored` IF you choose; the patterns block uses `restoredAtlasSource` / `restoredAtlasLess` — either is fine as long as the field names in the final `MaterializedProject` literal match types from Plan 36-01.

    Do NOT modify `src/main/override-migration.ts` body.
  </action>
  <verify>
    <automated>grep -c 'migrateOverrides(' src/main/project-io.ts ; grep -c 'restoredOverridesAtlasLess' src/main/project-io.ts ; grep -n 'routeToAtlasLess' src/main/project-io.ts | head -5 ; npm run typecheck 2>&amp;1 | grep -E "src/main/project-io.ts" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'migrateOverrides(' src/main/project-io.ts` returns 2 or more at this stage (Open seam after this task; the other 4 land in Task 2). Note: this count rises to 6+ after Task 2 lands.
    - `grep -n 'routeToAtlasLess' src/main/project-io.ts` returns 1 or more (legacy-routing block landed at Open seam).
    - `grep -n 'legacyMapPresent' src/main/project-io.ts` returns 1 or more.
    - `grep -n 'restoredOverridesAtlasLess: ' src/main/project-io.ts` returns 1 or more (Open seam MaterializedProject populates the field).
    - `grep -c "SEED-007 L-02" src/main/project-io.ts` returns 1 or more (doc comment present at the Open seam).
    - `npm run typecheck` reports no errors in `src/main/project-io.ts` originating from the Open seam (errors in the other two seams are EXPECTED until Task 2 lands).
    - `src/main/override-migration.ts` is unmodified (verify via `git diff --stat src/main/override-migration.ts` returns empty output).
  </acceptance_criteria>
  <done>Open seam (~line 579) has the legacy-routing block + per-bucket migration + sum/union. MaterializedProject assembly populates both `restoredOverrides` and `restoredOverridesAtlasLess`. override-migration.ts is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert the locate-skeleton recovery and resample seams to per-bucket migration + rename `mergedOverrides` → `mergedOverridesBuckets` at both rescue payload sites and the recovery validator</name>
  <files>src/main/project-io.ts</files>
  <read_first>
    - src/main/project-io.ts (lines 470-500 — first rescue payload; lines 720-810 — handleProjectReloadWithSkeleton with the validator + second rescue payload; lines 870-910 — recovery seam migrateOverrides; lines 1060-1130 — resample seam migrateOverrides)
    - .planning/phases/36-split-overrides-per-loader-mode/36-PATTERNS.md §3-A (per-bucket call pattern), §3-B (rescue payload rename), §3-C (validator rename)
    - .planning/phases/36-split-overrides-per-loader-mode/36-CONTEXT.md (D-12 rename rationale)
  </read_first>
  <behavior>
    - Test (Plan 36-04 / 36-05 + typecheck): after Task 2, all 6+ `migrateOverrides` calls (3 seams × 2 buckets) compile and execute.
    - Test: failed-Open returns `SkeletonNotFoundOnLoadError` with `mergedOverridesBuckets: { overrides, overridesAtlasLess }` (NOT the old `mergedOverrides`).
    - Test: `handleProjectReloadWithSkeleton` validator rejects payloads missing either `overrides` or `overridesAtlasLess` sub-field with `Unknown` error kind.
    - Test: `grep -c '^[^*]*mergedOverrides:' src/main/project-io.ts` returns 0 (lines starting with the old field name in code, excluding doc comments).
    - Test: `grep -c 'mergedOverridesBuckets' src/main/project-io.ts` returns 4 or more (2 rescue sites + 1 validator + read sites).
  </behavior>
  <action>
    Make four edits across `src/main/project-io.ts`.

    **Edit 1: Recovery seam (around line 870-876) — per-bucket migration.**
    Apply the same `aSrc` + `aLess` two-call pattern from Task 1 — but WITHOUT the legacy-routing decision block. The recovery seam receives buckets that are ALREADY split (from the renderer; see PATTERNS.md §3-A "Departure notes": legacy-routing logic is at the Open seam ONLY). Input shape: read both buckets from the validated `mergedOverridesBuckets.overrides` and `mergedOverridesBuckets.overridesAtlasLess`. Output: assemble `MaterializedProject` with both `restoredOverrides` and `restoredOverridesAtlasLess`.

    **Edit 2: Resample seam (around line 1074) — per-bucket migration.**
    Same pattern as Edit 1. The resample call accepts buckets from the renderer payload (see Plan 36-03 for the resample IPC payload shape — it sends ONLY the active bucket per the Pitfall-3 boundary, but the resample handler at this seam should still per-bucket-migrate against `summary.regions` because the inactive bucket may be present in the request). Read PATTERNS.md §3 "Departure notes" carefully: resample is one-shot for the active bucket but the renderer's other bucket stays untouched — the main-side resample handler at line 1074 area receives a payload that may carry one or both buckets depending on the exact IPC contract; mirror the recovery pattern (assume both buckets are present in the inbound payload; the renderer Plan 36-03 will send both at the resample IPC seam OR the legacy `overrides` single field — confirm by reading the existing inbound IPC payload shape at this seam in project-io.ts and threading both fields through if both are present, otherwise honor the existing single-field shape; the type contract from Plan 36-01 governs).

    **Edit 3: Rescue payload rename (both sites — line 479-491 and line 794-807).**
    Replace the old single `mergedOverrides: materialized.overrides,` line at BOTH sites with:
    ```typescript
              mergedOverridesBuckets: {
                overrides: materialized.overrides,
                overridesAtlasLess: materialized.overridesAtlasLess,
              },
    ```
    Per D-12 — active-bucket-only would be silent data loss for the inactive bucket on locate-skeleton recovery.

    **Edit 4: `handleProjectReloadWithSkeleton` recovery validator (around line 726-731).**
    Replace the existing `if (!a.mergedOverrides || typeof a.mergedOverrides !== 'object')` check with:
    ```typescript
      if (
        !a.mergedOverridesBuckets
        || typeof a.mergedOverridesBuckets !== 'object'
        || !(a.mergedOverridesBuckets as Record&lt;string, unknown&gt;).overrides
        || !(a.mergedOverridesBuckets as Record&lt;string, unknown&gt;).overridesAtlasLess
      ) {
        return {
          ok: false,
          error: { kind: 'Unknown', message: 'mergedOverridesBuckets must carry both buckets' },
        };
      }
    ```
    Reader of `a.mergedOverridesBuckets.overrides` and `a.mergedOverridesBuckets.overridesAtlasLess` downstream feeds into Edit 1 (recovery seam per-bucket migration).

    Update `MaterializedProject` assembly at recovery (line 878-906) and resample (line 1079-1126) — populate `restoredOverridesAtlasLess` alongside `restoredOverrides` at both.
  </action>
  <verify>
    <automated>grep -c 'migrateOverrides(' src/main/project-io.ts ; grep -c 'mergedOverridesBuckets' src/main/project-io.ts ; grep -v '^[[:space:]]*\*\|^[[:space:]]*//' src/main/project-io.ts | grep -c 'mergedOverrides:' ; grep -c 'restoredOverridesAtlasLess' src/main/project-io.ts ; npm run typecheck 2>&amp;1 | grep -E "src/main/project-io.ts" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'migrateOverrides(' src/main/project-io.ts` returns 6 or more (3 seams × 2 calls).
    - `grep -c 'mergedOverridesBuckets' src/main/project-io.ts` returns 4 or more (2 rescue payload writes + 1 validator + 1+ read sites).
    - `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' src/main/project-io.ts | grep -c 'mergedOverrides:'` returns 0 (old field name fully removed from code; filtered comments).
    - `grep -c 'restoredOverridesAtlasLess' src/main/project-io.ts` returns 3 or more (one per seam's MaterializedProject assembly).
    - `npm run typecheck` reports zero errors in `src/main/project-io.ts`.
    - `git diff --stat src/main/override-migration.ts` returns empty output (override-migration.ts unmodified).
    - The validator rejection message at Edit 4 site contains `'mergedOverridesBuckets must carry both buckets'` verbatim: `grep -c "mergedOverridesBuckets must carry both buckets" src/main/project-io.ts` returns 1.
  </acceptance_criteria>
  <done>All three seams call `migrateOverrides` per-bucket with sum/union; both rescue payloads carry `mergedOverridesBuckets`; recovery validator checks both sub-buckets; no stale `mergedOverrides:` references in code (doc comments fine); override-migration.ts unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | `handleProjectReloadWithSkeleton` accepts a payload object from the renderer. The renderer is trusted (same Electron app, no remote origin), but the validator at line ~726 has historically guarded against shape errors anyway. |
| disk → main (Open) | The Open seam validates `materialized.*` after `validateProjectFile` has already gated the file shape (Plan 36-01 owns input validation). Trust contract: by the time legacy-routing runs, both `materialized.overrides` and `materialized.overridesAtlasLess` are known to be Records with finite-number values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | Recovery validator accepts a `mergedOverridesBuckets` payload missing one of the two sub-buckets, leaking `undefined` into `migrateOverrides` (which would receive `Record<string, unknown>` `undefined` and crash) | mitigate | The validator at Edit 4 explicitly checks both `(a.mergedOverridesBuckets as Record<string, unknown>).overrides` AND `.overridesAtlasLess` are truthy; missing either returns `Unknown` error kind with verbatim message `'mergedOverridesBuckets must carry both buckets'`. Renderer (Plan 36-03) always sends both because `lastSaved` always carries both per D-11. |
| T-36-05 | Tampering | Routing decision misroutes on a malformed legacy file (e.g. `loaderMode === 'atlas-less'` in saved JSON but the actual user intent was atlas-source — there's no way to know after the fact) | accept | The saved `loaderMode` IS the user's last expressed intent per SEED-007 Decision 2-A rationale. Once the legacy map is routed, re-routing is a second migration — accepted risk. Worst case: the user opens the file, sees the override in the wrong bucket, manually re-applies. Severity: low (UX confusion, not data loss; the override values themselves are intact in the routed-to bucket). |
| T-36-06 | Information disclosure | Field rename (`mergedOverrides` → `mergedOverridesBuckets`) leaves stale field reads silently returning `undefined` for the inactive bucket if any reader is missed | mitigate | Compile-time TypeScript enforcement (the rename is at the type level in Plan 36-01); `grep -v 'comments' src/ -r | grep -c 'mergedOverrides[^B]'` MUST return 0 across the renderer (Plan 36-03 owns the App.tsx + AppShell.tsx half). Plan 36-05 quality-gate task includes this grep. |

ASVS L1 alignment: input validation at trust boundary (V5.1.x) — recovery validator. No `high` severity threats — security gate not triggered.
</threat_model>

<verification>
- `npm run typecheck` clean across `src/main/project-io.ts`.
- `grep -c 'migrateOverrides(' src/main/project-io.ts` returns 6 or more.
- `grep -c 'mergedOverridesBuckets' src/main/project-io.ts` returns 4 or more.
- `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' src/main/project-io.ts | grep -c 'mergedOverrides:'` returns 0.
- `grep -c 'restoredOverridesAtlasLess' src/main/project-io.ts` returns 3 or more.
- `git diff --stat src/main/override-migration.ts` is empty (helper body unchanged).
</verification>

<success_criteria>
OVR-02 satisfied: legacy file routing by saved `loaderMode` lives at the Open seam. OVR-04 satisfied: per-bucket migration runs at all three seams with `migratedKeyCount` summed and `staleOverrideKeys` unioned. D-12 satisfied: `SkeletonNotFoundOnLoadError` carries `mergedOverridesBuckets` carrying both buckets across the locate-skeleton recovery chain. Plan 36-03 can now read `restoredOverridesAtlasLess` from the IPC response shape.
</success_criteria>

<output>
After completion, create `.planning/phases/36-split-overrides-per-loader-mode/36-02-SUMMARY.md` recording: which lines were modified at each of the three seams, the exact migrateOverrides call count, any deviation from PATTERNS.md §3 (and why), and confirmation that override-migration.ts is unmodified.
</output>
