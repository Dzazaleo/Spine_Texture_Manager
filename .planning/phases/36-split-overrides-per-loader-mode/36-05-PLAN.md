---
phase: 36-split-overrides-per-loader-mode
plan: 05
type: execute
wave: 3
depends_on:
  - "36-01"
  - "36-02"
  - "36-03"
  - "36-04"
files_modified:
  - tests/renderer/appshell-mode-switch-divergence.spec.tsx
  - .planning/seeds/SEED-007-split-overrides-per-loader-mode.md
autonomous: true
requirements:
  - OVR-05
  - OVR-07
must_haves:
  truths:
    - "A new integration test file `tests/renderer/appshell-mode-switch-divergence.spec.tsx` exists and mounts AppShell via RTL + jsdom (mirroring `tests/renderer/override-migration-banner.spec.tsx` scaffolding)."
    - "The test asserts: apply an override in atlas-source mode, toggle to atlas-less, observe atlas-less bucket renders no override (the atlas-source value did not leak)."
    - "The test asserts: apply a (different) override in atlas-less mode, toggle back to atlas-source, observe atlas-source still shows its pre-switch value (the atlas-less value did not overwrite)."
    - "The test asserts: apply an override in atlas-less, switch to atlas-source, change samplingHz (triggers the line-1542 resample useEffect in AppShell.tsx), switch back to atlas-less — atlas-less bucket retains its pre-resample value. Integration-level regression catcher for the line-1542 hydration-site blocker (per checker review 2026-05-13)."
    - "The test passes: `npm test -- tests/renderer/appshell-mode-switch-divergence.spec.tsx` exits 0."
    - "`.planning/seeds/SEED-007-split-overrides-per-loader-mode.md` frontmatter `status:` flips from `dormant` to `closed` with a closing-phase reference in the frontmatter or a top-of-doc note."
    - "No stale `mergedOverrides:` field references remain across the codebase (renderer + main + types + tests, excluding doc-comment mentions and SUMMARY.md historical references)."
  artifacts:
    - path: tests/renderer/appshell-mode-switch-divergence.spec.tsx
      provides: "OVR-07 integration test — apply/toggle/assert-divergence in both directions + samplingHz-change preservation regression catcher"
      contains: "Phase 36 OVR-07"
    - path: .planning/seeds/SEED-007-split-overrides-per-loader-mode.md
      provides: "Frontmatter status flipped to closed with phase reference"
      contains: "status: closed"
  key_links:
    - from: tests/renderer/appshell-mode-switch-divergence.spec.tsx
      to: src/renderer/src/components/AppShell.tsx
      via: "AppShell default export + initialProject prop"
      pattern: "import.*AppShell"
    - from: tests/renderer/appshell-mode-switch-divergence.spec.tsx
      to: src/shared/types.ts
      via: "MaterializedProject + SaveResponse type imports for mock typing"
      pattern: "from.*shared/types"
---

<objective>
Land the OVR-07 AppShell mode-switch divergence integration test in a new file `tests/renderer/appshell-mode-switch-divergence.spec.tsx`, mirroring the existing AppShell-mounted RTL scaffolding from `tests/renderer/override-migration-banner.spec.tsx`. THREE test cases: (1) atlas-source override does NOT leak into atlas-less on toggle, (2) atlas-less override does NOT overwrite atlas-source on toggle-back, (3) samplingHz change preserves the inactive-mode bucket (regression catcher for the line-1542 hydration-site blocker per checker review 2026-05-13). Flip SEED-007 status from `dormant` to `closed`. Run a phase-wide quality gate (`npm test` + `npm run typecheck` + grep audits) to confirm zero stale `mergedOverrides:` references survive across all touched files.

Purpose: Satisfy OVR-07 (the visible-user-symptom regression test for the bug that triggered SEED-007) and the OVR-05 success-criterion 5 requirement (SEED-007 status flip at phase close). This is the final wave — gates the whole phase to "complete" only after every prior plan's promise is exercised by the test. The third test case specifically catches the samplingHz-resample hydration blocker that Plan 36-03 originally missed.
Output: New test file exists and passes (3 tests); SEED-007 frontmatter shows `status: closed`; full test suite green; no stale `mergedOverrides:` references in production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-split-overrides-per-loader-mode/36-CONTEXT.md
@.planning/phases/36-split-overrides-per-loader-mode/36-PATTERNS.md
@.planning/phases/36-split-overrides-per-loader-mode/36-01-PLAN.md
@.planning/phases/36-split-overrides-per-loader-mode/36-02-PLAN.md
@.planning/phases/36-split-overrides-per-loader-mode/36-03-PLAN.md
@.planning/phases/36-split-overrides-per-loader-mode/36-04-PLAN.md
@.planning/seeds/SEED-007-split-overrides-per-loader-mode.md
@CLAUDE.md

<interfaces>
Post-36-03 AppShell contract:
- Accepts `initialProject` prop matching the type returned by the `openProject` IPC handler — at minimum `summary`, `restoredOverrides`, `restoredOverridesAtlasLess`, `staleOverrideKeys`, `migratedKeyCount`, `samplingHz`, `lastOutDir`, `sortColumn`, `sortDir`, `projectFilePath`, `documentation`, `loaderMode`, `sharpenOnExport`, `safetyBufferPercent`.
- Renders panels that show override badges/values for each row keyed by region name.
- Exposes a loader-mode toggle button in the toolbar; clicking it routes through `onToggleLoaderMode` (Plan 36-03 Task 2) which updates `loaderMode` state and may surface the mode-toggle toast (D-01..D-04).
- OverrideDialog is opened via `onOpenOverrideDialog` from a panel row; the dialog's apply button calls `onApplyOverride(scope, percent)` which writes to the active bucket only.
- Has a samplingHz input/select that triggers the samplingHz-change resample useEffect at AppShell.tsx:1497-1572 — that useEffect calls `window.api.resampleProject(...)` and on `ok: true` hydrates BOTH `setOverrides(...)` AND (post-Plan-36-03 fix) `setOverridesAtlasLess(...)` from the response payload. Test 3 exercises this path.

Existing scaffolding analog at `tests/renderer/override-migration-banner.spec.tsx`:
- `vi.stubGlobal('api', { ...30+ IPC stubs... })` in `beforeEach`. The stub for `resampleProject` should return `{ ok: true, project: { ...materialized, restoredOverrides: {}, restoredOverridesAtlasLess: {<seeded>} } }` for the Test 3 case to confirm the renderer re-hydrates both buckets from the response.
- `render(<AppShell summary={summary} samplingHz={120} initialProject={{ ... }} />)`.
- `makeSummary()` helper at lines 33-57 builds a minimal `SkeletonSummary` with regions for `CIRCLE`, `SQUARE`, etc.

Locked test scope per OVR-07 (REQUIREMENTS.md):
- "Apply override in atlas-source mode, switch to atlas-less, assert atlas-less bucket is empty"
- "Apply in atlas-less, switch to atlas-source, assert atlas-source bucket retains its pre-switch value"

Extended test scope (per revision 2026-05-13 checker blocker — samplingHz hydration):
- "Apply in atlas-less, switch to atlas-source, change samplingHz, switch back — atlas-less bucket preserved"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/renderer/appshell-mode-switch-divergence.spec.tsx — mirror the override-migration-banner.spec.tsx scaffolding and assert per-mode override divergence in both directions PLUS samplingHz-change preservation</name>
  <files>tests/renderer/appshell-mode-switch-divergence.spec.tsx</files>
  <read_first>
    - tests/renderer/override-migration-banner.spec.tsx (full file — copy the `vi.stubGlobal('api', ...)` block + `makeSummary` helper + render scaffolding verbatim; pay particular attention to the `resampleProject` stub shape since Test 3 exercises it)
    - .planning/phases/36-split-overrides-per-loader-mode/36-PATTERNS.md §10 (verbatim test scaffold + departure notes on the toggle-button selector + RTL interactions)
    - src/renderer/src/components/AppShell.tsx (post-Plan-36-03 — for the toggle button accessible name, OverrideDialog apply button text/aria, panel row override badge selectors, AND the samplingHz control selector for Test 3; lines 1497-1572 for the samplingHz-change resample useEffect to confirm the test's stubbed `resampleProject` response will trigger the right hydration)
  </read_first>
  <behavior>
    - Test 1 (atlas-source → atlas-less divergence): Mount AppShell with both buckets empty + `loaderMode: 'auto'`. Open OverrideDialog from a panel row in atlas-source mode (use a region present in `summary.regions`, e.g. CIRCLE). Apply override 50%. Toggle to atlas-less mode via the toolbar. Assert: the atlas-less panel rows show NO override for CIRCLE (the atlas-source 50% did not leak).
    - Test 2 (atlas-less → atlas-source preservation): Mount fresh AppShell. Toggle to atlas-less. Apply override 75% in atlas-less for a region. Toggle back to atlas-source. Assert: the atlas-source bucket still shows its original pre-switch value (empty if fresh-mounted; the atlas-less 75% did not overwrite the atlas-source bucket).
    - Test 3 (NEW — samplingHz change preserves inactive-mode bucket): Mount fresh AppShell with `loaderMode: 'auto'`. Toggle to atlas-less. Apply override 75% in atlas-less for CIRCLE. Toggle to atlas-source. Change samplingHz from 120 to 60 (triggers the samplingHz-change resample useEffect at AppShell.tsx:1497-1572 → calls `window.api.resampleProject(...)` → response hydrates both buckets via the line-1542 fix). Stub `resampleProject` to return `restoredOverrides: {}` AND `restoredOverridesAtlasLess: { CIRCLE: 75 }` so the test confirms the renderer re-hydrates the atlas-less bucket from the response. Toggle back to atlas-less. Assert: CIRCLE still shows 75% (atlas-less bucket survived the samplingHz change). This is the integration-level regression catcher for the line-1542 hydration-site blocker.
    - `npm test -- tests/renderer/appshell-mode-switch-divergence.spec.tsx` exits 0 with all THREE tests reported as passing.
  </behavior>
  <action>
    Create the new file `tests/renderer/appshell-mode-switch-divergence.spec.tsx` per 36-PATTERNS.md §10 verbatim mirror, then add a third `it(...)` block for the samplingHz-change preservation regression test.

    Step 1 — Scaffolding (copy from override-migration-banner.spec.tsx wholesale):
    - File-top vitest environment hint: `// @vitest-environment jsdom`.
    - Imports: React, vitest (`afterEach`, `beforeEach`, `describe`, `expect`, `it`, `vi`), RTL (`cleanup`, `render`, `screen`), `userEvent` from `@testing-library/user-event`, `AppShell` from the AppShell source, type imports for `SkeletonSummary`, `SaveResponse`, `MaterializedProject`, and `DEFAULT_DOCUMENTATION` from `../../src/shared/types`.
    - `afterEach(cleanup);` at module scope.
    - `makeSummary()` helper — copy from override-migration-banner.spec.tsx lines 33-57 unchanged (or with the minimal adaptation needed so it returns regions for both `CIRCLE` and `SQUARE`).
    - `beforeEach` containing the full `vi.stubGlobal('api', { ...30+ IPC stubs... })` block from override-migration-banner.spec.tsx lines 59-107. **For Test 3 specifically, the `resampleProject` stub will be re-configured per-test using `vi.mocked(...)` or `(window.api.resampleProject as Mock).mockResolvedValueOnce(...)` to return a tailored response.** Do not omit stubs — missing IPC stubs cause runtime errors on AppShell mount.

    Step 2 — Test 1 (atlas-source → atlas-less divergence):
    Inside `describe('Phase 36 OVR-07 — AppShell mode-switch divergence', () => { ... })`, add:

    `it('atlas-source override does NOT leak into atlas-less bucket when toggling modes', async () => { ... })`

    Body:
    1. `const user = userEvent.setup();`
    2. `const summary = makeSummary();` (must include both CIRCLE and SQUARE region entries; if the existing helper doesn't, extend the fixture in this file or wrap `makeSummary` to add regions).
    3. `render(<AppShell summary={summary} samplingHz={120} initialProject={{ ... }} />)` with `initialProject` literal containing: `summary`, `restoredOverrides: {}`, `restoredOverridesAtlasLess: {}`, `staleOverrideKeys: []`, `migratedKeyCount: 0`, `samplingHz: 120`, `lastOutDir: null`, `sortColumn: null`, `sortDir: null`, `projectFilePath: '/a/b/proj.stmproj'`, `documentation: DEFAULT_DOCUMENTATION`, `loaderMode: 'auto'`, `sharpenOnExport: false`, `safetyBufferPercent: 0`. Cast the literal `as never` (per 36-PATTERNS.md §10 — the existing analog also uses an unsafe cast since the full inferred type is unwieldy).
    4. Apply override in atlas-source: find the row for CIRCLE and trigger override-dialog open (the exact RTL query depends on the row UI — likely a button with text `Override` or aria-label containing the region name; if there's a `data-testid` use it). Type `50` in the percent input. Click `Apply`.
    5. Assert post-Apply: the CIRCLE row in atlas-source mode shows an override badge containing `50%` (or whatever the existing UI renders for the active-override state). Use `screen.findByText(/50%/)` or query the row by `getByRole('row', { name: /CIRCLE/i })`.
    6. Toggle to atlas-less: find the toolbar mode-toggle button by accessible name (`Use Images Folder as Source` per the CONTEXT.md L-04 description, or `Use Atlas as Source` — exact text per AppShell.tsx:1797-1800 region). Click it.
    7. Assert divergence: the CIRCLE row in atlas-less mode does NOT show an override badge / the override-cell renders the no-override state (e.g. `screen.queryByText(/50%/)` returns null OR the row's override cell contains the default "—" or empty state).

    Step 3 — Test 2 (atlas-less → atlas-source preservation):
    Continue from the state at the end of Test 1 (or render fresh; per 36-PATTERNS.md §10 the cleaner pattern is one `it` per direction):

    `it('atlas-less override does NOT overwrite atlas-source bucket when toggling modes', async () => { ... })`

    Body:
    1. Fresh `render(...)` with `initialProject` having `restoredOverrides: { CIRCLE: 50 }` (atlas-source bucket pre-populated) AND `restoredOverridesAtlasLess: {}` AND `loaderMode: 'auto'`.
    2. Toggle to atlas-less.
    3. Apply override in atlas-less for CIRCLE at 75%.
    4. Toggle back to atlas-source.
    5. Assert: the CIRCLE row in atlas-source still shows `50%` (its pre-switch value), NOT `75%`. Use `screen.findByText(/50%/)`.
    6. Assert (negative): `screen.queryByText(/75%/)` returns null in the atlas-source view.

    Step 4 — Test 3 (NEW — samplingHz change preserves inactive-mode bucket):
    Add as third `it(...)` block immediately after Test 2.

    `it('samplingHz change preserves the inactive-mode bucket (regression catcher for AppShell.tsx:1542 hydration site, per blocker review 2026-05-13)', async () => { ... })`

    Body:
    1. `const user = userEvent.setup();`
    2. Before render, configure the `resampleProject` stub to return a response that mimics what main-process project-io.ts would send for a samplingHz change — including BOTH `restoredOverrides` AND `restoredOverridesAtlasLess` so the renderer's post-Plan-36-03 line-1542 hydration logic can re-mount both buckets:
       ```typescript
       const stubbedResampleResponse = {
         ok: true,
         project: {
           summary,
           restoredOverrides: {},                            // atlas-source: empty
           restoredOverridesAtlasLess: { CIRCLE: 75 },       // atlas-less: preserved value
           staleOverrideKeys: [],
           migratedKeyCount: 0,
           samplingHz: 60,                                   // new rate after change
           lastOutDir: null,
           sortColumn: null,
           sortDir: null,
           projectFilePath: '/a/b/proj.stmproj',
           documentation: DEFAULT_DOCUMENTATION,
           loaderMode: 'auto',
           sharpenOnExport: false,
           safetyBufferPercent: 0,
         },
       };
       vi.mocked(window.api.resampleProject).mockResolvedValueOnce(stubbedResampleResponse as never);
       ```
       (If `vi.mocked(...)` doesn't compile due to the global stub shape, fall back to `(window.api.resampleProject as ReturnType<typeof vi.fn>).mockResolvedValueOnce(...)`.)
    3. `render(<AppShell summary={summary} samplingHz={120} initialProject={{ ... }} />)` with `initialProject.restoredOverridesAtlasLess: { CIRCLE: 75 }` (seed the atlas-less bucket up-front), `loaderMode: 'auto'`. NOTE the seeding pattern: rather than applying the override interactively (which would also work but adds RTL steps), we seed via `initialProject` to keep this regression test focused on the samplingHz-hydration boundary.
    4. Toggle to atlas-less; assert CIRCLE shows 75% (confirms the seed propagated to the active slice).
    5. Toggle back to atlas-source (so the samplingHz-change resample useEffect fires for the atlas-source bucket's pipeline; the atlas-less bucket is now inactive — the bug we're catching is that the line-1542 hydration would wipe it).
    6. Change samplingHz to 60: locate the samplingHz input/select via RTL (likely a `getByLabelText(/sampling/i)` or `getByRole('spinbutton'|'combobox', { name: /sampling/i })`; if no stable selector exists, fall back to a `data-testid="sampling-hz-input"` additive change to AppShell.tsx — same pattern as the loader-mode-toggle data-testid fallback in Plan 36-03). Use `user.clear()` + `user.type('60')` (or `user.selectOptions` if it's a select).
    7. Wait for the resample to settle. The mocked `resampleProject` resolves synchronously with the tailored response; the useEffect's `if (resp.ok)` branch fires the two hydration setters (line 1542 + the new line-1543 sibling for `setOverridesAtlasLess`). Use `await screen.findByText(/60/)` on the samplingHz display or a `waitFor(...)` on the response settling.
    8. Toggle back to atlas-less.
    9. Assert: CIRCLE row still shows 75% in atlas-less. `await screen.findByText(/75%/)` succeeds. If line 1542's sibling hydration were missing, this would FAIL (atlas-less bucket would be empty post-resample).

    Notable departure notes per 36-PATTERNS.md §10:
    - The `loaderMode` toggle button text is dynamic (`Use Images Folder as Source` vs `Use Atlas as Source`). Query by partial match or by `aria-label` if present. If the executor cannot identify a stable selector after reading AppShell.tsx, fall back to adding a `data-testid="loader-mode-toggle"` to the toggle button in AppShell.tsx (small additive change to the existing Plan 36-03 work — acceptable per executor discretion if no stable selector exists today).
    - The samplingHz control similarly: prefer `getByLabelText`/`getByRole`; fall back to `data-testid="sampling-hz-input"` if needed.
    - The OverrideDialog interaction sequence (open dialog → type percent → click Apply) is the same pattern as any existing OverrideDialog-related test. If no such test exists, read AppShell.tsx + OverrideDialog.tsx to discover the actual DOM structure.

    Constraints:
    - Describe block name MUST contain `Phase 36 OVR-07` for grep verification.
    - Test names MUST contain `does NOT leak`, `does NOT overwrite`, and `samplingHz change preserves` respectively.
    - Tests MUST use `userEvent` for interactions (not raw `fireEvent` — `userEvent` matches real user behavior better for `await user.click(...)` etc.).
    - If a stable selector for a UI affordance cannot be found and a `data-testid` is added to AppShell.tsx, do so minimally — one `data-testid` per affordance, named in kebab-case (`loader-mode-toggle`, `override-apply-button`, `sampling-hz-input`, etc.).
    - The third test's `mockResolvedValueOnce` is critical — it must NOT use the default `vi.fn()` from the `beforeEach` stub block, otherwise `resp.ok` may be undefined and the useEffect's hydration branch never fires. Test 3 specifically asserts the hydration branch's atlas-less sibling call fires correctly.
  </action>
  <verify>
    <automated>npm test -- tests/renderer/appshell-mode-switch-divergence.spec.tsx 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/renderer/appshell-mode-switch-divergence.spec.tsx` is true.
    - `grep -c "Phase 36 OVR-07" tests/renderer/appshell-mode-switch-divergence.spec.tsx` returns 1 or more.
    - `grep -c "does NOT leak" tests/renderer/appshell-mode-switch-divergence.spec.tsx` returns 1.
    - `grep -c "does NOT overwrite" tests/renderer/appshell-mode-switch-divergence.spec.tsx` returns 1.
    - `grep -c "samplingHz change preserves" tests/renderer/appshell-mode-switch-divergence.spec.tsx` returns 1 (new regression test landed).
    - `grep -c "vi.stubGlobal('api'" tests/renderer/appshell-mode-switch-divergence.spec.tsx` returns 1 or more (IPC stubbing scaffold present).
    - `grep -c "userEvent.setup" tests/renderer/appshell-mode-switch-divergence.spec.tsx` returns 1 or more.
    - `grep -c "mockResolvedValueOnce" tests/renderer/appshell-mode-switch-divergence.spec.tsx` returns 1 or more (Test 3's tailored resample response stubbing present).
    - `grep -c "restoredOverridesAtlasLess" tests/renderer/appshell-mode-switch-divergence.spec.tsx` returns 3 or more (Test 1 + Test 2 + Test 3 all seed it via initialProject; Test 3 also references it in the stubbedResampleResponse fixture).
    - `npm test -- tests/renderer/appshell-mode-switch-divergence.spec.tsx` exits 0 with all THREE tests reported as passing.
    - If `data-testid` attributes were added to AppShell.tsx for selector stability, `git diff src/renderer/src/components/AppShell.tsx` shows only `data-testid` additions (no other changes to AppShell.tsx in Wave 3).
  </acceptance_criteria>
  <done>New test file created, mirrors the scaffolding from override-migration-banner.spec.tsx, asserts divergence in both directions PLUS samplingHz-change preservation, all three tests pass via `npm test -- tests/renderer/appshell-mode-switch-divergence.spec.tsx`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flip SEED-007 frontmatter status from `dormant` to `closed` and run the phase-wide quality gate (full test suite + grep audits)</name>
  <files>.planning/seeds/SEED-007-split-overrides-per-loader-mode.md</files>
  <read_first>
    - .planning/seeds/SEED-007-split-overrides-per-loader-mode.md (full file — confirm current `status: dormant` in frontmatter)
    - .planning/ROADMAP.md §"Phase 36" success-criteria 5 (SEED-007 status flip requirement)
  </read_first>
  <behavior>
    - SEED-007 frontmatter shows `status: closed` with a closing-phase reference.
    - Full test suite `npm test` exits 0 (every other phase's tests still green after Phase 36 lands).
    - `npm run typecheck` exits 0 across the entire repository.
    - Zero stale `mergedOverrides:` field references in production code (renderer + main + types + tests, excluding doc-comment mentions and SUMMARY.md historical references).
  </behavior>
  <action>
    Step 1 — Flip SEED-007 frontmatter:
    Edit `.planning/seeds/SEED-007-split-overrides-per-loader-mode.md`. Change the frontmatter line `status: dormant` to `status: closed`. Add a `closed_during: 36-split-overrides-per-loader-mode` field underneath (mirroring the existing `planted_during:` field). Do NOT delete the existing `## LOCKED Design Decisions` section — that's the historical record of what shipped. Optionally add a short top-of-doc note like `**Closed:** 2026-05-13 (Phase 36 — Split Overrides Per Loader Mode shipped)` immediately under the `# SEED-007` heading.

    Step 2 — Run the phase-wide quality gate:
    Execute the following commands and confirm clean output:

    1. `npm test` — full vitest suite. Expected: exit 0, all tests green.
    2. `npm run typecheck` — TypeScript across the repo. Expected: exit 0, no errors.
    3. Grep audit for stale `mergedOverrides:` references (the renamed field — see D-12):
       Run: `grep -rn 'mergedOverrides:' src/ tests/ | grep -v '\\*' | grep -v '//' | grep -v 'mergedOverridesBuckets' | grep -v 'SUMMARY'`.
       Expected: empty output (zero stale references in production / test code; doc-comment mentions of the old name are filtered out).
    4. Grep audit for the new field landing:
       Run: `grep -rn 'overridesAtlasLess' src/ tests/ | wc -l`.
       Expected: a number greater than 20 (types + project-file + project-io + AppShell + tests all reference it).
    5. Grep audit for the new IPC field:
       Run: `grep -rn 'restoredOverridesAtlasLess' src/ | wc -l`.
       Expected: a number greater than 4 (types + project-io 3 seams + AppShell 3 hydration sites — runReload + mountOpenResponse + samplingHz-resample).
    6. Grep audit for the new error payload field:
       Run: `grep -rn 'mergedOverridesBuckets' src/ | wc -l`.
       Expected: a number greater than 5 (types + project-io 2 rescue payloads + 1 validator + AppShell state + App.tsx send).
    7. Grep audit for SEED-007 closure:
       Run: `grep -c '^status: closed' .planning/seeds/SEED-007-split-overrides-per-loader-mode.md`.
       Expected: 1.

    Constraints:
    - If `npm test` reports failures in tests NOT modified by Phase 36 (i.e. unrelated regressions), STOP and surface the failure for human review. Phase 36 must not green-light a regression in unrelated code.
    - If `npm run typecheck` reports errors anywhere, STOP and resolve before continuing — the phase ships type-clean or not at all.
    - If the stale-reference grep returns non-empty output, the executor MUST inspect each hit. Hits in `*.md` SUMMARY files (historical record) are acceptable; hits in `*.ts` / `*.tsx` (production / test code) require a rename to `mergedOverridesBuckets`.
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -20 ; npm run typecheck 2>&amp;1 | tail -10 ; grep -rn 'mergedOverrides:' src/ tests/ | grep -v '\*' | grep -v '//' | grep -v 'mergedOverridesBuckets' | grep -v 'SUMMARY' ; grep -c '^status: closed' .planning/seeds/SEED-007-split-overrides-per-loader-mode.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^status: closed' .planning/seeds/SEED-007-split-overrides-per-loader-mode.md` returns 1.
    - `grep -c '^closed_during: 36' .planning/seeds/SEED-007-split-overrides-per-loader-mode.md` returns 1 (closing-phase reference present).
    - `npm test` exits 0 with full suite green.
    - `npm run typecheck` exits 0.
    - `grep -rn 'mergedOverrides:' src/ tests/ | grep -v '\*' | grep -v '//' | grep -v 'mergedOverridesBuckets' | grep -v 'SUMMARY'` returns empty output (zero stale code references; doc-comments and SUMMARYs filtered out).
    - `grep -rn 'overridesAtlasLess' src/ tests/ | wc -l` returns 20 or more.
    - `grep -rn 'restoredOverridesAtlasLess' src/ | wc -l` returns 4 or more.
    - `grep -rn 'mergedOverridesBuckets' src/ | wc -l` returns 5 or more.
  </acceptance_criteria>
  <done>SEED-007 frontmatter shows status: closed + closed_during: 36; full test suite green; typecheck clean; zero stale mergedOverrides references in code; grep audits all match expected counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → AppShell (jsdom) | RTL drives user interactions through jsdom. No real Electron / no IPC roundtrip — all `api.*` calls are vi-stubbed. The test exercises the renderer-only data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-15 | Tampering | Test that passes even when the bug is present (e.g., asserting the wrong DOM node, or testing the active-mode view post-toggle without confirming the inactive-mode bucket is actually empty) | mitigate | Three-test structure (Test 1: source→less, Test 2: less→source, Test 3: samplingHz-during-source-active) catches both leak directions PLUS the hydration-path bug. Different override values (50% vs 75%) catch copy/swap bugs. Negative assertions (`queryByText` returns null) catch the leak directly rather than just confirming the active view. |
| T-36-16 | Tampering | jsdom + RTL fragility — selector queries that don't resolve to the expected DOM element silently pass `expect(screen.queryByText(...)).toBeNull()` even when the bug IS present (no element found because the query is wrong, not because the bucket is empty) | mitigate | At least one POSITIVE assertion per test (`screen.findByText(/50%/)` after Apply in atlas-source mode, and after toggling back in Test 2; `screen.findByText(/75%/)` after the post-samplingHz toggle-back in Test 3) confirms the selector works against the override-rendered state; the corresponding negative assertion in the post-toggle view is then trustworthy. If `data-testid` is added for selector stability, the test selector resolves deterministically. |
| T-36-17 | Information disclosure | SEED-007 frontmatter says `closed` but the actual deliverables don't satisfy OVR-01..OVR-07 (false-positive closure) | mitigate | This plan's Task 2 runs `npm test` (full suite) + `npm run typecheck` + 4 grep audits BEFORE the status flip. The flip cannot land if any check fails (per the STOP constraint in Task 2 action). |
| T-36-19 | Tampering | Test 3 stub returns the WRONG response shape (e.g., omits `restoredOverridesAtlasLess` from the resample response), making the test pass vacuously because the bug-being-tested cannot trigger | mitigate | Acceptance criterion `grep -c "restoredOverridesAtlasLess" tests/renderer/appshell-mode-switch-divergence.spec.tsx >= 3` enforces the field appears in the stubbed response fixture. The Test 3 positive assertion `screen.findByText(/75%/)` only succeeds if the renderer actually hydrates the atlas-less bucket from the response (i.e., the line-1542 sibling hydration is wired correctly). |

ASVS L1 alignment: no `high` severity threats. Security gate not triggered.
</threat_model>

<verification>
- `npm test -- tests/renderer/appshell-mode-switch-divergence.spec.tsx` exits 0 (all 3 tests pass).
- `npm test` (full suite) exits 0.
- `npm run typecheck` exits 0.
- `grep -c '^status: closed' .planning/seeds/SEED-007-split-overrides-per-loader-mode.md` returns 1.
- `grep -c '^closed_during: 36' .planning/seeds/SEED-007-split-overrides-per-loader-mode.md` returns 1.
- `grep -rn 'mergedOverrides:' src/ tests/ | grep -v '\*' | grep -v '//' | grep -v 'mergedOverridesBuckets' | grep -v 'SUMMARY'` returns empty.
- `grep -c "Phase 36 OVR-07" tests/renderer/appshell-mode-switch-divergence.spec.tsx` returns 1 or more.
- `grep -c "samplingHz change preserves" tests/renderer/appshell-mode-switch-divergence.spec.tsx` returns 1.
</verification>

<success_criteria>
OVR-07 satisfied: divergence test asserts no leak in either direction PLUS samplingHz-change preservation. OVR-05 success-criterion 5 satisfied (SEED-007 status: dormant → closed at phase close). Full phase is shippable — every prior plan's contract exercised by a passing test, all grep audits clean, type-clean, full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/36-split-overrides-per-loader-mode/36-05-SUMMARY.md` recording: the test file's final test count (3) and exit code, the exact `npm test` pass count delta vs pre-phase, the final grep audit numbers (overridesAtlasLess / restoredOverridesAtlasLess / mergedOverridesBuckets), and SEED-007 status flip confirmation.
</output>
</content>
</invoke>