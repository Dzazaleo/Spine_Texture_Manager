---
phase: 21-seed-001-atlas-less-mode-json-images-folder-no-atlas
verified: 2026-05-02T21:00:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 5/5
  gaps_closed:
    - "G-01 — Mesh-attachment crash on missing PNG (closed by Plan 21-09 stub-region machinery + LoadResult.skippedAttachments wiring)"
    - "G-02 — Missing PNGs not surfaced to user (closed by Plan 21-10 MissingAttachmentsPanel + SkeletonSummary.skippedAttachments cascade)"
    - "G-03 — UI layout regression (toolbar slides on filter) (closed by Plan 21-11 cumulative fix chain — `min-h-screen` on AppShell root + `scrollbar-gutter:stable` on main + `min-h-[calc(100vh-200px)]` on GlobalMaxRenderPanel section)"
    - "G-04 — Toggle-resample-into-atlas-less did NOT re-detect missing PNGs (closed by Plan 21-12 caller-side LoaderOptions precedence at project-io.ts Sites 1+4 + sampler-worker.ts Site 5; src/core/loader.ts UNCHANGED)"
  gaps_remaining: []
  regressions: []
overrides: []
gaps: []
deferred:
  - truth: "fixtures/Girl warmup spec — pre-existing skip (1 vitest skip remaining post-Phase-21)"
    addressed_in: "Phase 999 backlog (D-21-WORKTREE-1 in deferred-items.md)"
    evidence: "Pre-existing environmental issue documented in deferred-items.md (D-21-WORKTREE-1). Reproduces on main without Plan 21 commits — orthogonal to Phase 21's scope."
  - truth: "scripts/probe-per-anim.ts:14 TS2339 — pre-existing dev script error"
    addressed_in: "Backlog (pre-existing)"
    evidence: "scripts/probe-per-anim.ts is a developer probe utility, not part of the shipped surface. The TS2339 'Property values does not exist on type SamplerOutput' error pre-dates Phase 21."
  - truth: "WR-01 silent-empty-atlas warning + WR-02 inconsistent resample-path validation (21-REVIEW.md)"
    addressed_in: "Phase 21 follow-up backlog (non-blocking)"
    evidence: "Code review (21-REVIEW.md) flagged 0 critical, 2 warnings (WR-01 + WR-02), 5 info pre-gap-closure; post-gap-closure code review (commit 370378d) added 0 critical / 0 warning / 3 info — none are blockers."
human_verification:
  - test: "G-04 re-test — UAT 4b Path 2 (toggle-resample atlas-less surfaces missing PNG)"
    expected: "Start with a canonical project (.atlas + images/ both present). Delete a single PNG (e.g. images/JOKER/BODY.png). Toggle 'Use Images Folder as Source' ON. App resamples into atlas-less mode. The MissingAttachmentsPanel must appear above the Global Max Render panel listing the deleted PNG (name + expectedPngPath). Stale entry must NOT remain in the Max Render Source list."
    why_human: "G-04 closure (Plan 21-12) is locked at the IPC + worker boundary by 4 vitest tests, but the user-visible cascade (MissingAttachmentsPanel renders, panel updates atomically post-resample) requires running the dev build with a live drag-drop + checkbox toggle interaction. UAT 4b sub-case (b) is the empirically-pinned reproducer."
  - test: "Drag-drop atlas-less project (LOAD-01 / ROADMAP criterion #1)"
    expected: "Drop fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json onto a running dev build (npm run dev). No AtlasNotFoundError modal surfaces. Global Max Render Source panel + Animation Breakdown panel populate with rows whose Source W/H columns match the PNG IHDR dims (cross-check via `sips -g pixelWidth -g pixelHeight` on each fixture PNG)."
    why_human: "Verifies UI panels render real data on drag-drop — requires running Electron dev build, cannot be exercised by vitest (jsdom does not implement HTMLCanvasElement.getContext or drag-drop file APIs). Already PASSED in prior UAT (Test 1 in 21-HUMAN-UAT.md); included here to confirm no regressions from the gap-closure waves."
  - test: "loaderMode toggle visual + functional (LOAD-01 / D-08 UI surface)"
    expected: "With an atlas-less project loaded, the checkbox 'Use Images Folder as Source' is visible in the toolbar (right-aligned cluster, between SearchBar and Atlas Preview button). Drop the canonical fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json — checkbox shows UNCHECKED. Click to ENABLE atlas-less mode — wait for resample (loading spinner brief) — panels REPOPULATE with png-header-derived dims."
    why_human: "Visual placement, label legibility, click target size, and resample-on-toggle latency feel are UX qualities only a human can verify. Already PASSED in prior UAT (Test 2)."
  - test: "Save / Reopen round-trip preserves loaderMode (LOAD-01 / D-08 persistence)"
    expected: "With canonical SIMPLE_PROJECT loaded and toggle ENABLED, File → Save (Cmd+S) to a tmp .stmproj. Close project. Open the saved .stmproj — toggle returns to ENABLED state; panels populate via atlas-less path with the same dims as the pre-Save state."
    why_human: "Exercises the full main↔renderer↔disk persistence cycle. Already PASSED in prior UAT (Test 3)."
  - test: "MissingImagesDirError dialog renders typed (not 'Unknown error')"
    expected: "Create a tmpdir with ONLY the JSON (no images/ folder). Drop the JSON onto the dev app while the loaderMode toggle is checked (atlas-less). The error dialog must be structured (not 'Unknown error') and the human-readable message must mention 'images/ folder' or 'atlas-less'."
    why_human: "IPC envelope routing locked by tests/main/ipc.spec.ts; renderer-side error dialog component selection + rendering is UX-bound. Sub-case (a) PASSED in prior UAT (Test 4 sub-case a — empty images/ folder fires MissingImagesDirError correctly)."
  - test: "AtlasNotFoundError verbatim message preserved in user-facing dialog (ROADMAP criterion #5)"
    expected: "Create a tmpdir with ONLY the JSON. Manually create a .stmproj adjacent that pins explicit atlasPath to a nonexistent file. Open that .stmproj. Dialog must show verbatim 'Spine projects require an .atlas file beside the .json (carries region metadata that the skeleton JSON alone does not have). Re-export from the Spine editor with the atlas included.'"
    why_human: "tests/core/loader-atlas-less.spec.ts 'Success criterion #5' test locks the message at the loader throw-site, but the user-visible dialog wording is human-only. Already PASSED in prior UAT (Test 5)."
---

# Phase 21: SEED-001 Atlas-Less Mode Verification Report (post-gap-closure)

**Phase Goal:** Long-dormant SEED-001 closes — loader detects "no .atlas file beside .json" and routes through a synthesized atlas instead of failing with `AtlasNotFoundError`. Round-trip: load json+images project → sample → export end-to-end works.

**Verified:** 2026-05-02T21:00:00Z
**Status:** human_needed
**Re-verification:** Yes — post G-01/G-02/G-03/G-04 closure (Plans 21-09 / 21-10 / 21-11 / 21-12)

## Re-Verification Summary

The prior verification (2026-05-01) returned `human_needed` with 5/5 must-haves verified at the data-plane layer. UAT subsequently surfaced 4 gaps:

- **G-01** (mesh-attachment crash on missing PNG) — closed by Plan 21-09 (stub-region machinery + `LoadResult.skippedAttachments`).
- **G-02** (missing PNGs silent-skipped, no UX surface) — closed by Plan 21-10 (MissingAttachmentsPanel + SkeletonSummary cascade).
- **G-03** (toolbar layout regression on per-attachment filter) — closed by Plan 21-11 (cumulative CSS fix chain: AppShell root `min-h-screen`, main `[scrollbar-gutter:stable]`, GlobalMaxRenderPanel `min-h-[calc(100vh-200px)]`; user confirmed UAT-3 pass).
- **G-04** (toggle-resample-into-atlas-less did NOT re-detect missing PNGs) — closed by Plan 21-12 (caller-side LoaderOptions precedence at project-io.ts Sites 1+4 + sampler-worker.ts Site 5; `src/core/loader.ts` UNCHANGED to preserve criterion #5).

All four gap closures are locked by automated tests (`630 passed | 1 skipped | 2 todo (633)`). Path-symmetry between cold-load atlas-less and toggle-resample atlas-less is now contractually established. The remaining work is the HUMAN-UAT re-pass for the visible cascade — primarily UAT 4b Path 2 (G-04 reproducer).

## Goal Achievement

### Observable Truths

The 5 must-haves are derived from ROADMAP.md Phase 21 Success Criteria (the roadmap contract).

| #   | Truth (from ROADMAP success criteria)                                                                                                                                                | Status                                       | Evidence                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | User drops a `json + images folder` project onto the app and the loader routes through a synthesized in-memory atlas; panels populate with PNG-derived dims. (LOAD-01)                | ✓ VERIFIED (data-plane) / ? HUMAN (UI-plane) | Data plane: `tests/core/loader-atlas-less.spec.ts` D-05+INV-8 + INV-9 pass — `loadSkeleton(SIMPLE_PROJECT_NO_ATLAS)` returns `atlasPath: null`, `sourceDims` with `source: 'png-header'`, `sourcePaths` populated. AppShell.tsx:1243 has `min-h-screen` (G-03 fix); AppShell.tsx:1498-1510 mounts MissingAttachmentsPanel above activeTab panels (G-02 fix). UI-plane confirmation: prior UAT Tests 1-3 PASSED.                                                       |
| 2   | PNG dims read via byte-only IHDR parsing — no `sharp`/libvips/pixel decoding occurs during load. `src/core/png-header.ts` contains no DOM/streaming/sharp imports. (LOAD-02)            | ✓ VERIFIED                                   | `src/core/png-header.ts` (112 lines) imports only `node:fs` + `./errors.js`. Layer-3 invariant grep `from 'sharp'\|libvips\|node:zlib\|node:stream\|document\.\|window\.` returns only the docblock comment. Uses `fs.openSync` + `fs.readSync(fd, buf, 0, 24, 0)` (24-byte head read). 5 vitest tests in `tests/core/png-header.spec.ts` pass.                                                                                |
| 3   | Synthesized atlas is a valid spine-core `TextureAtlas`: each region has name=PNG basename, dims=PNG header dims, page=PNG file, x/y=0, rotated=false; spine-core consumes it. (LOAD-03) | ✓ VERIFIED                                   | `src/core/synthetic-atlas.ts` (304 lines after 21-09 stub-region addition) exports `synthesizeAtlasText` + `SilentSkipAttachmentLoader` + `SynthResult` (now with `missingPngs: string[]`). 9 vitest tests in `tests/core/synthetic-atlas.spec.ts` pass: INV-3 (atlasText accepted by `new TextureAtlas(text)`), INV-4, Pitfall-2 path-field, INV-5 silent-skip, stub-region grammar, missingPngs shape, empty-dir-stubs-all.                                                  |
| 4   | Optimize Assets on atlas-less project succeeds end-to-end via the same Lanczos3 sharp pipeline; golden fixture exercises load → sample → export round-trip. (LOAD-04)                  | ✓ VERIFIED                                   | `tests/core/loader-atlas-less.spec.ts` INV-9 round-trip test passes: load → sampleSkeleton → analyze → findUnusedAttachments → buildExportPlan against `fixtures/SIMPLE_PROJECT_NO_ATLAS`. Asserts `plan.rows.length > 0`, every `row.sourcePath` ends in `.png` and contains `SIMPLE_PROJECT_NO_ATLAS`. The export pipeline is byte-identical to the canonical-mode path because the loader populates `sourceDims/sourcePaths/atlasSources` to mirror the canonical shape. |
| 5   | `AtlasNotFoundError` message preserved verbatim for actually-missing-atlas cases. (ROADMAP success criterion #5)                                                                     | ✓ VERIFIED (message) / ? HUMAN (dialog UX)   | `src/core/loader.ts` UNCHANGED by Plan 21-12 (`git diff --name-only 179b1dd~1..HEAD -- src/core/loader.ts` returns 0 lines). Throw at line 228 (D-06 explicit-atlasPath fail) + line 281/312 (no-atlas-no-images malformed-project guard). `src/core/errors.ts:44-47` message string unchanged. tests/core/loader-atlas-less.spec.ts 'Success criterion #5' test passes; tests/core/loader.spec.ts D-06 test passes. Prior UAT Test 5 PASSED. |

**Score:** 5/5 truths verified. Truths #1 and #5 retain UI-plane sub-aspects routed to HUMAN-UAT (consistent with prior verification — these orchestrator-acknowledged human items are NOT regressions).

### Gap Closures — Re-verified

| Gap  | Closure Plan | Closure Mechanism                                                                                                            | Tests Locking Closure                                                                                          | Status      |
| ---- | ------------ | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ----------- |
| G-01 | 21-09        | Stub-region grammar (1×1 page+region) emitted for missing PNGs in `src/core/synthetic-atlas.ts`; `LoadResult.skippedAttachments?:` field threads through both atlas-less branches in `src/core/loader.ts` | tests/core/loader-atlas-less.spec.ts Test 6 (G-01 falsifying); tests/core/synthetic-atlas.spec.ts (9 tests including 3 stub-region) | ✓ CLOSED    |
| G-02 | 21-10        | `SkeletonSummary.skippedAttachments` REQUIRED field; `src/main/summary.ts` filters peaks/rows/unusedAttachments by `skippedNames` Set; `src/renderer/src/panels/MissingAttachmentsPanel.tsx` (92 lines) renders above activeTab panels in AppShell.tsx:1498-1510 | tests/core/summary.spec.ts (3 G-02 tests: UNIT filter / INTEGRATION via SIMPLE_PROJECT_NO_ATLAS_MESH / IPC structuredClone); tests/renderer/missing-attachments-panel.spec.tsx (4 RTL tests) | ✓ CLOSED    |
| G-03 | 21-11        | AppShell.tsx:1243 root `min-h-screen` (UAT-3 root-cause fix); AppShell.tsx:1481 main `[scrollbar-gutter:stable]` (UAT-2 fix); GlobalMaxRenderPanel.tsx:797 outer section `min-h-[calc(100vh-200px)]` (UAT-1 revised fix) | UAT-3 user confirmed "issue resolved" 2026-05-02 (commit b07b79e); jsdom cannot test layout                  | ✓ CLOSED    |
| G-04 | 21-12        | Caller-side LoaderOptions precedence — when `loaderMode === 'atlas-less'`, omit `atlasPath` so loader takes D-08 synthesis branch. Applied at project-io.ts Sites 1 (line 414-420) + 4 (line 901-907); sampler-worker.ts Site 5 (line 113-119); Site 3 audit-only (already shape-correct). `src/core/loader.ts` UNCHANGED — branch order + verbatim AtlasNotFoundError preserved. | tests/core/loader-atlas-less.spec.ts G-04 Tests 7+8 (loader-contract + caller-correct shape); tests/main/project-io.spec.ts G-04 IPC falsifying test; tests/main/sampler-worker.spec.ts G-04 (Site 5) worker-boundary falsifier | ✓ CLOSED    |

### Required Artifacts

| Artifact                                                          | Expected                                                                                          | Status                            | Details                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/core/png-header.ts`                                          | Pure-TS PNG IHDR width/height byte parser (LOAD-02)                                               | ✓ VERIFIED + WIRED + DATA-FLOWING | 112 LoC. Exports `readPngDims` + `PngHeaderParseError` + `PngDims`. Imports only `node:fs` + `./errors.js`. Imported by `src/core/synthetic-atlas.ts:43`.                                                                                                                                                                          |
| `src/core/synthetic-atlas.ts`                                     | synthesizeAtlasText + SilentSkipAttachmentLoader + SynthResult.missingPngs (LOAD-03 + G-01)        | ✓ VERIFIED + WIRED + DATA-FLOWING | 304 LoC (was 229 pre-21-09). Stub-region emission for missing PNGs; `missingPngs: string[]` on SynthResult; pngPathsByRegionName/dimsByRegionName remain PNG-truthful (no stub leakage). Imported by `src/core/loader.ts:48-50`.                                                                                                                       |
| `src/core/loader.ts` (4-way branch + skippedAttachments threading) | D-05/D-06/D-07/D-08 branch order; AtlasNotFoundError verbatim; synth.missingPngs → LoadResult.skippedAttachments threading | ✓ VERIFIED + WIRED + DATA-FLOWING | 508 LoC (was 478 pre-21-09). Branch order locked: opts.atlasPath (line 219) before opts.loaderMode==='atlas-less' (line 241) before sibling.atlas-readable. UNCHANGED by Plan 21-12 (criterion #5 preserved). `synthMissingPngs` capture in both atlas-less branches; spread at return statement.                                          |
| `src/core/errors.ts` (MissingImagesDirError)                      | New typed error class (LOAD-01 plumbing)                                                          | ✓ VERIFIED + WIRED + DATA-FLOWING | Class extends SpineLoaderError. 3 hits in errors.ts; 1 hit in src/main/ipc.ts (KNOWN_KINDS Set).                                                                                                                                                                                                              |
| `src/shared/types.ts` (SkeletonSummary.skippedAttachments REQUIRED + LoaderOptions.loaderMode + atlasPath nullable) | LoadResult/SkeletonSummary atlasPath nullable; SourceDims +'png-header'; LoaderOptions.loaderMode; SkeletonSummary.skippedAttachments REQUIRED | ✓ VERIFIED + WIRED + DATA-FLOWING | grep confirms 'png-header' literal, `atlasPath: string \| null`, `loaderMode: 'auto' \| 'atlas-less'` on ProjectFileV1 + AppSessionState + MaterializedProject; `loaderMode?:` on SamplerWorkerData + ResampleArgs; `skippedAttachments: { name; expectedPngPath }[]` REQUIRED on SkeletonSummary at line 553.                                |
| `src/core/types.ts` (LoadResult.skippedAttachments OPTIONAL)      | OPTIONAL `{ name; expectedPngPath }[]` field (Plan 21-09 ISSUE-007)                                | ✓ VERIFIED + WIRED + DATA-FLOWING | 185 LoC (was 165 pre-21-09). Optional shape avoids TS2741 cascades; matches existing unusedAttachments?: precedent.                                                                                                                                                                                            |
| `src/core/project-file.ts` (validate/serialize/materialize)       | loaderMode pre-massage + per-field check + serialize + materialize + PartialMaterialized          | ✓ VERIFIED + WIRED + DATA-FLOWING | tests/core/project-file.spec.ts 'Phase 21 — loaderMode' describe block (3 tests) pass.                                                                                                                                                                                                                       |
| `src/main/ipc.ts` (KNOWN_KINDS)                                   | KNOWN_KINDS includes 'MissingImagesDirError'                                                      | ✓ VERIFIED + WIRED + DATA-FLOWING | grep confirms 1 hit. tests/main/ipc.spec.ts regression test asserts kind === 'MissingImagesDirError'.                                                                                                                                                                                                                       |
| `src/main/sampler-worker.ts` (loaderMode threading + Site 5 precedence) | RunSamplerJobParams + loadSkeleton call thread loaderMode through worker boundary; Site 5 caller-side precedence | ✓ VERIFIED + WIRED + DATA-FLOWING | Lines 113-119 implement caller-side precedence (Plan 21-12 G-04 fix). Line 96 RunSamplerJobParams.loaderMode?. Worker-boundary falsifier passes (G-04 Site 5 test).                                                                                                                                                  |
| `src/main/project-io.ts` (5 sites + recovery branch + Sites 1+4 precedence) | Sites 1–5 thread loaderMode; recovery validates a.loaderMode; Sites 1+4 caller-side precedence (Plan 21-12) | ✓ VERIFIED + WIRED + DATA-FLOWING | Site 1 (line 414-420) caller-side precedence with `loaderMode === 'atlas-less' { ... atlasPath OMITTED }`; Site 4 (line 901-907) same shape; Site 3 audit-only (already shape-correct per F1.2 sibling-discovery semantics); Site 5 mirrored in sampler-worker.ts. |
| `src/renderer/src/components/AppShell.tsx` (5 plumbing sites + G-02 mount + G-03 fixes) | State slot + buildSessionState + mountOpenResponse + IPC payload + UI checkbox + MissingAttachmentsPanel mount + min-h-screen + scrollbar-gutter:stable | ✓ VERIFIED + WIRED + DATA-FLOWING | 1726 LoC. Line 59 import MissingAttachmentsPanel. Line 1243 root div `min-h-screen` (G-03 fix). Line 1481 main `overflow-auto [scrollbar-gutter:stable]` (G-03). Line 1498-1510 MissingAttachmentsPanel mount. Existing loaderMode plumbing preserved.                                                                                |
| `src/renderer/src/panels/MissingAttachmentsPanel.tsx`              | Warning banner + count header + collapsible name → expectedPngPath list (G-02 closure)            | ✓ VERIFIED + WIRED + DATA-FLOWING | 92 LoC. Empty-returns-null first. Imported only by AppShell.tsx:59.                                                                                                                                                                                                                                                |
| `src/renderer/src/panels/GlobalMaxRenderPanel.tsx`                | min-h-[calc(100vh-200px)] outer section pin (G-03 closure)                                        | ✓ VERIFIED + WIRED + DATA-FLOWING | Line 797 has `min-h-[calc(100vh-200px)]` className.                                                                                                                                                                                                                                                                |
| `src/main/summary.ts` (skippedAttachments cascade + filter)       | Required SkeletonSummary.skippedAttachments + skippedNames Set filtering peaks/rows/unused        | ✓ VERIFIED + WIRED + DATA-FLOWING | 193 LoC (was 151 pre-21-10). 3 `skippedNames.has` filter sites; 1 set construction site at top of buildSummary.                                                                                                                                                                                              |
| `fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json` + 4 PNGs       | Golden fixture (LOAD-04) — JSON + 4 PNGs, NO .atlas                                              | ✓ VERIFIED                        | `ls` confirms SIMPLE_TEST.json + images/ subfolder with CIRCLE.png, SQUARE.png, SQUARE2.png, TRIANGLE.png. NO .atlas file (zsh "no matches found").                                                                                                                                                                |
| `fixtures/SIMPLE_PROJECT_NO_ATLAS_MESH/MeshOnly_TEST.json` + 1 PNG | G-01 falsifying repro fixture (Plan 21-09) — minimal mesh-with-deform-timeline rig                 | ✓ VERIFIED                        | 53-line JSON + MESH_REGION.png (637 bytes). Tests 6 (G-01) + tests/core/summary.spec.ts INTEGRATION test consume it.                                                                                                                                                                                              |
| Phase-21 test specs (10 files)                                    | png-header / synthetic-atlas / loader-atlas-less / loader / project-file / ipc / summary / project-io / sampler-worker / missing-attachments-panel | ✓ VERIFIED                        | All 10 spec files pass; 114/114 phase-21-touchpoint tests green.                                                                                                                                                                                                                                                  |

### Key Link Verification

| From                                            | To                                                                  | Via                                                                       | Status   | Details                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/core/synthetic-atlas.ts`                   | `src/core/png-header.ts`                                            | `readPngDims` for per-PNG dim resolution                                  | ✓ WIRED  | grep confirms 2 references inside synthetic-atlas.ts                                                                                                                                                                                                                                            |
| `src/core/synthetic-atlas.ts`                   | `src/core/errors.ts MissingImagesDirError`                          | catastrophic case throw site (D-10 narrowed in 21-09)                     | ✓ WIRED  | First-variant guard preserved unchanged; second-variant guard removed (replaced by stub-region machinery → skippedAttachments).                                                                                                                                                              |
| `src/core/loader.ts`                            | `src/core/synthetic-atlas.ts` synthesizeAtlasText + SilentSkipAttachmentLoader + missingPngs | Both atlas-less branches (D-08 line 241, fall-through line 308) call synthesizeAtlasText | ✓ WIRED  | Plan 21-09: `synthMissingPngs = synth.missingPngs` captured in BOTH branches.                                                                                                                                                                                                                  |
| `src/core/loader.ts`                            | `LoadResult.skippedAttachments?:`                                   | optional spread at return statement when synthMissingPngs.length > 0      | ✓ WIRED  | Mapped from `<regionName>.png` filenames to `{ name, expectedPngPath }`.                                                                                                                                                                                                                       |
| `src/main/summary.ts`                           | `LoadResult.skippedAttachments?:`                                   | `skippedNames = new Set(...)` at top of buildSummary                       | ✓ WIRED  | Drives 3 filter sites + return-statement field; SkeletonSummary.skippedAttachments REQUIRED.                                                                                                                                                                                                    |
| `src/renderer/src/components/AppShell.tsx`     | `MissingAttachmentsPanel`                                            | Mount above activeTab-conditional panels (line 1498-1510)                | ✓ WIRED  | `effectiveSummary.skippedAttachments ?? []` defensive prop access.                                                                                                                                                                                                                            |
| `src/main/project-io.ts` Site 1 (Open)         | `src/core/loader.ts` D-08 synthesis branch                          | caller-side precedence: omit atlasPath when loaderMode='atlas-less'      | ✓ WIRED  | Plan 21-12 fix at lines 414-420. Pre-fix: both options set → D-06 won → no synthesis.                                                                                                                                                                                                          |
| `src/main/project-io.ts` Site 4 (Resample)     | `src/core/loader.ts` D-08 synthesis branch                          | caller-side precedence (G-04 reproducer fix)                              | ✓ WIRED  | Plan 21-12 fix at lines 901-907. Site 4 IS the empirically-pinned G-04 fix; locked by tests/main/project-io.spec.ts G-04 IPC test.                                                                                                                                                              |
| `src/main/sampler-worker.ts` Site 5            | `src/core/loader.ts` D-08 synthesis branch                          | caller-side precedence (worker-side mirror)                               | ✓ WIRED  | Plan 21-12 fix at lines 113-119. Locked by tests/main/sampler-worker.spec.ts G-04 worker-boundary falsifier.                                                                                                                                                                                   |
| `src/core/loader.ts` AtlasNotFoundError throw  | `src/core/errors.ts AtlasNotFoundError class`                       | verbatim message preserved for explicit-atlasPath fail (D-06)             | ✓ WIRED  | Line 228 throw on explicit-atlasPath fail; lines 281+312 throw on no-atlas-no-images malformed-project guard. Loader UNCHANGED by 21-12 (`git diff` shows 0 lines). |
| `src/main/ipc.ts`                               | `src/core/errors.ts MissingImagesDirError`                          | KNOWN_KINDS Set discriminates by .name                                    | ✓ WIRED  | tests/main/ipc.spec.ts regression confirms kind === 'MissingImagesDirError' (not 'Unknown').                                                                                                                                                                                                    |
| `tests/core/loader-atlas-less.spec.ts`          | `fixtures/SIMPLE_PROJECT_NO_ATLAS/`                                 | round-trip exercises Plan 21-03's golden fixture                          | ✓ WIRED  | INV-9 round-trip exercises load → sample → analyze → buildExportPlan; 6 tests pass including G-01 (Test 6) + G-04 (Tests 7+8).                                                                                                                                                                  |
| `tests/main/project-io.spec.ts`                 | `handleProjectResample` Site 4                                       | G-04 IPC falsifying-regression test                                       | ✓ WIRED  | Synthesizes inline tmp .atlas containing MESH_REGION so D-06 read SUCCEEDS pre-fix; load-bearing falsifying property is `result.project.summary.skippedAttachments.length === 1`.                                                                                                              |
| `tests/main/sampler-worker.spec.ts`             | `runSamplerJob` Site 5                                               | G-04 worker-boundary falsifying-regression test                            | ✓ WIRED  | Pre-fix `result.type === 'error'` (D-06 throws Region-not-found); post-fix `result.type === 'complete'` (D-08 synthesis runs).                                                                                                                                                                  |

### Data-Flow Trace (Level 4)

| Artifact                                                 | Data Variable                                                       | Source                                          | Produces Real Data | Status     |
| -------------------------------------------------------- | ------------------------------------------------------------------- | ----------------------------------------------- | ------------------ | ---------- |
| `src/core/png-header.ts` readPngDims                     | width / height                                                       | `Buffer.readUInt32BE(16)` / `(20)` from file IHDR | Yes — real PNG file IHDR bytes | ✓ FLOWING |
| `src/core/synthetic-atlas.ts` synthesizeAtlasText        | atlasText / pngPathsByRegionName / dimsByRegionName / missingPngs   | walks parsedJson skins → readPngDims per region; emits 1×1 stub on miss | Yes — real fixtures + falsifying fixture both produce expected shapes | ✓ FLOWING |
| `src/core/loader.ts` LoadResult                          | sourceDims / sourcePaths / atlasSources / skippedAttachments?       | Either canonical atlas.regions OR synth.* maps + synth.missingPngs | Yes — INV-9 round-trip + G-01 Test 6 confirm | ✓ FLOWING |
| `src/main/summary.ts` SkeletonSummary                    | skippedAttachments (REQUIRED) + filtered peaks/rows/unused           | LoadResult.skippedAttachments via skippedNames Set | Yes — UNIT + INTEGRATION + IPC structuredClone tests confirm | ✓ FLOWING |
| `src/renderer/src/components/AppShell.tsx` MissingAttachmentsPanel mount | `effectiveSummary.skippedAttachments`                               | summary IPC payload via mountOpenResponse / resample | Yes — RTL tests confirm component renders count + list when populated | ✓ FLOWING |
| `src/renderer/src/components/AppShell.tsx` toggle UI     | loaderMode useState                                                  | initialProject?.loaderMode (Open) / setLoaderMode (toggle click) | Yes — onChange writes 'atlas-less'\|'auto'; useEffect dep array → resample fires | ✓ FLOWING |
| `src/main/project-io.ts` Sites 1+4 loaderOpts             | loaderOpts.{atlasPath,loaderMode}                                    | caller-side precedence shaping                  | Yes — when loaderMode='atlas-less', atlasPath OMITTED → D-08 runs → skippedAttachments populated → MissingAttachmentsPanel | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior                                                                  | Command                                                                                                  | Result                                  | Status   |
| ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------- | -------- |
| Full vitest suite                                                          | `npm run test`                                                                                           | "Test Files 56 passed (56) / Tests 630 passed \| 1 skipped \| 2 todo (633)" | ✓ PASS |
| Phase-21 spec files (10 files: png-header / synthetic-atlas / loader-atlas-less / loader / project-file / ipc / summary / project-io / sampler-worker / missing-attachments-panel) | `npx vitest run tests/core/loader-atlas-less.spec.ts tests/core/png-header.spec.ts tests/core/synthetic-atlas.spec.ts tests/core/loader.spec.ts tests/core/project-file.spec.ts tests/main/ipc.spec.ts tests/core/summary.spec.ts tests/main/project-io.spec.ts tests/main/sampler-worker.spec.ts tests/renderer/missing-attachments-panel.spec.tsx` | "Test Files 10 passed (10) / Tests 114 passed (114)" | ✓ PASS |
| G-04 falsifying tests (3 across 3 files)                                   | `npx vitest run -t "G-04" tests/core/loader-atlas-less.spec.ts tests/main/project-io.spec.ts tests/main/sampler-worker.spec.ts` | 4 passed (loader contract Tests 7+8 + IPC + worker-boundary) | ✓ PASS |
| G-01 falsifying test                                                        | `npx vitest run -t "G-01" tests/core/loader-atlas-less.spec.ts`                                          | 1 passed (Test 6 — `MESH_REGION.png` deleted; load no-throw + skippedAttachments populated) | ✓ PASS |
| MissingAttachmentsPanel RTL tests                                           | `npx vitest run tests/renderer/missing-attachments-panel.spec.tsx`                                       | 4 passed (empty / singular / plural / expand) | ✓ PASS |
| TypeScript typecheck                                                       | `npm run typecheck`                                                                                       | only pre-existing scripts/probe-per-anim.ts:14 error (deferred) | ✓ PASS (pre-existing only) |
| Layer 3 invariant (Phase 21 core files)                                    | `grep -E "from 'sharp'\|libvips\|node:zlib\|node:stream\|document\\.\|window\\." src/core/png-header.ts src/core/synthetic-atlas.ts src/core/loader.ts src/core/types.ts` | only docblock comment in png-header.ts | ✓ PASS |
| Loader UNCHANGED by Plan 21-12 (criterion #5 preservation)                 | `git diff --name-only 179b1dd~1..HEAD -- src/core/loader.ts \| wc -l`                                    | 0                                       | ✓ PASS |
| Branch order (D-06 explicit-atlasPath at line 219, D-08 loaderMode at 241) | `grep -n "opts.atlasPath \|opts.loaderMode" src/core/loader.ts`                                          | 219 (D-06) < 241 (D-08) — correct order; criterion #5 preserved | ✓ PASS |
| Caller-side precedence at all 3 sites (Plan 21-12)                        | `grep -c "atlasPath intentionally OMITTED" src/main/project-io.ts src/main/sampler-worker.ts`            | 2 hits in project-io.ts (Sites 1+4) + 1 hit in sampler-worker.ts (Site 5) = 3 total | ✓ PASS |
| Fixture has NO .atlas file                                                | `ls fixtures/SIMPLE_PROJECT_NO_ATLAS/*.atlas`                                                            | "no matches found" (zsh)                | ✓ PASS |
| KNOWN_KINDS Set includes 'MissingImagesDirError'                          | `grep -c "MissingImagesDirError" src/main/ipc.ts`                                                        | 1 hit                                   | ✓ PASS |
| AtlasNotFoundError verbatim message string preserved                       | `grep -c "Spine projects require an .atlas file beside the .json" src/core/errors.ts`                   | 1 hit (criterion #5)                    | ✓ PASS |
| MissingAttachmentsPanel mounted in AppShell                                | `grep -c "MissingAttachmentsPanel" src/renderer/src/components/AppShell.tsx`                             | 2 hits (import + JSX mount)             | ✓ PASS |
| AppShell root uses `min-h-screen` (G-03 root cause)                        | `grep -c "min-h-screen" src/renderer/src/components/AppShell.tsx`                                        | 1 hit at line 1243                      | ✓ PASS |
| GlobalMaxRenderPanel pins `min-h-[calc(100vh-200px)]`                      | `grep -c "min-h-\\[calc(100vh-200px)\\]" src/renderer/src/panels/GlobalMaxRenderPanel.tsx`               | 2 hits (one in comment, one in className at line 797) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s)                | Description                                                                                          | Status      | Evidence                                                                                                                                                                                                       |
| ----------- | ----------------------------- | ---------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LOAD-01     | 21-02, 21-05, 21-06, 21-07, 21-08, 21-09, 21-10, 21-11, 21-12 | Loader detects "no `.atlas` beside `.json`" + routes through synthesized atlas; AtlasNotFoundError preserved for actually-missing-atlas. | ✓ SATISFIED (data plane + UI plane via UAT) | Loader 4-way branch (loader.ts:219-322); MissingImagesDirError + KNOWN_KINDS routing; .stmproj loaderMode round-trip; AppShell toggle; G-01 stub-region; G-02 MissingAttachmentsPanel; G-03 layout fix; G-04 caller-side precedence. |
| LOAD-02     | 21-01                         | New `src/core/png-header.ts` reads width/height from PNG IHDR via byte parsing only — no sharp/libvips/decoding. | ✓ SATISFIED | png-header.ts (112 LoC); Layer 3 grep clean; 5 vitest tests pass.                                                                                                                                                 |
| LOAD-03     | 21-04                         | `src/core/synthetic-atlas.ts` constructs in-memory TextureAtlas from per-region PNG headers; one region per PNG. | ✓ SATISFIED | synthetic-atlas.ts (304 LoC); 9 vitest tests pass; spine-core's TextureAtlas parser consumes synthesized text without throw.                                                                                       |
| LOAD-04     | 21-03, 21-08                  | Round-trip — load `json + images folder` (no `.atlas`) → sample → export to `images-optimized/` succeeds end-to-end. Golden fixture covers the path. | ✓ SATISFIED | tests/core/loader-atlas-less.spec.ts INV-9 round-trip test passes against fixtures/SIMPLE_PROJECT_NO_ATLAS; plan.rows.length > 0; rows reference fixture PNGs.                                                  |

**No orphaned requirements.** All 4 LOAD-* IDs in REQUIREMENTS.md → Phase 21 are claimed by at least one plan in this phase.

### Anti-Patterns Found

| File                              | Line                | Pattern                                | Severity  | Impact                                                                                                                                                                                                                          |
| --------------------------------- | ------------------- | -------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/core/synthetic-atlas.ts`     | (docblocks)         | "structural placeholder" (in comment) | ℹ️ Info   | Documents the rationale for `type SpineSequence = unknown` (spine-core does not re-export the Sequence type from package root). Not a code stub — it's a type alias. Already noted in prior verification.                       |
| Phase 21 source files (combined)  | (none)              | TODO / FIXME / placeholder / coming soon | (none)    | grep for TODO/FIXME/XXX/HACK/PLACEHOLDER patterns across png-header.ts + synthetic-atlas.ts + loader.ts returned NO hits.                                                                                                          |
| Stub regions for missing PNGs (1×1 grammar) | synthetic-atlas.ts | "stub region" (load-bearing, not stub-as-placeholder) | (none) | The 1×1 stub region is the FIX for G-01 — load-bearing machinery satisfying spine-core's atlas/attachment/animation chain. PNG-truthful invariant on pngPathsByRegionName/dimsByRegionName ensures no leakage to sourceDims/sourcePaths/atlasSources. |

### Human Verification Required

6 items need human testing (1 new G-04 retest + 5 carry-overs from prior verification):

#### 1. G-04 RETEST — UAT 4b Path 2 (toggle-resample atlas-less surfaces missing PNG) [NEW POST-CLOSURE]

**Test:** Start with a canonical project (.atlas + images/ both present). Delete a single PNG (e.g. `images/JOKER/BODY.png`). Toggle "Use Images Folder as Source" ON. App resamples into atlas-less mode.
**Expected:** The MissingAttachmentsPanel must appear above the Global Max Render panel listing the deleted PNG (name + expectedPngPath). The stale entry must NOT remain in the Max Render Source list. Pre-21-12 behavior was: panel renders as if PNG were still present; deleted attachment still appears with stale dims.
**Why human:** G-04 closure is locked by 4 vitest tests at the IPC + worker boundary, but the user-visible cascade (MissingAttachmentsPanel renders, panel updates atomically post-resample) requires a live drag-drop + checkbox toggle interaction. This is THE empirically-pinned reproducer from UAT 4b sub-case (b).

#### 2. LOAD-01 drag-drop UX (ROADMAP criterion #1, UI plane) [PASSED PRIOR UAT]

**Test:** `npm run dev`, then drag-and-drop `fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json` onto the running app window.
**Expected:** No `AtlasNotFoundError` modal surfaces. Global Max Render Source panel + Animation Breakdown panel both populate with rows for CIRCLE / SQUARE / SQUARE2 / TRIANGLE.
**Why human:** Visual confirmation of UI population. tests/core/loader-atlas-less.spec.ts INV-9 locks the data plane. Already PASSED prior UAT (Test 1).

#### 3. loaderMode toggle visual + functional (D-08 UI surface) [PASSED PRIOR UAT]

**Test:** With atlas-less project loaded, look at the toolbar (right-aligned cluster). Confirm checkbox labeled "Use Images Folder as Source" is visible. Drop the canonical fixture, click to enable atlas-less mode, confirm panels REPOPULATE.
**Expected:** Toggle visible, click target reasonable, resample fires within ~1s, panels visibly repopulate.
**Why human:** Tailwind class rendering + click target sizing + resample-latency feel. Already PASSED prior UAT (Test 2).

#### 4. Save / Reopen round-trip preserves loaderMode (D-08 persistence) [PASSED PRIOR UAT]

**Test:** With canonical SIMPLE_PROJECT loaded and toggle ENABLED, File → Save (Cmd+S) to a tmp `.stmproj`. Close project. Reopen the saved `.stmproj`.
**Expected:** Toggle returns to ENABLED state; panels populate via atlas-less path with same dims as pre-Save state.
**Why human:** Validates the full main↔renderer↔disk persistence cycle. Already PASSED prior UAT (Test 3).

#### 5. MissingImagesDirError dialog renders typed (not "Unknown error") [SUB-CASE-A PASSED PRIOR UAT]

**Test:** Create a tmpdir with ONLY the JSON (no images/ folder). Drop the JSON onto the dev app while the loaderMode toggle is checked (atlas-less).
**Expected:** Error dialog is structured (NOT "Unknown error"); message mentions "images/ folder" or "atlas-less".
**Why human:** IPC envelope routing locked by tests/main/ipc.spec.ts; renderer-side dialog is UX-bound. Sub-case (a) PASSED prior UAT (Test 4 sub-case a — empty images/ folder).

#### 6. AtlasNotFoundError verbatim message preserved in user-facing dialog (ROADMAP criterion #5) [PASSED PRIOR UAT]

**Test:** Create a tmpdir with ONLY the JSON. Manually create a `.stmproj` adjacent that pins explicit `atlasPath` to a nonexistent file. Open that `.stmproj`.
**Expected:** Error dialog shows the verbatim message "Spine projects require an .atlas file beside the .json (carries region metadata that the skeleton JSON alone does not have). Re-export from the Spine editor with the atlas included."
**Why human:** Renderer-side dialog wording. Already PASSED prior UAT (Test 5).

The HUMAN-UAT.md file at `.planning/phases/21-seed-001-atlas-less-mode-json-images-folder-no-atlas/21-HUMAN-UAT.md` already records UAT-1 results (Tests 1-3 + 4a + 5 PASSED; 4b and 4c surfaced as gaps now closed). This re-verification adds Test 4b retest as item 1 above.

### Gaps Summary

**No automated gaps.** All 5 ROADMAP success criteria satisfied at the code/contract layer (5/5 truths verified). All 4 prior UAT-surfaced gaps (G-01, G-02, G-03, G-04) are CLOSED with locking automated tests:

- **G-01** — 1 falsifying test (Test 6 in tests/core/loader-atlas-less.spec.ts) + 9 synthetic-atlas tests
- **G-02** — 3 cascade tests in tests/core/summary.spec.ts + 4 RTL tests in tests/renderer/missing-attachments-panel.spec.tsx
- **G-03** — UAT-3 user confirmation (no automated layout test possible in jsdom)
- **G-04** — 2 loader-contract tests + 1 IPC test + 1 worker-boundary falsifier (4 total across 3 files)

Vitest suite: 630 passing / 1 skipped / 2 todo (56 files). Typecheck: only pre-existing errors (deferred).

**Human verification required** for the 6 UI/UX-bound items above. Item 1 (G-04 retest UAT 4b Path 2) is the new must-pass for this re-verification — the empirically-pinned reproducer that surfaced G-04. The remaining 5 items are carry-overs from the prior verification, all of which already PASSED in UAT-1 and need only a sanity re-pass to confirm no regressions from the gap-closure waves (especially G-03's layout-fix CSS chain).

**Non-blocking items deferred:**
- Pre-existing fixtures/Girl warmup spec skip (D-21-WORKTREE-1 in deferred-items.md) — environmental, orthogonal to Phase 21.
- Pre-existing scripts/probe-per-anim.ts:14 TS2339 — dev probe script, not shipped surface.
- 21-REVIEW.md WR-01 (silent-empty-atlas) + WR-02 (resample-path validation inconsistency) — non-blocking edge cases.
- Code review for G-04 closure session (commit 370378d) added 0 critical / 0 warning / 3 info — none blocking.

---

_Verified: 2026-05-02T21:00:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification mode: post G-01/G-02/G-03/G-04 closure (Plans 21-09 / 21-10 / 21-11 / 21-12)_
