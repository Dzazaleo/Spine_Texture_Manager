---
phase: 21-seed-001-atlas-less-mode-json-images-folder-no-atlas
verified: 2026-05-01T23:46:28Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: null
human_verification:
  - test: "Drag-drop atlas-less project (LOAD-01 / ROADMAP criterion #1)"
    expected: "Drop fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json onto a running dev build (npm run dev). No AtlasNotFoundError modal surfaces. Global Max Render Source panel + Animation Breakdown panel populate with rows whose Source W/H columns match the PNG IHDR dims (cross-check via `sips -g pixelWidth -g pixelHeight` on each fixture PNG)."
    why_human: "Verifies UI panels render real data on drag-drop — requires running Electron dev build, cannot be exercised by vitest (jsdom does not implement HTMLCanvasElement.getContext or drag-drop file APIs). The data-plane half is locked by tests/core/loader-atlas-less.spec.ts INV-9; the UI-plane half (panels visibly populating after a real drag-drop) is the remaining gap."
  - test: "loaderMode toggle visual + functional (LOAD-01 / D-08 UI surface)"
    expected: "With an atlas-less project loaded, the checkbox 'Use Images Folder as Source' is visible in the toolbar (right-aligned cluster, between SearchBar and Atlas Preview button). Drop the canonical fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json — checkbox shows UNCHECKED. Click to ENABLE atlas-less mode — wait for resample (loading spinner brief) — panels REPOPULATE with png-header-derived dims."
    why_human: "Visual placement, label legibility, click target size, and resample-on-toggle latency feel are UX qualities only a human can verify. AppShell.tsx:1330-1344 wires the markup correctly per grep; tests/renderer cannot meaningfully assert on Tailwind class rendering or click behavior at this depth."
  - test: "Save / Reopen round-trip preserves loaderMode (LOAD-01 / D-08 persistence)"
    expected: "With canonical SIMPLE_PROJECT loaded and toggle ENABLED, File → Save (Cmd+S) to a tmp .stmproj. Close project (drop different fixture or reload window). Open the saved .stmproj — toggle returns to ENABLED state; panels populate via atlas-less path with the same dims as the pre-Save state."
    why_human: "Exercises the full main↔renderer↔disk persistence cycle (validateProjectFile pre-massage, serializeProjectFile, materializeProjectFile, AppShell.mountOpenResponse, sampler-worker re-load). The code-level round-trip is locked by tests/core/project-file.spec.ts (Phase 21 — loaderMode describe block) but the user-driven Save/Open ceremony (file dialogs, modal flow) is human-only."
  - test: "MissingImagesDirError dialog renders typed (not 'Unknown error')"
    expected: "Create a tmpdir with ONLY the JSON (no images/ folder). Drop the JSON onto the dev app while the loaderMode toggle is checked (atlas-less). The error dialog must be structured (not 'Unknown error') and the human-readable message must mention 'images/ folder' or 'atlas-less'. tests/main/ipc.spec.ts asserts kind='MissingImagesDirError' routes through KNOWN_KINDS, but only a human can verify the renderer's error-dialog UI surfaces it correctly."
    why_human: "IPC envelope routing is locked by tests/main/ipc.spec.ts. The renderer-side error dialog rendering (which dialog component fires, what title it shows, whether the missingPngs trail is readable) is UX-bound."
  - test: "AtlasNotFoundError verbatim message preserved in user-facing dialog (ROADMAP criterion #5)"
    expected: "Create a tmpdir with ONLY the JSON. Manually create a .stmproj adjacent that pins explicit atlasPath to a nonexistent file. Open that .stmproj. The error dialog must show the verbatim message 'Spine projects require an .atlas file beside the .json (carries region metadata that the skeleton JSON alone does not have). Re-export from the Spine editor with the atlas included.' (locked from src/core/errors.ts:44-47)."
    why_human: "tests/core/loader-atlas-less.spec.ts 'Success criterion #5' test locks the message string at the loader throw-site, but ROADMAP criterion #5 is about the user-visible dialog wording. The renderer's error dialog must surface the message verbatim — only a human can confirm the dialog component does not truncate, reformat, or translate."
overrides: []
gaps: []
deferred:
  - truth: "fixtures/Girl warmup spec — pre-existing skip (1 vitest skip remaining post-Phase-21)"
    addressed_in: "Phase 999 backlog (D-21-WORKTREE-1 in deferred-items.md)"
    evidence: "Pre-existing environmental issue documented in .planning/phases/21-seed-001-atlas-less-mode-json-images-folder-no-atlas/deferred-items.md (D-21-WORKTREE-1). Reproduces on main without Plan 21 commits — orthogonal to Phase 21's scope."
  - truth: "Two pre-existing TS6133 warnings (onQueryChange unused in panels) — pre-existing typecheck noise"
    addressed_in: "Backlog (deferred-items.md confirms pre-existing on base commit f09c29b)"
    evidence: "Verified pre-existing per deferred-items.md §Plan 21-05 entry: stash + re-typecheck against base commit f09c29b reproduced both warnings without Plan 21 changes. Not a Phase 21 regression."
  - truth: "scripts/probe-per-anim.ts:14 TS2339 — pre-existing dev script error"
    addressed_in: "Backlog (pre-existing)"
    evidence: "scripts/probe-per-anim.ts is a developer probe utility, not part of the shipped surface. The TS2339 'Property values does not exist on type SamplerOutput' error pre-dates Phase 21 — orthogonal to the atlas-less scope."
  - truth: "WR-01 silent-empty-atlas warning + WR-02 inconsistent resample-path validation"
    addressed_in: "Phase 21-REVIEW.md follow-up (non-blocking warnings)"
    evidence: "Code review (21-REVIEW.md) flagged 0 critical, 2 warnings (WR-01 + WR-02), 5 info. Both warnings are edge-case behavior comments — not goal-blocking. Per orchestrator's note in user prompt: 'None are blockers.'"
---

# Phase 21: SEED-001 Atlas-Less Mode Verification Report

**Phase Goal:** Long-dormant SEED-001 closes — loader detects "no .atlas file beside .json" and routes through a synthesized atlas instead of failing with `AtlasNotFoundError`. Round-trip: load json+images project → sample → export end-to-end works.

**Verified:** 2026-05-01T23:46:28Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

The 5 must-haves are derived from ROADMAP.md Phase 21 Success Criteria (the roadmap contract). PLAN-frontmatter must-haves were merged but reduce to the same 5 ROADMAP truths.

| #   | Truth (from ROADMAP success criteria)                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | User drops a `json + images folder` project onto the app and the loader routes through a synthesized in-memory atlas; panels populate with PNG-derived dims. (LOAD-01)                | ✓ VERIFIED (data-plane) / ? HUMAN (UI-plane) | Data plane: tests/core/loader-atlas-less.spec.ts D-05+INV-8 test passes — `loadSkeleton(SIMPLE_PROJECT_NO_ATLAS)` returns `atlasPath: null`, sourceDims with `source: 'png-header'`, sourcePaths populated. AppShell.tsx:1330-1344 renders the toggle and AppShell.tsx:1099 includes loaderMode in resampleProject IPC payload. UI-plane (panels visibly populating on real drag-drop) requires HUMAN-UAT.                                                       |
| 2   | PNG dims read via byte-only IHDR parsing — no `sharp`/libvips/pixel decoding occurs during load. `src/core/png-header.ts` contains no DOM/streaming/sharp imports. (LOAD-02)            | ✓ VERIFIED  | `src/core/png-header.ts` (112 lines) imports only `node:fs` + `./errors.js`. Layer-3 invariant grep `from 'sharp'\|libvips\|node:zlib\|node:stream\|document\.\|window\.` returns only docblock match (the comment documenting the invariant). Uses `fs.openSync` + `fs.readSync(fd, buf, 0, 24, 0)` (24-byte head read, NOT readFileSync). 5 vitest tests in tests/core/png-header.spec.ts pass.                                                                |
| 3   | Synthesized atlas is a valid spine-core `TextureAtlas`: each region has name=PNG basename, dims=PNG header dims, page=PNG file, x/y=0, rotated=false; spine-core consumes it. (LOAD-03) | ✓ VERIFIED  | `src/core/synthetic-atlas.ts` (229 lines) exports synthesizeAtlasText + SilentSkipAttachmentLoader + SynthResult. tests/core/synthetic-atlas.spec.ts 7+ tests pass: INV-3 (atlasText accepted by `new TextureAtlas(text)`), INV-4 (region resolution with x=0, y=0, degrees=0), Pitfall-2 path-field, INV-5 silent-skip per-region, INV-6 catastrophic, SilentSkipAttachmentLoader returns null on missing region.                                                  |
| 4   | Optimize Assets on atlas-less project succeeds end-to-end via the same Lanczos3 sharp pipeline; golden fixture exercises load → sample → export round-trip. (LOAD-04)                  | ✓ VERIFIED  | tests/core/loader-atlas-less.spec.ts INV-9 round-trip test passes: load → sampleSkeleton → analyze → findUnusedAttachments → buildExportPlan against fixtures/SIMPLE_PROJECT_NO_ATLAS. Asserts `plan.rows.length > 0`, every row.sourcePath ends in `.png` and contains `SIMPLE_PROJECT_NO_ATLAS`, sourceW/sourceH > 0. The export pipeline is byte-identical to the canonical-mode path because the loader populates sourceDims/sourcePaths/atlasSources to mirror the canonical shape (loader.ts:367-459). |
| 5   | `AtlasNotFoundError` message preserved verbatim for actually-missing-atlas cases. (ROADMAP success criterion #5)                                                                     | ✓ VERIFIED (message) / ? HUMAN (dialog UX) | Message-string lockdown: tests/core/loader-atlas-less.spec.ts 'Success criterion #5' test asserts `caught.message.toContain('Spine projects require an .atlas file beside the .json')` for explicit-unreadable-atlasPath case. tests/core/loader.spec.ts D-06 test locks the same. src/core/loader.ts:222 still throws `AtlasNotFoundError` verbatim. src/core/errors.ts:44-47 message string unchanged. User-facing dialog rendering needs HUMAN-UAT. |

**Score:** 5/5 truths verified at the data-plane / contract layer. Two truths (#1 and #5) have UI-plane sub-aspects routed to HUMAN-UAT per the orchestrator's explicit note.

### Required Artifacts

| Artifact                                                          | Expected                                                                                          | Status                | Details                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/core/png-header.ts`                                          | Pure-TS PNG IHDR width/height byte parser (LOAD-02)                                               | ✓ VERIFIED + WIRED + DATA-FLOWING | 112 LoC. Exports readPngDims + PngHeaderParseError + PngDims. Uses `fs.openSync`+`fs.readSync` (24-byte head read). Big-endian decode (Buffer.readUInt32BE). 5 vitest tests pass. Imported by `src/core/synthetic-atlas.ts:43`.                                                                                                                |
| `src/core/synthetic-atlas.ts`                                     | synthesizeAtlasText + SilentSkipAttachmentLoader (LOAD-03)                                        | ✓ VERIFIED + WIRED + DATA-FLOWING | 229 LoC. Walks JSON skins[*].attachments[slot][entry] filtering on type ∈ {region, mesh, linkedmesh}. Keys regions on att.path ?? entryName (Pitfall 2). Throws MissingImagesDirError on D-10 catastrophic case. Imported by `src/core/loader.ts:48-50`.                                                                                       |
| `src/core/loader.ts` (4-way branch)                               | D-05/D-06/D-07/D-08 branch order; AtlasNotFoundError verbatim (LOAD-01 + criterion #5)           | ✓ VERIFIED + WIRED + DATA-FLOWING | 478 LoC. Branch order locked: opts.atlasPath (line 214) before opts.loaderMode==='atlas-less' (line 236) before sibling.atlas-readable (line 266) before fall-through. tests/core/loader.spec.ts D-05/D-06/D-07/D-08 tests pass. AtlasNotFoundError throw at line 223 + 275 + 306 (malformed-project guard preserves the legacy F1.4 contract).            |
| `src/core/errors.ts` (MissingImagesDirError)                      | New typed error class (LOAD-01 plumbing)                                                          | ✓ VERIFIED + WIRED + DATA-FLOWING | 132 LoC. Class extends SpineLoaderError, sets `.name='MissingImagesDirError'`. Three-field constructor (searchedPath, skeletonPath, missingPngs?). Imported by synthetic-atlas.ts:44. Routes through KNOWN_KINDS in src/main/ipc.ts:126.                                                                                                                  |
| `src/shared/types.ts` (loaderMode + atlasPath nullable)           | LoadResult/SkeletonSummary atlasPath nullable; SourceDims +'png-header'; LoaderOptions.loaderMode | ✓ VERIFIED + WIRED + DATA-FLOWING | grep confirms 'png-header' literal, `atlasPath: string \| null`, `loaderMode: 'auto' \| 'atlas-less'` on ProjectFileV1, AppSessionState, MaterializedProject; `loaderMode?:` on SamplerWorkerData + ResampleArgs.                                                                                                                                |
| `src/core/project-file.ts` (validate/serialize/materialize)       | loaderMode pre-massage + per-field check + serialize + materialize + PartialMaterialized          | ✓ VERIFIED + WIRED + DATA-FLOWING | Lines 178-185 pre-massage missing→'auto' + reject invalid. Line 300 serialize. Line 349 PartialMaterialized field. Line 403 materialize fallback. tests/core/project-file.spec.ts 'Phase 21 — loaderMode' describe block (3 tests) pass.                                                                                                       |
| `src/main/ipc.ts` (KNOWN_KINDS)                                   | KNOWN_KINDS includes 'MissingImagesDirError'                                                      | ✓ VERIFIED + WIRED + DATA-FLOWING | Line 126 of src/main/ipc.ts has 'MissingImagesDirError' in KNOWN_KINDS Set. tests/main/ipc.spec.ts regression test asserts kind === 'MissingImagesDirError' (not 'Unknown') for envelope routing.                                                                                                                                                  |
| `src/main/sampler-worker.ts` (loaderMode threading)               | RunSamplerJobParams + loadSkeleton call thread loaderMode through worker boundary                 | ✓ VERIFIED + WIRED + DATA-FLOWING | Line 96 RunSamplerJobParams.loaderMode?. Line 105-110 loadSkeleton call uses loaderOpts builder. Line 192 workerData → params plumbing.                                                                                                                                                                                                                |
| `src/main/project-io.ts` (5 sites + recovery branch)              | Sites 1–5 thread loaderMode; recovery validates a.loaderMode                                      | ✓ VERIFIED + WIRED + DATA-FLOWING | Site 1 (line 402-409): handleProjectOpenFromPath loaderOpts builder. Site 2 (line 491): runSamplerInWorker `loaderMode: materialized.loaderMode`. Site 3 (line 670-756): handleProjectReloadWithSkeleton recovery validator + loadSkeleton + runSamplerInWorker. Site 4 (line 870-877): resampleProject loaderOpts. Site 5 (line 920-935): resample worker. |
| `src/renderer/src/components/AppShell.tsx` (5 plumbing sites)     | State slot + buildSessionState + mountOpenResponse + IPC payload + UI checkbox                    | ✓ VERIFIED + WIRED + DATA-FLOWING | Line 272 useState slot. Line 651 buildSessionState. Line 827 setLoaderMode in mountOpenResponse. Line 1099 IPC payload. Line 1147 useEffect dep. Line 1330-1344 UI checkbox 'Use Images Folder as Source'.                                                                                                                                          |
| `fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json` + 4 PNGs       | Golden fixture (LOAD-04 prerequisite) — JSON + 4 PNGs, NO .atlas                                  | ✓ VERIFIED            | Directory listing shows SIMPLE_TEST.json + images/ subfolder with CIRCLE.png, SQUARE.png, SQUARE2.png, TRIANGLE.png. NO .atlas file. shasum verified byte-identical to fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json (0b95e17a…) and fixtures/EXPORT_PROJECT/images/*.png (each pair matches).                                                              |
| `tests/core/png-header.spec.ts`                                   | Happy + 4 negative path tests                                                                     | ✓ VERIFIED            | 141 LoC; vitest 5 tests pass.                                                                                                                                                                                                                                                                                                     |
| `tests/core/synthetic-atlas.spec.ts`                              | INV-3..INV-6 + Pitfall 2 path-field + SilentSkipAttachmentLoader                                  | ✓ VERIFIED            | 283 LoC; vitest 7+ tests pass.                                                                                                                                                                                                                                                                                                    |
| `tests/core/loader-atlas-less.spec.ts`                            | Round-trip integration (LOAD-04 closure) — 5 passing tests                                        | ✓ VERIFIED            | 173 LoC; 5 tests pass; INV-9 round-trip exercises load → sample → analyze → buildExportPlan; no `.skip` qualifiers.                                                                                                                                                                                                                                |
| `tests/core/loader.spec.ts` (Phase 21 D-05/D-06/D-07/D-08 block)  | New describe block locking 4-way branch order                                                     | ✓ VERIFIED            | 368 LoC; 'Phase 21 — atlas-less mode' describe block contains 4 it() blocks; all pass.                                                                                                                                                                                                                                            |
| `tests/core/project-file.spec.ts` (loaderMode round-trip)          | 3 new tests: pre-massage, reject-invalid, serialize→materialize round-trip                        | ✓ VERIFIED            | 383 LoC; 'Phase 21 — loaderMode' describe block (3 tests) pass.                                                                                                                                                                                                                                                                  |
| `tests/main/ipc.spec.ts` (MissingImagesDirError envelope)         | Regression test — IPC envelope produces kind='MissingImagesDirError' (not 'Unknown')              | ✓ VERIFIED            | 470 LoC; regression test asserts envelope routing.                                                                                                                                                                                                                                                                                |

### Key Link Verification

| From                                            | To                                                                  | Via                                                                       | Status   | Details                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/core/synthetic-atlas.ts`                   | `src/core/png-header.ts`                                            | readPngDims for per-PNG dim resolution                                    | ✓ WIRED  | Line 43: `import { readPngDims } from './png-header.js';` Used inside synthesizeAtlasText loop (line 113) for each region's PNG.                                                                                                                                                                |
| `src/core/synthetic-atlas.ts`                   | `src/core/errors.ts MissingImagesDirError`                          | catastrophic case throw site (D-10)                                       | ✓ WIRED  | Line 44 import; lines 96-101 + 137-139 throw on the two catastrophic variants.                                                                                                                                                                                                                  |
| `src/core/loader.ts`                            | `src/core/synthetic-atlas.ts` synthesizeAtlasText + SilentSkipAttachmentLoader | atlas-less branches call synthesizeAtlasText, wrap atlas in SilentSkipAttachmentLoader | ✓ WIRED  | Lines 47-50 import. Line 239 (D-08 branch) + line 308 (D-05 fall-through branch) call synthesizeAtlasText. Line 343-345 conditional SilentSkipAttachmentLoader wiring.                                                                                                                            |
| `src/core/loader.ts`                            | `src/core/types.ts LoaderOptions.loaderMode`                        | opts.loaderMode read at branch-order step                                 | ✓ WIRED  | Line 236: `} else if (opts.loaderMode === 'atlas-less') {`                                                                                                                                                                                                                                       |
| `src/core/loader.ts AtlasNotFoundError throw`   | `src/core/errors.ts AtlasNotFoundError class`                       | verbatim message preserved for explicit-atlasPath fail (D-06)             | ✓ WIRED  | Line 223 throw on explicit-atlasPath fail; line 306 throw on no-atlas-no-images malformed-project guard. Both reach src/core/errors.ts:44-47 message verbatim.                                                                                                                                  |
| `src/main/ipc.ts`                               | `src/core/errors.ts MissingImagesDirError`                          | KNOWN_KINDS Set discriminates by .name                                    | ✓ WIRED  | KNOWN_KINDS Set at ipc.ts line 122-133 includes 'MissingImagesDirError' (line 126). Routing test in tests/main/ipc.spec.ts confirms kind === 'MissingImagesDirError' (not 'Unknown').                                                                                                            |
| `src/main/sampler-worker.ts`                    | `src/core/loader.ts opts.loaderMode`                                | workerData.loaderMode → loadSkeleton(skeletonPath, { atlasPath, loaderMode }) | ✓ WIRED  | sampler-worker.ts line 105-110 loadSkeleton call uses loaderOpts builder including `params.loaderMode`. Line 192 wires workerData → params.                                                                                                                                                       |
| `src/main/project-io.ts` (5 sites)              | `src/core/loader.ts opts.loaderMode` + sampler-worker                | materialized.loaderMode (Sites 1+2) / a.loaderMode (Sites 3+4+5)         | ✓ WIRED  | Site 1 line 409 / Site 2 line 491 / Site 3 line 670 (validator) + 684 (loader) + 755 (worker) / Site 4 line 876 / Site 5 line 935. All 5 sites confirmed by grep.                                                                                                                              |
| `src/renderer/src/components/AppShell.tsx`      | `src/main/project-io.ts resampleProject IPC payload`               | loaderMode field on ResampleArgs payload                                  | ✓ WIRED  | AppShell.tsx line 1099 `loaderMode,` in window.api.resampleProject({ … }) call. ResampleArgs type in src/shared/types.ts line 810 has `loaderMode?:`. main-side handler at project-io.ts line 920+ reads `a.loaderMode`.                                                                          |
| `tests/core/loader-atlas-less.spec.ts`          | `fixtures/SIMPLE_PROJECT_NO_ATLAS/`                                  | round-trip exercises Plan 21-03's golden fixture                          | ✓ WIRED  | Line 27 `ATLAS_LESS_FIXTURE = path.resolve('fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json')`. INV-9 test resolves the path and loads it.                                                                                                                                                       |
| `tests/core/loader-atlas-less.spec.ts`          | `src/core/export.ts buildExportPlan`                                 | round-trip integration: load → sample → analyze → buildExportPlan        | ✓ WIRED  | Line 153 `const plan: ExportPlan = buildExportPlan(summary as SkeletonSummary, new Map());`. Asserts `plan.rows.length > 0` (line 157) and per-row sourcePath ends in `.png` and contains `SIMPLE_PROJECT_NO_ATLAS`.                                                                              |

### Data-Flow Trace (Level 4)

| Artifact                                                 | Data Variable                                | Source                                          | Produces Real Data | Status     |
| -------------------------------------------------------- | -------------------------------------------- | ----------------------------------------------- | ------------------ | ---------- |
| `src/core/png-header.ts` readPngDims                     | width / height                               | `Buffer.readUInt32BE(16)` / `(20)` from file IHDR | Yes — real PNG file IHDR bytes (4 fixture PNGs in tests + arbitrary user PNGs at runtime) | ✓ FLOWING |
| `src/core/synthetic-atlas.ts` synthesizeAtlasText        | atlasText / pngPathsByRegionName / dimsByRegionName | walks parsedJson skins → readPngDims per region | Yes — every test fixture + real-world PNG produces non-empty atlas text | ✓ FLOWING |
| `src/core/loader.ts` LoadResult                           | sourceDims / sourcePaths / atlasSources       | Either canonical atlas.regions OR synth.* maps  | Yes — INV-9 round-trip test confirms downstream consumers (sample, analyze, buildExportPlan) read real data | ✓ FLOWING |
| `src/renderer/src/components/AppShell.tsx` toggle UI     | loaderMode useState                           | initialProject?.loaderMode (Open) / setLoaderMode (toggle click) | Yes — onChange writes 'atlas-less'\|'auto'; useEffect dep array includes loaderMode → resample fires | ✓ FLOWING |
| `src/renderer/src/components/AppShell.tsx` panels        | DisplayRow rendering                         | summary.peaks (post-resample)                    | Yes — Phase 19 panels already render summary.peaks; atlas-less mode populates same shape (verified by INV-9) | ⚠️ NEEDS HUMAN-UAT confirmation in running dev build |

### Behavioral Spot-Checks

| Behavior                                                                  | Command                                                                                                  | Result                                  | Status   |
| ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------- | -------- |
| Phase-21 vitest suite (6 spec files)                                      | `npx vitest run tests/core/loader-atlas-less.spec.ts tests/core/png-header.spec.ts tests/core/synthetic-atlas.spec.ts tests/core/loader.spec.ts tests/core/project-file.spec.ts tests/main/ipc.spec.ts` | "Test Files  6 passed (6) / Tests 75 passed (75)" | ✓ PASS |
| Full vitest suite                                                          | `npm run test`                                                                                          | "Test Files 55 passed (55) / Tests 617 passed | 1 skipped | 2 todo (620)" | ✓ PASS |
| TypeScript typecheck                                                       | `npm run typecheck`                                                                                      | only pre-existing scripts/probe-per-anim.ts:14 error | ✓ PASS (pre-existing only) |
| Layer 3 invariant (Phase 21 core files)                                    | `grep -E "from 'sharp'\|require\\('sharp'\\)\|libvips\|@aws\|node:zlib\|node:stream\|document\\.\|window\\." src/core/png-header.ts src/core/synthetic-atlas.ts` | only docblock comment (no actual import) | ✓ PASS |
| Branch order (D-06 explicit-atlasPath before D-08 loaderMode override)     | `awk '/opts.atlasPath !== undefined/{a=NR} /opts.loaderMode === .atlas-less./{b=NR} END {…}' src/core/loader.ts` | atlasPath at line 214, loaderMode at line 236 (a < b) | ✓ PASS |
| Fixture integrity — JSON byte-identical to source                         | `shasum fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`       | `0b95e17a…` matches both                | ✓ PASS |
| Fixture integrity — all 4 PNGs byte-identical to EXPORT_PROJECT source     | `shasum fixtures/SIMPLE_PROJECT_NO_ATLAS/images/*.png fixtures/EXPORT_PROJECT/images/*.png`              | each pair matches (CIRCLE / SQUARE / SQUARE2 / TRIANGLE) | ✓ PASS |
| Fixture has NO .atlas file                                                | `ls fixtures/SIMPLE_PROJECT_NO_ATLAS/*.atlas`                                                            | "no matches found" (zsh)                | ✓ PASS |
| KNOWN_KINDS Set includes 'MissingImagesDirError'                          | `grep -c "'MissingImagesDirError'" src/main/ipc.ts`                                                      | 1 hit (line 126)                        | ✓ PASS |
| AtlasNotFoundError verbatim message string preserved (errors.ts)          | `grep -c "Spine projects require an .atlas file beside the .json" src/core/errors.ts`                    | 1 hit (line 45)                         | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s)                | Description                                                                                          | Status      | Evidence                                                                                                                                                                                                       |
| ----------- | ----------------------------- | ---------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LOAD-01     | 21-02, 21-05, 21-06, 21-07, 21-08 | Loader detects "no `.atlas` beside `.json`" + routes through synthesized atlas; AtlasNotFoundError preserved for actually-missing-atlas. | ✓ SATISFIED (data plane) / ? UI plane | Loader 4-way branch (loader.ts:214-322); MissingImagesDirError class + KNOWN_KINDS routing; .stmproj loaderMode round-trip; AppShell toggle. UI-plane confirmation routes through HUMAN-UAT. |
| LOAD-02     | 21-01                         | New `src/core/png-header.ts` reads width/height from PNG IHDR via byte parsing only — no sharp/libvips/decoding. | ✓ SATISFIED | png-header.ts (112 LoC); Layer 3 grep clean; 5 vitest tests pass.                                                                                                                                                 |
| LOAD-03     | 21-04                         | `src/core/synthetic-atlas.ts` constructs in-memory TextureAtlas from per-region PNG headers; one region per PNG. | ✓ SATISFIED | synthetic-atlas.ts (229 LoC); 7+ vitest tests pass; spine-core's TextureAtlas parser consumes synthesized text without throw (INV-3 + INV-4 confirm).                                                              |
| LOAD-04     | 21-03, 21-08                  | Round-trip — load `json + images folder` (no `.atlas`) → sample → export to `images-optimized/` succeeds end-to-end. Golden fixture covers the path. | ✓ SATISFIED | tests/core/loader-atlas-less.spec.ts INV-9 round-trip test passes: load → sampleSkeleton → analyze → findUnusedAttachments → buildExportPlan against fixtures/SIMPLE_PROJECT_NO_ATLAS; plan.rows.length > 0; rows reference fixture PNGs.        |

**No orphaned requirements.** All 4 LOAD-* IDs in REQUIREMENTS.md → Phase 21 are claimed by at least one plan in this phase.

### Anti-Patterns Found

| File                              | Line                | Pattern                                | Severity  | Impact                                                                                                                                                                                                                          |
| --------------------------------- | ------------------- | -------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/core/synthetic-atlas.ts`     | 56 (docblock)       | "structural placeholder" (in comment) | ℹ️ Info   | Documents the rationale for `type SpineSequence = unknown` (spine-core does not re-export the Sequence type from package root). Not a code stub — it's a type alias used in the SilentSkipAttachmentLoader override signatures. Code review WR/IN-04 already documents the eventual remediation when spine-core 4.3+ stabilizes the re-export. |
| Phase 21 source files (combined)  | (none)              | TODO / FIXME / placeholder / coming soon | (none)    | grep for TODO/FIXME/XXX/HACK/PLACEHOLDER patterns across png-header.ts + synthetic-atlas.ts + loader.ts returned NO hits.                                                                                                          |

### Human Verification Required

5 items need human testing (orchestrator note explicitly routes Plan 21-08 Task 3 HUMAN-UAT through this path):

#### 1. LOAD-01 drag-drop UX (ROADMAP criterion #1, UI plane)

**Test:** `npm run dev`, then drag-and-drop `fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json` onto the running app window.
**Expected:** No `AtlasNotFoundError` modal surfaces. Global Max Render Source panel + Animation Breakdown panel both populate with rows for CIRCLE / SQUARE / SQUARE2 / TRIANGLE. Each row's Source W/H column matches the PNG IHDR dims (verifiable via `sips -g pixelWidth -g pixelHeight fixtures/SIMPLE_PROJECT_NO_ATLAS/images/CIRCLE.png` etc.).
**Why human:** Visual confirmation of UI population on real drag-drop. tests/core/loader-atlas-less.spec.ts INV-9 locks the data-plane (load → buildExportPlan); jsdom-based renderer specs cannot assert on canvas + drag-drop UI rendering at this depth.

#### 2. loaderMode toggle visual + functional (D-08 UI surface)

**Test:** With the atlas-less project loaded, look at the toolbar (right-aligned cluster, between SearchBar and Atlas Preview). Confirm checkbox labeled "Use Images Folder as Source" is visible. Drop the canonical fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json (which has both .atlas + images/) — confirm checkbox shows UNCHECKED. Click to enable atlas-less mode — confirm panels REPOPULATE with png-header-derived dims.
**Expected:** Toggle is visible, click target is reasonable, resample fires within ~1 second of toggle, panels visibly repopulate.
**Why human:** Tailwind-class rendering + click target sizing + resample-latency feel are UX qualities. The wiring is locked by grep at AppShell.tsx:1330-1344 + 1099 + 1147.

#### 3. Save / Reopen round-trip preserves loaderMode (D-08 persistence)

**Test:** With canonical SIMPLE_PROJECT loaded and toggle ENABLED, File → Save (Cmd+S) to a tmp `.stmproj`. Close project. Reopen the saved `.stmproj`.
**Expected:** Toggle returns to ENABLED state; panels populate via atlas-less path with same dims as pre-Save state.
**Why human:** Validates the full main↔renderer↔disk persistence cycle (file dialogs, modal flow). Code-level round-trip locked by tests/core/project-file.spec.ts but ceremony is human-only.

#### 4. MissingImagesDirError dialog renders typed (not "Unknown error")

**Test:** Create a tmpdir with ONLY the JSON (no images/ folder). Drop the JSON onto the dev app while the loaderMode toggle is checked (atlas-less). Without the toggle, the loader's malformed-project guard throws `AtlasNotFoundError` verbatim (see test 5 below) — so this test specifically exercises the toggle-on path.
**Expected:** Error dialog is structured (NOT "Unknown error"); message mentions "images/ folder" or "atlas-less".
**Why human:** IPC envelope routing locked by tests/main/ipc.spec.ts; renderer-side error dialog component selection + rendering is UX-bound.

#### 5. AtlasNotFoundError verbatim message preserved in user-facing dialog (ROADMAP criterion #5)

**Test:** Create a tmpdir with ONLY the JSON. Manually create a `.stmproj` adjacent that pins explicit `atlasPath` to a nonexistent file. Open that `.stmproj`.
**Expected:** Error dialog shows the verbatim message "Spine projects require an .atlas file beside the .json (carries region metadata that the skeleton JSON alone does not have). Re-export from the Spine editor with the atlas included." (locked from src/core/errors.ts:44-47).
**Why human:** tests/core/loader-atlas-less.spec.ts 'Success criterion #5' test locks the message at the loader throw-site; the user-visible dialog wording (no truncation, reformatting, or translation) is human-only.

### Gaps Summary

**No automated gaps.** All 5 ROADMAP success criteria are satisfied at the code/contract layer (5/5 truths verified), all 17 artifacts exist + are substantive + are wired + flow data, all 11 key links are wired, all 4 LOAD-* requirement IDs are claimed and satisfied. The 75 phase-21 tests pass; the full 617-test vitest suite passes; typecheck shows only pre-existing errors.

**Human verification required** for the 5 UI/UX-bound items above. Per the orchestrator's explicit note, Plan 21-08 Task 3 is a HUMAN-UAT checkpoint by design — the verifier surfaces these as `human_needed` so the workflow can persist them into a HUMAN-UAT.md file. ROADMAP success criteria #1 (panels populate visibly on drag-drop) and #5 (error dialog wording) have UI-plane sub-aspects that can only be confirmed by a human running the dev build.

**Non-blocking items deferred:**
- WR-01 silent-empty-atlas warning — edge case (zero region attachments + images/ exists). Code review (21-REVIEW.md) flags as comment-or-fix, not blocker.
- WR-02 inconsistent resample-path validation — trust-boundary inconsistency between `validateProjectFile` (rejects invalid loaderMode) and resample IPC handler (silently coerces to undefined / 'auto'). Non-blocking; the renderer never produces invalid loaderMode in normal use.
- 1 vitest skip remaining (fixtures/Girl warmup spec) — pre-existing environmental issue documented in deferred-items.md (D-21-WORKTREE-1); orthogonal to Phase 21.
- 3 pre-existing typecheck warnings/errors (scripts/probe-per-anim.ts:14 + 2 panel onQueryChange unused) — verified pre-existing on base commit f09c29b per deferred-items.md.

---

_Verified: 2026-05-01T23:46:28Z_
_Verifier: Claude (gsd-verifier)_
