---
phase: 21
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json
  - fixtures/SIMPLE_PROJECT_NO_ATLAS/images/CIRCLE.png
  - fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE.png
  - fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE2.png
  - fixtures/SIMPLE_PROJECT_NO_ATLAS/images/TRIANGLE.png
autonomous: true
requirements: [LOAD-04]
tags: [fixture, golden-test-asset]

must_haves:
  truths:
    - "fixtures/SIMPLE_PROJECT_NO_ATLAS/ exists with SIMPLE_TEST.json + images/ folder + 4 PNGs"
    - "No .atlas file in the fixture directory"
    - "JSON is byte-identical to fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json (same skeleton/animations/regions)"
    - "PNGs are byte-identical copies of fixtures/EXPORT_PROJECT/images/*.png (CIRCLE, SQUARE, TRIANGLE, SQUARE2)"
  artifacts:
    - path: "fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json"
      provides: "Spine 4.2 JSON with attachment references for CIRCLE/SQUARE/TRIANGLE/SQUARE2"
      contains: "skeleton"
    - path: "fixtures/SIMPLE_PROJECT_NO_ATLAS/images/CIRCLE.png"
      provides: "Per-region source PNG for CIRCLE attachment"
    - path: "fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE.png"
      provides: "Per-region source PNG for SQUARE attachment"
    - path: "fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE2.png"
      provides: "Per-region source PNG for SQUARE2 attachment"
    - path: "fixtures/SIMPLE_PROJECT_NO_ATLAS/images/TRIANGLE.png"
      provides: "Per-region source PNG for TRIANGLE attachment"
  key_links:
    - from: "fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json"
      to: "fixtures/SIMPLE_PROJECT_NO_ATLAS/images/*.png"
      via: "per-region PNG file basename matches JSON attachment region name"
      pattern: "skeleton.skins"
---

<objective>
Create the golden fixture `fixtures/SIMPLE_PROJECT_NO_ATLAS/` that exercises the atlas-less load → sample → export round-trip in Plan 08's integration tests. The fixture is constructed by COPYING from existing fixtures (no new content — preserves test traceability).

Purpose: Closes the Wave-0 fixture dependency for Plans 04, 06, 08. Without this fixture, the synthetic-atlas spec, loader-atlas-less spec, and round-trip integration test cannot run.

Output: 1 JSON file + 4 PNG files in `fixtures/SIMPLE_PROJECT_NO_ATLAS/`. NO `.atlas` file (the entire point — exercises the synthesis fall-through).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-seed-001-atlas-less-mode-json-images-folder-no-atlas/21-CONTEXT.md
@.planning/phases/21-seed-001-atlas-less-mode-json-images-folder-no-atlas/21-RESEARCH.md
@.planning/phases/21-seed-001-atlas-less-mode-json-images-folder-no-atlas/21-PATTERNS.md
@.planning/phases/21-seed-001-atlas-less-mode-json-images-folder-no-atlas/21-VALIDATION.md

<interfaces>
<!-- Source fixture inventory verified during planning: -->

fixtures/SIMPLE_PROJECT/ contents:
- SIMPLE_TEST.atlas (144 bytes)  ← do NOT copy
- SIMPLE_TEST.json (19,200 bytes) ← copy this
- SIMPLE_TEST.png (42,007 bytes) ← do NOT copy (this is the packed atlas page, not per-region)
- (other variants like skeleton.json, skeleton2.json — ignore)

fixtures/EXPORT_PROJECT/images/ contents (already per-region PNGs, ready to copy):
- CIRCLE.png (2,975 bytes)
- SQUARE.png (5,376 bytes)
- SQUARE2.png (637 bytes)
- TRIANGLE.png (3,621 bytes)

The JSON references attachments named CIRCLE, SQUARE, SQUARE2, TRIANGLE via the skin/slot/attachment hierarchy. Each attachment's PNG must exist at fixtures/SIMPLE_PROJECT_NO_ATLAS/images/<name>.png for the synthesizer's PNG header reads to succeed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fixtures/SIMPLE_PROJECT_NO_ATLAS/ + copy JSON + 4 PNGs</name>
  <files>fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json, fixtures/SIMPLE_PROJECT_NO_ATLAS/images/CIRCLE.png, fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE.png, fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE2.png, fixtures/SIMPLE_PROJECT_NO_ATLAS/images/TRIANGLE.png</files>
  <read_first>
    - fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json (the source JSON — verify it lists CIRCLE, SQUARE, SQUARE2, TRIANGLE in skin attachments before copying)
    - fixtures/EXPORT_PROJECT/images/ (verify all 4 PNG files exist before copying)
    - .planning/phases/21-seed-001-atlas-less-mode-json-images-folder-no-atlas/21-CONTEXT.md (line 153 + canonical_refs section — fixture name + content list)
    - .planning/phases/21-seed-001-atlas-less-mode-json-images-folder-no-atlas/21-VALIDATION.md (Wave 0 Requirements — fixture composition list)
    - .planning/phases/21-seed-001-atlas-less-mode-json-images-folder-no-atlas/21-PATTERNS.md (§fixtures/SIMPLE_PROJECT_NO_ATLAS/ section)
  </read_first>
  <action>
**Step A — Verify source files exist:**

```bash
ls -la fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json
ls -la fixtures/EXPORT_PROJECT/images/CIRCLE.png fixtures/EXPORT_PROJECT/images/SQUARE.png fixtures/EXPORT_PROJECT/images/SQUARE2.png fixtures/EXPORT_PROJECT/images/TRIANGLE.png
```

All 5 files MUST exist. If any are missing, abort and report — do not improvise replacements.

**Step B — Verify the source JSON references the expected region names:**

```bash
grep -E '"(CIRCLE|SQUARE|SQUARE2|TRIANGLE)"' fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json | head -10
```

The output should show the four region names appearing as attachment keys or path values. If any are missing, report and STOP — the fixture composition assumed these four regions and the synthesizer test depends on them.

**Step C — Create the directory structure:**

```bash
mkdir -p fixtures/SIMPLE_PROJECT_NO_ATLAS/images
```

**Step D — Copy the JSON (byte-exact):**

```bash
cp fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json
```

**Step E — Copy all 4 PNGs (byte-exact):**

```bash
cp fixtures/EXPORT_PROJECT/images/CIRCLE.png fixtures/SIMPLE_PROJECT_NO_ATLAS/images/CIRCLE.png
cp fixtures/EXPORT_PROJECT/images/SQUARE.png fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE.png
cp fixtures/EXPORT_PROJECT/images/SQUARE2.png fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE2.png
cp fixtures/EXPORT_PROJECT/images/TRIANGLE.png fixtures/SIMPLE_PROJECT_NO_ATLAS/images/TRIANGLE.png
```

**Step F — Verify NO .atlas file got created:**

```bash
ls fixtures/SIMPLE_PROJECT_NO_ATLAS/*.atlas 2>&1
```

Expected output: "No such file or directory" — the absence of `.atlas` is the entire point of this fixture.

**Step G — Verify byte-identical copies via shasum:**

```bash
shasum fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json
shasum fixtures/EXPORT_PROJECT/images/CIRCLE.png fixtures/SIMPLE_PROJECT_NO_ATLAS/images/CIRCLE.png
shasum fixtures/EXPORT_PROJECT/images/SQUARE.png fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE.png
shasum fixtures/EXPORT_PROJECT/images/SQUARE2.png fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE2.png
shasum fixtures/EXPORT_PROJECT/images/TRIANGLE.png fixtures/SIMPLE_PROJECT_NO_ATLAS/images/TRIANGLE.png
```

The hash for each pair MUST match — this confirms byte-identical copies and gives a reproducible fixture provenance trail.

**Do NOT modify the JSON or PNG content.** This fixture is purely a directory layout test asset; the content is inherited verbatim from existing golden fixtures.
  </action>
  <verify>
    <automated>test -f fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json && test -f fixtures/SIMPLE_PROJECT_NO_ATLAS/images/CIRCLE.png && test -f fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE.png && test -f fixtures/SIMPLE_PROJECT_NO_ATLAS/images/SQUARE2.png && test -f fixtures/SIMPLE_PROJECT_NO_ATLAS/images/TRIANGLE.png && ! test -e fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.atlas</automated>
  </verify>
  <acceptance_criteria>
    - Directory exists: `test -d fixtures/SIMPLE_PROJECT_NO_ATLAS`
    - JSON exists: `test -f fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json`
    - All 4 PNGs exist: `ls fixtures/SIMPLE_PROJECT_NO_ATLAS/images/*.png | wc -l` returns 4
    - PNG names match expected: `ls fixtures/SIMPLE_PROJECT_NO_ATLAS/images/ | sort | tr '\n' ' '` returns "CIRCLE.png SQUARE.png SQUARE2.png TRIANGLE.png "
    - NO .atlas file: `ls fixtures/SIMPLE_PROJECT_NO_ATLAS/*.atlas 2>/dev/null | wc -l` returns 0
    - JSON shasum equals source: `[ "$(shasum fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json | cut -d' ' -f1)" = "$(shasum fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json | cut -d' ' -f1)" ]`
    - CIRCLE.png shasum equals source: `[ "$(shasum fixtures/SIMPLE_PROJECT_NO_ATLAS/images/CIRCLE.png | cut -d' ' -f1)" = "$(shasum fixtures/EXPORT_PROJECT/images/CIRCLE.png | cut -d' ' -f1)" ]`
    - JSON references all 4 regions: `grep -cE '"(CIRCLE|SQUARE|SQUARE2|TRIANGLE)"' fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json` returns at least 4
  </acceptance_criteria>
  <done>fixtures/SIMPLE_PROJECT_NO_ATLAS/ contains SIMPLE_TEST.json + 4 per-region PNGs in images/ subfolder; no .atlas file; all files are byte-identical copies of existing fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(No trust boundary changes — this is a test fixture under fixtures/, never bundled into the shipped app, never read by user-supplied paths.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering | Fixture content drifts vs source | mitigate | shasum verification in acceptance criteria locks byte-identical provenance to existing golden fixtures (SIMPLE_PROJECT and EXPORT_PROJECT). |
| T-21-03-02 | Information Disclosure | Fixture committed to repo | accept | All source fixtures (SIMPLE_PROJECT, EXPORT_PROJECT) already in repo; this is just a third arrangement of the same content. |
</threat_model>

<verification>
1. `test -d fixtures/SIMPLE_PROJECT_NO_ATLAS` returns 0.
2. `ls fixtures/SIMPLE_PROJECT_NO_ATLAS/images/ | sort` returns exactly: CIRCLE.png, SQUARE.png, SQUARE2.png, TRIANGLE.png.
3. `ls fixtures/SIMPLE_PROJECT_NO_ATLAS/*.atlas 2>/dev/null | wc -l` returns 0 (no atlas file).
4. SHA-1 hashes match source fixtures (proves byte-identical copies).
</verification>

<success_criteria>
- Golden fixture exists at fixtures/SIMPLE_PROJECT_NO_ATLAS/ with the documented composition.
- Plans 04 + 06 + 08 can resolve `path.resolve('fixtures/SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST.json')` and find a valid Spine JSON beside an images-only folder.
</success_criteria>

<output>
After completion, create `.planning/phases/21-seed-001-atlas-less-mode-json-images-folder-no-atlas/21-03-fixture-SUMMARY.md` recording: file count, total fixture byte size, the four expected region names confirmed in the JSON.
</output>
