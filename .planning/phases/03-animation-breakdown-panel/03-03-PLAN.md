---
phase: 03-animation-breakdown-panel
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/renderer/src/App.tsx
  - fixtures/SIMPLE_PROJECT/.cli-golden.txt
autonomous: false
requirements:
  - F4.1
  - F4.2
  - F4.3
  - F4.4

threat_model:
  boundaries:
    - name: Renderer top-level routing (App.tsx loaded branch)
      description: App.tsx swaps the loaded-branch child component from GlobalMaxRenderPanel (direct Phase 2 rendering) to AppShell (Phase 3 wrapping of both panels). DropZone still wraps the entire window; idle/loading/error branches stay unchanged. The change is purely a child-component substitution.
    - name: Phase-level human verification (Electron runtime behavior)
      description: Phases 1 + 2 both caught Rule 4 deviations at human-verify that escaped every automated gate (Plan 01-05 sandbox+ESM preload, Plan 02-03 Node 24 main-bundle ESM ban + per-slot-vs-per-texture dedupe). Plan 03-03 retains the human-verify checkpoint as the load-bearing last gate — sampler signature changes + renderer state interactions cannot be fully exercised by unit tests.
  threats:
    - id: T-03-03-01
      category: Denial of Service via broken render path
      component: App.tsx loaded-branch substitution
      severity: low
      disposition: mitigate
      mitigation: "The swap is a two-line diff (import + render site). Electron runtime loading the app after the swap exercises the full IPC path + both panels via the tab strip, validating end-to-end that Plan 03-01 + Plan 03-02 land cleanly together. A broken swap surfaces as a blank loaded screen or a React runtime error immediately visible to the human-verify checker."
    - id: T-03-03-02
      category: Tampering via sampler-signature regression escaping unit tests
      component: Electron runtime sampling on real user rigs
      severity: medium
      disposition: mitigate
      mitigation: "N2.1 perf gate (500ms SIMPLE_TEST budget) stays live in tests/core/sampler.spec.ts across Plan 03-01's extension. Human-verify exercises Jokerman (23 attachments × 18 animations) + optionally Girl (145 attachments × 15 animations) by manual drop — if sampler time exceeds expectations or per-animation cards render incorrectly on real rigs, verifier flags before phase close. Acceptable runtime headroom: 500ms N2.1 gate, Jokerman expected ~20-40ms, Girl expected ~200-400ms."
    - id: T-03-03-03
      category: Tampering via CJS main/preload regression (Plan 02-03 Rule 4 class)
      component: Electron packaged-app boot after sampler extension
      severity: medium
      disposition: mitigate
      mitigation: "Plan 03-01 does not introduce any top-level await, dynamic import(), or new ESM-requiring dependency — sampler extension is pure synchronous TS. Four arch.spec.ts guards (2 preload-CJS + 2 main-CJS) run on every `npm run test` and lock the CJS output shape. Human-verify includes an `npm run dev` smoke — if Electron fails to boot, that is a Plan 01-05/02-03 class regression and verifier escalates immediately."
    - id: T-03-03-04
      category: Information Disclosure via D-17 console echo (inherited)
      component: App.tsx useEffect console.log
      severity: low
      disposition: accept
      mitigation: "The D-17 console.log(state.summary) echo is retained from Phase 1 for developer tooling. Now includes the new animationBreakdown field, which is primitives-only — no sensitive data. Production packaging is a Phase 9 concern per prior-phase security notes. Phase 3 does not add new console output paths."

must_haves:
  truths:
    - "src/renderer/src/App.tsx imports AppShell instead of the prior Phase 2 panel in the loaded branch; the swap is a two-line diff (import + render site)."
    - "AppState discriminated union + DropZone full-window wrapper + idle/loading/error branches + D-17 console echo useEffect all stay byte-identical."
    - "Dropping fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json in the dev Electron window renders the AppShell tab strip with Global first (active) and Animation Breakdown second."
    - "Global tab shows 3 dedup'd rows matching Phase 2 (CIRCLE, SQUARE, TRIANGLE)."
    - "Animation tab renders the Setup Pose card expanded (3 rows) + 4 animation cards collapsed (PATH, SIMPLE_ROTATION, SIMPLE_SCALE, TRANSFORM). Each header follows the D-65 format. Expanding a non-empty card shows the 7-column table with correct Bone Path, size labels, scale label, and disabled Override button."
    - "Clicking a Source Animation chip in the Global panel switches to the Animation tab, scrolls + auto-expands + flashes the target card for ~900ms."
    - "Clicking a chip for a Setup Pose row routes to the Setup Pose top card with the same behavior."
    - "Cross-card search filters rows by attachment name, auto-expands cards with matches (D-71), and highlights matches via the Phase 2 mark class."
    - "Switching tabs back-and-forth does not re-flash the previously-focused card (RESEARCH Pitfall 5 resolved)."
    - "Dropping a fresh skeleton resets activeTab to global and userExpanded to the initial setup-pose-only seed."
    - "Packaged .dmg boots and drop flow works identically (Plan 01-05 gate preserved — CJS main + preload still intact)."
    - "fixtures/SIMPLE_PROJECT/.cli-golden.txt cleaned up post-verify (no longer needed by any future phase)."
    - "03-VALIDATION.md frontmatter flipped to `nyquist_compliant: true` + `wave_0_complete: true`."
    - "STATE.md updated with Phase 3 close progress."
    - "All existing automated gates stay green: npm run test + npm run typecheck + npx electron-vite build + CLI byte-for-byte diff empty."
  artifacts:
    - path: "src/renderer/src/App.tsx"
      provides: "Loaded branch renders AppShell"
      contains: "AppShell"
    - path: ".planning/phases/03-animation-breakdown-panel/03-VALIDATION.md"
      provides: "Per-task verification map populated + frontmatter flipped"
    - path: ".planning/STATE.md"
      provides: "Phase 3 progress recorded through Plan 03-03 close"
  key_links:
    - from: "src/renderer/src/App.tsx loaded branch"
      to: "src/renderer/src/components/AppShell.tsx"
      via: "import { AppShell } from './components/AppShell'"
      pattern: "AppShell summary=\\{state\\.summary\\}"
    - from: "src/renderer/src/components/AppShell.tsx"
      to: "both panels"
      via: "conditional render under activeTab state"
      pattern: "activeTab === 'animation'"
---

<objective>
Wire AppShell into App.tsx's loaded branch (a two-line diff), run the full automated gate suite, and run the Plan 03-03 `checkpoint:human-verify` gate that exercises the end-to-end drop flow + tab switching + collapsible cards + Bone Path + disabled Override button + Source Animation jump-target behavior in the dev Electron window.

Purpose: this is the Phase 3 integration + verification step. Plan 03-01's data layer + Plan 03-02's renderer landed independently; this plan wires them into the app's top-level state machine and runs the only gate that can catch runtime-only issues (Electron renderer behavior, CSS discrepancies, tab+expand+flash interaction flows, sampler runtime on real rigs). Phases 1 + 2 both caught critical Rule 4 deviations at human-verify that automated gates missed; Plan 03-03 retains that discipline.

Also covers phase-close administrivia: `.cli-golden.txt` cleanup (the repo-relative gitignored handoff artifact retained from Plan 02-01 through Phase 3 is no longer needed), 03-VALIDATION.md frontmatter flip, STATE.md update.

Output:
- `src/renderer/src/App.tsx` (modified) — two-line diff: import swap + render-site tag swap. Header JSDoc preserves Phase 2's prose (no forbidden-literal footguns).
- Human-verify signoff recorded in Plan 03-03 summary.
- `fixtures/SIMPLE_PROJECT/.cli-golden.txt` removed (gitignored cleanup).
- `.planning/phases/03-animation-breakdown-panel/03-VALIDATION.md` frontmatter flipped to `nyquist_compliant: true` + `wave_0_complete: true`.
- `.planning/STATE.md` updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-animation-breakdown-panel/03-CONTEXT.md
@.planning/phases/03-animation-breakdown-panel/03-RESEARCH.md
@.planning/phases/03-animation-breakdown-panel/03-UI-SPEC.md
@.planning/phases/03-animation-breakdown-panel/03-PATTERNS.md
@.planning/phases/03-animation-breakdown-panel/03-VALIDATION.md
@.planning/phases/03-animation-breakdown-panel/03-01-PLAN.md
@.planning/phases/03-animation-breakdown-panel/03-02-PLAN.md
@.planning/phases/02-global-max-render-source-panel/02-03-PLAN.md
@.planning/phases/01-electron-react-scaffold/01-05-PLAN.md
@CLAUDE.md

Sources to study before touching code:
@src/renderer/src/App.tsx
@src/renderer/src/components/AppShell.tsx
@src/renderer/src/panels/AnimationBreakdownPanel.tsx
@src/renderer/src/panels/GlobalMaxRenderPanel.tsx
@fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json

<interfaces>
<!-- Contracts the executor must use. Copy these verbatim. -->

Current `src/renderer/src/App.tsx` import (line 23):

```tsx
import { GlobalMaxRenderPanel } from './panels/GlobalMaxRenderPanel';
```

After this plan:

```tsx
import { AppShell } from './components/AppShell';
```

Current `src/renderer/src/App.tsx` render site (line 71):

```tsx
      {state.status === 'loaded' && <GlobalMaxRenderPanel summary={state.summary} />}
```

After this plan:

```tsx
      {state.status === 'loaded' && <AppShell summary={state.summary} />}
```

Everything else in App.tsx stays byte-identical:
- Header JSDoc (lines 1-20) — already uses prose ("the global-max render panel"); no literal identifier to rewrite.
- AppState discriminated union (lines 30-34).
- handleLoadStart / handleLoad useCallbacks (lines 39-49).
- D-17 console.log echo useEffect (lines 51-57).
- DropZone wrapper + idle/loading/error branches (lines 59-83).

Expected Electron runtime behavior after the swap (drop SIMPLE_TEST.json):

1. AppShell header appears above the panel body. Filename chip shows the skeleton JSON absolute path. Tab strip shows `Global` (active, font-semibold + orange underline) and `Animation Breakdown` (inactive, muted).
2. Global tab (default active). Phase 2 table renders with 3 dedup'd rows (CIRCLE, SQUARE, TRIANGLE), default sort peakScale DESC, search + select-all + shift-click all functional. Source Animation column renders as a clickable button (onJumpToAnimation prop defined) with hover state visible on mouseover.
3. Click Animation Breakdown tab. Panel title "Animation Breakdown" + cross-card SearchBar. Setup Pose (Default) card expanded by default with 3 rows sorted Scale DESC. SQUARE row wins via SQUARE2 slot (2.000× pre-scaled bone); scaleLabel reads `2.000×`. Four animation cards collapsed.
4. Click a collapsed animation card header. Caret flips to `▾`; body expands inline showing the 7-column table with correct Bone Path (mid-ellipsis on long chains), Source W×H, Scale, Peak W×H, Frame, and disabled Override button (hover shows `title="Coming in Phase 4"` tooltip via native browser hover).
5. Type `CIRCLE` in the breakdown panel SearchBar. Auto-expand behavior kicks in per D-71; match highlighted via `<mark>`; cards with zero matches show filtered-suffix header.
6. Clear search (ESC or click the clear button). User-expanded state restores.
7. Switch back to Global tab. Tab switches; breakdown panel unmounts.
8. In Global tab, click a Source Animation chip. Tab switches to Animation Breakdown. Target card scrolls into view, auto-expands, flashes with the accent ring for ~900ms. After flash, ring clears; manual navigation is clean.
9. Click a chip for a Setup Pose row. Routes to Setup Pose card with scroll + flash.
10. Tab switch Global → Animation → Global → Animation: no re-flash (Pitfall 5 resolved).
11. Drop a NEW skeleton JSON. DropZone's loading state flashes; loaded state re-renders AppShell with activeTab reset to 'global' and userExpanded reset to the initial seed.

Dev environment setup (verify BEFORE the human-verify session):

```bash
env | grep ELECTRON_RUN_AS_NODE
# If set, unset and restart shell:
unset ELECTRON_RUN_AS_NODE
```

CLI byte-for-byte gate (T-03-03-03 class — reused from Plan 03-01 Task 4):

```bash
npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json > fixtures/SIMPLE_PROJECT/.cli-after.txt 2>&1
diff fixtures/SIMPLE_PROJECT/.cli-golden.txt fixtures/SIMPLE_PROJECT/.cli-after.txt
# Must be empty.
```

After re-diff passes, the golden can be removed (Plan 02-01 to Plan 03-03 handoff complete):

```bash
rm fixtures/SIMPLE_PROJECT/.cli-golden.txt fixtures/SIMPLE_PROJECT/.cli-after.txt
```

Both are covered by the `fixtures/SIMPLE_PROJECT/.cli-*.txt` gitignore pattern.

Grep-forbidden tokens watchlist: same as Plans 03-01 + 03-02 (inherited). This plan's only code change is the two-line App.tsx swap; no new docstrings introduced. The existing App.tsx header (Phase 2 close) uses prose, no literal `GlobalMaxRenderPanel` citation.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap App.tsx loaded branch from GlobalMaxRenderPanel to AppShell + CLI re-diff + cleanup</name>
  <files>src/renderer/src/App.tsx, fixtures/SIMPLE_PROJECT/.cli-golden.txt</files>
  <read_first>
    - src/renderer/src/App.tsx (FULL — confirm the two touch-point lines; header JSDoc stays byte-identical)
    - src/renderer/src/components/AppShell.tsx (Plan 03-02 Task 1 — confirm exports + prop surface)
    - fixtures/SIMPLE_PROJECT/.cli-golden.txt (Plan 02-01 golden — must exist from retention)
    - .planning/phases/03-animation-breakdown-panel/03-01-PLAN.md (confirm Plan 03-01 completed and all tests GREEN)
    - .planning/phases/03-animation-breakdown-panel/03-02-PLAN.md (confirm AppShell + AnimationBreakdownPanel both exist)
  </read_first>
  <action>
Step 1 — run the full automated gate BEFORE the App.tsx swap to confirm clean Phase 3 Wave 1+2 state:

```bash
npm run typecheck    # both projects green
npm run test         # all tests GREEN
npm run test -- tests/arch.spec.ts   # 4/4 arch guards green
```

If any gate fails, STOP — do not land the App.tsx swap over a broken base.

Step 2 — CLI byte-for-byte re-diff:

```bash
test -s fixtures/SIMPLE_PROJECT/.cli-golden.txt
npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json > fixtures/SIMPLE_PROJECT/.cli-after.txt 2>&1
diff fixtures/SIMPLE_PROJECT/.cli-golden.txt fixtures/SIMPLE_PROJECT/.cli-after.txt
```

If diff is non-empty, STOP and audit Plan 03-01's sampler adapter.

Step 3 — edit `src/renderer/src/App.tsx`:

(a) Replace the import at line 23:

```tsx
// BEFORE:
import { GlobalMaxRenderPanel } from './panels/GlobalMaxRenderPanel';

// AFTER:
import { AppShell } from './components/AppShell';
```

(b) Replace the render site at line 71:

```tsx
// BEFORE:
      {state.status === 'loaded' && <GlobalMaxRenderPanel summary={state.summary} />}

// AFTER:
      {state.status === 'loaded' && <AppShell summary={state.summary} />}
```

(c) The header JSDoc (lines 1-20) uses prose ("the global-max render panel"); it remains accurate. Do NOT edit the header.

(d) Grep-literal compliance check (should all pass — no new literals introduced):

```bash
! grep -q "font-medium" src/renderer/src/App.tsx
! grep -q "process.platform" src/renderer/src/App.tsx
```

Step 4 — run the full automated gate suite AFTER the swap:

```bash
npm run typecheck
npm run test
npm run test -- tests/arch.spec.ts
npx electron-vite build
```

Step 5 — verify Electron dev boot (pre-human-verify smoke):

```bash
unset ELECTRON_RUN_AS_NODE
npm run dev &
sleep 5
kill %1 2>/dev/null || true
```

If the Electron window fails to open, STOP and investigate the main/preload CJS contract.

Step 6 — cleanup the golden file:

```bash
rm -f fixtures/SIMPLE_PROJECT/.cli-golden.txt
rm -f fixtures/SIMPLE_PROJECT/.cli-after.txt
```

Both are gitignored; removal produces no git status change.

Step 7 — commit atomically. Scope: `refactor(03-breakdown): wire AppShell into App.tsx loaded branch`.
  </action>
  <verify>
    <automated>grep -q "import { AppShell } from './components/AppShell';" src/renderer/src/App.tsx &amp;&amp; grep -q "AppShell summary=" src/renderer/src/App.tsx &amp;&amp; ! grep -q "import { GlobalMaxRenderPanel }" src/renderer/src/App.tsx &amp;&amp; ! grep -q "GlobalMaxRenderPanel summary=" src/renderer/src/App.tsx &amp;&amp; ! grep -q "font-medium" src/renderer/src/App.tsx &amp;&amp; ! grep -q "process.platform" src/renderer/src/App.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run test &amp;&amp; npx electron-vite build 2>&amp;1 | grep -E "built in|output" &amp;&amp; ! test -f fixtures/SIMPLE_PROJECT/.cli-golden.txt &amp;&amp; ! test -f fixtures/SIMPLE_PROJECT/.cli-after.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { AppShell } from './components/AppShell';" src/renderer/src/App.tsx` → exit 0
    - `grep -q "<AppShell summary={state.summary} />" src/renderer/src/App.tsx` → exit 0
    - `! grep -q "import { GlobalMaxRenderPanel } from './panels/GlobalMaxRenderPanel';" src/renderer/src/App.tsx` → exit 0 (old import removed)
    - `! grep -q "<GlobalMaxRenderPanel summary={state.summary} />" src/renderer/src/App.tsx` → exit 0 (old render site removed)
    - `! grep -q "font-medium" src/renderer/src/App.tsx` → exit 0 (UI-SPEC negative grep preserved)
    - `! grep -q "process.platform" src/renderer/src/App.tsx` → exit 0 (D-23 preserved)
    - `npm run typecheck` → exit 0 on both projects
    - `npm run test` → exit 0 with ≥80 passed + 1 skip (no regression from Plan 03-02 close)
    - `npm run test -- tests/arch.spec.ts` → exit 0 (4/4 arch guards)
    - `npx electron-vite build` → exit 0
    - `! test -f fixtures/SIMPLE_PROJECT/.cli-golden.txt` → exit 0 (cleaned up)
    - `! test -f fixtures/SIMPLE_PROJECT/.cli-after.txt` → exit 0 (cleaned up)
  </acceptance_criteria>
  <done>
    App.tsx renders AppShell in the loaded branch (two-line diff); all automated gates green; Electron dev boot smoke passes; golden cleanup complete. Ready for Task 2 human-verify gate.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify end-to-end drop flow + tab switching + card UX + jump-target behavior</name>
  <what-built>
Plan 03-01 + Plan 03-02 + Task 1's App.tsx wiring together deliver the full Phase 3 Animation Breakdown panel experience:
- Core data: sampler emits globalPeaks + perAnimation + setupPosePeaks in a single lifecycle pass; analyzer produces `AnimationBreakdown[]` with Setup Pose top card + one card per animation; IPC payload carries the new field.
- Renderer: AppShell top-tab container with filename chip + tab strip; AnimationBreakdownPanel with collapsible cards + Bone Path + disabled Override + cross-card search + flash highlight; GlobalMaxRenderPanel Source Animation chip upgraded to interactive jump-target button; filename chip hoisted out of the Global panel into AppShell.
- Wiring: App.tsx loaded branch now renders `<AppShell summary={state.summary} />` instead of `<GlobalMaxRenderPanel ... />`.

No new IPC channels. No new runtime dependencies. Full test suite green (~80 passed + 1 skip); build green; CLI byte-for-byte preserved.
  </what-built>
  <how-to-verify>
**Pre-verify setup:**

```bash
# 1. Scrub any ELECTRON_RUN_AS_NODE leak (Plan 02-03 environment caveat).
unset ELECTRON_RUN_AS_NODE
env | grep ELECTRON_RUN_AS_NODE  # must print nothing

# 2. Confirm all automated gates green.
npm run typecheck
npm run test
npm run test -- tests/arch.spec.ts   # 4/4 guards
npx electron-vite build

# 3. Start dev Electron window.
npm run dev
```

**Verification checks (exercise each in the dev Electron window):**

1. **Idle state.** Before dropping anything: empty-state message visible (`Drop a .spine JSON file anywhere in this window`). No tabs, no panels.

2. **Drop SIMPLE_TEST.json** (`fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`). Loading hint flashes briefly; loaded state renders:
   - Header bar with filename chip (shows absolute path) at left, tab strip `[ Global ] [ Animation Breakdown ]` centered.
   - `Global` tab is active: orange color + bold weight + 2px orange underline.
   - `Animation Breakdown` tab inactive: muted text, no underline.
   - Below header: Global panel renders with SearchBar + 3 dedup'd rows (CIRCLE, SQUARE, TRIANGLE).
   - Default sort: peakScale DESC. SQUARE row at top with 2.000× (pre-scaled SQUARE2 bone winner).
   - Source Animation column shows each row's originating animation chip. HOVER over a chip: the chip should show a subtle background tint (`hover:bg-accent/10` — a faint orange) indicating it's now a clickable button.
   - D-17 console echo: open DevTools (View → Toggle Developer Tools) → console shows `[Spine Texture Manager] Loaded skeleton summary:` followed by the full SkeletonSummary object INCLUDING `animationBreakdown: Array(5)` (1 setup-pose + 4 animation cards).

3. **Click `Animation Breakdown` tab.** Tab strip updates: `Animation Breakdown` becomes active (orange + bold + underline); `Global` becomes inactive. Panel body switches to the breakdown panel:
   - Title "Animation Breakdown" at the top left.
   - SearchBar with placeholder `Filter rows across cards…` (tabbed-ellipsis, single character).
   - Setup Pose (Default) card at top, EXPANDED by default. Header: `▾ Setup Pose (Default) — 3 unique assets referenced`. Table inside shows 3 rows, Scale DESC: SQUARE (2.000×) first, then CIRCLE and TRIANGLE (both approximately 1.000×).
   - Below: 4 collapsed animation cards in JSON order (PATH, SIMPLE_ROTATION, SIMPLE_SCALE, TRANSFORM). Each header: `▸ {animationName} — {N} unique assets referenced` or `▸ {animationName} — No assets referenced` if the animation produced zero affected attachments (SIMPLE_ROTATION may fall into this bucket per RESEARCH Pitfall 1 — pure rotation can have zero scale delta; this is acceptable, NOT a bug).
   - Cards are visually distinct: each is a bordered rectangle with subtle background; the expanded card has a divider line between header and table body.

4. **Expand a collapsed animation card.** Click any animation card's header. Caret flips from `▸` to `▾`; body expands inline showing the 7-column table. Verify each column:
   - **Attachment:** shows attachment name (CIRCLE / SQUARE / TRIANGLE on this fixture).
   - **Bone Path:** shows truncated path with `→` separator. For TRIANGLE (slot on CHAIN_8), the path is 11 tokens long and truncates to something like `root → … → TRIANGLE → TRIANGLE`. HOVER over the Bone Path cell: native browser tooltip should reveal the full path (`root → CTRL → CHAIN_2 → CHAIN_3 → CHAIN_4 → CHAIN_5 → CHAIN_6 → CHAIN_7 → CHAIN_8 → TRIANGLE → TRIANGLE`).
   - **Source W×H:** integer size like `64×64`.
   - **Scale:** formatted `X.XXX×` with 3 decimals and trailing Unicode multiplication sign.
   - **Peak W×H:** whole-pixel size like `114×114`.
   - **Frame:** integer frame number, right-aligned.
   - **Actions:** disabled Override button. HOVER the button: native tooltip shows `Coming in Phase 4`. Button is visually dimmed (opacity 50). Clicking it does nothing (disabled).

5. **Click a card header to COLLAPSE.** Caret returns to `▸`, body disappears. Inline collapse with no animation.

6. **Cross-card search.** Type `CIRCLE` in the panel's SearchBar:
   - All cards remain rendered (per UI-SPEC).
   - Cards containing CIRCLE auto-expand (D-71). Setup Pose was already expanded; any animation card containing CIRCLE (which should be all of them on SIMPLE_TEST given CIRCLE's weighted-mesh rig) expands with matching rows showing `CIRCLE` highlighted in the Attachment column with an orange `<mark>` background.
   - Cards with zero CIRCLE matches (unlikely on this fixture) would show headers `▸ {name} — 0 / {N} unique assets — filtered` and stay collapsed.
   - Headers of matched cards update to `▾ {name} — M / N unique assets — filtered`.

7. **Clear search.** Press ESC (first press clears the query) or click the ✕ button in the SearchBar. Cards revert to their user-chosen expand state (user-collapsed animation cards stay collapsed if the user clicked to collapse them during the search; cards that were only expanded by the D-71 union re-collapse).

8. **Tab switch: Animation Breakdown → Global.** Click `Global` tab. The breakdown panel unmounts; Global panel remounts with its prior state (sort, search query, selection Set — all preserved from before the tab switch because the GlobalMaxRenderPanel is part of the AppShell's conditional render; state lives in that component's useState hooks and is preserved only while the component stays mounted, so after a switch-out and switch-back the Global panel's state DOES reset to defaults — this is expected behavior per CONTEXT D-50, NO persistence between switches).

9. **Source Animation jump (animation row).** Back on the Global tab, identify a row whose Source Animation chip shows an animation name (e.g., CIRCLE might show `SIMPLE_SCALE` or similar). Click the chip:
   - Tab switches to Animation Breakdown instantly.
   - The matching animation card scrolls into view (smooth scroll anchoring at top).
   - The card auto-expands (adds to userExpanded).
   - The card border briefly flashes with an orange outline (`ring-2 ring-accent` — accent-colored outer ring). Flash lasts ~900ms then clears.
   - After the flash, manual tab navigation is unaffected — no further flashing.

10. **Source Animation jump (setup-pose row).** Find a row whose Source Animation chip shows `Setup Pose (Default)` (on SIMPLE_TEST, SQUARE's dedupe winner is the SQUARE2 slot at setup pose — its chip should show this label). Click the chip:
    - Tab switches to Animation Breakdown.
    - Setup Pose card scrolls into view + flashes briefly. Since Setup Pose is expanded by default, no expand-state change visible.

11. **Re-flash isolation (Pitfall 5 resolution).** After completing check 9 or 10, click `Global` tab, wait 2 seconds, click `Animation Breakdown` tab: the previously-flashed card should NOT re-flash. Cards stay in their current expand state without any ring-accent.

12. **Drop a fresh SIMPLE_TEST.json again.** (Or drop a different .json to test reset behavior — a different skeleton would re-sample; SIMPLE_TEST re-drops are fine too.) The AppShell should fully remount:
    - activeTab returns to `Global`.
    - userExpanded returns to the initial setup-pose-only seed.
    - Any search query clears.

13. **Complex rig spot check (optional but recommended).** Drop `fixtures/Jokerman/` or `fixtures/Girl/` if available locally:
    - Loading state flashes (may take 1-5 seconds for Girl).
    - Global tab shows the per-texture dedup'd table with many rows.
    - Animation Breakdown tab shows Setup Pose card at top (expanded) + one card per animation (Jokerman ~18 animations; Girl ~15). Collapsed-by-default posture keeps the panel compact even on Girl.
    - Expanding Jokerman's `blink` animation (if present) should exercise the AttachmentTimeline arm of D-54 — the card should list attachments that scale-delta alone would miss (the eye-swap on the blink).
    - Bone Path on deep chains: tooltip reveals full chain; truncated form stays readable.

14. **Packaged .dmg smoke** (OPTIONAL — only if time permits):

```bash
npx electron-vite build && npx electron-builder --mac dmg
# Wait for build. Open release/*.dmg; drag app to Applications; launch with Gatekeeper bypass.
# Drop SIMPLE_TEST.json into the packaged app window; verify Animation Breakdown panel renders identically.
```

**Negative checks — things that MUST NOT happen:**

- **font-medium grep:** `grep -rn "font-medium" src/renderer/src/components/AppShell.tsx src/renderer/src/panels/AnimationBreakdownPanel.tsx src/renderer/src/panels/GlobalMaxRenderPanel.tsx src/renderer/src/App.tsx` must exit with no matches.
- **No console errors** in DevTools console across all 14 verification steps (aside from the legitimate D-17 summary echo).
- **No React `key` warnings** in console (AnimationCard uses `key={card.cardId}` which is unique per summary.animationBreakdown entry).
- **No broken layout** at the default window size (1280×800): the panel fits without horizontal scroll; the filename chip doesn't clip; the tab strip doesn't overflow.
  </how-to-verify>
  <resume-signal>Type "approved" when all 14 checks pass (or 13 of 14 if complex-rig or packaged .dmg is skipped), or describe any issues observed. If any check reveals a bug, file a gap-fix task with: (1) exact reproduction steps, (2) expected vs observed behavior, (3) which plan/file likely owns the fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-verify — flip 03-VALIDATION.md frontmatter + update STATE.md + commit</name>
  <files>.planning/phases/03-animation-breakdown-panel/03-VALIDATION.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/03-animation-breakdown-panel/03-VALIDATION.md (current — nyquist_compliant: false, wave_0_complete: false; per-task map placeholder rows)
    - .planning/STATE.md (current)
  </read_first>
  <action>
Step 1 — flip 03-VALIDATION.md frontmatter. Current frontmatter:

```yaml
 ---
phase: 3
slug: animation-breakdown-panel
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-23
 ---
```

After this plan:

```yaml
 ---
phase: 3
slug: animation-breakdown-panel
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-23
verified: 2026-04-23-or-current-date
 ---
```

Step 2 — populate the per-task verification map with actual task IDs + statuses. Replace each `03-01-XX` placeholder with the concrete plan+task like `03-01-T1`, `03-01-T2`, etc. Set Status column to `✅ green` for every row that passed.

Step 3 — update `.planning/STATE.md`:

- Under "Current phase": update to `**Phase 3 — Animation Breakdown panel** (3/3 plans executed; plan-level human-verify signed off YYYY-MM-DD; phase-level `/gsd-verify-work 3` is the next gate)`.
- Under "Current plan": update to `**Plan 03-03 closed.** Next: run `/gsd-verify-work 3` to validate Phase 3 against requirements (F4.1 / F4.2 / F4.3 / F4.4) and close the phase.`
- Under "Last completed": prepend a `Plan 03-03 (YYYY-MM-DD):` entry summarizing Task 1's swap + Task 2's human-verify outcome.
- Append rows to the Performance Metrics table for Plans 03-01 / 03-02 / 03-03.
- Update "Last session" block with current timestamp + resume state.

Step 4 — commit. Scope: `docs(03): phase 3 validation signoff + STATE update`.

Step 5 — final phase-close gate: after Task 3's commit lands, the next orchestrator step is `/gsd-verify-work 3`. That command is external to this plan; the plan completes here.
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/03-animation-breakdown-panel/03-VALIDATION.md &amp;&amp; grep -q "wave_0_complete: true" .planning/phases/03-animation-breakdown-panel/03-VALIDATION.md &amp;&amp; grep -q "status: approved" .planning/phases/03-animation-breakdown-panel/03-VALIDATION.md &amp;&amp; grep -q "Phase 3 — Animation Breakdown" .planning/STATE.md &amp;&amp; grep -q "Plan 03-03" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "nyquist_compliant: true" .planning/phases/03-animation-breakdown-panel/03-VALIDATION.md` → exit 0
    - `grep -q "wave_0_complete: true" .planning/phases/03-animation-breakdown-panel/03-VALIDATION.md` → exit 0
    - `grep -q "status: approved" .planning/phases/03-animation-breakdown-panel/03-VALIDATION.md` → exit 0
    - `! grep -q "nyquist_compliant: false" .planning/phases/03-animation-breakdown-panel/03-VALIDATION.md` → exit 0
    - `! grep -q "wave_0_complete: false" .planning/phases/03-animation-breakdown-panel/03-VALIDATION.md` → exit 0
    - `grep -q "Phase 3 — Animation Breakdown" .planning/STATE.md` → exit 0 (current phase updated)
    - `grep -q "Plan 03-03" .planning/STATE.md` → exit 0 (plan close entry present)
    - `grep -q "/gsd-verify-work 3" .planning/STATE.md` → exit 0 (next-gate reference)
  </acceptance_criteria>
  <done>
    03-VALIDATION.md frontmatter flipped; STATE.md updated through Phase 3 close; commit landed. Phase 3 ready for `/gsd-verify-work 3` as the next orchestrator step.
  </done>
</task>

</tasks>

<verification>
Plan-level gates for 03-03:
- **F4.1 end-to-end:** Collapsible cards render with the D-65 header format; expanding shows 7-column table. Verified in human-verify Task 2 checks 3-5.
- **F4.2 end-to-end:** Setup Pose (Default) card renders first + expanded + 3 rows on SIMPLE_TEST. Verified in check 3.
- **F4.3 end-to-end:** Bone Path renders with `→` separator + mid-ellipsis on long chains + title tooltip. Source + Scale + Peak + Frame columns populated. Verified in check 4.
- **F4.4 end-to-end:** Override button rendered disabled + `title="Coming in Phase 4"` tooltip + visual dimming (opacity-50). Verified in check 4.
- **D-49 + D-72 (resolve Phase 2 deferrals):** Filename chip hoisted into AppShell + Source Animation chip upgraded to jump-target button with flash. Verified in checks 2 + 9 + 10.
- **D-66 flash (RESEARCH Pitfall 5 resolved):** Jump-target card flashes ~900ms; subsequent tab-switching produces no re-flash. Verified in check 11.
- **CLI byte-for-byte preserved:** Task 1 Step 2 runs the re-diff against `fixtures/SIMPLE_PROJECT/.cli-golden.txt`; must be empty. T-03-03-03 mitigation.
- **N2.1 perf gate preserved:** Unit test assertion unchanged; human-verify spot-check on complex rigs (Jokerman/Girl) gives a runtime sanity signal. T-03-03-02 mitigation.
- **CJS main + preload preserved:** 4 arch.spec.ts guards stay green; Electron dev boot succeeds. T-03-03-03 mitigation.
- **Layer 3 preserved:** All renderer files (App.tsx, AppShell.tsx, AnimationBreakdownPanel.tsx, GlobalMaxRenderPanel.tsx) contain no imports from src/core/*. Auto-scanned on every test run.
- **Plan 03-03 checkpoint:human-verify gate:** Task 2's 14-check interactive walkthrough is the authoritative phase-close signal — Phases 1 + 2 both caught critical bugs at this gate that escaped every automated check. T-03-03-01 mitigation.
- **Phase-close administrivia:** golden file cleanup + 03-VALIDATION.md frontmatter flip + STATE.md update all land under Task 3.
</verification>

<success_criteria>
Plan 03-03 is complete when:
- `src/renderer/src/App.tsx` imports `AppShell` and renders `<AppShell summary={state.summary} />` in the `status: 'loaded'` branch; `GlobalMaxRenderPanel` no longer appears in App.tsx.
- Automated gates all green: `npm run typecheck` (both projects) + `npm run test` (~80 passed + 1 skip) + `npm run test -- tests/arch.spec.ts` (4/4 guards) + `npx electron-vite build` + `npm run cli` (byte-for-byte diff empty against retained Plan 02-01 golden).
- Task 2 `checkpoint:human-verify` signed off — all 14 verification checks (or 13 if complex-rig/packaged-DMG skipped) pass.
- `fixtures/SIMPLE_PROJECT/.cli-golden.txt` + `.cli-after.txt` removed (cleanup; both gitignored).
- `.planning/phases/03-animation-breakdown-panel/03-VALIDATION.md` frontmatter flipped: `nyquist_compliant: true`, `wave_0_complete: true`, `status: approved`, `verified: YYYY-MM-DD`.
- `.planning/STATE.md` updated: current phase entry reflects Phase 3 close; last-completed entry for Plan 03-03 added; Performance Metrics rows for Plans 03-01/02/03 appended.
- All commits on branch with consistent scope prefix `feat(03-breakdown): ...` / `refactor(03-breakdown): ...` / `docs(03): ...`.
- Phase 3 READY FOR `/gsd-verify-work 3`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-animation-breakdown-panel/03-03-SUMMARY.md` using the standard summary template. Include:
- Plan 03-03 task-by-task outcomes.
- Human-verify Task 2 result: `SIGNED OFF` + date + any gap-fixes surfaced.
- Next action: `/gsd-verify-work 3`.
- Net test count change across Phase 3 (66+1 at Phase 2 close → ~80+1 at Phase 3 close; actual count from final `npm run test`).
</output>
