---
phase: 42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding
plan: 05
type: execute
wave: 5
depends_on: ["42-04"]
files_modified:
  - tests/runtime/runtime-distinctness.spec.ts
  - .github/workflows/ci.yml
  - tests/safe01/safe01-freeze-guard.spec.ts
autonomous: true
gap_closure: true
requirements: [RT-01, SAFE-01]
must_haves:
  truths:
    - "An automated regression test (`tests/runtime/runtime-distinctness.spec.ts`) asserts the ROADMAP-SC-#2 contract verbatim: `require('@esotericsoftware/spine-core/package.json').version` === `4.3.0` and `require('spine-core-42/package.json').version` === `4.2.111` and the two differ (the literal `adapter42.version !== adapter43.version` contract), AND `Slider` + `BonePose` (the two ROADMAP-named symbols) are exported from the 4.3 module and ABSENT from `spine-core-42`"
    - "Both `import * as sc43 from '@esotericsoftware/spine-core'` (= 4.3.0) and `import * as sc42 from 'spine-core-42'` (= 4.2.111) resolve under vitest without throwing (both are non-null module objects) — proving the dual-install is real, not just declared in package.json"
    - "A dual-universe smoke (research Pitfall 4) asserts a class present in BOTH modules (`Skeleton`) is NOT the same reference across the two installs — proving two genuinely distinct module instances, not one hoisted copy"
    - "`npx vitest run tests/arch.spec.ts` still exits 0 after the new `tests/runtime/runtime-distinctness.spec.ts` lands (it imports BOTH alias specifiers ON PURPOSE; the RT-03 backstop arch anchor is `src/**`-scoped and does NOT sweep `tests/**`, so this is a sanctioned `tests/` co-import — same idiom as the already-green `tests/runtime/d13-43-load-smoke.spec.ts`)"
    - "The CR-01 D-09 hardening (user-decided HARDEN NOW, 2026-05-16): both `.github/workflows/ci.yml` (the D-09 step) and `tests/safe01/safe01-freeze-guard.spec.ts` (the vitest D-09 enforcement) resolve the npm-alias-introducing commit via the EXPLICIT-oldest form `git log --reverse -S 'spine-core-42' --format=%H -- package.json | head -1` (NOT the `tail -1`/`.pop()` LAST-hit form), AND fail loudly with a clear message if the `spine-core-42` literal is no longer present in package.json — closing the forward-fragility data-integrity blocker. Behavior-equivalent today: the resolved alias commit is STILL `cc5783f` (pickaxe set size = 1)"
    - "SAFE-01 stays green (`npx vitest run tests/safe01` exits 0) and the freeze-guard A→B ancestry assertion STILL HARD-PASSES (the non-vacuous path, not the skip path) after the CR-01 hardening — the SAFE-01 ordering invariant is unchanged, only made stronger"
    - "Neither task perturbs any frozen commit: no modification to package.json / package-lock.json (frozen COMMIT B `cc5783f`), src/renderer/** / @esotericsoftware/spine-player / tsconfig.web.json (Phase-47-owned), or 42-01..42-04 ancestry (A `1b5327d` → B `cc5783f` → repoint `1a8c18b` → C `b6f3177` → D `2360c51`, all ancestors of HEAD). Both 42-05 commits are NEW additive descendants of HEAD (D `2360c51`)"
  artifacts:
    - path: "tests/runtime/runtime-distinctness.spec.ts"
      provides: "RT-01 / ROADMAP-SC-#2 automated regression lock: version distinctness (4.3.0 != 4.2.111) + Slider/BonePose 4.3-only + dual-module-instance smoke"
      min_lines: 60
      contains: "spine-core-42"
    - path: ".github/workflows/ci.yml"
      provides: "CR-01-hardened D-09 step: explicit-oldest --reverse|head-1 alias-commit resolution + grep-presence guard (forward-fragility closed; behavior-equivalent today)"
      contains: "git log --reverse -S 'spine-core-42'"
    - path: "tests/safe01/safe01-freeze-guard.spec.ts"
      provides: "CR-01-hardened vitest D-09 enforcement: explicit-oldest --reverse alias-commit resolution + literal-presence assertion; A→B ancestry still hard-passes"
      contains: "--reverse"
  key_links:
    - from: "tests/runtime/runtime-distinctness.spec.ts"
      to: "ROADMAP Phase-42 Success Criterion #2 (RT-01: 'a runtime-distinctness test asserts adapter42.version !== adapter43.version and that Slider/BonePose exist only in the 4.3 module')"
      via: "automated vitest assertions on resolved package.json .version + Object.keys export presence/absence across both installs"
      pattern: "Slider|BonePose"
    - from: ".github/workflows/ci.yml + tests/safe01/safe01-freeze-guard.spec.ts (CR-01-hardened D-09)"
      to: "the SAFE-01 baseline-predates-alias ordering invariant (commit A `1b5327d` is a git ancestor of the alias commit `cc5783f`)"
      via: "explicit-oldest `git log --reverse -S 'spine-core-42' ... | head -1` + literal-presence guard, then `git merge-base --is-ancestor`"
      pattern: "git log --reverse -S 'spine-core-42'"
---

<objective>
This is a **GAP-CLOSURE** plan. Phase 42 (plans 42-01..42-04, all committed and FROZEN) was verified and found **one partial gap against a ROADMAP non-negotiable success criterion**, plus one human-decision item (CR-01) the orchestrator has ALREADY resolved with the user (decision: **HARDEN NOW**, 2026-05-16). This single additive plan closes both with two independent, mechanical, fully-specified, ancestry-irrelevant tasks. It does **NOT** replan or touch 42-01..42-04 — those are frozen ancestry anchors (the A→B→repoint→C→D commit chain is itself the SAFE-01 acceptance test; amending/rebasing them is ~100× recovery cost, research Pitfall 1).

**The single gap (RT-01, ROADMAP Phase-42 Success Criterion #2 — a non-negotiable contract):** "...a runtime-distinctness test asserts `adapter42.version !== adapter43.version` and that `Slider`/`BonePose` exist only in the 4.3 module." The dual-install is functionally correct and independently verified (canonical `@esotericsoftware/spine-core`@4.3.0 exports `Slider`/`BonePose`; `spine-core-42`=4.2.111 does NOT; both resolve under node/vitest/tsc). BUT the ROADMAP-contracted **automated regression test** `tests/runtime/runtime-distinctness.spec.ts` **does not exist** — it was collaterally removed by the Option-1 re-plan commit `c87a95f` (the re-plan note documented only the orthogonal typecheck-scope narrowing; the distinctness-test removal was undocumented scope reduction). ROADMAP SC #2 still mandates the regression lock. Task 1 restores it as a single consolidated spec — small, behavior-neutral, ancestry-irrelevant, a brand-new follow-up test file that perturbs NO frozen commit.

**CR-01 (user-decided HARDEN NOW):** A code-review BLOCKER (42-REVIEW.md): the D-09 SAFE-01 ancestry resolution uses `git log -S 'spine-core-42' ... | tail -1` (ci.yml) / `.pop()` (vitest freeze-guard) which selects the LAST pickaxe hit, not the introducing commit — forward-fragile if a future phase removes-then-re-adds the literal. It resolves correctly TODAY (pickaxe set size = 1, so `tail -1` == `--reverse | head -1` == the frozen COMMIT B `cc5783f`), so Task 2's fix is **behavior-equivalent today** — a robustness hardening that closes a data-integrity forward-fragility, NOT a behavior change.

Purpose: ROADMAP SC #2 makes the runtime-distinctness regression lock a non-negotiable Phase-42 acceptance criterion; without the automated test, a future dependency change that silently homogenized the two installs (e.g. a botched lockfile rewrite collapsing `spine-core-42` onto 4.3.0) would ship undetected — the exact silent-corruption failure-class the v1.6 dual-runtime de-risking machinery exists to catch. CR-01 closes the symmetric forward-fragility on the SAFE-01 ordering gate itself: once Phase 44/45 churns package.json, the `tail -1`/`.pop()` LAST-hit selection could resolve the wrong commit and either false-pass (ancestry trivially holds against an ancient commit) or false-fail on a clean history — a drifted 4.2 baseline could ship undetected. Both are test/CI-only, no production code path, no new dependency.

Plain-English (project owner is a git beginner — [[user_git_experience]] / [[feedback_explain_git]]): We are adding ONE new automatic test that double-checks the two Spine engines really ARE different (the new 4.3 one and the old 4.2 one) — it reads each engine's version label and confirms they don't match, and confirms the brand-new 4.3-only parts (`Slider`, `BonePose`) exist in the new engine and are genuinely missing from the old one. The ROADMAP always said we must have this automatic check; an earlier re-plan accidentally dropped it, so we are putting it back. Separately, we are making the existing "was the before-photo taken before the renovation?" safety check more robust: right now it reads Git's history with a command that, if a FUTURE step ever removes-and-re-adds the engine nickname, could read the WRONG entry and either wave through a broken before-photo or wrongly reject a good one. You chose to harden it now (rather than wait until Phase 44/45 — where that future churn actually happens). The fix changes the command to one that explicitly takes the OLDEST history entry (the genuine first appearance), and adds a loud failure if the engine nickname has vanished entirely. It does the EXACT same thing today (we verified: same answer, the frozen commit `cc5783f`) — it just stays correct under future history edits. Neither change touches the locked engine-install commit or any of the frozen Phase-42 commits — both are brand-new commits stacked on top.

Output: ONE new test file `tests/runtime/runtime-distinctness.spec.ts` (Task 1, its own atomic commit) restoring the ROADMAP-SC-#2 regression lock; the CR-01 D-09 hardening applied to both `.github/workflows/ci.yml` and `tests/safe01/safe01-freeze-guard.spec.ts` (Task 2, its own atomic commit). Two atomic commits, both NEW additive descendants of HEAD (D `2360c51`), neither perturbing a frozen commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-CONTEXT.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-PATTERNS.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-VERIFICATION.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-REVIEW.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-REPLAN-NOTE.md

<interfaces>
<!-- ALL values below are machine-confirmed live this planning session (2026-05-16, HEAD 03b66de). The executor MUST re-confirm export NAMES against the resolved module at execution time (log Object.keys first) so a renamed export fails loudly rather than silently — but the version strings, the CR-01 behavior-equivalence, and the alias-commit SHA are verified facts; use them verbatim, do NOT re-investigate. -->

VERSION FACTS (live-confirmed — version is NOT an exported constant; read from resolved package.json):
- `require('@esotericsoftware/spine-core/package.json').version` -> `'4.3.0'`  (the canonical 4.3 install)
- `require('spine-core-42/package.json').version` -> `'4.2.111'`  (`spine-core-42` = `npm:@esotericsoftware/spine-core@4.2.111`; its package.json `name` is still `@esotericsoftware/spine-core`, `version` is `4.2.111`)
- The two differ -> the literal ROADMAP `adapter42.version !== adapter43.version` contract.

4.3 MODULE EXPORTS (live-confirmed via `Object.keys`/`typeof` on the RESOLVED module this session):
- `@esotericsoftware/spine-core` (4.3.0): `Slider` = function, `BonePose` = function, `Posed` = function, `SlotPose` = function, `Skeleton` = function. **`Pose` resolved to `undefined`** in the live 4.3.0 module — DO NOT hard-assert `Pose` (it would false-fail). The ROADMAP names ONLY `Slider` and `BonePose` — those two are the non-negotiable contract; `Posed`/`SlotPose` are live-confirmed-present supporting Pose-architecture symbols and MAY be asserted as a stronger lock. The executor MUST log `Object.keys(sc43).filter(k => ['Slider','BonePose','Pose','Posed','SlotPose'].includes(k))` (and a broader dump) BEFORE the assertions so a future export rename fails loudly with a visible diff rather than silently.
- `spine-core-42` (4.2.111): `Slider`/`BonePose`/`Posed`/`SlotPose` are ALL `undefined` (4.2 predates the Pose-architecture rewrite). `Skeleton` IS present in BOTH (the dual-universe smoke target — Pitfall 4).

CR-01 BEHAVIOR-EQUIVALENCE (live-confirmed this session at HEAD 03b66de — use verbatim, do NOT re-derive):
- `git log -S 'spine-core-42' --format=%H -- package.json | tail -1` -> `cc5783ffc211214ff9255a643130bd09d81589e5` (the frozen COMMIT B `cc5783f`)
- `git log --reverse -S 'spine-core-42' --format=%H -- package.json | head -1` -> `cc5783ffc211214ff9255a643130bd09d81589e5` (IDENTICAL — the CR-01 fix is behavior-equivalent TODAY)
- pickaxe set size: `git log -S 'spine-core-42' --format=%H -- package.json | wc -l` -> `1` (single occurrence today; this is exactly WHY `tail -1` happens to equal `--reverse | head -1` now and WHY the fix changes nothing today while closing the forward-fragility)
- `grep -q 'spine-core-42' package.json` -> present (the presence guard passes today)
- COMMIT A (SAFE-01 baseline `_manifest.json` introducing commit) = `1b5327d`; `git merge-base --is-ancestor 1b5327d cc5783f` -> exit 0 (A is an ancestor of B — the SAFE-01 ordering invariant, unchanged by Task 2).

FROZEN ANCESTRY (do NOT amend/rebase/reorder/modify any of these):
A `1b5327d` -> B `cc5783f` (= the frozen dual-install; package.json + package-lock.json — Task 2 must NOT modify these) -> repoint `1a8c18b` -> C `b6f3177` -> D `2360c51`. All ancestors of HEAD. Plan 42-05's TWO commits are NEW additive descendants of HEAD (D). Do NOT modify `src/renderer/**`, `@esotericsoftware/spine-player`, or `tsconfig.web.json` (the Phase-47-owned spine-player `.d.ts` 4.3-leak — explicitly NOT a Phase-42 defect, NOT touched here).

LANDMINE — RESOLVED (stated so the executor does not re-investigate, but the acceptance criteria still command the proving check): `tests/arch.spec.ts` has TWO Phase-42 anchors. The RT-04 anchor globs `src/core/runtime/**/*.ts` (line 299). The RT-03 backstop "no file imports BOTH alias specifiers" anchor globs `src/**/*.{ts,tsx}` (line 315) — **`src/**` ONLY; there is ZERO `tests/**` glob anywhere in arch.spec.ts** (confirmed this session: `grep -n "globSync('tests" tests/arch.spec.ts` -> no match). The new `tests/runtime/runtime-distinctness.spec.ts` lives in `tests/` and imports BOTH `@esotericsoftware/spine-core` AND `spine-core-42` ON PURPOSE — this is a sanctioned `tests/` co-import, NOT a `src/**` co-mingle violation. The already-green sibling `tests/runtime/d13-43-load-smoke.spec.ts` imports the bare 4.3 specifier in `tests/` and passes — the idiom is established. The Task-1 acceptance criteria STILL include a hard `npx vitest run tests/arch.spec.ts` exit-0 check as the empirical proof the new file did not trip the arch wall (do not skip the check just because the analysis says it is safe).

VITEST AUTO-DISCOVERY (confirmed `vitest.config.ts:15`): `include: ['tests/**/*.spec.ts', 'tests/**/*.spec.tsx']` — the new `tests/runtime/runtime-distinctness.spec.ts` is auto-picked. **NO vitest.config.ts change is needed or permitted.** (`vitest.config.ts` is NOT in `files_modified`.)

CR-01 EXACT FIX (from 42-REVIEW.md "### CR-01 -> **Fix:**" — reproduce this shape; the executor MUST read the ACTUAL current text of both files first and edit the real lines — the line numbers below are approximate, the structure is exact):

For `.github/workflows/ci.yml` — the D-09 step is currently `name: Assert SAFE-01 baseline predates the npm-alias commit (D-09)` at ~lines 113-129. Current alias-commit line (~117): `ALIAS_COMMIT="$(git log -S 'spine-core-42' --format=%H -- package.json | tail -1)"`. The hardened step (preserve the existing `BASE_COMMIT` resolution, the existing empty-input/shallow-clone guard, and the existing merge-base assertion verbatim; ONLY add the presence guard at the top and replace the ALIAS_COMMIT resolution line):
```bash
- name: Assert SAFE-01 baseline predates the npm-alias commit (D-09)
  shell: bash
  run: |
    grep -q "spine-core-42" package.json \
      || { echo "::error::D-09: spine-core-42 alias no longer present in package.json — gate cannot be evaluated"; exit 1; }
    BASE_COMMIT="$(git log --diff-filter=A --format=%H -- tests/safe01/baselines/_manifest.json | tail -1)"
    # --reverse | head -1 = the OLDEST pickaxe hit = the introducing commit,
    # explicit (do NOT rely on `tail -1` of an unordered-intent set — CR-01).
    ALIAS_COMMIT="$(git log --reverse -S 'spine-core-42' --format=%H -- package.json | head -1)"
    echo "SAFE-01 baseline (_manifest.json added) : $BASE_COMMIT"
    echo "npm-alias commit (spine-core-42 in pkg) : $ALIAS_COMMIT"
    if [ -z "$BASE_COMMIT" ] || [ -z "$ALIAS_COMMIT" ]; then
      echo "::error::D-09 ancestry inputs unresolved — shallow clone? (need fetch-depth: 0)"
      exit 1
    fi
    if git merge-base --is-ancestor "$BASE_COMMIT" "$ALIAS_COMMIT"; then
      echo "D-09 OK: SAFE-01 baseline $BASE_COMMIT is a git ancestor of the npm-alias commit $ALIAS_COMMIT"
    else
      echo "::error::D-09 VIOLATION: SAFE-01 baseline $BASE_COMMIT is NOT an ancestor of the npm-alias commit $ALIAS_COMMIT — the 4.2 baseline was captured AFTER the refactor; the regression gate is void."
      exit 1
    fi
```
NOTE: keep the `BASE_COMMIT` line's existing `| tail -1` — `--diff-filter=A` is a chronologically-ordered ADD-only log and the existing comment/intent there is correct; CR-01 is ONLY about the alias-commit pickaxe resolution (`-S` is NOT chronologically meaningful with `tail -1`). The executor edits ONLY: (a) add the two-line `grep -q ... || { ...; exit 1; }` presence guard as the FIRST run-block lines, and (b) replace the `ALIAS_COMMIT="$(git log -S 'spine-core-42' --format=%H -- package.json | tail -1)"` line with the `--reverse ... | head -1` form + its explanatory comment.

For `tests/safe01/safe01-freeze-guard.spec.ts` — the D-09 enforcement is the first `it(...)` (`'the baseline commit is a git ancestor of the npm-alias commit'`, ~lines 37-111). The alias resolution is currently the `sh(['log','-S','spine-core-42','--format=%H','--','package.json'])` call (~lines 55-62) followed by `.split('\n').filter(Boolean).pop()` (~line 63 — the `.pop()` LAST-hit form). The hardened equivalent: (a) BEFORE resolving commits, read `package.json` and assert the literal `spine-core-42` is still present, failing with an analogous loud message if not; (b) replace the alias resolution with the explicit-oldest `--reverse` form taking the FIRST line (`[0]`) instead of `.pop()`. Concretely:
```typescript
// CR-01 presence guard: if the alias literal is gone, the gate cannot be
// evaluated — fail LOUDLY (mirrors the ci.yml D-09 grep-presence guard) rather
// than vacuously pass against a removed dependency.
const pkgJsonText = readFileSync(path.resolve(REPO_ROOT, 'package.json'), 'utf8');
if (!pkgJsonText.includes('spine-core-42')) {
  throw new Error(
    'D-09: spine-core-42 alias no longer present in package.json — the SAFE-01 ' +
      'ordering gate cannot be evaluated (a removed alias must fail loudly, not ' +
      'vacuously pass).',
  );
}

// CR-01: the OLDEST pickaxe hit = the introducing commit, taken EXPLICITLY via
// `--reverse | head` ([0]) — do NOT use `.pop()` of an unordered-intent `-S`
// set (`-S` lists every add/remove/re-add; `.pop()` is the oldest only while
// the literal is added exactly once and never churned — CR-01 forward-fragility).
const aliasLog = sh([
  'log',
  '--reverse',
  '-S',
  'spine-core-42',
  '--format=%H',
  '--',
  'package.json',
]);
const aliasCommit = aliasLog.split('\n').filter(Boolean)[0];
```
The existing `if (!aliasCommit) { return; }` skip-with-reason path (alias not yet introduced -> vacuously satisfied; the arch.spec.ts ENOENT-tolerant precedent), the `if (!baselineCommit) throw` path, the `git merge-base --is-ancestor` assertion, and the `_meta.generatedCommit` secondary tamper cross-check MUST all be preserved verbatim — CR-01 ONLY changes the alias-commit RESOLUTION (presence guard + `--reverse`+`[0]`), nothing else. The second `it(...)` (`'there is NO env-gated regen branch in the baseline spec'`) is UNTOUCHED. NOTE: `safe01-freeze-guard.spec.ts` already imports `readFileSync` from `node:fs` and `* as path from 'node:path'` and has `REPO_ROOT` defined (lines 26-29) — reuse them; do NOT add duplicate imports. NOTE 2: the second `it`'s no-regen meta-test greps the SIBLING file `safe01-baseline.spec.ts` for `/UPDATE_FIXTURES|process\.env|SHOULD_UPDATE/` — it reads `safe01-baseline.spec.ts`, NOT this file; your new `readFileSync(... 'package.json')` is in `safe01-freeze-guard.spec.ts`, so it does NOT trip that meta-test. (Confirm at execute time by reading the second `it`.)
</interfaces>

<analog_excerpts>
<!-- Read each at execution time before writing. -->
Task 1 — `tests/runtime/d13-43-load-smoke.spec.ts` (READ IN FULL before writing the new file): the exact `tests/runtime/` real-module / `REPO_ROOT = resolve(__dirname, '..', '..')` / "this brand-new test legitimately imports the bare 4.3 specifier ON PURPOSE, NOT a spine-core-42 repoint target" docstring idiom to mirror — see its header comment lines 1-46 and its `import { ... } from '@esotericsoftware/spine-core'` (resolves to canonical 4.3.0). The new distinctness file ADDS a second `import * as sc42 from 'spine-core-42'` and a docstring explicitly stating it imports BOTH specifiers ON PURPOSE (the distinctness probe, NOT a co-mingle violation; NOT a repoint target). PATTERNS.md Topic E: analog is the real-module/no-`vi.mock` idiom.
Task 1 — `tests/runtime/handle-brand-negative.ts` (sibling-file conventions in `tests/runtime/`: header docstring style, import grouping).
Task 1 (landmine proof) — `tests/arch.spec.ts:289-325` (the two Phase-42 anchors: RT-04 globs `src/core/runtime/**`, RT-03 backstop globs `src/**/*.{ts,tsx}` — `src/**` ONLY; the new `tests/` file is out of scope of both, same as the green `d13-43-load-smoke.spec.ts`).
Task 2 — `.github/workflows/ci.yml:113-129` (the CURRENT D-09 step — `name: Assert SAFE-01 baseline predates the npm-alias commit (D-09)`; current alias line ~117 `git log -S 'spine-core-42' ... | tail -1`; the comment block ~104-112 explaining the belt-and-suspenders intent).
Task 2 — `tests/safe01/safe01-freeze-guard.spec.ts:36-111` (the CURRENT vitest D-09 enforcement — alias resolution ~55-63 `sh(['log','-S','spine-core-42',...]).split('\n').filter(Boolean).pop()`; the skip-with-reason path ~65-72; the merge-base assertion ~88-90; the `_meta.generatedCommit` cross-check ~97-110 — ALL preserved verbatim except the alias resolution).
Task 2 — 42-REVIEW.md "### CR-01" -> "**Fix:**" (the verbatim hardened ci.yml step + the "apply the identical `--reverse | head -1` + presence-guard correction to safe01-freeze-guard.spec.ts" instruction — the authoritative fix source).
</analog_excerpts>

<ordering_invariant>
**This is a GAP-CLOSURE plan landing on top of the FROZEN A->B->repoint->C->D chain (A `1b5327d` -> B `cc5783f` -> repoint `1a8c18b` -> C `b6f3177` -> D `2360c51`, all ancestors of HEAD).** `depends_on: ["42-04"]` and `wave: 5` express "lands strictly after the frozen waves 1-4" — 42-01..04 are already executed/committed, so this is ordering-expression only (no re-execution of prior plans). Plan 42-05's TWO commits are NEW additive descendants of HEAD. They MUST NOT amend, rebase, or reorder any frozen commit; MUST NOT modify `package.json`/`package-lock.json` (the frozen COMMIT B `cc5783f` anchor); MUST NOT touch `src/renderer/**` / `@esotericsoftware/spine-player` / `tsconfig.web.json` (the Phase-47-owned spine-player `.d.ts` 4.3-leak — explicitly NOT a Phase-42 defect). Task 1 and Task 2 are independent (disjoint file sets — Task 1: `tests/runtime/runtime-distinctness.spec.ts`; Task 2: `.github/workflows/ci.yml` + `tests/safe01/safe01-freeze-guard.spec.ts`); either commit order is acceptable, but each is its own atomic commit. Neither perturbs a frozen commit. The CR-01 hardening is behavior-equivalent TODAY (verified: resolved alias commit STILL `cc5783f`; pickaxe set size = 1) — Task 2 makes the SAFE-01 ordering gate STRONGER (closes forward-fragility), it does NOT change its current verdict.
</ordering_invariant>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/runtime/runtime-distinctness.spec.ts — restore the RT-01 / ROADMAP-SC-#2 regression lock</name>
  <files>tests/runtime/runtime-distinctness.spec.ts (NEW — the only file this task creates or modifies)</files>
  <read_first>
    - tests/runtime/d13-43-load-smoke.spec.ts (READ IN FULL — mirror its `tests/runtime/` header-docstring idiom, `REPO_ROOT = resolve(__dirname, '..', '..')` pattern, real-module/no-`vi.mock` style, and the "this NEW test legitimately imports the bare 4.3 specifier ON PURPOSE — NOT a spine-core-42 repoint target" framing; the new file extends this to "imports BOTH specifiers on purpose — the distinctness probe")
    - tests/runtime/handle-brand-negative.ts (sibling `tests/runtime/` file conventions: docstring style, import grouping)
    - tests/arch.spec.ts:289-325 (the two Phase-42 anchors — confirm at execute time that RT-04 globs `src/core/runtime/**` and the RT-03 backstop globs `src/**/*.{ts,tsx}`; neither sweeps `tests/**`, so this new `tests/` file co-importing both specifiers is sanctioned — the same reason `d13-43-load-smoke.spec.ts` is green)
    - 42-VERIFICATION.md (the `gaps:` frontmatter block + "Gaps Summary" — the `missing:` item specifies a SINGLE file covering (a) both resolve, (b) `.version` differ, (c) Slider/BonePose 4.3-only-and-absent-from-spine-core-42)
    - vitest.config.ts (confirm line 15 `include: ['tests/**/*.spec.ts', ...]` auto-picks the new spec — NO config change is needed or permitted)
  </read_first>
  <action>
**Create exactly ONE new file `tests/runtime/runtime-distinctness.spec.ts`. Do NOT modify any other file. This is a pure real-module test — NO `vi.mock` (importing the spine-core packages directly is allowed and intended here; that is the entire point of the distinctness probe).**

This restores the ROADMAP-SC-#2 regression lock collaterally removed by the Option-1 re-plan (`c87a95f`). Merge the pre-replan Task-3 `alias-resolution` + `runtime-distinctness` concerns into ONE consolidated file (42-VERIFICATION.md's `missing:` block specifies a single file covering version + export distinctness).

**Header docstring (mirror `d13-43-load-smoke.spec.ts:1-46` style):** State that this file is the RT-01 / ROADMAP-Phase-42-SC-#2 automated regression lock; that it **legitimately imports BOTH `@esotericsoftware/spine-core` (the canonical 4.3.0) AND `spine-core-42` (the 4.2.111 alias) ON PURPOSE** — it is the runtime-distinctness probe, NOT a `spine-core-42` repoint target and NOT a `src/**` both-specifier co-mingle violation (the RT-03 backstop arch anchor is `src/**`-scoped only; the already-green sibling `d13-43-load-smoke.spec.ts` establishes the `tests/` co-import idiom). Note it was restored as a gap-closure follow-up (42-05) because the Option-1 re-plan `c87a95f` collaterally dropped it while only documenting the orthogonal typecheck narrowing.

**Imports:** `import { describe, expect, it } from 'vitest';` `import { createRequire } from 'node:module';` `import * as sc43 from '@esotericsoftware/spine-core';` `import * as sc42 from 'spine-core-42';`. Use `const require = createRequire(import.meta.url);` then read versions via `require('@esotericsoftware/spine-core/package.json').version` and `require('spine-core-42/package.json').version` (version is NOT an exported constant — confirmed; it MUST come from the resolved package.json, not from the module namespace). (If the file's tsconfig/ESM context makes `createRequire` awkward, the equivalent is fine: read+`JSON.parse` the resolved `package.json` paths via `require.resolve`. The load-bearing fact is: version comes from package.json, not a module export.)

**Before the assertions, LOG the actual resolved export surface so a future rename fails loudly with a visible diff** (mirrors the executor-confirms-export-names mandate): emit, via a one-time `console.log` or an `expect` message, `Object.keys(sc43)` filtered to the contract-relevant names (`['Slider','BonePose','Pose','Posed','SlotPose','Skeleton']`) AND a broader `Object.keys(sc43).length` sanity number, plus the two resolved version strings. This makes a silent export rename visible at test-output time, not a mystery failure.

**Assertions (a)-(e):**
(a) **Both resolve:** `expect(sc43).toBeTypeOf('object')` and `expect(sc42).toBeTypeOf('object')`, both non-null (e.g. `expect(sc43).not.toBeNull()`). The very act of the two top-level `import * as` lines resolving under vitest without throwing is the primary proof; assert non-null objects to make it explicit.
(b) **Version distinctness (the literal ROADMAP `adapter42.version !== adapter43.version` contract):** `const v43 = require('@esotericsoftware/spine-core/package.json').version; const v42 = require('spine-core-42/package.json').version;` then `expect(v43).toBe('4.3.0');` `expect(v42).toBe('4.2.111');` `expect(v43).not.toBe(v42);` (the explicit `!==` contract). Include all three — the two exact-value pins catch a silent dependency drift AND the `!==` is the verbatim ROADMAP language.
(c) **The 4.3 module exports the ROADMAP-named symbols + supporting Pose-architecture symbols:** assert `Slider` AND `BonePose` (the two ROADMAP-named, non-negotiable) are present: `expect(typeof (sc43 as Record<string, unknown>).Slider).not.toBe('undefined');` and the same for `BonePose`. ALSO assert `Posed` and `SlotPose` are present (live-confirmed; stronger lock). **DO NOT assert `Pose` — it resolved to `undefined` in the live 4.3.0 module this session; asserting it would false-fail.** (The executor's pre-assertion log of `Object.keys(sc43)` is what catches a future rename of `Slider`/`BonePose` loudly.)
(d) **Those symbols are ABSENT from `spine-core-42`:** `expect((sc42 as Record<string, unknown>).Slider).toBeUndefined();` and the same for `BonePose`, `Posed`, `SlotPose` (4.2.111 predates the Pose-architecture rewrite — live-confirmed all `undefined`).
(e) **Dual-universe smoke (research Pitfall 4):** a class present in BOTH installs is NOT the same reference across modules — `expect((sc43 as Record<string, unknown>).Skeleton).toBeTypeOf('function');` `expect((sc42 as Record<string, unknown>).Skeleton).toBeTypeOf('function');` `expect((sc43 as Record<string, unknown>).Skeleton).not.toBe((sc42 as Record<string, unknown>).Skeleton);` — proves two genuinely distinct module instances, not a single hoisted copy.

Structure as one `describe(...)` with focused `it(...)` blocks (e.g. `it('both spine-core installs resolve under vitest')`, `it('the two .version values differ (4.3.0 vs 4.2.111) — the adapter42.version !== adapter43.version contract)')`, `it('Slider/BonePose are exported from 4.3 and ABSENT from spine-core-42')`, `it('a class in BOTH installs is a distinct reference per module (Pitfall 4 dual-universe smoke)')`). Use `const REPO_ROOT = resolve(__dirname, '..', '..');` only if a path is needed; the version reads do not need it (use `require`/`require.resolve` of the package specifiers).

This is a TEST-ONLY addition. It changes NO production code, adds NO dependency (it imports already-installed, lockfile-pinned packages), and perturbs NO frozen commit. `vitest.config.ts:15` auto-picks it (`tests/**/*.spec.ts`) — do NOT touch vitest.config.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/runtime-distinctness.spec.ts</automated>
    <automated>npx vitest run tests/arch.spec.ts</automated>
    <automated>npx vitest run tests/safe01</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/runtime/runtime-distinctness.spec.ts` succeeds (the file exists)
    - `grep -c "spine-core-42" tests/runtime/runtime-distinctness.spec.ts` returns >= 1 (the 4.2 alias is imported/referenced)
    - `grep -Ec 'Slider|BonePose' tests/runtime/runtime-distinctness.spec.ts` returns >= 1 (the two ROADMAP-named symbols are asserted)
    - `npx vitest run tests/runtime/runtime-distinctness.spec.ts` exits 0 (the new spec passes — version distinctness + Slider/BonePose-4.3-only + dual-universe smoke all green)
    - `npx vitest run tests/arch.spec.ts` exits 0 (LANDMINE PROOF: the RT-03 `src/**`-scoped backstop anchor did NOT trip on the new `tests/` file co-importing both specifiers — the arch wall is intact, the new file is a sanctioned `tests/` co-import)
    - `npx vitest run tests/safe01` exits 0 (SAFE-01 baseline byte-equal still green — this file changes no core math; sanity that the test addition perturbed nothing)
    - The Task-1 commit `git log -1 --stat` shows ONLY `tests/runtime/runtime-distinctness.spec.ts` — and NOT package.json / package-lock.json / src/renderer/** / tsconfig.web.json / vitest.config.ts (no frozen-commit perturbation, no config change)
    - The commit is a NEW descendant of HEAD (not an amend/rebase of any frozen commit): `git merge-base --is-ancestor 2360c51 HEAD` exits 0 and the new commit's first-parent chain includes the frozen D `2360c51`
  </acceptance_criteria>
  <done>An automated regression test exists and passes that asserts `require('@esotericsoftware/spine-core/package.json').version` (4.3.0) !== `require('spine-core-42/package.json').version` (4.2.111), that `Slider`/`BonePose` are exported from the 4.3 module and absent from `spine-core-42`, and that a shared class (`Skeleton`) is a distinct reference per install — the ROADMAP Phase-42 Success Criterion #2 contract is now regression-locked. `tests/arch.spec.ts` still exits 0 (the new `tests/` co-import did not trip the `src/**`-scoped RT-03 backstop). One atomic commit touching only the new file; a new descendant of HEAD; no frozen commit perturbed.</done>
</task>

<task type="auto">
  <name>Task 2: CR-01 — harden the D-09 SAFE-01 ancestry resolution in ci.yml + safe01-freeze-guard.spec.ts (user-decided HARDEN NOW)</name>
  <files>.github/workflows/ci.yml, tests/safe01/safe01-freeze-guard.spec.ts (the ONLY two files this task modifies — both edited in one atomic commit)</files>
  <read_first>
    - 42-REVIEW.md "### CR-01" -> "**Fix:**" (the authoritative verbatim hardened ci.yml step + the "apply the identical `--reverse | head -1` + presence-guard correction to safe01-freeze-guard.spec.ts" instruction — this is the exact fix to reproduce)
    - .github/workflows/ci.yml (READ the ACTUAL current D-09 step — `name: Assert SAFE-01 baseline predates the npm-alias commit (D-09)`, ~lines 113-129; the current alias line `ALIAS_COMMIT="$(git log -S 'spine-core-42' --format=%H -- package.json | tail -1)"` ~line 117; the explanatory comment block ~104-112 — edit the REAL lines, the line numbers in this plan are approximate)
    - tests/safe01/safe01-freeze-guard.spec.ts (READ the ACTUAL current D-09 enforcement — the first `it(...)` ~lines 37-111; the alias resolution `sh(['log','-S','spine-core-42','--format=%H','--','package.json'])` + `.split('\n').filter(Boolean).pop()` ~lines 55-63; the skip-with-reason `if (!aliasCommit) return;` ~65-72; the `git merge-base --is-ancestor` assertion ~88-90; the `_meta.generatedCommit` cross-check ~97-110; the existing `readFileSync`/`path`/`REPO_ROOT` imports ~24-29 — reuse them, do NOT duplicate)
    - 42-VERIFICATION.md "Human Verification Required" -> "#1. CR-01" (confirms CR-01 resolves correctly TODAY — pickaxe size 1, behavior-equivalent — so this is a robustness hardening, not a behavior change; the user chose HARDEN NOW)
  </read_first>
  <action>
**Apply the CR-01 fix to BOTH files in ONE atomic commit. This is a robustness hardening that is BEHAVIOR-EQUIVALENT TODAY (verified live: `git log -S 'spine-core-42' --format=%H -- package.json | tail -1` == `git log --reverse -S 'spine-core-42' --format=%H -- package.json | head -1` == `cc5783f`; pickaxe set size = 1). It closes a forward-fragility (a future remove-then-re-add of the literal would make the LAST-hit `tail -1`/`.pop()` resolve the wrong commit and silently void the SAFE-01 ordering gate). Do NOT modify package.json/package-lock.json and do NOT touch any other safe01 source.**

**File 1 — `.github/workflows/ci.yml`** (the D-09 step `name: Assert SAFE-01 baseline predates the npm-alias commit (D-09)`):
1. Add, as the FIRST two lines of the step's `run: |` block, the presence guard:
   ```bash
   grep -q "spine-core-42" package.json \
     || { echo "::error::D-09: spine-core-42 alias no longer present in package.json — gate cannot be evaluated"; exit 1; }
   ```
2. Replace the line `ALIAS_COMMIT="$(git log -S 'spine-core-42' --format=%H -- package.json | tail -1)"` with:
   ```bash
   # --reverse | head -1 = the OLDEST pickaxe hit = the introducing commit,
   # explicit (do NOT rely on `tail -1` of an unordered-intent set — CR-01).
   ALIAS_COMMIT="$(git log --reverse -S 'spine-core-42' --format=%H -- package.json | head -1)"
   ```
3. PRESERVE verbatim: the `BASE_COMMIT="$(git log --diff-filter=A --format=%H -- tests/safe01/baselines/_manifest.json | tail -1)"` line (its `tail -1` is correct — `--diff-filter=A` is chronologically meaningful, CR-01 is ONLY about the `-S` pickaxe), the `echo` provenance lines, the `if [ -z "$BASE_COMMIT" ] || [ -z "$ALIAS_COMMIT" ]` shallow-clone guard, and the `if git merge-base --is-ancestor ...` assertion + its error messages. The end-state must match the verbatim hardened step in 42-REVIEW.md "### CR-01 -> **Fix:**" (modulo the BASE_COMMIT `--diff-filter=A | tail -1` which the review's fix block also keeps). Make NO other edit to ci.yml — the 3-OS matrix, triggers, `fetch-depth: 0`, `npm run typecheck:node`, `npm run test`, and the `bundle-smoke` job are all untouched.

**File 2 — `tests/safe01/safe01-freeze-guard.spec.ts`** (the first `it(...)`, `'the baseline commit is a git ancestor of the npm-alias commit'`):
1. Add a CR-01 presence guard at the TOP of the `it(...)` body, BEFORE the alias resolution (the file already imports `readFileSync` from `node:fs`, `* as path from 'node:path'`, and defines `REPO_ROOT` at ~line 29 — REUSE them, add NO duplicate imports):
   ```typescript
   const pkgJsonText = readFileSync(path.resolve(REPO_ROOT, 'package.json'), 'utf8');
   if (!pkgJsonText.includes('spine-core-42')) {
     throw new Error(
       'D-09: spine-core-42 alias no longer present in package.json — the SAFE-01 ' +
         'ordering gate cannot be evaluated (a removed alias must fail loudly, not ' +
         'vacuously pass).',
     );
   }
   ```
2. Replace the alias resolution `sh(['log','-S','spine-core-42','--format=%H','--','package.json'])` + `.split('\n').filter(Boolean).pop()` with the explicit-oldest `--reverse` + first-element form:
   ```typescript
   // CR-01: the OLDEST pickaxe hit = the introducing commit, taken EXPLICITLY
   // via `--reverse` + first element ([0]) — NOT `.pop()` of an unordered-intent
   // `-S` set (`-S` lists every add/remove/re-add; `.pop()` is the oldest only
   // while the literal is added exactly once and never churned — CR-01).
   const aliasLog = sh([
     'log',
     '--reverse',
     '-S',
     'spine-core-42',
     '--format=%H',
     '--',
     'package.json',
   ]);
   const aliasCommit = aliasLog.split('\n').filter(Boolean)[0];
   ```
3. PRESERVE verbatim: the `baselineCommit` resolution (its `--diff-filter=A` + `.pop()` is correct — chronologically meaningful; the review's fix scope is the `-S` pickaxe only), the `if (!aliasCommit) { return; }` skip-with-reason path (and its explanatory comment — the arch.spec.ts ENOENT-tolerant precedent for the not-yet-introduced case), the `if (!baselineCommit) throw`, the `git merge-base --is-ancestor` `expect(...).not.toThrow()` assertion, and the entire `_meta.generatedCommit` secondary tamper cross-check block. The SECOND `it(...)` (`'there is NO env-gated regen branch in the baseline spec'`) is UNTOUCHED — confirm at execute time that its assertion greps the SIBLING `safe01-baseline.spec.ts` (NOT this file) for `/UPDATE_FIXTURES|process\.env|SHOULD_UPDATE/`, so the new `readFileSync(... 'package.json')` you add to THIS file does not trip it.

After both edits, the resolved `ALIAS_COMMIT`/`aliasCommit` is STILL `cc5783f` (verified live this session — behavior-equivalent today; pickaxe set size = 1) and the A->B `git merge-base --is-ancestor 1b5327d cc5783f` still exits 0, so the freeze-guard A->B ancestry assertion STILL HARD-PASSES (the non-vacuous path — NOT the skip path, because the alias literal IS present today). This is a scoping/robustness correction, not a weakening.
  </action>
  <verify>
    <automated>npx vitest run tests/safe01/safe01-freeze-guard.spec.ts</automated>
    <automated>npx vitest run tests/safe01 tests/arch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "git log --reverse -S 'spine-core-42'" .github/workflows/ci.yml` succeeds (the explicit-oldest `--reverse` alias-commit form is present in ci.yml)
    - The old LAST-hit form is GONE from ci.yml: `grep -F "git log -S 'spine-core-42' --format=%H -- package.json | tail -1" .github/workflows/ci.yml` returns nothing / exit 1 (the `tail -1` pickaxe selection no longer exists)
    - `grep -q 'grep -q "spine-core-42" package.json' .github/workflows/ci.yml` succeeds (the ci.yml presence guard exists)
    - `grep -q -- "--reverse" tests/safe01/safe01-freeze-guard.spec.ts` succeeds (the explicit-oldest `--reverse` form is present in the vitest freeze-guard)
    - The ALIAS resolution `.pop()` LAST-hit form is GONE: read the file and confirm the alias `sh([...])` block now contains `'--reverse'` and the result is taken via `[0]`, not `.pop()` (the `baselineCommit` `--diff-filter=A` + `.pop()` is correctly UNCHANGED — only the `-S` alias block changed)
    - `grep -q "spine-core-42 alias no longer present" tests/safe01/safe01-freeze-guard.spec.ts` succeeds (the vitest presence guard with its loud message exists)
    - `npx vitest run tests/safe01/safe01-freeze-guard.spec.ts` exits 0 AND the A->B ancestry `it` HARD-PASSES (the run reports it passed, NOT skipped — the alias literal IS present today so `aliasCommit` resolves to `cc5783f` and the `merge-base --is-ancestor` assertion runs and passes)
    - Behavior-equivalence proof recorded in the SUMMARY: `git log --reverse -S 'spine-core-42' --format=%H -- package.json | head -1` equals `git log -S 'spine-core-42' --format=%H -- package.json | tail -1` equals `cc5783f` today (resolved alias commit unchanged by the hardening)
    - `npx vitest run tests/safe01 tests/arch.spec.ts` exits 0 (full SAFE-01 suite + arch anchors still green — the hardening perturbed nothing; SAFE-01 baseline byte-equal unaffected)
    - The Task-2 commit `git log -1 --stat` shows ONLY `.github/workflows/ci.yml` + `tests/safe01/safe01-freeze-guard.spec.ts` — and NOT package.json / package-lock.json / any other safe01 source / src/renderer/** (no frozen-commit perturbation)
    - The commit is a NEW descendant of HEAD (not an amend/rebase of any frozen commit): the new commit's first-parent chain includes the frozen D `2360c51`
  </acceptance_criteria>
  <done>Both `.github/workflows/ci.yml` and `tests/safe01/safe01-freeze-guard.spec.ts` resolve the npm-alias-introducing commit via the explicit-oldest `git log --reverse -S 'spine-core-42' ... | head -1`/`[0]` form and fail loudly if the `spine-core-42` literal is absent from package.json — the CR-01 forward-fragility data-integrity blocker is closed. Behavior-equivalent today (resolved alias commit STILL `cc5783f`; pickaxe set size = 1). The freeze-guard A->B ancestry assertion still hard-passes; SAFE-01 stays green. One atomic commit touching only the two files; a new descendant of HEAD; no frozen commit perturbed; package.json/package-lock.json untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> the SAFE-01 baseline-predates-alias ordering invariant | The D-09 gate trusts `git log` pickaxe output to identify the npm-alias-introducing commit. Task 2 STRENGTHENS this boundary (closes the forward-fragility where a future remove-then-re-add of the literal could make the LAST-hit selection resolve the wrong commit and silently void the gate). |
| installed npm packages -> the runtime-distinctness regression assertion | Task 1's test trusts the resolved `@esotericsoftware/spine-core` / `spine-core-42` package.json `.version` and module exports. These are already-installed, lockfile-integrity-pinned (sha512) packages from the frozen COMMIT B — no new external input is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05-01 | Tampering | The SAFE-01 ordering gate (`ci.yml` D-09 + `safe01-freeze-guard.spec.ts`) | mitigate | This is the exact threat Task 2 closes: a future remove-then-re-add of `spine-core-42` in package.json could make `tail -1`/`.pop()` resolve a non-introducing commit and false-pass (a drifted 4.2 baseline ships undetected) or false-fail. Mitigation: explicit-oldest `--reverse|head-1`/`[0]` resolution + a `grep -q spine-core-42 package.json` presence guard that fails loudly on a removed alias instead of vacuously passing. Behavior-equivalent today (pickaxe set size = 1) so no regression risk in the hardening itself. |
| T-42-05-02 | Tampering | The runtime-distinctness test's version source | mitigate | A renamed/relocated `version` (or a botched lockfile collapsing `spine-core-42` onto 4.3.0) could make the two installs silently homogenize. Mitigation: Task 1 reads `.version` from the RESOLVED package.json (not a forgeable module export), asserts exact pins (`4.3.0`/`4.2.111`) AND the `!==`, AND the dual-universe `Skeleton` non-identity smoke — a homogenized install fails the test loudly. The executor also logs `Object.keys(sc43)` before asserting so a future export rename of `Slider`/`BonePose` fails with a visible diff, not silently. |
| T-42-05-03 | Information Disclosure | — | accept | No PII, no secrets, no auth surface. Both tasks are test/CI-only over already-installed packages and local git history; no new external input, no injection surface, no network call, no credential read. Low-value target; nothing to disclose. |
| T-42-05-04 | Denial of Service | CI gate (added presence-grep + an extra vitest spec) | accept | The added `grep -q` is O(1) on package.json; the new spec resolves two already-installed modules and runs a handful of assertions (sub-second). Negligible CI-time cost; no DoS surface. Accepted. |

No threat is rated `high`. The net security posture of this plan is **positive**: Task 2 makes the SAFE-01 data-integrity gate strictly stronger (closes the CR-01 forward-fragility blocker), and Task 1 adds a regression lock that detects silent dual-install homogenization. No new production code path, no new dependency, no new external input, no auth/injection surface.
</threat_model>

<verification>
Phase-level checks after both tasks land (both commits are new additive descendants of HEAD `2360c51`):

1. **The gap is closed:** `tests/runtime/runtime-distinctness.spec.ts` exists and `npx vitest run tests/runtime/runtime-distinctness.spec.ts` exits 0 — the ROADMAP Phase-42 SC #2 automated regression lock (version distinctness + Slider/BonePose 4.3-only + dual-universe smoke) is restored and green.
2. **CR-01 closed (HARDEN NOW):** both `.github/workflows/ci.yml` and `tests/safe01/safe01-freeze-guard.spec.ts` use the explicit-oldest `--reverse` alias-commit resolution + a `spine-core-42`-presence guard; the `tail -1`/`.pop()` LAST-hit alias form is gone from both. `npx vitest run tests/safe01/safe01-freeze-guard.spec.ts` exits 0 with the A->B ancestry `it` HARD-PASSING (non-vacuous path, reported passed not skipped).
3. **Behavior-equivalence today:** `git log --reverse -S 'spine-core-42' --format=%H -- package.json | head -1` == `git log -S 'spine-core-42' --format=%H -- package.json | tail -1` == `cc5783f` (the frozen COMMIT B). Task 2 changed the resolution method, not today's verdict.
4. **No frozen-commit perturbation:** `git merge-base --is-ancestor 2360c51 HEAD` exits 0; the frozen chain A `1b5327d` -> B `cc5783f` -> repoint `1a8c18b` -> C `b6f3177` -> D `2360c51` is intact and all ancestors of HEAD; no commit was amended/rebased/reordered; `package.json`/`package-lock.json`/`src/renderer/**`/`tsconfig.web.json`/`vitest.config.ts` are byte-untouched by both Task-1 and Task-2 commits.
5. **No collateral regression:** `npx vitest run tests/safe01 tests/arch.spec.ts` exits 0 (SAFE-01 baseline byte-equal + arch anchors green); the new `tests/` co-import did not trip the `src/**`-scoped RT-03 backstop arch anchor.
6. **Two atomic commits:** Task 1 commit touches ONLY `tests/runtime/runtime-distinctness.spec.ts`; Task 2 commit touches ONLY `.github/workflows/ci.yml` + `tests/safe01/safe01-freeze-guard.spec.ts`. Plain-English commit messages (project owner is a git beginner — [[user_git_experience]] / [[feedback_explain_git]]).
</verification>

<success_criteria>
- ROADMAP Phase-42 Success Criterion #2 (RT-01) is regression-locked by an automated, passing test asserting `adapter42.version !== adapter43.version` (4.2.111 vs 4.3.0) and `Slider`/`BonePose` exported from the 4.3 module and absent from `spine-core-42`.
- CR-01 (user-decided HARDEN NOW) is closed: the D-09 SAFE-01 ancestry gate, in BOTH ci.yml and the vitest freeze-guard, resolves the introducing commit explicitly (`--reverse|head-1`/`[0]`) and fails loudly on a removed alias literal — forward-fragility closed, behavior-equivalent today (resolved alias commit STILL `cc5783f`).
- SAFE-01 stays green; the freeze-guard A->B ancestry assertion still hard-passes (non-vacuous path); `tests/safe01 tests/arch.spec.ts` exits 0.
- No frozen commit (42-01..42-04: A `1b5327d`, B `cc5783f`, repoint `1a8c18b`, C `b6f3177`, D `2360c51`) is amended/rebased/reordered; `package.json`/`package-lock.json` and the Phase-47-owned renderer/spine-player/tsconfig.web surfaces are untouched; both 42-05 commits are new additive descendants of HEAD.
- `<threat_model>` present with a STRIDE register; every threat has a disposition; no threat rated `high`; net posture is positive (Task 2 strengthens the SAFE-01 data-integrity gate).
</success_criteria>

<output>
After completion, create `.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-05-SUMMARY.md` recording: the two commit SHAs (both new descendants of HEAD `2360c51`); the resolved alias-commit behavior-equivalence proof (`--reverse|head-1` == `tail -1` == `cc5783f`, pickaxe set size = 1); the live-confirmed export surface logged by the new spec (`Object.keys(sc43)` for the contract symbols + the two version strings); confirmation the freeze-guard A->B `it` hard-passed (not skipped); and confirmation no frozen commit was perturbed and package.json/package-lock.json/renderer/tsconfig.web were untouched.
</output>
