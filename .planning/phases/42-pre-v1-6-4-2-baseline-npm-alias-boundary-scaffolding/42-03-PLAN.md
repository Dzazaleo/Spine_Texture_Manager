---
phase: 42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding
plan: 03
type: execute
wave: 3
depends_on: ["42-02"]
files_modified:
  - src/core/runtime/types.ts
  - src/core/runtime/runtime.ts
  - src/core/types.ts
  - tests/arch.spec.ts
  - tests/runtime/handle-brand-negative.ts
  - tests/runtime/runtime-types.spec.ts
autonomous: true
requirements: [RT-03, RT-04]
must_haves:
  truths:
    - "A 4.2 runtime object reaching a 4.3 boundary (or vice-versa) is a COMPILE-TIME error: opaque branded handles carry a required non-optional `__rt` runtime tag and a per-kind `unique symbol` brand (defeats structural typing)"
    - "`npm run typecheck:node` exits 0 on a fresh-clone-equivalent tree (the Phase-42 dual-runtime gate scope — ignoring untracked gitignored `scripts/probe-*.ts`/`scripts/diagnose-*.ts`/`tests/_trace_tmp/` scratch absent on a fresh clone / CI `npm ci`; satisfiable because 42-02's repoint + the locked TS6133 fix landed first, and the additive optional field + the new pure `core/runtime/` module introduce no breakage) AND a dedicated `// @ts-expect-error` negative fixture proves a cross-runtime handle mix fails `tsc` (the wall is real, not aspirational). NOTE: `typecheck:web` is RED via spine-player's own bare `Player.d.ts` 4.3 hoist — a KNOWN, EXPECTED, Phase-47-OWNED leak (documented in 42-02), NOT a Phase-42 defect and NOT in this plan's scope"
    - "`src/core/runtime/**` imports no DOM/Electron/sharp/node:fs AND no spine-core package in Phase 42 (signatures only) — enforced by a new `tests/arch.spec.ts` named anchor (RT-04)"
    - "No source file imports BOTH spine-core alias specifiers — enforced by a new `tests/arch.spec.ts` backstop anchor (RT-03 defense-in-depth behind the brand)"
    - "`LoadResult` gains an OPTIONAL `runtime?: SpineRuntime` field — additive, no existing `core/` consumer changes, sampler/bounds NOT rewired (that is Phase 43); COMMIT C makes NO logic change to loader/sampler/bounds/analyzer/bones/synthetic-atlas/types (their import specifier was already mechanically renamed by 42-02's repoint commit — that is the ONLY pre-existing change)"
  artifacts:
    - path: "src/core/runtime/types.ts"
      provides: "unique-symbol branded opaque handles + required __rt RuntimeTag + brandHandle/unwrapHandle/handleRuntime helpers (trivial casts over unknown, NO spine-core import)"
      min_lines: 50
      contains: "unique symbol"
    - path: "src/core/runtime/runtime.ts"
      provides: "SpineRuntime interface — signatures only (no bodies); pickRuntime as `export declare function`; import type only from ./types.js"
      min_lines: 40
      contains: "interface SpineRuntime"
    - path: "src/core/types.ts"
      provides: "LoadResult gains optional `runtime?: SpineRuntime` (additive)"
      contains: "runtime?: SpineRuntime"
    - path: "tests/arch.spec.ts"
      provides: "RT-04 core/runtime Layer-3 anchor + RT-03 no-co-mingled-imports backstop anchor (extends existing file)"
      contains: "core/runtime"
    - path: "tests/runtime/handle-brand-negative.ts"
      provides: "// @ts-expect-error compile-negative fixture proving a cross-runtime handle mix fails tsc"
      contains: "@ts-expect-error"
  key_links:
    - from: "src/core/runtime/runtime.ts"
      to: "src/core/runtime/types.ts"
      via: "import type { RuntimeTag, OpaqueSkeleton, ... } from './types.js'"
      pattern: "import type.*from ['\"]\\./types"
    - from: "src/core/types.ts (LoadResult)"
      to: "src/core/runtime/runtime.ts"
      via: "import type { SpineRuntime } from './runtime/runtime.js'; runtime?: SpineRuntime"
      pattern: "runtime\\?: SpineRuntime"
    - from: "tests/arch.spec.ts new RT-04 anchor"
      to: "src/core/runtime/**/*.ts"
      via: "globSync('src/core/runtime/**/*.ts') forbids sharp/node:fs/electron/spine-core imports"
      pattern: "src/core/runtime"
---

<objective>
Stand up the `core/runtime/` opaque-handle boundary scaffolding (**COMMIT C**): `unique symbol`-branded handle types carrying a **required** runtime tag, the `SpineRuntime` interface **signature surface only** (no bodies), the additive optional `LoadResult.runtime?` field, and two new `tests/arch.spec.ts` anchors (RT-04 Layer-3 purity + RT-03 no-co-mingled-imports backstop). A `// @ts-expect-error` negative fixture proves the cross-runtime compile wall is real.

Purpose: RT-03 makes a 4.2-object-at-a-4.3-boundary a **compile-time** error (stronger than any runtime guard or arch grep — the locked constraint per [[feedback_explicit_identity_over_inference]]: identity is *threaded, not inferred*). RT-04 keeps the new module Layer-3 pure (CLAUDE.md Fact #5). This lays both walls (the `unique symbol` brand = the primary wall; the no-co-mingled-imports arch anchor = the backstop) BEFORE any Phase-43 adapter impl can leak a raw spine type. **No spine-core import here** — Phase 42 scaffolds signatures only; the two adapter impls that import spine-core are Phase 43 (RT-02). 42-02 already mechanically repointed every pre-existing bare `@esotericsoftware/spine-core` consumer onto `spine-core-42` (a separate commit after the frozen COMMIT B; specifier-string rename only, zero logic change) — so `npm run typecheck:node` is already exit 0 on a fresh-clone-equivalent tree when COMMIT C begins; this plan adds only the new pure module + the additive optional field + tests on top of that clean baseline.

**Scope note (the locked Phase-42 typecheck contract — `42-REPLAN-NOTE.md` v2 §3):** the Phase-42 typecheck gate is `npm run typecheck:node` on a fresh-clone-equivalent tree (ignoring untracked gitignored `scripts/probe-*.ts`/`scripts/diagnose-*.ts`/`tests/_trace_tmp/` scratch, which is absent on a fresh clone / CI `npm ci`), NOT a literal full `npm run typecheck`. `npm run typecheck:web` is RED via `node_modules/@esotericsoftware/spine-player/dist/Player.d.ts:29`'s bare `@esotericsoftware/spine-core` → 4.3.0 hoist into `src/renderer/src/modals/AnimationPlayerModal.tsx` (~22 errors) — a **KNOWN, EXPECTED, Phase-47-OWNED** consequence of the locked 4.3-canonical direction (documented in 42-02; spine-player → 4.3.0 bump is Phase 47, sequenced last + revertible). This plan does NOT touch `src/renderer/**`, `AnimationPlayerModal.tsx`, spine-player, or `tsconfig.web.json`; the rejected Options 2/3 (`42-REPLAN-NOTE.md` v2 §4) are off the table.

Plain-English: We're installing the safety interlocks before the machinery. The "branded handle" is like a keyed connector that physically cannot plug a 4.2 part into a 4.3 socket — the TypeScript compiler refuses to build if anyone tries. We prove the interlock works with a deliberate "this should fail to compile" test. We do NOT wire up the actual engines yet (that's Phase 43) — just the connectors and the rule that no single file is allowed to touch both engines at once. (The old 4.2 files already had their engine nickname swapped to `spine-core-42` by the previous plan — we don't change their behavior here, we only add new files.) We check the *node* side (math + tests + main process) is perfectly clean; the *web*-side viewer-modal error from the player library's own internal engine reference is a known, expected Phase-47 job — not ours, not a bug here.

Output: `src/core/runtime/types.ts` + `runtime.ts` (pure, signatures only), `LoadResult.runtime?` added, two arch anchors appended, a compile-negative fixture — all in COMMIT C, a git descendant of 42-02's repoint commit (which is a descendant of the frozen COMMIT B `cc5783f`; the chain is A→B→repoint→C→D).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-PATTERNS.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-CONTEXT.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-REPLAN-NOTE.md

<interfaces>
<!-- The RT-03 + SpineRuntime designs are RESOLVED verbatim in RESEARCH.md. The executor MUST copy RESEARCH §RT-03 (the full types.ts block, lines ~302-365) and §SpineRuntime Interface (the full runtime.ts block, lines ~381-447) into the new files exactly. They are reproduced here as the contract; read RESEARCH at execution time for the authoritative copy + the surrounding rationale. -->

`src/core/runtime/types.ts` (RESEARCH §RT-03 — copy verbatim): `export type RuntimeTag = '4.2' | '4.3';` then per-handle-kind `declare const XBrand: unique symbol;` for Skeleton, SkeletonData, AnimationState, Slot, Attachment, Skin, Animation, Atlas; an `interface OpaqueHandle<B extends symbol> { readonly [k: symbol]: never; readonly __brand: B; readonly __rt: RuntimeTag; }` (`__rt` REQUIRED, non-optional — the locked constraint); the eight `export type Opaque* = OpaqueHandle<typeof *Brand>;`; and `brandHandle<H>(raw: unknown, rt: RuntimeTag): H` (Object.defineProperty `__rt` non-enumerable, cast — the single sanctioned boundary cast), `unwrapHandle<T>(h): T` (cast), `handleRuntime(h): RuntimeTag` (returns `h.__rt`). NO spine-core import; NO DOM/Electron/sharp/node:fs — operates only on `unknown`.

`src/core/runtime/runtime.ts` (RESEARCH §SpineRuntime Interface — copy verbatim, SIGNATURES ONLY): `import type {RuntimeTag, OpaqueSkeleton, OpaqueSkeletonData, OpaqueAnimationState, OpaqueSlot, OpaqueAttachment, OpaqueSkin, OpaqueAnimation, OpaqueAtlas} from './types.js';` then `export interface SpineRuntime { readonly tag: RuntimeTag; ...~30 method signatures... }` and `export declare function pickRuntime(tag: RuntimeTag): SpineRuntime;`. The exact ~30 signatures (loader-side: makeAtlas, parseSkeleton, applyRotatedRegionFix; lifecycle: makeSkeleton, makeAnimationState, skins, animations, animationDuration, animationName, skinName, setSkin, setupPose, setupPoseSlots, clearTracks, setAnimation, stateUpdate, stateApply, skeletonUpdate, updateWorldTransform; visibility: slots, slotName, slotAttachment, slotColorAlpha, skinEntries; bounds: attachmentKind, regionWorldVertices, vertexWorldVertices, boneAxisScale, attachmentRegionMeta, attachmentUVs, sequenceRegions) are enumerated verbatim in RESEARCH §SpineRuntime Interface — copy that block. **RESEARCH refinement to APPLY: keep ONLY `boneAxisScale(slot)`; do NOT add `slotBone` (a bone has no opaque handle type — exposing it would force an `OpaqueBone` the math layer never needs).** NO method bodies. NO spine-core import. `pickRuntime` is `export declare function` (body is Phase 43 / RT-02).

`src/core/types.ts` LoadResult additive field (RESEARCH §`LoadResult.runtime` field — copy verbatim):
```typescript
import type { SpineRuntime } from './runtime/runtime.js';
export interface LoadResult {
  // ...ALL existing fields UNCHANGED...
  /** Phase 42 (RT-03): the runtime adapter that parsed this skeleton.
   *  Declared in Phase 42; populated by loader.ts and consumed by
   *  sampler.ts/bounds.ts in Phase 43 (RT-02). Optional until then. */
  runtime?: SpineRuntime;
}
```
OPTIONAL (`?`) is load-bearing — keeps every existing consumer compiling unchanged; Phase 43 narrows to required.

Phase-42 typecheck gate (LOCKED — `42-REPLAN-NOTE.md` v2 §3; consistent end-to-end with 42-02 and 42-04). `npm run typecheck` = `npm run typecheck:node && npm run typecheck:web`. The Phase-42 gate is **`npm run typecheck:node`** (= `tsc --noEmit -p tsconfig.node.json`; `tsconfig.node.json` `include` covers `src/main/**`, `src/core/**`, `tests/**/*.ts`, `scripts/**/*.ts`) on a **fresh-clone-equivalent tree**: exit 0 ignoring untracked gitignored scratch — `scripts/probe-*.ts` (`.gitignore:73`), `scripts/diagnose-*.ts` (`.gitignore:74`), `tests/_trace_tmp/` (`.gitignore:85`) — which does NOT exist on a fresh clone / CI `npm ci`. The re-executor MUST NOT "fix"/edit/delete that gitignored scratch (correctly out of scope, absent on CI). `npm run typecheck:web` is RED (~22 errors in `src/renderer/src/modals/AnimationPlayerModal.tsx`) via `node_modules/@esotericsoftware/spine-player/dist/Player.d.ts:29`'s bare `@esotericsoftware/spine-core` → 4.3.0 hoist — a KNOWN, EXPECTED, Phase-47-OWNED leak (documented in 42-02), NOT a Phase-42 defect and NOT this plan's scope. `tsconfig.node.json` does NOT include `src/renderer/src/modals/**`, so the new `core/runtime/` scaffolding + the additive `LoadResult.runtime?` field are entirely within the clean `typecheck:node` scope and are unaffected by the Phase-47-owned `typecheck:web` leak.
</interfaces>

<analog_excerpts>
<!-- Read each at execution time before writing. -->
Pure-`core/`-module + typed-export idiom (module SHAPE — the brand mechanic itself is greenfield, no in-repo precedent: `grep 'unique symbol|__brand' src/` → zero hits; copy RESEARCH §RT-03 verbatim for the mechanic) — `src/core/png-header.ts`, `src/core/overrides.ts`, `src/core/usage.ts`, `src/core/errors.ts` (zero-DOM/Electron/sharp pure modules). (PATTERNS.md Topic C / Shared S6.)
Interface-declaration idiom (doc-commented fields, readonly where invariant) — `src/core/types.ts:192-221` (SampleRecord) and the `LoadResult` interface `src/core/types.ts:55-93` itself. (PATTERNS.md Topic C.)
Existing carve-out-set + glob-scanner to MATCH exactly (do NOT rebuild — extend) — `tests/arch.spec.ts:148-178` (the `src/core/**` fs/sharp scanner; `core/runtime/**` falls under it automatically with NO carve-out). (PATTERNS.md Topic D.)
ENOENT-tolerant "Wave-0 anchor before the file exists" named-anchor idiom — `tests/arch.spec.ts:205-216` (the verbatim "File doesn't exist yet … when it lands the grep applies" early-return comment style). (PATTERNS.md Topic D / Shared S3.)
The two anchor bodies to append are given VERBATIM in RESEARCH §Code Examples ("Extending tests/arch.spec.ts for RT-04") and PATTERNS.md Topic D points 1+2 — copy them.
Already-repointed import idiom (read at execute time) — every `loader.ts`/`sampler.ts`/`bounds.ts`/`analyzer.ts`/`bones.ts`/`synthetic-atlas.ts`/`types.ts` import line now reads `from 'spine-core-42'` (mechanically renamed by 42-02's repoint commit). COMMIT C adds ZERO further change to those lines or any logic in those files.
</analog_excerpts>

<ordering_invariant>
This plan's commit (**COMMIT C**) is a git descendant of 42-02's repoint commit, which is a descendant of the frozen COMMIT B (`cc5783f`, the alias, Plan 02), which is a descendant of COMMIT A (`1b5327d`, the SAFE-01 baseline). Sequential waves (this is Wave 3) make this automatic. The frozen COMMIT B is detect-and-skip in 42-02 — do NOT rebase/reorder it or any earlier commit (research Pitfall 1). Plan 01's `safe01-freeze-guard.spec.ts` ancestry assertion must remain hard-passing throughout this plan (it is part of the full `npm run test` suite, excluding the documented pre-existing local-only gitignored-rig skips in deferred-items.md §1–2).
</ordering_invariant>
</context>

<tasks>

<task type="auto">
  <name>Task 1: core/runtime/types.ts (branded opaque handles) + runtime.ts (SpineRuntime signatures only) + LoadResult.runtime?</name>
  <files>src/core/runtime/types.ts, src/core/runtime/runtime.ts, src/core/types.ts</files>
  <read_first>
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "RT-03: Opaque Branded Handles (concrete design)" (the full verbatim types.ts block + the "why stronger than the arch grep" + the Phase-42-scope-boundary note) AND § "SpineRuntime Interface (signature surface — RESOLVED)" (the full verbatim runtime.ts block + the `boneAxisScale(slot)`-only / no-`slotBone` refinement + "do NOT scaffold bodies/runtime-42/runtime-43") AND § "Code Examples → LoadResult.runtime field"
    - 42-REPLAN-NOTE.md §3 (the locked Phase-42 typecheck contract: the gate is `typecheck:node` fresh-clone, NOT full `typecheck`; the `typecheck:web` spine-player `.d.ts` leak is Phase-47-OWNED) AND §1 (COMMIT B `cc5783f` frozen — detect-and-skip in 42-02; do NOT rebase)
    - 42-CONTEXT.md "Claude's Discretion" → Opaque branded-handle mechanics (locked: `__rt` is a REQUIRED field, identity threaded-not-inferred, cross-runtime mix is a COMPILE error) + SpineRuntime signature surface delegation
    - 42-PATTERNS.md Topic C (`types.ts`, `runtime.ts`, `src/core/types.ts` additive-field sections) + Shared S6 (pure-core-module purity)
    - package.json `scripts` (`typecheck:node` = `tsc --noEmit -p tsconfig.node.json`) + tsconfig.node.json `include` (does NOT include `src/renderer/src/modals/**`; DOES include `scripts/**/*.ts` — why gitignored scratch leaks locally but is absent on a fresh clone)
    - src/core/png-header.ts (the pure-module SHAPE analog — note it is a node:fs carve-out; runtime/ needs NO carve-out)
    - src/core/types.ts:55-93 (the LoadResult interface — add the optional field in-place, all existing fields UNCHANGED) and :192-221 (the doc-commented interface idiom). Note its import line already reads `from 'spine-core-42'` (42-02's repoint) — leave that line as-is; COMMIT C only ADDS the SpineRuntime import + the optional field.
  </read_first>
  <action>
Create `src/core/runtime/types.ts` — copy the RESEARCH §RT-03 "concrete design" block VERBATIM (do not re-derive; it is RESOLVED):
- `export type RuntimeTag = '4.2' | '4.3';`
- Per-handle-kind `declare const <Kind>HandleBrand: unique symbol;` for: Skeleton, SkeletonData, AnimationState, Slot, Attachment, Skin, Animation, Atlas (8 brands).
- `interface OpaqueHandle<B extends symbol> { readonly [k: symbol]: never; readonly __brand: B; readonly __rt: RuntimeTag; }` — `__rt` is **REQUIRED and non-optional** (the locked constraint: thread identity, do not infer — [[feedback_explicit_identity_over_inference]]).
- The 8 `export type Opaque<Kind> = OpaqueHandle<typeof <Kind>HandleBrand>;`.
- `export function brandHandle<H extends OpaqueHandle<symbol>>(raw: unknown, rt: RuntimeTag): H` — `Object.defineProperty(raw as object, '__rt', { value: rt, enumerable: false, configurable: true }); return raw as H;` (the single sanctioned boundary cast — quarantined here).
- `export function unwrapHandle<T>(h: OpaqueHandle<symbol>): T { return h as unknown as T; }`
- `export function handleRuntime(h: OpaqueHandle<symbol>): RuntimeTag { return h.__rt; }`
- **NO** `import` of `@esotericsoftware/spine-core`, `spine-core-42`, `sharp`, `electron`, `node:fs`, or any DOM — the file operates only on `unknown` (keeps `core/runtime/` Layer-3 pure in Phase 42; the helper bodies are trivial type-casts with no spine-core dependency, so Phase 43's adapters have the factory ready).
- Add the explicit comment block from RESEARCH (the Phase-42-scope-boundary note: signatures + trivial cast bodies only; the two adapter impls are Phase 43).

Create `src/core/runtime/runtime.ts` — copy the RESEARCH §SpineRuntime Interface block VERBATIM (SIGNATURES ONLY):
- `import type { RuntimeTag, OpaqueSkeleton, OpaqueSkeletonData, OpaqueAnimationState, OpaqueSlot, OpaqueAttachment, OpaqueSkin, OpaqueAnimation, OpaqueAtlas } from './types.js';`
- `export interface SpineRuntime { readonly tag: RuntimeTag; <the ~30 signatures enumerated verbatim in RESEARCH §SpineRuntime Interface> }`. **Apply the RESEARCH refinement: include `boneAxisScale(slot: OpaqueSlot): { x: number; y: number };` and do NOT include `slotBone` (a bone has no opaque handle type).**
- `export declare function pickRuntime(tag: RuntimeTag): SpineRuntime;` (declaration only — body is Phase 43 / RT-02).
- **NO method bodies. NO `runtime-42.ts`/`runtime-43.ts`. NO spine-core import.** `import type` from `./types.js` only.

Modify `src/core/types.ts`: add `import type { SpineRuntime } from './runtime/runtime.js';` and add the optional field to `LoadResult` (RESEARCH §LoadResult.runtime field — verbatim):
```typescript
  /** Phase 42 (RT-03): the runtime adapter that parsed this skeleton.
   *  Declared in Phase 42; populated by loader.ts and consumed by
   *  sampler.ts/bounds.ts in Phase 43 (RT-02). Optional until then. */
  runtime?: SpineRuntime;
```
ALL existing `LoadResult` fields UNCHANGED. The `?` is load-bearing — every existing `core/` consumer must still compile with zero changes. **COMMIT C makes NO logic/behavior change to `loader.ts`, `sampler.ts`, `bounds.ts`, `analyzer.ts`, `bones.ts`, `synthetic-atlas.ts`, `types.ts` (Phase 43 wires sampler/bounds; Phase 42's D-13 4.3 smoke deliberately bypasses the unchanged loader). Their import specifier was already mechanically renamed to `spine-core-42` by 42-02's repoint commit — that is the ONLY pre-existing change to those files; COMMIT C adds zero further changes to them (in `types.ts`, COMMIT C adds ONLY the new `SpineRuntime` import line + the additive optional field — it does NOT touch the already-repointed `spine-core-42` import line).**

Run `npm run typecheck:node` — it MUST exit 0 on a fresh-clone-equivalent tree (i.e. zero errors in any git-tracked file; 42-02's repoint + the locked TS6133 fix already made the `typecheck:node`-tracked baseline clean, and the additive optional field + the new pure module introduce no breakage). Practical check: `npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/"` returns ZERO lines. **Do NOT run/assert full `npm run typecheck`** as this task's gate and do NOT touch `src/renderer/**`/`tsconfig.web.json`/spine-player — `typecheck:web` red is the KNOWN/EXPECTED/Phase-47-OWNED spine-player `.d.ts` leak (documented in 42-02), not a Phase-42 defect.
  </action>
  <verify>
    <automated>npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/" | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/" | wc -l` is `0` — ZERO `typecheck:node` errors in any git-tracked file (clean on a fresh-clone-equivalent tree; satisfiable because 42-02's repoint + TS6133 fix landed first; the additive optional field + new pure module add no breakage; the only-local errors are entirely inside gitignored scratch absent on CI and NOT fixed here). Full `npm run typecheck` is NOT this task's gate — `typecheck:web` red is the Phase-47-OWNED spine-player `.d.ts` leak
    - `grep -c "unique symbol" src/core/runtime/types.ts` ≥ 1 (the brand mechanic is present)
    - `grep -cE "readonly __rt: RuntimeTag" src/core/runtime/types.ts` ≥ 1 AND `grep -c "__rt?:" src/core/runtime/types.ts` == 0 (the runtime tag is REQUIRED, non-optional)
    - `grep -cE "export (function|declare function) (brandHandle|unwrapHandle|handleRuntime)" src/core/runtime/types.ts` ≥ 3 (factory/guard helpers present)
    - `grep -cE "from ['\"](@esotericsoftware/spine-core|spine-core-42|sharp|electron|node:fs)['\"]" src/core/runtime/types.ts src/core/runtime/runtime.ts` == 0 (Layer-3 pure; no spine-core in Phase 42 — note: NEITHER the bare specifier NOR the `spine-core-42` alias appears in the new runtime/ files)
    - `grep -c "interface SpineRuntime" src/core/runtime/runtime.ts` ≥ 1 AND `grep -c "export declare function pickRuntime" src/core/runtime/runtime.ts` ≥ 1 (interface + declared-only factory)
    - `grep -c "slotBone" src/core/runtime/runtime.ts` == 0 AND `grep -c "boneAxisScale" src/core/runtime/runtime.ts` ≥ 1 (the RESEARCH refinement applied)
    - `grep -cE "runtime\?: SpineRuntime" src/core/types.ts` ≥ 1 (LoadResult additive optional field)
    - `git log -1 --stat` for COMMIT C shows NO change to `src/core/loader.ts`, `src/core/sampler.ts`, `src/core/bounds.ts` (COMMIT C does not touch them; 42-02's earlier repoint commit owns the only change to them — a mechanical specifier rename, no logic change). This is a PER-COMMIT assertion (what COMMIT C itself changed), NOT a "pristine vs the 42-01 baseline" assertion — those files legitimately differ from the 42-01 baseline by exactly the 42-02 specifier rename.
    - `npx vitest run` green (full suite, excluding the documented pre-existing local-only gitignored-rig skips in deferred-items.md §1–2) — INCLUDING `tests/safe01/safe01-baseline.spec.ts` still byte-equal (no `core/` math changed — the 42-02 specifier rename is behavior-neutral, RE-VERIFIED in 42-02) and `safe01-freeze-guard.spec.ts` still hard-passing the ancestry assertion
  </acceptance_criteria>
  <done>The opaque-handle types (required `__rt` + `unique symbol` brand) and the `SpineRuntime` interface (signatures only, `boneAxisScale`-only refinement applied, no spine-core import) exist; `LoadResult.runtime?` is additively declared; `npm run typecheck:node` is exit 0 on a fresh-clone-equivalent tree (NOT full `typecheck` — `typecheck:web` red is the Phase-47-OWNED spine-player leak); COMMIT C made NO logic change to sampler/bounds/loader/analyzer/bones/synthetic-atlas/types (their specifier was already repointed by 42-02; COMMIT C only adds the new module + the optional field); the SAFE-01 baseline is still byte-equal.</done>
</task>

<task type="auto">
  <name>Task 2: Two tests/arch.spec.ts anchors (RT-04 Layer-3 + RT-03 no-co-mingled backstop) + the // @ts-expect-error compile-negative fixture</name>
  <files>tests/arch.spec.ts, tests/runtime/handle-brand-negative.ts (+ tsconfig include line if needed)</files>
  <read_first>
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "Code Examples → Extending tests/arch.spec.ts for RT-04" (the two anchor bodies VERBATIM) AND § "RT-03 Opaque Branded Handles" (the "why stronger than the arch grep — defense in depth" rationale for the negative fixture)
    - 42-REPLAN-NOTE.md §3 (the locked Phase-42 typecheck contract: the negative fixture is checked via `typecheck:node` — `tsconfig.node.json` includes `tests/**/*.ts`; do NOT assert full `typecheck`; the `typecheck:web` spine-player leak is Phase-47-OWNED)
    - 42-PATTERNS.md Topic D (the two anchor bodies points 1+2, the `spine-core-42` consistency note, and the "globSync self-handles an empty dir" note) + Shared S3
    - tsconfig.node.json (`include` covers `tests/**/*.ts` — so `tests/runtime/handle-brand-negative.ts` is in `typecheck:node` scope automatically; confirm this at execute time)
    - tests/arch.spec.ts:148-178 (the existing src/core/** scanner — the idiom to MATCH) and :205-229 (the ENOENT-tolerant named-anchor with the verbatim comment style)
    - src/core/runtime/types.ts (from Task 1 — the negative fixture imports the Opaque* types from here)
  </read_first>
  <action>
**Append** two named anchors to `tests/arch.spec.ts` (do NOT rebuild a scanner — extend the existing file; copy the bodies VERBATIM from RESEARCH §Code Examples / PATTERNS.md Topic D):

1. **RT-04 anchor** — `describe('Phase 42 RT-04: src/core/runtime/ is Layer-3 pure (no DOM/Electron/sharp/spine-core in Phase 42)', ...)`: `globSync('src/core/runtime/**/*.ts')`, for each file `readFileSync` and push to `offenders` if it matches `/from ['"]sharp['"]|from ['"]node:fs(\/promises)?['"]|from ['"]electron['"]|from ['"]@esotericsoftware\/spine-core['"]|from ['"]spine-core-42['"]/`; `expect(offenders).toEqual([])`. (Phase 42: runtime/ is signatures only — NO spine-core import yet, neither the bare specifier NOR the `spine-core-42` alias; the two adapter impls that import it are Phase 43 / RT-02. `globSync` self-handles an empty/late dir → zero files = empty offenders = green, so no ENOENT guard needed here.)
2. **RT-03 backstop anchor** — `describe('Phase 42 RT-03 backstop: no source file imports BOTH spine-core alias specifiers', ...)`: `globSync('src/**/*.{ts,tsx}')`, for each file set `has43 = /from ['"]@esotericsoftware\/spine-core['"]/.test(text)` and `has42 = /from ['"]spine-core-42['"]/.test(text)`; if `has43 && has42` push to `offenders`; `expect(offenders).toEqual([])`. **Use `spine-core-42` consistently** (the RESOLVED alias key — RESEARCH §Standard Stack; the scoped `@esotericsoftware/spine-core-43` was the rejected naive-direction illustration). This is the defense-in-depth backstop BEHIND the `unique symbol` brand (the brand is the primary compile-time wall; this arch grep is the second wall — exactly what the locked constraint "stronger than the arch-spec grep" requires). Note: after 42-02's repoint, every pre-existing src/ consumer imports ONLY `spine-core-42` (never the bare specifier), so this anchor is green — it now actively backstops that the repoint left no file co-mingling both. (The Phase-47-owned `typecheck:web` spine-player `.d.ts` leak originates in `node_modules/`, NOT a `src/**` file, so it is correctly outside this `src/**`-scoped anchor.)

Create `tests/runtime/handle-brand-negative.ts` — a **compile-negative** fixture proving the cross-runtime wall is real (RESEARCH §RT-03 "why stronger than the arch grep"; the contract says a cross-runtime mix is a COMPILE-TIME error, so prove it):
- `import type { OpaqueSkeleton, OpaqueSlot } from '../../src/core/runtime/types.js';`
- Declare a function expecting one handle kind/runtime; attempt to pass an incompatible one (e.g. an `OpaqueSlot` where `OpaqueSkeleton` is required, and/or construct a plain object lacking `__rt`/`__brand`) under a `// @ts-expect-error` directive — `tsc` MUST report an error there, which `@ts-expect-error` consumes; if the mix were ACCIDENTALLY assignable, `@ts-expect-error` itself becomes an "unused @ts-expect-error" error and `npm run typecheck:node` FAILS. This makes the wall self-proving under the existing typecheck script.
- Include at least one positive line (a correctly-typed handle usage with NO `@ts-expect-error`) so the file also proves valid usage compiles.
- File MUST be included by the `typecheck:node` tsconfig — `tsconfig.node.json` `include` already globs `tests/**/*.ts`, so `tests/runtime/handle-brand-negative.ts` is picked up automatically; confirm at execute time (if for any reason it is not under that glob, add it where the existing test tsconfig globs `tests/**`). Confirm `npm run typecheck:node` picks it up (RESEARCH: the negative fixture is verified via the typecheck script + the `@ts-expect-error`).

Run `npm run typecheck:node` (the negative fixture must make it exit 0 on a fresh-clone-equivalent tree — the `@ts-expect-error` is consumed by a real error; check: `npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/"` returns ZERO lines) and `npx vitest run tests/arch.spec.ts` (both new anchors green; `core/runtime/` is pure and no `src/**` file co-mingles the two specifiers). **Do NOT run/assert full `npm run typecheck`** — `typecheck:web` red is the KNOWN/EXPECTED/Phase-47-OWNED spine-player `.d.ts` leak (documented in 42-02), not a Phase-42 defect; do NOT touch `src/renderer/**`/`tsconfig.web.json`/spine-player.

This is the last task of COMMIT C. Commit message states it is the boundary scaffolding, post-alias + post-repoint, in plain English.
  </action>
  <verify>
    <automated>npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/" | wc -l | grep -qx '0' && npx vitest run tests/arch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/" | wc -l` is `0` (the `// @ts-expect-error` in the negative fixture is consumed by a genuine compile error — the wall is real; an unused `@ts-expect-error` would make this FAIL; clean on a fresh-clone-equivalent tree because 42-02's repoint + TS6133 fix landed first). Full `npm run typecheck` is NOT this task's gate — `typecheck:web` red is the Phase-47-OWNED spine-player `.d.ts` leak
    - `npx vitest run tests/arch.spec.ts` exits 0 with the two new describe blocks present and passing
    - `grep -c "Phase 42 RT-04" tests/arch.spec.ts` ≥ 1 AND `grep -c "Phase 42 RT-03 backstop" tests/arch.spec.ts` ≥ 1
    - `grep -cE "globSync\('src/core/runtime" tests/arch.spec.ts` ≥ 1 AND the RT-04 anchor regex includes both `@esotericsoftware\\/spine-core` and `spine-core-42` (no spine-core import allowed in Phase 42 runtime/ — neither bare nor alias)
    - `grep -c "@ts-expect-error" tests/runtime/handle-brand-negative.ts` ≥ 1 AND `grep -cE "Opaque(Skeleton|Slot)" tests/runtime/handle-brand-negative.ts` ≥ 1 (imports the branded handle types and exercises a cross-kind mix)
    - `grep -c "spine-core-43" tests/arch.spec.ts` == 0 (the rejected naive-direction key is NOT used; only `spine-core-42`)
    - `npx vitest run` green — full suite (excluding the documented pre-existing local-only gitignored-rig skips in deferred-items.md §1–2), INCLUDING `tests/safe01/safe01-baseline.spec.ts` byte-equal and `safe01-freeze-guard.spec.ts` ancestry still hard-passing
    - `git log -1 --stat` for COMMIT C shows only `src/core/runtime/**`, `src/core/types.ts`, `tests/arch.spec.ts`, `tests/runtime/handle-brand-negative.ts` (+ optionally a tsconfig include line) — NO `loader.ts`/`sampler.ts`/`bounds.ts`, NO `package.json`, NO `src/renderer/**`/`tsconfig.web.json` (per-commit assertion: 42-02's earlier repoint commit owns the specifier rename to those files; COMMIT C adds nothing to them; the Phase-47-owned spine-player `typecheck:web` leak is not touched here)
  </acceptance_criteria>
  <done>The RT-04 Layer-3 anchor and the RT-03 no-co-mingled-imports backstop are appended and green; a `// @ts-expect-error` fixture proves a cross-runtime handle mix is a real `tsc` error under `typecheck:node` (defense-in-depth: brand = primary wall, arch anchor = backstop); `npm run typecheck:node` is exit 0 on a fresh-clone-equivalent tree (NOT full `typecheck` — `typecheck:web` red is the Phase-47-OWNED spine-player leak); the SAFE-01 baseline is still byte-equal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 4.2 runtime objects ↔ 4.3 runtime objects (the dual-type universe) | Structurally-similar identically-named classes from two spine-core copies; a wrong-runtime object crossing the adapter boundary is silent runtime corruption |
| `core/runtime/` module ↔ rest of `core/` | The Layer-3 purity invariant (CLAUDE.md Fact #5) must hold for the new module |
| spine-player's own bare `.d.ts` → renderer typecheck:web | Phase-47-OWNED (NOT a Phase-42 / not a `core/` surface): out of this plan's scope by the locked resolution; this plan's anchors are `src/**`-scoped and the leak originates in `node_modules/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-11 | Tampering | Dual type-universe structural corruption — a 4.2 `Skeleton` flows into a 4.3-typed parameter; `tsc` passes on structural overlap; runtime garbage | mitigate | RT-03 `unique symbol` per-kind brand + REQUIRED non-optional `__rt` tag makes a raw spine `Skeleton` (from either runtime) and any cross-kind/cross-runtime handle UNASSIGNABLE — a `tsc` error (compile-time, stronger than any runtime guard). Proven real by the `// @ts-expect-error` negative fixture (acceptance: `npm run typecheck:node` would FAIL if the mix were accidentally assignable) |
| T-42-12 | Elevation of privilege (boundary bypass) | A source file imports BOTH spine-core specifiers and hand-mixes raw objects, defeating the brand | mitigate | The RT-03 backstop arch anchor (`no src/**/*.ts imports @esotericsoftware/spine-core AND spine-core-42`) is the second wall behind the brand — defense-in-depth exactly as the locked constraint requires ("stronger than the arch-spec grep" = brand FIRST, grep SECOND). After 42-02's repoint every consumer imports ONLY `spine-core-42`, so this anchor actively backstops that the repoint left zero co-mingling. Acceptance asserts the anchor exists and is green |
| T-42-13 | Tampering | `core/runtime/` imports DOM/Electron/sharp (or spine-core prematurely in Phase 42, bare OR `spine-core-42`) breaking Layer-3 / leaking a raw type early | mitigate | The RT-04 arch anchor globs `src/core/runtime/**/*.ts` and forbids sharp/node:fs/electron AND both spine-core specifiers (bare and `spine-core-42`); acceptance asserts zero such imports in `types.ts`/`runtime.ts`. Phase 42 is signatures only — the adapter impls that legitimately import spine-core are Phase 43 |
| T-42-14 | Tampering | An "unused `@ts-expect-error`" silently passing → the negative fixture proves nothing (false-green) | mitigate | The fixture is checked via the existing `npm run typecheck:node` script (`tsconfig.node.json` includes `tests/**/*.ts`): TypeScript reports an unused `@ts-expect-error` as an error, so a fixture that no longer triggers a real compile error FAILS `typecheck:node` loudly (acceptance criterion) |
| T-42-15 | Tampering | A `core/` change perturbs the SAFE-01 baseline (silent regression) | mitigate | This plan touches only NEW pure modules + an additive optional field + tests; COMMIT C makes NO logic change to `loader.ts`/`sampler.ts`/`bounds.ts` (per-commit `git log -1 --stat` assertion — 42-02's repoint commit owns the only, behavior-neutral, specifier-rename change, already SAFE-01-RE-VERIFIED in 42-02). The full `tests/safe01` suite (byte-equal + ancestry) runs as part of `npm run test` and must stay green |
| T-42-24 (inherited) | Tampering / false-defect | The Phase-47-owned `typecheck:web` spine-player `.d.ts` 4.3-leak is "fixed" here (pre-empting Phase 47 / masking real future drift) OR mis-flagged by a downstream verifier as a Phase-42/COMMIT-C miss | mitigate (no new high threat) | This plan's typecheck gate is scoped to `typecheck:node` (fresh-clone) per the locked resolution; acceptance criteria forbid touching `src/renderer/**`/`tsconfig.web.json`/spine-player and the per-commit `git log` assertion confirms COMMIT C does not touch them. The RT-03/RT-04 anchors are `src/**`-scoped while the leak originates in `node_modules/@esotericsoftware/spine-player/dist/Player.d.ts` — correctly outside scope. The leak is documented as KNOWN/EXPECTED/Phase-47-OWNED in 42-02 (+ its SUMMARY). No new dependency/parse/auth surface — a pure scoping correction; rejected Options 2/3 are off the table |

ASVS L1 V2/V3/V4/V6-of-secrets/V7 are N/A: pure type-scaffolding + arch-test plan, no auth/session/access-control/secrets/error-handling surface. V5 (input validation) is unchanged — `loader.ts` is deliberately NOT modified here (loader dispatch is Phase 44; 42-02's specifier rename is behavior-neutral). The genuine surface is **type-system integrity of the cross-runtime boundary** (T-42-11/12, mitigated by the brand + backstop anchor + proven by the compile-negative fixture). The 42-02 repoint introduced no new surface here (it reused the already-pinned frozen alias); the `typecheck:web` narrowing introduces no new high threat (T-42-24 — scoping correction, Phase-47-owned).
</threat_model>

<verification>
- `npm run typecheck:node` exits 0 on a fresh-clone-equivalent tree — `npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/"` returns ZERO lines (42-02's repoint + the locked TS6133 fix landed first, so the `typecheck:node`-tracked baseline is already clean; the additive optional field compiles cleanly; the `// @ts-expect-error` negative fixture is consumed by a real error — the cross-runtime wall is real). This is the Phase-42 gate; full `npm run typecheck` is NOT asserted — `typecheck:web` red is the KNOWN/EXPECTED/Phase-47-OWNED spine-player `Player.d.ts` 4.3-leak (documented in 42-02), NOT a Phase-42 defect
- `npx vitest run tests/arch.spec.ts` — RT-04 (`core/runtime/` Layer-3 pure, no spine-core in Phase 42 — neither bare nor `spine-core-42`) and RT-03 backstop (no co-mingled imports; backstops the 42-02 repoint) anchors green
- `npx vitest run` — FULL suite green (excluding the documented pre-existing local-only gitignored-rig skips in deferred-items.md §1–2), including `tests/safe01/safe01-baseline.spec.ts` byte-equal (proof no `core/` math changed — the 42-02 specifier rename is behavior-neutral, already RE-VERIFIED in 42-02) and `safe01-freeze-guard.spec.ts` ancestry hard-passing
- `git log -1 --stat` for COMMIT C confirms it adds NO change to `loader.ts`/`sampler.ts`/`bounds.ts` and NO change to `src/renderer/**`/`tsconfig.web.json` (per-commit assertion; 42-02's repoint commit, not COMMIT C, owns the loader/sampler/bounds specifier rename — no logic change; the Phase-47-owned spine-player `typecheck:web` leak is not touched here)
- COMMIT C is a git descendant of 42-02's repoint commit → the frozen COMMIT B (`cc5783f`) → COMMIT A (`1b5327d`) (sequential waves; the ancestry chain A→B→repoint→C is intact; COMMIT B is detect-and-skip in 42-02, not rebased)
</verification>

<success_criteria>
- RT-03 satisfied: opaque branded handles carry a required `__rt` runtime tag + per-kind `unique symbol` brand; a cross-runtime/cross-kind mix is a compile-time error proven real by the `// @ts-expect-error` fixture (checked under `typecheck:node`); the no-co-mingled-imports arch anchor is the backstop (defense-in-depth, brand-first; it also backstops that 42-02's repoint left zero co-mingling)
- RT-04 satisfied: `core/runtime/**` imports no DOM/Electron/sharp/node:fs and no spine-core in Phase 42 (signatures only — neither the bare specifier nor the `spine-core-42` alias), enforced by the new arch anchor — CLAUDE.md Fact #5 / Layer-3 invariant green
- `LoadResult.runtime?` is additively declared (optional — no existing consumer changes); COMMIT C makes NO logic change to sampler/bounds/loader/analyzer/bones/synthetic-atlas/types (42-02's repoint owns their only change — a behavior-neutral specifier rename; Phase 43 / RT-02 does the actual rewiring)
- The SAFE-01 baseline is still byte-equal and the freeze-guard ancestry assertion is still hard-passing — Phase 42's scaffolding is genuinely behavior-neutral; `npm run typecheck:node` is exit 0 on a fresh-clone-equivalent tree (satisfiable because 42-02 landed first), and the `typecheck:web` spine-player `.d.ts` leak is the documented Phase-47-OWNED known-item (NOT a Phase-42 defect; full `typecheck` is NOT this phase's gate)
</success_criteria>

<output>
After completion, create `.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-03-SUMMARY.md`. Record: the exact `SpineRuntime` method-signature set scaffolded (and explicit confirmation `slotBone` was NOT added per the RESEARCH refinement), the COMMIT C SHA, explicit confirmation that COMMIT C itself made NO logic change to `loader.ts`/`sampler.ts`/`bounds.ts` (the per-commit `git log -1 --stat` evidence — 42-02's repoint commit owns their only, behavior-neutral, specifier-rename change) and the SAFE-01 baseline stayed byte-equal, that `npm run typecheck:node` is exit 0 on a fresh-clone-equivalent tree (NOT full `typecheck` — and a one-line restatement that the `typecheck:web` spine-player `Player.d.ts` 4.3-leak is the KNOWN/EXPECTED/Phase-47-OWNED known-item documented in 42-02, NOT a Phase-42 defect), and how the `// @ts-expect-error` fixture is wired into `npm run typecheck:node`.
</output>
