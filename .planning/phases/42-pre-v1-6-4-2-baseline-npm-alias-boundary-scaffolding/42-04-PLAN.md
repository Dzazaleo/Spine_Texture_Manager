---
phase: 42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding
plan: 04
type: execute
wave: 4
depends_on: ["42-03"]
files_modified:
  - .github/workflows/ci.yml
  - tests/runtime/d13-43-load-smoke.spec.ts
  - tests/safe01/phase-gate.ts
  - tests/safe01/phase44-fixture-guard.spec.ts
  - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-OWNER-EXPORT-SPEC.md
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "A new `.github/workflows/ci.yml` runs on push(any branch) + pull_request→main + workflow_dispatch and NEVER on tags; `release.yml` is byte-untouched (the tag/auto-update path stays isolated)"
    - "The 3-OS matrix (ubuntu-latest, windows-2022, macos-14) runs `npm ci` + `npm run typecheck:node` + `npm run test` + an explicit visible `git merge-base --is-ancestor` step, with `fetch-depth: 0` (full history — required for the ancestry check)"
    - "The CI typecheck step is the bare `npm run typecheck:node` (the dual-runtime gate scope — `src/main` + `src/core` + `tests`), NOT full `npm run typecheck`; it asserts a clean exit 0 with NO hedging/allowlist/scratch-exclusion shim. CI runs a fresh `npm ci` clone so the gitignored scratch (`scripts/probe-*.ts`, `scripts/diagnose-*.ts`, `tests/_trace_tmp/`) is NATURALLY ABSENT → `typecheck:node` is genuinely exit 0 there. `npm run typecheck:web` is RED via spine-player's own bare `Player.d.ts` 4.3 hoist — a KNOWN, EXPECTED, Phase-47-OWNED leak (documented in 42-02); CI does NOT assert `typecheck:web` and CI does NOT add a scratch-exclusion shim"
    - "A `bundle-smoke` job runs ONLY on pull_request→main: `npm run build`, asserts BOTH spine-core copies survive packaging, runs the BUILT worker (not src/) against a 4.2 fixture + the in-repo 4.3 JSON"
    - "The in-repo 4.3 JSON loads through the 4.3 runtime's `SkeletonJson` DIRECTLY (bypassing the still-4.2-only loader.ts) without the v1.4 `SpineVersionUnsupportedError` — integrity only, not value (D-13)"
    - "A phase-gated guard FAILS once the milestone reaches Phase 44 if the owner ORCL-01/SLIDER-01 fixture dirs are still absent; a committed `42-OWNER-EXPORT-SPEC.md` lets the owner export all 5 artifacts in one Spine session, off the critical path (D-01..D-05)"
    - "The phase-wide 4-commit ordering A (`1b5327d`) → B (`cc5783f`, frozen/detect-skip) → repoint → C → D and the `typecheck:node`-green-on-fresh-clone invariant are mutually consistent end-to-end (now satisfiable — that is the whole point of the re-plan): the explicit CI git-ancestry step re-proves the ordering AND the CI `npm run typecheck:node` step is genuinely exit 0 on the same fresh `npm ci` clone, with no contradiction"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Dual-runtime gate workflow: 3-OS test matrix + git-ancestry step + PR-only bundle-smoke; never tags; paths-ignore docs-only; pinned action SHAs; typecheck step is the bare `npm run typecheck:node` asserting a clean exit 0 (no hedging, no scratch-exclusion shim — fresh `npm ci` clone makes the gitignored scratch naturally absent; `typecheck:web` is the Phase-47-OWNED spine-player leak, NOT asserted)"
      contains: "merge-base"
    - path: "tests/runtime/d13-43-load-smoke.spec.ts"
      provides: "4.3 SkeletonJson direct-load of the in-repo 4.3 JSON past the v1.4 reject (integrity, not value); documented hand-authored-stable-4.3 fallback if the beta fixture fails to parse"
      min_lines: 25
    - path: "tests/safe01/phase-gate.ts"
      provides: "Committed `export const CURRENT_PHASE = 42` integer marker (Q2 RESOLVED: committed constant, not STATE.md parsing)"
      contains: "CURRENT_PHASE"
    - path: "tests/safe01/phase44-fixture-guard.spec.ts"
      provides: "it.skipIf(CURRENT_PHASE < 44) while in 42/43; hard-fails at ≥44 if ORCL-01/SLIDER-01 fixture dirs absent (D-13 owner-blocker cannot slip past Phase 44)"
      min_lines: 20
    - path: ".planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-OWNER-EXPORT-SPEC.md"
      provides: "Self-contained one-session owner handoff for all 5 export artifacts (4 rigs): ORCL-01 non-IK ×(4.3+4.2), SLIDER-01 with exact analytical params, XTRA-01, XTRA-02; atlas-source only; redistributable"
      contains: "OWNER"
  key_links:
    - from: ".github/workflows/ci.yml test job"
      to: "git history (D-09 ancestry)"
      via: "explicit `git merge-base --is-ancestor` step with actions/checkout fetch-depth: 0"
      pattern: "fetch-depth: 0"
    - from: ".github/workflows/ci.yml bundle-smoke job"
      to: "out/main/sampler-worker.cjs (built, not src/)"
      via: "if: github.event_name == 'pull_request'; npm run build; run BUILT worker vs 4.2 + 4.3 fixtures; assert both spine-core copies packaged"
      pattern: "github.event_name == 'pull_request'"
    - from: "tests/runtime/d13-43-load-smoke.spec.ts"
      to: "@esotericsoftware/spine-core (4.3.0) SkeletonJson"
      via: "construct SkeletonJson directly (NOT loadSkeleton — loader.ts unchanged in Phase 42); assert no SpineVersionUnsupportedError + constraints parse"
      pattern: "SkeletonJson"
    - from: "tests/safe01/phase44-fixture-guard.spec.ts"
      to: "tests/safe01/phase-gate.ts CURRENT_PHASE"
      via: "it.skipIf(CURRENT_PHASE < 44); at >=44 expect(ORCL_01_DIR && SLIDER_01_DIR exist)"
      pattern: "CURRENT_PHASE"
---

<objective>
Land the CI-01 dual-runtime gate (**COMMIT D**): a new `.github/workflows/ci.yml` carrying the 3-OS test matrix + the explicit git-ancestry assertion + the PR-only production-bundle smoke; the D-13 4.3 load-smoke (4.3 `SkeletonJson` direct, bypassing the unchanged loader); the Phase-44 owner-fixture-absence guard; and the committed `42-OWNER-EXPORT-SPEC.md` so the owner's blocked 4.3 fixture exports run in parallel, off the critical path.

Purpose: CI makes the SAFE-01 ordering invariant + the dual-install integrity machine-checked on every code push as the 6-phase milestone lands — not reviewer memory (D-09). `release.yml` stays **byte-untouched** so the standing "don't push v1.6 tags / tags trigger auto-update" guard ([[feedback_dont_push_release_tags]]) is never perturbed. The Phase-44 guard ensures the owner blocker cannot silently slip past its scheduled boundary.

**The CI typecheck gate (LOCKED — `42-REPLAN-NOTE.md` v2 §3; Option 1, user-selected 2026-05-16):** the CI typecheck step is the bare **`npm run typecheck:node`** (the dual-runtime gate scope — `src/main` + `src/core` + `tests`), NOT full `npm run typecheck`. CI runs on a fresh `npm ci` clone, so the gitignored scratch files (`scripts/probe-*.ts` `.gitignore:73`, `scripts/diagnose-*.ts` `.gitignore:74`, `tests/_trace_tmp/` `.gitignore:85`) are **naturally absent** on the CI checkout → `typecheck:node` is **genuinely exit 0** there; **NO scratch-exclusion shim / allowlist / "modulo baseline" wrapper is needed or permitted in CI** (the shim is unnecessary because CI's `npm ci` clone simply does not contain the scratch). This is satisfiable end-to-end because 42-02's repoint commit eliminated ALL bare `@esotericsoftware/spine-core` orphans in `typecheck:node` scope (src/ + tests/) AND folded the locked one-token `tests/main/image-worker-rotation.spec.ts:190` TS6133 fix (deferred-items.md §3 — the pre-existing error verified at the 42-01 base commit; `tests/main/**` is in `tsconfig.node.json` `include`). `npm run typecheck:web` is RED (~22 errors in `src/renderer/src/modals/AnimationPlayerModal.tsx`) via `node_modules/@esotericsoftware/spine-player/dist/Player.d.ts:29`'s bare `@esotericsoftware/spine-core` → 4.3.0 hoist — a **KNOWN, EXPECTED, Phase-47-OWNED** consequence of the locked 4.3-canonical direction (documented in 42-02). CI does **NOT** run/assert `typecheck:web` and does **NOT** add any shim for it; Phase 47 (spine-player → 4.3.0 bump + viewer regression, sequenced last + revertible) owns and resolves it. The CI typecheck contract is therefore end-to-end consistent with 42-02's and 42-03's: a clean `npm run typecheck:node` exit 0 on a fresh clone, no hedging anywhere — and the phase-wide 4-commit ordering A→B→repoint→C→D and this `typecheck:node`-green invariant are now mutually consistent (the explicit CI ancestry step and the CI `typecheck:node` step both pass on the same fresh `npm ci` clone — the whole point of the re-plan).

Plain-English (project owner is a git beginner — [[user_git_experience]] / [[feedback_explain_git]]): This adds an automatic checker that runs every time you push code (it stays quiet when you only edit planning notes or docs, and it NEVER runs on release tags — releases are a completely separate, untouched system). The checker re-proves, on three operating systems, that the "before" photo (Plan 01) was taken before the renovation (the frozen alias commit `cc5783f`) — using Git's own history. It also runs the TypeScript checker on the *node* side only (the math + tests + main process — `npm run typecheck:node`) and expects a perfectly clean pass (zero errors). That is genuinely possible on the automatic checker because it always starts from a brand-new clean copy of the project — so the throwaway scratch files that only exist on your machine simply are not there, and there is nothing to hedge about. We deliberately do NOT check the *web* side: the animation-viewer's player library has its own internal reference to the engine by its plain name, so the viewer modal won't type-check on the web side until Phase 47 bumps the player to 4.3 (that is the last, easily-reversible step and it owns this clean-up). A heavier packaging check runs only when you open a pull request into `main`. We also write a short instruction sheet for you (`42-OWNER-EXPORT-SPEC.md`) so you can export the four test rigs from the Spine editor whenever convenient — that work happens on the side and never blocks the code.

Output: `ci.yml` (new; `release.yml` untouched) + the D-13 4.3 load-smoke + the phase-gate marker + the Phase-44 guard + `42-OWNER-EXPORT-SPEC.md`, all in COMMIT D (a git descendant of COMMIT C, which is a descendant of 42-02's repoint commit → the frozen COMMIT B `cc5783f` → COMMIT A `1b5327d`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-PATTERNS.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-CONTEXT.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-REPLAN-NOTE.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/deferred-items.md

<interfaces>
<!-- The ci.yml design + D-13 mechanism are RESOLVED verbatim in RESEARCH §CI Architecture. Copy them. Read RESEARCH at execution time for the authoritative blocks. -->

`ci.yml` triggers (RESEARCH §CI Architecture — RESOLVED, copy verbatim):
```yaml
on:
  push:
    branches: ['**']
    paths-ignore: ['.planning/**', '**/*.md', 'docs/**', 'LICENSE', '.gitignore']
  pull_request:
    branches: [main]
    paths-ignore: ['.planning/**', '**/*.md', 'docs/**', 'LICENSE']
  workflow_dispatch:
  # NEVER tags: — release.yml owns tags (D-10/D-11)
```
`test` job: `strategy.fail-fast: true`, `matrix.os: [ubuntu-latest, windows-2022, macos-14]` (mirror release.yml EXACTLY — Linux retained in CI *test* though dropped as a *release* target per [[project_linux_deferred]]). Steps: `actions/checkout` **with `fetch-depth: 0`** (CRITICAL — `git merge-base --is-ancestor`/`git log --diff-filter=A` need full history; the default depth-1 shallow clone breaks the D-09 gate — research Pitfall 5), `actions/setup-node` node 22 cache npm, `npm ci`, `npm run typecheck:node` (the bare command — the dual-runtime gate scope; asserts a clean exit 0; NO "modulo pre-existing baseline"/allowlist/scratch-exclusion shim — a fresh `npm ci` clone has the gitignored scratch naturally absent so `typecheck:node` is genuinely exit 0; full `npm run typecheck` is NOT run — `typecheck:web` is the Phase-47-OWNED spine-player `Player.d.ts` leak), `npm run test`, then an explicit visible-in-log git-ancestry step (the bare `git merge-base --is-ancestor "$(git log --diff-filter=A --format=%H -- tests/safe01/baselines/_manifest.json | tail -1)" "$(git log -S 'spine-core-42' --format=%H -- package.json | tail -1)"` — belt-and-suspenders so the gate is visible even if the vitest suite is later restructured; D-09; note `git log -S 'spine-core-42' -- package.json` resolves to the frozen COMMIT B `cc5783f`).
`bundle-smoke` job: `if: github.event_name == 'pull_request'`, `needs: test`, `runs-on: ubuntu-latest` (one OS sufficient for a packaging-integrity smoke), `actions/checkout` fetch-depth: 0, setup-node 22, `npm ci`, `npm run build`, then assert BOTH `node_modules/@esotericsoftware/spine-core` (4.3.0) AND `node_modules/spine-core-42` (4.2.111) survive in the packed app, and run the BUILT `out/main/sampler-worker.cjs` (NOT `src/`) against a committed 4.2 fixture AND the in-repo 4.3 JSON (Pitfall 8 — tree-shaking/packaging drop of the dynamically-dispatched runtime is caught at merge; pay packaging cost only at PR→main, D-12).
Action SHAs MUST be pinned to full commit SHAs (mirror `release.yml`'s pinned-SHA convention — copy the exact SHAs from release.yml: `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5` # v4.3.1, `actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020` # v4.4.0; confirm against the live release.yml at execution time).

D-13 4.3 load-smoke (RESEARCH §CI Architecture D-13 + §Pitfall 7 — CRITICAL bypass): the gated `loadSkeleton('fixtures/SPINE_4_3_TEST/SPINE_4_3_TEST.json')` STILL throws `SpineVersionUnsupportedError` (Phase 42 does NOT modify loader.ts — that is Phase 44; 42-02's specifier rename to loader.ts is behavior-neutral, the version-guard logic is byte-unchanged). So the smoke MUST drive the 4.3 runtime's `SkeletonJson` DIRECTLY:
```typescript
import { SkeletonJson, AtlasAttachmentLoader, TextureAtlas } from '@esotericsoftware/spine-core'; // = 4.3.0
// build a 4.3 SkeletonJson(new AtlasAttachmentLoader(new TextureAtlas(<SPINE_4_3_TEST .atlas text>))),
// readSkeletonData(JSON.parse(SPINE_4_3_TEST.json))
// ASSERT: does NOT throw SpineVersionUnsupportedError (never calls the gated loader)
// ASSERT: skeletonData parses (the constraints[] array is consumed by the 4.3 SkeletonJson)
```
Caveat (RESEARCH §A2/§Q3): `fixtures/SPINE_4_3_TEST/SPINE_4_3_TEST.json` is `4.3.91-beta` with `root.constraints`. D-13 asserts integrity only (parses past the v1.4 reject without throwing) so minor beta-vs-stable drift that still parses is acceptable. **Fallback (Q3 contingency — implement as a guarded branch):** if the beta JSON fails to parse under stable 4.3.0 `SkeletonJson`, the test creates/uses a tiny hand-authored stable-4.3 JSON (`{"skeleton":{"spine":"4.3.0"},"constraints":[],"bones":[{"name":"root"}],"slots":[...],"skins":[...]}` + minimal region) committed as `fixtures/SPINE_4_3_MIN/SPINE_4_3_MIN.json` and asserts on THAT instead — record which path was taken in the SUMMARY.

Phase-44 guard marker (Q2 RESOLVED — lock the committed-constant mechanism, NOT STATE.md parsing): `tests/safe01/phase-gate.ts` exports `export const CURRENT_PHASE = 42 as const;` (research-recommended: robust, explicit, no STATE.md format-drift risk — RESEARCH §Open Questions Q2 / §A4). The roadmapper/owner bumps this integer as the milestone advances. The guard: `it.skipIf(CURRENT_PHASE < 44)` while in 42/43; at `CURRENT_PHASE >= 44`, hard-`expect(existsSync(ORCL_01_DIR) && existsSync(SLIDER_01_DIR)).toBe(true)`.

Typecheck contract reconciliation (LOCKED — end-to-end consistent with 42-02 and 42-03; `42-REPLAN-NOTE.md` v2 §3 + deferred-items.md §3). `npm run typecheck` = `npm run typecheck:node && npm run typecheck:web`. ONE pre-existing typecheck error existed at the 42-01 base commit `166523f4aaca5e002bf612bb7ae9d5b7c422d206`: `tests/main/image-worker-rotation.spec.ts(190,13): error TS6133: 'data' is declared but its value is never read` — `tests/main/**` is in `tsconfig.node.json` `include`, so it is in `typecheck:node` scope. The 4.3 flip in the frozen COMMIT B (`cc5783f`) additionally orphaned every bare consumer (~58 `typecheck:node` errors). 42-02's repoint commit (the separate commit immediately after the frozen COMMIT B) resolves BOTH: it repoints every bare `@esotericsoftware/spine-core` consumer (src/ + tests/, count-free fresh-grep scope) to `spine-core-42`, AND folds the locked one-token TS6133 fix (`data`→`_data` at `image-worker-rotation.spec.ts:190`, flagged inline `// repoint deviation: pre-existing TS6133, see 42 deferred-items.md §3`). Net effect by the time CI runs in this plan: `npm run typecheck:node` is exit 0 **on a fresh `npm ci` clone** (CI has no gitignored scratch). This plan's CI typecheck step is therefore the bare `npm run typecheck:node` asserting a clean exit 0 with NO "modulo pre-existing baseline"/allowlist/scratch-exclusion shim anywhere — the contract is identical to 42-02's and 42-03's so the executor and checker see one consistent end-to-end contract. CI does NOT run/assert full `npm run typecheck` (the `typecheck:web` spine-player `Player.d.ts` 4.3-leak is the Phase-47-OWNED known-item, NOT a Phase-42 defect — documented in 42-02; Phase 47 owns it).
</interfaces>

<analog_excerpts>
<!-- Read each at execution time before writing. -->
`test` job matrix + pinned-action + setup-node idiom to MIRROR (and the exact pinned SHAs to copy) — `.github/workflows/release.yml:27-57` (PATTERNS.md Topic G / Shared S5). `release.yml` itself stays BYTE-UNTOUCHED.
`it.skipIf` condition-guarded idiom — `tests/main/sampler-worker-girl.spec.ts:23-28` (the [[feedback_gitignore_fixtures_check_test_refs]] precedent; PATTERNS.md Topic E/F).
Typed-error reject envelope being asserted-ABSENT — `src/core/loader.ts:137-141` (the `major>=5 || (major===4 && minor>=3)` throw) + `src/core/errors.ts` `SpineVersionUnsupportedError` (`.name` is load-bearing; PATTERNS.md Topic E `d13-43-load-smoke.spec.ts` section). The smoke asserts this is NOT thrown because it never calls the gated loader. (Note: `loader.ts`'s import specifier now reads `spine-core-42` per 42-02's repoint — behavior-neutral; the version-guard logic is byte-unchanged.)
Skip-until-condition idiom for phase-gate — PATTERNS.md Topic F `phase44-fixture-guard.spec.ts` + Shared S3.
Owner handoff has NO code analog (PATTERNS.md Topic H) — structure derived from CONTEXT.md D-01..D-05 + D-13.
`tsconfig.node.json` `include` / `package.json` `scripts` — confirm `typecheck:node` = `tsc --noEmit -p tsconfig.node.json` and that `tsconfig.node.json` includes `tests/**/*.ts` + `scripts/**/*.ts` (the `scripts/**/*.ts` glob is why gitignored scratch leaks LOCALLY but is absent on a fresh `npm ci` CI clone — so CI needs NO scratch-exclusion shim).
`.gitignore:73`/`74`/`85` — `scripts/probe-*.ts`, `scripts/diagnose-*.ts`, `tests/_trace_tmp/` (the scratch globs naturally absent on a fresh `npm ci` clone; CI does NOT add a shim for them and does NOT touch them).
</analog_excerpts>

<ordering_invariant>
COMMIT D (this plan) is a git descendant of COMMIT C → 42-02's repoint commit → the frozen COMMIT B (`cc5783f`) → COMMIT A (`1b5327d`). Sequential waves make this automatic (this is Wave 4). The frozen COMMIT B is detect-and-skip in 42-02 — do NOT rebase/reorder it or any earlier commit (research Pitfall 1). The new `ci.yml`'s explicit ancestry step is the CI realization of D-09 — it re-proves `merge-base --is-ancestor A B` in CI on every code push (requires `fetch-depth: 0`; `git log -S 'spine-core-42' -- package.json` resolves to the frozen `cc5783f`). Plan 01's `safe01-freeze-guard.spec.ts` ancestry assertion is ALSO run by `npm run test` inside the `ci.yml` `test` job — belt-and-suspenders (the vitest test AND the explicit CI step both enforce it, per D-09 "machine-checked, not reviewer memory"). The 4-commit ordering A→B→repoint→C→D and the CI `npm run typecheck:node`-green-on-fresh-clone invariant are mutually consistent end-to-end (now satisfiable — the explicit CI ancestry step and the CI `typecheck:node` step both pass on the same fresh `npm ci` clone). Do NOT touch `release.yml`. Do NOT add a `tags:` trigger to `ci.yml`. Do NOT add a scratch-exclusion shim to `ci.yml` (unnecessary — fresh `npm ci` clone has no gitignored scratch).
</ordering_invariant>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-13 4.3 load-smoke (4.3 SkeletonJson direct, bypassing the unchanged loader) + the beta→stable fallback contingency</name>
  <files>tests/runtime/d13-43-load-smoke.spec.ts (+ fixtures/SPINE_4_3_MIN/ only if the Q3 beta fallback path is taken)</files>
  <read_first>
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "CI Architecture → D-13 4.3 CI arm" (the verbatim direct-SkeletonJson mechanism) AND § "Common Pitfalls" Pitfall 7 (do NOT call the gated loadSkeleton — it still rejects 4.3 in Phase 42) AND § Assumptions A2 + § Open Questions Q3 (the beta-vs-stable parse risk + the hand-authored-stable-4.3 fallback)
    - 42-REPLAN-NOTE.md §3 (the locked Phase-42 typecheck contract: CI asserts `typecheck:node` only; the `typecheck:web` spine-player leak is Phase-47-OWNED) AND §1 (COMMIT B `cc5783f` frozen — detect-and-skip in 42-02; do NOT rebase)
    - 42-CONTEXT.md D-13 (dual-install integrity only, NOT sampled-value correctness; value correctness is Phase 44's oracle)
    - 42-PATTERNS.md Topic E `d13-43-load-smoke.spec.ts` section (the typed-error-asserted-absent idiom)
    - src/core/loader.ts:122-188 (checkSpineVersion `major>=5||(major===4&&minor>=3)` throw + checkSpine43Schema) + src/core/errors.ts (SpineVersionUnsupportedError; `.name`) — note loader.ts's import specifier is `spine-core-42` post-42-02 (behavior-neutral; the guard logic is byte-unchanged)
    - tests/main/sampler-worker-girl.spec.ts (real-module no-mock idiom) + tests/main/repack.loose-parity.spec.ts:42-48 (REPO_ROOT)
    - The in-repo 4.3 fixture: fixtures/SPINE_4_3_TEST/SPINE_4_3_TEST.json (`4.3.91-beta`, has root.constraints) + its sibling .atlas
  </read_first>
  <action>
Create `tests/runtime/d13-43-load-smoke.spec.ts` (RESEARCH §CI Architecture D-13 + Pitfall 7 — the bypass is CRITICAL):
- `import { SkeletonJson, AtlasAttachmentLoader, TextureAtlas } from '@esotericsoftware/spine-core';` (this resolves to 4.3.0 — the canonical package; this NEW test legitimately imports the bare 4.3 specifier, which is exactly the canonical 4.3.0 — it is NOT a repoint target). Confirm the exact 4.3.0 export names at execution time (TextureAtlas/AtlasAttachmentLoader/SkeletonJson — adjust if 4.3.0 renamed any; RESEARCH lists these).
- `REPO_ROOT = path.resolve(__dirname, '..', '..')`. Read `fixtures/SPINE_4_3_TEST/SPINE_4_3_TEST.json` (text → `JSON.parse`) and its sibling `.atlas` text.
- Construct a 4.3 `SkeletonJson(new AtlasAttachmentLoader(new TextureAtlas(<atlas text>)))` and call `readSkeletonData(parsedJson)` — i.e. drive the 4.3 runtime DIRECTLY. **Do NOT call `loadSkeleton` from `src/core/loader.ts`** — Phase 42 does NOT modify loader.ts logic; the gated `loadSkeleton` STILL throws `SpineVersionUnsupportedError` on this fixture (verified RESEARCH; Pitfall 7). The whole point of D-13 is that the 4.3 *runtime* can read it even though our *gated loader* still rejects it (the loader flip is Phase 44/45).
- ASSERT: `readSkeletonData` does NOT throw `SpineVersionUnsupportedError` (it never hits the gated loader — assert by `expect(() => ...).not.toThrow()` and, if it does throw, assert the thrown error's `.name` is NOT `'SpineVersionUnsupportedError'`).
- ASSERT: the parsed `skeletonData` is truthy and the 4.3 `constraints[]` array was consumed (e.g. `skeletonData` has the expected bones/skins — integrity, NOT value correctness; D-13 explicitly does NOT assert sampled peak values, that is Phase 44's 1e-4 oracle).
- **Q3 fallback contingency (implement as a real guarded branch, not just a comment):** wrap the beta-fixture parse in a try; if `readSkeletonData` THROWS for a beta-vs-stable schema reason (NOT a `SpineVersionUnsupportedError` — a genuine 4.3.0 `SkeletonJson` parse failure on the `4.3.91-beta` shape, RESEARCH §A2), then create `fixtures/SPINE_4_3_MIN/SPINE_4_3_MIN.json` — a tiny hand-authored STABLE 4.3 skeleton (`{"skeleton":{"spine":"4.3.0",...},"constraints":[],"bones":[{"name":"root"}],"slots":[{"name":"s","bone":"root","attachment":"r"}],"skins":[{"name":"default","attachments":{"s":{"r":{"type":"region","width":1,"height":1}}}}],"animations":{}}` + a 1-region `.atlas`) and run the same direct-`SkeletonJson` assertion against THAT instead. Record in the SUMMARY which path executed (beta parsed clean = no fallback fixture committed; beta failed = `SPINE_4_3_MIN` committed). This makes the LOW-confidence beta risk a handled contingency, not a latent failure.

Commit. (This commit is part of COMMIT D's wave; the ancestry invariant only constrains the *alias* commit (the frozen `cc5783f`) vs the *baseline* commit (`1b5327d`) — D-13/CI artifacts are post-alias by construction.)
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/d13-43-load-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/runtime/d13-43-load-smoke.spec.ts` exits 0
    - `grep -c "SkeletonJson" tests/runtime/d13-43-load-smoke.spec.ts` ≥ 1 AND `grep -c "loadSkeleton" tests/runtime/d13-43-load-smoke.spec.ts` == 0 (drives the 4.3 runtime DIRECTLY — never calls the gated Phase-42 loader; Pitfall 7)
    - `grep -cE "not\.toThrow|SpineVersionUnsupportedError" tests/runtime/d13-43-load-smoke.spec.ts` ≥ 1 (asserts the v1.4 reject is NOT hit)
    - `grep -cE "SPINE_4_3_MIN|4\.3\.91-beta|fallback" tests/runtime/d13-43-load-smoke.spec.ts` ≥ 1 (the Q3 beta→stable fallback contingency is implemented as a real branch)
    - The SUMMARY records which path executed (beta parsed clean, OR `fixtures/SPINE_4_3_MIN/` committed); if committed, `git ls-files fixtures/SPINE_4_3_MIN/SPINE_4_3_MIN.json` returns the path
    - `git diff --name-only` for COMMIT D shows NO change to `src/core/loader.ts` (Phase 42 does NOT modify the loader — D-13; 42-02's earlier specifier rename is behavior-neutral and not part of COMMIT D)
    - `npx vitest run` green — full suite (excluding the documented pre-existing local-only gitignored-rig skips in deferred-items.md §1–2), including `tests/safe01/safe01-baseline.spec.ts` byte-equal + `safe01-freeze-guard.spec.ts` ancestry hard-passing
  </acceptance_criteria>
  <done>The D-13 4.3 load-smoke proves the 4.3 runtime parses an in-repo 4.3 JSON directly past the v1.4 reject (integrity only, value correctness deferred to Phase 44); the beta-vs-stable risk is a handled fallback branch; loader.ts logic is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-gate marker + Phase-44 owner-fixture-absence guard (Q2 RESOLVED: committed CURRENT_PHASE constant)</name>
  <files>tests/safe01/phase-gate.ts, tests/safe01/phase44-fixture-guard.spec.ts</files>
  <read_first>
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "CI Architecture → Phase-44 owner-fixture-absence guard" + § "Open Questions Q2" + § "Assumptions A4" (the committed-constant recommendation over STATE.md parsing — LOCK this)
    - 42-CONTEXT.md D-13 ("Add a guard that FAILS CI if the owner oracle/slider fixtures are still absent by Phase 44 — the owner blocker cannot silently slip past its scheduled boundary") and the "Claude's Discretion → fixture directory naming" (must NOT collide with fixtures/SPINE_4_3_TEST/ or fixtures/test_4.3/; suggested SIMPLE_PROJECT_*-style sibling + fixtures/SLIDER_4_3/)
    - 42-PATTERNS.md Topic F `phase44-fixture-guard.spec.ts` + `phase-gate.ts` section + Shared S3
    - tests/main/sampler-worker-girl.spec.ts:23-28 (the `it.skipIf` precedent)
  </read_first>
  <action>
**Lock Q2 (the planner decision the orchestrator required):** Use a committed constant marker, NOT `.planning/STATE.md` parsing (RESEARCH §Q2/§A4 recommendation — robust, explicit, no STATE.md format-drift risk; [[feedback_explicit_identity_over_inference]] in spirit: thread the phase explicitly, do not infer it from a brittle parse).

Create `tests/safe01/phase-gate.ts`:
```typescript
/** v1.6 milestone phase marker. Bumped by the roadmapper/owner as the milestone
 *  advances (42→43→44…). The Phase-44 owner-fixture-absence guard (D-13) reads
 *  this: while CURRENT_PHASE < 44 the guard is skip-with-reason; at >= 44 it
 *  HARD-FAILS if the owner ORCL-01/SLIDER-01 fixtures are still absent — so the
 *  owner blocker cannot silently slip past its scheduled boundary.
 *  Q2 RESOLVED (RESEARCH §Open Questions): a committed constant, NOT STATE.md
 *  parsing (no format-drift risk). */
export const CURRENT_PHASE = 42 as const;
```

Create `tests/safe01/phase44-fixture-guard.spec.ts` (RESEARCH §CI Architecture Phase-44 guard; the `it.skipIf` precedent):
- `import { CURRENT_PHASE } from './phase-gate.js';`
- Define the expected owner-fixture directory paths using the confirmed naming convention (lock it here; must NOT collide with the existing Phase-32 `fixtures/SPINE_4_3_TEST/`/`fixtures/test_4.3/`). Use: `ORCL_01_DIR = fixtures/SIMPLE_PROJECT_43/` (the SIMPLE_PROJECT_*-style sibling for the ORCL-01 4.3+4.2 oracle rig — distinct from `SIMPLE_PROJECT/`) and `SLIDER_01_DIR = fixtures/SLIDER_4_3/` (RESEARCH's suggested slider analog). Record these chosen names in the SUMMARY and in `42-OWNER-EXPORT-SPEC.md` (Task 3) so the owner exports into exactly these dirs.
- `it.skipIf(CURRENT_PHASE < 44)('Phase 44+: owner ORCL-01 + SLIDER-01 4.3 fixtures MUST exist (D-13 — owner blocker cannot slip past Phase 44)', () => { expect(existsSync(ORCL_01_DIR), 'ORCL-01 4.3 oracle rig missing — owner export overdue (see 42-OWNER-EXPORT-SPEC.md)').toBe(true); expect(existsSync(SLIDER_01_DIR), 'SLIDER-01 4.3 rig missing — owner export overdue').toBe(true); })`.
- While `CURRENT_PHASE === 42` this test SKIPS with the reason recorded (vacuously satisfied — the owner exports in parallel, off the critical path; D-01). When the roadmapper bumps `CURRENT_PHASE` to 44 it flips to a hard failure if the dirs are still absent.

Commit.
  </action>
  <verify>
    <automated>npx vitest run tests/safe01/phase44-fixture-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/safe01/phase44-fixture-guard.spec.ts` exits 0 with the test reported as SKIPPED (because `CURRENT_PHASE === 42 < 44` — the owner-export is off the critical path by design)
    - `grep -cE "export const CURRENT_PHASE = 42" tests/safe01/phase-gate.ts` ≥ 1 (the committed-constant marker — Q2 locked)
    - `grep -c "STATE.md" tests/safe01/phase-gate.ts tests/safe01/phase44-fixture-guard.spec.ts` == 0 (does NOT parse STATE.md — the rejected brittle approach)
    - `grep -cE "it\.skipIf\(CURRENT_PHASE < 44\)" tests/safe01/phase44-fixture-guard.spec.ts` ≥ 1
    - `grep -cE "SIMPLE_PROJECT_43|SLIDER_4_3" tests/safe01/phase44-fixture-guard.spec.ts` ≥ 1 (the locked owner-fixture dir names; non-colliding with SPINE_4_3_TEST/test_4.3) AND `grep -c "SPINE_4_3_TEST" tests/safe01/phase44-fixture-guard.spec.ts` == 0 (does NOT reuse the Phase-32 reject fixture as the oracle)
    - `npx vitest run` green (the skipped guard does not fail the suite; SAFE-01 byte-equal + ancestry still green; excluding the documented pre-existing local-only gitignored-rig skips in deferred-items.md §1–2)
  </acceptance_criteria>
  <done>The Phase-44 guard uses a committed `CURRENT_PHASE` constant (Q2 locked — no STATE.md parsing); it skips-with-reason while in Phase 42 and will hard-fail at ≥44 if the owner ORCL-01/SLIDER-01 fixtures (in the locked non-colliding `SIMPLE_PROJECT_43/` + `SLIDER_4_3/` dirs) are still absent — the owner blocker cannot silently slip past Phase 44.</done>
</task>

<task type="auto">
  <name>Task 3: ci.yml dual-runtime gate (release.yml untouched) + the committed 42-OWNER-EXPORT-SPEC.md handoff</name>
  <files>.github/workflows/ci.yml, .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-OWNER-EXPORT-SPEC.md</files>
  <read_first>
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "CI Architecture (CI-01, D-10/D-11/D-12/D-13 — RESOLVED)" (the verbatim `on:`/`paths-ignore`/`test`/`bundle-smoke`/`fetch-depth: 0` blocks) AND § "Common Pitfalls" Pitfall 5 (shallow-clone breaks ancestry) + Pitfall 8 (production-bundle smoke rationale) + § "Production-Bundle Smoke"
    - 42-REPLAN-NOTE.md §3 ("42-04 (amend)" — the CI-01 gate asserts `npm run typecheck:node` NOT full `npm run typecheck`; CI's fresh `npm ci` clone makes gitignored scratch naturally absent so NO scratch-exclusion shim is needed in CI; D-13/git-ancestry/3-OS matrix/Phase-44 guard/42-OWNER-EXPORT-SPEC.md UNCHANGED; the 4-commit ordering and `typecheck:node`-green invariant are mutually consistent) AND §1 (COMMIT B `cc5783f` frozen) AND §4 (rejected Options 2/3 off the table)
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/deferred-items.md §3 (the one pre-existing TS6133 — 42-02's repoint commit folded it; the CI `typecheck:node` step asserts a clean exit 0, no modulo-baseline hedging)
    - 42-CONTEXT.md D-10 (new ci.yml, release.yml untouched), D-11 (triggers + paths-ignore; NEVER tags), D-12 (lane split: 3-OS test every push, bundle-smoke PR→main only), D-13 (4.3 CI arm integrity-only + the Phase-44 guard), and D-01..D-05 (the owner export handoff scope)
    - package.json `scripts` (`typecheck:node` = `tsc --noEmit -p tsconfig.node.json`; `typecheck` = `typecheck:node && typecheck:web` — CI runs `typecheck:node` ONLY) + tsconfig.node.json `include` (covers `tests/**/*.ts` + `scripts/**/*.ts`; the `scripts/**/*.ts` glob is why a non-fresh tree leaks scratch — but CI's `npm ci` clone has no scratch, so NO shim)
    - .gitignore lines 73/74/85 (`scripts/probe-*.ts`, `scripts/diagnose-*.ts`, `tests/_trace_tmp/`) — naturally absent on a fresh `npm ci` clone; CI does NOT add a shim for them
    - 42-PATTERNS.md Topic G (release.yml `test` job analog + the exact pinned action SHAs + the `fetch-depth: 0` note + "release.yml gets ZERO edits") + Topic H (the owner-handoff doc structure) + Shared S5
    - .github/workflows/release.yml (read in FULL — mirror the `test` job matrix/runner/pinned-SHA shape; this file MUST stay byte-untouched; copy the exact `actions/checkout`/`actions/setup-node` pinned SHAs from here)
    - tests/safe01/phase44-fixture-guard.spec.ts (Task 2 — the locked owner-fixture dir names `SIMPLE_PROJECT_43/` + `SLIDER_4_3/` to reference in the owner spec)
    - https://github.com/EsotericSoftware/spine-editor/issues/891 (reference in the owner spec so the owner understands WHY ORCL-01 is non-IK — D-03)
  </read_first>
  <action>
Create `.github/workflows/ci.yml` — copy the RESEARCH §CI Architecture RESOLVED blocks verbatim; mirror `release.yml`'s `test`-job matrix/runner/pinned-SHA shape (read release.yml in full first; copy its exact pinned `actions/checkout`/`actions/setup-node` SHAs):
- **`on:`** — `push: branches: ['**'] paths-ignore: ['.planning/**','**/*.md','docs/**','LICENSE','.gitignore']`; `pull_request: branches: [main] paths-ignore: ['.planning/**','**/*.md','docs/**','LICENSE']`; `workflow_dispatch:`. **NO `tags:` anywhere** (release.yml owns tags; the auto-update path stays isolated — [[feedback_dont_push_release_tags]]). (GitHub `paths-ignore` semantics: a push is skipped only if EVERY changed file matches an ignore glob — a commit touching `src/**` too still runs. This is exactly D-11's "a commit that also touches code still runs".)
- **`test` job** — `strategy: { fail-fast: true, matrix: { os: [ubuntu-latest, windows-2022, macos-14] } }` (mirror release.yml EXACTLY — Linux retained in CI test though dropped as a release target per [[project_linux_deferred]]); `runs-on: ${{ matrix.os }}`. Steps: `actions/checkout@<release.yml's pinned SHA>` **`with: { fetch-depth: 0 }`** (CRITICAL — the D-09 git-ancestry check needs full history; default shallow depth-1 breaks it in CI while it works locally — research Pitfall 5), `actions/setup-node@<release.yml's pinned SHA>` `with: { node-version: 22, cache: 'npm' }`, `run: npm ci` (lockfile-frozen, integrity-verified — never `npm install`), `run: npm run typecheck:node` (this step is the BARE `npm run typecheck:node` — the dual-runtime gate scope, `src/main`+`src/core`+`tests`; it asserts a clean exit 0 with NO "modulo pre-existing baseline"/allowlist/scratch-exclusion wrapper anywhere. A fresh `npm ci` clone has the gitignored scratch — `scripts/probe-*.ts`, `scripts/diagnose-*.ts`, `tests/_trace_tmp/` — NATURALLY ABSENT, so `typecheck:node` is genuinely exit 0 on CI; do NOT add any scratch-exclusion shim. Do NOT run full `npm run typecheck` — `typecheck:web` is the Phase-47-OWNED spine-player `Player.d.ts` 4.3-leak, NOT a Phase-42 defect; it is satisfiable end-to-end because 42-02's repoint commit eliminated ALL bare `@esotericsoftware/spine-core` orphans in `typecheck:node` scope (src/ + tests/) AND folded the locked one-token `tests/main/image-worker-rotation.spec.ts:190` TS6133 fix per deferred-items.md §3 + the locked reconciliation — the contract is identical to 42-02's and 42-03's), `run: npm run test`, then an explicit named step `Assert SAFE-01 baseline predates the npm-alias commit (D-09)` running the bare `git merge-base --is-ancestor "$(git log --diff-filter=A --format=%H -- tests/safe01/baselines/_manifest.json | tail -1)" "$(git log -S 'spine-core-42' --format=%H -- package.json | tail -1)"` (visible-in-log belt-and-suspenders; D-09 — `git log -S 'spine-core-42' -- package.json` resolves to the frozen COMMIT B `cc5783f`; the vitest freeze-guard ALSO enforces this inside `npm run test`, intentionally redundant per "machine-checked, not reviewer memory").
- **`bundle-smoke` job** — `if: github.event_name == 'pull_request'`, `needs: test`, `runs-on: ubuntu-latest`. Steps: checkout `fetch-depth: 0`, setup-node 22 cache npm, `npm ci`, `npm run build` (electron-vite build + electron-builder), then a step asserting BOTH `node_modules/@esotericsoftware/spine-core` (4.3.0) AND `node_modules/spine-core-42` (4.2.111) are present in the packed app, and a step running the BUILT `out/main/sampler-worker.cjs` (NOT `src/` — confirm the exact built worker path against `electron.vite.config`/`package.json` build output at execution time; RESEARCH names `out/main/sampler-worker.cjs`) against a committed 4.2 fixture (e.g. `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`) AND the in-repo 4.3 JSON (the D-13 fixture, or `SPINE_4_3_MIN` if Task 1 took the fallback) — proving tree-shaking/packaging did NOT drop the dynamically-dispatched runtime (Pitfall 8). PR→main only (pay the slow packaging cost at merge — D-12).
- **Every `uses:` pinned to a full commit SHA** (mirror release.yml's convention — copy its exact SHAs; supply-chain hygiene for the new workflow, RESEARCH §Security Domain V14).
- **Verify `release.yml` is byte-untouched:** `git diff --name-only` for COMMIT D MUST NOT list `.github/workflows/release.yml`.

Create `.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-OWNER-EXPORT-SPEC.md` — a self-contained one-Spine-session handoff (CONTEXT D-01..D-05 + D-13; PATTERNS.md Topic H; NO code). It MUST contain, unambiguously:
- **Purpose + timing (D-01):** the owner exports these in parallel, off the critical path, the moment Phase 42 lands; the hard blocker never sits on the critical path.
- **All 5 export artifacts in ONE Spine session (D-02), 4 rigs:**
  1. **ORCL-01** — a `SIMPLE_TEST`-equivalent rig, **non-IK by design / `TransformConstraint`-only** (D-03 — mirror today's `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` whose `TransformConstraint`-on-`SQUARE` is the `appliedPose` correctness canary), exported as **BOTH "Version 4.3" AND "Version 4.2"** → 2 artifacts. Explain WHY non-IK: it sidesteps spine-editor#891 (the 4.3→4.2 downgrade IK-scramble) entirely — link `https://github.com/EsotericSoftware/spine-editor/issues/891`. Export into `fixtures/SIMPLE_PROJECT_43/` (the locked non-colliding dir from Task 2 — NOT `SPINE_4_3_TEST/`/`test_4.3/`).
  2. **SLIDER-01** — a minimal slider rig: one slider drives one bone's X over a known time window. **Specify the EXACT analytically-derivable parameters here** (bone name, slider min/max, the keyed time window, the resulting closed-form peak X — so Phase 46's closed-form oracle has ground truth). Export into `fixtures/SLIDER_4_3/`.
  3. **XTRA-01** — a 4.3 transform-constraint multi-map rig: one source property → multiple differently-typed target properties, local↔world with clamp.
  4. **XTRA-02** — a 4.3 IK `scaleYMode` rig exercising `Uniform` + `Volume`; note default `None` is confirmed 4.2-equivalent.
- **Artifact set per rig (D-04):** atlas-source only — `.json` + `.atlas` + page PNG(s). NO atlas-less variant (math is loaderMode-invariant by construction — [[project_strict_loadermode_separation]]).
- **Redistributability (D-05):** state explicitly the rigs MUST be the owner's own assets, committed in-repo and redistributable — do NOT export a licensed/third-party rig.
- **Dir-naming table:** ORCL-01 → `fixtures/SIMPLE_PROJECT_43/` (both 4.3 + 4.2 JSON inside), SLIDER-01 → `fixtures/SLIDER_4_3/`, XTRA-01/XTRA-02 → chosen non-colliding sibling dirs (state them; must not collide with `SPINE_4_3_TEST/`/`test_4.3/`).
- A note that the Phase-44 guard (`tests/safe01/phase44-fixture-guard.spec.ts`) will HARD-FAIL CI at Phase 44 if `SIMPLE_PROJECT_43/` + `SLIDER_4_3/` are still absent — so this export is due before the milestone reaches Phase 44.

Commit COMMIT D with a plain-English message stating it is the CI gate + owner handoff, post-scaffolding, and that `release.yml` was deliberately untouched.
  </action>
  <verify>
    <automated>npx vitest run tests/safe01 tests/runtime tests/arch.spec.ts && npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/" | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists; `grep -cE "^\s*tags:" .github/workflows/ci.yml` == 0 (NEVER triggers on tags — release.yml owns those)
    - `grep -c "fetch-depth: 0" .github/workflows/ci.yml` ≥ 2 (both the test job and bundle-smoke job set full history — required for the D-09 ancestry check; Pitfall 5)
    - `grep -cE "merge-base.*--is-ancestor" .github/workflows/ci.yml` ≥ 1 (the explicit visible-in-log D-09 step) AND `grep -c "spine-core-42" .github/workflows/ci.yml` ≥ 1
    - `grep -cE "github.event_name == 'pull_request'" .github/workflows/ci.yml` ≥ 1 (bundle-smoke is PR→main only — D-12) AND `grep -cE "ubuntu-latest|windows-2022|macos-14" .github/workflows/ci.yml` ≥ 3 (the 3-OS matrix mirrors release.yml)
    - `grep -cE "npm run typecheck:node" .github/workflows/ci.yml` ≥ 1 AND `grep -cE "npm run typecheck($|[^:])" .github/workflows/ci.yml` == 0 (the CI typecheck step is the BARE `npm run typecheck:node` — the dual-runtime gate scope; full `npm run typecheck` is NOT invoked, because `typecheck:web` is the Phase-47-OWNED spine-player `Player.d.ts` leak) AND `grep -ciE "modulo|pre-existing baseline|allowlist|known.error|probe-\*|diagnose-\*|_trace_tmp" .github/workflows/ci.yml` == 0 (NO baseline-hedging AND NO scratch-exclusion shim anywhere — a fresh `npm ci` clone has the gitignored scratch naturally absent, so the bare `npm run typecheck:node` is genuinely exit 0 on CI with nothing to exclude)
    - `grep -cE "paths-ignore" .github/workflows/ci.yml` ≥ 1 AND the list includes `.planning/**` and `**/*.md` (docs-only commits stay quiet — D-11)
    - `grep -cE "uses: actions/(checkout|setup-node)@[0-9a-f]{40}" .github/workflows/ci.yml` ≥ 2 (every action pinned to a full commit SHA — copied from release.yml)
    - `git diff --name-only` for COMMIT D does NOT list `.github/workflows/release.yml` (release.yml byte-untouched — D-10; the tag/auto-update guard is never perturbed)
    - `42-OWNER-EXPORT-SPEC.md` exists and contains all of: `891` (the spine-editor#891 reference), `non-IK` (or `TransformConstraint`-only ORCL-01 design — D-03), `Version 4.3` AND `Version 4.2` (the dual export — D-02), `atlas-source` (D-04), `redistributable` (D-05), `SIMPLE_PROJECT_43` AND `SLIDER_4_3` (the locked non-colliding dir names matching Task 2's guard)
    - `npx vitest run tests/safe01 tests/runtime tests/arch.spec.ts` exits 0 AND `npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/" | wc -l` is `0` (zero git-tracked `typecheck:node` errors — clean on a fresh-clone-equivalent tree; 42-02's repoint + TS6133 fix made this satisfiable end-to-end; full `npm run typecheck` is NOT this plan's gate) AND `npx vitest run` green (the full Phase-42 surface is green excluding the documented pre-existing local-only gitignored-rig skips in deferred-items.md §1–2; SAFE-01 byte-equal + ancestry hard-passing)
  </acceptance_criteria>
  <done>`ci.yml` runs the 3-OS dual-runtime gate (test matrix + the BARE `npm run typecheck:node` asserting a clean exit 0 with no modulo-baseline hedging and no scratch-exclusion shim — fresh `npm ci` clone makes the gitignored scratch naturally absent; `typecheck:web` is the Phase-47-OWNED spine-player leak, NOT asserted + explicit git-ancestry step with `fetch-depth: 0`) on every code push and the PR-only production-bundle smoke; `release.yml` is byte-untouched (tag/auto-update path isolated); `42-OWNER-EXPORT-SPEC.md` is committed and self-contained enough for the owner to export all 5 artifacts (4 rigs) in one session into the locked non-colliding dirs, off the critical path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow supply chain | A new GitHub Actions workflow runs with repo credentials; unpinned actions are a supply-chain surface |
| committed git history → CI ancestry gate | The D-09 invariant is re-asserted in CI; a shallow clone or a bypassable step would silently void it |
| in-repo 4.3 JSON → 4.3 spine-core SkeletonJson (a second runtime now parses untrusted-shaped JSON) | Input-parsing robustness of the newly-added 4.3 runtime |
| release/auto-update path (release.yml) ↔ the new ci.yml | These MUST stay isolated — a `tags:` trigger on ci.yml would entangle the auto-update delivery guard |
| spine-player's own bare `.d.ts` → renderer typecheck:web | Phase-47-OWNED (NOT a Phase-42 / CI surface): CI asserts `typecheck:node` only; the `typecheck:web` leak originates in `node_modules/` and is out of CI's Phase-42 gate by the locked resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-16 | Tampering | Unpinned `uses:` action SHA in the new `ci.yml` (workflow supply-chain) | mitigate | Every `uses:` pinned to a full commit SHA, copied from `release.yml`'s existing pinned-SHA convention (RESEARCH §Security Domain V14). Acceptance: `grep -cE "uses: actions/(checkout|setup-node)@[0-9a-f]{40}"` ≥ 2 |
| T-42-17 | Tampering | Shallow clone (`actions/checkout` default depth-1) makes the CI git-ancestry gate error or false-pass — works locally, breaks only in CI | mitigate | `fetch-depth: 0` on every job running the ancestry check (research Pitfall 5 — the verified GitHub full-history opt-in). Acceptance: `grep -c "fetch-depth: 0"` ≥ 2. The gate is enforced TWICE (the vitest freeze-guard inside `npm run test` AND the explicit visible CI step) — D-09 belt-and-suspenders |
| T-42-18 | Tampering / EoP | `ci.yml` accidentally triggers on tags → entangles the release/auto-update delivery path (the standing "don't push v1.6 tags" guard) | mitigate | `ci.yml` has NO `tags:` trigger (push branches + PR→main + workflow_dispatch only); `release.yml` byte-untouched. Acceptance: `grep -cE "^\s*tags:" ci.yml` == 0 AND `git diff --name-only` does not list `release.yml` ([[feedback_dont_push_release_tags]]) |
| T-42-19 | Tampering | electron-builder tree-shakes/drops the dynamically-dispatched 4.3 runtime — works in dev, breaks in the packaged app | mitigate | The `bundle-smoke` job asserts BOTH spine-core copies survive packaging and runs the BUILT `out/main/sampler-worker.cjs` (not `src/`) against a 4.2 + a 4.3 fixture (Pitfall 8 — the `feedback_platform_divergent_check_stale_build` class). PR→main only (D-12) |
| T-42-20 | Spoofing / DoS | Malformed/hostile skeleton JSON parsed by the newly-added 4.3 `SkeletonJson` (a second untrusted-input parser) | accept (bounded) | Phase 42's D-13 smoke exercises the 4.3 parser with an IN-REPO fixture only (not arbitrary user input). The existing `loader.ts` version-guard (logic UNCHANGED in Phase 42; 42-02's specifier rename is behavior-neutral) remains the strict parse boundary for the user-facing path; loader DISPATCH to the 4.3 runtime is Phase 44 (with its own guards). Phase 42 does not widen the user-facing parse surface — noted so this plan does not accidentally do so |
| T-42-21 | Tampering | A reviewer-bypassable ordering check (D-09 enforced only by human review) | mitigate | The ancestry gate is machine-checked in TWO independent places: `tests/safe01/safe01-freeze-guard.spec.ts` (run by `npm run test` in the `test` job) AND an explicit `git merge-base --is-ancestor` CI step visible in the log — D-09's "machine-checked, not reviewer memory" |
| T-42-23 | Tampering | The CI typecheck gate hedged ("modulo pre-existing baseline" / an allowlist / a scratch-exclusion shim) → a NEW typecheck regression hides behind the hedge | mitigate | The CI typecheck step is the BARE `npm run typecheck:node` asserting a clean exit 0 — NO modulo/allowlist/known-error/scratch-exclusion wrapper anywhere. This is satisfiable on CI's fresh `npm ci` clone (the gitignored scratch is naturally absent there — no shim needed) ONLY because 42-02's repoint commit eliminated all bare-specifier orphans in `typecheck:node` scope AND folded the one locked pre-existing TS6133 (deferred-items.md §3 + the locked reconciliation); the contract is identical across 42-02/42-03/42-04. Acceptance: `grep -ciE "modulo|pre-existing baseline|allowlist|known.error|probe-\*|diagnose-\*|_trace_tmp" ci.yml` == 0 AND the bare `npm run typecheck:node` exits 0 on the fresh clone — any future regression fails the bare gate with no place to hide |
| T-42-24 (inherited) | Tampering / false-defect | The Phase-47-owned `typecheck:web` spine-player `.d.ts` 4.3-leak is "fixed" in CI/Phase 42 (pre-empting Phase 47 / masking real future drift) OR mis-flagged by a downstream verifier as a Phase-42/CI miss | mitigate (no new high threat) | CI asserts `npm run typecheck:node` ONLY (the locked dual-runtime gate scope); it does NOT run/assert `typecheck:web` and adds NO shim for it. The leak originates in `node_modules/@esotericsoftware/spine-player/dist/Player.d.ts` (not a repo file) and is documented as KNOWN/EXPECTED/Phase-47-OWNED in 42-02 (+ its SUMMARY) and restated here. Acceptance forbids `npm run typecheck` (non-`:node`) in ci.yml. No new dependency/parse/auth surface — a pure scoping correction consistent end-to-end with 42-02/42-03; rejected Options 2/3 are off the table |

ASVS L1 V2/V3/V4 are N/A (no auth/session/access-control). V5 is unchanged (loader logic untouched — Phase 42 does not widen the user-facing parse surface; T-42-20 accepted/bounded; 42-02's specifier rename is behavior-neutral). V6-integrity (T-42-16, lockfile-via-`npm ci`) + V14-configuration (T-42-16/18, pinned SHAs + release.yml isolation) are the genuine surface — fully mitigated. The literal-`typecheck:node`-exit-0 contract (T-42-23) removes the "hedge hides a regression" surface; the `typecheck:web` narrowing introduces no new high threat (T-42-24 — scoping correction, Phase-47-owned, no shim).
</threat_model>

<verification>
- `npx vitest run tests/safe01 tests/runtime tests/arch.spec.ts` — the full Phase-42 test surface green (SAFE-01 byte-equal + enumeration + freeze-guard hard-passing ancestry, RT-01 resolution/distinctness, RT-03 backstop + negative fixture, RT-04 Layer-3, D-13 4.3 load-smoke, Phase-44 guard skipped-with-reason)
- `npm run typecheck:node` exits 0 on a fresh-clone-equivalent tree — `npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/"` returns ZERO lines (the CI step is the bare `npm run typecheck:node` — the dual-runtime gate scope; CI's fresh `npm ci` clone has the gitignored scratch naturally absent so it is genuinely exit 0 there with NO scratch-exclusion shim; satisfiable end-to-end because 42-02's repoint commit eliminated all bare `@esotericsoftware/spine-core` orphans in `typecheck:node` scope AND folded the locked one-token `image-worker-rotation.spec.ts:190` TS6133 fix per deferred-items.md §3 + the locked reconciliation — the contract is identical to 42-02's and 42-03's). Full `npm run typecheck` is NOT run — `typecheck:web` is the Phase-47-OWNED spine-player `Player.d.ts` 4.3-leak (documented in 42-02), NOT a Phase-42 defect; `npm run test` — full suite green (excluding the documented pre-existing local-only gitignored-rig skips, deferred-items.md §1–2)
- `ci.yml` static checks: no `tags:` trigger; `fetch-depth: 0` ≥ 2; explicit `merge-base --is-ancestor` step; the typecheck step is the bare `npm run typecheck:node` with NO modulo/allowlist/known-error/scratch-exclusion hedging and NO full `npm run typecheck`; `bundle-smoke` gated `if: github.event_name == 'pull_request'`; 3-OS matrix; pinned action SHAs; `paths-ignore` includes `.planning/**` + `**/*.md`
- `git diff --name-only` for COMMIT D does NOT include `.github/workflows/release.yml` (byte-untouched)
- `42-OWNER-EXPORT-SPEC.md` self-contained: all 4 rigs / 5 artifacts, D-03 non-IK rationale + #891 link, D-04 atlas-source, D-05 redistributable, the locked non-colliding dir names matching the Phase-44 guard
- COMMIT D is a git descendant of COMMIT C→42-02-repoint→ frozen B (`cc5783f`)→A (`1b5327d`); the ancestry chain is intact and CI-enforced; the 4-commit ordering A→B→repoint→C→D and the CI `typecheck:node`-green-on-fresh-clone invariant are mutually consistent end-to-end
</verification>

<success_criteria>
- CI-01 satisfied: a new `ci.yml` runs the full suite + SAFE-01 gate + the BARE `npm run typecheck:node` (asserting a clean exit 0 — no modulo-baseline hedging, no scratch-exclusion shim; CI's fresh `npm ci` clone makes the gitignored scratch naturally absent; satisfiable because 42-02's repoint + the locked TS6133 fix landed first; full `npm run typecheck` is NOT run — `typecheck:web` is the Phase-47-OWNED spine-player leak) + the explicit git-ancestry assertion on a 3-OS matrix from a fresh `npm ci` on every code push, and the electron-builder production-bundle smoke (both spine-core copies packaged; built worker vs a 4.2 + a 4.3 fixture) on PR→main; `release.yml` byte-untouched; never triggers on tags; docs-only commits skipped
- D-13 fully realized: the 4.3 runtime parses an in-repo 4.3 JSON directly past the v1.4 reject (integrity only — value correctness deferred to the Phase 44 oracle), with the beta→stable fallback handled; loader.ts logic unchanged
- The Phase-44 owner-fixture-absence guard uses a committed `CURRENT_PHASE` constant (Q2 LOCKED — no STATE.md parsing) and will hard-fail at ≥44 if the owner ORCL-01/SLIDER-01 fixtures are absent — the owner blocker cannot silently slip past its boundary
- `42-OWNER-EXPORT-SPEC.md` is committed the moment Phase 42 lands so the owner exports all 5 artifacts (4 rigs) in one Spine session, off the critical path (D-01..D-05); ORCL-01 is non-IK by design (sidesteps spine-editor#891 → ORCL-03's #891 human-verify gate is a v1.6 no-op)
- The SAFE-01 baseline is still byte-equal and the ancestry assertion still hard-passing — Phase 42 delivered all four interlocking artifacts with 4.2 behavior provably frozen; the typecheck contract is a clean `npm run typecheck:node` exit 0 on a fresh clone end-to-end (42-02 → 42-03 → 42-04 all assert the same `typecheck:node` thing, no hedging anywhere), and the 4-commit ordering A→B→repoint→C→D is mutually consistent with that invariant — the whole point of the re-plan
</success_criteria>

<output>
After completion, create `.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-04-SUMMARY.md`. Record: the COMMIT D SHA, explicit confirmation `release.yml` was byte-untouched (`git diff` evidence), which D-13 path executed (beta parsed clean OR `SPINE_4_3_MIN` fallback committed), the locked owner-fixture dir names, that the Phase-44 guard skipped-with-reason as designed, explicit confirmation the CI typecheck step is the BARE `npm run typecheck:node` (NOT full `npm run typecheck`) asserting a clean exit 0 with NO modulo-baseline hedging AND NO scratch-exclusion shim (and that this is satisfiable because CI's fresh `npm ci` clone has the gitignored scratch naturally absent, and because 42-02's repoint + the locked TS6133 fix per deferred-items.md §3 landed first), a one-line restatement that the `typecheck:web` spine-player `Player.d.ts` 4.3-leak is the KNOWN/EXPECTED/Phase-47-OWNED known-item (documented in 42-02), NOT a Phase-42 defect, and a one-line note that all four Phase-42 deliverables (SAFE-01, RT-01, RT-03/RT-04, CI-01) are green with the SAFE-01 baseline byte-equal and the A→B→repoint→C→D ancestry chain intact + CI-enforced + mutually consistent with the `typecheck:node`-green-on-fresh-clone invariant.
</output>
