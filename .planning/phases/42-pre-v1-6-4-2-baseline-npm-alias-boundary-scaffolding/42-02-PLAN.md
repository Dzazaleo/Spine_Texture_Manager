---
phase: 42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - package.json
  - package-lock.json
  - "src/** and tests/** bare @esotericsoftware/spine-core import sites (Task 2 consumer-repoint — exact set discovered by a FRESH count-free grep at execute time)"
  - "tests/main/image-worker-rotation.spec.ts (Task 2 — the locked one-token TS6133 reconciliation deviation)"
autonomous: true
requirements: [RT-01, SAFE-01]
must_haves:
  truths:
    - "COMMIT B (`cc5783f` — `@esotericsoftware/spine-core@4.3.0` canonical + `spine-core-42` = exact `npm:@esotericsoftware/spine-core@4.2.111`; spine-player untouched at 4.2.111) is ALREADY LANDED and FROZEN; the re-executor DETECTS it (`git cat-file -e cc5783f`) and SKIPS Task 1 entirely — no re-pin, no re-commit, no reorder"
    - "`git merge-base --is-ancestor 1b5327d cc5783f` exits 0 — COMMIT A is already a git ancestor of the frozen COMMIT B (ancestry satisfied at the frozen anchors, not re-derived)"
    - "Every pre-existing BARE `@esotericsoftware/spine-core` import site (in `src/` AND `tests/`, discovered by a FRESH count-free grep at execute time — `grep -rlE \"['\\\"]@esotericsoftware/spine-core['\\\"]\" src/ tests/`; NO hand-enumerated list or count is carried forward even if a note hands one) is mechanically repointed to `spine-core-42` in a SEPARATE commit immediately after COMMIT B — ZERO logic change (only the import-specifier string moves), identical 4.2.111 runtime"
    - "SAFE-01 byte-equality RE-VERIFIES GREEN after the repoint (`npx vitest run tests/safe01` exits 0) — only the module NAME moved, the runtime is the SAME 4.2.111 the bare specifier resolved to before; the SAFE-01 frozen baseline committed by 42-01 is untouched"
    - "`npm run typecheck:node` exits 0 on a fresh-clone-equivalent tree after the repoint commit — i.e. ignoring untracked gitignored scratch (`scripts/probe-*.ts` .gitignore:73, `scripts/diagnose-*.ts` .gitignore:74, `tests/_trace_tmp/` .gitignore:85) which does NOT exist on a fresh clone / CI `npm ci` checkout; the repoint eliminated ALL bare-specifier orphans in `typecheck:node` scope AND folded the locked one-token `tests/main/image-worker-rotation.spec.ts:190` TS6133 reconciliation (deferred-items.md §3)"
    - "The `typecheck:web` / `src/renderer/src/modals/AnimationPlayerModal.tsx` / spine-player-`Player.d.ts` 4.3-leak (~22 errors) is a KNOWN, EXPECTED, Phase-47-OWNED consequence of the locked 4.3-canonical direction — explicitly documented here and surfaced in 42-02-SUMMARY.md so the downstream verifier does NOT flag it as a Phase-42 miss; it is NOT a Phase-42 defect and is NOT fixed in Phase 42"
    - "`@esotericsoftware/spine-player` is STILL 4.2.111 (NOT bumped — that is Phase 47; the alias DIRECTION makes Phase 47 a clean drop-in)"
  artifacts:
    - path: "package.json"
      provides: "ALREADY at the COMMIT-B state (frozen): spine-core→4.3.0 canonical + spine-core-42 exact-pinned npm alias; spine-player unchanged at 4.2.111 — detect-and-skip, do NOT regenerate"
      contains: "spine-core-42"
    - path: "package-lock.json"
      provides: "ALREADY at the COMMIT-B state (frozen): node_modules/spine-core-42 entry (name @esotericsoftware/spine-core, version 4.2.111, integrity sha512) — detect-and-skip"
      contains: "spine-core-42"
  key_links:
    - from: "every pre-existing bare @esotericsoftware/spine-core import site (src/ + tests/, count-free fresh grep)"
      to: "spine-core-42 (the exact-pinned 4.2.111 alias, already frozen in COMMIT B)"
      via: "mechanical specifier-token rename on import/from lines (value AND `import type`), discrete commit immediately after COMMIT B; SAFE-01 re-verify gate"
      pattern: "spine-core-42"
    - from: "COMMIT A (`1b5327d`, SAFE-01 baseline) → COMMIT B (`cc5783f`, alias) → repoint commit"
      to: "the A→B→repoint→C→D linear ordering"
      via: "git merge-base --is-ancestor (A→B satisfied at frozen anchors); repoint commit is a descendant of the frozen COMMIT B"
      pattern: "merge-base"
---

<objective>
RT-01's dual-install is **already landed and frozen as COMMIT B (`cc5783f`)**: `@esotericsoftware/spine-core@4.3.0` is the **canonical** package and `4.2.111` is installed side-by-side via the exact-pinned, lockfile-committed npm alias `spine-core-42` (orchestrator-verified pure: 2 files only; `git merge-base --is-ancestor 1b5327d cc5783f` → TRUE; spine-player untouched at 4.2.111). **Task 1 is DONE + FROZEN — the re-executor MUST detect COMMIT B exists (`git cat-file -e cc5783f`) and SKIP it: no re-pin, no re-commit, no reorder (research Pitfall 1 — amending/rebasing/reordering an ancestry commit is ~100× recovery cost and applies to BOTH COMMIT A and COMMIT B).**

The remaining Phase-42 work in this plan: in a **separate commit immediately after COMMIT B (same wave)**, mechanically repoint every pre-existing BARE `@esotericsoftware/spine-core` consumer (src/ + tests/) onto the `spine-core-42` alias — discovered by a **count-free fresh grep at execute time** — so the 4.3 flip does not orphan the 4.2-written code. ZERO logic change (only the import-specifier string moves); SAFE-01 re-verified byte-equal.

Purpose: RT-01 is the single highest-blast-radius change in the milestone. The alias **direction** is load-bearing and counter-intuitive: 4.3 is canonical (not 4.2) so that `spine-player` / `spine-webgl`'s bare `export * from "@esotericsoftware/spine-core"` resolves correctly when Phase 47 bumps the player — inverting the direction silently corrupts spine-player into a split-brain (research Pitfall 3). "4.3 canonical" *definitionally* means no legacy code may resolve spine-core bare-as-4.2 — the moment the bare specifier flipped to 4.3 (COMMIT B), every pre-existing bare consumer compiles against 4.3's Pose rewrite. The only Phase-42-valid fix that keeps the 4.2 runtime AND a clean `npm run typecheck:node` is to repoint those consumers onto `spine-core-42` (a mechanical module-name rename, identical code). Phase 43 (RT-02) later migrates these `spine-core-42` direct imports → the `load.runtime.*` facade — a clean two-step migration.

**KNOWN, EXPECTED, Phase-47-OWNED consequence (NOT a Phase-42 defect — read this carefully):** `node_modules/@esotericsoftware/spine-player/dist/Player.d.ts:29` imports a **BARE** `@esotericsoftware/spine-core` specifier. spine-player ships at 4.2.111 but its own `.d.ts` rides the bare specifier, so after COMMIT B that bare specifier resolves to **4.3.0** and spine-player's public types resolve against 4.3's Pose-rewrite and leak into the git-tracked renderer file `src/renderer/src/modals/AnimationPlayerModal.tsx` (~22 `typecheck:web` errors). This repoint mechanism is **structurally incapable** of fixing it — the red file imports **spine-player**, not bare `spine-core`. Per the user-locked resolution (Option 1, 2026-05-16; `42-REPLAN-NOTE.md` v2 §3), Phase 42's typecheck assertion is narrowed to `typecheck:node` and the `typecheck:web` spine-player `.d.ts` 4.3-leak is **formally handed to Phase 47** — the v1.6 ROADMAP already assigns the spine-player → 4.3.0 bump + viewer regression to Phase 47 (sequenced last, independently revertible, depends only on Phase 42). `typecheck:node` (the real Phase-44 oracle scope / 42-04 CI dual-runtime gate scope — `src/main` + `src/core` + `tests`) is **clean on any fresh clone**; only `typecheck:web` via spine-player's own bare `.d.ts` hoist is red, and that surface is Phase 47's by design. This is a *scoping correction*, NOT a weakening: `src/core`/`src/main`/`tests` are genuinely green; the rejected Options 2 (type-isolation shim in Phase 42) and 3 (pull Phase 47's bump forward) are off the table (`42-REPLAN-NOTE.md` v2 §4).

Plain-English (project owner is a git beginner — [[user_git_experience]] / [[feedback_explain_git]]): The new Spine 4.3 engine is already installed as the main one with the old 4.2 engine bolted on under the explicit nickname `spine-core-42` (that was the previous, now-locked commit `cc5783f` — we do NOT redo it; we just check it's there and move on). The remaining job: the old 4.2 code used to ask for the engine by its plain name; now that the plain name points at the new 4.3 engine, we change every old file to ask for the engine by its explicit nickname `spine-core-42` instead — no behavior change at all, the old engine is byte-for-byte the same, the SAFE-01 "before" photo still matches. One side-effect we are NOT fixing here on purpose: the animation-viewer's player library has its own internal reference to the engine by its plain name, so it now sees the new 4.3 types and the viewer modal won't type-check on the *web* side. That is a known, expected, Phase-47 job (Phase 47 bumps the player to 4.3 — it's the last, easily-reversible step and is supposed to clean this up). We check the *node* side (the math + tests + main process) is perfectly clean, and we write down that the *web* side viewer-modal issue is Phase 47's, so nobody later mistakes it for a Phase 42 bug.

Output: COMMIT B detected-and-skipped (already frozen `cc5783f`); a separate post-COMMIT-B repoint commit moving every bare consumer (count-free fresh-grep scope) onto `spine-core-42` + the locked TS6133 reconciliation; SAFE-01 re-verified byte-equal; `npm run typecheck:node` clean on a fresh-clone-equivalent tree; the Phase-47-owned `typecheck:web` spine-player `.d.ts` leak explicitly documented here and required in 42-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-PATTERNS.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-CONTEXT.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-01-SUMMARY.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-REPLAN-NOTE.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/deferred-items.md

<interfaces>
<!-- COMMIT B is FROZEN. The shape below is the ALREADY-LANDED state of `cc5783f` — recorded for context ONLY. The re-executor does NOT install or re-pin; it detects and skips Task 1. -->
Frozen COMMIT B (`cc5783f`) `package.json` `dependencies` state (VERIFIED live this session — already present, do NOT regenerate):
```jsonc
"@esotericsoftware/spine-core": "4.3.0",                          // 4.3 CANONICAL (load-bearing direction) — ALREADY LANDED
"spine-core-42": "npm:@esotericsoftware/spine-core@4.2.111",      // exact-pinned alias — ALREADY LANDED
"@esotericsoftware/spine-player": "4.2.111"                       // NOT bumped — Phase 47 owns this
```
Frozen lockfile entry (already in `package-lock.json` at `cc5783f` — integrity matches the registry; `npm ci` deterministic):
```jsonc
"node_modules/spine-core-42": {
  "name": "@esotericsoftware/spine-core",
  "version": "4.2.111",
  "resolved": "https://registry.npmjs.org/@esotericsoftware/spine-core/-/spine-core-4.2.111.tgz",
  "integrity": "sha512-dh4OOJXjLvgHtwjXDIXr4FMrWo4uvVrSugkuSckeerdxRnZVyBLmvmKDiZ915qVmMiA6IEveVHOFPULzD98K6A==",
  "license": "LicenseRef-LICENSE"
}
```
Detection command (the re-executor runs this — if it succeeds, Task 1 is DONE; skip it): `git cat-file -e cc5783f` (exit 0 = COMMIT B present). Ancestry already satisfied at the frozen anchors: `git merge-base --is-ancestor 1b5327d cc5783f` exits 0 (do NOT re-derive — COMMIT A `1b5327d` is already a git ancestor of the frozen COMMIT B `cc5783f`).
Version facts (VERIFIED 2026-05-16): `@esotericsoftware/spine-core@latest` → 4.3.0 (NO beta tags ever on npm); 4.3.0 exports `Slider`, `BonePose`, `Pose`, `Posed`, `SlotPose` (Pose-architecture rewrite); ABSENT from 4.2.111.

Bare-consumer repoint scope (the count-free FRESH grep is the ONLY authoritative source — re-derive at execute time, NEVER carry a hand-list). The authoritative scope is **every** bare `@esotericsoftware/spine-core` import site in `src/` AND `tests/`, determined by `grep -rlE "['\"]@esotericsoftware/spine-core['\"]" src/ tests/` at execute time (count-free, mirrors SAFE-01 D-08 auto-discovery — a site added before execution is auto-covered). **The re-plan note's "7 src/core" enumeration is KNOWN-INCOMPLETE and MUST NOT be followed literally — it WRONGLY omits `src/main/summary.ts` and the typecheck-scope tests; following any hand-list reproduces the original defect (this is the v1 + Phase-42 Wave-2 load-bearing lesson — [[feedback_npm_alias_port_orphans_bare_consumers]]).** `spine-core-42` resolves to the SAME 4.2.111 the bare specifier resolves to TODAY → pure module-name rename, identical code, SAFE-01 byte-equality preserved, tests still test 4.2.

Fresh-clone semantics for the `typecheck:node` gate (LOCKED — `42-REPLAN-NOTE.md` v2 §2/§3; the re-executor MUST honor this and MUST NOT try to "fix" gitignored scratch). `npm run typecheck` = `npm run typecheck:node && npm run typecheck:web`. `typecheck:node` = `tsc --noEmit -p tsconfig.node.json` (its `include` covers `src/main/**`, `src/core/**`, `tests/**/*.ts`, AND `scripts/**/*.ts`). On *this local worktree* `typecheck:node` reports ~140 errors that are **ALL** inside untracked gitignored scratch — `scripts/probe-*.ts` (`.gitignore:73`), `scripts/diagnose-*.ts` (`.gitignore:74`), `tests/_trace_tmp/` (`.gitignore:85`). Those files **do NOT exist on a fresh clone / CI `npm ci` checkout**, so the Task-2 gate is `npm run typecheck:node` **on a fresh-clone-equivalent tree** = exit 0. The re-executor MUST NOT edit/delete/"fix" those gitignored scratch files — they are correctly out of Phase-42 scope and naturally absent on CI (the Wave-2 executor logged exactly this; do not repeat the mistake of trying to clean them). The Task-2 gate is the TWO conditions below — NOT a literal full `npm run typecheck` exit 0.

The Phase-47-owned KNOWN-ITEM (document here + require in SUMMARY — NOT a Phase-42 defect, NOT fixed here): `node_modules/@esotericsoftware/spine-player/dist/Player.d.ts:29` does `import { Animation, AnimationState, Color, Disposable, Downloader, Skeleton, StringMap, TimeKeeper, TrackEntry } from "@esotericsoftware/spine-core";` — a BARE specifier. spine-player ships 4.2.111 but its `.d.ts` rides the bare specifier → after COMMIT B it resolves to 4.3.0 → spine-player's public types resolve against 4.3's Pose-rewrite and ~22 errors surface in the git-tracked `src/renderer/src/modals/AnimationPlayerModal.tsx` under `typecheck:web` (`tsconfig.web.json`, the renderer project — NOT in `typecheck:node` scope; `tsconfig.node.json` does not include `src/renderer/src/modals/**`). This is the expected fallout of the locked 4.3-canonical direction (research Pitfall 3, "spine-player split-brain", which 42-02's own purpose statement always warned about). **Phase 47 (spine-player 4.2.111 → 4.3.0 bump + viewer regression, sequenced last + independently revertible, depends only on Phase 42) owns and resolves this** (ROADMAP Phase 47; STATE.md:128). 42-02's repoint mechanism cannot and must not touch it. The downstream verifier must NOT flag `typecheck:web` red as a Phase-42 miss.

Locked TS6133 reconciliation (deferred-items.md §3 — implement exactly, do NOT choose an alternative). ONE pre-existing typecheck error predates Phase 42, verified at the 42-01 base commit `166523f4aaca5e002bf612bb7ae9d5b7c422d206`: `tests/main/image-worker-rotation.spec.ts(190,13): error TS6133: 'data' is declared but its value is never read`. `tests/main/**` is in `tsconfig.node.json` `include` → this error is in `typecheck:node` scope. LOCKED reconciliation: the consumer-repoint commit (Task 2) ALSO applies a one-token fix to `tests/main/image-worker-rotation.spec.ts:190` — rename the unused `data` → `_data` (or remove it) — as an **explicitly-flagged deviation** in that commit, with an inline code comment `// repoint deviation: pre-existing TS6133, see 42 deferred-items.md §3`. Net effect: after the repoint commit, `npm run typecheck:node` is a clean exit 0 on a fresh-clone-equivalent tree.
</interfaces>

<analog_excerpts>
<!-- Read each at execution time before writing. -->
Frozen dual-install state — `package.json:26,35` (already `4.3.0` + `spine-core-42`) + lockfile `node_modules/spine-core-42` block. (PATTERNS.md Topic E — for CONTEXT ONLY; COMMIT B is detect-and-skip, do NOT regenerate.)
Bare-specifier import idiom being repointed (read at execute time via the fresh grep) — every `import ... from '@esotericsoftware/spine-core'` / `import type ... from '@esotericsoftware/spine-core'` line across `src/` + `tests/`; ONLY the specifier token changes.
TS6133 site — `tests/main/image-worker-rotation.spec.ts:~190` (the locked one-token `data`→`_data` reconciliation, flagged inline).
`tsconfig.node.json` `include` (the `typecheck:node` scope) — `tsconfig.node.json` (covers `src/main/**`, `src/core/**`, `tests/**/*.ts`, `scripts/**/*.ts`; the `scripts/**/*.ts` glob is why gitignored `scripts/probe-*.ts`/`diagnose-*.ts` scratch leaks locally but is absent on a fresh clone).
`.gitignore` scratch lines — `.gitignore:73` (`scripts/probe-*.ts`), `.gitignore:74` (`scripts/diagnose-*.ts`), `.gitignore:85` (`tests/_trace_tmp/`). The re-executor does NOT touch these.
spine-player bare-`.d.ts` (the Phase-47-owned leak — DO NOT touch) — `node_modules/@esotericsoftware/spine-player/dist/Player.d.ts:29` (bare `@esotericsoftware/spine-core` import) leaking into `src/renderer/src/modals/AnimationPlayerModal.tsx`.
</analog_excerpts>

<ordering_invariant>
**COMMIT B (`cc5783f`) is FROZEN and is already a git descendant of Plan 01's SAFE-01 baseline commit COMMIT A (`1b5327d`) — `git merge-base --is-ancestor 1b5327d cc5783f` exits 0 at the frozen anchors. The re-executor detects COMMIT B and SKIPS Task 1 (no re-pin/re-commit/reorder — research Pitfall 1 applies to BOTH A and B, ~100× recovery cost).** The Task 2 consumer-repoint commit MUST be a git descendant of the frozen COMMIT B (same wave, immediately after — auto-satisfied by resuming Wave 2 execution on top of `cc5783f`). The phase-wide 4-commit ordering is **A (`1b5327d`) → B (`cc5783f`) → repoint → C → D**. Do NOT amend, rebase, or reorder COMMIT A or COMMIT B, and do NOT fold the repoint into COMMIT B (it is frozen). Plan 01's `safe01-freeze-guard.spec.ts` Assertion 1 already hard-asserts the A→B ancestry (COMMIT B introduced the literal `spine-core-42` into `package.json`).
</ordering_invariant>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DONE + FROZEN — detect COMMIT B (`cc5783f`) already landed and SKIP (no re-pin, no re-commit, no reorder)</name>
  <files>package.json, package-lock.json (FROZEN at COMMIT B `cc5783f` — detect-and-skip; do NOT modify)</files>
  <read_first>
    - 42-REPLAN-NOTE.md §1 ("What is DONE and FROZEN" — COMMIT B `cc5783f` is an immutable ancestry anchor; detect-and-skip; research Pitfall 1 applies to BOTH A and B)
    - 42-REPLAN-NOTE.md §3 ("42-02 (amend)" — "Task 1 / COMMIT B (`cc5783f`): mark DONE + FROZEN. Plan text must direct the re-executor to detect COMMIT B exists and skip it (no re-pin, no re-commit, no reorder). A→B ancestry already satisfied.")
    - .planning/STATE.md ("Current Position" — 42-02 Task 1 done+frozen, COMMIT B `cc5783f`, pure, A→B ancestry verified)
    - 42-01-SUMMARY.md (confirm COMMIT A SHA `1b5327d` — the ancestry parent of the frozen COMMIT B)
  </read_first>
  <action>
**This task is DONE and FROZEN. Do NOT install, do NOT re-pin, do NOT re-commit, do NOT reorder. Detect that COMMIT B already exists and SKIP to Task 2.**

COMMIT B = `cc5783f` (`feat(42-02): COMMIT B — RT-01 dual spine-core (4.3.0 canonical + spine-core-42 alias 4.2.111)`) is an immutable ancestry anchor, orchestrator-verified at the Wave-2 halt: pure (exactly 2 files — `package.json` + `package-lock.json`); exact pins (`@esotericsoftware/spine-core@4.3.0` canonical, `spine-core-42` = exact `npm:@esotericsoftware/spine-core@4.2.111`, `@esotericsoftware/spine-player` untouched at `4.2.111`); lockfile reproducible; the one Rule-1 caret-range deviation was auto-corrected to exact pins (threat T-42-07) before the commit. Research Pitfall 1 (no amend/rebase/reorder of an ancestry commit; ~100× recovery cost) applies to BOTH COMMIT A (`1b5327d`) and COMMIT B (`cc5783f`).

**Detection (run these; if they pass, Task 1 is satisfied — proceed to Task 2 WITHOUT touching package.json/package-lock.json):**
1. `git cat-file -e cc5783f` exits 0 (COMMIT B is present in history).
2. `git merge-base --is-ancestor 1b5327d cc5783f` exits 0 (COMMIT A is already a git ancestor of the frozen COMMIT B — ancestry satisfied at the frozen anchors; do NOT re-derive).
3. `node -p "require('./package.json').dependencies['@esotericsoftware/spine-core']"` prints `4.3.0` AND `node -p "require('./package.json').dependencies['spine-core-42']"` prints `npm:@esotericsoftware/spine-core@4.2.111` AND `node -p "require('./package.json').dependencies['@esotericsoftware/spine-player']"` prints `4.2.111` (the frozen COMMIT-B state is already in the working tree).

If all three pass: **Task 1 is complete by virtue of the frozen COMMIT B. Do nothing else here. Do NOT run `npm install`. Do NOT re-commit package.json/package-lock.json. Proceed directly to Task 2 (the repoint), which lands as a separate commit immediately after the frozen COMMIT B.** (If — and only if — `git cat-file -e cc5783f` FAILS, STOP and escalate: the frozen anchor is missing and the orchestrator's pre-spawn verification was wrong; do NOT attempt to recreate COMMIT B yourself.)
  </action>
  <verify>
    <automated>git cat-file -e cc5783f && git merge-base --is-ancestor 1b5327d cc5783f && node -p "require('./package.json').dependencies['spine-core-42']"</automated>
  </verify>
  <acceptance_criteria>
    - `git cat-file -e cc5783f` exits 0 (the frozen COMMIT B is present — detect-and-skip confirmed)
    - `git merge-base --is-ancestor 1b5327d cc5783f` exits 0 (COMMIT A `1b5327d` is already a git ancestor of the frozen COMMIT B `cc5783f` — the phase acceptance test, satisfied at the frozen anchors, NOT re-derived)
    - `node -p "require('./package.json').dependencies['@esotericsoftware/spine-core']"` prints exactly `4.3.0` AND `node -p "require('./package.json').dependencies['spine-core-42']"` prints exactly `npm:@esotericsoftware/spine-core@4.2.111` AND `node -p "require('./package.json').dependencies['@esotericsoftware/spine-player']"` prints exactly `4.2.111` (the frozen COMMIT-B state is intact in the working tree)
    - The re-executor created NO new commit for Task 1 and ran NO `npm install` (Task 1 is detect-and-skip; the next commit in history after `cc5783f` is the Task-2 repoint commit, NOT a duplicate COMMIT B) — verifiable: `git log --format=%H -- package.json | head -1` is `cc5783f` (no newer package.json commit was created by this plan)
  </acceptance_criteria>
  <done>COMMIT B (`cc5783f`) is confirmed already landed and frozen (detect-and-skip): the dual-install is intact (4.3.0 canonical + `spine-core-42` aliased 4.2.111, spine-player at 4.2.111), and COMMIT A `1b5327d` is machine-proven to already be a git ancestor of `cc5783f`. No re-pin, no re-commit, no reorder occurred. Execution proceeds to Task 2 (the repoint, a separate commit immediately after the frozen COMMIT B).</done>
</task>

<task type="auto">
  <name>Task 2: Consumer-repoint — move every bare @esotericsoftware/spine-core import (src/ + tests/) onto spine-core-42 + the locked TS6133 reconciliation; separate commit immediately after the frozen COMMIT B</name>
  <files>every src/** + tests/** file with a bare @esotericsoftware/spine-core import (FRESH count-free grep at execute time — NO hand-list carried forward), plus tests/main/image-worker-rotation.spec.ts (the locked TS6133 deviation)</files>
  <read_first>
    - 42-REPLAN-NOTE.md §3 ("42-02 (amend)" — Task 2 method unchanged: count-free fresh grep over `src/` AND `tests/`; repoint bare → `spine-core-42`; ZERO logic change; the locked TS6133 reconciliation; separate commit immediately after COMMIT B; the REPLACED two-condition success criterion; the Phase-47-owned known-item) AND §1 (COMMIT B frozen) AND §4 (rejected Options 2/3 are off the table)
    - 42-REPLAN-NOTE.md §2 (the fresh-clone semantics of the `typecheck:node` gate — the gitignored scratch is NOT yours to fix)
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/deferred-items.md §3 (the ONE pre-existing TS6133 at `tests/main/image-worker-rotation.spec.ts:190`, the locked one-token reconciliation)
    - package.json `scripts` block (confirm `typecheck:node` = `tsc --noEmit -p tsconfig.node.json` and `typecheck` = `typecheck:node && typecheck:web` — the Task-2 gate is `typecheck:node`, NOT full `typecheck`)
    - tsconfig.node.json (`include` covers `src/main/**`, `src/core/**`, `tests/**/*.ts`, `scripts/**/*.ts`; this is why gitignored `scripts/probe-*.ts`/`diagnose-*.ts` leak locally but are absent on a fresh clone — DO NOT fix them)
    - .gitignore lines 73 (`scripts/probe-*.ts`), 74 (`scripts/diagnose-*.ts`), 85 (`tests/_trace_tmp/`) — the scratch globs that are correctly out of scope and absent on CI
    - The file(s) being repointed — discovered by the fresh grep below (read each before editing it; the change is the specifier token ONLY)
    - tests/arch.spec.ts (the existing no-co-mingled-imports anchor — each consumer must import exactly ONE specifier after the repoint)
  </read_first>
  <action>
This is a **separate, discrete commit, SAME wave, IMMEDIATELY AFTER the frozen COMMIT B (`cc5783f`)**. It is a git descendant of COMMIT B (auto-satisfied by resuming Wave-2 execution on top of `cc5783f`). It is NOT folded into COMMIT B (COMMIT B is frozen — do NOT touch `package.json`/`package-lock.json`).

**Step 1 — fresh grep (count-free; the ONLY authoritative scope at execute time):**
```
grep -rlE "['\"]@esotericsoftware/spine-core['\"]" src/ tests/
```
This returns EVERY file that imports the bare `@esotericsoftware/spine-core` specifier in `src/` and `tests/` — the exact repoint set, re-derived NOW. A file added before execution is auto-covered (this mirrors SAFE-01 D-08 auto-discovery). **FORBIDDEN: carrying forward any hand-enumerated file list or count, even if a note hands you one. The re-plan note's "7 src/core" enumeration is KNOWN-INCOMPLETE — it omits `src/main/summary.ts` and the typecheck-scope test files; following any hand-list reproduces the original defect ([[feedback_npm_alias_port_orphans_bare_consumers]]). The grep output IS the set. Do NOT cross-check it against, prune it to, or extend it from any prior list — use exactly what the grep returns.**

**Step 2 — mechanical specifier rename (ZERO logic change):** For EVERY file the grep returned, on import/from lines ONLY, rename the specifier string `@esotericsoftware/spine-core` → `spine-core-42`. This applies to BOTH value imports (`import { Skeleton } from '@esotericsoftware/spine-core'`) AND type-only imports (`import type { Slot } from '@esotericsoftware/spine-core'`) AND any dynamic `import('@esotericsoftware/spine-core')`. Change NOTHING else — not import names, not bindings, not any other line; ONLY the import-specifier string moves. `spine-core-42` resolves to the SAME 4.2.111 the bare specifier resolved to before COMMIT B, so this is a pure module-name rename: identical code, identical runtime, SAFE-01 byte-equality preserved, tests still exercise 4.2.

**Step 3 — the locked TS6133 reconciliation (explicitly-flagged deviation):** In `tests/main/image-worker-rotation.spec.ts` at line ~190, the pre-existing `error TS6133: 'data' is declared but its value is never read` (deferred-items.md §3 — verified at the 42-01 base commit `166523f4aaca5e002bf612bb7ae9d5b7c422d206`, predates Phase 42; `tests/main/**` is in `tsconfig.node.json` `include` so this is in `typecheck:node` scope). Apply the LOCKED one-token fix: rename the unused `data` → `_data` (or remove the unused binding), and add the inline comment exactly:
```
// repoint deviation: pre-existing TS6133, see 42 deferred-items.md §3
```
This is the ONLY non-mechanical change in this commit and it is explicitly flagged.

**Step 4 — the Task-2 gate (EXACTLY two conditions — NOT a literal full `npm run typecheck` exit 0):**
  1. **SAFE-01 byte-equality RE-VERIFIES GREEN:** run `npx vitest run tests/safe01` — it MUST be green (byte-equal). Only the module NAME changed; the runtime is the identical 4.2.111. The SAFE-01 frozen baseline files (committed by 42-01) are NOT touched by this commit.
  2. **`npm run typecheck:node` exits 0 on a fresh-clone-equivalent tree:** run `npm run typecheck:node`. The gate is exit 0 *ignoring untracked gitignored scratch* — `scripts/probe-*.ts` (`.gitignore:73`), `scripts/diagnose-*.ts` (`.gitignore:74`), `tests/_trace_tmp/` (`.gitignore:85`). Those files do NOT exist on a fresh clone / CI `npm ci`; on *this local worktree* they may produce ~140 errors that are ENTIRELY inside those gitignored scratch paths. **DO NOT "fix", edit, or delete that gitignored scratch — it is correctly out of Phase-42 scope and naturally absent on CI (the Wave-2 executor logged exactly this; do not repeat the mistake).** To confirm fresh-clone-equivalence, verify every remaining `typecheck:node` error (if any) is in a path matching `scripts/probe-*.ts`, `scripts/diagnose-*.ts`, or `tests/_trace_tmp/` — i.e. ZERO `typecheck:node` errors in any git-tracked file (the repoint eliminated all bare-specifier orphans in `typecheck:node` scope and the TS6133 fix folded the one tracked pre-existing error). Practical check: `git stash -u` is NOT needed; instead assert `npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/"` returns ZERO lines (no tracked-file `typecheck:node` error remains).

Also run `npx vitest run tests/arch.spec.ts` — green: no source file imports BOTH alias specifiers; each repointed consumer imports exactly ONE (`spine-core-42`).

**Do NOT run/assert full `npm run typecheck`** as the Task-2 gate, and do NOT attempt to make `typecheck:web` green. `typecheck:web` is RED via spine-player's own bare `Player.d.ts` hoist (`node_modules/@esotericsoftware/spine-player/dist/Player.d.ts:29` → ~22 errors in `src/renderer/src/modals/AnimationPlayerModal.tsx`). That is a **KNOWN, EXPECTED, Phase-47-OWNED** consequence of the locked 4.3-canonical direction — NOT a Phase-42 defect, NOT this commit's job, NOT fixable by this repoint (the red file imports spine-player, not bare spine-core). The rejected Options 2 (type-isolation shim in Phase 42) and 3 (pull Phase 47's bump forward) are off the table (`42-REPLAN-NOTE.md` v2 §4). Do NOT touch `src/renderer/**`, `AnimationPlayerModal.tsx`, spine-player, or `tsconfig.web.json` here.

**Step 5 — commit (separate, immediately after the frozen COMMIT B).** PLAIN ENGLISH message ([[user_git_experience]] / [[feedback_explain_git]]), e.g.:
> `Repoint 4.2 consumers onto the spine-core-42 nickname. The old 4.2 code used to ask for the engine by its plain name; now that the plain name points at the new 4.3 engine (the previous, frozen commit cc5783f), every old file asks for the engine by its explicit nickname spine-core-42 instead. Zero behavior change — the old engine is byte-for-byte the same 4.2.111. The SAFE-01 "before" photo still matches. Also folds the one pre-existing unused-variable typecheck nit in image-worker-rotation.spec.ts (flagged inline; see 42 deferred-items.md §3) so typecheck:node is a clean exit 0 on a fresh clone. The web-side viewer-modal type error from spine-player's own internal engine reference is a known, expected Phase-47 job (not fixed here). Phase 43 later moves these direct imports behind the load.runtime.* facade.`

Do NOT touch `tests/safe01/**` source. Do NOT touch `package.json`/`package-lock.json` (those are the FROZEN COMMIT B — keeping them out of this commit preserves the frozen anchor and keeps this commit a pure repoint).
  </action>
  <verify>
    <automated>npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/" | wc -l | grep -qx '0' && npx vitest run tests/safe01 tests/arch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rlE "['\"]@esotericsoftware/spine-core['\"]" src/ tests/` returns ZERO files (every bare site in `src/`+`tests/` now reads `spine-core-42`; count-free — the grep is the authoritative completeness check; auto-covers any file added before execution)
    - `npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/" | wc -l` is `0` — ZERO `typecheck:node` errors in any git-tracked file (fresh-clone-equivalent exit 0: the repoint eliminated all bare-specifier orphans in `typecheck:node` scope AND the locked `image-worker-rotation.spec.ts` TS6133 was folded; the only-locally-present errors are entirely inside gitignored `scripts/probe-*.ts` / `scripts/diagnose-*.ts` / `tests/_trace_tmp/` scratch, which is absent on CI `npm ci` and is NOT fixed here)
    - `grep -c "repoint deviation: pre-existing TS6133, see 42 deferred-items.md §3" tests/main/image-worker-rotation.spec.ts` ≥ 1 (the locked reconciliation is present and explicitly flagged) AND the unused `data` at line ~190 was renamed/removed (`grep -n "_data" tests/main/image-worker-rotation.spec.ts` shows the renamed binding, OR the binding is gone)
    - `npx vitest run tests/safe01` exits 0 (SAFE-01 byte-equal RE-VERIFIED — identical 4.2.111 runtime; only the module name moved; the 42-01 frozen baseline is untouched)
    - `npx vitest run tests/arch.spec.ts` exits 0 — no source file imports BOTH alias specifiers; each repointed consumer imports exactly ONE (`spine-core-42`)
    - `git log -1 --stat` for THIS repoint commit shows the repointed files + `tests/main/image-worker-rotation.spec.ts`, and does NOT show `package.json`/`package-lock.json` (those are the FROZEN COMMIT B `cc5783f`) and does NOT show any `tests/safe01/**` source change and does NOT show any `src/renderer/**`/`tsconfig.web.json` change (the spine-player `typecheck:web` leak is Phase-47-owned, not touched here) — the repoint commit is a clean separate descendant of the frozen COMMIT B
    - `git log --format=%H -- package.json | head -1` is `cc5783f` (the frozen COMMIT B is still the most recent package.json commit — Task 2 did NOT create a new package.json commit)
    - `npx vitest run` (full suite, EXCLUDING the documented pre-existing local-only gitignored-rig skips in deferred-items.md §1–2) is green — every pre-existing test now importing `spine-core-42` = the same 4.2.111, plus the SAFE-01 baseline byte-equal
  </acceptance_criteria>
  <done>Every pre-existing bare `@esotericsoftware/spine-core` consumer (src/ + tests/, discovered count-free by a fresh grep — NO hand-list carried forward) is mechanically repointed to `spine-core-42` in a separate commit immediately after the frozen COMMIT B (`cc5783f`); the locked one-token TS6133 reconciliation is folded and flagged inline; SAFE-01 RE-VERIFIES byte-equal (4.2.111 runtime unchanged, the 42-01 frozen baseline untouched) AND `npm run typecheck:node` is exit 0 on a fresh-clone-equivalent tree (the two-condition Task-2 gate — NOT a literal full `npm run typecheck`); the gitignored scratch and the Phase-47-owned `typecheck:web` spine-player `.d.ts` leak were correctly left untouched; no file co-mingles both specifiers; the frozen COMMIT B is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules / lockfile | The alias resolves a second tarball; supply-chain surface. FROZEN at COMMIT B (`cc5783f`) — integrity-pinned, verified at the halt; not re-derived in the re-plan |
| committed git history → the SAFE-01 ordering invariant | COMMIT B (`cc5783f`) is a frozen descendant of COMMIT A (`1b5327d`); detect-and-skip preserves the ancestry; a rebase/amend that reordered them would void SAFE-01 (research Pitfall 1) |
| 4.2-written consumers ↔ the version-flipped bare specifier | The 4.3 flip (COMMIT B) orphaned every bare consumer; the repoint moves them onto the explicit 4.2.111 alias — a local module-name rename, NO new dependency/registry surface |
| spine-player's own bare `.d.ts` → renderer typecheck:web | Phase-47-OWNED (NOT a Phase-42 surface): spine-player's `Player.d.ts:29` bare import hoists 4.3 into `AnimationPlayerModal.tsx`; out of this plan's scope by the locked resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06 | Tampering | Dependency-confusion / tampered tarball on the `spine-core-42` alias or 4.3.0 canonical | mitigate (FROZEN at COMMIT B) | The committed `package-lock.json` integrity sha512 was verified at COMMIT B (`cc5783f`) against `npm view ... dist.integrity`; CI uses `npm ci` (integrity-verified, never `npm install`) — enforced in Plan 04's `ci.yml`. COMMIT B is detect-and-skip; the supply-chain surface is unchanged from the frozen anchor |
| T-42-07 | Tampering | Patch-shaped breaking/security drift (`^`/`~` range pulls a changed API) | mitigate (FROZEN at COMMIT B) | EXACT pins were locked at COMMIT B (`4.3.0`, `npm:...@4.2.111` — no `^`/`~`); the Wave-2 caret-range deviation was auto-corrected before `cc5783f`. Task 1's acceptance criteria re-assert the exact literal strings are still present (detect-and-skip integrity check) |
| T-42-08 | Tampering | Naive alias direction (4.2 canonical, 4.3 aliased) → spine-player split-brain | mitigate (FROZEN at COMMIT B) | Direction is LOCKED 4.3-canonical and frozen in `cc5783f`; CONTEXT.md:62 / PROJECT.md:88 / RESEARCH Pitfall 3 verified. Task 1 re-asserts spine-player stays 4.2.111 (the inversion's tell). The `typecheck:web` leak is the EXPECTED correct-direction fallout, formally handed to Phase 47 (not a defect — see T-42-24) |
| T-42-09 | Tampering | COMMIT B rebased/amended before COMMIT A → ordering voided | mitigate | COMMIT B is FROZEN (detect-and-skip; research Pitfall 1 — no amend/rebase/reorder). Task 1's acceptance criterion runs `git merge-base --is-ancestor 1b5327d cc5783f` (exits 0 at the frozen anchors). Plan 01's `safe01-freeze-guard.spec.ts` already hard-asserts it; CI re-runs it (Plan 04) |
| T-42-22 | Tampering | The consumer-repoint silently changes behavior (not a pure specifier-string rename) or a hand-list misses a site → a bare consumer still rides 4.3 (the original defect persists) | mitigate | The repoint is a SPECIFIER-STRING-ONLY rename to `spine-core-42` (resolves to the SAME 4.2.111 the bare specifier resolved to before — identical code). Completeness is a count-free FRESH grep (`grep -rlE ... '@esotericsoftware/spine-core' src/ tests/` returns ZERO afterward) so no site is missed and NO hand-list is carried forward (the v1 hand-list defect class — [[feedback_npm_alias_port_orphans_bare_consumers]]). Behavior-equivalence is proven by the SAFE-01 byte-equal RE-VERIFY gate + the full vitest run. NO new dependency/registry surface — `spine-core-42` is the already-frozen, already-integrity-pinned alias from COMMIT B |
| T-42-24 (NEW) | Tampering / false-defect | The Phase-47-owned `typecheck:web` spine-player `.d.ts` 4.3-leak is mis-handled — either "fixed" in Phase 42 (pre-empting Phase 47's charter / masking real future drift) OR mis-flagged by a downstream verifier as a Phase-42 miss | mitigate (no new high threat introduced) | NO new high threat: this is a *scoping correction*, not a code change. The leak is explicitly DOCUMENTED here (objective + interfaces + must_haves) and REQUIRED in 42-02-SUMMARY.md as KNOWN/EXPECTED/Phase-47-OWNED; the Task-2 gate asserts `typecheck:node` (not full `typecheck`) so the executor never "fixes" `typecheck:web`; acceptance criteria forbid touching `src/renderer/**`/`tsconfig.web.json`/spine-player. The narrowing introduces NO new dependency, parse, or auth surface — `typecheck:node` (the genuine dual-runtime + CI gate scope) is genuinely green on a fresh clone; the rejected Options 2/3 (which WOULD add surface) are off the table |

ASVS L1 V2/V3/V4/V5(beyond existing loader guard)/V7 are N/A: COMMIT B is a frozen dependency-graph change (detect-and-skip) + a mechanical specifier-string repoint, no auth/session/access-control/input-parsing surface introduced. The genuine surface is supply-chain integrity (V6-integrity) of the new dependency — fully mitigated and FROZEN at COMMIT B (`npm ci`-verified integrity-pinned lockfile, T-42-06/07); the repoint adds NO new registry/dependency surface (T-42-22). The `typecheck:web` narrowing introduces NO new high threat (T-42-24) — it is a scoping correction with the leak formally owned by Phase 47.
</threat_model>

<verification>
- `git cat-file -e cc5783f` exits 0 AND `git merge-base --is-ancestor 1b5327d cc5783f` exits 0 — the frozen COMMIT B is present and COMMIT A is already its git ancestor (detect-and-skip; the phase acceptance test, satisfied at the frozen anchors)
- `grep -rlE "['\"]@esotericsoftware/spine-core['\"]" src/ tests/` returns ZERO (every bare consumer repointed; count-free completeness check; NO hand-list carried forward)
- `npm run typecheck:node` is exit 0 on a fresh-clone-equivalent tree — i.e. `npm run typecheck:node 2>&1 | grep -E "error TS" | grep -vE "scripts/probe-|scripts/diagnose-|tests/_trace_tmp/"` returns ZERO lines (no git-tracked `typecheck:node` error; the only-local errors are entirely inside gitignored scratch absent on CI — NOT fixed here). This is the Task-2 gate, NOT a literal full `npm run typecheck`
- `npx vitest run tests/safe01` — RE-VERIFIED byte-equal AFTER the repoint (only the module name moved; identical 4.2.111 runtime; the 42-01 frozen baseline files untouched)
- `npx vitest run tests/arch.spec.ts` — no source file co-mingles both specifiers; each repointed consumer imports exactly one (`spine-core-42`)
- `npx vitest run` — FULL suite green (excluding the documented pre-existing local-only gitignored-rig skips, deferred-items.md §1–2), INCLUDING `tests/safe01/safe01-baseline.spec.ts` byte-equal and `tests/safe01/safe01-freeze-guard.spec.ts` hard-passing the A→B ancestry assertion
- KNOWN/EXPECTED/Phase-47-OWNED (NOT a Phase-42 defect, NOT verified-green here): `npm run typecheck:web` is RED (~22 errors in `src/renderer/src/modals/AnimationPlayerModal.tsx`) via `node_modules/@esotericsoftware/spine-player/dist/Player.d.ts:29`'s bare `@esotericsoftware/spine-core` hoist to 4.3.0. Phase 47 (spine-player → 4.3.0 bump + viewer regression, sequenced last + revertible) owns and resolves this. The 42-02-SUMMARY.md MUST record this so the downstream verifier does not flag it as a Phase-42 miss
- `git log` shows the frozen COMMIT B (`cc5783f`, alias) strictly after COMMIT A (`1b5327d`, baseline), then the repoint commit strictly after `cc5783f`; `@esotericsoftware/spine-player` still `4.2.111`
</verification>

<success_criteria>
- RT-01 satisfied: COMMIT B (`cc5783f`) — `@esotericsoftware/spine-core@4.3.0` canonical + `spine-core-42`→`@esotericsoftware/spine-core@4.2.111` exact-pinned alias, lockfile-committed, `npm ci`-reproducible — is FROZEN and detect-and-skipped (no re-pin/re-commit/reorder); spine-player still 4.2.111
- The cross-plan defect is resolved per the locked re-plan note v2 §3: every pre-existing bare consumer (src/ + tests/, count-free fresh-grep scope, NO hand-list carried forward) is repointed to `spine-core-42` in a separate commit immediately after the frozen COMMIT B, with the locked TS6133 reconciliation folded — the Task-2 gate is the TWO conditions (SAFE-01 byte-equal RE-VERIFIED + `npm run typecheck:node` exit 0 on a fresh-clone-equivalent tree), NOT a literal full `npm run typecheck` exit 0
- The `typecheck:web` / `AnimationPlayerModal.tsx` / spine-player-`Player.d.ts` 4.3-leak (~22 errors) is explicitly documented as KNOWN/EXPECTED/Phase-47-OWNED (objective + interfaces + must_haves + REQUIRED in 42-02-SUMMARY.md) — NOT a Phase-42 defect; Phase 47 (sequenced last + revertible) resolves it; the rejected Options 2/3 are off the table
- The alias DIRECTION is the load-bearing inverted one (4.3 canonical, frozen in `cc5783f`) so Phase 47's spine-player bump is a clean drop-in; spine-player itself is untouched at 4.2.111
- COMMIT A (`1b5327d`) is a git ancestor of the frozen COMMIT B (`cc5783f`) and the repoint commit is a descendant of `cc5783f` (machine-verified); the phase-wide 4-commit ordering A→B→repoint→C→D holds; Plan 01's freeze-guard ancestry assertion stays hard-passing
- The SAFE-01 baseline is still byte-equal after the repoint (specifier-string-only change, no `core/` math change) — proving the repoint is plumbing-only and 4.2 behavior is genuinely frozen; Phase 43 later migrates the `spine-core-42` direct imports → the `load.runtime.*` facade (clean two-step migration)
</success_criteria>

<output>
After completion, create `.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-02-SUMMARY.md`. Record: that COMMIT B (`cc5783f`) was detected-and-skipped (frozen — no re-pin/re-commit/reorder), the `git merge-base --is-ancestor 1b5327d cc5783f` result (exit 0 at the frozen anchors), the separate repoint-commit SHA and that it is a descendant of `cc5783f`, the exact count-free set of files the fresh grep repointed (and explicit confirmation `src/main/summary.ts` + the typecheck-scope test files were included — NO hand-list was carried forward; the re-plan note's "7 src/core" enumeration was NOT followed), confirmation the locked TS6133 reconciliation was applied + flagged inline, that `npm run typecheck:node` is exit 0 on a fresh-clone-equivalent tree (zero git-tracked errors; the gitignored scratch was correctly NOT touched) and that this is the Task-2 gate (NOT a literal full `npm run typecheck`), that SAFE-01 RE-VERIFIED byte-equal (42-01 frozen baseline untouched), explicit confirmation that `spine-player` is still 4.2.111, and — REQUIRED — an explicit "KNOWN / EXPECTED / Phase-47-OWNED, NOT a Phase-42 defect" note that `npm run typecheck:web` is RED (~22 errors in `src/renderer/src/modals/AnimationPlayerModal.tsx`) via `node_modules/@esotericsoftware/spine-player/dist/Player.d.ts:29`'s bare `@esotericsoftware/spine-core` → 4.3.0 hoist, that Phase 47 (spine-player → 4.3.0 bump + viewer regression, sequenced last + revertible) owns and resolves it, and that the rejected Options 2/3 were not introduced — worded so the downstream verifier does NOT flag `typecheck:web` red as a Phase-42 miss.
</output>
