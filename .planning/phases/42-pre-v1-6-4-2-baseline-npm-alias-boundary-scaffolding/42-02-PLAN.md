---
phase: 42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - package.json
  - package-lock.json
  - tests/runtime/alias-resolution.spec.ts
  - tests/runtime/runtime-distinctness.spec.ts
  - "src/** and tests/** bare @esotericsoftware/spine-core import sites (Task 2 consumer-repoint — exact set discovered by a FRESH grep at execute time, count-free)"
  - "tests/main/image-worker-rotation.spec.ts (Task 2 — the locked one-token TS6133 reconciliation deviation)"
autonomous: true
requirements: [RT-01, SAFE-01]
must_haves:
  truths:
    - "From a fresh `npm ci`, `@esotericsoftware/spine-core` resolves to 4.3.0 (canonical) and `spine-core-42` resolves to 4.2.111 (the exact-pinned alias) — both side-by-side"
    - "`adapter42.version !== adapter43.version` (4.2.111 vs 4.3.0) and `Slider`/`BonePose`/`Pose`/`SlotPose` are exported from the 4.3 module and ABSENT from `spine-core-42`"
    - "Every pre-existing BARE `@esotericsoftware/spine-core` import site (in `src/` AND `tests/`, discovered by a FRESH grep at execute time — count-free, auto-covers any site added before execution) is mechanically repointed to `spine-core-42` in a SEPARATE commit immediately after COMMIT B — ZERO logic change, identical 4.2.111 runtime"
    - "`npm run typecheck` is a CLEAN LITERAL exit 0 after the repoint commit — the repoint eliminated ALL bare-specifier orphans AND folded the locked one-token `tests/main/image-worker-rotation.spec.ts:190` TS6133 reconciliation (see deferred-items.md §3); no \"modulo pre-existing baseline\" hedging anywhere"
    - "SAFE-01 byte-equal gate (`tests/safe01`) RE-PASSES after the repoint — only the module NAME changed, the runtime is the SAME 4.2.111 the bare specifier resolved to before; SAFE-01 frozen baseline (42-01) untouched"
    - "The alias is committed in COMMIT B, a git DESCENDANT of the SAFE-01 baseline commit (COMMIT A); the repoint commit is a git DESCENDANT of COMMIT B — Plan 01's freeze-guard now hard-asserts the ancestry instead of skipping"
    - "`@esotericsoftware/spine-player` is STILL 4.2.111 (NOT bumped — that is Phase 47; the alias DIRECTION makes Phase 47 a clean drop-in)"
  artifacts:
    - path: "package.json"
      provides: "spine-core→4.3.0 canonical + spine-core-42 exact-pinned npm alias; spine-player unchanged at 4.2.111"
      contains: "spine-core-42"
    - path: "package-lock.json"
      provides: "Committed lockfile with node_modules/spine-core-42 entry (name @esotericsoftware/spine-core, version 4.2.111, integrity sha512) — npm ci reproducibility"
      contains: "spine-core-42"
    - path: "tests/runtime/alias-resolution.spec.ts"
      provides: "Both specifiers resolve to distinct versions under vitest/Node"
      min_lines: 20
    - path: "tests/runtime/runtime-distinctness.spec.ts"
      provides: "v42≠v43; Slider/BonePose/Pose/SlotPose 4.3-only assertion"
      min_lines: 20
  key_links:
    - from: "package.json dependencies"
      to: "npm registry @esotericsoftware/spine-core"
      via: "\"@esotericsoftware/spine-core\": \"4.3.0\" + \"spine-core-42\": \"npm:@esotericsoftware/spine-core@4.2.111\""
      pattern: "npm:@esotericsoftware/spine-core@4\\.2\\.111"
    - from: "tests/runtime/runtime-distinctness.spec.ts"
      to: "both spine-core modules"
      via: "import from '@esotericsoftware/spine-core' (4.3.0) and from 'spine-core-42' (4.2.111); assert version + export distinctness"
      pattern: "spine-core-42"
    - from: "every pre-existing bare @esotericsoftware/spine-core import site (src/ + tests/)"
      to: "spine-core-42 (the exact-pinned 4.2.111 alias)"
      via: "mechanical specifier-token rename on import/from lines (value AND `import type`), discrete commit immediately after COMMIT B; SAFE-01 re-verify gate"
      pattern: "spine-core-42"
    - from: "Plan 01 tests/safe01/safe01-freeze-guard.spec.ts"
      to: "this commit (COMMIT B)"
      via: "git log -S 'spine-core-42' now finds this commit → merge-base --is-ancestor hard-asserts COMMIT A precedes it"
      pattern: "merge-base"
---

<objective>
Land the RT-01 dual-install: make `@esotericsoftware/spine-core@4.3.0` the **canonical** package and install `4.2.111` side-by-side via the exact-pinned, lockfile-committed npm alias `spine-core-42`. Commit `package.json` + `package-lock.json` together as **COMMIT B** — a git **descendant** of the SAFE-01 baseline commit (COMMIT A from Plan 01). Then, in a **separate commit immediately after COMMIT B (same wave)**, mechanically repoint every pre-existing BARE `@esotericsoftware/spine-core` consumer (src/ + tests/) onto the `spine-core-42` alias so the 4.3 flip does not orphan the 4.2-written code — ZERO logic change, SAFE-01 re-verified byte-equal.

Purpose: RT-01 is the single highest-blast-radius change in the milestone (it touches every 4.2 user code path). The alias **direction** is load-bearing and counter-intuitive: 4.3 is canonical (not 4.2) so that `spine-player` / `spine-webgl`'s bare `export * from "@esotericsoftware/spine-core"` resolves correctly when Phase 47 bumps the player — inverting the direction silently corrupts spine-player into a split-brain (research Pitfall 3). The literal alias key is **`spine-core-42`** (RESOLVED in RESEARCH §Standard Stack — verified live; the scoped `@esotericsoftware/spine-core-43` form is the rejected naive-direction naming). **The locked cross-plan resolution (re-plan note §3):** "4.3 canonical" *definitionally* means no legacy code may resolve spine-core bare-as-4.2 — the moment the bare specifier flips to 4.3, every pre-existing bare consumer compiles against 4.3's Pose rewrite (~58 typecheck errors). The only Phase-42-valid fix that keeps the 4.2 runtime AND `npm run typecheck` exit 0 is to repoint those consumers onto `spine-core-42` (a mechanical module-name rename, identical code). Phase 43 (RT-02) later migrates these `spine-core-42` direct imports → the `load.runtime.*` facade — a clean two-step migration.

Plain-English (project owner is a git beginner — [[user_git_experience]] / [[feedback_explain_git]]): This is the renovation. We're adding the new Spine 4.3 engine as the main one, and keeping the old 4.2 engine bolted on under the explicit nickname `spine-core-42`. Because Plan 01 already took and locked the "before" photo in an EARLIER commit, Git's history now proves the photo predates this change. Plan 01's guard test, which politely waited (skipped) while there was no alias, now flips on automatically and FAILS the build if anyone ever reorders these. **First commit:** we change only two files (`package.json` and its lockfile). **Second commit, right after:** the old 4.2 code used to ask for the engine by its plain name; now that the plain name points at the new 4.3 engine, we change every old file to ask for the engine by its explicit nickname `spine-core-42` instead — no behavior change at all, the old engine is byte-for-byte the same, the SAFE-01 "before" photo still matches. We do NOT touch the animation viewer's player library — that's a separate, later, easily-reversible step (Phase 47).

Output: `package.json` + `package-lock.json` committed together (COMMIT B) with the alias; a separate post-COMMIT-B repoint commit moving every bare consumer onto `spine-core-42` + the locked TS6133 reconciliation; resolution + runtime-distinctness tests proving both copies resolve distinctly; SAFE-01 re-verified byte-equal; Plan 01's freeze-guard now hard-asserting `COMMIT A` is an ancestor of `COMMIT B`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-PATTERNS.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-CONTEXT.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-01-SUMMARY.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-REPLAN-NOTE.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/deferred-items.md

<interfaces>
<!-- The verified-live exact package.json shape and lockfile entry (RESEARCH §Standard Stack — reproduced live this session). Copy verbatim. -->
Current `package.json` `dependencies` (VERIFIED this session):
```jsonc
"@esotericsoftware/spine-core": "4.2.111",
"@esotericsoftware/spine-player": "4.2.111",
```
Target after RT-01 (the EXACT shape — RESEARCH §Standard Stack, verified-live):
```jsonc
"@esotericsoftware/spine-core": "4.3.0",                          // 4.3 CANONICAL (load-bearing direction)
"spine-core-42": "npm:@esotericsoftware/spine-core@4.2.111",      // exact-pinned alias
"@esotericsoftware/spine-player": "4.2.111"                       // NOT bumped — Phase 47 owns this
```
Lockfile entry npm auto-writes (VERIFIED verbatim this session — integrity matches `npm view @esotericsoftware/spine-core@4.2.111 dist.integrity` → `npm ci` is deterministic):
```jsonc
"node_modules/spine-core-42": {
  "name": "@esotericsoftware/spine-core",
  "version": "4.2.111",
  "resolved": "https://registry.npmjs.org/@esotericsoftware/spine-core/-/spine-core-4.2.111.tgz",
  "integrity": "sha512-dh4OOJXjLvgHtwjXDIXr4FMrWo4uvVrSugkuSckeerdxRnZVyBLmvmKDiZ915qVmMiA6IEveVHOFPULzD98K6A==",
  "license": "LicenseRef-LICENSE"
}
```
Install command (run from repo root): `npm install @esotericsoftware/spine-core@4.3.0 spine-core-42@npm:@esotericsoftware/spine-core@4.2.111`
Version facts (VERIFIED 2026-05-16): `npm view @esotericsoftware/spine-core@latest version` → 4.3.0; versions → ...4.2.116, 4.3.0 (NO beta tags ever on npm); `@4.3.0 dependencies` → none. 4.3.0 exports `Slider`, `BonePose`, `Pose`, `Posed`, `SlotPose` (Pose-architecture rewrite); these are ABSENT from 4.2.111.

Bare-consumer repoint scope (LOCKED — re-plan note §3 + the orchestrator-verified topology). The re-plan note §3's "7 src/core" list is INCOMPLETE and must NOT be followed literally (it reproduces the defect by omitting `src/main/summary.ts` + the tests in typecheck scope). The repoint scope is **every** bare `@esotericsoftware/spine-core` import site in `src/` AND `tests/`, determined by a FRESH GREP at execute time (count-free, mirrors SAFE-01 D-08 auto-discovery — a site added before execution is auto-covered). For reference (NOT a hardcoded list to hand-edit — re-derive by grep):
- PRODUCTION (8 files): `src/core/synthetic-atlas.ts`, `src/core/bounds.ts`, `src/core/sampler.ts`, `src/core/bones.ts` (`import type { Slot }`), `src/core/types.ts`, `src/core/loader.ts`, `src/core/analyzer.ts` (`import type {...}`), `src/main/summary.ts` (`import { Skeleton }` — NOT in the note's 7-file list; omitting it reproduces the defect).
- TESTS in `npm run typecheck` scope (`tsconfig.node.json` `include` covers `tests/**/*.ts` + src/main/src/core; the 42-04 CI gate `npm run typecheck` covers them): `tests/core/analyzer.spec.ts`, `tests/core/bones.spec.ts`, `tests/core/bounds-rotation-aabb.spec.ts`, `tests/core/bounds.spec.ts`, `tests/core/sampler.spec.ts`, `tests/core/synthetic-atlas.spec.ts`, `tests/main/atlas-writer.spec.ts`.
`spine-core-42` resolves to the SAME 4.2.111 the bare specifier resolves to TODAY → pure module-name rename, identical code, SAFE-01 byte-equality preserved, tests still test 4.2.

Locked TS6133 reconciliation (deferred-items.md §3 — implement exactly, do NOT choose an alternative). ONE pre-existing typecheck error predates Phase 42, verified at the 42-01 base commit: `tests/main/image-worker-rotation.spec.ts(190,13): error TS6133: 'data' is declared but its value is never read`. The re-plan note asserts "typecheck = exit 0 at COMMIT A"; this 1 error contradicts a bare "exit 0". LOCKED reconciliation: the consumer-repoint commit (Task 2) ALSO applies a one-token fix to `tests/main/image-worker-rotation.spec.ts:190` — rename the unused `data` → `_data` (or remove it) — as an **explicitly-flagged deviation** in that commit, with an inline code comment `// repoint deviation: pre-existing TS6133, see 42 deferred-items.md §3`. Net effect: after the repoint commit, `npm run typecheck` is a clean LITERAL exit 0. 42-04's CI gate asserts a literal `npm run typecheck` exit 0 with NO "modulo baseline" hedging.
</interfaces>

<analog_excerpts>
<!-- Read each at execution time before writing. -->
Current dependency idiom — `package.json:26-27`; lockfile shape — `package-lock.json` `node_modules/@esotericsoftware/spine-core` (~:1483) + `spine-player` (~:1489). (PATTERNS.md Topic E.)
Real-module / no-mock test idiom — `tests/main/sampler-worker-girl.spec.ts` (import the real production module, assert its actual behavior, no `vi.mock`). (PATTERNS.md Topic E.)
Repo-root path idiom — `tests/main/repack.loose-parity.spec.ts:42-48` (Shared S1).
Bare-specifier import idiom being repointed (read at execute time via the fresh grep) — every `import ... from '@esotericsoftware/spine-core'` / `import type ... from '@esotericsoftware/spine-core'` line across `src/` + `tests/`; ONLY the specifier token changes.
</analog_excerpts>

<ordering_invariant>
**This plan's COMMIT B MUST be a git descendant of Plan 01's SAFE-01 baseline commit (COMMIT A). The Task 2 consumer-repoint commit MUST be a git descendant of COMMIT B (same wave, immediately after).** This is automatically true with sequential wave execution (Plan 01 Wave 1; this Wave 2; COMMIT B then the repoint commit, in order). The verification below proves the A→B ancestry explicitly via `git merge-base --is-ancestor`. Do NOT amend, rebase, or reorder COMMIT A into or after COMMIT B, and do NOT fold the repoint into COMMIT B — that would void SAFE-01 or break COMMIT B purity (research Pitfall 1, ~100× recovery cost). Plan 01's `safe01-freeze-guard.spec.ts` Assertion 1 flips from skip-with-reason to a HARD ancestry assertion the moment COMMIT B introduces the literal `spine-core-42` into `package.json`.
</ordering_invariant>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the dual spine-core (4.3.0 canonical + spine-core-42 alias); commit package.json + lockfile as COMMIT B</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "Standard Stack" (the verified-live alias key, exact package.json shape, exact lockfile entry, install command, the `spine-core-42` decision rationale) AND § "Common Pitfalls" Pitfall 3 (naive-direction split-brain) + Pitfall 8 (npm-alias traps)
    - 42-CONTEXT.md "Claude's Discretion" → npm alias key literal (RESOLVED: `spine-core-42`) + the locked load-bearing DIRECTION (4.3 canonical)
    - 42-PATTERNS.md Topic E `package.json` + `package-lock.json` section
    - 42-REPLAN-NOTE.md (the authoritative cross-plan hand-off — §3 forces the COMMIT-B-pure + separate-repoint shape)
    - 42-01-SUMMARY.md — confirm the COMMIT A SHA and that package.json was 4.2.111-only there (this plan must be a descendant)
    - package.json (current deps + the `scripts` block — confirm no script needs changing) and the existing package-lock.json spine-core entry shape
  </read_first>
  <action>
**Pre-flight (the ordering invariant — do this FIRST):** Verify Plan 01's COMMIT A exists and `package.json` is currently still 4.2.111-only:
- `git log --diff-filter=A --format=%H -- tests/safe01/baselines/_manifest.json | tail -1` returns a non-empty SHA (COMMIT A exists).
- `git grep -lE 'spine-core@4\.3|spine-core-42' -- package.json` returns NOTHING (the alias is not yet present — we are about to create COMMIT B).
If COMMIT A does not exist, STOP — Plan 01 must land first (it is Wave 1; this is Wave 2). Do not proceed.

Run the verified-live install command from repo root:
```
npm install @esotericsoftware/spine-core@4.3.0 spine-core-42@npm:@esotericsoftware/spine-core@4.2.111
```
Then hand-verify `package.json` `dependencies` is EXACTLY (RESEARCH §Standard Stack — verified shape):
```jsonc
"@esotericsoftware/spine-core": "4.3.0",
"spine-core-42": "npm:@esotericsoftware/spine-core@4.2.111",
"@esotericsoftware/spine-player": "4.2.111"
```
- `@esotericsoftware/spine-core` MUST be `4.3.0` (exact, NOT `^4.3.0` — exact-pin defends against patch-shaped API/security drift, the `uniform→scaleY→scaleYMode` precedent; RESEARCH §Security Domain).
- `spine-core-42` MUST be `npm:@esotericsoftware/spine-core@4.2.111` (exact, the RESOLVED literal key — NOT a scoped `@esotericsoftware/spine-core-43` form; RESEARCH §Standard Stack rationale).
- `@esotericsoftware/spine-player` MUST remain `4.2.111` — **DO NOT bump it. Phase 47 owns the player bump.** If the install touched it, revert that line.

Confirm `package-lock.json` now has a `node_modules/spine-core-42` entry with `"name": "@esotericsoftware/spine-core"`, `"version": "4.2.111"`, and an `integrity` `sha512-...` (it should match the registry — `npm ci` reproducibility; RESEARCH verified the hash). Run `npm ci` once locally and confirm it completes with no lockfile churn (`git diff --stat package-lock.json` is empty after `npm ci`).

**Commit COMMIT B** = `package.json` + `package-lock.json` together (single commit). Commit message in PLAIN ENGLISH stating the ordering, e.g.:
> `COMMIT B: RT-01 dual spine-core — 4.3.0 canonical + spine-core-42 alias (4.2.111). Captured AFTER the SAFE-01 baseline (COMMIT A); git ancestry is the acceptance test (D-09). spine-player intentionally NOT bumped (Phase 47).`

**COMMIT B purity (this commit only):** COMMIT B itself touches ONLY `package.json` + `package-lock.json` — NO `src/**`, NO `tests/**` in COMMIT B. Do NOT touch `tests/safe01/**` anywhere in this plan. `npm run typecheck` is EXPECTED to be RED right after COMMIT B (the bare consumers now compile against 4.3 — ~58 errors); that red state is RESOLVED by Task 2's repoint commit, which lands in this same wave immediately after. Do NOT attempt to make typecheck green inside COMMIT B by editing `src/**` — that would void COMMIT B purity (re-plan note §3 locked shape).
  </action>
  <verify>
    <automated>npm ci</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('./package.json').dependencies['@esotericsoftware/spine-core']"` prints exactly `4.3.0`
    - `node -p "require('./package.json').dependencies['spine-core-42']"` prints exactly `npm:@esotericsoftware/spine-core@4.2.111`
    - `node -p "require('./package.json').dependencies['@esotericsoftware/spine-player']"` prints exactly `4.2.111` (NOT bumped)
    - `grep -c '"spine-core-42"' package-lock.json` ≥ 1 AND the `node_modules/spine-core-42` lockfile block contains `"version": "4.2.111"` and an `"integrity": "sha512-` value
    - `npm ci` completes exit 0 AND `git diff --stat package-lock.json` is empty afterward (lockfile is reproducible — no churn)
    - `git merge-base --is-ancestor "$(git log --diff-filter=A --format=%H -- tests/safe01/baselines/_manifest.json | tail -1)" "$(git log -S 'spine-core-42' --format=%H -- package.json | tail -1)"` exits 0 (COMMIT A is a git ancestor of COMMIT B — the acceptance test for the whole phase)
    - `git log -1 --stat` for COMMIT B shows ONLY `package.json` + `package-lock.json` (no `src/**`, no `tests/**` in this commit — COMMIT B purity preserved; the repoint is the NEXT commit, Task 2)
  </acceptance_criteria>
  <done>The dual-install is committed in COMMIT B with the load-bearing direction (4.3 canonical, `spine-core-42` aliased 4.2.111), spine-player untouched, the lockfile reproducible under `npm ci`, COMMIT B byte-pure (two files only), and COMMIT A machine-proven to be a git ancestor of COMMIT B. (typecheck is intentionally still red here — Task 2's repoint, next commit same wave, makes it a clean literal exit 0.)</done>
</task>

<task type="auto">
  <name>Task 2: Consumer-repoint — move every bare @esotericsoftware/spine-core import (src/ + tests/) onto spine-core-42 + the locked TS6133 reconciliation; separate commit immediately after COMMIT B</name>
  <files>every src/** + tests/** file with a bare @esotericsoftware/spine-core import (FRESH grep at execute time — count-free), plus tests/main/image-worker-rotation.spec.ts (the locked TS6133 deviation)</files>
  <read_first>
    - 42-REPLAN-NOTE.md (the authoritative hand-off — §3 is the LOCKED resolution shape: repoint as a discrete post-COMMIT-B commit with its own SAFE-01 re-verify gate; §1 = COMMIT A frozen; §4 rejected alternatives are off the table)
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/deferred-items.md (§3 — the ONE pre-existing TS6133 at `tests/main/image-worker-rotation.spec.ts:190`, the locked one-token reconciliation)
    - The file(s) being repointed — discovered by the fresh grep below (read each before editing it; the change is the specifier token ONLY)
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "Standard Stack" (the `spine-core-42` literal — NOT a scoped form) AND § "Common Pitfalls" Pitfall 3 (why direction is locked)
    - tests/arch.spec.ts (the existing no-co-mingled-imports anchor — each consumer must import exactly ONE specifier after the repoint)
  </read_first>
  <action>
This is a **separate, discrete commit, SAME wave, IMMEDIATELY AFTER COMMIT B**. It is a git descendant of COMMIT B (auto-satisfied by sequential execution). It is NOT folded into COMMIT B (COMMIT B purity is locked).

**Step 1 — fresh grep (count-free, the authoritative scope at execute time):**
```
grep -rlE "['\"]@esotericsoftware/spine-core['\"]" src/ tests/
```
This returns EVERY file that imports the bare `@esotericsoftware/spine-core` specifier in `src/` and `tests/` — the exact repoint set, re-derived NOW (a file added before execution is auto-covered; this mirrors SAFE-01 D-08 auto-discovery — do NOT rely on a hardcoded count or the re-plan note §3's incomplete "7 src/core" list, which omits `src/main/summary.ts` and the typecheck-scope tests and would REPRODUCE the defect).

**Step 2 — mechanical specifier rename (ZERO logic change):** For EVERY file the grep returned, on import/from lines ONLY, rename the specifier token `@esotericsoftware/spine-core` → `spine-core-42`. This applies to BOTH value imports (`import { Skeleton } from '@esotericsoftware/spine-core'`) AND type-only imports (`import type { Slot } from '@esotericsoftware/spine-core'`). Change NOTHING else — not import names, not bindings, not any other line. `spine-core-42` resolves to the SAME 4.2.111 the bare specifier resolved to before COMMIT B, so this is a pure module-name rename: identical code, identical runtime, SAFE-01 byte-equality preserved, tests still exercise 4.2.

**Step 3 — the locked TS6133 reconciliation (explicitly-flagged deviation):** In `tests/main/image-worker-rotation.spec.ts` at line ~190, the pre-existing `error TS6133: 'data' is declared but its value is never read` (deferred-items.md §3 — verified at the 42-01 base commit, predates Phase 42). Apply the LOCKED one-token fix: rename the unused `data` → `_data` (or remove the unused binding), and add the inline comment exactly:
```
// repoint deviation: pre-existing TS6133, see 42 deferred-items.md §3
```
This is the ONLY non-mechanical change in this commit and it is explicitly flagged. Net effect: `npm run typecheck` becomes a clean LITERAL exit 0 (the ~58 bare-orphan errors are gone via the repoint AND the 1 pre-existing TS6133 is folded — no "modulo baseline" hedging anywhere downstream; 42-04's CI gate asserts a literal exit 0).

**Step 4 — SAFE-01 re-verify gate:** Run `npx vitest run tests/safe01`. It MUST be green (byte-equal). Only the module NAME changed; the runtime is the identical 4.2.111. The SAFE-01 frozen baseline files (committed by 42-01) are NOT touched by this commit. Run `npx vitest run tests/arch.spec.ts` — green: no source file imports BOTH alias specifiers; each repointed consumer imports exactly ONE (`spine-core-42`).

**Step 5 — commit (separate, immediately after COMMIT B).** PLAIN ENGLISH message ([[user_git_experience]] / [[feedback_explain_git]]), e.g.:
> `Repoint 4.2 consumers onto the spine-core-42 nickname. The old 4.2 code used to ask for the engine by its plain name; now that the plain name points at the new 4.3 engine (previous commit), every old file asks for the engine by its explicit nickname spine-core-42 instead. Zero behavior change — the old engine is byte-for-byte the same 4.2.111. The SAFE-01 "before" photo still matches. Also folds the one pre-existing unused-variable typecheck nit in image-worker-rotation.spec.ts (flagged inline; see 42 deferred-items.md §3) so typecheck is a clean exit 0. Phase 43 later moves these direct imports behind the load.runtime.* facade.`

Do NOT touch `tests/safe01/**` source. Do NOT touch `package.json`/`package-lock.json` (those are COMMIT B, the prior commit — keeping them out of this commit preserves COMMIT B purity and keeps this commit a pure repoint).
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run tests/safe01 tests/arch.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "(from|import\()\s*['\"]@esotericsoftware/spine-core['\"]" src/ tests/` returns ZERO matches (every bare site now reads `spine-core-42`; count-free — the grep is the authoritative completeness check)
    - `npm run typecheck` exits 0 — a CLEAN LITERAL 0 (the ~58 bare-orphan errors eliminated by the repoint AND the locked `image-worker-rotation.spec.ts` TS6133 folded; NO "modulo pre-existing baseline" hedging)
    - `grep -c "repoint deviation: pre-existing TS6133, see 42 deferred-items.md §3" tests/main/image-worker-rotation.spec.ts` ≥ 1 (the locked reconciliation is present and explicitly flagged) AND `grep -cE "\bdata\b" tests/main/image-worker-rotation.spec.ts` shows the unused `data` was renamed/removed at line ~190
    - `npx vitest run tests/safe01` exits 0 (SAFE-01 byte-equal RE-VERIFIED — identical 4.2.111 runtime; only the module name changed; the 42-01 frozen baseline is untouched)
    - `npx vitest run tests/arch.spec.ts` exits 0 — no source file imports BOTH alias specifiers; each repointed consumer imports exactly ONE (`spine-core-42`)
    - `git log -1 --stat` for THIS repoint commit shows the repointed files + `tests/main/image-worker-rotation.spec.ts`, and does NOT show `package.json`/`package-lock.json` (those are COMMIT B, the prior commit) and does NOT show any `tests/safe01/**` source change — COMMIT B purity preserved, repoint commit is a clean separate descendant of COMMIT B
    - `npm run test` green (full suite — every pre-existing test, now importing `spine-core-42` = the same 4.2.111, plus the SAFE-01 baseline still byte-equal)
  </acceptance_criteria>
  <done>Every pre-existing bare `@esotericsoftware/spine-core` consumer (src/ + tests/, discovered count-free by a fresh grep) is mechanically repointed to `spine-core-42` in a separate commit immediately after COMMIT B; the locked one-token TS6133 reconciliation is folded and flagged inline; `npm run typecheck` is a clean literal exit 0; SAFE-01 is RE-VERIFIED byte-equal (4.2.111 runtime unchanged, the 42-01 frozen baseline untouched); no file co-mingles both specifiers; COMMIT B purity preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Alias-resolution + runtime-distinctness tests; confirm Plan 01 freeze-guard now hard-asserts</name>
  <files>tests/runtime/alias-resolution.spec.ts, tests/runtime/runtime-distinctness.spec.ts</files>
  <read_first>
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "Standard Stack" (the 4.3-only exports list: Slider/BonePose/Pose/Posed/SlotPose) AND § "CI Architecture" → D-13 runtime-distinctness paragraph (`adapter42.version !== adapter43.version`)
    - 42-PATTERNS.md Topic E `alias-resolution.spec.ts` + `runtime-distinctness.spec.ts` section (real-module/no-mock idiom)
    - tests/main/sampler-worker-girl.spec.ts — the real-module, no-`vi.mock` assertion idiom
    - tests/main/repack.loose-parity.spec.ts:42-48 — REPO_ROOT idiom (Shared S1)
    - tests/safe01/safe01-freeze-guard.spec.ts (from Plan 01) — its Assertion 1 now flips to hard-assert; this task confirms it
  </read_first>
  <action>
Create `tests/runtime/alias-resolution.spec.ts` (pure real-module test — NO `vi.mock`; this is allowed to import the spine-core packages directly):
- `import * as sc43 from '@esotericsoftware/spine-core';` and `import * as sc42 from 'spine-core-42';`
- Resolve each module's version. spine-core does not export a version constant directly — read it from the resolved package.json: e.g. `const v43 = require('@esotericsoftware/spine-core/package.json').version;` and `const v42 = require('spine-core-42/package.json').version;` (the alias resolves `spine-core-42` to the 4.2.111 package — `require('spine-core-42/package.json').version` returns `4.2.111`).
- Assert `v43 === '4.3.0'`, `v42 === '4.2.111'`, and `v43 !== v42`.

Create `tests/runtime/runtime-distinctness.spec.ts` (RESEARCH §Standard Stack — the 4.3-only Pose-architecture exports):
- Import both modules as above.
- Assert the 4.3 module exports `Slider`, `BonePose`, `Pose`, `Posed`, `SlotPose` (e.g. `expect(typeof sc43.Slider).not.toBe('undefined')` for each — confirm exact export names against the resolved module at execution time; RESEARCH lists `Slider`/`BonePose`/`Pose`/`Posed`/`SlotPose`).
- Assert these are ABSENT from the 4.2 module (`expect((sc42 as Record<string, unknown>).Slider).toBeUndefined()` etc.).
- Assert a structurally-named class present in BOTH (e.g. `Skeleton`) is NOT the same reference across modules (`expect(sc43.Skeleton).not.toBe((sc42 as Record<string, unknown>).Skeleton)`) — proving they are two distinct module instances, not a hoisted single copy (the Pitfall 4 dual-type-universe smoke at runtime).
Use `REPO_ROOT = path.resolve(__dirname, '..', '..')` only if any path is needed (none should be — pure module-resolution assertions).

Then **confirm the ordering invariant flipped**: run `npx vitest run tests/safe01/safe01-freeze-guard.spec.ts`. Plan 01's Assertion 1 was skip-with-reason while the alias was absent; now that COMMIT B introduced `spine-core-42` into `package.json`, `git log -S 'spine-core-42' -- package.json` returns COMMIT B and the test must HARD-PASS the `git merge-base --is-ancestor COMMIT_A COMMIT_B` assertion (no longer the vacuous skip path). Record in the SUMMARY that the guard transitioned skip→hard-pass.

Commit the two new test files (this commit is part of this plan's wave, after the repoint commit; the ordering invariant only cares that the *alias* commit (COMMIT B) is a descendant of the baseline commit — these tests are additive and post-alias by construction).
  </action>
  <verify>
    <automated>npx vitest run tests/runtime/alias-resolution.spec.ts tests/runtime/runtime-distinctness.spec.ts tests/safe01/safe01-freeze-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/runtime/alias-resolution.spec.ts tests/runtime/runtime-distinctness.spec.ts` exits 0
    - `grep -c "spine-core-42" tests/runtime/alias-resolution.spec.ts` ≥ 1 AND `grep -c "spine-core-42" tests/runtime/runtime-distinctness.spec.ts` ≥ 1 (the resolved alias literal, not a scoped form)
    - `grep -cE 'Slider|BonePose|Pose|SlotPose' tests/runtime/runtime-distinctness.spec.ts` ≥ 1 (asserts the 4.3-only Pose-architecture exports)
    - `npx vitest run tests/safe01/safe01-freeze-guard.spec.ts` exits 0 AND the run shows the ancestry assertion executed as a HARD pass (not the skip-with-reason path) — confirm by temporarily logging or by the SUMMARY note that `git log -S 'spine-core-42' -- package.json` now returns a commit
    - `npm run test` green (full suite — the new tests + every pre-existing test, including the SAFE-01 baseline which MUST still be byte-equal because Phase 42 changed no `core/` math — only the import specifier token moved in Task 2)
    - `npm run typecheck` exits 0 (clean literal 0 — the Task 2 repoint + TS6133 fix landed first)
    - `tests/arch.spec.ts` still green: no source file imports BOTH spine-core specifiers (Task 2 repointed every consumer to exactly one specifier; this task adds no `src/**` imports)
  </acceptance_criteria>
  <done>Both spine-core copies resolve to distinct versions with the 4.3-only Pose exports present only in 4.3; the SAFE-01 baseline is still byte-equal (no `core/` math change — only the Task 2 specifier rename); and Plan 01's freeze-guard has transitioned from skip-with-reason to a hard, passing git-ancestry assertion — "order is the acceptance test" is now actively enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules / lockfile | The new alias resolves a second tarball; a tampered/confused tarball is the supply-chain surface |
| committed git history → the SAFE-01 ordering invariant | COMMIT B must be a descendant of COMMIT A; a rebase/amend that reorders them voids SAFE-01 |
| 4.2-written consumers ↔ the version-flipped bare specifier | The 4.3 flip orphans every bare consumer; the repoint moves them onto the explicit 4.2.111 alias — a local module-name rename, NO new dependency/registry surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06 | Tampering | Dependency-confusion / tampered tarball on the new `spine-core-42` alias or the 4.3.0 canonical | mitigate | Committed `package-lock.json` with the `integrity` sha512 (RESEARCH verified it matches `npm view ... dist.integrity`); CI uses `npm ci` (integrity-verified), never `npm install` (enforced in Plan 04's `ci.yml`). Acceptance criterion asserts the lockfile integrity block exists and `npm ci` produces no churn |
| T-42-07 | Tampering | Patch-shaped breaking/security drift in spine-core (`^`/`~` range pulls a changed API) | mitigate | EXACT pin both (`4.3.0`, `npm:...@4.2.111` — no `^`/`~`). Acceptance criteria assert the exact literal strings. The `uniform→scaleY→scaleYMode` churn is the documented precedent for why exact-pin matters here |
| T-42-08 | Tampering | Naive alias direction (4.2 kept canonical, 4.3 aliased) → spine-player split-brain | mitigate | Direction is LOCKED 4.3-canonical; `runtime-distinctness.spec.ts` asserts `Slider`/`BonePose` are 4.3-only and the two `Skeleton` classes are distinct references — a split-brain (hoisted single copy) fails this. Acceptance criteria assert spine-player stays 4.2.111 (the inversion's tell) |
| T-42-09 | Tampering | COMMIT B rebased/amended before COMMIT A → ordering voided | mitigate | Plan 01's `safe01-freeze-guard.spec.ts` now hard-asserts `git merge-base --is-ancestor A B`; this plan's acceptance criterion runs the same check independently. CI re-runs it as an explicit visible step (Plan 04) |
| T-42-10 | Tampering | Unpinned CI action SHA | accept (deferred to Plan 04) | `ci.yml` is Plan 04; it pins every `uses:` to a full SHA (mirrors `release.yml`). Out of this plan's file scope — noted for traceability |
| T-42-22 | Tampering | The consumer-repoint silently changes behavior (not a pure module-name rename) or misses a site → a bare consumer still rides 4.3 (the original defect persists) | mitigate | The repoint is a SPECIFIER-TOKEN-ONLY rename to `spine-core-42` which resolves to the SAME 4.2.111 the bare specifier resolved to before — identical code. Completeness is a count-free fresh grep (`grep -rnE ... '@esotericsoftware/spine-core'` returns ZERO) so no site is missed (auto-covers a newly-added file). Behavior-equivalence is proven by the SAFE-01 byte-equal RE-VERIFY gate + full `npm run test`. NO new dependency or registry surface — `spine-core-42` is the already-locked, already-integrity-pinned alias from COMMIT B; the repoint is a local rename only (this is why the supply-chain surface is materially unchanged from T-42-06/07) |

ASVS L1 V2/V3/V4/V5(beyond existing loader guard)/V7 are N/A: this plan is a pure dependency-graph change + a mechanical module-name repoint + module-resolution tests, no auth/session/access-control/input-parsing surface introduced. The genuine surface is **supply-chain integrity (V6-integrity)** of the new dependency — fully mitigated by the committed integrity-pinned lockfile + `npm ci` (T-42-06/07); the repoint adds NO new registry/dependency surface (it reuses the COMMIT-B alias — T-42-22).
</threat_model>

<verification>
- `npm ci` exits 0 with no `package-lock.json` churn (reproducible dual-install)
- `npm run typecheck` exits 0 — a CLEAN LITERAL exit 0 AFTER the Task 2 repoint commit: the ~58 bare-specifier orphans are eliminated by the repoint AND the locked one-token `tests/main/image-worker-rotation.spec.ts:190` TS6133 (deferred-items.md §3) is folded into the repoint commit as an explicitly-flagged deviation. No "modulo pre-existing baseline" hedging anywhere — 42-04's CI gate asserts this same literal exit 0
- `npx vitest run tests/runtime` — both resolution + distinctness specs green
- `npx vitest run tests/safe01` — RE-VERIFIED byte-equal AFTER the repoint (only the module name changed; identical 4.2.111 runtime; the 42-01 frozen baseline files untouched)
- `grep -rnE "(from|import\()\s*['\"]@esotericsoftware/spine-core['\"]" src/ tests/` returns ZERO (every bare consumer repointed; count-free completeness check)
- `npm run test` — FULL suite green, INCLUDING `tests/safe01/safe01-baseline.spec.ts` byte-equal (proof the alias install + the specifier-only repoint did not perturb 4.2 sampler output) and `tests/safe01/safe01-freeze-guard.spec.ts` now hard-passing the ancestry assertion
- `git merge-base --is-ancestor <COMMIT_A> <COMMIT_B>` exits 0 (the phase acceptance test); the repoint commit is a git descendant of COMMIT B (sequential, same wave)
- `git log` shows COMMIT B (alias) strictly after COMMIT A (baseline), then the repoint commit strictly after COMMIT B; `@esotericsoftware/spine-player` still `4.2.111`
</verification>

<success_criteria>
- RT-01 satisfied: `@esotericsoftware/spine-core@4.3.0` canonical + `spine-core-42`→`@esotericsoftware/spine-core@4.2.111` exact-pinned alias, lockfile-committed, `npm ci`-reproducible, both resolving distinctly under vitest/Node + `tsc`
- The cross-plan defect is resolved per the locked re-plan note §3: every pre-existing bare consumer (src/ + tests/, count-free fresh-grep scope) is repointed to `spine-core-42` in a separate commit immediately after a PURE COMMIT B, with the locked TS6133 reconciliation folded — `npm run typecheck` is a clean literal exit 0 (no hedging), and SAFE-01 (frozen 42-01 baseline) is RE-VERIFIED byte-equal
- The alias DIRECTION is the load-bearing inverted one (4.3 canonical) so Phase 47's spine-player bump is a clean drop-in; spine-player itself is untouched at 4.2.111
- COMMIT B is a git descendant of COMMIT A and the repoint commit a descendant of COMMIT B (machine-verified); Plan 01's freeze-guard transitioned from skip-with-reason to a hard, passing ancestry assertion — SAFE-01's "order is the acceptance test" is now actively enforced
- The SAFE-01 baseline is still byte-equal after the install + repoint (specifier-token-only change, no `core/` math change) — proving the dual-install + repoint is plumbing-only and 4.2 behavior is genuinely frozen; Phase 43 later migrates the `spine-core-42` direct imports → the `load.runtime.*` facade (clean two-step migration)
</success_criteria>

<output>
After completion, create `.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-02-SUMMARY.md`. Record: the COMMIT B SHA, the separate repoint-commit SHA, the `git merge-base --is-ancestor COMMIT_A COMMIT_B` result (must be exit 0) and that the repoint commit is a descendant of COMMIT B, the exact count-free set of files the fresh grep repointed (and explicit confirmation `src/main/summary.ts` + the typecheck-scope tests were included — the re-plan note §3's "7 src/core" list was incomplete), confirmation the locked TS6133 reconciliation was applied + flagged inline, that `npm run typecheck` is a clean literal exit 0, that SAFE-01 RE-VERIFIED byte-equal (42-01 frozen baseline untouched), explicit confirmation that `spine-player` is still 4.2.111, the resolved versions of both spine-core specifiers, and that Plan 01's freeze-guard flipped skip→hard-pass.
</output>
</content>
</invoke>
