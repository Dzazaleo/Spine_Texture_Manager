---
phase: 42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/safe01/canonical-json.ts
  - tests/safe01/canonical-json.spec.ts
  - tests/safe01/discover-fixtures.ts
  - tests/safe01/safe01-baseline.spec.ts
  - tests/safe01/safe01-enumeration.spec.ts
  - tests/safe01/safe01-freeze-guard.spec.ts
  - tests/safe01/baselines/_manifest.json
  - tests/safe01/baselines/
  - .gitignore
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - "Running `npx vitest run tests/safe01/safe01-baseline.spec.ts` on the unchanged 4.2.111 runtime passes: every git-tracked fixture that samples produces output byte-identical to its committed canonical baseline JSON"
    - "A new or silently-dropped sampling fixture fails `tests/safe01/safe01-enumeration.spec.ts` (the discovered set must equal the committed manifest — D-08 dropout-is-failure)"
    - "The canonical serializer surfaces NaN/Infinity/-0 as string sentinels (never null/0) and is byte-stable across two serializations (D-07 silent-corruption guard, proven by canonical-json.spec.ts)"
    - "The SAFE-01 baseline is captured and committed while package.json still pins ONLY @esotericsoftware/spine-core@4.2.111 (no alias yet — this commit is COMMIT A, the git ancestor of the RT-01 alias commit)"
    - "There is NO UPDATE_FIXTURES / process.env-gated regen branch anywhere in the SAFE-01 specs (D-09 — regenerating requires deliberately deleting the freeze guard)"
  artifacts:
    - path: "tests/safe01/canonical-json.ts"
      provides: "Deterministic recursive sorted-key SamplerOutput serializer with explicit non-finite/-0 string-sentinel handling and 15-sig-digit float clamp"
      min_lines: 40
    - path: "tests/safe01/discover-fixtures.ts"
      provides: "Auto-discovery walking fixtures/**/*.json with the loadSkeleton+sampleSkeleton 'samples OK' predicate; two-tier (git-tracked vs gitignored heavy rigs per D-08-R)"
      min_lines: 30
    - path: "tests/safe01/safe01-baseline.spec.ts"
      provides: "Strict toEqual of canonical(SamplerOutput) vs committed per-fixture JSON; NO UPDATE_FIXTURES branch"
      min_lines: 30
    - path: "tests/safe01/safe01-enumeration.spec.ts"
      provides: "Discovered git-tracked set == committed _manifest.json sorted set (dropout-is-failure)"
      min_lines: 20
    - path: "tests/safe01/safe01-freeze-guard.spec.ts"
      provides: "D-09 git-ancestry assertion (skip-with-reason until alias exists) + no-regen meta-test grepping safe01-baseline.spec.ts"
      min_lines: 30
    - path: "tests/safe01/baselines/_manifest.json"
      provides: "Committed enumerated git-tracked fixture set + _meta provenance (generatedCommit/generatedAt) — the single ancestry-checked artifact"
      contains: "_meta"
  key_links:
    - from: "tests/safe01/safe01-baseline.spec.ts"
      to: "tests/safe01/canonical-json.ts + tests/safe01/discover-fixtures.ts"
      via: "imports canonicalize() and discover(); compares against tests/safe01/baselines/<DIR>__<BASE>.json"
      pattern: "canonicaliz|discover"
    - from: "tests/safe01/discover-fixtures.ts"
      to: "src/core/loader.ts + src/core/sampler.ts"
      via: "loadSkeleton(f) then sampleSkeleton(load) as the 'samples OK' predicate (version-reject fixtures throw → excluded)"
      pattern: "loadSkeleton|sampleSkeleton"
    - from: "tests/safe01/safe01-freeze-guard.spec.ts"
      to: "git history"
      via: "git log --diff-filter=A on _manifest.json + git log -S 'spine-core-42' on package.json + git merge-base --is-ancestor"
      pattern: "merge-base.*is-ancestor"
---

<objective>
Capture the byte-equal 4.2 golden snapshot of the full `SamplerOutput` for every git-tracked in-repo fixture that samples through the **unchanged** spine-core 4.2.111 runtime, and commit it as **COMMIT A** — the git ancestor of the not-yet-existing RT-01 npm-alias commit. This is SAFE-01: the single most load-bearing de-risking artifact of the entire v1.6 milestone.

Purpose: "Order is the acceptance test" (D-09). You cannot baseline behavior after you change it. If the alias lands first, the baseline freezes already-refactored behavior and the regression gate validates nothing — a silent ~8% undersize for the entire paying 4.2 user base, recoverable only at ~100× cost (research Pitfall 1/5). This plan MUST land — committed — before Plan 02 touches `package.json`.

Plain-English (project owner is a git beginner — [[user_git_experience]] / [[feedback_explain_git]]): Think of this like a notarized "before" photograph. We photograph exactly what the app computes today, sign and date the photo, and lock it in a safe. Later, when we add the new Spine 4.3 engine, the build will automatically refuse to proceed unless this photo was taken FIRST (proven by Git's own commit history). Taking the photo after we start renovating would make it worthless. This plan is the photograph; Plan 02 is the renovation. Git records the order automatically — no human has to remember it.

Output: A committed `tests/safe01/` harness (pure-Node test utilities — NEVER in `core/`, preserving CLAUDE.md Fact #5 / RT-04) + per-fixture committed canonical baseline JSON + an enumeration manifest, all in one commit while spine-core is still 4.2.111-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-PATTERNS.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-CONTEXT.md
@.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-VALIDATION.md

<interfaces>
<!-- The exact SamplerOutput shape the baseline freezes (D-06). From src/core/sampler.ts:119-123 — read at execution time, do NOT re-derive. -->
From src/core/sampler.ts:97-123:
```typescript
export interface PeakRecord extends SampleRecord {
  isSetupPosePeak: boolean;            // true if no animation timeline touched this attachment
}
export interface SamplerOutput {
  globalPeaks: Map<string, PeakRecord>;     // key: `${skinName}/${slotName}/${attachmentName}`
  perAnimation: Map<string, PeakRecord>;    // key: `${animationName}/${skinName}/${slotName}/${attachmentName}`
  setupPosePeaks: Map<string, PeakRecord>;  // key: `${skinName}/${slotName}/${attachmentName}`
}
```
`PeakRecord extends SampleRecord` (src/core/types.ts:192-237). Numeric fields to freeze byte-exact: `time, frame, peakScaleX, peakScaleY, peakScale, worldW, worldH, sourceW, sourceH`. String keys: `attachmentKey, skinName, slotName, attachmentName, regionName?, animationName`. Booleans: `isSetupPosePeak, isSequenceFrame?`. (Read src/core/types.ts:192-237 at execution time for the exact field list — do not guess.)

Sampler entry points the discovery predicate calls (read src/core/loader.ts + src/core/sampler.ts public exports at execution time):
- `loadSkeleton(skeletonPath: string): Promise<LoadResult>` (or sync — confirm signature) from src/core/loader.ts — version-reject fixtures throw `SpineVersionUnsupportedError` HERE.
- `sampleSkeleton(load, opts?): SamplerOutput` from src/core/sampler.ts — produces the three Maps. Default samplerHz 120 (CLAUDE.md Fact #6 / SamplerOptions default at sampler.ts:84-87).

The 13 git-tracked fixtures (`git ls-files 'fixtures/**/*.json'`): Chicken-Min, EXPORT_PROJECT/EXPORT, INHERIT_TIMELINE/INHERIT_TEST, SIMPLE_PROJECT/SIMPLE_TEST, SIMPLE_PROJECT/SIMPLE_TEST_GHOST, SIMPLE_PROJECT/skeleton, SIMPLE_PROJECT/skeleton2, SIMPLE_PROJECT_NO_ATLAS/SIMPLE_TEST, SIMPLE_PROJECT_NO_ATLAS_MESH/MeshOnly_TEST, SPINE_3_8_TEST/SPINE_3_8_TEST, SPINE_4_3_TEST/SPINE_4_3_TEST, spine_rotated/EXPORT/skeleton, spine_stripWS/EXPORT/skeleton. SPINE_3_8_TEST + SPINE_4_3_TEST throw `SpineVersionUnsupportedError` at `loadSkeleton` → naturally excluded by the predicate (no hand-list — D-08). The other ~11 are candidates; the predicate decides (some sidecars like skeleton2.json / SIMPLE_TEST_GHOST.json may be partial — let the predicate exclude non-sampling ones, then review the manifest once at capture so the committed set is intentional, per RESEARCH §Fixture Auto-Discovery sidecar note).
</interfaces>

<analog_excerpts>
<!-- The proven in-repo precedents to copy the SHAPE of. Read each analog file at execution time before writing. -->
Baseline-load + missing-key→throw + repo-root idiom — `tests/main/repack.loose-parity.spec.ts:42-48, 69-80, 205-216` (PATTERNS.md Topic A). COPY the shape; DO NOT copy `SHOULD_UPDATE = process.env.UPDATE_FIXTURES === '1'` (line 49) or `saveBaselines()` — D-09 forbids any regen path.
Glob + per-file predicate loop — `tests/arch.spec.ts:150, 167-176` (PATTERNS.md Topic B / Shared S2).
Skip-until-exists-then-assert-hard (for the freeze-guard's git-ancestry until the alias commit exists) — `tests/arch.spec.ts:205-216` ENOENT-tolerant early-return (PATTERNS.md Topic F / Shared S3).
`_meta` provenance header — `tests/fixtures/repack-baselines.json:2-9` (PATTERNS.md Topic A). SAFE-01 diverges: NO `note` advertising a regen command; ADD `generatedCommit` + `generatedAt`.
Pure-module + explicit-edge-case + typed-error idiom (module SHAPE only, lives in tests/ not core/) — `src/core/png-header.ts:20-35, 52-54` (PATTERNS.md Topic A — the endianness silent-corruption comment is the exact discipline the NaN/-0 handling needs).
Anti-pattern reference (do NOT build a SAFE-01 equivalent) — `scripts/repack-refresh-baselines.mjs:144-153` (the `UPDATE_FIXTURES: '1'` spawnSync escape hatch SAFE-01 must lack).
</analog_excerpts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic canonical-JSON serializer + round-trip self-test (the Nyquist seam)</name>
  <files>tests/safe01/canonical-json.ts, tests/safe01/canonical-json.spec.ts</files>
  <read_first>
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "Canonical-JSON Serialization (D-07 — RESOLVED)" — the verbatim resolved scheme; copy it exactly
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-PATTERNS.md Topic A `tests/safe01/canonical-json.ts` and `canonical-json.spec.ts` sections
    - src/core/png-header.ts:20-54 — the pure-module + explicit-edge-case-comment + typed-error SHAPE analog (this file lives in tests/, NOT core/ — RESEARCH §Recommended Project Structure note)
    - src/core/sampler.ts:97-123 — the SamplerOutput / PeakRecord shape being serialized
    - src/core/types.ts:192-237 — the exact SampleRecord numeric/string/boolean field list
    - tests/main/repack.loose-parity.spec.ts:42-48 — the REPO_ROOT path-resolution idiom (Shared S1)
  </read_first>
  <behavior>
    - Test: canonicalizing an object with `NaN` emits the string `"NaN"` (NOT null) at that key
    - Test: `Infinity` emits `"Infinity"`, `-Infinity` emits `"-Infinity"`
    - Test: `Object.is(x, -0)` emits `"-0"` (NOT `0`)
    - Test: a finite number is emitted via `Number(x.toPrecision(15))` (last-bit float noise neutralized; 1e-9 peak-latch epsilon and 1e-3 user tolerance both preserved — they are far above 15-sig-digit ambiguity)
    - Test: keys are sorted recursively at EVERY depth (not just top-level — a deeply nested `{b,a}` serializes as `{a,b}`)
    - Test: a `Map<string,PeakRecord>` round-trips to a sorted-key plain object
    - Test: two successive `canonicalize()` calls on the same input produce byte-identical strings (determinism)
  </behavior>
  <action>
Create `tests/safe01/canonical-json.ts` — a pure-Node test utility (NO import from `src/core/**`; NO `sharp`/`electron`; `node:`-builtins only if needed). Export `canonicalize(out: { globalPeaks: Map<string, unknown>; perAnimation: Map<string, unknown>; setupPosePeaks: Map<string, unknown> }, meta: Record<string, unknown>): string`.

Implement EXACTLY the RESEARCH §Canonical-JSON RESOLVED scheme (copy verbatim — do not re-derive):
1. **Map → sorted-key plain object.** For each of the three maps, sort entries by key ascending; emit as a plain object with keys inserted in sorted order.
2. **Recursive key sort at EVERY depth.** Write an explicit recursive canonicalizer that rebuilds every nested object with `Object.keys(o).sort()` ordering (the shallow `JSON.stringify(v, Object.keys(v).sort())` form sorts ONLY top-level — insufficient for nested PeakRecord; this is verified in RESEARCH §A bug). Then `JSON.stringify(canonical, null, 2)`.
3. **Explicit non-finite + signed-zero handling — the silent-corruption guard.** Before serialization, walk every number value:
   - `Number.isNaN(x)` → emit the string sentinel `"NaN"` (NEVER `null` — `null` reads as "no data" and would let a silent-undersize bug producing NaN false-pass; this is the exact failure mode SAFE-01 exists to catch).
   - `x === Infinity` → `"Infinity"`; `x === -Infinity` → `"-Infinity"`.
   - `Object.is(x, -0)` → `"-0"` (preserves sign; plain `JSON.stringify` collapses `-0`→`0`).
   - Finite: `Number(x.toPrecision(15))` then default JSON formatting.
   Add an explicit code comment naming the trap, in the same discipline as `png-header.ts`'s endianness comment (e.g. `// JSON.stringify(NaN)→"null", (-0)→"0" silently — a silent-undersize regression producing a non-finite would false-pass the SAFE-01 gate. String sentinels make it unmissable in git diff.`).
4. Output the per-fixture file body: `{ "_meta": {...meta}, "globalPeaks": {...}, "perAnimation": {...}, "setupPosePeaks": {...} }` with the `_meta` block FIRST.

Then create `tests/safe01/canonical-json.spec.ts` — the round-trip determinism self-test (RESEARCH §Canonical-JSON "Round-trip self-test"). Canonicalize a synthetic object containing `NaN`, `Infinity`, `-Infinity`, `-0`, `1e-9`, and a deeply-nested `{ z: { b: 1, a: 2 } }`; assert (a) output is byte-stable across two `canonicalize()` calls, (b) each sentinel string literally appears in the output, (c) nested keys are sorted. Use the `REPO_ROOT = path.resolve(__dirname, '..', '..')` idiom from `repack.loose-parity.spec.ts:42-48` if any path is needed (none should be — this is a pure in-test fixture, no baseline file).

This is the Nyquist seam: the serializer must be proven deterministic BEFORE it is trusted to gate `core/` output. It has no dependency on the baseline files (Task 2) so it lands and passes first.
  </action>
  <verify>
    <automated>npx vitest run tests/safe01/canonical-json.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/safe01/canonical-json.spec.ts` exits 0 with all assertions passing
    - `tests/safe01/canonical-json.ts` exists; `grep -E "from ['\"](sharp|electron)['\"]|from ['\"]\.\./\.\./src/core" tests/safe01/canonical-json.ts` returns NO matches (pure test utility, not in core/)
    - `grep -c '"NaN"' tests/safe01/canonical-json.ts` ≥ 1 AND `grep -c '"-0"' tests/safe01/canonical-json.ts` ≥ 1 (string sentinels present, not null)
    - `grep -cE 'toPrecision\(15\)' tests/safe01/canonical-json.ts` ≥ 1 (the 15-sig-digit float clamp)
    - `grep -vE '^\s*(//|\*|/\*)' tests/safe01/canonical-json.spec.ts | grep -cE 'NaN|Infinity|-0' ` ≥ 1 (the self-test exercises the sentinel cases in code, not just comments)
    - `npm run test` still green (no pre-existing test broken)
  </acceptance_criteria>
  <done>The canonical serializer is deterministic, surfaces every non-finite/-0 case as a distinct string sentinel (never null/0), sorts keys recursively, and its self-test passes — proving the gate mechanism before it gates anything.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture auto-discovery + COMMIT-A capture of per-fixture baselines + enumeration manifest</name>
  <files>tests/safe01/discover-fixtures.ts, tests/safe01/safe01-baseline.spec.ts, tests/safe01/safe01-enumeration.spec.ts, tests/safe01/baselines/_manifest.json, tests/safe01/baselines/ (per-fixture *.json), .gitignore</files>
  <read_first>
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "Fixture Auto-Discovery (D-08 — RESOLVED)" (incl. the Option A two-tier resolution and the discovery-predicate pseudocode) AND § "Canonical-JSON Serialization" point 4/5 (per-fixture file layout + `_manifest.json` schema)
    - 42-CONTEXT.md D-08 and D-08-R (the locked two-tier discovery — git-tracked subset committed + green on fresh clone; gitignored heavy rigs baselined locally + gitignored + `it.skipIf`-guarded)
    - 42-PATTERNS.md Topic B (`discover-fixtures.ts`, `safe01-baseline.spec.ts`, `safe01-enumeration.spec.ts`) and Topic E `it.skipIf` precedent
    - tests/main/repack.loose-parity.spec.ts:69-80, 205-216 — baseline-load + missing-key→throw idiom (copy shape; NO UPDATE_FIXTURES)
    - tests/arch.spec.ts:150, 167-176 — globSync + predicate-loop idiom
    - tests/main/sampler-worker-girl.spec.ts:23-28 — the `it.skipIf(process.env.CI)` gitignored-heavy-rig precedent ([[feedback_gitignore_fixtures_check_test_refs]])
    - src/core/loader.ts (public `loadSkeleton` signature) + src/core/sampler.ts (`sampleSkeleton` signature, SamplerOptions default 120 Hz)
    - .gitignore (the heavy-rig exclusions: fixtures/Girl/, fixtures/SKINS/, fixtures/CHJ/, fixtures/3Queens/, fixtures/Jokerman/, etc.)
  </read_first>
  <action>
Create `tests/safe01/discover-fixtures.ts` (pure-Node test utility — NO `src/core` import path except importing the public `loadSkeleton`/`sampleSkeleton` entrypoints, which is allowed; NO sharp/electron). Export `discover(): { included: { fixture: string; output: SamplerOutput }[]; excluded: { fixture: string; reason: string }[] }`:
- `globSync('fixtures/**/*.json')` (use `node:fs` `globSync` as in arch.spec.ts:150).
- For each: run the "samples OK" predicate — `loadSkeleton(f)` then `sampleSkeleton(load)` (default 120 Hz). On success: include + record canonical output. On throw (version-reject fixtures `SPINE_3_8_TEST`/`SPINE_4_3_TEST` throw `SpineVersionUnsupportedError` at `loadSkeleton` — verified RESEARCH): exclude + record the reason string (do NOT silently drop — RESEARCH §Fixture Auto-Discovery "record reason in a discovery log for human review").
- **Two-tier (D-08-R Option A):** expose which discovered fixtures are git-tracked vs gitignored. Determine git-tracked membership by running `git ls-files <fixture>` (via `node:child_process` `execFileSync`, test-side only) OR by a committed allow-list derived from `git ls-files 'fixtures/**/*.json'` — pick the `execFileSync('git', ['ls-files', f])` approach (robust, no hand-list, consistent with D-08 "no hand-maintained list"; empty stdout ⇒ gitignored).

Then capture **COMMIT A**:
1. Run `discover()`. For each **git-tracked** included fixture, write `tests/safe01/baselines/<DIR>__<JSONBASENAME>.json` (path-sep `/`→`__`, e.g. `SIMPLE_PROJECT__SIMPLE_TEST.json`) using `canonicalize()` from Task 1 with `_meta = { fixture, generatedCommit: <git rev-parse HEAD>, generatedAt: <ISO>, samplerHz: 120, spineCoreVersion: "4.2.111", schema: "safe01/v1" }`.
2. For each **gitignored heavy-rig** included fixture present locally, write the same `<DIR>__<BASE>.json` into `tests/safe01/baselines/` BUT add a `.gitignore` rule so those specific heavy-rig baseline files are NOT committed (e.g. append patterns covering the gitignored-rig basenames under `tests/safe01/baselines/`). Net per D-08-R: fresh-clone CI sees ONLY the git-tracked baselines (deterministic, green); a developer with heavy rigs gets local drift coverage.
3. Write `tests/safe01/baselines/_manifest.json` = `{ "_meta": { generatedCommit, generatedAt, spineCoreVersion: "4.2.111", schema: "safe01/v1" }, "fixtures": [ <sorted list of git-tracked included fixture paths> ] }`. This is the single ancestry-checked artifact (D-09) + the D-08 enumeration target. Heavy/gitignored rigs are NOT listed in the committed manifest.

Then create `tests/safe01/safe01-baseline.spec.ts`:
- `REPO_ROOT = path.resolve(__dirname, '..', '..')` (Shared S1).
- Re-run `discover()`. For each git-tracked included fixture, `expect(canonicalize(out, ...)).toEqual(<committed baseline file content>)` — **strict full-JSON equality, NOT a digest** (D-07: a tripped gate must show exactly which `${skin}/${slot}/${attachment}` record drifted in `git diff`). Compare the parsed objects EXCLUDING the volatile `_meta` block (the `_meta.generatedCommit`/`generatedAt` differ per run by design — assert structural equality of `globalPeaks`/`perAnimation`/`setupPosePeaks` only; `_meta` is provenance, not frozen data).
- For gitignored heavy rigs: a `describe`/`it` block guarded so it runs ONLY when the fixture is present locally — use the `it.skipIf` precedent from `sampler-worker-girl.spec.ts:23-28` (skip when the fixture path does not exist on disk). When present, same strict `toEqual` vs the (gitignored, local) baseline file.
- **Missing-baseline → throw** with a diagnostic message (copy the `repack.loose-parity.spec.ts:69-80` shape) — but the message must NOT advertise an `UPDATE_FIXTURES` command (D-09). Instead: "SAFE-01 baseline missing for <fixture>. This baseline is frozen (D-09); a new sampling fixture's baseline is added by a deliberate, reviewed commit — NOT a regen env var."
- **CRITICAL — NO regen branch:** there must be NO `process.env.UPDATE_FIXTURES` / `SHOULD_UPDATE` / write-baseline path anywhere in this spec (D-09; the freeze-guard meta-test in Task 3 greps this file for exactly that).

Then create `tests/safe01/safe01-enumeration.spec.ts`: re-run `discover()`, take the sorted git-tracked included set, assert it deep-equals `_manifest.json`'s `fixtures` array (sorted). A previously-sampling fixture that starts throwing drops out of `discovered` → set diverges → test fails loudly (D-08 dropout-is-failure). A new sampling fixture appears in `discovered` not `manifest` → fails until its baseline + manifest entry are added (the only sanctioned baseline-addition path; pre-existing baselines stay frozen).

Review the committed manifest once: confirm the included set is intentional (sidecars `skeleton2.json` / `SIMPLE_TEST_GHOST.json` etc. are in or out by genuine sampling behavior, not accident — RESEARCH §Fixture Auto-Discovery sidecar note). Record the intentional set in the plan SUMMARY.

**Do NOT modify `src/core/**` anywhere in this plan. Do NOT touch `package.json`.** spine-core MUST remain `4.2.111`-only through this entire commit (this is COMMIT A; the alias is Plan 02 / COMMIT B).
  </action>
  <verify>
    <automated>npx vitest run tests/safe01/safe01-baseline.spec.ts tests/safe01/safe01-enumeration.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/safe01/safe01-baseline.spec.ts tests/safe01/safe01-enumeration.spec.ts` exits 0 (every git-tracked sampling fixture is byte-equal to its committed baseline; discovered set == manifest)
    - `tests/safe01/baselines/_manifest.json` exists and contains a `_meta` object with `generatedCommit` and `generatedAt` keys AND a `fixtures` array (≥ 1 entry; `git ls-files tests/safe01/baselines/_manifest.json` returns the path = it is git-tracked)
    - At least `tests/safe01/baselines/SIMPLE_PROJECT__SIMPLE_TEST.json` exists and is git-tracked (`git ls-files` returns it) and contains the keys `globalPeaks`, `perAnimation`, `setupPosePeaks`
    - `grep -rE 'UPDATE_FIXTURES|SHOULD_UPDATE|process\.env' tests/safe01/safe01-baseline.spec.ts` returns NO matches (D-09 no-regen)
    - `git grep -lE 'spine-core@4\.3|spine-core-42|npm:@esotericsoftware' -- package.json` returns NO matches (spine-core is STILL 4.2.111-only — this is COMMIT A, before the alias)
    - `git diff --name-only` (post-implementation, pre-commit) shows ONLY files under `tests/safe01/` and `.gitignore` — NO `src/core/**`, NO `package.json`, NO `package-lock.json`
    - `npm run test` green (the existing regression suite still passes — Phase 42 broke nothing)
  </acceptance_criteria>
  <done>Every git-tracked fixture that samples through the unchanged 4.2.111 runtime has a committed canonical baseline; the discovered set is enumerated and asserted (dropout = failure); heavy/proprietary rigs are gitignored + presence-guarded per D-08-R; spine-core is still 4.2.111-only.</done>
</task>

<task type="auto">
  <name>Task 3: D-09 freeze guard — git-ancestry assertion (skip-until-alias) + no-regen meta-test</name>
  <files>tests/safe01/safe01-freeze-guard.spec.ts</files>
  <read_first>
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-RESEARCH.md § "Git-Ancestor Assertion (D-09 — RESOLVED)" — the verbatim mechanism (the three git commands + the vitest sketch + the robustness notes); copy it exactly
    - 42-CONTEXT.md D-09 (machine-checked freeze enforcement; NO casual UPDATE_FIXTURES regen)
    - 42-PATTERNS.md Topic F `safe01-freeze-guard.spec.ts` section (analog + the skip-with-reason-until-exists idiom)
    - tests/arch.spec.ts:205-216 — the ENOENT-tolerant skip-then-assert-hard precedent (Shared S3; apply the same "not yet introduced → vacuously satisfied, skip" discipline to git history, not files)
    - tests/safe01/safe01-baseline.spec.ts (from Task 2 — this guard greps it for the absence of any regen branch)
  </read_first>
  <action>
Create `tests/safe01/safe01-freeze-guard.spec.ts`. Two assertions, copying the RESEARCH §Git-Ancestor Assertion RESOLVED mechanism verbatim:

**Assertion 1 — git-ancestry (D-09, the acceptance test for the whole phase):**
```typescript
import { execFileSync } from 'node:child_process';
const sh = (a: string[]) => execFileSync('git', a, { encoding: 'utf8' }).trim();

it('SAFE-01: the baseline commit is a git ancestor of the npm-alias commit (D-09)', () => {
  const baselineCommit = sh(['log','--diff-filter=A','--format=%H','--','tests/safe01/baselines/_manifest.json'])
    .split('\n').filter(Boolean).pop();   // OLDEST add (tail) — the original introduction
  const aliasLog = sh(['log','-S','spine-core-42','--format=%H','--','package.json']);
  const aliasCommit = aliasLog.split('\n').filter(Boolean).pop();
  if (!aliasCommit) {
    // The alias is introduced in Plan 02 / COMMIT B, AFTER this baseline (COMMIT A).
    // Until then the pickaxe returns empty → ancestry is VACUOUSLY satisfied.
    // Skip-with-reason (the arch.spec.ts:205-216 ENOENT-tolerant precedent applied
    // to git history), NOT fail. Flips to a hard assert once RT-01 lands.
    return;
  }
  if (!baselineCommit) throw new Error('SAFE-01 manifest has no introducing commit — baseline not committed?');
  // execFileSync throws on non-zero exit → an ancestry violation throws → test fails.
  expect(() =>
    execFileSync('git', ['merge-base','--is-ancestor', baselineCommit, aliasCommit])
  ).not.toThrow();
});
```
Robustness (RESEARCH §Git-Ancestor robustness notes): `git merge-base --is-ancestor` exit 0 = is-ancestor (the exact topological primitive — NOT timestamp comparison; timestamps are non-monotonic across rebases). `git log -S 'spine-core-42' -- package.json` finds the alias-introducing commit (the alias key is the chosen unique literal — RESEARCH §Standard Stack — appears exactly once). `| tail`/`.pop()` after `--diff-filter=A` takes the OLDEST add (defends against a later delete+re-add). Add a secondary cross-check: assert `_manifest.json`'s `_meta.generatedCommit` equals OR is an ancestor of `baselineCommit` (a regenerated-content-without-fresh-commit tamper signal).

**Assertion 2 — no-regen meta-test (D-09):**
```typescript
it('SAFE-01: there is NO UPDATE_FIXTURES/process.env-gated regen branch in the baseline spec (D-09)', () => {
  const src = readFileSync(path.resolve(__dirname, 'safe01-baseline.spec.ts'), 'utf8');
  expect(src, 'SAFE-01 baseline spec must not contain an env-gated regen path (D-09 — regen requires deliberately deleting this guard)')
    .not.toMatch(/UPDATE_FIXTURES|process\.env|SHOULD_UPDATE/);
});
```
This makes "regenerating SAFE-01 requires deliberately removing the freeze guard" structurally true — there is no env-var path (unlike `repack.loose-parity.spec.ts:49`). Regeneration = a human deliberately editing committed JSON, and the ancestry/`generatedCommit` cross-check noticing the introducing-commit ↔ recorded-SHA mismatch.

This file is part of COMMIT A. At commit time the alias does not exist → Assertion 1 skips-with-reason (vacuously satisfied) and Assertion 2 asserts hard. After Plan 02 / COMMIT B lands the alias, Assertion 1 flips to a hard ancestry assertion automatically with no edit.

**Still no `src/core/**` or `package.json` changes.** This is the last task of COMMIT A.
  </action>
  <verify>
    <automated>npx vitest run tests/safe01/safe01-freeze-guard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/safe01/safe01-freeze-guard.spec.ts` exits 0 (Assertion 1 skips-with-reason because the alias commit does not exist yet — COMMIT A precedes COMMIT B; Assertion 2 passes hard)
    - `grep -cE "merge-base.*--is-ancestor" tests/safe01/safe01-freeze-guard.spec.ts` ≥ 1
    - `grep -cE "diff-filter=A" tests/safe01/safe01-freeze-guard.spec.ts` ≥ 1 AND `grep -cE "-S.*spine-core-42|'spine-core-42'" tests/safe01/safe01-freeze-guard.spec.ts` ≥ 1 (pins to the resolved alias literal, not package-lock.json)
    - `grep -cE 'not\.toMatch.*UPDATE_FIXTURES|UPDATE_FIXTURES' tests/safe01/safe01-freeze-guard.spec.ts` ≥ 1 (the no-regen meta-test exists)
    - `git grep -lE 'spine-core@4\.3|spine-core-42' -- package.json` returns NO matches (still COMMIT A — alias absent)
    - `npm run typecheck` exits 0 AND `npm run test` green
    - Commit message for COMMIT A states the SAFE-01-baseline-predates-alias ordering in plain English (e.g. "COMMIT A: SAFE-01 4.2 byte baseline — captured BEFORE the npm alias; git ancestry is the acceptance test")
  </acceptance_criteria>
  <done>The freeze guard is committed in COMMIT A; it skips-with-reason while the alias is absent and will hard-assert git ancestry the moment Plan 02 introduces `spine-core-42` — making "order is the acceptance test" machine-enforced, not reviewer memory; and it structurally forbids a casual regen path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed git history → CI/regression gate | The D-09 ancestry invariant is only as strong as git history integrity; a baseline regenerated without a fresh commit, or an ordering bypass, would silently void SAFE-01 |
| fixture file → sampler (existing, unchanged) | Skeleton JSON is parsed by spine-core 4.2.111's `SkeletonJson` (existing trust boundary — Phase 42 does NOT modify it; the loader version-guard stays the parse boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | SAFE-01 baseline regenerated silently (the existential phase threat) | mitigate | `safe01-freeze-guard.spec.ts` Assertion 2 greps `safe01-baseline.spec.ts` for any `UPDATE_FIXTURES`/`process.env` regen branch and asserts ABSENT; the `_meta.generatedCommit` ↔ introducing-commit cross-check flags content regenerated without a fresh commit. No `scripts/safe01-refresh-*.mjs` is created (the `repack-refresh-baselines.mjs` escape hatch is deliberately NOT replicated — D-09) |
| T-42-02 | Tampering | Baseline captured after (or in the same commit as) the alias → gate validates nothing | mitigate | `safe01-freeze-guard.spec.ts` Assertion 1: `git merge-base --is-ancestor <baseline> <alias>` (machine-checked topological primitive, not timestamps). Acceptance criterion asserts `package.json` is still 4.2.111-only at COMMIT A. CI re-runs this as an explicit step (Plan 04) |
| T-42-03 | Information disclosure | A proprietary/licensed heavy rig (Jokerman, Girl, SKINS) committed via its baseline file | mitigate | D-08-R Option A: heavy-rig baseline files are gitignored; only the git-tracked redistributable subset is committed and listed in `_manifest.json`. Acceptance criterion: `git diff --name-only` shows only `tests/safe01/**` + `.gitignore`; `git ls-files` confirms heavy-rig baselines are NOT tracked |
| T-42-04 | Information disclosure (silent) | A silent-undersize regression producing `NaN`/`Infinity`/`-0` serialized as `null`/`0` → gate false-passes | mitigate | `canonical-json.ts` emits distinct string sentinels (`"NaN"`/`"Infinity"`/`"-0"`), never `null`/`0`; `canonical-json.spec.ts` self-test proves the sentinels appear and the serializer is byte-deterministic before it gates `core/` |
| T-42-05 | Spoofing | Malformed/hostile skeleton version string | accept (out of Phase 42 scope) | Loader dispatch is Phase 44. Phase 42 does NOT modify `loader.ts`; the existing strict `major.minor` parse guard remains the boundary. Noted so this plan does not accidentally widen it — no loader change here |

ASVS L1 categories V2 (authn), V3 (session), V4 (access control), V6-of-secrets, V7 (error handling beyond existing typed errors) are genuinely N/A: this is a pure in-process test/serialization plan with no network, no auth, no secrets, no new external surface. The only real surface is **supply-chain/integrity of the regression invariant itself** (T-42-01/02, mitigated above).
</threat_model>

<verification>
- `npx vitest run tests/safe01` — all SAFE-01 specs green (baseline byte-equal, enumeration matches manifest, canonical self-test deterministic, freeze-guard skips-with-reason + no-regen meta-test passes)
- `npm run typecheck` exits 0 (no type regression)
- `npm run test` — full pre-existing suite still green (Phase 42 Plan 01 added only test files + baselines + a .gitignore rule; broke nothing)
- `git ls-files 'fixtures/**/*.json'` cross-checked against `_manifest.json` `fixtures[]` — the committed set is exactly the git-tracked subset that samples (version-reject + non-sampling sidecars excluded; intentional set recorded in SUMMARY)
- `git log -1 --stat` for COMMIT A shows ONLY `tests/safe01/**` + `.gitignore` — no `src/core/**`, no `package.json`/`package-lock.json`
</verification>

<success_criteria>
- SAFE-01 fully satisfied for the git-tracked redistributable fixture set: a deterministic byte-equal golden snapshot of the full `SamplerOutput` (all three maps — D-06) for every sampling git-tracked fixture is committed
- The baseline is captured and committed in COMMIT A while `package.json` still pins only `@esotericsoftware/spine-core@4.2.111` (proven by acceptance criteria) — this commit will be the git ancestor of the Plan 02 alias commit
- The freeze guard (`safe01-freeze-guard.spec.ts`) is committed and will machine-assert the ancestry the instant Plan 02 introduces `spine-core-42`; it structurally forbids a casual regen path (D-09)
- D-08-R two-tier honored: fresh-clone CI sees only the git-tracked baselines (deterministic, green); heavy/proprietary rigs are gitignored + presence-guarded for local-only drift coverage
- Zero `src/core/**` and zero dependency changes in this plan (CLAUDE.md Fact #5 / RT-04 preserved; the serializer + discovery are pure-Node test utilities in `tests/`, never `core/`)
</success_criteria>

<output>
After completion, create `.planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-01-SUMMARY.md`. Record: the exact intentional included fixture set in `_manifest.json` (and which sidecars were excluded and why), the COMMIT A SHA, confirmation that `package.json` was still 4.2.111-only at that commit, and that the freeze-guard skipped-with-reason (alias not yet present) as designed.
</output>
