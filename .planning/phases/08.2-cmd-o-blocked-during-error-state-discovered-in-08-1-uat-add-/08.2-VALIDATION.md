---
phase: 08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-
status: pending-uat
nyquist_compliant: true
generated: 2026-04-26
plans_complete: 5/5
canonical_regression_target: cmd-o-during-error-state-gating (08.1 UAT discovery)
---

# Phase 08.2 Validation Report

Closes the canonical 08.1 UAT regression discovery (Cmd+O blocked while error / projectLoadFailed UI is mounted) by shipping the native Electron application Menu surface end-to-end. The Cmd+O accelerator now lives on the main-side Menu and fires regardless of renderer AppState — including the recovery-banner state where the bug originally manifested.

Phase goal additionally ships the standard macOS top-level menu template (App / File / Edit / View / Window / Help per D-174), Open Recent persistence (`recent.json` in `userData`, 10-slot cap, per D-177/D-178/D-180), Cmd+Shift+S for Save As (D-185), and modal-open menu suppression (D-184).

## Per-plan validation

| Plan    | Goal                                                                  | Spec/Grep evidence | Status |
| ------- | --------------------------------------------------------------------- | ------------------ | ------ |
| 08.2-01 | Open Recent persistence (`src/main/recent.ts` + tests)                | `tests/main/recent.spec.ts` 9 cases GREEN; hygiene grep `import { app } from 'electron';` (single line, exact match) — no other electron symbols imported into recent.ts; atomic-write idiom (`<path>.tmp` + `fs.rename`) verified by case (f); `validateRecentFile` discriminated-envelope guard (D-156 mirror) verified by case (h); `MAX_RECENT = 10` cap verified by case (e). +9 passing tests. | ✅ |
| 08.2-02 | App-menu builder + Cmd+O accelerator (D-173, D-174, D-185, D-188)     | `tests/main/menu.spec.ts` 7 cases GREEN; `Menu.setApplicationMenu` + `Menu.buildFromTemplate` wired in `src/main/index.ts`; 5 role-based menus (`appMenu` / `editMenu` / `viewMenu` / `windowMenu` / `help`); 3 accelerators (`CmdOrCtrl+O` / `CmdOrCtrl+S` / `CmdOrCtrl+Shift+S`); 4 menu IPC channels (`menu:open-clicked` / `menu:open-recent-clicked` / `menu:save-clicked` / `menu:save-as-clicked`); `autoHideMenuBar: true` preserved on Win/Linux per D-186; initial menu install at `app.whenReady()`. +7 passing tests. | ✅ |
| 08.2-03 | IPC + preload wiring (D-181 menu:notify-state, D-180 addRecent threading, D-175 preload bridges) | `tests/main/ipc.spec.ts` 2 cases GREEN (rebuild + silent-reject); `ipcMain.on('menu:notify-state', …)` field-by-field validation gate (3 typeof boolean checks + non-object guard); `addRecent(absolutePath)` + `void applyMenu(getCurrentMenuState(), getMainWindow())` threaded into `handleProjectSaveAs` + `handleProjectOpenFromPath` ok-arms only — `handleProjectSave` (overwrite-existing-path) explicitly UNCHANGED per D-180 (`awk` extract returns `addRecent` count of 0); 5 new preload methods (`notifyMenuState` + 4 `onMenu*` subscriptions following Pitfall 9 wrapped-const idiom); `interface Api` extended with same 5 method signatures. +2 passing tests. | ✅ |
| 08.2-04 | Renderer wiring (D-175, D-176, D-181, D-184, D-187 — the canonical Cmd+O fix structurally lands) | `App.tsx` ships `appShellMenuRef` parallel to `beforeDropRef`; 4 menu-event subscription `useEffect` with 4-unsub Pitfall 15 cleanup; per-AppState `notifyMenuState` push `useEffect` keyed on `state.status` covering `idle` / `error` / `projectLoadFailed`; both AppShell mount points forward `appShellMenuRef`. `AppShell.tsx` keydown listener (lines 626-643 pre-edit) DELETED end-to-end (no `metaKey` / `ctrlKey` / `addEventListener('keydown'` / `removeEventListener('keydown'` references remain); `notifyMenuState` push `useEffect` keyed on the 4 modal slots; `appShellMenuRef` registration `useEffect` parallel to `onBeforeDropRef`; dedicated `onClickSaveAs` callback extracted (D-175 split). 8.1 reproducers (`8.1-VR-01` / `8.1-VR-02` / `8.1-VR-03a` / `8.1-VR-03b`) continue GREEN. The 1 broken `Cmd+S triggers Save` case at `tests/renderer/save-load.spec.tsx` lines 101-112 is the keydown-targeted case Plan 04 explicitly predicted would break — Plan 05 retires it. | ✅ |
| 08.2-05 | Renderer test extensions + close-out                                  | `tests/renderer/save-load.spec.tsx`: `Phase 08.2 menu wiring` describe block ships 6 cases (`8.2-MENU-01..06`) including the canonical `8.2-MENU-04` (Cmd+O on `projectLoadFailed` regression test); the `Keyboard shortcuts (D-140 + Pitfall 6)` describe block at lines 92-121 pre-edit is RETIRED (replaced inline by `8.2-MENU-06` — the inverse assertion that the renderer keydown path is gone); 5 menu-surface mocks (`notifyMenuState`, `onMenuOpen`, `onMenuOpenRecent`, `onMenuSave`, `onMenuSaveAs`) added to `vi.stubGlobal('api', …)` block. Full vitest suite **297 passed + 1 skipped + 1 todo** (was 292 with 1 broken → +6 new −2 retired +1 unbroken = +5; matches plan). Locked-file diffs all 0 lines. CLI byte-for-byte unchanged. arch.spec 10/10 GREEN. | ✅ |

## Per-spec GREEN evidence

| Spec ID    | File                              | Status |
| ---------- | --------------------------------- | ------ |
| 8.2-MENU-01 | tests/renderer/save-load.spec.tsx | ✅ |
| 8.2-MENU-02 | tests/renderer/save-load.spec.tsx | ✅ |
| 8.2-MENU-03 | tests/renderer/save-load.spec.tsx | ✅ |
| 8.2-MENU-04 | tests/renderer/save-load.spec.tsx (the canonical 08.1 UAT regression reproducer) | ✅ |
| 8.2-MENU-05 | tests/renderer/save-load.spec.tsx | ✅ |
| 8.2-MENU-06 | tests/renderer/save-load.spec.tsx (replaces the retired keydown describe block — inverse assertion that the renderer keydown path is gone) | ✅ |

## Locked-file invariants (D-170 / D-171 / D-102 / CLAUDE.md fact #3)

| File                                              | Lock                                              | Diff vs 08.2 base `cfa7c6a` | Status |
| ------------------------------------------------- | ------------------------------------------------- | --------------------------- | ------ |
| scripts/cli.ts                                    | Phase 5 D-102 + CLAUDE.md rule #3                 | 0 lines                     | ✅     |
| src/core/sampler.ts                               | Phase 5 D-102 + CLAUDE.md rule #3                 | 0 lines                     | ✅     |
| src/core/loader.ts                                | Informal lock (no 08.2 changes required)          | 0 lines                     | ✅     |
| src/core/project-file.ts                          | D-171 (.stmproj v1 schema unchanged)              | 0 lines                     | ✅     |

(Phase 8.1 baseline `f3a4d19` likewise returns 0 lines for all four — confirming no Phase 08.2 drift on the long-locked files.)

## Layer 3 invariants

- `tests/arch.spec.ts` 10/10 GREEN (no `src/core/*` imports added to App.tsx or AppShell.tsx; no `electron` / `node:fs` / `sharp` imports added to `src/core/*`).
- `src/main/recent.ts` lives in `src/main`, NOT `src/core`. Hygiene grep clean: `import { app } from 'electron';` is the single allowed electron-named-import (verified via `grep -E "import\\s*\\{[^}]*\\}\\s*from\\s*'electron'" src/main/recent.ts`).
- `src/main/index.ts` extended with menu builder inline (PATTERNS.md Option A); no new `src/main/menu.ts` module split (Phase 9 polish call if it grows).
- App.tsx + AppShell.tsx — no new `src/core/*` imports.

## Test baseline

| Phase close            | Passed | Skipped | Todo | Net delta |
| ---------------------- | ------ | ------- | ---- | --------- |
| Phase 8 close          | 270    | 1       | 3    | —         |
| Phase 08.1 close       | 275    | 1       | 1    | +5 from Phase 8 |
| Phase 08.2 close (this) | **297** | 1       | 1    | **+22 from Phase 08.1** |

Per-plan delta: 08.2-01 +9 (recent), 08.2-02 +7 (menu), 08.2-03 +2 (ipc), 08.2-04 −1 (predicted keydown break), 08.2-05 +6 new −2 retired keydown +1 unbroken = +5 net. Sum: 9 + 7 + 2 − 1 + 5 = +22 (matches: 275 + 22 = 297).

## CLI byte-for-byte smoke

```
$ npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json

> spine-texture-manager@0.0.0 cli
> tsx scripts/cli.ts fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json

Attachment         Skin     Source W×H  Peak W×H       Scale  Source Animation  Frame
-----------------  -------  ----------  -------------  -----  ----------------  -----
CIRCLE/CIRCLE      default  699×699     1440.4×1411.4  2.018  PATH              27
SQUARE/SQUARE      default  1000×1000   2102.8×2102.8  1.500  PATH              0
TRIANGLE/TRIANGLE  default  833×759     1870.6×1979.1  2.000  PATH              0

Sampled in 22.7 ms at 120 Hz (4 attachments across 1 skins, 4 animations)
exit 0
```

CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000 — byte-for-byte unchanged from Phase 5 / 6 / 7 / 8 / 8.1 baselines.

## Build artifact sizes

| Artifact              | Phase 08.1 close | Phase 08.2 close | Delta             |
| --------------------- | ---------------- | ---------------- | ----------------- |
| out/main/index.cjs    | 58.53 kB         | 64.48 kB         | +5.95 kB (+10.2%) |
| out/preload/index.cjs | 8.03 kB          | 10.78 kB         | +2.75 kB (+34.2%) |
| out/renderer/*.js     | 693.29 kB        | 695.53 kB        | +2.24 kB (+0.32%) |
| out/renderer/*.css    | 23.93 kB         | 23.93 kB         | 0 kB (0%)         |

Main bundle growth = the new `buildAppMenu` / `applyMenu` / `MenuState` / 3 getters + `mainWindowRef` wiring + `'menu:notify-state'` IPC handler + `addRecent` + `applyMenu` rebuild calls in `handleProjectSaveAs` / `handleProjectOpenFromPath` + the new `recent.ts` module. Preload growth = 5 new contextBridge methods (1 send + 4 subscriptions with Pitfall 9 wrapped-const idiom). Renderer growth = 4 menu-event subscriptions in App.tsx + per-AppState push useEffect + appShellMenuRef + `onClickSaveAs` extraction in AppShell.tsx, partially offset by the deleted keydown listener (-23 lines). All deltas reflect the new menu surface and are within expectation for the phase scope.

## Manual UAT — pending signoff

The 4-reproducer UAT is the load-bearing real-Electron verification (T-08.2-05-02). Task 3 of `08.2-05-renderer-tests-and-close-out-PLAN.md` walks the user through:

| Reproducer | Description                                                              | Phase 08.2 expected outcome |
| ---------- | ------------------------------------------------------------------------ | --------------------------- |
| 1          | Cmd+O on initial idle state (no project loaded)                          | OS file picker opens. Pre-08.2 nothing happened (AppShell unmounted). |
| 2          | Cmd+O after a "Not a .json or .stmproj" toast (`error` AppState)         | OS file picker opens. Error UI persists in background; clears on successful drop. |
| 3 ⚠️       | **Cmd+O on the projectLoadFailed recovery banner — THE CANONICAL 08.1 UAT REGRESSION REPRODUCER** | OS file picker opens. The recovery banner stays visible behind it. Cancel → banner is still there; Locate skeleton… still works. **Pre-08.2: Cmd+O did nothing in this state.** |
| 4          | Open Recent menu round-trip — Save As → File → Open Recent → click       | Project loads. Recent submenu re-bumps the clicked path to top per D-180. Clear Menu wipes the submenu to `(No recent projects)` placeholder. |

Bonus checks (from `08.2-05-PLAN.md` `<how-to-verify>`): Edit menu Cmd+C/V/A/Z work in OverrideDialog text input (D-188); macOS smell test (App-name menu shows About / Hide / Quit; Cmd+Q quits cleanly with SaveQuitDialog flow if dirty); Win/Linux autoHideMenuBar:true verification via Alt / F10 (D-186).

## Closing posture

This validation report is the automated half of the Phase 08.2 close-out. The pending UAT signoff (Task 3 of Plan 08.2-05) is the load-bearing real-Electron verification — the 6 spec cases assert renderer-side wiring (handler captured / handler invoked / downstream call made), but the canonical fix lives in the cross-process menu accelerator dispatch which can only be observed through a live `Menu.setApplicationMenu` call.

When manual UAT signs off (Reproducer 3 PASS specifically), this file's `status` flips from `pending-uat` to `signed-off` and the Phase 08.2 close-out commit threads `08.2 COMPLETE` into `STATE.md` + flips the 5 ROADMAP.md plan checkboxes to `[x]`.

---

*Generated by Plan 08.2-05 (close-out) — automated exit-criteria sweep + per-plan validation map.*
