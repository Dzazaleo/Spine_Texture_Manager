---
phase: 08.2
plan: 05
type: execute
wave: 5
depends_on: [08.2-02, 08.2-04]
files_modified:
  - tests/renderer/save-load.spec.tsx
  - .planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - F9.1
  - F9.2
must_haves:
  truths:
    - "tests/renderer/save-load.spec.tsx ships 6 new menu-wiring cases (8.2-MENU-01..06) covering: notifyMenuState mount push, modalOpen flip on OverrideDialog, onMenuOpen → openProject (loaded state), onMenuOpen in projectLoadFailed state STILL fires open flow (the canonical 08.1 UAT regression test), onMenuOpenRecent flow, AppShell keydown listener removal verification"
    - "8.2-MENU-04 (the Cmd+O-on-recovery-banner regression test) is GREEN — proves the entire phase goal end-to-end"
    - "The deleted save-load.spec.tsx Keyboard shortcuts describe block at lines 92-121 is REPLACED with menu-event coverage (8.2-MENU-06 tests the listener is gone)"
    - "Manual UAT signed off on 4 reproducers: (1) Cmd+O on initial drop state fires picker; (2) Cmd+O on error state (drop a .png) fires picker; (3) Cmd+O on projectLoadFailed recovery banner fires picker — THE 08.1 UAT REGRESSION REPRODUCER; (4) menu File → Open Recent → click a path opens the project"
    - "08.2-VALIDATION.md per-task map populated; STATE.md advanced to Phase 08.2 COMPLETE; ROADMAP.md plan checkboxes flipped"
    - "Full vitest suite GREEN with all 5 plans landed: 275 baseline + recent (9) + menu (7) + ipc (2) + 8.2-MENU (6) - retired keydown (2) = 297 minimum passing"
  artifacts:
    - path: tests/renderer/save-load.spec.tsx
      provides: "Phase 08.2 menu wiring describe block (6 cases) + retired Keyboard shortcuts describe block (replaced inline by 8.2-MENU-06)"
      contains: "describe('Phase 08.2 menu wiring'"
      contains: "8.2-MENU-04"
      min_lines: 380
    - path: .planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-VALIDATION.md
      provides: "per-plan exit-criteria map for Phase 08.2 (5 plans × tasks)"
      contains: "08.2-01\\b"
      contains: "08.2-05\\b"
      min_lines: 80
    - path: .planning/STATE.md
      provides: "Phase 08.2 advanced to COMPLETE; current pointer flipped from 08.1 → 08.2 → 9"
      contains: "08.2"
    - path: .planning/ROADMAP.md
      provides: "Phase 08.2 plan list with all 5 PLAN.md checkboxes flipped to [x]"
      contains: "[x] 08.2-01-recent-storage-PLAN.md"
      contains: "[x] 08.2-05-renderer-tests-and-close-out-PLAN.md"
  key_links:
    - from: tests/renderer/save-load.spec.tsx (8.2-MENU-04)
      to: "the Phase 08.1 UAT regression — Cmd+O blocked during projectLoadFailed state"
      via: "drop a malformed file to force projectLoadFailed → invoke captured onMenuOpen handler → assert openProject called"
      pattern: "8\\.2-MENU-04"
    - from: .planning/STATE.md
      to: "Phase 08.2 COMPLETE marker"
      via: "current.phase advanced to 9 OR 08.2 COMPLETE block added"
      pattern: "08\\.2.*COMPLETE\\|complete"
---

<objective>
Verify the Phase 08.2 phase goal end-to-end:
1. **Cmd+O fires in EVERY AppState** — the canonical 08.1 UAT bug is fixed end-to-end. Asserted by spec `8.2-MENU-04` (drop a malformed file to force `projectLoadFailed` → invoke captured `onMenuOpen` handler → assert `openProject` called).
2. **The keydown listener is gone** — asserted by `8.2-MENU-06` (dispatch a Cmd+O keydown event into the window; assert `openProject` is NOT called via that path).
3. **Native menu Save / Save As route through AppShell handlers** — asserted by `8.2-MENU-01` (mount push) + AppShell ref-bridge tests inferred via existing 8.1 specs continuing to pass.
4. **Open Recent works** — asserted by `8.2-MENU-05` (invoke `onMenuOpenRecent('/abs/path.stmproj')` → `openProjectFromPath` called).
5. **Modal-open suppression** — asserted by `8.2-MENU-02` (open OverrideDialog → next notifyMenuState carries `modalOpen:true`).

Plus close-out admin: 08.2-VALIDATION.md per-plan map, manual UAT signoff on 4 reproducers, STATE.md advance, ROADMAP.md plan checkboxes flipped.

Output: One touched spec file (test extensions + retire the keydown describe block) + 3 admin docs (validation map, STATE.md, ROADMAP.md). Phase 08.2 closes after manual UAT signoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-CONTEXT.md
@.planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-PATTERNS.md
@.planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-01-SUMMARY.md
@.planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-02-SUMMARY.md
@.planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-03-SUMMARY.md
@.planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-04-SUMMARY.md

<!-- File we are modifying -->
@tests/renderer/save-load.spec.tsx

<!-- The 8.1 close-out doc as a structural template for 08.2-VALIDATION.md -->
@.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea

<!-- Pattern analog for 8.2-MENU-XX cases -->

<interfaces>
<!-- Existing test fixtures and helpers in save-load.spec.tsx that the 6 new cases reuse: -->

```typescript
// makeSummary() at lines 20-42 — synthetic SkeletonSummary
// vi.stubGlobal('api', { ... }) at lines 44-62 — preload-mock surface
//   The new cases extend this stub with the 5 menu-event mocks below
// existing renders: render(<App />) and render(<AppShell ... />) — both forms used

// Plan 04 added these to window.api; this plan's tests mock them:
//   notifyMenuState: vi.fn()
//   onMenuOpen: vi.fn(() => () => undefined)
//   onMenuOpenRecent: vi.fn(() => () => undefined)
//   onMenuSave: vi.fn(() => () => undefined)
//   onMenuSaveAs: vi.fn(() => () => undefined)
// The mock fn returned by onMenu*() is the unsubscribe; the real callback
// the renderer registered is captured via .mock.calls[0][0].
```

The wait-mechanism gotcha (already known from Plans 08.1-04 / 08.1-05): React 19's
concurrent state-update scheduling requires `await screen.findBy*` (not `await Promise.resolve()`)
when asserting state-driven UI changes. The 4 new cases that drive React state mutations follow
this discipline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend save-load.spec.tsx — add 8.2-MENU-01..06 cases + retire Keyboard shortcuts describe block</name>

  <read_first>
    - tests/renderer/save-load.spec.tsx (full 1-335) — current state of the spec file
    - .planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-PATTERNS.md (§"tests/renderer/save-load.spec.tsx (TOUCHED — extension cases 8.2-MENU-01 through 06)" lines 990-1071)
    - .planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-CONTEXT.md (§domain test plan, lines 56-62 — cases 8.2-MENU-01..06 with the canonical 8.2-MENU-04 reproducer)
  </read_first>

  <action>
    Modify `tests/renderer/save-load.spec.tsx` in two coordinated edits.

    **Edit 1: Extend the `vi.stubGlobal('api', { ... })` block at lines 44-62 with the 5 menu mocks**

    Add to the existing api stub object (PATTERNS.md lines 996-1009 verbatim):
    ```typescript
    // Phase 08.2 — menu surface mocks. The fn returned by onMenu*() is the
    // unsubscribe; the registered callback is captured via .mock.calls[0][0].
    notifyMenuState: vi.fn(),
    onMenuOpen: vi.fn(() => () => undefined),
    onMenuOpenRecent: vi.fn(() => () => undefined),
    onMenuSave: vi.fn(() => () => undefined),
    onMenuSaveAs: vi.fn(() => () => undefined),
    ```

    **Edit 2: DELETE the existing Keyboard shortcuts describe block at lines 92-121**

    The block:
    ```typescript
    describe('Keyboard shortcuts (D-140 + Pitfall 6)', () => {
      it('Cmd+S triggers Save', async () => { ... });
      it('Cmd+S suppressed when modal open (T-08-SHORT)', async () => { ... });
    });
    ```

    DELETE this entire block. PATTERNS.md lines 1067-1071 confirm: "the listener is REMOVED (D-176) — these tests must be either (a) deleted, or (b) replaced. Recommend deleting + replacing with the 8.2-MENU-XX cases." 8.2-MENU-06 covers the inverse assertion (the keydown does NOT call openProject).

    **Edit 3: ADD `describe('Phase 08.2 menu wiring', () => { ... })` block — 6 cases**

    Insert AT THE END of the file (after the last existing describe block — `SaveQuitDialog three-button flow` etc., around lines 122-335). The block contains 6 cases per PATTERNS.md lines 1014-1064 + CONTEXT.md lines 56-61:

    **8.2-MENU-01 — AppShell mount push**:
    ```typescript
    describe('Phase 08.2 menu wiring', () => {
      it('8.2-MENU-01: AppShell calls notifyMenuState on mount with summary loaded', async () => {
        const { container } = render(<App />);
        const dropTarget = container.firstElementChild as HTMLElement;
        const f = new File(['{}'], 'SIMPLE_TEST.json', { type: 'application/json' });
        fireEvent.drop(dropTarget, { dataTransfer: { files: [f] } as unknown as DataTransfer });
        // Wait for the load → AppShell mount → useEffect chain.
        await screen.findByText(/^CIRCLE$/i);
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const api = (globalThis as any).api;
        // The mount-time push has canSave/canSaveAs:true (summary loaded), modalOpen:false.
        expect(api.notifyMenuState).toHaveBeenCalledWith(
          expect.objectContaining({ canSave: true, canSaveAs: true, modalOpen: false }),
        );
      });
    ```

    **CRITICAL — DropZone load flow stub:** the existing test infrastructure (lines 44-62) already mocks `loadSkeletonFromFile` to return a successful summary. Confirm by reading lines 44-62 BEFORE writing 8.2-MENU-01 — the mock returns summary with `peaks: [{attachmentName: 'CIRCLE', ...}]`, so `findByText(/^CIRCLE$/i)` is the right wait predicate.

    **8.2-MENU-02 — Modal flip**:
    ```typescript
      it('8.2-MENU-02: Opening OverrideDialog flips modalOpen to true (D-184)', async () => {
        const { container } = render(<App />);
        const dropTarget = container.firstElementChild as HTMLElement;
        const f = new File(['{}'], 'SIMPLE_TEST.json', { type: 'application/json' });
        fireEvent.drop(dropTarget, { dataTransfer: { files: [f] } as unknown as DataTransfer });
        const circleRow = await screen.findByText(/^CIRCLE$/i);
        // Open OverrideDialog by double-clicking the Scale cell.
        // Same mechanism the 8.1-VR-03a spec uses (lines 137-143 of save-load.spec.tsx).
        const row = circleRow.closest('tr')!;
        const scaleCell = within(row).getByText(/0\.500×/);
        await userEvent.dblClick(scaleCell);
        // The OverrideDialog mount triggers a notifyMenuState push with modalOpen:true.
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const api = (globalThis as any).api;
        // Find any call where modalOpen===true AFTER the mount push.
        await screen.findByRole('dialog');
        const modalPushed = api.notifyMenuState.mock.calls.some(
          (c: unknown[]) => (c[0] as { modalOpen?: boolean }).modalOpen === true,
        );
        expect(modalPushed).toBe(true);
      });
    ```

    **CRITICAL — modal-mutation row target:** Plan 08.1-05's gap-fix Dev #2 documented that `makeSummary()` peaks contain CIRCLE + SQUARE only (NO TRIANGLE) — use CIRCLE consistently. The Scale cell's text match `/0\.500×/` matches the existing makeSummary CIRCLE row (peakScale: 0.5). Re-verify by reading makeSummary at line 20 BEFORE writing.

    **8.2-MENU-03 — onMenuOpen callback fires picker (loaded state)**:
    ```typescript
      it('8.2-MENU-03: onMenuOpen callback fires handleBeforeDrop → openProject + handleProjectLoad (loaded state)', async () => {
        render(<App />);
        // The App.tsx menu-event useEffect runs on mount; capture the callback.
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const api = (globalThis as any).api;
        expect(api.onMenuOpen.mock.calls.length).toBe(1);
        const cb = api.onMenuOpen.mock.calls[0][0] as () => Promise<void>;
        await cb();
        expect(api.openProject).toHaveBeenCalled();
      });
    ```

    **8.2-MENU-04 — THE CANONICAL 08.1 UAT REGRESSION REPRODUCER**:
    ```typescript
      it('8.2-MENU-04: onMenuOpen in projectLoadFailed state still fires open flow (08.1 UAT fix)', async () => {
        // Drive App into projectLoadFailed by dropping a .stmproj that fails to
        // load with SkeletonNotFoundOnLoadError. Mock openProjectFromFile or
        // openProjectFromPath (whichever DropZone routes the .stmproj through)
        // to return ok:false with kind:'SkeletonNotFoundOnLoadError' + the
        // 7 threaded recovery fields per Phase 8.1 D-158.
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const api = (globalThis as any).api;
        api.openProjectFromFile = vi.fn().mockResolvedValue({
          ok: false,
          error: {
            kind: 'SkeletonNotFoundOnLoadError',
            message: 'Skeleton not found',
            originalSkeletonPath: '/missing/skel.json',
            projectPath: '/abs/proj.stmproj',
            mergedOverrides: {},
            samplingHz: 120,
            lastOutDir: null,
            sortColumn: null,
            sortDir: null,
          },
        } as OpenResponse);

        const { container } = render(<App />);
        const dropTarget = container.firstElementChild as HTMLElement;
        const f = new File(['{}'], 'broken.stmproj', { type: 'application/json' });
        fireEvent.drop(dropTarget, { dataTransfer: { files: [f] } as unknown as DataTransfer });

        // Wait for App to enter projectLoadFailed (recovery banner mounts).
        await screen.findByRole('alert');

        // Now invoke the captured onMenuOpen — it MUST still trigger openProject.
        // THIS IS THE CANONICAL 08.1 UAT BUG ASSERTION.
        const cb = api.onMenuOpen.mock.calls[0][0] as () => Promise<void>;
        await cb();
        expect(api.openProject).toHaveBeenCalled();
      });
    ```

    **CRITICAL — DropZone .stmproj routing:** the file extension `.stmproj` is checked by DropZone (Phase 8 D-145). Read DropZone.tsx (or check `tests/renderer/save-load.spec.tsx` 8.1-VR-01 case at lines 127-150 for the canonical pattern) to confirm the dataTransfer file shape that triggers the openProjectFromFile path. Adjust if necessary — the spec's load-bearing assertion is the openProject call AFTER projectLoadFailed mounts.

    **8.2-MENU-05 — Open Recent**:
    ```typescript
      it('8.2-MENU-05: onMenuOpenRecent fires openProjectFromPath after dirty-guard', async () => {
        render(<App />);
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const api = (globalThis as any).api;
        api.openProjectFromPath = vi.fn().mockResolvedValue({
          ok: true,
          project: {
            summary: makeSummary(),
            restoredOverrides: {},
            staleOverrideKeys: [],
            samplingHz: 120,
            lastOutDir: null,
            sortColumn: null,
            sortDir: null,
            projectFilePath: '/abs/path.stmproj',
          },
        });
        const cb = api.onMenuOpenRecent.mock.calls[0][0] as (path: string) => Promise<void>;
        await cb('/abs/path.stmproj');
        expect(api.openProjectFromPath).toHaveBeenCalledWith('/abs/path.stmproj');
      });
    ```

    **8.2-MENU-06 — Keydown listener IS gone (replaces the deleted describe block)**:
    ```typescript
      it('8.2-MENU-06: AppShell keydown listener is gone — Cmd+O dispatch does NOT call openProject directly', async () => {
        const summary = makeSummary();
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        render(<AppShell summary={summary} samplingHz={120} {...({} as any)} />);
        // Reset the openProject mock so the App.tsx-level menu subscription's
        // captured cb (which DOES call openProject when invoked) doesn't pollute
        // this assertion. Note: this test mounts AppShell directly, NOT App,
        // so the App.tsx menu-event useEffect doesn't run.
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        (globalThis as any).api.openProject.mockClear();
        fireEvent.keyDown(window, { key: 'o', metaKey: true });
        await Promise.resolve();
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        expect((globalThis as any).api.openProject).not.toHaveBeenCalled();
      });
    });  // closes describe('Phase 08.2 menu wiring')
    ```

    Commit message: `test(08.2-05): add 8.2-MENU-01..06 (incl. canonical Cmd+O-on-recovery-banner reproducer); retire keydown describe block (D-176)`
  </action>

  <verify>
    <automated>npx vitest run tests/renderer/save-load.spec.tsx</automated>
    Expected: all cases GREEN — 5 from 8.1 (VR-01, VR-02, VR-03a, VR-03b, SaveQuitDialog flow) + 2 from Phase 8 (Save reuses + dirty marker) + 6 from 8.2-MENU = 13 cases.
  </verify>

  <acceptance_criteria>
    - **6 new it cases:** `grep -c "8\\.2-MENU-0[1-6]" tests/renderer/save-load.spec.tsx` returns at least `6`
    - **Canonical 8.2-MENU-04 case present:** `grep -q "8.2-MENU-04: onMenuOpen in projectLoadFailed state still fires open flow" tests/renderer/save-load.spec.tsx`
    - **Phase 08.2 menu wiring describe label:** `grep -q "describe('Phase 08.2 menu wiring'" tests/renderer/save-load.spec.tsx`
    - **Old `Keyboard shortcuts (D-140 + Pitfall 6)` describe block DELETED:** `! grep -q "describe('Keyboard shortcuts (D-140" tests/renderer/save-load.spec.tsx`
    - **Old "Cmd+S suppressed when modal open" case DELETED (covered now by 8.2-MENU-02):** `! grep -q "Cmd\\+S suppressed when modal open" tests/renderer/save-load.spec.tsx`
    - **5 menu mocks added to api stub:** `awk '/vi.stubGlobal\\('"'"'api'"'"'/,/});/' tests/renderer/save-load.spec.tsx | grep -c "notifyMenuState\|onMenuOpen\|onMenuOpenRecent\|onMenuSave\|onMenuSaveAs" | awk '$1>=5 {exit 0} {exit 1}'`
    - **All cases GREEN:** `npx vitest run tests/renderer/save-load.spec.tsx` exits 0
    - **Full suite GREEN:** `npx vitest run` exits 0 — total ≥ 297 (275 baseline + Plans 01/02/03/05 deltas, minus 2 retired keydown cases)
    - **Locked-file diffs empty:** `git diff scripts/cli.ts src/core/sampler.ts src/core/loader.ts src/core/project-file.ts | wc -l` returns `0`
  </acceptance_criteria>

  <done>
    save-load.spec.tsx ships 6 new 8.2-MENU cases including the canonical 8.2-MENU-04 regression reproducer; Keyboard shortcuts describe block retired; full vitest suite GREEN; locked-file invariants intact.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Automated exit-criteria sweep + populate 08.2-VALIDATION.md</name>

  <read_first>
    - .planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea (look for 08.1-VALIDATION.md to use as a structural template)
    - .planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-01-SUMMARY.md (and -02, -03, -04) — the per-plan summaries this validation map cites
    - All 5 PLAN.md files (08.2-01 through 08.2-05) — the must_haves blocks tell you what each plan promised
  </read_first>

  <action>
    Run the full automated exit-criteria sweep + create `.planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-VALIDATION.md`.

    **Sweep commands (run sequentially; each must exit 0):**
    1. `npx vitest run` — full suite GREEN (≥ 297 cases)
    2. `npx electron-vite build` — production build GREEN
    3. `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` — exits 0; output unchanged byte-for-byte vs the Phase 5 baseline
    4. `git diff scripts/cli.ts src/core/sampler.ts src/core/loader.ts src/core/project-file.ts | wc -l` returns `0` (locked-file invariants)
    5. `git diff scripts/cli.ts` (specifically) prints nothing
    6. `npx vitest run tests/arch.spec.ts` — Layer 3 invariants GREEN (renderer ↛ core; core ↛ sharp/node:fs; project-file.ts ↛ electron)
    7. **Hygiene grep** — `src/main/recent.ts` imports only `app` from `electron`: `grep -E "import\s*\{[^}]*\}\s*from\s*'electron'" src/main/recent.ts` shows exactly `import { app } from 'electron';`

    **08.2-VALIDATION.md format** (mirror 08.1-VALIDATION.md structure if it exists; otherwise produce):

    ```markdown
    # Phase 08.2 — Validation Map

    **Status:** [signed-off | pending UAT]
    **Date:** 2026-04-XX
    **Plans:** 5/5 GREEN

    ## Per-plan exit-criteria map

    | Plan | Tasks | must_have ID | Acceptance | Status |
    |------|-------|--------------|------------|--------|
    | 08.2-01 | 2 | recent.ts loads/adds/clears w/ atomic write + hand-rolled validator | tests/main/recent.spec.ts 9 cases GREEN; hygiene grep clean | ✅ |
    | 08.2-02 | 2 | buildAppMenu + applyMenu + state helpers; initial menu install at app.whenReady | tests/main/menu.spec.ts 7 cases GREEN; Menu.setApplicationMenu wired; autoHideMenuBar:true preserved | ✅ |
    | 08.2-03 | 4 | menu:notify-state IPC handler with trust-boundary validation; addRecent threaded into Save As / Open success arms; preload + Api extended | tests/main/ipc.spec.ts 2 cases GREEN; project-io.ts threading verified by acceptance greps; preload Pitfall 9 wrapped const verified | ✅ |
    | 08.2-04 | 3 | App.tsx subscriptions + per-AppState push; AppShell keydown DELETED; appShellMenuRef registration; onClickSaveAs extracted | existing 8.1 reproducers GREEN; structural Cmd+O fix landed | ✅ |
    | 08.2-05 | 2 | 8.2-MENU-01..06 cases incl. canonical 8.2-MENU-04 regression test | tests/renderer/save-load.spec.tsx full GREEN; full suite ≥ 297 | ✅ |

    ## Locked-file invariants

    - `scripts/cli.ts` — diff vs Phase 5 baseline: 0 lines ✅
    - `src/core/sampler.ts` — diff: 0 lines ✅
    - `src/core/loader.ts` — diff: 0 lines ✅
    - `src/core/project-file.ts` — diff: 0 lines (Phase 8.1 D-171 lock holds) ✅

    ## CLI byte-for-byte

    `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` output:
    - CIRCLE 2.018 ✅
    - SQUARE 1.500 ✅
    - TRIANGLE 2.000 ✅
    - Sampling 23.8 ms at 120 Hz ✅

    ## Layer 3 invariants

    - `src/main/recent.ts` imports only `app` from electron — hygiene grep clean ✅
    - `src/main/recent.ts` is in src/main, NOT src/core ✅
    - App.tsx + AppShell.tsx — no new src/core imports ✅
    - tests/arch.spec.ts GREEN ✅

    ## Test count

    - Phase 08.1 baseline: 275 passed + 1 todo + 1 skipped
    - Phase 08.2 delta: +9 (recent) +7 (menu) +2 (ipc) +6 (8.2-MENU) -2 (retired keydown) = +22
    - Phase 08.2 total: ≥ 297 passed + 1 todo + 1 skipped ✅
    ```

    Commit message: `docs(08.2): automated exit-criteria sweep + 08.2-VALIDATION.md per-plan map`

    **Required user action gate:** after the automated sweep passes, this task ends. Task 3 (the manual UAT checkpoint) requires human verification.
  </action>

  <verify>
    <automated>npx vitest run && npx electron-vite build && npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json</automated>
    Expected: all three exit 0.
  </verify>

  <acceptance_criteria>
    - **Full suite GREEN:** `npx vitest run` exits 0 with at least 297 passing
    - **Build GREEN:** `npx electron-vite build` exits 0
    - **CLI byte-for-byte:** `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` exits 0; sampling time present in output
    - **Locked-file invariants:** `git diff scripts/cli.ts src/core/sampler.ts src/core/loader.ts src/core/project-file.ts | wc -l` returns `0`
    - **Hygiene grep clean (recent.ts):** `grep -E "import\\s*\\{[^}]*\\}\\s*from\\s*'electron'" src/main/recent.ts | grep -v "^import { app } from 'electron';" | wc -l` returns `0`
    - **arch.spec GREEN:** `npx vitest run tests/arch.spec.ts` exits 0
    - **VALIDATION.md exists:** `test -f .planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-VALIDATION.md`
    - **VALIDATION.md cites all 5 plans:** `grep -cE "08\\.2-0[1-5]\\b" .planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-VALIDATION.md` returns at least `5`
  </acceptance_criteria>

  <done>
    Automated sweep all GREEN; 08.2-VALIDATION.md committed with per-plan map + locked-file confirmation + CLI byte-for-byte confirmation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — 4 reproducers (incl. the canonical 08.1 UAT regression reproducer)</name>

  <files>(no source files modified — manual verification only)</files>

  <action>Pause execution and request human verification per the &lt;how-to-verify&gt; block below. The 4 reproducers exercise the entire Phase 08.2 surface against a live Electron Menu in dev mode. Wait for the user to type "approved" before proceeding to Task 4 close-out.</action>

  <verify>Human responds with "approved" via the &lt;resume-signal&gt; block.</verify>

  <done>All 4 reproducers signed off by human; specifically Reproducer 3 (Cmd+O on projectLoadFailed recovery banner — the canonical 08.1 UAT regression reproducer) MUST pass.</done>

  <what-built>
    The complete Phase 08.2 surface:
    - Native Electron application Menu (App / File / Edit / View / Window / Help)
    - File menu: Open… (Cmd+O), Open Recent ► submenu, Save (Cmd+S), Save As… (Cmd+Shift+S), Close Window (Cmd+W)
    - Edit menu via role:'editMenu' (Cmd+C/V/A/Z continue working in OverrideDialog text input)
    - Open Recent persistence at app.getPath('userData')/recent.json (10-slot cap, dedupe + push-to-front)
    - Cmd+O accelerator that fires in EVERY AppState — the 08.1 UAT bug fix
    - The renderer keydown listener (AppShell.tsx:626-643) DELETED — single source of truth is the menu accelerator
    - Modal-open suppression: opening any modal (OverrideDialog / OptimizeDialog / AtlasPreview / SaveQuitDialog) disables File menu items
  </what-built>

  <how-to-verify>
    Run the dev app: `npm run dev`. Then exercise these 4 reproducers in order:

    **Reproducer 1 — Cmd+O on initial idle state (no project loaded)**
    1. Launch app. Empty drop-zone shows.
    2. Press **Cmd+O** (macOS) or **Ctrl+O** (Win/Linux) directly — DO NOT click anywhere first.
    3. **Expected:** OS file picker opens.
    4. Cancel the picker. App stays in idle.
    5. **Pre-08.2 behavior:** nothing happened (the AppShell keydown listener wasn't mounted in idle state). **Post-08.2: picker opens immediately.** ✅

    **Reproducer 2 — Cmd+O after a "Not a .json or .stmproj" toast (error AppState)**
    1. From idle, drag-drop a `.png` file (or any non-`.json` non-`.stmproj` file) onto the window.
    2. The error UI surfaces: muted-orange `.kind: .message` block.
    3. Press **Cmd+O**.
    4. **Expected:** OS file picker opens. The error UI persists in the background (it'll clear on a successful drop).
    5. Drop `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` via the picker (or cancel and drag-drop). App enters `loaded` state. ✅

    **Reproducer 3 — Cmd+O on the projectLoadFailed recovery banner — THE CANONICAL 08.1 UAT REGRESSION REPRODUCER** ⚠️
    1. From idle, drag-drop a `.stmproj` file whose `skeletonPath` field points at a missing JSON. (Easy way: open `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.stmproj` if one exists, or temporarily rename `SIMPLE_TEST.json` to force the recovery flow when an existing `.stmproj` is dropped.)
    2. The recovery banner mounts: `Skeleton not found: ...` with `Locate skeleton…` and `Dismiss` buttons.
    3. Press **Cmd+O**.
    4. **Expected:** OS file picker opens. The recovery banner stays visible behind it.
    5. Cancel the picker. The recovery banner is still there. The Locate skeleton... button still works.
    6. **Pre-08.2 behavior:** Cmd+O did nothing in this state. **Post-08.2: picker opens.** ✅
    7. Restore renamed file if applicable.

    **Reproducer 4 — Open Recent menu round-trip**
    1. Open `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`. Click toolbar Save → choose a path (e.g., `/tmp/test1.stmproj`). Save As succeeds; `/tmp/test1.stmproj` now in recent.json.
    2. Open the **File** menu in the menu bar. Hover **Open Recent ►**. Submenu shows `test1.stmproj` (basename label, full path on hover-tooltip).
    3. Click somewhere else to close the menu.
    4. Drop `SIMPLE_TEST.json` again (re-mount AppShell). Save As again to `/tmp/test2.stmproj`. Now File → Open Recent shows `test2.stmproj` at top, `test1.stmproj` below.
    5. Click **File → Open Recent → test1.stmproj**. The project loads.
    6. **Expected:** project loaded; menu Open Recent now shows `test1.stmproj` at top (re-bumped per D-180).
    7. Click **File → Open Recent → Clear Menu**. Submenu now shows `(No recent projects)` placeholder. ✅

    **Bonus check — Edit menu in modal (D-188 verification):**
    1. Load any project. Double-click the Scale cell of any peak row → OverrideDialog mounts with an integer input.
    2. Type a number, then **Cmd+A** to select all, then **Cmd+C** to copy. Cmd+V to paste.
    3. **Expected:** all three work. The dropdown menu's Edit menu items work in the OverrideDialog input. ✅

    **macOS smell test (D-174 / "feels like a real app"):**
    1. Click the App-name menu (the one to the left of `File` on macOS). Confirm: About / Hide / Hide Others / Show All / Quit Spine Texture Manager.
    2. Confirm Cmd+Q quits cleanly (with SaveQuitDialog flow if dirty).

    **autoHideMenuBar verification (D-186) — Win/Linux only, skip on macOS:**
    1. Launch the app. Confirm the menu bar is hidden by default.
    2. Press Alt or F10. Menu bar appears. Open File menu — same items present. ✅

    Type "approved" in the conversation if all 4 reproducers pass. Describe specific failures otherwise.
  </how-to-verify>

  <resume-signal>
    Type "approved" or describe failure(s) per reproducer.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Phase close-out — STATE.md advance + ROADMAP.md plan checkboxes flip</name>

  <read_first>
    - .planning/STATE.md — current pointer (post-08.1 = 2026-04-26 close-out commit)
    - .planning/ROADMAP.md (offset 316, limit 12 — the Phase 08.2 plan list)
    - .planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-VALIDATION.md (just created Task 2)
  </read_first>

  <action>
    After Task 3 manual UAT signs off, do the final admin:

    **1. Flip ROADMAP.md plan checkboxes (lines ~324-326 — Phase 08.2 Plans block).**

    Currently:
    ```
    Plans:
    - [ ] TBD
    ```

    Replace with:
    ```
    Plans:
    - [x] 08.2-01-recent-storage-PLAN.md — recent.ts pure-main module (loadRecent / addRecent / clearRecent / validateRecentFile) + tests/main/recent.spec.ts (9 cases). ✅ 2026-04-XX — commit `<hash>`. SUMMARY: 08.2-01-SUMMARY.md.
    - [x] 08.2-02-app-menu-builder-PLAN.md — buildAppMenu + applyMenu inline in src/main/index.ts (D-174 standard template + D-188 role:* + D-185 accelerators); tests/main/menu.spec.ts 7 cases. ✅ 2026-04-XX. SUMMARY: 08.2-02-SUMMARY.md.
    - [x] 08.2-03-ipc-preload-wiring-PLAN.md — menu:notify-state IPC handler + project-io.ts addRecent threading (D-180) + preload + Api 5 new methods (Pitfall 9). ✅ 2026-04-XX. SUMMARY: 08.2-03-SUMMARY.md.
    - [x] 08.2-04-renderer-wiring-PLAN.md — App.tsx subscriptions + per-AppState push (D-187 — Cmd+O fix); AppShell keydown DELETED (D-176); appShellMenuRef + onClickSaveAs extraction. ✅ 2026-04-XX. SUMMARY: 08.2-04-SUMMARY.md.
    - [x] 08.2-05-renderer-tests-and-close-out-PLAN.md — 8.2-MENU-01..06 incl. canonical 8.2-MENU-04 regression test; manual UAT 4 reproducers signed off; STATE.md + ROADMAP.md advanced. ✅ 2026-04-XX. SUMMARY: 08.2-05-SUMMARY.md.
    ```

    Also update the count: change `**Plans:** 0 plans (run /gsd-plan-phase 08.2 to break down)` → `**Plans:** 5/5 plans complete`.

    **2. Advance STATE.md.**

    Add/update a Phase 08.2 COMPLETE section (mirror Phase 08.1's close-out section structure if present). Include:
    - Phase 08.2 COMPLETE marker with date.
    - All 5 plans signed off.
    - All 4 manual UAT reproducers signed off.
    - Test count: ≥ 297 passing.
    - Locked-file invariants intact.
    - CLI byte-for-byte unchanged.
    - Pointer advanced from "Phase 08.1 COMPLETE" → "Phase 08.2 COMPLETE; ready for Phase 9".

    **3. Final close-out commit.**

    Stage:
    - `.planning/STATE.md`
    - `.planning/ROADMAP.md`
    - (any 08.2-VALIDATION.md edits if not already committed in Task 2)

    Commit message:
    ```
    docs(08.2): close phase after manual UAT — Cmd+O fix verified in idle/error/projectLoadFailed; native File menu shipped (D-173, D-174, D-185, D-188)

    All 5 plans signed off. 4-reproducer manual UAT signed off including the canonical 08.1 UAT regression reproducer (Cmd+O on projectLoadFailed recovery banner). Layer 3 + locked-file invariants intact. CLI byte-for-byte unchanged. Test count ≥ 297 passing.
    ```
  </action>

  <verify>
    <automated>git log -1 --pretty=%s | grep -q "08.2" && cat .planning/ROADMAP.md | grep -A 10 "Phase 08.2:" | grep -c "\[x\]"</automated>
    Expected: latest commit subject mentions 08.2; ROADMAP.md has 5 [x] entries under Phase 08.2 plans.
  </verify>

  <acceptance_criteria>
    - **ROADMAP.md plan checkboxes flipped:** `awk '/### Phase 08\\.2:/,/^---$/' .planning/ROADMAP.md | grep -c "^- \\[x\\]" | awk '$1>=5 {exit 0} {exit 1}'`
    - **ROADMAP.md plan count updated:** `grep -q "5/5 plans complete\|5 plans" .planning/ROADMAP.md`
    - **STATE.md mentions Phase 08.2 COMPLETE:** `grep -q "08.2.*COMPLETE\\|complete" .planning/STATE.md`
    - **08.2-VALIDATION.md exists and is committed:** `test -f .planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-VALIDATION.md && git log -1 --pretty=%s -- .planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-VALIDATION.md`
    - **Final close-out commit exists:** `git log --pretty=%s | head -3 | grep -q "08.2"`
    - **Locked-file invariants intact:** `git diff HEAD~5 HEAD -- scripts/cli.ts src/core/sampler.ts src/core/loader.ts src/core/project-file.ts | wc -l` returns `0`
    - **Full suite still GREEN:** `npx vitest run` exits 0
  </acceptance_criteria>

  <done>
    ROADMAP.md Phase 08.2 plan list shows 5 [x] entries with commit hashes + SUMMARY.md links; STATE.md advanced to Phase 08.2 COMPLETE; close-out commit captures the phase signoff. Phase 08.2 is shippable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-time globalThis.api stub → renderer code | Tests inject mocks via `vi.stubGlobal('api', ...)`; the renderer's defenses (handleBeforeDrop, modal-state derivations) are exercised against these stubs as if they were real preload methods. |
| Manual UAT — actual user → live menu | The user exercises real menu accelerators against a real Electron Menu in dev mode; this is the highest-fidelity verification of the entire 08.2 surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-05-01 | Repudiation | Manual UAT signoff is human; could the user mis-perceive a still-broken Cmd+O as fixed? | mitigate | Reproducer 3 (the canonical 08.1 UAT regression) explicitly walks through the recovery-banner state where the bug originally manifested. The expected outcome is unambiguous: "OS file picker opens." If it doesn't open, the user reports that specific failure. |
| T-08.2-05-02 | Tampering | Test mocks could mask a real bug — e.g., the api.openProject mock always returning ok:true | mitigate | Manual UAT (Task 3) is the load-bearing real-Electron verification. The 6 spec cases test logic flow (handler captured, handler invoked, downstream call made); the user UAT tests that the real menu accelerator fires the captured handler. Two-layer defense. |
| T-08.2-05-03 | Information Disclosure | The validation map cites commit hashes — could it leak any sensitive info? | accept | Commit hashes in a private workspace .planning/ folder are bounded to the workspace. Standard practice. |
| T-08.2-05-04 | Tampering | The test 8.2-MENU-04's mocking of openProjectFromFile could miss a real-world routing edge — e.g., a different DropZone branch | mitigate | The canonical reproducer is verified manually in Task 3 Reproducer 3. If the spec assertion path diverges from the manual flow, the manual UAT catches it. |

</threat_model>

<verification>
- All 6 cases in tests/renderer/save-load.spec.tsx 'Phase 08.2 menu wiring' describe block GREEN, including the canonical 8.2-MENU-04.
- The retired Keyboard shortcuts describe block (lines 92-121 pre-edit) is gone — replaced functionally by 8.2-MENU-06 (the inverse assertion).
- Full vitest suite GREEN: ≥ 297 passing.
- electron-vite build GREEN.
- CLI byte-for-byte unchanged.
- Layer 3 invariants intact (arch.spec.ts GREEN; recent.ts hygiene clean).
- 08.2-VALIDATION.md per-plan map committed.
- Manual UAT 4 reproducers signed off — including the canonical 08.1 UAT regression reproducer (Cmd+O on projectLoadFailed recovery banner).
- ROADMAP.md plan list shows 5 [x] entries with commit hashes + SUMMARY.md links.
- STATE.md advanced to Phase 08.2 COMPLETE.
</verification>

<success_criteria>
1. tests/renderer/save-load.spec.tsx ships 6 new 8.2-MENU cases including the canonical 8.2-MENU-04 (Cmd+O on projectLoadFailed regression test).
2. The deleted Keyboard shortcuts describe block is REPLACED by 8.2-MENU-06 (the inverse assertion).
3. Automated sweep (full vitest, build, CLI byte-for-byte, locked-file diffs, hygiene grep) all GREEN.
4. 08.2-VALIDATION.md per-plan map committed.
5. Manual UAT 4 reproducers signed off — Cmd+O fires in idle / error / projectLoadFailed; Open Recent round-trip works.
6. ROADMAP.md Phase 08.2 plan list flipped to 5 [x] entries; STATE.md advanced.
7. Final close-out commit captures Phase 08.2 COMPLETE.
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-cmd-o-blocked-during-error-state-discovered-in-08-1-uat-add-/08.2-05-SUMMARY.md` summarising:
- Two atomic commits before checkpoint (test extension, validation map + automated sweep)
- One atomic commit after manual UAT (close-out)
- Test count delta (+6 menu wiring, -2 retired keydown = +4 net)
- All 4 manual UAT reproducers passed
- Phase 08.2 COMPLETE — ready for Phase 9 OR `/gsd-verify-work 08.2`
</output>
