---
phase: 08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea
plan: 01
subsystem: ui
tags: [typescript, vitest, react, electron-ipc, discriminated-union, tdd-red]

# Dependency graph
requires:
  - phase: 08-save-load-project-state
    provides: SerializableError envelope (single interface), AppShell.onClickOpen with empty-string recovery payload, App.tsx AppState with no projectLoadFailed branch, two it.todo placeholders in tests/renderer/save-load.spec.tsx, single missing-skeleton spec in tests/main/project-io.spec.ts
provides:
  - SerializableError discriminated union — `'SkeletonNotFoundOnLoadError'` arm carries 7 cached recovery fields (projectPath, originalSkeletonPath, mergedOverrides, samplingHz, lastOutDir, sortColumn, sortDir); other 7 kinds keep {kind, message}
  - 4 RED renderer specs in tests/renderer/save-load.spec.tsx (8.1-VR-01 .stmproj-drop App.tsx recovery banner; 8.1-VR-02 AppShell toolbar Open threaded projectPath; 8.1-VR-03a dirty + .json drop SaveQuitDialog; 8.1-VR-03b dirty + .stmproj drop SaveQuitDialog)
  - 1 RED main spec in tests/main/project-io.spec.ts (8.1-IPC-01 threaded recovery fields)
  - Type-narrowing contract: `if (err.kind === 'SkeletonNotFoundOnLoadError')` exposes the 7 fields without `as` casts
affects: [08.1-02-main-io-thread-fields, 08.1-03-appshell-onClickOpen, 08.1-04-app-tsx-projectLoadFailed-and-onBeforeDrop-ref-bridge, 08.1-05-close-out]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Discriminated-union typed-error envelope: tag the kind so each error variant can carry kind-specific payload while staying serializable across the IPC boundary"
    - "Wave-0 RED-scaffolding (extension): land the type contract first, then commit failing specs that assert the post-fix contract — downstream plans drive specs to GREEN one at a time"

key-files:
  created: []
  modified:
    - "src/shared/types.ts (SerializableError interface → discriminated union; +29 LOC net per task commit)"
    - "tests/main/project-io.spec.ts (+29 LOC: new 8.1-IPC-01 spec inside the F9.2 describe block, AFTER the existing T-08-MISS spec)"
    - "tests/renderer/save-load.spec.tsx (+181/-21 LOC: App import, beforeEach mock extension, 2 it.todo placeholders flipped to 8.1-VR-03a/b real specs, new 8.1-VR-01 + 8.1-VR-02 describe blocks, redundant DropZone .stmproj describe deleted)"

key-decisions:
  - "Discriminated-union refactor (D-158) chosen over WR-02 fix (b) hide-button-on-empty-path: papering over the bug would still leave the toolbar Open path stuck on a dead-end recovery banner; principled fix gives both Open paths parity recovery data"
  - "DropZone .stmproj describe block deleted (not kept as a 1-todo placeholder): 8.1-VR-03b already exercises the .stmproj DropZone branch end-to-end via App.tsx, so a standalone DropZone-only it.todo would be redundant duplication. The CONTEXT D-166 baseline math '1 skipped, 1 todo' is satisfied by the surviving Phase 7 atlas-preview.spec.ts D-126 it.todo"
  - "RED specs commit BEFORE wiring (Wave-0 RED-scaffolding pattern): Plans 08.1-02/03/04/05 each drive one or more of the 5 grep-anchored test names to GREEN; locking the test contract first prevents inter-plan typecheck flapping (the same pattern from Plan 08-01)"

patterns-established:
  - "Discriminated-union error envelopes — when a typed-error arm needs to carry kind-specific payload (recovery context, retry hints, validation details), promote the flat interface to a tagged union with kind-specific arms. Future error envelopes (Phase 9 export errors, settings validation errors) should follow this shape rather than packing optional fields into a single shape"
  - "Wave-0 type-contract-first: when refactoring a shared type that crosses multiple consumers (5 here: project-io.ts, AppShell.tsx, App.tsx, ipc.ts, the test files), land the type extension as a standalone commit so downstream plans can rely on a stable contract — even if the type extension causes deliberate downstream typecheck breaks at the construction sites that need updating"

requirements-completed: [VR-01, VR-02, VR-03, F9.2, D-143, D-149]

# Metrics
duration: ~10 min
completed: 2026-04-26
---

# Phase 08.1 Plan 01: SerializableError discriminated-union + 5 RED specs Summary

**SerializableError promoted to discriminated union with 7-field SkeletonNotFoundOnLoadError recovery payload (D-158); 4 RED renderer specs + 1 RED main spec land the test contract for the locate-skeleton + new-skeleton-drop dirty-guard fixes.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-26T10:42:55Z (per STATE.md last_updated)
- **Completed:** 2026-04-26T10:50:18Z
- **Tasks:** 3/3
- **Files modified:** 3 (src/shared/types.ts, tests/main/project-io.spec.ts, tests/renderer/save-load.spec.tsx)

## Accomplishments

- **SerializableError discriminated union landed in src/shared/types.ts (D-158, D-171).** The `'SkeletonNotFoundOnLoadError'` arm now carries 7 cached recovery fields (`projectPath`, `originalSkeletonPath`, `mergedOverrides`, `samplingHz`, `lastOutDir`, `sortColumn`, `sortDir`); the other 7 kinds keep `{kind, message}`. Type-narrowing via `err.kind === 'SkeletonNotFoundOnLoadError'` typechecks without `as` casts. D-171 invariant intact: `src/core/project-file.ts` is NOT touched (the on-disk `.stmproj` v1 schema is unchanged).
- **5 grep-anchored RED specs locked the post-fix test contract.** 4 in `tests/renderer/save-load.spec.tsx` (8.1-VR-01 App.tsx recovery banner; 8.1-VR-02 AppShell threaded projectPath; 8.1-VR-03a/b SaveQuitDialog dirty-guard for new-skeleton-drop / new-project-drop). 1 in `tests/main/project-io.spec.ts` (8.1-IPC-01 threaded fields populated by `handleProjectOpenFromPath`). All 5 fail with the post-fix assertion shape, providing RED-gate evidence for downstream plans.
- **Both prior `it.todo` placeholders are gone** (lines 129 + 173 of save-load.spec.tsx). The "DropZone branch on .stmproj (D-142)" describe was deleted entirely — 8.1-VR-03b covers the .stmproj DropZone branch end-to-end via App.tsx, making the standalone DropZone-only shell redundant. `grep -c "it.todo" tests/renderer/save-load.spec.tsx` returns 0.

## Verbatim diff (Task 1) — SerializableError refactor

**BEFORE** (src/shared/types.ts:490-501, single interface):
```typescript
export interface SerializableError {
  kind:
    | 'SkeletonJsonNotFoundError'
    | 'AtlasNotFoundError'
    | 'AtlasParseError'
    | 'ProjectFileNotFoundError'
    | 'ProjectFileParseError'
    | 'ProjectFileVersionTooNewError'
    | 'SkeletonNotFoundOnLoadError'
    | 'Unknown';
  message: string;
}
```

**AFTER** (discriminated union per D-158 verbatim):
```typescript
export type SerializableError =
  | {
      kind: 'SkeletonNotFoundOnLoadError';
      message: string;
      // Cached recovery payload — fed to handleProjectReloadWithSkeleton
      // when the user picks a replacement skeleton (D-149 chain).
      projectPath: string;
      originalSkeletonPath: string;
      mergedOverrides: Record<string, number>;
      samplingHz: number;
      lastOutDir: string | null;
      sortColumn: string | null;
      sortDir: 'asc' | 'desc' | null;
    }
  | {
      kind:
        | 'SkeletonJsonNotFoundError'
        | 'AtlasNotFoundError'
        | 'AtlasParseError'
        | 'ProjectFileNotFoundError'
        | 'ProjectFileParseError'
        | 'ProjectFileVersionTooNewError'
        | 'Unknown';
      message: string;
    };
```

## New spec IDs (5 total)

| Spec ID | File | Status |
|---------|------|--------|
| 8.1-VR-01 | tests/renderer/save-load.spec.tsx | RED (Plan 08.1-04 turns GREEN — App.tsx `projectLoadFailed` AppState branch + recovery banner) |
| 8.1-VR-02 | tests/renderer/save-load.spec.tsx | RED (Plan 08.1-03 turns GREEN — AppShell.onClickOpen reads threaded fields instead of empty literals) |
| 8.1-VR-03a | tests/renderer/save-load.spec.tsx | RED (Plan 08.1-04 turns GREEN — D-163 onBeforeDropRef bridge for `.json` drop) |
| 8.1-VR-03b | tests/renderer/save-load.spec.tsx | RED (Plan 08.1-04 turns GREEN — D-163 onBeforeDropRef bridge for `.stmproj` drop) |
| 8.1-IPC-01 | tests/main/project-io.spec.ts | RED (Plan 08.1-02 turns GREEN — main `handleProjectOpenFromPath` populates 7 threaded fields from `materialized` + `absolutePath`) |

## Deliberate downstream typecheck breaks (Plan 08.1-02 fixes)

The discriminated-union refactor causes 5 typecheck failures in 2 files. All 5 are at construction sites that need to be updated to either thread the 7 new fields or narrow more precisely:

- `src/main/project-io.ts:338` (handleProjectOpenFromPath rescue branch) — `{kind: 'SkeletonNotFoundOnLoadError', message: err.message}` is now missing 7 required fields. Plan 08.1-02 reads from `materialized` (line 323) + `absolutePath` (line 261).
- `src/main/project-io.ts:505` (handleProjectReloadWithSkeleton rescue branch) — same break, same fix.
- `src/main/project-io.ts:349, 511` (generic `err.name as SerializableError['kind']` casts) — `SerializableError['kind']` now includes `'SkeletonNotFoundOnLoadError'` but the non-skeleton arm of the union excludes it. Plan 08.1-02 narrows the cast to the 7 non-skeleton kinds.
- `src/main/ipc.ts:257` (handleSkeletonLoad envelope) — same `err.name` cast pattern as above; same fix shape.

Pre-existing deferred error (out of scope for 08.1): `scripts/probe-per-anim.ts:14:31` TS2339 — logged in `.planning/phases/04-scale-overrides/deferred-items.md` and `.planning/phases/07-atlas-preview-modal/deferred-items.md` since Phase 4.

## Task Commits

Each task was committed atomically:

1. **Task 1: Refactor SerializableError to discriminated union (D-158, D-171)** — `765c2a4` (refactor)
2. **Task 2: Add 8.1-IPC-01 RED spec to tests/main/project-io.spec.ts (D-166)** — `a40fffc` (test)
3. **Task 3: Flip 2 it.todos + add 8.1-VR-01/02 → 4 RED renderer specs (D-166)** — `d756dd9` (test)

**Plan metadata commit:** appended at close-out (this commit).

## Files Created/Modified

- `src/shared/types.ts` — SerializableError flat interface → discriminated union; 7 new threaded fields on the SkeletonNotFoundOnLoadError arm; verbose JSDoc cites D-158/D-171/T-01-02-02/T-08.1-01.
- `tests/main/project-io.spec.ts` — added 1 new spec (8.1-IPC-01) inside the F9.2 describe, AFTER the existing T-08-MISS spec. Verifies the 7 threaded fields after type-narrowing via `result.error.kind === 'SkeletonNotFoundOnLoadError'`.
- `tests/renderer/save-load.spec.tsx` — added App import; extended beforeEach mocks (loadSkeletonFromFile, locateSkeleton, reloadProjectWithSkeleton, openProjectFromFile now have realistic resolved-value mocks instead of bare `vi.fn()`); replaced `it.todo('dirty + drop opens guard')` with two real specs (8.1-VR-03a + 8.1-VR-03b); added 8.1-VR-01 describe (App.tsx recovery banner) + 8.1-VR-02 describe (AppShell threaded projectPath); deleted the prior 'DropZone branch on .stmproj (D-142)' describe with the surviving `it.todo`.

## Decisions Made

- **D-158 verbatim adopted (chose WR-02 fix (a) over fix (b)).** Hiding the Locate button when path is empty (fix b) would paper over the bug instead of fixing it; users on the toolbar Open path would still hit the missing-skeleton dead end. The discriminated-union approach gives both Open paths parity recovery data.
- **DropZone .stmproj describe deleted, not preserved as a placeholder.** The plan offered a choice ("leave as it.todo" vs "delete entirely"). 8.1-VR-03b already mounts `<App />` and drops a `.stmproj` file, exercising the DropZone .stmproj branch end-to-end; the standalone DropZone-only shell would be redundant. CONTEXT D-166 baseline math "1 skipped, 1 todo" is satisfied by the surviving `tests/core/atlas-preview.spec.ts` D-126 it.todo (Phase 7).
- **Wave-0 RED-scaffolding pattern applied (matches Plan 08-01 precedent).** Type contract lands first; downstream plans drive grep-anchored specs to GREEN one at a time. Lesson #1 from Phase 8's STATE.md key-lessons: "the Wave-0 RED-spec scaffolding in Plan 01 paid off — every subsequent plan ran with zero rework on the test contract".

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - grep-literal hygiene] Reworded comment to avoid the literal `it.todo` string**
- **Found during:** Task 3 (final verification step)
- **Issue:** The plan's acceptance criterion `grep -c "it.todo" tests/renderer/save-load.spec.tsx` must return 0, but my initial inline comment ("the two prior `it.todo` placeholders are flipped to real RED specs") contained the literal `it.todo` substring and made the grep return 1.
- **Fix:** Reworded the comment to "the two prior placeholder shells are flipped to real RED specs" — same semantic content, zero `it.todo` literal.
- **Files modified:** tests/renderer/save-load.spec.tsx (1 comment block)
- **Verification:** `grep -c "it.todo" tests/renderer/save-load.spec.tsx` returns 0.
- **Committed in:** `d756dd9` (Task 3 commit)
- **Pattern:** Same grep-literal-compliance class as STATE.md notes for Plan 01-01 Dev #4, 01-02 Dev #3/#4, 01-03 Dev #3, 01-04 Dev #1, 02-01 Dev #3, 02-03, 04-01 Dev #1.

---

**Total deviations:** 1 auto-fixed (1 grep-literal hygiene)
**Impact on plan:** No scope creep. The fix preserved comment semantics while satisfying the literal grep gate.

## Issues Encountered

None — plan executed cleanly. The 5 RED specs fail with exactly the error shapes the plan predicted (`projectPath is undefined`, `expected '' to be '/a/b/proj.stmproj'`, `SaveQuitDialog with new-skeleton-drop title must mount: expected null to be truthy`, `projectLoadFailed banner should mount with role=alert: expected null to be truthy`).

## Locked-file invariants confirmed clean (vs Phase 8.1 baseline `90dd202`)

| File | Diff (lines) | Invariant |
|------|--------------|-----------|
| `src/core/project-file.ts` | 0 | D-171 — .stmproj v1 schema unchanged in 8.1 |
| `src/core/sampler.ts` | 0 | CLAUDE.md rule #3 + Phase 5 D-102 |
| `scripts/cli.ts` | 0 | Phase 5 D-102 — CLI byte-for-byte |
| `src/core/loader.ts` | 0 | Informal lock — no fixes need it |

`npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` exits 0 with CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000 — byte-for-byte unchanged.

## Test baseline impact

- **Before (Phase 8 close):** 270 passed + 1 skipped + 3 todo + 0 failed
- **After (Plan 08.1-01 close):** 270 passed + 1 skipped + 1 todo + 5 failed
- **Delta:** +5 RED specs (4 renderer + 1 main), −2 todos (both flipped/deleted), 0 GREEN flips (those land in Plans 02-05). Existing 270 GREEN specs preserved with no regressions.

CONTEXT D-166 baseline expectation tracking: "270 passed → ≥273 passed... 1 skipped, 3 todo → 1 skipped, 1 todo (the two flipped)". Plan 08.1-01 delivers the "1 skipped, 1 todo" half (the 5 RED specs flip to GREEN incrementally across Plans 02-05).

## TDD Gate Compliance

Plan 08.1-01 is the RED phase of a multi-plan TDD cycle (the GREEN/REFACTOR phases are Plans 08.1-02 through 08.1-05). The plan-level type was `tdd` with `tdd_mode: true`. Per the executor TDD discipline:

- Task 1 commit type is `refactor(...)` (not `test(...)`) because it lands a type contract change with no behavioral assertions yet — but the change is what makes the subsequent test() commits compile cleanly.
- Tasks 2 and 3 commits are `test(...)` and represent the RED gate. The post-Task-2/3 test runs prove the assertions FAIL with the post-fix shape (RED-gate evidence captured in commit messages).
- The GREEN gate for each spec lands in a downstream plan (08.1-02 turns 8.1-IPC-01 GREEN; 08.1-03 turns 8.1-VR-02 GREEN; 08.1-04 turns 8.1-VR-01 + VR-03a + VR-03b GREEN).
- The plan-level RED commit (`a40fffc` for IPC, `d756dd9` for renderer) exists in git log; downstream GREEN feat() commits follow per the standard TDD cycle.

## Next Phase Readiness

Plan 08.1-02 (Wave 1) is unblocked. It owns:
- Wiring the 7 threaded fields at `src/main/project-io.ts:333-343` (the `SkeletonJsonNotFoundError` rescue branch) — this clears the 4 deliberate downstream typecheck breaks AND turns 8.1-IPC-01 GREEN.
- Same threading at `src/main/project-io.ts:496-510` (the second SkeletonJsonNotFoundError rescue branch in `handleProjectReloadWithSkeleton`) — clears the 5th typecheck break.
- Narrowing the `err.name as SerializableError['kind']` casts at `src/main/project-io.ts:349, 511` and `src/main/ipc.ts:257` to exclude `'SkeletonNotFoundOnLoadError'` (since the SpineLoaderError catch arms can never produce that kind).

Subsequent plans (08.1-03 AppShell wire, 08.1-04 App.tsx + ref-bridge, 08.1-05 close-out) read the new SerializableError shape via type-narrowing — no further type contract changes expected.

## Self-Check: PASSED

**Files exist:**
- `src/shared/types.ts` — FOUND (modified)
- `tests/main/project-io.spec.ts` — FOUND (modified)
- `tests/renderer/save-load.spec.tsx` — FOUND (modified)

**Commits exist:**
- `765c2a4` (Task 1: refactor types) — FOUND
- `a40fffc` (Task 2: 8.1-IPC-01 RED) — FOUND
- `d756dd9` (Task 3: 4 RED renderer specs) — FOUND

**11 plan-level verification gates:**
- ✓ Gate 1: `grep -c "kind: 'SkeletonNotFoundOnLoadError'" src/shared/types.ts` = 1
- ✓ Gate 2: `grep -c "export type SerializableError" src/shared/types.ts` = 1
- ✓ Gate 3: `grep -c "export interface SerializableError" src/shared/types.ts` = 0
- ✓ Gate 4: `grep -c "8.1-VR-01\|8.1-VR-02\|8.1-VR-03a\|8.1-VR-03b" tests/renderer/save-load.spec.tsx` = 4
- ✓ Gate 5: `grep -c "8.1-IPC-01" tests/main/project-io.spec.ts` = 1
- ✓ Gate 6: `grep -c "it.todo" tests/renderer/save-load.spec.tsx` = 0
- ✓ Gate 7: `git diff 90dd202..HEAD -- src/core/project-file.ts | wc -l` = 0 (D-171 vs 8.1 baseline)
- ✓ Gate 8: `git diff 90dd202..HEAD -- src/core/sampler.ts | wc -l` = 0
- ✓ Gate 9: `git diff 90dd202..HEAD -- scripts/cli.ts | wc -l` = 0
- ✓ Gate 10: `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` exits 0 with CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000 unchanged
- ✓ Gate 11: type-narrowing `if (result.error.kind === 'SkeletonNotFoundOnLoadError')` compiles cleanly with no `as` casts at the 7-field read sites in tests/main/project-io.spec.ts

---
*Phase: 08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea*
*Completed: 2026-04-26*
