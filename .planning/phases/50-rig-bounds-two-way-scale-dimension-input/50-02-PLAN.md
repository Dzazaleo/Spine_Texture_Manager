---
phase: 50-rig-bounds-two-way-scale-dimension-input
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - src/renderer/src/modals/VariantDialog.tsx
  - src/renderer/src/modals/variant-scale-derive.ts
  - tests/renderer/variant-twoway.spec.ts
  - tests/renderer/variant-twoway.spec.tsx
autonomous: true
requirements: [SCALEUI-01]
tags: [renderer, variant-dialog, two-way-binding, scale, react, layer-3]

must_haves:
  truths:
    - "Both target width and target height are editable plus the factor, aspect-locked, scaling stays uniform — choosing which dimension anchors the still-uniform factor, never anisotropic (D-01)"
    - "The scale factor s is the single source of truth that export/bake consumes; the W/H px fields are views of s (px = s × bboxAxis); the last-edited field sets s and the others re-derive with no round-trip drift on the edited axis (D-02)"
    - "Typed pixel targets are honored EXACTLY — typing target W sets s = px ÷ bboxAxis with no snapping to nice factors; factor displayed as Number(s.toFixed(4)), pixels as whole numbers (D-03)"
    - "Over-range: a typed W or H giving s ≥ 1 is allowed and the ≥1 factor is shown, but Export is disabled and the existing inline scaled-down hint shows; the authoritative reject stays the main-side VariantScaleError (D-04)"
    - "The Scale card is enriched INLINE in the existing single-pane VariantDialog (replacing the basic field) with the tabs deferred to Phase 51 — no structural refactor (D-09)"
  artifacts:
    - path: "src/renderer/src/modals/variant-scale-derive.ts"
      provides: "pure helpers pxFromScale / scaleFromPx / displayFactor (renderer-local, Layer-3-safe, no core import)"
      exports: ["pxFromScale", "scaleFromPx", "displayFactor"]
    - path: "src/renderer/src/modals/VariantDialog.tsx"
      provides: "enriched Scale card: bbox W×H reference line + three coupled aspect-locked inputs"
      contains: "scaleFromPx"
    - path: "tests/renderer/variant-twoway.spec.ts"
      provides: "V8 pure derivation helper unit tests"
    - path: "tests/renderer/variant-twoway.spec.tsx"
      provides: "V9 two-way / V10 no-drift / V11 over-range / V12 no-geometry jsdom component tests"
  key_links:
    - from: "src/renderer/src/modals/VariantDialog.tsx"
      to: "props.summary.bbox"
      via: "reads the precomputed setup-pose bbox as the reference axes (no new IPC/props)"
      pattern: "summary\\.bbox|props\\.summary\\.bbox"
    - from: "src/renderer/src/modals/VariantDialog.tsx"
      to: "src/renderer/src/modals/variant-scale-derive.ts"
      via: "px field onChange calls scaleFromPx(px, axis) -> onScaleChange(s)"
      pattern: "scaleFromPx\\("
    - from: "src/renderer/src/modals/VariantDialog.tsx"
      to: "props.onScaleChange"
      via: "every field edit writes the canonical s through the existing prop"
      pattern: "onScaleChange\\("
---

<objective>
Deliver the two-way scale↔dimension input (SCALEUI-01): replace the basic numeric scale field in `VariantDialog.tsx` with the rig-bounds reference line + three coupled, aspect-locked inputs (factor / target-W px / target-H px) backed by pure, unit-testable derivation helpers. Reads `summary.bbox` from 50-01 — zero new IPC, zero new props.

Purpose: Let the animator think in pixels ("fit to 512 wide for the mobile atlas") OR in a factor, whichever is natural — entering one shows the others. "What you type is what you get": typed px targets are honored exactly (D-03, no snapping); the factor `s` stays the single source of truth that the existing Phase-49 export path consumes (D-02).

Output: `variant-scale-derive.ts` (the three pure helpers), the enriched in-place Scale card in `VariantDialog.tsx`, and the V8-V12 test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-rig-bounds-two-way-scale-dimension-input/50-CONTEXT.md
@.planning/phases/50-rig-bounds-two-way-scale-dimension-input/50-RESEARCH.md
@.planning/phases/50-rig-bounds-two-way-scale-dimension-input/50-PATTERNS.md
@.planning/phases/50-rig-bounds-two-way-scale-dimension-input/50-VALIDATION.md
@.planning/phases/50-rig-bounds-two-way-scale-dimension-input/50-01-PLAN.md

<interfaces>
<!-- Contracts the executor needs — extracted from live source this session. Use directly; no codebase exploration needed. -->

50-01 delivers (consume, do not re-implement):
```typescript
// src/shared/types.ts — SkeletonSummary additive field:
bbox: { w: number; h: number } | null;   // null = degenerate rig (no textured geometry)
```

VariantDialog existing seams to KEEP (src/renderer/src/modals/VariantDialog.tsx):
```typescript
// :77-79 — props already exist; DO NOT add new props for the bbox or px values.
/** D-05 basic numeric scale field (0 < s < 1). */
scale: number;                          // = props.scale, the canonical s (D-02)
onScaleChange: (n: number) => void;     // every field edit writes s through this

// :118-119 — the D-04/D-08 over-range pre-check to KEEP (disables Export + shows hint):
const scaleInvalid =
  !Number.isFinite(props.scale) || props.scale <= 0 || props.scale >= 1;

// :267 — the inline 4-decimal normalization (== variant-export.ts formatScaleToken); reuse, do NOT import the Node module:
const scaleToken = String(Number(props.scale.toFixed(4)));

// :318 — the parse-guard idiom for the existing scale input:
props.onScaleChange(Number.isFinite(parsed) ? parsed : 0);

// :326 — the existing inline hint shown when scaleInvalid; :565 — Export disabled={scaleInvalid}.
```
The field block to REPLACE is VariantDialog.tsx:299-331 (its own comment at :300 says "Phase 50 enriches this control"). AppShell already passes `summary={summary} scale={variantScale} onScaleChange={setVariantScale}` (AppShell.tsx:2584-2587) -> `summary.bbox` reaches the dialog with ZERO new wiring.

The pure helper bodies (RESEARCH Pattern 3 — exact):
```typescript
export const pxFromScale  = (s: number, axis: number): number => Math.round(s * axis);
export const scaleFromPx  = (px: number, axis: number): number => px / axis;   // D-03: EXACT, no snap
export const displayFactor = (s: number): number => Number(s.toFixed(4));      // == formatScaleToken (D-03)
```

Tailwind literal-class idiom to clone VERBATIM (Pitfall 5 — Tailwind v4 sees literal strings only):
- existing scale input class (VariantDialog.tsx:309-323 area): `"w-20 bg-surface border border-border text-fg px-2 py-1 disabled:opacity-50 disabled:cursor-not-allowed"`
- OptimizeDialog safety-buffer numeric field idiom (OptimizeDialog.tsx:647-677): labeled `<input type="number">` + parse-guard + clamp + `title` + trailing-unit `<span className="text-fg-muted">`; field class `"w-16 bg-surface border border-border text-fg px-2 py-1 disabled:opacity-50 disabled:cursor-not-allowed"`
- card wrapper (VariantDialog.tsx:302 / OptimizeDialog.tsx:645): `"border border-border rounded-md bg-surface p-3 mb-4"` with a `<span className="text-xs text-fg-muted mb-2 block">` heading
- inline-hint markup (VariantDialog.tsx:326-330): `<p className="mt-2 text-xs text-[color:var(--color-danger)]">`

The jsdom test harness (tests/renderer/variant-dialog.spec.tsx — copy verbatim):
```tsx
// @vitest-environment jsdom
import * as React from 'react';                                  // :17
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';   // :19
import { act, cleanup, fireEvent, render, screen, waitFor } from '@testing-library/react';
import { VariantDialog } from '../../src/renderer/src/modals/VariantDialog';   // :28
import type { SkeletonSummary } from '../../src/shared/types';   // :29
// beforeEach: vi.stubGlobal('api', { exportVariant, pickOutputDirectory, openOutputFolder });   :44
// afterEach: vi.unstubAllGlobals(); cleanup();
function makeSummary(): SkeletonSummary { /* :77 — has runtimeTag:'4.2'; ADD bbox */ }
function buildProps(over): ComponentProps<typeof VariantDialog> { /* :87 — has onScaleChange: vi.fn() :102 */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure derivation helpers + Wave-0 tests (V8 helpers, V9-V12 component shells, RED)</name>
  <files>src/renderer/src/modals/variant-scale-derive.ts, tests/renderer/variant-twoway.spec.ts, tests/renderer/variant-twoway.spec.tsx</files>
  <read_first>
    - tests/renderer/variant-dialog.spec.tsx (FULL — the jsdom harness at :17-114: imports, `vi.stubGlobal('api',...)` at :44, `makeSummary()` at :77 with `runtimeTag:'4.2'`, `buildProps()` at :87 with `onScaleChange: vi.fn()` at :102, the controlled-value + onScaleChange-fires assertions at :117-150)
    - src/renderer/src/modals/VariantDialog.tsx:77-79 (props), :118-119 (scaleInvalid), :267 (toFixed(4)), :299-331 (the field to replace), :565 (Export disabled)
    - src/shared/types.ts (the `bbox: {w,h}|null` field 50-01 added to SkeletonSummary)
  </read_first>
  <behavior>
    - V8 (pure helpers, D-03): `pxFromScale(0.5, 2190) === 1095`; `pxFromScale(s, axis) === Math.round(s*axis)` for sampled inputs; `scaleFromPx(512, 2190) === 512/2190` EXACTLY (no snapping — assert strict `===`, e.g. `scaleFromPx(512,2190)` equals `512/2190`, NOT a rounded value); `displayFactor(0.123456) === 0.1235` and `displayFactor(s) === Number(s.toFixed(4))`.
    - V9 (two-way binding, D-02): render `VariantDialog` with `bbox:{w:2190,h:1847}` and `scale:0.5`. (a) The width field displays `1095` and the height field displays `924` (round(0.5*1847)) — both derived from s. (b) Editing the width field to `512` fires `onScaleChange` with exactly `512/2190`. (c) Editing the factor field to `0.25` fires `onScaleChange` with `0.25`. Scaling stays uniform (one `s`, both axes follow — never two independent scales). Test name contains "two-way".
    - V10 (no drift, D-02): type `512` into the width field while it is focused; the field's displayed/controlled value stays `512` (does NOT re-round to `511`/`513`). Assert the input's value is `'512'` after the change while focused. Test name contains "no drift".
    - V11 (over-range, D-04): with `bbox:{w:2190,h:1847}`, type a width that makes `s >= 1` (e.g. `3000` -> s≈1.37). The entry is ALLOWED (onScaleChange fires with `3000/2190`), the displayed factor is the ≥1 value, the Export button is disabled, and the existing inline "scaled-down" hint is present. (Drive this by rendering with `scale: 1.37` to assert the disabled+hint state, AND by firing the over-range edit to assert it is allowed.) Test name contains "over-range".
    - V12 (no geometry, degenerate UI): render with `bbox: null`. The two px fields are disabled (and/or blank), the bbox reference line shows a graceful "no setup-pose geometry"-style message, and the factor field is still usable (typing a factor fires onScaleChange). Test name contains "no geometry".
  </behavior>
  <action>
    Create `src/renderer/src/modals/variant-scale-derive.ts` (renderer-local, Layer-3-safe — NO import of core/ or the Node-only `formatScaleToken`) with exactly the three helper bodies from the interfaces block. Add a doc-comment: these mirror `variant-export.ts` `formatScaleToken` (`Number(s.toFixed(4))`, D-03) but are renderer-local to honor the Layer-3 gate (arch.spec.ts:20); `scaleFromPx` is the EXACT quotient (no snapping, D-03) and `s` itself is never rounded (only the display).

    Create `tests/renderer/variant-twoway.spec.ts` (pure-helper unit, no jsdom): import the three helpers from `'../../src/renderer/src/modals/variant-scale-derive'` and implement V8 per the behavior block. This can go GREEN immediately once the helper file exists (it has no UI dependency).

    Create `tests/renderer/variant-twoway.spec.tsx` (jsdom component): copy the harness from `variant-dialog.spec.tsx:1-114` verbatim (the `@vitest-environment jsdom` pragma, the `vi.stubGlobal('api',...)` beforeEach, the afterEach cleanup). EXTEND its `makeSummary()` to include `bbox: { w: 2190, h: 1847 }` and add a `bbox: null` variant builder for V12. Reuse `buildProps({ scale, onScaleChange: vi.fn(), ... })`. Implement V9-V12 per the behavior block. To target fields, query by label/role or `data-testid` you add in Task 2 — author the tests to the field labels you will use ("Factor", "Width", "Height" or the equivalent), and note inline which testids/labels Task 2 must provide. These component tests are RED now (the enriched control does not yet exist).

    Run the suites. V8 should be GREEN (helpers exist); V9-V12 RED (control not built). Commit: `test(50-02): variant two-way derivation helpers + component test shells (V8-V12)`.
  </action>
  <verify>
    <automated>npx vitest run tests/renderer/variant-twoway.spec.ts && (npx vitest run tests/renderer/variant-twoway.spec.tsx 2>&1 | grep -E "FAIL|two-way|over-range|no drift|no geometry" | head; echo "V9-V12 RED expected (enriched control not yet built)")</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/renderer/src/modals/variant-scale-derive.ts` exits 0
    - `grep -E "export const pxFromScale|export const scaleFromPx|export const displayFactor" src/renderer/src/modals/variant-scale-derive.ts` finds all three
    - `grep "px / axis" src/renderer/src/modals/variant-scale-derive.ts` exits 0 (scaleFromPx exact, no snap) AND `grep "Number(s.toFixed(4))" src/renderer/src/modals/variant-scale-derive.ts` exits 0
    - `grep -vE "^\s*(//|\*)" src/renderer/src/modals/variant-scale-derive.ts | grep -cE "from ['\"](\.\.?/)*core/|formatScaleToken"` outputs 0 (no core import, no Node formatScaleToken — Layer-3-safe)
    - `npx vitest run tests/renderer/variant-twoway.spec.ts` exits 0 (V8 helpers GREEN)
    - `grep -E "two-way|no drift|over-range|no geometry" tests/renderer/variant-twoway.spec.tsx` finds all four test-name tokens
    - `grep "bbox" tests/renderer/variant-twoway.spec.tsx` exits 0 (makeSummary extended with bbox + a null variant)
    - The `.spec.tsx` suite FAILS (RED) — the enriched control does not exist yet
  </acceptance_criteria>
  <done>variant-scale-derive.ts exists with the three pure helpers (Layer-3-safe); V8 unit test GREEN; variant-twoway.spec.tsx authored with V9-V12 RED; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enrich the VariantDialog Scale card in place (GREEN for V9-V12)</name>
  <files>src/renderer/src/modals/VariantDialog.tsx</files>
  <read_first>
    - src/renderer/src/modals/VariantDialog.tsx (FULL — especially :77-79 props, :118-119 scaleInvalid, :247 the Enter-key gate, :267 toFixed(4), :299-331 the field block to REPLACE, :309-323 the existing input class string, :326-330 the inline hint markup, :565 Export disabled)
    - src/renderer/src/modals/OptimizeDialog.tsx:645-677 (the card wrapper + labeled numeric input idiom: parse-guard + clamp + title + trailing-unit span — the closest analog for the px fields)
    - src/renderer/src/modals/variant-scale-derive.ts (the helpers Task 1 created)
    - tests/renderer/variant-twoway.spec.tsx (the field labels/testids the tests query — implement to match)
    - src/shared/types.ts (the `bbox` field shape on SkeletonSummary)
  </read_first>
  <action>
    Enrich IN PLACE (D-09) — replace ONLY the field block at `VariantDialog.tsx:299-331`. Do NOT add new props, do NOT introduce tabs (deferred to Phase 51), do NOT restructure the dialog. Import the helpers: `import { pxFromScale, scaleFromPx, displayFactor } from './variant-scale-derive';`.

    Read the reference from the prop: `const bbox = props.summary.bbox;` (the field 50-01 added — already arrives via `summary={summary}`, AppShell.tsx:2584). KEEP the existing `scaleInvalid` pre-check (:118-119) and the existing Export `disabled={scaleInvalid}` (:565) and the existing inline hint (:326-330) untouched in behavior.

    Build the enriched Scale card (reuse the card-wrapper + `<span>` heading classes verbatim — Pitfall 5):
    1. **Bbox reference line.** If `bbox != null`: show `Setup-pose size: {Math.round(bbox.w)} × {Math.round(bbox.h)} px` (display-rounded; derivations use the UNROUNDED `bbox.w/h` — display rounding ≠ math rounding, RESEARCH Open Q1). If `bbox == null`: show a graceful `Setup-pose size: unavailable (no textured geometry)` (RESEARCH Open Q2).
    2. **Factor field** (controlled by `props.scale`): value shows `displayFactor(props.scale)`; onChange parses with the existing guard idiom (`const parsed = parseFloat(e.target.value); props.onScaleChange(Number.isFinite(parsed) ? parsed : 0);`).
    3. **Target Width field** (px): value shows `pxFromScale(props.scale, bbox.w)` when NOT the actively-edited field; on edit, render the user's raw typed string (no round-trip drift, D-02 / Pitfall 4 — track an "active field" state or the raw input string for the focused px field). onChange: `const parsed = parseFloat(e.target.value); if (bbox != null && bbox.w > 0 && Number.isFinite(parsed)) props.onScaleChange(scaleFromPx(parsed, bbox.w));` (guard `bbox != null && axis > 0` before dividing — RESEARCH §Security V5).
    4. **Target Height field** (px): symmetric with `bbox.h`.
    5. **Aspect lock + uniform:** all three fields are views of the single `s`; editing any one calls `onScaleChange(s)` and the other two re-derive from `props.scale` on the next render. There is exactly ONE scale — never two independent axis scales (D-01 uniform-only).
    6. **Degenerate (`bbox == null`):** disable (and blank) the two px fields; keep the factor field fully functional.
    7. **Over-range (D-04):** no new logic needed — the typed px sets `s` via `scaleFromPx`; when `s >= 1` the existing `scaleInvalid` already disables Export and shows the inline hint. Do NOT hard-clamp mid-edit (D-04 rejects it) — allow the entry.

    Give each field a stable label/`data-testid` matching what `variant-twoway.spec.tsx` queries. Reuse the existing input class string for the factor field; reuse the OptimizeDialog field class (`w-16 ...`) for the px fields; trailing-unit `<span className="text-fg-muted">px</span>` on the px fields. Keep the displayed factor mirroring `Number(props.scale.toFixed(4))` via `displayFactor` (D-03).

    Run the component suite — V9-V12 must go GREEN. Then `npm run typecheck:web` and the full directly-affected suites. Commit: `feat(50-02): enrich VariantDialog Scale card with two-way factor<->px control (in place, D-09)`.
  </action>
  <verify>
    <automated>npx vitest run tests/renderer/variant-twoway.spec.ts tests/renderer/variant-twoway.spec.tsx tests/renderer/variant-dialog.spec.tsx && npm run typecheck:web</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from './variant-scale-derive'" src/renderer/src/modals/VariantDialog.tsx` exits 0
    - `grep -E "props\.summary\.bbox|summary\.bbox" src/renderer/src/modals/VariantDialog.tsx` exits 0 (reads the precomputed reference)
    - `grep "scaleFromPx(" src/renderer/src/modals/VariantDialog.tsx` exits 0 (px edits set s exactly)
    - `grep "pxFromScale(" src/renderer/src/modals/VariantDialog.tsx` exits 0 (px fields derived from s)
    - `grep -E "bbox != null|bbox === null|bbox == null" src/renderer/src/modals/VariantDialog.tsx` exits 0 (degenerate guard + axis>0 before dividing)
    - `grep -cE "import .* from ['\"](\.\.?/)*core/" src/renderer/src/modals/VariantDialog.tsx` outputs 0 AND `grep -c "formatScaleToken" src/renderer/src/modals/VariantDialog.tsx` outputs 0 (Layer-3 — renderer never imports core/ or the Node token helper)
    - `grep -cE "tablist|role=\"tab\"|TabList|<Tab" src/renderer/src/modals/VariantDialog.tsx` outputs 0 (no tabs — deferred to Phase 51, D-09)
    - `npx vitest run tests/renderer/variant-twoway.spec.tsx` exits 0 (V9-V12 GREEN) AND `tests/renderer/variant-dialog.spec.tsx` still passes (no regression to Phase-49 dialog tests)
    - `npm run typecheck:web` exits 0
  </acceptance_criteria>
  <done>VariantDialog Scale card enriched in place: bbox reference line + factor/W/H coupled aspect-locked inputs reading summary.bbox, writing s via onScaleChange, exact px->s (no snap), no-drift on edited axis, over-range allowed-but-Export-disabled, degenerate graceful; V9-V12 + the existing variant-dialog tests green; typecheck:web clean; no tabs introduced; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard -> VariantDialog numeric inputs | Free-text typed into the factor/W/H fields. Already-parsed defensively (`parseFloat`/`parseInt` + `Number.isFinite` fallback). No new external/network input. |
| renderer -> core/main internals | The renderer must NOT reach into core/ or main modules; it consumes only the precomputed `props.summary.bbox` and the existing `scale`/`onScaleChange` props. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-FIN | Tampering / DoS (display) | px->scale derivation in VariantDialog | mitigate | Guard `bbox != null && axis > 0` before `scaleFromPx` divides (avoid `Infinity`/`NaN` from divide-by-zero on a degenerate rig); `parseFloat` + `Number.isFinite` fallback on every input; V12 asserts the `bbox == null` degenerate UI keeps the factor field usable with px fields disabled. |
| T-50-LAYER | Elevation of privilege (layer-boundary violation) | VariantDialog.tsx + variant-scale-derive.ts imports | mitigate | The renderer reads precomputed `summary.bbox` only; the three helpers are renderer-local (no core/ import, no Node `formatScaleToken` — the 1-liner is copied inline per D-03); the existing arch.spec.ts:20 renderer-↛-core grep enforces it; the V7 anchor (50-01) covers the core side. |
| T-50-RANGE | Tampering (out-of-range scale to the export edge) | over-range entry (s ≥ 1) | mitigate | Renderer disables Export + shows the inline hint as defense-in-depth (D-04, reusing the Phase-49 `scaleInvalid` pre-check); the AUTHORITATIVE reject stays the main-side `VariantScaleError` (unchanged this phase). V11 asserts the entry is allowed but Export is disabled. |

No HIGH-severity threats: this phase adds no auth/session/network/file-write/new-IPC surface — it is a local desktop UI control over already-loaded skeleton data; the authoritative `s`-range gate remains main-side (RESEARCH §Security Domain).
</threat_model>

<verification>
- V8: `npx vitest run tests/renderer/variant-twoway.spec.ts -t "derivation"` — pxFromScale/scaleFromPx (exact, no snap)/displayFactor.
- V9: `npx vitest run tests/renderer/variant-twoway.spec.tsx -t "two-way"` — editing factor updates both px; editing a px sets s=px/axis + re-derives the others; uniform (D-02).
- V10: `npx vitest run tests/renderer/variant-twoway.spec.tsx -t "no drift"` — typed 512 stays 512 while focused (D-02).
- V11: `npx vitest run tests/renderer/variant-twoway.spec.tsx -t "over-range"` — s≥1 allowed, ≥1 factor shown, Export disabled, hint shown (D-04, T-50-RANGE).
- V12: `npx vitest run tests/renderer/variant-twoway.spec.tsx -t "no geometry"` — bbox==null disables px fields, factor field usable (T-50-FIN).
- Wave merge: `npm run test` + `npm run typecheck:node` + `npm run typecheck:web` green (modulo the ~11 pre-existing tests/renderer/* MixBlend IMPORT failures, Phase-47-owned — project_renderer_mixblend_preexisting_failure).
- Manual UAT (the single screenshot gate, 50-VALIDATION §Manual-Only): in `npm run dev` open a 4.2 + a 4.3 project, open "Export Variant…", confirm the W×H reference is a sane size, type a factor and watch both px fields follow, type a target W (512) and confirm H follows aspect-locked + the typed value doesn't drift, type a value ≥ bbox and confirm Export disables with the hint. Record in `50-HUMAN-UAT.md` (avoid the opened≠rendered trap — the criterion is the rendered, correctly-coupled control).
</verification>

<success_criteria>
- The Scale card is enriched in place (D-09 — no tabs, no structural refactor) with a bbox W×H reference line + three coupled aspect-locked inputs (factor / target-W / target-H).
- The factor `s` is the single source of truth; px fields are views (`px = round(s × axis)`); the last-edited field sets `s` via the existing `onScaleChange`; no round-trip drift on the edited axis (D-02).
- Typed px honored exactly (`s = px / axis`, no snap); factor shown as `Number(s.toFixed(4))`, px as whole numbers (D-03).
- Over-range (`s ≥ 1`) allowed but Export disabled + inline hint, authoritative reject stays main-side (D-04).
- `bbox == null` degrades gracefully (px fields disabled, factor field usable).
- The renderer reads `summary.bbox` only — no new IPC/props, no core/ import (Layer-3, T-50-LAYER).
- V8-V12 green; `npm run typecheck:web` exits 0; the existing variant-dialog.spec.tsx still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/50-rig-bounds-two-way-scale-dimension-input/50-02-SUMMARY.md`.
</output>
