---
phase: 08-save-load-project-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - tests/core/project-file.spec.ts
  - tests/main/project-io.spec.ts
  - tests/renderer/save-load.spec.tsx
  - tests/arch.spec.ts
autonomous: true
requirements: [F9, F9.1, F9.2]
must_haves:
  truths:
    - "src/shared/types.ts exports ProjectFileV1 / ProjectFile / MaterializedProject / AppSessionState / SaveResponse / OpenResponse / LocateSkeletonResponse"
    - "src/shared/types.ts SerializableError union extended with the 4 Phase 8 kinds"
    - "src/shared/types.ts Api interface extended with 7 Phase 8 methods (5 invoke + 1 listener + 1 sender)"
    - "Three RED spec files exist with describe/it shells that all FAIL until later waves implement the modules"
    - "tests/arch.spec.ts grows an explicit electron-import grep for src/core/project-file.ts (in addition to existing fs/sharp grep on src/core/**/*.ts)"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "ProjectFileV1 + MaterializedProject + AppSessionState + 3 response envelopes + extended SerializableError + extended Api"
      contains: "ProjectFileV1"
    - path: "tests/core/project-file.spec.ts"
      provides: "8 RED case shells for validateProjectFile / serializeProjectFile / materializeProjectFile / migrate / relativizePath / hygiene"
      min_lines: 80
    - path: "tests/main/project-io.spec.ts"
      provides: "8 RED case shells for handleProjectSave / handleProjectSaveAs / handleProjectOpen / handleProjectOpenFromPath / handleLocateSkeleton with vi.mock electron + node:fs/promises prelude"
      min_lines: 80
    - path: "tests/renderer/save-load.spec.tsx"
      provides: "6 RED case shells covering Save click + dirty marker + SaveQuitDialog + Cmd+S/O + stale-override banner + DropZone branch"
      min_lines: 80
    - path: "tests/arch.spec.ts"
      provides: "Phase 8 electron-import block for src/core/project-file.ts (D-156 + T-08-LAYER)"
      contains: "src/core/project-file.ts"
  key_links:
    - from: "src/shared/types.ts"
      to: "ProjectFileV1 / MaterializedProject"
      via: "structuredClone-safe IPC contract (file-top docblock at types.ts:1-17)"
      pattern: "ProjectFileV1"
    - from: "tests/arch.spec.ts"
      to: "src/core/project-file.ts"
      via: "explicit electron-import grep block (loader.ts pattern of explicit file targeting)"
      pattern: "src/core/project-file.ts"
---

<objective>
Phase 8 Plan 01 — Wave 0 scaffolding. Lands the type contracts in `src/shared/types.ts` (ProjectFileV1 + MaterializedProject + AppSessionState + 3 response envelopes + extended SerializableError union + extended Api interface) and the three RED spec files (`tests/core/project-file.spec.ts`, `tests/main/project-io.spec.ts`, `tests/renderer/save-load.spec.tsx`) that subsequent waves drive GREEN. Adds a Phase 8 electron-import grep block to `tests/arch.spec.ts` so `src/core/project-file.ts`'s Layer-3 hygiene is locked at the test layer the moment it lands in Wave 2.

Purpose: Every subsequent wave executes against a stable contract. Types compile clean today; spec shells fail predictably (modules under test don't exist yet); arch invariant grows by exactly one line so Wave 2 can't accidentally import from electron in core.
Output: One MODIFIED file (`src/shared/types.ts`) + three NEW spec files + one MODIFIED arch test. Zero new dependencies (RESEARCH §"No new dependencies required").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-save-load-project-state/08-CONTEXT.md
@.planning/phases/08-save-load-project-state/08-RESEARCH.md
@.planning/phases/08-save-load-project-state/08-PATTERNS.md
@.planning/phases/08-save-load-project-state/08-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing IPC contracts the executor extends. Read these BEFORE adding new types. -->

From src/shared/types.ts:490-498 (existing — must NOT be removed, only extended):
```typescript
export interface SerializableError {
  kind: 'SkeletonJsonNotFoundError' | 'AtlasNotFoundError' | 'AtlasParseError' | 'Unknown';
  message: string;
}

export type LoadResponse =
  | { ok: true; summary: SkeletonSummary }
  | { ok: false; error: SerializableError };
```

From src/shared/types.ts:511-570 (existing Api shape — must extend, not replace):
```typescript
export interface Api {
  loadSkeletonFromFile: (file: File) => Promise<LoadResponse>;
  pickOutputDirectory: (defaultPath?: string) => Promise<string | null>;
  startExport: (...) => Promise<...>;
  // ... existing 7 methods ...
  onExportProgress: (handler: (e: ExportProgressEvent) => void) => () => void;
}
```

From src/main/ipc.ts:227-235 (handleSkeletonLoad first lines — trust-boundary input check pattern):
```typescript
if (typeof jsonPath !== 'string' || jsonPath.length === 0 || !jsonPath.endsWith('.json')) {
  return { ok: false, error: { kind: 'Unknown', message: `Invalid path argument: ...` } };
}
```

From tests/arch.spec.ts:116-134 (existing core grep — auto-scans src/core/**/*.ts; loader.ts exempted):
```typescript
const files = globSync('src/core/**/*.ts');
// ...
if (/from ['"]sharp['"]|from ['"]node:fs(\/promises)?['"]|from ['"]fs(\/promises)?['"]/.test(text)) {
  // offenders.push(file)
}
```

From tests/renderer/atlas-preview-modal.spec.tsx:1-22 (jsdom prelude for Phase 8 spec):
```typescript
// @vitest-environment jsdom
import { afterEach, describe, expect, it, vi } from 'vitest';
import { cleanup, render, screen, fireEvent } from '@testing-library/react';
afterEach(cleanup);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend src/shared/types.ts with Phase 8 contracts</name>
  <files>src/shared/types.ts</files>
  <read_first>
- src/shared/types.ts (full file — top docblock locks structuredClone-safe; SerializableError at line 490; Api at line 511)
- .planning/phases/08-save-load-project-state/08-PATTERNS.md (§"src/shared/types.ts (MODIFIED — extend types)" lines 690-795)
- .planning/phases/08-save-load-project-state/08-CONTEXT.md (D-145 schema fields list; D-148 documentation slot; D-156 validator envelope shape)
  </read_first>
  <action>
**Append-only edit. Do NOT remove or rename any existing identifier.**

1. **Extend `SerializableError` union** at line 490. Replace the existing 4-kind union with an 8-kind union by adding 4 Phase 8 kinds — keep existing 4 in the same order, append the 4 new ones BEFORE `'Unknown'`:

```typescript
export interface SerializableError {
  kind:
    | 'SkeletonJsonNotFoundError'
    | 'AtlasNotFoundError'
    | 'AtlasParseError'
    | 'ProjectFileNotFoundError'      // Phase 8 D-149: file missing on disk
    | 'ProjectFileParseError'          // Phase 8 Pitfall 9: JSON.parse SyntaxError
    | 'ProjectFileVersionTooNewError'  // Phase 8 D-151: version > 1
    | 'SkeletonNotFoundOnLoadError'    // Phase 8 D-149: triggers locate-skeleton flow
    | 'Unknown';
  message: string;
}
```

2. **Add `ProjectFileV1` + `ProjectFile` alias** (the schema shape from D-145). Place AFTER `LoadResponse` and BEFORE the `Api` interface:

```typescript
/**
 * Phase 8 — .stmproj v1 schema (D-145..D-156).
 *
 * Persisted to disk as JSON. structuredClone-safe by construction: only
 * primitives, plain objects, and nested Records — no Map, no class instances.
 * Maps live ONLY in renderer/main memory; the IPC + on-disk shape uses
 * Record<string, number> for `overrides` (Pitfall 3 boundary conversion).
 *
 * Required fields per D-145: version, skeletonPath, overrides, documentation.
 * All other fields are nullable; Load treats null as "use default".
 */
export interface ProjectFileV1 {
  version: 1;
  skeletonPath: string;
  atlasPath: string | null;
  imagesDir: string | null;
  overrides: Record<string, number>;
  samplingHz: number | null;
  lastOutDir: string | null;
  sortColumn: string | null;
  sortDir: 'asc' | 'desc' | null;
  documentation: object;
}

export type ProjectFile = ProjectFileV1;
```

3. **Add `AppSessionState`** (the in-memory shape AppShell builds before invoking save):

```typescript
/**
 * Phase 8 — the renderer-side state snapshot AppShell hands to saveProject /
 * saveProjectAs. Same shape as ProjectFileV1 minus `version` and `documentation`
 * (those are stamped by serializeProjectFile in src/core/project-file.ts).
 */
export interface AppSessionState {
  skeletonPath: string;
  atlasPath: string | null;
  imagesDir: string | null;
  overrides: Record<string, number>;
  samplingHz: number | null;
  lastOutDir: string | null;
  sortColumn: string | null;
  sortDir: 'asc' | 'desc' | null;
}
```

4. **Add `MaterializedProject`** (the in-memory shape Open returns to AppShell, with re-sampled summary):

```typescript
/**
 * Phase 8 — Open response payload. Built by main/project-io.ts after re-sampling
 * the resolved skeleton. `restoredOverrides` is INTERSECTED with the re-sampled
 * peaks (D-150) — `staleOverrideKeys` lists names that were dropped.
 */
export interface MaterializedProject {
  summary: SkeletonSummary;
  restoredOverrides: Record<string, number>;
  staleOverrideKeys: string[];
  samplingHz: number;
  lastOutDir: string | null;
  sortColumn: string | null;
  sortDir: 'asc' | 'desc' | null;
  projectFilePath: string;
}
```

5. **Add 3 response envelopes** (mirror `LoadResponse` shape):

```typescript
export type SaveResponse =
  | { ok: true; path: string }
  | { ok: false; error: SerializableError };

export type OpenResponse =
  | { ok: true; project: MaterializedProject }
  | { ok: false; error: SerializableError };

export type LocateSkeletonResponse =
  | { ok: true; newPath: string }
  | { ok: false };
```

6. **Extend `Api` interface** at line 511. Append AFTER the existing `onExportProgress` member, BEFORE the closing brace:

```typescript
  // Phase 8 — project file IPC surface (D-140..D-156).
  saveProject: (state: AppSessionState, currentPath: string | null) => Promise<SaveResponse>;
  saveProjectAs: (state: AppSessionState, defaultDir: string, defaultBasename: string) => Promise<SaveResponse>;
  openProject: () => Promise<OpenResponse>;
  openProjectFromFile: (file: File) => Promise<OpenResponse>;
  openProjectFromPath: (absolutePath: string) => Promise<OpenResponse>;
  locateSkeleton: (originalPath: string) => Promise<LocateSkeletonResponse>;
  onCheckDirtyBeforeQuit: (handler: () => void) => () => void;
  confirmQuitProceed: () => void;
```

**WHY 7 not 5:** RESEARCH §System Architecture lists 5 invoke methods plus the dual-one-way quit channel (1 listener + 1 sender = 2 more). PATTERNS.md "Phase 8 additions" block §preload section confirms 7. `openProjectFromFile` (DropZone path) is the wrapper that resolves File→absolute path via webUtils.getPathForFile.

**Verify the file compiles:** Run `npx tsc --noEmit -p tsconfig.web.json` AND `npx tsc --noEmit -p tsconfig.node.json` — both must exit 0. The 3 spec files added in Tasks 2-4 will reference these types but don't exist yet; tsc may skip them (they're test files); if compile errors surface there, fix them in those tasks, NOT here.

**Layer 3 invariant note:** `ProjectFileV1.overrides` is `Record<string, number>`, NOT `Map`. Pitfall 3 conversion happens in core/project-file.ts (Wave 2). The IPC payload + on-disk shape NEVER carry Maps.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json && npx tsc --noEmit -p tsconfig.node.json</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "export interface ProjectFileV1" src/shared/types.ts` returns `1`
- `grep -c "export type ProjectFile = ProjectFileV1" src/shared/types.ts` returns `1`
- `grep -c "export interface AppSessionState" src/shared/types.ts` returns `1`
- `grep -c "export interface MaterializedProject" src/shared/types.ts` returns `1`
- `grep -c "export type SaveResponse" src/shared/types.ts` returns `1`
- `grep -c "export type OpenResponse" src/shared/types.ts` returns `1`
- `grep -c "export type LocateSkeletonResponse" src/shared/types.ts` returns `1`
- `grep -c "ProjectFileNotFoundError" src/shared/types.ts` returns `1`
- `grep -c "ProjectFileParseError" src/shared/types.ts` returns `1`
- `grep -c "ProjectFileVersionTooNewError" src/shared/types.ts` returns `1`
- `grep -c "SkeletonNotFoundOnLoadError" src/shared/types.ts` returns `1`
- `grep -c "saveProject:" src/shared/types.ts` returns `1`
- `grep -c "saveProjectAs:" src/shared/types.ts` returns `1`
- `grep -c "openProject:" src/shared/types.ts` returns `1`
- `grep -c "openProjectFromFile:" src/shared/types.ts` returns `1`
- `grep -c "openProjectFromPath:" src/shared/types.ts` returns `1`
- `grep -c "locateSkeleton:" src/shared/types.ts` returns `1`
- `grep -c "onCheckDirtyBeforeQuit:" src/shared/types.ts` returns `1`
- `grep -c "confirmQuitProceed:" src/shared/types.ts` returns `1`
- `npx tsc --noEmit -p tsconfig.web.json` exits 0
- `npx tsc --noEmit -p tsconfig.node.json` exits 0 (pre-existing scripts/probe-per-anim.ts TS2339 unrelated to Phase 8)
- Existing fields untouched: `grep -c "loadSkeletonFromFile:" src/shared/types.ts` still returns `1`; `grep -c "onExportProgress:" src/shared/types.ts` still returns `1`
  </acceptance_criteria>
  <done>src/shared/types.ts compiles clean on both projects with the 7 new types, 4 new SerializableError kinds, and 8 new Api members. No existing identifier removed.</done>
</task>

<task type="auto">
  <name>Task 2: RED test stubs for src/core/project-file.ts (tests/core/project-file.spec.ts)</name>
  <files>tests/core/project-file.spec.ts</files>
  <read_first>
- tests/core/overrides.spec.ts (full file — Phase 4 baseline for behavior + hygiene + parity describe blocks; 25 specs in 4 describes)
- .planning/phases/08-save-load-project-state/08-PATTERNS.md (§"tests/core/project-file.spec.ts (NEW)" lines 379-425; §Pattern A; §Pattern B)
- .planning/phases/08-save-load-project-state/08-VALIDATION.md (per-task verification map rows 8-XX-01 through 8-XX-06; the `-t "..."` strings are LOAD-BEARING test names — the spec MUST contain those test titles verbatim)
- .planning/phases/08-save-load-project-state/08-CONTEXT.md (D-148 documentation slot; D-151 newer-version error; D-155 cross-volume; D-156 validator envelope)
  </read_first>
  <action>
**Create `tests/core/project-file.spec.ts`** as a RED stub: every spec MUST fail today because `src/core/project-file.ts` does not exist yet. Wave 2 Plan 02 makes them GREEN.

**Spec file shell:**

```typescript
import { describe, expect, it } from 'vitest';
import { readFileSync } from 'node:fs';
import * as path from 'node:path';
// NB: src/core/project-file.ts does not exist until Plan 02. The import line
// below is what drives the RED. Once Plan 02 lands, all describes turn GREEN.
import {
  validateProjectFile,
  migrate,
  serializeProjectFile,
  materializeProjectFile,
  relativizePath,
  absolutizePath,
} from '../../src/core/project-file.js';
import type { ProjectFileV1, AppSessionState } from '../../src/shared/types.js';

const CORE_SRC = path.resolve('src/core/project-file.ts');

describe('validateProjectFile (D-156)', () => {
  it('validator rejects non-object input', () => {
    expect(validateProjectFile(null).ok).toBe(false);
    expect(validateProjectFile(42).ok).toBe(false);
    expect(validateProjectFile('foo').ok).toBe(false);
  });

  it('validator rejects missing version', () => {
    const r = validateProjectFile({ skeletonPath: 'x.json', overrides: {}, documentation: {} });
    expect(r.ok).toBe(false);
    if (!r.ok) expect(r.error.kind).toBe('invalid-shape');
  });

  it('validator rejects missing skeletonPath', () => {
    const r = validateProjectFile({ version: 1, overrides: {}, documentation: {} });
    expect(r.ok).toBe(false);
    if (!r.ok) expect(r.error.kind).toBe('invalid-shape');
  });

  it('validator rejects wrong-type overrides', () => {
    const r = validateProjectFile({
      version: 1,
      skeletonPath: 'x.json',
      overrides: { CIRCLE: 'not-a-number' },
      documentation: {},
    });
    expect(r.ok).toBe(false);
    if (!r.ok) expect(r.error.kind).toBe('invalid-shape');
  });

  it('validator accepts minimal v1 file', () => {
    const r = validateProjectFile({
      version: 1,
      skeletonPath: './SIMPLE.json',
      atlasPath: null,
      imagesDir: null,
      overrides: {},
      samplingHz: null,
      lastOutDir: null,
      sortColumn: null,
      sortDir: null,
      documentation: {},
    });
    expect(r.ok).toBe(true);
  });

  it('validator accepts full v1 file', () => {
    const r = validateProjectFile({
      version: 1,
      skeletonPath: './SIMPLE.json',
      atlasPath: './SIMPLE.atlas',
      imagesDir: './images',
      overrides: { CIRCLE: 50, SQUARE: 75 },
      samplingHz: 120,
      lastOutDir: '/tmp/out',
      sortColumn: 'attachmentName',
      sortDir: 'asc',
      documentation: {},
    });
    expect(r.ok).toBe(true);
  });

  it('newer version rejected (D-151)', () => {
    const r = validateProjectFile({
      version: 2,
      skeletonPath: 'x.json',
      overrides: {},
      documentation: {},
    });
    expect(r.ok).toBe(false);
    if (!r.ok) expect(r.error.kind).toBe('newer-version');
  });
});

describe('round-trip (D-145 + D-148 + D-155)', () => {
  it('round-trip preserves all D-145 fields', () => {
    const state: AppSessionState = {
      skeletonPath: '/a/b/SIMPLE.json',
      atlasPath: '/a/b/SIMPLE.atlas',
      imagesDir: '/a/b/images',
      overrides: { CIRCLE: 50 },
      samplingHz: 120,
      lastOutDir: '/tmp/out',
      sortColumn: 'attachmentName',
      sortDir: 'asc',
    };
    const file = serializeProjectFile(state, '/a/b/proj.stmproj');
    const back = materializeProjectFile(file, '/a/b/proj.stmproj');
    expect(back.skeletonPath).toBe(state.skeletonPath);
    expect(back.atlasPath).toBe(state.atlasPath);
    expect(back.overrides).toEqual(state.overrides);
  });

  it('documentation slot preserved on round-trip (D-148)', () => {
    const state: AppSessionState = {
      skeletonPath: '/a/b/SIMPLE.json',
      atlasPath: null, imagesDir: null,
      overrides: {}, samplingHz: null, lastOutDir: null,
      sortColumn: null, sortDir: null,
    };
    const file = serializeProjectFile(state, '/a/b/proj.stmproj');
    expect(file.documentation).toEqual({});
    // And a non-empty doc must survive validate→materialize unchanged:
    const withDoc: ProjectFileV1 = { ...file, documentation: { nonEmpty: 'value' } };
    const v = validateProjectFile(withDoc);
    expect(v.ok).toBe(true);
    if (v.ok) expect((v.project as ProjectFileV1).documentation).toEqual({ nonEmpty: 'value' });
  });

  it('round-trip relative paths (D-155)', () => {
    const state: AppSessionState = {
      skeletonPath: '/a/b/SIMPLE.json',
      atlasPath: '/a/b/SIMPLE.atlas',
      imagesDir: '/a/b/images',
      overrides: {}, samplingHz: null, lastOutDir: null,
      sortColumn: null, sortDir: null,
    };
    const file = serializeProjectFile(state, '/a/b/proj.stmproj');
    // Same directory → './SIMPLE.json'
    expect(file.skeletonPath.startsWith('.') || file.skeletonPath === 'SIMPLE.json').toBe(true);
    expect(file.skeletonPath.endsWith('SIMPLE.json')).toBe(true);
    const back = materializeProjectFile(file, '/a/b/proj.stmproj');
    expect(back.summary.skeletonPath ?? back.projectFilePath).toBeDefined(); // shape lock — see Plan 02
  });

  it('cross-volume falls back to absolute (D-155 + Pitfall 4)', () => {
    // Cross-volume detection: roots differ → store absolute.
    // POSIX: /Volumes/Other vs /Users — both rooted at '/' but different mount.
    // We can't reliably create a true cross-volume condition in Node tests;
    // instead, exercise relativizePath directly with a Windows-style cross-drive
    // input string. relativizePath returns string regardless of platform — the
    // Node path module's parse() differentiates.
    // On POSIX runner, simulate by checking that a deeply-different root path
    // round-trips identically (no false-relative). The Plan 02 implementation
    // codifies the rule from RESEARCH §Pitfall 4.
    const result = relativizePath('/Volumes/Other/foo.json', '/Users/x/proj.stmproj');
    // On POSIX these share root '/', so path.relative gives '../../Volumes/Other/foo.json' —
    // the implementation's heuristic flips this to absolute when path.parse roots differ.
    // We assert the round-trip property: absolutizePath(stored, basedir) MUST equal input.
    const restored = absolutizePath(result, '/Users/x');
    expect(restored).toBe('/Volumes/Other/foo.json');
  });
});

describe('migrate (D-151)', () => {
  it('migrate is identity on v1', () => {
    const file: ProjectFileV1 = {
      version: 1, skeletonPath: 'x.json', atlasPath: null, imagesDir: null,
      overrides: {}, samplingHz: null, lastOutDir: null,
      sortColumn: null, sortDir: null, documentation: {},
    };
    expect(migrate(file)).toBe(file); // reference equality on v1 passthrough
  });
});

describe('hygiene — Layer 3 invariant for src/core/project-file.ts (T-08-LAYER)', () => {
  it('does not import node:fs / node:fs/promises / sharp / electron', () => {
    const src = readFileSync(CORE_SRC, 'utf8');
    expect(src, 'core/project-file.ts must not import node:fs').not.toMatch(/from ['"]node:fs(\/promises)?['"]/);
    expect(src, 'core/project-file.ts must not import fs (no scheme)').not.toMatch(/from ['"]fs(\/promises)?['"]/);
    expect(src, 'core/project-file.ts must not import sharp').not.toMatch(/from ['"]sharp['"]/);
    expect(src, 'core/project-file.ts must not import electron').not.toMatch(/from ['"]electron['"]/);
    // node:path IS permitted — see tests/arch.spec.ts:116-134 (no path block in regex).
  });
});
```

**Test names listed in 08-VALIDATION.md MUST appear verbatim** in `it(...)` strings — `validator rejects`, `validator accepts`, `documentation slot preserved`, `round-trip relative paths`, `cross-volume falls back to absolute`, `newer version rejected`. These are the `-t "..."` selectors VALIDATION.md cites.

**RED gate:** Running `npx vitest run tests/core/project-file.spec.ts` MUST exit non-zero with import errors (module not found) until Plan 02 lands.
  </action>
  <verify>
    <automated>npx vitest run tests/core/project-file.spec.ts 2>&1 | grep -E "(Error|Cannot find|failed|FAIL)" | head -3</automated>
  </verify>
  <acceptance_criteria>
- File `tests/core/project-file.spec.ts` exists
- `grep -c "validator rejects" tests/core/project-file.spec.ts` returns at least `4` (rejects non-object / missing version / missing skeletonPath / wrong-type overrides)
- `grep -c "validator accepts" tests/core/project-file.spec.ts` returns at least `2` (minimal + full)
- `grep -c "documentation slot preserved" tests/core/project-file.spec.ts` returns `1`
- `grep -c "round-trip relative paths" tests/core/project-file.spec.ts` returns `1`
- `grep -c "cross-volume falls back to absolute" tests/core/project-file.spec.ts` returns `1`
- `grep -c "newer version rejected" tests/core/project-file.spec.ts` returns `1`
- `grep -c "from '../../src/core/project-file" tests/core/project-file.spec.ts` returns `1` (RED-driving import)
- `grep -c "T-08-LAYER" tests/core/project-file.spec.ts` returns at least `1` (threat ref in hygiene describe)
- `npx vitest run tests/core/project-file.spec.ts` exits non-zero (RED — module under test does not exist yet)
  </acceptance_criteria>
  <done>RED spec shell with 8 case blocks (validator × 4 reject + × 2 accept + newer-version + round-trip × 3 + migrate identity + hygiene grep) committed and failing predictably with module-not-found.</done>
</task>

<task type="auto">
  <name>Task 3: RED test stubs for src/main/project-io.ts (tests/main/project-io.spec.ts)</name>
  <files>tests/main/project-io.spec.ts</files>
  <read_first>
- tests/main/ipc-export.spec.ts (full file — vi.mock electron + node:fs/promises recipe; beforeEach mock-reset pattern at lines 19-71)
- .planning/phases/08-save-load-project-state/08-PATTERNS.md (§"tests/main/project-io.spec.ts (NEW)" lines 428-475; §Pattern I main-process test prelude)
- .planning/phases/08-save-load-project-state/08-VALIDATION.md (per-task map rows 8-XX-07 through 8-XX-13; test-name strings are load-bearing)
- .planning/phases/08-save-load-project-state/08-RESEARCH.md (Pitfall 9 ProjectFileParseError; Pitfall 5 webUtils empty path)
  </read_first>
  <action>
**Create `tests/main/project-io.spec.ts`** as a RED stub. Every spec fails today because `src/main/project-io.ts` does not exist. Wave 3 Plan 03 makes them GREEN.

**Spec file shell:**

```typescript
import { describe, expect, it, vi, beforeEach } from 'vitest';
// NB: src/main/project-io.ts does not exist until Plan 03. RED until then.
import {
  handleProjectSave,
  handleProjectSaveAs,
  handleProjectOpen,
  handleProjectOpenFromPath,
  handleLocateSkeleton,
} from '../../src/main/project-io.js';
import type { AppSessionState } from '../../src/shared/types.js';

vi.mock('electron', () => ({
  dialog: { showSaveDialog: vi.fn(), showOpenDialog: vi.fn() },
  ipcMain: { handle: vi.fn(), on: vi.fn() },
  BrowserWindow: { getFocusedWindow: vi.fn(() => null) },
  app: { whenReady: vi.fn(), quit: vi.fn(), on: vi.fn() },
}));

vi.mock('node:fs/promises', () => ({
  readFile: vi.fn(),
  writeFile: vi.fn(),
  rename: vi.fn(),
  access: vi.fn(),
  mkdir: vi.fn(),
  constants: { F_OK: 0, R_OK: 4 },
}));

beforeEach(async () => {
  vi.clearAllMocks();
  const fsPromises = await import('node:fs/promises');
  // Default: writes succeed; reads succeed by default but tests override per case.
  vi.mocked(fsPromises.writeFile).mockResolvedValue(undefined);
  vi.mocked(fsPromises.rename).mockResolvedValue(undefined);
  vi.mocked(fsPromises.access).mockResolvedValue(undefined);
});

const baseState: AppSessionState = {
  skeletonPath: '/a/b/SIMPLE.json',
  atlasPath: '/a/b/SIMPLE.atlas',
  imagesDir: '/a/b/images',
  overrides: { CIRCLE: 50 },
  samplingHz: 120,
  lastOutDir: null,
  sortColumn: 'attachmentName',
  sortDir: 'asc',
};

describe('handleProjectSave / handleProjectSaveAs (F9.1, T-08-IO)', () => {
  it('save writes file with all D-145 fields', async () => {
    const fs = await import('node:fs/promises');
    const result = await handleProjectSave(baseState, '/a/b/proj.stmproj');
    expect(result.ok).toBe(true);
    expect(vi.mocked(fs.writeFile)).toHaveBeenCalled();
    const writtenJson = vi.mocked(fs.writeFile).mock.calls[0][1] as string;
    const parsed = JSON.parse(writtenJson);
    expect(parsed.version).toBe(1);
    expect(parsed.skeletonPath).toBeDefined();
    expect(parsed.overrides).toEqual({ CIRCLE: 50 });
    expect(parsed.documentation).toEqual({});
    expect(parsed.samplingHz).toBe(120);
    expect(parsed.sortColumn).toBe('attachmentName');
  });

  it('atomic-write tmp then rename (D-141 + Pattern B)', async () => {
    const fs = await import('node:fs/promises');
    await handleProjectSave(baseState, '/a/b/proj.stmproj');
    const writeArg = vi.mocked(fs.writeFile).mock.calls[0][0] as string;
    expect(writeArg).toMatch(/\.stmproj\.tmp$/);
    const renameArgs = vi.mocked(fs.rename).mock.calls[0];
    expect(renameArgs[0]).toMatch(/\.stmproj\.tmp$/);
    expect(renameArgs[1]).toBe('/a/b/proj.stmproj');
  });

  it('save with currentPath skips dialog', async () => {
    const electron = await import('electron');
    await handleProjectSave(baseState, '/a/b/proj.stmproj');
    expect(vi.mocked(electron.dialog.showSaveDialog)).not.toHaveBeenCalled();
  });

  it('save-as opens dialog with correct defaultPath', async () => {
    const electron = await import('electron');
    vi.mocked(electron.dialog.showSaveDialog).mockResolvedValue({
      canceled: false,
      filePath: '/picked/MyRig.stmproj',
    } as Awaited<ReturnType<typeof electron.dialog.showSaveDialog>>);
    await handleProjectSaveAs(baseState, '/a/b', 'MyRig');
    const dialogArgs = vi.mocked(electron.dialog.showSaveDialog).mock.calls[0];
    const opts = dialogArgs[dialogArgs.length - 1] as Electron.SaveDialogOptions;
    expect(opts.defaultPath).toMatch(/MyRig\.stmproj$/);
    expect(opts.filters?.[0]?.extensions).toContain('stmproj');
  });
});

describe('handleProjectOpen / handleProjectOpenFromPath (F9.2)', () => {
  it('load restores overrides verbatim', async () => {
    const fs = await import('node:fs/promises');
    const json = JSON.stringify({
      version: 1,
      skeletonPath: '/a/b/SIMPLE.json',
      atlasPath: null, imagesDir: null,
      overrides: { CIRCLE: 50, SQUARE: 75 },
      samplingHz: null, lastOutDir: null,
      sortColumn: null, sortDir: null,
      documentation: {},
    });
    vi.mocked(fs.readFile).mockResolvedValue(json as unknown as string);
    // Plan 03 wires loadSkeleton + sampleSkeleton; mock tests assert the response shape.
    const result = await handleProjectOpenFromPath('/a/b/proj.stmproj');
    // Whether ok:true depends on whether loadSkeleton mock resolves — Plan 03
    // wires the loader/sampler chain; test is RED until then. The shape we
    // assert when ok:true:
    if (result.ok) {
      expect(result.project.restoredOverrides).toEqual({ CIRCLE: 50, SQUARE: 75 });
    }
  });

  it('load threads samplingHz into sampleSkeleton', async () => {
    // Asserts the materialized output exposes the resolved samplingHz (default 120 when null).
    const fs = await import('node:fs/promises');
    const json = JSON.stringify({
      version: 1,
      skeletonPath: '/a/b/SIMPLE.json',
      atlasPath: null, imagesDir: null,
      overrides: {}, samplingHz: 60, lastOutDir: null,
      sortColumn: null, sortDir: null, documentation: {},
    });
    vi.mocked(fs.readFile).mockResolvedValue(json as unknown as string);
    const result = await handleProjectOpenFromPath('/a/b/proj.stmproj');
    if (result.ok) expect(result.project.samplingHz).toBe(60);
  });

  it('load drops stale override keys (D-150)', async () => {
    const fs = await import('node:fs/promises');
    const json = JSON.stringify({
      version: 1,
      skeletonPath: '/a/b/SIMPLE.json',
      atlasPath: null, imagesDir: null,
      overrides: { CIRCLE: 50, GHOST: 75 }, // GHOST not in re-sampled rig
      samplingHz: null, lastOutDir: null,
      sortColumn: null, sortDir: null, documentation: {},
    });
    vi.mocked(fs.readFile).mockResolvedValue(json as unknown as string);
    const result = await handleProjectOpenFromPath('/a/b/proj.stmproj');
    if (result.ok) {
      expect(result.project.staleOverrideKeys).toContain('GHOST');
      expect(result.project.restoredOverrides.GHOST).toBeUndefined();
    }
  });

  it('missing skeleton returns typed error (D-149, T-08-MISS)', async () => {
    const fs = await import('node:fs/promises');
    const json = JSON.stringify({
      version: 1,
      skeletonPath: '/nonexistent/path.json',
      atlasPath: null, imagesDir: null,
      overrides: {}, samplingHz: null, lastOutDir: null,
      sortColumn: null, sortDir: null, documentation: {},
    });
    vi.mocked(fs.readFile).mockResolvedValue(json as unknown as string);
    const result = await handleProjectOpenFromPath('/a/b/proj.stmproj');
    if (!result.ok) expect(result.error.kind).toBe('SkeletonNotFoundOnLoadError');
  });

  it('newer version returns typed error (D-151, T-08-VER)', async () => {
    const fs = await import('node:fs/promises');
    vi.mocked(fs.readFile).mockResolvedValue(JSON.stringify({ version: 2, skeletonPath: 'x.json', overrides: {}, documentation: {} }) as unknown as string);
    const result = await handleProjectOpenFromPath('/a/b/proj.stmproj');
    if (!result.ok) expect(result.error.kind).toBe('ProjectFileVersionTooNewError');
  });

  it('malformed JSON returns ProjectFileParseError (Pitfall 9)', async () => {
    const fs = await import('node:fs/promises');
    vi.mocked(fs.readFile).mockResolvedValue('{ broken json' as unknown as string);
    const result = await handleProjectOpenFromPath('/a/b/proj.stmproj');
    if (!result.ok) expect(result.error.kind).toBe('ProjectFileParseError');
  });

  it('atlas auto-discovery on null path (D-152)', async () => {
    // When atlasPath is null in the file, loader's existing F1.2 sibling
    // detection runs. Plan 03 simply forwards atlasPath:null to loadSkeleton's
    // optional opts.atlasPath — loader handles the rest.
    const fs = await import('node:fs/promises');
    const json = JSON.stringify({
      version: 1,
      skeletonPath: '/a/b/SIMPLE.json',
      atlasPath: null, imagesDir: null,
      overrides: {}, samplingHz: null, lastOutDir: null,
      sortColumn: null, sortDir: null, documentation: {},
    });
    vi.mocked(fs.readFile).mockResolvedValue(json as unknown as string);
    // The Plan 03 implementation must NOT pass atlasPath to loadSkeleton when null —
    // it relies on the loader's sibling auto-discovery. This test asserts the
    // response is well-formed (ok or SkeletonNotFoundOnLoadError, not Unknown).
    const result = await handleProjectOpenFromPath('/a/b/proj.stmproj');
    if (!result.ok) expect(['SkeletonNotFoundOnLoadError', 'AtlasNotFoundError']).toContain(result.error.kind);
  });
});
```

**Test names listed in 08-VALIDATION.md MUST appear verbatim** in `it(...)` strings: `save writes file with all D-145 fields`, `atomic-write tmp then rename`, `load restores overrides verbatim`, `load threads samplingHz into sampleSkeleton`, `load drops stale override keys`, `missing skeleton returns typed error`, `atlas auto-discovery on null path`. These are the `-t "..."` selectors used by VALIDATION.md.

**RED gate:** `npx vitest run tests/main/project-io.spec.ts` exits non-zero (module not found) until Plan 03 lands.
  </action>
  <verify>
    <automated>npx vitest run tests/main/project-io.spec.ts 2>&1 | grep -E "(Error|Cannot find|failed|FAIL)" | head -3</automated>
  </verify>
  <acceptance_criteria>
- File `tests/main/project-io.spec.ts` exists
- `grep -c "save writes file with all D-145 fields" tests/main/project-io.spec.ts` returns `1`
- `grep -c "atomic-write tmp then rename" tests/main/project-io.spec.ts` returns `1`
- `grep -c "load restores overrides verbatim" tests/main/project-io.spec.ts` returns `1`
- `grep -c "load threads samplingHz into sampleSkeleton" tests/main/project-io.spec.ts` returns `1`
- `grep -c "load drops stale override keys" tests/main/project-io.spec.ts` returns `1`
- `grep -c "missing skeleton returns typed error" tests/main/project-io.spec.ts` returns `1`
- `grep -c "newer version returns typed error" tests/main/project-io.spec.ts` returns `1`
- `grep -c "malformed JSON returns ProjectFileParseError" tests/main/project-io.spec.ts` returns `1`
- `grep -c "atlas auto-discovery on null path" tests/main/project-io.spec.ts` returns `1`
- `grep -c "vi.mock('electron'" tests/main/project-io.spec.ts` returns `1`
- `grep -c "vi.mock('node:fs/promises'" tests/main/project-io.spec.ts` returns `1`
- `grep -c "from '../../src/main/project-io" tests/main/project-io.spec.ts` returns `1` (RED-driving import)
- `npx vitest run tests/main/project-io.spec.ts` exits non-zero (RED — module-under-test missing)
  </acceptance_criteria>
  <done>RED spec with 4 save cases + 7 open cases (atomic-write trace, F9.2 verbatim restore, samplingHz threading, D-150 stale-override drop, D-149 missing-skeleton, D-151 newer-version, Pitfall 9 parse error, D-152 atlas auto-discovery) committed; vitest fails predictably.</done>
</task>

<task type="auto">
  <name>Task 4: RED test stubs for renderer Save/Load UI + extend arch.spec for Phase 8</name>
  <files>tests/renderer/save-load.spec.tsx, tests/arch.spec.ts</files>
  <read_first>
- tests/renderer/atlas-preview-modal.spec.tsx (full file — jsdom prelude lines 1-22, vi.stubGlobal recipe, makeSummary helper at lines 30-82)
- tests/arch.spec.ts (full file — line 116-134 existing core grep block)
- .planning/phases/08-save-load-project-state/08-PATTERNS.md (§"tests/renderer/save-load.spec.tsx (NEW)" lines 478-518; §"tests/arch.spec.ts (MODIFIED)" lines 1051-1069)
- .planning/phases/08-save-load-project-state/08-VALIDATION.md (per-task map rows 8-XX-14 through 8-XX-21)
  </read_first>
  <action>
**Two files in this task: a new RED renderer spec and an additive arch.spec block.**

---

**Part A — Create `tests/renderer/save-load.spec.tsx`:**

```tsx
// @vitest-environment jsdom
/**
 * Phase 8 Plan 04 — renderer specs for AppShell Save/Open buttons + dirty
 * marker + SaveQuitDialog + Cmd/Ctrl+S/O shortcuts + stale-override banner +
 * DropZone .stmproj branch.
 *
 * RED until Plan 04 wires AppShell + DropZone + SaveQuitDialog. Imports below
 * intentionally reference modules/exports that do not yet exist, OR existing
 * modules whose new behavior has not been wired yet.
 */
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
import { cleanup, render, screen, fireEvent } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { AppShell } from '../../src/renderer/src/components/AppShell';
import type { SkeletonSummary, OpenResponse, SaveResponse } from '../../src/shared/types';

afterEach(cleanup);

function makeSummary(): SkeletonSummary {
  // Synthetic minimal SkeletonSummary that satisfies AppShell's mount.
  // Plan 04 may need to enrich this — adapt as required during GREEN.
  return {
    skeletonPath: '/a/b/SIMPLE.json',
    atlasPath: '/a/b/SIMPLE.atlas',
    bones: [], slots: [], skins: [], animations: [],
    peaks: [
      { attachmentName: 'CIRCLE', skinName: 'default', slotName: 'slot-circle', sourceW: 64, sourceH: 64,
        worldW: 32, worldH: 32, peakScale: 0.5, animationName: 'idle', frame: 12, sourcePath: '/a/b/images/CIRCLE.png' } as any,
      { attachmentName: 'SQUARE', skinName: 'default', slotName: 'slot-square', sourceW: 64, sourceH: 64,
        worldW: 64, worldH: 64, peakScale: 1.0, animationName: 'idle', frame: 0, sourcePath: '/a/b/images/SQUARE.png' } as any,
    ],
    elapsedMs: 5,
  } as unknown as SkeletonSummary;
}

beforeEach(() => {
  vi.stubGlobal('api', {
    saveProject: vi.fn().mockResolvedValue({ ok: true, path: '/a/b/proj.stmproj' } as SaveResponse),
    saveProjectAs: vi.fn().mockResolvedValue({ ok: true, path: '/a/b/proj.stmproj' } as SaveResponse),
    openProject: vi.fn().mockResolvedValue({ ok: true, project: { summary: makeSummary(), restoredOverrides: {}, staleOverrideKeys: [], samplingHz: 120, lastOutDir: null, sortColumn: null, sortDir: null, projectFilePath: '/a/b/proj.stmproj' } } as OpenResponse),
    openProjectFromFile: vi.fn(),
    openProjectFromPath: vi.fn(),
    locateSkeleton: vi.fn(),
    onCheckDirtyBeforeQuit: vi.fn(() => () => undefined),
    confirmQuitProceed: vi.fn(),
    pickOutputDirectory: vi.fn(),
    startExport: vi.fn(),
    cancelExport: vi.fn(),
    onExportProgress: vi.fn(() => () => undefined),
    openOutputFolder: vi.fn(),
    probeExportConflicts: vi.fn(),
  });
});

describe('AppShell Save/Open buttons (D-140, D-141)', () => {
  it('Save reuses currentProjectPath', async () => {
    const summary = makeSummary();
    render(<AppShell summary={summary} elapsedMs={5} samplingHz={120} />);
    // Plan 04 wires: clicking Save with currentProjectPath !== null calls
    // window.api.saveProject (NOT saveProjectAs).
    const saveBtn = screen.getByRole('button', { name: /^Save$/i });
    await userEvent.click(saveBtn);
    // First Save with no path opens save-as; subsequent calls use save.
    // The contract Plan 04 must satisfy:
    expect((globalThis as any).api.saveProjectAs.mock.calls.length + (globalThis as any).api.saveProject.mock.calls.length).toBeGreaterThan(0);
  });

  it('dirty marker bullet renders when state mutates (D-144)', () => {
    const summary = makeSummary();
    render(<AppShell summary={summary} elapsedMs={5} samplingHz={120} />);
    // Untitled clean → 'Untitled' literal in chip.
    // Plan 04: a dirty mutation (override added) prepends '• '.
    // We assert the clean baseline here; the dirty path is exercised after a
    // user action that Plan 04's onApplyOverride flow fires.
    expect(screen.getByText(/Untitled|SIMPLE\.json/i)).toBeTruthy();
  });
});

describe('Keyboard shortcuts (D-140 + Pitfall 6)', () => {
  it('Cmd+S triggers Save', async () => {
    const summary = makeSummary();
    render(<AppShell summary={summary} elapsedMs={5} samplingHz={120} />);
    fireEvent.keyDown(window, { key: 's', metaKey: true });
    // Plan 04: window-level listener fires onClickSave when no modal is open.
    await Promise.resolve();
    const calls = (globalThis as any).api.saveProject.mock.calls.length + (globalThis as any).api.saveProjectAs.mock.calls.length;
    expect(calls).toBeGreaterThan(0);
  });

  it('Cmd+S suppressed when modal open (T-08-SHORT)', async () => {
    const summary = makeSummary();
    render(<AppShell summary={summary} elapsedMs={5} samplingHz={120} />);
    // Mount a fake modal element with role="dialog" (the suppression heuristic).
    const fakeModal = document.createElement('div');
    fakeModal.setAttribute('role', 'dialog');
    document.body.appendChild(fakeModal);
    fireEvent.keyDown(window, { key: 's', metaKey: true });
    await Promise.resolve();
    expect((globalThis as any).api.saveProject).not.toHaveBeenCalled();
    expect((globalThis as any).api.saveProjectAs).not.toHaveBeenCalled();
    document.body.removeChild(fakeModal);
  });
});

describe('SaveQuitDialog three-button flow (D-143)', () => {
  it('dirty + drop opens guard', async () => {
    // Plan 04 wires DropZone branch to call AppShell's pre-drop dirty check.
    // When isDirty is true, the SaveQuitDialog mounts before the new file load proceeds.
    // We assert the dialog mount path by checking that role=dialog appears once
    // we simulate the dirty + drop sequence — Plan 04's contract.
    expect(true).toBe(true); // shell — Plan 04 fills behavior
  });
});

describe('Stale-override banner (D-150)', () => {
  it('stale override banner renders count + names', async () => {
    // Plan 04 wires the banner to setStaleOverridesNotice([...names]) when Open
    // resolves with non-empty staleOverrideKeys. We assert it via Open with stale keys.
    const summary = makeSummary();
    (globalThis as any).api.openProject = vi.fn().mockResolvedValue({
      ok: true,
      project: {
        summary, restoredOverrides: { CIRCLE: 50 }, staleOverrideKeys: ['GHOST', 'OLD_HAT'],
        samplingHz: 120, lastOutDir: null, sortColumn: null, sortDir: null,
        projectFilePath: '/a/b/proj.stmproj',
      },
    } as OpenResponse);
    render(<AppShell summary={summary} elapsedMs={5} samplingHz={120} />);
    // Trigger the open path the way Plan 04 wires it (Cmd+O or Open button click):
    fireEvent.keyDown(window, { key: 'o', metaKey: true });
    await Promise.resolve(); await Promise.resolve();
    // After Plan 04 wires the banner, the text below must appear:
    // expect(screen.getByText(/saved overrides skipped/i)).toBeTruthy();
    expect(true).toBe(true); // shell
  });
});

describe('DropZone branch on .stmproj (D-142)', () => {
  it('dropzone branch on stmproj', async () => {
    // Plan 04 extends DropZone to dispatch on extension. .stmproj routes to
    // window.api.openProjectFromFile(file); .json routes to existing
    // window.api.loadSkeletonFromFile(file). This shell asserts the contract.
    expect(true).toBe(true); // shell — Plan 04 fills behavior
  });
});
```

**Test names listed in 08-VALIDATION.md MUST appear verbatim**: `Save reuses currentProjectPath`, `Cmd+S triggers Save`, `Cmd+S suppressed when modal open`, `dirty + drop opens guard`, `dirty marker bullet`, `stale override banner`, `dropzone branch on stmproj`. Several test bodies are intentionally trivial in this RED stub — Plan 04 fills the assertion bodies during GREEN. The `it(...)` titles MUST exist now so VALIDATION.md's `-t "..."` selectors resolve to a test that exists (even if it's `expect(true).toBe(true)` placeholder).

**Note on `ts-expect-error` style:** the synthetic SkeletonSummary uses `as any` / `as unknown as SkeletonSummary` — Plan 04 may need to fully type the helper during GREEN. Acceptable.

---

**Part B — Extend `tests/arch.spec.ts` with Phase 8 electron-import block:**

Read existing file first; the existing core grep at lines 116-134 auto-scans `src/core/**/*.ts` for `sharp` / `node:fs` / `fs`. It does NOT block `electron`. Plan 8's `src/core/project-file.ts` MUST also be free of `electron` imports. Append a new `describe` block AFTER the existing core grep:

```typescript
// Append at end of file, AFTER the existing 'Architecture boundary: src/core
// must not import sharp / node:fs / node:fs/promises' describe block.

describe('Phase 8 Layer 3: src/core/project-file.ts must not import electron (T-08-LAYER)', () => {
  it('src/core/project-file.ts does not import from electron', () => {
    // Specifically guards the new project-file module — the existing core grep
    // (lines ~116-134) covers fs/sharp; this block adds electron coverage and
    // names the file explicitly so future Phase-8 hygiene drift is caught even
    // if the existing globSync rule changes.
    const filePath = 'src/core/project-file.ts';
    let text = '';
    try {
      text = readFileSync(filePath, 'utf8');
    } catch {
      // File doesn't exist yet (Plan 02 lands it). When it lands the grep applies.
      return;
    }
    expect(text, `${filePath} must not import from electron`).not.toMatch(/from ['"]electron['"]/);
    expect(text, `${filePath} must not import from node:fs`).not.toMatch(/from ['"]node:fs(\/promises)?['"]/);
    expect(text, `${filePath} must not import from sharp`).not.toMatch(/from ['"]sharp['"]/);
  });
});
```

**Why an additive block instead of editing the existing regex on line 128:** the existing grep applies to ALL `src/core/**/*.ts` and a regex change could affect Phase 6's `loader.ts` exemption logic. A standalone block targeting `src/core/project-file.ts` by name is safer and self-documents the Phase 8 invariant. This matches the "name-target the new file" precedent in the existing arch tests (loader.ts is name-exempted at line 117 comment).

**Important:** the new block MUST use a try/catch graceful-skip while Plan 02 hasn't shipped — the test suite must remain green in Wave 1. Once Plan 02 lands, the file exists and the assertions activate automatically.
  </action>
  <verify>
    <automated>npm run test -- tests/arch.spec.ts && npx vitest run tests/renderer/save-load.spec.tsx 2>&1 | grep -E "(failed|FAIL|Error)" | head -5</automated>
  </verify>
  <acceptance_criteria>
- File `tests/renderer/save-load.spec.tsx` exists and starts with `// @vitest-environment jsdom`
- `grep -c "Save reuses currentProjectPath" tests/renderer/save-load.spec.tsx` returns `1`
- `grep -c "Cmd+S triggers Save" tests/renderer/save-load.spec.tsx` returns `1`
- `grep -c "Cmd+S suppressed when modal open" tests/renderer/save-load.spec.tsx` returns `1`
- `grep -c "dirty + drop opens guard" tests/renderer/save-load.spec.tsx` returns `1`
- `grep -c "dirty marker bullet" tests/renderer/save-load.spec.tsx` returns `1`
- `grep -c "stale override banner" tests/renderer/save-load.spec.tsx` returns `1`
- `grep -c "dropzone branch on stmproj" tests/renderer/save-load.spec.tsx` returns `1`
- `grep -c "vi.stubGlobal('api'" tests/renderer/save-load.spec.tsx` returns `1`
- `grep -c "T-08-LAYER" tests/arch.spec.ts` returns at least `1`
- `grep -c "src/core/project-file.ts" tests/arch.spec.ts` returns at least `1`
- `npm run test -- tests/arch.spec.ts` exits 0 (existing 8 + new block green; new block graceful-skips when project-file.ts absent)
- `npx vitest run tests/renderer/save-load.spec.tsx` exits non-zero IF any test references unimplemented behavior; trivial `expect(true)` shells exist where the behavior lands in Plan 04 — those PASS today, the keyboard + Save tests likely FAIL (RED) because Plan 04 hasn't wired the listener
- Existing arch.spec coverage preserved: `grep -c "describe(" tests/arch.spec.ts` returns at least `7` (Phase 0+1+6+7 baseline + new Phase 8 block)
  </acceptance_criteria>
  <done>RED renderer spec with 7 named test blocks + arch.spec extended with Phase 8 electron-import hygiene block (graceful-skip when target file absent). Existing 240+ test suite stays green; new spec fails predictably for the behavior-tied tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer ↛ core (Layer 3) | Renderer imports `../lib/*-view.ts` mirrors only — never `src/core/*` directly |
| core ↛ I/O (Layer 3) | `src/core/*` cannot import `node:fs`, `sharp`, `electron`, or DOM symbols |
| Phase 8 contracts | Type extensions in `src/shared/types.ts` cross all three processes — must remain structuredClone-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-LAYER | Tampering | `src/core/project-file.ts` (lands Plan 02) | mitigate | Wave 1 adds explicit electron-import hygiene block in `tests/arch.spec.ts` (Task 4 Part B) — graceful-skips when file absent, activates when Plan 02 lands. Existing globSync grep covers fs/sharp; new block adds electron + names file explicitly. |
| T-08-IPC | Tampering | `src/shared/types.ts` Api surface | mitigate | All 7 new Api methods follow D-10 typed-error envelope (`SaveResponse` / `OpenResponse` / `LocateSkeletonResponse` mirror `LoadResponse`). RED spec stubs reference these envelopes in `result.ok` discriminator — implementations cannot drift. |
| T-08-CONTRACT | Information Disclosure | `ProjectFileV1` schema | mitigate | structuredClone-safe by construction (file-top docblock at `src/shared/types.ts:1-17` lock). `overrides: Record<string, number>` not Map — RED test for round-trip would catch JSON.stringify(Map) → "{}" silent failure (Pitfall 3). |
| T-08-RED | Tampering | RED test stubs | accept | Spec files import non-existent modules (`src/core/project-file.ts`, `src/main/project-io.ts`); vitest treats these as test failures, not crashes. Standard TDD RED state — Wave 2/3/4 turn them GREEN. |
</threat_model>

<verification>
After Task 4:
1. `npm run test` full suite exits — Phase 7 baseline 240+1+1; Phase 8 Wave 1 may add some passing arch tests + RED test failures. Target: NEW failures in `tests/core/project-file.spec.ts` + `tests/main/project-io.spec.ts` + a few in `tests/renderer/save-load.spec.tsx`. Existing green count must NOT decrease.
2. `npx tsc --noEmit -p tsconfig.web.json` AND `npx tsc --noEmit -p tsconfig.node.json` both exit 0.
3. `npx electron-vite build` exits 0 (types.ts changes do not affect runtime).
4. `git diff scripts/cli.ts` empty — Phase 5 D-102 lock preserved.
5. `git diff src/core/sampler.ts` empty — CLAUDE.md rule #3 lock preserved.
</verification>

<success_criteria>
- 5 files touched: 1 modified (`src/shared/types.ts`), 3 new spec stubs, 1 modified arch spec
- 7 new types exported from `src/shared/types.ts`; 4 new SerializableError kinds; 8 new Api members
- 3 RED specs each fail predictably with module-not-found (project-file / project-io) or behavior-not-yet-wired (save-load.spec.tsx)
- arch.spec extended with explicit electron-import block targeting `src/core/project-file.ts` by name (graceful-skip until file exists)
- Both typecheck projects exit 0
- Existing test suite green count not regressed
- Phase 7 locked files (`scripts/cli.ts`, `src/core/sampler.ts`) untouched
</success_criteria>

<output>
After completion, create `.planning/phases/08-save-load-project-state/08-01-SUMMARY.md` recording:
- Files modified / created
- Test name strings landed (must match VALIDATION.md `-t` selectors verbatim — list them)
- Any deviations (Rule 1/2/3/4 per critical_project_rules)
- Pre-existing typecheck warnings noted but not regressed
- Confirmation that locked files (`scripts/cli.ts`, `src/core/sampler.ts`) are byte-identical
- Test counts: existing green delta + new RED count
</output>
