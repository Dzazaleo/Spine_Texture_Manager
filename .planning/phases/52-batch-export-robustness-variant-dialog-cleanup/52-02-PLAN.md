---
phase: 52-batch-export-robustness-variant-dialog-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/ipc.ts
autonomous: true
requirements: [EXPORT-06]
must_haves:
  truths:
    - "D-04: both variant handlers (variant:export single ~:1094, variant:exportBatch ~:1132) pass Number(safetyBufferPercent) — the redundant `|| 0` is dropped at BOTH"
    - "D-04: a documenting comment at BOTH handlers records the deliberate coerce-and-clamp divergence (single canonical clamp in exportOneVariant step 2b) vs export:start's validate-and-reject"
    - "D-04: single + batch coerce IDENTICALLY (byte-parallel) — the variant channels are NOT converted to validate-and-reject"
    - "D-09: the happy path is behavior-preserving — a valid finite safetyBufferPercent produces byte-identical output (the body's Number.isFinite clamp coerces NaN→0 exactly as `|| 0` did for the NaN case)"
  artifacts:
    - path: "src/main/ipc.ts"
      provides: "Variant-channel safetyBufferPercent coercion unified to Number(...) + documenting comment at both handlers"
      contains: "coerce-and-clamp"
  key_links:
    - from: "variant:export + variant:exportBatch handlers"
      to: "exportOneVariant step 2b safeBuffer clamp (variant-export.ts:135-137)"
      via: "pass Number(safetyBufferPercent); the body's Number.isFinite guard is the single authority"
      pattern: "Number\\(safetyBufferPercent\\)"
---

<objective>
Unify the variant-channel `safetyBufferPercent` coercion at the IPC boundary (D-04, WR-04, SC#3). Drop the redundant `Number(safetyBufferPercent) || 0` at BOTH variant handlers in `src/main/ipc.ts` → pass `Number(safetyBufferPercent)`, relying on the single canonical clamp in `exportOneVariant` step 2b (`variant-export.ts:135-137`, the `Number.isFinite` guard that coerces NaN→0). Add a documenting comment at both handlers recording the deliberate coerce-and-clamp policy (intentionally distinct from `export:start`'s validate-and-reject).

Purpose: remove dead redundant coercion and document why the variant channels coerce-and-clamp instead of validate-and-reject — without changing happy-path behavior.
Output: edited `src/main/ipc.ts` (no new files). Do NOT convert the variant channels to validate-and-reject (D-04 / D-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source of truth for the locked decision + exact code excerpts (READ before editing):
@.planning/phases/52-batch-export-robustness-variant-dialog-cleanup/52-CONTEXT.md
@.planning/phases/52-batch-export-robustness-variant-dialog-cleanup/52-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs — extracted from src/main. Use directly. -->

The canonical clamp the variant channels rely on (variant-export.ts:135-137) — the single authority:
```typescript
const safeBuffer = Number.isFinite(safetyBufferPercent)
  ? Math.max(0, Math.min(25, Math.trunc(safetyBufferPercent)))
  : 0;
```

The validate-and-reject precedent the variant channels deliberately do NOT adopt (ipc.ts:361-396):
```typescript
function validateExportOpts(outputMode: unknown, atlasOpts: unknown): string | null {
  // … returns a string error message; handleStartExport short-circuits on non-null
}
```

The two CURRENT identical boundary-coercion lines to change — ipc.ts:1094 (single variant:export) and ipc.ts:1132 (batch variant:exportBatch):
```typescript
        Number(safetyBufferPercent) || 0,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-04 — drop redundant `|| 0` + document coerce-and-clamp at BOTH variant handlers</name>
  <read_first>
    - .planning/phases/52-batch-export-robustness-variant-dialog-cleanup/52-CONTEXT.md (D-04 decision text — the VERBATIM documenting comment + the do-NOT-validate-and-reject directive)
    - .planning/phases/52-batch-export-robustness-variant-dialog-cleanup/52-PATTERNS.md (§"D-04 — WR-04" — both sites :1094 / :1132, the byte-parallel requirement, the canonical clamp it relies on)
    - src/main/ipc.ts (read :1058-1134 — both variant handlers and their coercion ladders; and :361-396 — validateExportOpts, the precedent NOT to adopt)
  </read_first>
  <files>src/main/ipc.ts</files>
  <action>
    Change BOTH variant-handler `safetyBufferPercent` coercions identically.

    SITE 1 — `variant:export` (single), currently `ipc.ts:1094`, the LAST argument passed
    to `handleExportVariant(...)`. CURRENT:
    ```typescript
            Number(safetyBufferPercent) || 0,
    ```
    REPLACE WITH (drop `|| 0`, add the documenting comment ABOVE the line):
    ```typescript
            // D-04 (WR-04): variant channels coerce-and-clamp (single canonical clamp in
            // exportOneVariant step 2b); intentionally distinct from export:start's
            // validate-and-reject (validateExportOpts) because the renderer pre-clamps and
            // the body is the single authority. The body's Number.isFinite guard coerces
            // NaN→0, so the boundary `|| 0` was redundant.
            Number(safetyBufferPercent),
    ```

    SITE 2 — `variant:exportBatch` (batch), currently `ipc.ts:1132`, the LAST argument
    passed to `handleExportVariantBatch(...)`. CURRENT:
    ```typescript
            Number(safetyBufferPercent) || 0,
    ```
    REPLACE WITH THE IDENTICAL form (same comment + same `Number(safetyBufferPercent),`)
    — keep single + batch byte-parallel (the rest of the coercion ladder at :1081-1093 /
    :1119-1131 is already mirrored, so the two blocks must stay mirrored).

    The comment text is verbatim from CONTEXT D-04 / PATTERNS.md (the "coerce-and-clamp
    … single authority" wording is mandatory; you may keep the extra "redundant `|| 0`"
    sentence). Do NOT touch any other line in either handler. Do NOT add a
    `validateVariantOpts`-style reject path — that is explicitly forbidden (D-04 / D-09).
  </action>
  <acceptance_criteria>
    - `grep -c "Number(safetyBufferPercent) || 0" src/main/ipc.ts` returns 0 (both `|| 0` forms removed).
    - `grep -c "Number(safetyBufferPercent)," src/main/ipc.ts` returns >= 2 (both handlers now pass the bare coercion — note the trailing comma matches the argument position).
    - `grep -c "coerce-and-clamp" src/main/ipc.ts` returns >= 2 (the documenting comment at BOTH handlers).
    - `grep -c "validate-and-reject" src/main/ipc.ts` returns >= 2 (comment references the deliberate divergence).
    - No new `validateVariant`/reject function was added (the variant channels still call `handleExportVariant`/`handleExportVariantBatch` directly).
    - `npx vitest run tests/main/variant-batch-faithful.spec.ts` exits 0.
    - `npm run typecheck:node` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:node && npx vitest run tests/main/variant-batch-faithful.spec.ts</automated>
  </verify>
  <done>Both variant handlers pass `Number(safetyBufferPercent)` (no `|| 0`); the verbatim coerce-and-clamp comment is present at both sites; no validate-and-reject path was added; typecheck:node + the batch faithfulness suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC `variant:export` / `variant:exportBatch`) | `safetyBufferPercent` arrives from the renderer (the documented trust boundary, ipc.ts:30-32) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03 | Tampering (unbounded input) | `safetyBufferPercent` at the variant IPC boundary | mitigate | Dropping the boundary `|| 0` does NOT weaken the trust boundary: the single canonical clamp in `exportOneVariant` step 2b (`Number.isFinite(x) ? Math.max(0, Math.min(25, Math.trunc(x))) : 0`, variant-export.ts:135-137) coerces NaN/Infinity→0 and clamps to [0,25] BEFORE the value reaches `buildExportPlan`. A compromised renderer sending `safetyBufferPercent: 100000` is still clamped to 25; sending `NaN` is still coerced to 0. The `Number(...)` cast preserves the wire-type coercion. The body is the single authority by design (D-04). |
| T-52-04 | (none — documentation only) | the documenting comment | accept | Comment-only; no runtime surface. |

**No HIGH-severity threat.** The clamp authority is unchanged — only the redundant boundary coercion is removed and the policy documented. ASVS L1 input-handling is satisfied by the retained step-2b clamp.
</threat_model>

<verification>
- `grep -c "Number(safetyBufferPercent) || 0" src/main/ipc.ts` == 0.
- `grep -c "coerce-and-clamp" src/main/ipc.ts` >= 2.
- `npm run typecheck:node` exits 0.
- `npx vitest run tests/main/variant-batch-faithful.spec.ts` exits 0.
</verification>

<success_criteria>
- D-04: both variant handlers pass `Number(safetyBufferPercent)` (redundant `|| 0` dropped at both); documenting comment present at both; single + batch byte-parallel; no validate-and-reject conversion.
- D-09: happy-path output byte-identical (valid finite buffer behaves the same; clamp authority unchanged).
</success_criteria>

<output>
After completion, create `.planning/phases/52-batch-export-robustness-variant-dialog-cleanup/52-02-SUMMARY.md`
</output>
