---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - .github/release-template.md
  - package.json
autonomous: true
requirements: [CI-01, CI-02, CI-03, CI-04, CI-05, CI-06, REL-01, REL-02]
tags: [ci, github-actions, release, electron-builder, infrastructure]

must_haves:
  truths:
    - "On tag push matching 'v*.*.*', GitHub Actions starts a workflow run within 30 seconds"
    - "The workflow runs vitest BEFORE any installer build job is scheduled"
    - "Three platform builds (mac/win/linux) start concurrently after the test job succeeds"
    - "If any build job fails, the publish job is skipped and no draft release is created"
    - "On successful tag-push run, a draft GitHub Release is created with three installer assets attached"
    - "The release body has the four documented sections rendered with no remaining ${VERSION}/${TAG}/${INSTALL_DOC_LINK} placeholders"
    - "On workflow_dispatch from main, the same test+build jobs run but the publish job is skipped (no release created)"
    - "build:mac/win/linux scripts in package.json carry --publish never as defense against electron-builder GH_TOKEN auto-publish"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered + workflow_dispatch CI release pipeline"
      contains: "name: release"
      min_lines: 130
    - path: ".github/release-template.md"
      provides: "envsubst-rendered release-notes template (REL-02)"
      contains: "## Summary"
      min_lines: 25
    - path: "package.json"
      provides: "build:mac/win/linux scripts with --publish never bake-in (Pitfall 2 defense)"
      contains: "--publish never"
  key_links:
    - from: ".github/workflows/release.yml#publish.needs"
      to: "[build-mac, build-win, build-linux]"
      via: "GHA needs: graph"
      pattern: "needs:\\s*\\[build-mac,\\s*build-win,\\s*build-linux\\]"
    - from: ".github/workflows/release.yml#publish"
      to: ".github/release-template.md"
      via: "envsubst step"
      pattern: "envsubst < \\.github/release-template\\.md"
    - from: ".github/workflows/release.yml#publish"
      to: "softprops/action-gh-release@v2.6.2 (SHA-pinned)"
      via: "draft + multi-asset upload"
      pattern: "softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65"
    - from: ".github/workflows/release.yml#test"
      to: "package.json#scripts.test"
      via: "npm run test"
      pattern: "npm run test"
    - from: ".github/workflows/release.yml#build-{mac,win,linux}"
      to: "package.json#scripts.build:{mac,win,linux}"
      via: "npm run build:<os> -- --publish never"
      pattern: "npm run build:(mac|win|linux) -- --publish never"
---

<objective>
Author the entire Phase 11 CI greenfield surface in a single wave: the GitHub Actions workflow YAML, the release-notes template, and the defensive `--publish never` patch to `package.json` build scripts.

Purpose: Establishes the user-facing distribution channel by tag-triggering parallel cross-platform installer builds and atomic draft-release publication. Closes the locked architecture from 11-CONTEXT.md (D-01..D-23) and the standard stack + patterns + 8 pitfalls from 11-RESEARCH.md.

Output:
- `.github/workflows/release.yml` — verbatim from 11-RESEARCH.md §"Code Examples" lines 753–906.
- `.github/release-template.md` — verbatim from 11-RESEARCH.md §"Code Examples" lines 908–938.
- `package.json` — append `--publish never` to `build:mac` / `build:win` / `build:linux` (RESEARCH §"Defensive package.json script update" lines 940–950).

Three atomic commits, one per file. No live workflow run in this plan — that's Plan 02 (Wave 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-CONTEXT.md
@.planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-RESEARCH.md
@.planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-PATTERNS.md
@.planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-VALIDATION.md
@electron-builder.yml
@package.json
@CLAUDE.md

<interfaces>
<!-- Existing build scripts the workflow shells out to. -->
<!-- Source: package.json lines 17–19 (current state, BEFORE this plan's edit). -->

```json
"build:mac": "electron-vite build && electron-builder --mac dmg",
"build:win": "electron-vite build && electron-builder --win nsis",
"build:linux": "electron-vite build && electron-builder --linux AppImage",
"test": "vitest run",
"typecheck": "npm run typecheck:node && npm run typecheck:web"
```

<!-- electron-builder.yml outputs (Phase 10 contract) — what the workflow uploads. -->
<!-- Source: electron-builder.yml. -->

```yaml
# directories.output: release/
# artifactName: ${productName}-${version}-${arch}.${ext}
# Expected files post-build:
#   release/Spine Texture Manager-1.1.0-rc1-arm64.dmg     (mac)
#   release/Spine Texture Manager-1.1.0-rc1-x64.exe       (win)
#   release/Spine Texture Manager-1.1.0-rc1-x86_64.AppImage (linux)
```

<!-- SHA-pinned action versions (RESEARCH §Standard Stack — these EXACT SHAs are the contract). -->

```yaml
actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5         # v4.3.1
actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020       # v4.4.0
actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4.6.2
actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
```

<!-- Runner image pins (D-06). -->

```yaml
test:        ubuntu-latest    # vitest only, no compiled deps
build-mac:   macos-14         # arm64 native — REQUIRED for sharp's @img/sharp-darwin-arm64
build-win:   windows-2022
build-linux: ubuntu-22.04     # libfuse2 (NOT libfuse2t64); avoids GLIBC mismatch
publish:     ubuntu-latest    # only touches GitHub Releases API
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Defensive --publish never patch on package.json build scripts</name>
  <files>package.json</files>

  <read_first>
    - package.json (current state of build:mac / build:win / build:linux scripts on lines 17–19)
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-RESEARCH.md §"Defensive `package.json` script update" (lines 940–952) — authoritative target shape
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-RESEARCH.md §"Pitfall 1" (lines 627–650) — WHY this matters: electron-builder unconditionally checks for `GH_TOKEN` and auto-creates a release if found; baking `--publish never` into the script makes the protection travel with the build invocation regardless of caller environment
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-CONTEXT.md §"Decisions" (D-01 → release.yml is the canonical workflow file; D-21 → no extra secrets; this patch is defense-in-depth for D-21 holding)
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-PATTERNS.md §`package.json (MODIFIED)` lines 212–243 — confirms self-analog: extend the existing `electron-vite build && electron-builder --<os> <target>` pattern with a trailing space-separated `--publish never` flag, no other changes
  </read_first>

  <action>
    Edit `package.json` to append ` --publish never` (single space, then literal `--publish never`) to each of the three `build:*` script values. The `build:dry` script is NOT modified (it uses `--dir`, not a publish target — dry-pack only).

    Target state (per RESEARCH §"Defensive `package.json` script update"):

    ```json
    "build:mac": "electron-vite build && electron-builder --mac dmg --publish never",
    "build:win": "electron-vite build && electron-builder --win nsis --publish never",
    "build:linux": "electron-vite build && electron-builder --linux AppImage --publish never",
    ```

    Other `package.json` keys remain UNCHANGED: `version`, `dependencies`, `devDependencies`, `engines`, `main`, `type`, `name`, `scripts.test`, `scripts.test:watch`, `scripts.typecheck*`, `scripts.cli`, `scripts.dev`, `scripts.build`, `scripts.build:dry`, `scripts.preview`. Use Edit (NOT Write — this is a 3-line surgical change).

    Single atomic commit after this edit (commit cadence per RESEARCH §"Patterns"):
    `chore(11-01): bake --publish never into build:mac/win/linux scripts (Pitfall 2 defense)`
  </action>

  <verify>
    <automated>grep -c "electron-builder --(mac|win|linux) [a-zA-Z]\+ --publish never" package.json | grep -q "^3$" && npm run typecheck && npm run test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -E '"build:mac":\s*"electron-vite build && electron-builder --mac dmg --publish never"' package.json` returns exactly one match.
    - `grep -E '"build:win":\s*"electron-vite build && electron-builder --win nsis --publish never"' package.json` returns exactly one match.
    - `grep -E '"build:linux":\s*"electron-vite build && electron-builder --linux AppImage --publish never"' package.json` returns exactly one match.
    - `grep -c '"build:dry":' package.json` returns `1` and the value is unchanged from pre-edit state (still ends with `--dir`, NOT `--publish never`).
    - `node -e "console.log(require('./package.json').version)"` still prints `1.1.0-rc1` (no version drift).
    - `npm run typecheck` exits 0 (existing TS source unchanged).
    - `npm run test` exits 0 (331 tests still pass; this edit touches no runtime code).
    - `git diff --stat HEAD~1 package.json` shows exactly 3 lines changed (the three `build:*` script values), no other lines.
  </acceptance_criteria>

  <done>
    package.json's three platform build scripts carry `--publish never`; existing test suite still green; commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author .github/release-template.md verbatim from RESEARCH</name>
  <files>.github/release-template.md</files>

  <read_first>
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-RESEARCH.md §"Code Examples" lines 908–938 — the EXACT target file content
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-RESEARCH.md §"Pattern 7" lines 524–578 — placeholder contract: ${VERSION}, ${TAG}, ${INSTALL_DOC_LINK}; envsubst (NOT sed/Mustache/Jinja); REL-02 four-section structure
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-CONTEXT.md §"Decisions" D-11, D-12, D-13 — section structure, envsubst substitution mechanism, inline first-launch redundancy until Phase 12 INSTALL.md lands
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-PATTERNS.md §`.github/release-template.md` lines 163–209 — confirms placeholder contract + Phase-12 forward note
    - Confirm `.github/` directory does not yet exist (`ls .github 2>/dev/null` returns empty / error); the file creation creates the directory
  </read_first>

  <action>
    Create `.github/release-template.md` with content **verbatim** from 11-RESEARCH.md §"Code Examples" lines 911–938. Do NOT paraphrase, do NOT reword the install instructions, do NOT alter section ordering — the live verification in Plan 02 greps for these literal strings.

    Required content structure (must match RESEARCH lines 911–938 exactly):

    ```markdown
    # Spine Texture Manager ${VERSION}

    ## Summary

    <!-- One-line summary of what this release contains. Edit before publishing. -->

    ## New in this version

    <!-- Bullet list of user-facing changes. Edit before publishing. -->

    ## Known issues

    <!-- Any issues testers should know about. Edit before publishing. -->

    ## Install instructions

    Choose the installer for your platform:

    - **macOS (Apple Silicon):** Download the `.dmg`. After mounting, drag to /Applications. **First launch:** macOS will block the app (it's ad-hoc-signed). Open System Settings → Privacy & Security → scroll to the bottom → click "Open Anyway" next to the Spine Texture Manager row.
    - **Windows (x64):** Download the `.exe`. Double-click. SmartScreen will show "Windows protected your PC" — click "More info" → "Run anyway". Then walk through the NSIS installer.
    - **Linux (x64):** Download the `.AppImage`. Make it executable: `chmod +x "Spine Texture Manager-${VERSION}-x86_64.AppImage"`, then run it. On Ubuntu 24.04+ you may need `sudo apt install libfuse2t64`.

    For full install instructions: ${INSTALL_DOC_LINK}

    ## Tag

    This release was built from tag `${TAG}`.
    ```

    Use the Write tool to create this file (greenfield — new directory + new file). Do NOT use Bash heredoc.

    Atomic commit:
    `feat(11-01): add .github/release-template.md (REL-02 envsubst-rendered release notes)`
  </action>

  <verify>
    <automated>test -f .github/release-template.md && grep -q "^# Spine Texture Manager \${VERSION}$" .github/release-template.md && grep -q "^## Summary$" .github/release-template.md && grep -q "^## New in this version$" .github/release-template.md && grep -q "^## Known issues$" .github/release-template.md && grep -q "^## Install instructions$" .github/release-template.md && grep -q "^## Tag$" .github/release-template.md && grep -q "\${INSTALL_DOC_LINK}" .github/release-template.md && grep -q "\${TAG}" .github/release-template.md</automated>
  </verify>

  <acceptance_criteria>
    - File `.github/release-template.md` exists.
    - File starts with `# Spine Texture Manager ${VERSION}` (literal first line).
    - File contains all four `##` section headings in order: `## Summary`, `## New in this version`, `## Known issues`, `## Install instructions` (`grep -E '^## (Summary|New in this version|Known issues|Install instructions|Tag)$' .github/release-template.md | wc -l` returns `5`).
    - File contains exactly three placeholder tokens: `${VERSION}` (appears at least twice — H1 and Linux chmod line), `${TAG}` (appears once in `## Tag`), `${INSTALL_DOC_LINK}` (appears once after install instructions).
    - The three platform install bullets are present (`grep -c '^- \*\*\(macOS\|Windows\|Linux\)' .github/release-template.md` returns `3`).
    - The macOS bullet contains the literal phrase `"Open Anyway"`; the Windows bullet contains `"Run anyway"`; the Linux bullet contains `chmod +x` and `libfuse2t64`. (These are the v1.1 inline-redundant first-launch instructions per D-13, replaced in Phase 12 by INSTALL.md link.)
    - File line count ≥ 25 (`wc -l < .github/release-template.md` ≥ 25).
    - `git status --short .github/` shows the file as added (`A` or `??`).
    - `npm run typecheck && npm run test` still pass (this file is markdown — no runtime impact).
  </acceptance_criteria>

  <done>
    .github/release-template.md exists with the four REL-02 sections, three envsubst placeholders, three platform install bullets, and committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author .github/workflows/release.yml verbatim from RESEARCH</name>
  <files>.github/workflows/release.yml</files>

  <read_first>
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-RESEARCH.md §"Code Examples" lines 753–906 — the COMPLETE target workflow YAML. Copy verbatim, preserving all SHAs, runner pins, comment lines, and step ordering.
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-RESEARCH.md §"Common Pitfalls" lines 625–751 — all 8 pitfalls and their mitigations are baked into the target YAML; understand WHY each line is there before transcribing
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-CONTEXT.md §"Decisions" D-01..D-23 — every locked decision must be present in the YAML; cross-reference D-IDs against YAML structure
    - .planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-PATTERNS.md §`.github/workflows/release.yml` lines 26–161 — confirms style guide (top-of-file rationale block, 2-space YAML indent, decision-ID inline comments)
    - electron-builder.yml lines 1–8 — in-repo style analog for the top-of-file comment block (rationale + decision-ID references)
    - package.json — confirm `scripts.build:mac`, `build:win`, `build:linux` carry `--publish never` after Task 1; the YAML's `-- --publish never` then becomes belt-and-braces (RESEARCH §Shared Patterns)
    - 11-VALIDATION.md §"Falsifiable Acceptance Criteria" (lines 90–105) — the 8 binary checks that Plan 02 will run against this YAML; structure must satisfy them
  </read_first>

  <action>
    Create `.github/workflows/release.yml` with content **verbatim** from 11-RESEARCH.md §"Code Examples" lines 755–906. The YAML is ~152 lines including the rationale comment block. Use the Write tool — greenfield directory, no Bash heredoc.

    Required structure (every element below must be present; RESEARCH provides the complete artifact):

    1. **Top-of-file comment block** (RESEARCH lines 756–765) — name of workflow, trigger summary, runner image rationale, action SHA-pinning rationale. Match `electron-builder.yml`'s rationale-block convention.

    2. **`name: release`** (RESEARCH line 767).

    3. **`on:` block** (D-04, CI-01, CI-06): `push.tags: ['v*.*.*']` AND `workflow_dispatch:` (no inputs per D-05). RESEARCH lines 769–773.

    4. **`concurrency:` block** (D-19): `group: release-${{ github.ref }}`, `cancel-in-progress: false`. RESEARCH lines 775–777.

    5. **`permissions: contents: write`** (D-20) — workflow-level minimum scope. RESEARCH lines 779–780.

    6. **`test` job** (CI-03):
       - `runs-on: ubuntu-latest`
       - `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1`
       - `actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4.4.0` with `node-version: 22`, `cache: 'npm'`
       - `npm ci`
       - **Tag↔package.json version guard** (D-08, D-10): step gated by `if: github.event_name == 'push'`, runs the bash block from RESEARCH lines 793–803 that diffs `${GITHUB_REF_NAME#v}` against `node -p "require('./package.json').version"` and fails with `::error::` annotations on mismatch.
       - `npm run typecheck`
       - `npm run test`

    7. **`build-mac` job** (CI-02 #1):
       - `needs: test`
       - `runs-on: macos-14` (D-06 — arm64 native; required for sharp's `@img/sharp-darwin-arm64` resolution per Pitfall 1 / Phase 10 RESEARCH)
       - same checkout + setup-node + `npm ci` preamble
       - `npm run build:mac -- --publish never` (belt-and-braces; package.json already has it from Task 1)
       - `env: CSC_IDENTITY_AUTO_DISCOVERY: false` on the build step (Pitfall 7 — defensive: prevents electron-builder from probing for keychain identities on a clean runner)
       - `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4.6.2` with `name: installer-mac`, `path: release/*.dmg`, `if-no-files-found: error`, `retention-days: 14`
       - **NO `env: GH_TOKEN: ...` block anywhere** (Pitfall 1 — exporting GH_TOKEN to electron-builder triggers auto-publish)

    8. **`build-win` job** (CI-02 #2): identical shape on `windows-2022`, `npm run build:win -- --publish never`, `path: release/*.exe`, `name: installer-win`. NO macOS-specific env vars.

    9. **`build-linux` job** (CI-02 #3): identical shape on `ubuntu-22.04` (D-06; D-17 — NO `apt install libfuse2` step; FUSE only needed at AppImage runtime, not build time), `npm run build:linux -- --publish never`, `path: release/*.AppImage`, `name: installer-linux`. NO `apt install`, NO smoke-launch (D-18).

    10. **`publish` job** (CI-04, CI-05, REL-01, REL-02):
        - `needs: [build-mac, build-win, build-linux]` (D-02 — atomicity gate; NO partial publish possible)
        - `runs-on: ubuntu-latest`
        - **`if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')`** (D-04 — CI-06 dry-run gate; skip on workflow_dispatch)
        - `permissions: contents: write` (job-level reaffirm)
        - `actions/checkout` step (needed for `.github/release-template.md`)
        - **`actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4.3.0`** with `pattern: installer-*`, `merge-multiple: true`, `path: ./assets`
        - **`ls -la ./assets/`** debug step
        - **envsubst step** (D-12, REL-02): `env: VERSION: ${{ github.ref_name }}`, `TAG: ${{ github.ref_name }}`, `INSTALL_DOC_LINK: https://github.com/${{ github.repository }}/blob/main/README.md`, then `run: envsubst < .github/release-template.md > release-body.md`
        - **`softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65  # v2.6.2`** (D-03) with: `tag_name: ${{ github.ref_name }}`, `name: ${{ github.ref_name }}`, `body_path: release-body.md`, `draft: true`, `prerelease: ${{ contains(github.ref_name, '-') }}` (auto-flags `-rc1` etc. as prerelease), `fail_on_unmatched_files: true` (T-11-02 mitigation), `files:` block with three lines: `assets/*.dmg`, `assets/*.exe`, `assets/*.AppImage`.

    Inline comment style: every non-obvious decision gets a one-line `# D-XX` or `# CI-XX` / `# REL-XX` traceability comment per the `electron-builder.yml` style convention (PATTERNS §"Comment-Driven Decision Traceability"). Examples:
    - `cancel-in-progress: false       # D-19 — never cancel a half-built release`
    - `contents: write                 # D-20 — minimum scope`
    - `if-no-files-found: error        # CI-05 upstream guard against empty-asset upload`
    - `if: github.event_name == 'push'  # D-10 — skip on workflow_dispatch dry runs`

    YAML rules:
    - 2-space indentation throughout (no tabs); confirm with `awk '/\t/' .github/workflows/release.yml | wc -l` returning `0`.
    - All five third-party action `uses:` directives use full 40-character commit SHAs with trailing `# vX.Y.Z` version comments (PATTERNS §"SHA-Pinning of GitHub Actions").
    - No floating `@v4` / `@main` references anywhere.

    Atomic commit:
    `feat(11-01): add .github/workflows/release.yml — tag-triggered cross-platform CI with atomic draft-release publish (CI-01..CI-06, REL-01, REL-02)`
  </action>

  <verify>
    <automated>test -f .github/workflows/release.yml && grep -q "^name: release$" .github/workflows/release.yml && grep -q "^on:$" .github/workflows/release.yml && grep -qE "^\s+- 'v\*\.\*\.\*'$" .github/workflows/release.yml && grep -q "workflow_dispatch:" .github/workflows/release.yml && grep -q "concurrency:" .github/workflows/release.yml && grep -q "cancel-in-progress: false" .github/workflows/release.yml && grep -q "contents: write" .github/workflows/release.yml && grep -E "^\s+test:$" .github/workflows/release.yml && grep -E "^\s+build-mac:$" .github/workflows/release.yml && grep -E "^\s+build-win:$" .github/workflows/release.yml && grep -E "^\s+build-linux:$" .github/workflows/release.yml && grep -E "^\s+publish:$" .github/workflows/release.yml && grep -qE "needs:\s*\[build-mac,\s*build-win,\s*build-linux\]" .github/workflows/release.yml && grep -q "if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')" .github/workflows/release.yml && grep -c "if-no-files-found: error" .github/workflows/release.yml | grep -q "^3$" && grep -q "fail_on_unmatched_files: true" .github/workflows/release.yml && grep -q "softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65" .github/workflows/release.yml && grep -q "actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5" .github/workflows/release.yml && grep -q "actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020" .github/workflows/release.yml && grep -q "actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02" .github/workflows/release.yml && grep -q "actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093" .github/workflows/release.yml && grep -q "envsubst < .github/release-template.md" .github/workflows/release.yml && ! grep -q "GH_TOKEN" .github/workflows/release.yml && ! grep -qE "^\s*-?\s*apt[- ]get?\s+install.*libfuse2" .github/workflows/release.yml && ! grep -q "actions/cache@.*electron-builder" .github/workflows/release.yml</automated>
  </verify>

  <acceptance_criteria>
    - File `.github/workflows/release.yml` exists.
    - **Top-of-file rationale block:** first ~10 lines are `#` comments naming the runner-image pinning rationale and the SHA-pinning rationale (PATTERNS §"Comment-Driven Decision Traceability"). `head -10 .github/workflows/release.yml | grep -c '^#'` returns ≥ 5.
    - **Workflow-level keys:** `grep -c '^name: release$' release.yml` = 1; `grep -c '^on:$' release.yml` = 1; `grep -c '^concurrency:$' release.yml` = 1; `grep -c '^permissions:$' release.yml` = 1; `grep -c '^jobs:$' release.yml` = 1.
    - **Trigger surface (CI-01, CI-06):** YAML contains both `tags: [ 'v*.*.*' ]` (or equivalent indent) AND `workflow_dispatch:` under `on:`.
    - **Concurrency (D-19):** `cancel-in-progress: false` is present.
    - **Workflow-level permissions (D-20):** `contents: write` appears at the workflow level (not just inside `publish`).
    - **All five jobs declared:** `grep -cE '^  (test|build-mac|build-win|build-linux|publish):$' release.yml` returns `5`.
    - **`needs:` graph (CI-03, CI-05, D-02):**
       - `grep -A1 '^  build-mac:' release.yml` next non-blank line includes `needs: test`.
       - `grep -A1 '^  build-win:' release.yml` next non-blank line includes `needs: test`.
       - `grep -A1 '^  build-linux:' release.yml` next non-blank line includes `needs: test`.
       - `grep -A1 '^  publish:' release.yml` shows `needs: [build-mac, build-win, build-linux]`.
    - **Runner pins (D-06):** `runs-on: ubuntu-latest` appears for `test` AND `publish`; `runs-on: macos-14` appears once (build-mac); `runs-on: windows-2022` appears once (build-win); `runs-on: ubuntu-22.04` appears once (build-linux).
    - **Tag↔version guard (D-08, D-10):** YAML contains a step gated by `if: github.event_name == 'push'` that runs `TAG_VERSION="${GITHUB_REF_NAME#v}"` and compares to `require('./package.json').version`.
    - **Build invocations:** `grep -c 'npm run build:(mac|win|linux) -- --publish never' release.yml` returns `3`.
    - **Action SHAs pinned:** all five action SHAs from the interfaces block are present verbatim:
       - `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5`
       - `actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020`
       - `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02`
       - `actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093`
       - `softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65`
    - **No floating versions:** `grep -E 'uses:\s*[^@]+@(v|main|master)[0-9]*$' release.yml` returns no matches (every `uses:` has a 40-char SHA).
    - **`if-no-files-found: error` on every upload-artifact step:** `grep -c 'if-no-files-found: error' release.yml` returns `3` (one per build job).
    - **`retention-days: 14`** appears `3` times (one per build job).
    - **`cache: 'npm'`** appears 4 times (test + 3 build jobs).
    - **`npm ci`** (NOT `npm install`) appears 4 times (test + 3 build jobs); `grep -c 'npm install' release.yml` returns `0`.
    - **macOS defensive env (Pitfall 7):** `CSC_IDENTITY_AUTO_DISCOVERY: false` appears once, scoped to the build-mac job's build step.
    - **Publish job atomicity gate (D-04, CI-06):** the literal string `if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')` appears once, on the publish job.
    - **envsubst step (D-12, REL-02):** literal `envsubst < .github/release-template.md > release-body.md` appears once.
    - **Render env vars:** publish job's envsubst step has `env:` block with `VERSION:`, `TAG:`, and `INSTALL_DOC_LINK:` keys, all referencing `${{ github.ref_name }}` or `${{ github.repository }}` appropriately.
    - **softprops invocation:** `tag_name: ${{ github.ref_name }}`, `name: ${{ github.ref_name }}`, `body_path: release-body.md`, `draft: true`, `fail_on_unmatched_files: true`, and a `files:` block listing `assets/*.dmg`, `assets/*.exe`, `assets/*.AppImage` are all present in the publish job.
    - **`prerelease: ${{ contains(github.ref_name, '-') }}`** appears once (auto-flags `-rc1` etc. as prerelease).
    - **Anti-pattern absences (Pitfalls 1, 3, 5):**
       - `grep -c 'GH_TOKEN' release.yml` returns `0` (T-11-01 mitigation).
       - `grep -c 'GITHUB_TOKEN' release.yml` returns `0` (the publish action consumes `${{ github.token }}` implicitly; no manual export).
       - `grep -E 'apt[- ]get?\s+install.*libfuse2' release.yml` returns no matches (D-17 — FUSE not needed at build time).
       - `grep -E 'actions/cache@.*electron-builder' release.yml` returns no matches (D-15 — caching deferred).
       - `grep -E '(certificateFile|CSC_LINK|APPLE_ID|APPLE_APP_SPECIFIC_PASSWORD|notarize)' release.yml` returns no matches (no signing in v1.1).
       - `grep -E '(latest\.yml|latest-mac\.yml)' release.yml` returns no matches (Phase 12 territory).
       - `grep -E '(sentry|source-?map.*upload|sourcemaps create-release)' -i release.yml` returns no matches (Phase 13 territory).
    - **No tabs:** `awk '/\t/' .github/workflows/release.yml | wc -l` returns `0`.
    - **File length:** `wc -l < .github/workflows/release.yml` returns ≥ 130 (the full pattern requires ~150 lines including comments and blank separators).
    - `npm run typecheck && npm run test` still pass (this YAML touches no runtime code).
    - `git status --short .github/workflows/release.yml` shows the file as added.
  </acceptance_criteria>

  <done>
    .github/workflows/release.yml exists with all five jobs, the SHA-pinned actions, the tag-version guard, the artifact-handoff atomicity pattern, the workflow_dispatch dry-run gate, the envsubst-rendered draft release, and committed. Anti-pattern grep checks all return zero. Phase 11 file authoring is complete; Plan 02 is now the live verification surface.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → GitHub Actions runner | Untrusted execution environment (third-party actions resolve to code at SHA); pinned SHAs eliminate drift. |
| Build job → publish job | GHA artifact handoff (`upload-artifact@v4` → `download-artifact@v4`); the only way installer bytes flow to the publish step. No direct API call from build jobs to GitHub Releases. |
| publish job → GitHub Releases API | `${{ github.token }}` (auto-injected with `contents: write` per D-20); used by `softprops/action-gh-release@v2.6.2` only. No manual `env: GH_TOKEN` export at any layer. |
| Tag push → workflow trigger | The first authentic moment of asset creation; tag↔package.json version guard (D-08) is the integrity check at this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | `GITHUB_TOKEN` available to build jobs that should not need it (Pitfall 1: electron-builder unconditionally checks for `GH_TOKEN` in env and auto-publishes if found, breaking atomicity AND exposing the token to a longer-running surface) | mitigate | (a) Workflow-level `permissions: contents: write` (D-20) — minimum scope, no `packages:` / `id-token:`. (b) NO `env: GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` on any build job (PATTERNS §"Never Export `GH_TOKEN` / `GITHUB_TOKEN` to env"). (c) `--publish never` baked into both `package.json#scripts.build:*` (Task 1) AND `release.yml#build-*` invocations (Task 3) — belt-and-braces defense. (d) The publish job consumes `${{ github.token }}` implicitly via the `softprops/action-gh-release` action's default `token:` input — token never appears in shell env. (e) macOS-specific defensive: `CSC_IDENTITY_AUTO_DISCOVERY: false` to prevent electron-builder from probing for keychain identities (Pitfall 7). |
| T-11-02 | Tampering / Integrity | Partial-asset draft release published if a build-stage upload fails after some assets are uploaded but before all three are gathered | mitigate | (a) D-02 artifact-handoff pattern: build jobs upload to GHA workflow artifacts (NOT to a release), and the `publish` job is the only step that touches the GitHub Releases API. (b) `publish.needs: [build-mac, build-win, build-linux]` — if any build job fails or is cancelled, `publish` is never scheduled (status "Skipped"); no draft release is created. (c) `if-no-files-found: error` on every `actions/upload-artifact@v4` step (Task 3 acceptance — appears 3×) — catches silent zero-file uploads where electron-builder reports success but emits no installer. (d) `softprops/action-gh-release@v2.6.2` configured with `fail_on_unmatched_files: true` — catches the pathological "download-artifact returned an empty directory" case at the publish stage. (e) Phase verification (Plan 02) runs an explicit atomicity audit: maintainer reads YAML and confirms (i) `publish.needs:` includes all three build jobs, (ii) `publish.if:` excludes `workflow_dispatch`, (iii) `if-no-files-found: error` on all three uploads, (iv) `fail_on_unmatched_files: true` on softprops. This is VALIDATION.md falsifiable acceptance criterion #8. |
| T-11-03 | Spoofing / Supply Chain | Third-party GitHub Action silently swaps to malicious code via tag re-point (`@v4` style references) | mitigate | All five third-party `uses:` directives pin to full 40-character commit SHAs with trailing `# vX.Y.Z` version comments (PATTERNS §"SHA-Pinning"). Bumping is a deliberate PR (update both SHA and comment), never a floating tag re-point. Acceptance: `grep -E 'uses:\s*[^@]+@(v|main|master)[0-9]*$' release.yml` returns no matches. |

**Severity assessment (per phase init `security_enforcement` config, ASVS L1 / block_on: high):** All three threats are LOW-MEDIUM severity given the public-repo + tag-triggered + draft-only nature of this release pipeline. Mitigations are in-line with each YAML edit, not deferred. No threat blocks the phase; all three are addressed in Plan 01's task acceptance criteria.
</threat_model>

<verification>
After all three tasks land:

- `git log --oneline -3` shows three atomic commits in this order:
  1. `chore(11-01): bake --publish never into build:mac/win/linux scripts (Pitfall 2 defense)`
  2. `feat(11-01): add .github/release-template.md (REL-02 envsubst-rendered release notes)`
  3. `feat(11-01): add .github/workflows/release.yml — tag-triggered cross-platform CI with atomic draft-release publish (CI-01..CI-06, REL-01, REL-02)`
- `npm run typecheck && npm run test` exits 0 (existing 331 tests still pass; no runtime code modified).
- `find .github -type f` lists exactly two files: `.github/workflows/release.yml` and `.github/release-template.md`.
- All grep-verifiable acceptance criteria from each task are TRUE.
- Plan 02 (Wave 2) is now unblocked; it depends on these three commits being on `main`.
</verification>

<success_criteria>
- Three new artifacts exist: `.github/workflows/release.yml`, `.github/release-template.md`, and the `--publish never` patch on `package.json`.
- All locked CONTEXT decisions D-01..D-23 are encoded in the YAML (verified by acceptance-criteria greps; no live workflow run yet — that's Plan 02).
- All eight RESEARCH pitfalls are mitigated by structure (no `GH_TOKEN` env, no `apt install libfuse2`, no electron-builder cache, SHA-pinned actions, `if-no-files-found: error`, `fail_on_unmatched_files: true`, version guard, `CSC_IDENTITY_AUTO_DISCOVERY: false`).
- Anti-patterns are absent (verified by negative greps): no signing keys, no `latest.yml`, no source-map upload, no Sentry, no vitest YAML test.
- Existing test suite (331 tests) remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-release-pipeline-github-actions-draft-release/11-01-SUMMARY.md` documenting: files created, commit SHAs, anti-pattern greps run, and a forward note that Plan 02 will exercise the workflow live (no behavior is verified end-to-end until Plan 02 runs).
</output>
