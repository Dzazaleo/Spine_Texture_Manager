---
phase: 43-runtime-adapter-facade-verified-4-3-api-mapping
plan: 03
type: execute
wave: 2
depends_on: [43-02, 43-04]
files_modified:
  - src/core/loader.ts
  - src/core/sampler.ts
  - src/core/bounds.ts
  - src/core/types.ts
autonomous: true
requirements: [RT-02, SAFE-02, SAFE-03]
user_setup: []

must_haves:
  truths:
    - "sampler.ts, bounds.ts, and loader.ts import NO spine-core package — the RT-02 arch anchor (Plan 01) is GREEN"
    - "loader.ts hard-picks pickRuntime('4.2') UNCONDITIONALLY (no version detection — D-02) and the parse seam (SkeletonJson/TextureAtlas/AtlasAttachmentLoader + the Phase-33 patch) is relocated into runtime-42.ts; loader calls rt.makeAtlas/parseSkeleton/applyRotatedRegionFix"
    - "Every git-tracked 4.2 fixture sampled through the rewired adapter is byte-identical (strict toEqual on canonicalized full SamplerOutput) to the FROZEN Phase-42 SAFE-01 baseline — the HARD phase-exit gate (SAFE-02)"
    - "SAFE-01 enumeration + freeze-guard stay green; baselines are NOT regenerated (D-09)"
  artifacts:
    - path: "src/core/loader.ts"
      provides: "spine-core-free loader; hard-pick pickRuntime('4.2'); LoadResult.runtime populated; parse seam delegated to the runtime"
      contains: "pickRuntime"
    - path: "src/core/sampler.ts"
      provides: "spine-core-free sampler; ~12 leaf calls routed through load.runtime.*; LOCKED tick order preserved"
    - path: "src/core/bounds.ts"
      provides: "spine-core-free, instanceof-free bounds math; (rt, sk) threaded through attachmentWorldAABB/computeRenderScale"
  key_links:
    - from: "src/core/loader.ts"
      to: "src/core/runtime/runtime.ts (pickRuntime)"
      via: "const rt = pickRuntime('4.2') — unconditional hard-pick (D-02)"
      pattern: "pickRuntime\\\\(['\"]4\\\\.2['\"]\\\\)"
    - from: "src/core/sampler.ts + src/core/bounds.ts"
      to: "load.runtime (SpineRuntime)"
      via: "all spine-core leaf calls routed through rt.*"
      pattern: "load\\\\.runtime|rt\\\\."
    - from: "tests/safe01/safe01-baseline.spec.ts"
      to: "the FROZEN Phase-42 SAFE-01 baselines"
      via: "strict toEqual(frozenPart(live), frozenPart(committed)) through the rewired path"
      pattern: "toEqual"
---

<objective>
Rewire the three consumers (`loader.ts`, `sampler.ts`, `bounds.ts`) to call
through `load.runtime.*` so NONE imports a spine-core package, then prove the
SAFE-02 hard phase-exit gate: every git-tracked 4.2 fixture sampled through the
rewired adapter is byte-identical to the FROZEN Phase-42 SAFE-01 baseline.

Purpose: This is the behavior-neutrality proof. The 4.2.111 runtime is unchanged
spine-core (Plan 02 relocated its call shapes verbatim); if SAFE-02 moves, the
facade leaks and the port stops (SAFE-02 is the only HARD exit gate per D-01).
RT-02's arch anchor (Plan 01) goes GREEN here; D-02's full parse-relocation +
unconditional 4.2 hard-pick lands here.

Output: spine-core-free `loader.ts`/`sampler.ts`/`bounds.ts`; `LoadResult.runtime`
populated; SAFE-02 + SAFE-01 enumeration + freeze-guard all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-CONTEXT.md
@.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-RESEARCH.md
@.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-PATTERNS.md

<interfaces>
<!-- The runtime is consumed via load.runtime (SpineRuntime). Plan 02/04 built
     the two impls + pickRuntime. This plan ONLY rewires the consumers to rt.* —
     the sampler/bounds ALGORITHM is byte-UNCHANGED (RESEARCH: only ~12 leaf
     calls move; logic is identical). -->

From src/core/types.ts:186-191 (already declared — populate, do NOT make non-optional):
```typescript
export interface LoadResult {
  // ...
  runtime?: SpineRuntime;   // optional until Phase 43 — Plan 03 populates it via the hard-coded 4.2 pick
}
```

From src/core/runtime/runtime.ts (the SpineRuntime surface — Plan 02/04 implemented both):
```typescript
export declare function pickRuntime(tag: RuntimeTag): SpineRuntime;  // Plan 02 gave this a body
// rt.makeAtlas / rt.parseSkeleton / rt.applyRotatedRegionFix / rt.makeSkeleton /
// rt.makeAnimationState / rt.skins / rt.animations / rt.setSkin / rt.setupPose /
// rt.setupPoseSlots / rt.clearTracks / rt.setAnimation / rt.stateUpdate /
// rt.stateApply / rt.skeletonUpdate / rt.updateWorldTransform / rt.slots /
// rt.slotName / rt.slotAttachment / rt.skinEntries / rt.attachmentKind /
// rt.regionWorldVertices / rt.vertexWorldVertices / rt.boneAxisScale /
// rt.attachmentRegionMeta / rt.attachmentUVs / rt.sequenceRegions /
// rt.attachmentTimelineNames    (Q1 additive — Plan 01)
```

The PATTERNS rewire tables (43-PATTERNS.md — the EXACT line→replacement maps):
- § "src/core/sampler.ts (MODIFY ...)" — the ~12 leaf-call line→rt.* table; the LOCKED-TICK invariant; the Pass-1.5 invariant
- § "src/core/bounds.ts (MODIFY ...)" — the (rt, sk) signature change; the 10 instanceof sites → rt.attachmentKind; the hullAreaRatio .region/.uvs → rt.attachmentRegionMeta/attachmentUVs
- § "src/core/loader.ts (MODIFY ...)" — the parse-seam relocation table; the unconditional pickRuntime('4.2'); LoadResult.runtime at loader.ts:858
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the loader parse seam into runtime-42.ts call sites + hard-pick pickRuntime('4.2'); rewire sampler.ts + bounds.ts to load.runtime.* (RT-02 anchor → GREEN)</name>
  <read_first>
    - src/core/loader.ts:32-40 (the spine-core import to remove), :64-89 (StubTexture/createStubTextureLoader — already relocated into runtime-42 by Plan 02; delete here), :485-529 (atlas + SkeletonJson construction), :531-614 (the Phase-33 rotated-region patch — relocated into runtime-42.applyRotatedRegionFix by Plan 02; delete here), :845-875 (the LoadResult return — add `runtime: rt`)
    - src/core/sampler.ts:47-53 (import to remove), :158-172 (ctor + setup-pose), :228-285 (Pass 1.5 — preserve verbatim, only swap the leaf calls), :289-339 (the AttachmentTimeline walk + the LOCKED tick + setAnimationWith)
    - src/core/bounds.ts:31-41 (import to remove), :59-92 (attachmentWorldAABB — signature gains (rt, sk)), :148-290 (computeRenderScale + hullAreaRatio — instanceof + .region/.uvs), :360-399 (weightedSumMeshRenderScale + boneAxisScales)
    - src/core/types.ts:186-191 (LoadResult.runtime — already declared optional; do NOT make non-optional)
    - .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-PATTERNS.md §§ "src/core/loader.ts", "src/core/sampler.ts", "src/core/bounds.ts", "src/core/types.ts" (the verbatim line→replacement tables + the LOCKED-TICK / Pass-1.5 / Q3-signature-ripple invariants)
    - .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-RESEARCH.md §§ "Pattern 3" (loader wiring D-02), "Pitfall 3" (SAFE-02 strict, no regen, the SIMPLE_PROJECT_43 exclusion), "Open Questions" Q3 (the bounds (rt,sk) ripple is ONE coordinated edit), "Pitfall 4" (the .region/.uvs NEW finding routing)
  </read_first>
  <action>
This is ONE coordinated edit across loader.ts + sampler.ts + bounds.ts +
types.ts (RESEARCH Q3: the `bounds.ts` `(rt, sk)` signature ripple touches the
hot-loop call sites and MUST be done together, not piecemeal). The sampler/bounds
ALGORITHM is byte-UNCHANGED — only the ~12 leaf calls move behind `rt.*`
(RESEARCH § Architectural Responsibility Map: "Logic UNCHANGED").

**loader.ts (D-02 full parse-relocation + unconditional hard-pick):**
- DELETE the import block `loader.ts:32-40` (the 7 spine-core symbols). KEEP
  `node:fs`/`node:path` (loader is the sanctioned FS load-time carve-out —
  `arch.spec.ts:163`, UNCHANGED).
- DELETE `class StubTexture` + `createStubTextureLoader` (`loader.ts:64-89`) —
  Plan 02 relocated them into `runtime-42.ts`. Update any in-repo importer of
  the exported `createStubTextureLoader` (grep `createStubTextureLoader` repo-
  wide first; if a test/CLI imports it, re-point that importer to a thin
  loader-side re-export OR to the runtime — prefer: keep a `loader.ts` re-export
  `export { createStubTextureLoader }` ONLY if it can be sourced without a
  spine-core import; if not, update the importer to not need it. The RT-02
  anchor forbids loader.ts importing spine-core, so the re-export cannot pull
  spine-core back in).
- `import { pickRuntime } from './runtime/runtime.js';`
- After the EXISTING `checkSpineVersion`/`checkSpine43Schema` guards (these stay
  EXACTLY as-is — still throw `<4.2`/`≥4.3`; Phase 43 does NOT repurpose them —
  that is Phase 44/45): `const rt = pickRuntime('4.2');` — **UNCONDITIONAL hard-
  pick, NO `resolveRuntimeTag`, NO version detection** (D-02; Phase 44 owns
  dispatch).
- Replace `new TextureAtlas(...)` + the page-stub loop (both the canonical-mode
  branch ~`loader.ts:485-498` and the atlas-less synth branch ~`:852-857`) with
  `const atlas = rt.makeAtlas(atlasText);` — the 4-way atlas-resolution control
  flow (D-06/07/08) is UNCHANGED; only the `new TextureAtlas(...)`+stub
  construction moves behind `rt.makeAtlas`. (Note: `rt.makeAtlas` returns an
  `OpaqueAtlas`; thread that handle to `rt.parseSkeleton`.)
- Replace the `SilentSkipAttachmentLoader`/`AtlasAttachmentLoader` +
  `new SkeletonJson(...)` + `.readSkeletonData(parsedJson)` block
  (`loader.ts:525-529`) with `const skeletonData = rt.parseSkeleton(parsedJson,
  atlas, isAtlasLess);` (the atlas-less branch logic moved into
  `runtime-42.parseSkeleton` per Plan 02).
- Replace the entire Phase-33 rotated-region offset patch
  (`loader.ts:531-614`, the `if (!isAtlasLess) { ...offset[] write... }`) with
  `if (!isAtlasLess) rt.applyRotatedRegionFix(skeletonData);` (the verbatim
  math is inside `runtime-42.applyRotatedRegionFix` per Plan 02; the
  `!isAtlasLess` gate stays loader-side to preserve the exact original
  guard).
- Add `runtime: rt,` to the `return { ... }` object at `loader.ts:858`
  (alongside `skeletonData,`/`atlas: atlas!,`). The downstream `atlas` usages
  (sourceDims build at `loader.ts:620+`, `atlasSources`) read `atlas.regions`
  etc. — if those now operate on an `OpaqueAtlas`, the loader needs the raw
  regions for the sourceDims map; the cleanest preservation: have
  `rt.makeAtlas` ALSO be usable for the sourceDims walk by NOT changing the
  sourceDims code's access to `atlas.regions` — i.e. the `OpaqueAtlas` is a
  branded passthrough of the real `TextureAtlas`, so existing
  `atlas.regions`/`atlas.pages` reads still work at runtime (the brand is a
  compile-time phantom; `types.ts:50` attaches `__rt` non-enumerably and
  returns the same object). To keep loader.ts spine-core-TYPE-free while still
  reading `atlas.regions` for sourceDims, cast the `OpaqueAtlas` through a
  narrow local structural type (`{ regions: Array<{ name; ... }>; pages: ... }`)
  at the sourceDims site — do NOT re-import `TextureAtlas`. Preserve the
  sourceDims/atlasSources logic byte-for-byte otherwise (it is SAFE-02-gated).

**types.ts:** leave `runtime?: SpineRuntime` OPTIONAL (PATTERNS § types.ts —
do NOT make non-optional; partial `LoadResult` test/CLI builders exist).
Optionally update the trailing comment "optional until then" → "populated Phase
43 (43-03)". No type-shape change.

**sampler.ts (route ~12 leaf calls; algorithm byte-unchanged):**
- DELETE the import block `sampler.ts:47-53` entirely.
- At the top of `sampleSkeleton`, after `const load` is in scope:
  `const rt = load.runtime; if (rt == null) throw new Error('sampleSkeleton:
  load.runtime missing — loader must populate it (43-03)');` (defensive — the
  hard-pick always populates it; the throw makes a regression loud).
- Apply the PATTERNS § sampler.ts line→replacement table EXACTLY:
  `new Skeleton(...)`→`rt.makeSkeleton(load.skeletonData)`; `new
  AnimationState(new AnimationStateData(...))`→`rt.makeAnimationState(load.
  skeletonData)`; `load.skeletonData.skins`→`rt.skins(load.skeletonData)`;
  `skeleton.setSkin(skin)`→`rt.setSkin(sk, skin)`;
  `skeleton.setToSetupPose()`→`rt.setupPose(sk)`;
  `skeleton.setSlotsToSetupPose()`→`rt.setupPoseSlots(sk)`;
  `state.clearTracks()`→`rt.clearTracks(st)`;
  `skeleton.updateWorldTransform(Physics.pose|reset|update)`→
  `rt.updateWorldTransform(sk,'pose'|'reset'|'update')`;
  `skin.getAttachments()`→`rt.skinEntries(skin)`;
  `skeleton.slots[i]`→`rt.slots(sk)[i]`; `slot.data.name`→`rt.slotName(slot)`;
  `(attachment as {region?}).region?.name`→`rt.attachmentRegionMeta(a)?.name`
  (the Pitfall-4 NEW-finding routing — at sampler.ts:247 AND any other
  `attachment.region?.name`); `load.skeletonData.animations`→
  `rt.animations(load.skeletonData)`; `anim.duration`→`rt.animationDuration
  (anim)`; `anim.name`→`rt.animationName(anim)`;
  `state.setAnimationWith(0, anim, false)`→`rt.setAnimation(st, 0, anim,
  false)`; the AttachmentTimeline walk `for (const tl of anim.timelines) if
  (tl instanceof AttachmentTimeline)...`→`const animAttachmentNames =
  rt.attachmentTimelineNames(anim);` (Q1 method); `attachmentWorldAABB(slot,
  attachment)`→`attachmentWorldAABB(rt, sk, slot, a)`;
  `computeRenderScale(slot, attachment)`→`computeRenderScale(rt, sk, slot, a)`;
  `fanOutSequencePeaks(skeleton, ...)`→thread `rt` so its `a.sequence?.regions`
  reads go through `rt.sequenceRegions(a)`.
- **LOCKED TICK ORDER (CLAUDE.md Fact #3 — `sampler.ts:318-323`):** the 1:1
  substitution MUST preserve `rt.stateUpdate(st,dt) → rt.stateApply(st,sk) →
  rt.skeletonUpdate(sk,dt) → rt.updateWorldTransform(sk,'update')` in that
  EXACT order. Same for the `Physics.reset`-once-per-animation determinism
  anchor (`sampler.ts:309-311`).
- **Pass 1.5 invariant (`sampler.ts:228-285`, memory:
  `project_sampler_visibility_invariant`):** preserve the defer/dedupe logic
  VERBATIM — only swap the leaf calls (`skin.getAttachments`→`rt.skinEntries`,
  `slot.data.name`→`rt.slotName`, `.region?.name`→`rt.attachmentRegionMeta(a)
  ?.name`). Do NOT alter the `globalPeaks.has(key)` defer or the dual-write.
- `skinName`: replace `skin.name` reads with `rt.skinName(skin)` where the key
  is built (`${skin.name}/...`); keep the key string shape identical.

**bounds.ts (instanceof-free; (rt, sk) threaded — RESEARCH Q3, ONE edit):**
- DELETE the import block `bounds.ts:31-41` entirely. Replace param types with
  the opaque handles + `SpineRuntime` (`import type { SpineRuntime } from
  './runtime/runtime.js'; import type { OpaqueSkeleton, OpaqueSlot,
  OpaqueAttachment } from './runtime/types.js';`).
- `attachmentWorldAABB`: change signature to `(rt: SpineRuntime, sk:
  OpaqueSkeleton, slot: OpaqueSlot, a: OpaqueAttachment)`; body becomes the
  RESEARCH § "bounds.ts — instanceof-free" example: `const kind =
  rt.attachmentKind(a); if (kind==='region') return aabbFromFloat32(rt.
  regionWorldVertices(slot,a),4); if (kind==='skip') return null; if
  (kind==='mesh'||kind==='vertex') { const v=rt.vertexWorldVertices(sk,slot,
  a); if (v.length<=0) return null; return aabbFromFloat32(v, v.length/2); }
  return null;` — the kind ORDER lives INSIDE the adapter's `attachmentKind`
  (Plan 02/04), NOT here.
- `computeRenderScale`: mirror the same `(rt, sk, slot, a)` signature. Every
  `instanceof` site in `bounds.ts` (`:64,73-77,83`, `:153,158-163,168,179`) →
  the `rt.attachmentKind(a)` switch. The peak-anchored canonical-correction
  math (`bounds.ts:248-289`, memory: `project_peak_anchored_invariants`) is
  byte-UNCHANGED — only the SOURCE of `canonW/canonH/origW/origH/pageW/pageH`
  changes from direct `attachment.region`/`.uvs` member reads to
  `rt.attachmentRegionMeta(a)` / `rt.attachmentUVs(a)` (Pitfall 4 routing): in
  `hullAreaRatio`, replace `attachment.region as {...}` (`:219-227`) with
  `rt.attachmentRegionMeta(a)` (read `.pageW/.pageH/.originalW/.originalH/
  .canonW/.canonH`), and `attachment.uvs` (`:230`) with `rt.attachmentUVs(a)`.
  The hullArea/sqrt/correction formula stays character-identical.
- `weightedSumMeshRenderScale` (`bounds.ts:360-389`) + `boneAxisScales`
  (`:392-399`): replace `bone.getWorldScaleX/Y()` with `rt.boneAxisScale(slot)`
  for the slot-bone path. The per-influence weighted loop at `:380-385`
  iterates `skeletonBones[bones[v]]` per influence and has no opaque handle —
  this is a generic-VertexAttachment fallback that `MeshAttachment` never hits
  (it always ships `triangles[]`; the `:360` docstring confirms it is
  unreachable in practice). Add a narrow inline comment: `// generic-
  VertexAttachment per-influence path: unreachable for MeshAttachment
  (triangles[] always present, bounds.ts:360 docstring). If a future
  triangulation-less VertexAttachment subtype reaches here, it needs a
  per-bone adapter accessor (43 interface has boneAxisScale(slot) only —
  out of scope; flagged).` Do NOT add an interface method (the path is
  unreachable; SAFE-02's frozen corpus has no such attachment so it stays
  byte-green).

**Verification of the RT-02 anchor:** after this edit, `sampler.ts`/`bounds.ts`/
`loader.ts` import NO spine-core package → the Plan-01 RT-02 anchor flips GREEN.

Do NOT modify any `tests/safe01/**` file or any baseline JSON (D-09 — SAFE-02
re-runs the EXISTING harness through the rewired path; baselines stay frozen).
Do NOT add `fixtures/SIMPLE_PROJECT_43/` to git in this plan (Plan 05 owns the
4.3-only fixture commit; committing the 4.2 sibling here would trip the SAFE-01
enumeration — Pitfall 3 / Q2). Do NOT touch the `checkSpineVersion`/
`checkSpine43Schema` guards or the typed-error envelope (Phase 44/45 own
reject/dispatch — D-02).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep -c "error TS" | grep -qx 0 && npx vitest run tests/arch.spec.ts tests/safe01/safe01-baseline.spec.ts tests/safe01/safe01-enumeration.spec.ts tests/safe01/safe01-freeze-guard.spec.ts 2>&1 | grep -Eq "passed" && ! npx vitest run tests/arch.spec.ts tests/safe01/safe01-baseline.spec.ts tests/safe01/safe01-enumeration.spec.ts tests/safe01/safe01-freeze-guard.spec.ts 2>&1 | grep -Eq "[1-9][0-9]* failed" && echo "RT-02 anchor GREEN + SAFE-02 byte-equal + enumeration + freeze-guard all green"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lE "from ['\"](@esotericsoftware/spine-core|spine-core-42)['\"]" src/core/sampler.ts src/core/bounds.ts src/core/loader.ts` returns NOTHING (zero of the three import a spine-core package — the RT-02 invariant)
    - `npx vitest run tests/arch.spec.ts -t "Phase 43 RT-02"` PASSES (the Plan-01 anchor is now GREEN — the offenders list is empty)
    - `grep -cE "pickRuntime\(['\"]4\.2['\"]\)" src/core/loader.ts` returns at least `1` AND `grep -c "resolveRuntimeTag\|detectVersion\|pickRuntime\(resolve\|checkSpine43Schema.*pickRuntime" src/core/loader.ts` returns `0` (UNCONDITIONAL hard-pick — NO version detection; D-02)
    - `grep -c "runtime: rt" src/core/loader.ts` returns at least `1` (LoadResult.runtime populated)
    - `grep -c "new SkeletonJson\|new TextureAtlas\|new AtlasAttachmentLoader\|class StubTexture\|attachment.offset\[0\]" src/core/loader.ts` returns `0` (the entire parse seam + Phase-33 offset[] write relocated out of loader.ts; D-02)
    - `grep -cE "\binstanceof (RegionAttachment|VertexAttachment|MeshAttachment|BoundingBoxAttachment|PathAttachment|PointAttachment|ClippingAttachment)\b" src/core/bounds.ts` returns `0` (bounds.ts is instanceof-free — Pitfall 2)
    - `grep -c "attachmentWorldAABB(rt, sk, slot\|attachmentWorldAABB(\s*rt" src/core/bounds.ts` AND `grep -c "computeRenderScale(rt, sk, slot\|computeRenderScale(\s*rt" src/core/bounds.ts` each return at least `1` (the Q3 (rt, sk) signature change landed)
    - `grep -cE "attachment\.region|att\.region|\.uvs\b" src/core/bounds.ts | : ; grep -nE "attachment\.region|attachment\.uvs" src/core/bounds.ts | grep -vc "//" | grep -qx 0` (no direct .region/.uvs member reads in bounds.ts — routed via rt.attachmentRegionMeta/attachmentUVs; Pitfall 4)
    - `grep -c "load.runtime\|rt\." src/core/sampler.ts` returns at least `10` (the ~12 leaf calls routed)
    - `grep -B2 -A4 "stateUpdate" src/core/sampler.ts | grep -A4 "stateUpdate" | grep -q "stateApply" && grep -A6 "rt.stateUpdate" src/core/sampler.ts | grep -q "updateWorldTransform" ` — the LOCKED tick order `stateUpdate→stateApply→skeletonUpdate→updateWorldTransform('update')` is preserved contiguously (CLAUDE.md Fact #3)
    - `npx vitest run tests/safe01/safe01-baseline.spec.ts` PASSES — every git-tracked 4.2 fixture's full canonicalized SamplerOutput is strict-`toEqual` to its FROZEN baseline (SAFE-02 — the HARD exit gate; a single drifted `${skin}/${slot}/${attachment}` record fails it)
    - `npx vitest run tests/safe01/safe01-enumeration.spec.ts` PASSES (no fixture dropped; SIMPLE_PROJECT_43 still untracked → still `excluded` → manifest unchanged)
    - `npx vitest run tests/safe01/safe01-freeze-guard.spec.ts` PASSES (baseline-predates-alias ancestry still holds; no env-regen path introduced — D-09)
    - `git status --porcelain tests/safe01/baselines/` is EMPTY (NOT one baseline regenerated — D-09)
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0
  </acceptance_criteria>
  <done>sampler.ts/bounds.ts/loader.ts import no spine-core package (RT-02 anchor GREEN); loader hard-picks pickRuntime('4.2') unconditionally with the parse seam + Phase-33 patch relocated and LoadResult.runtime populated; bounds.ts is instanceof-free with (rt, sk) threaded and .region/.uvs routed through the adapter; the LOCKED tick order is preserved; SAFE-02 byte-equal + SAFE-01 enumeration + freeze-guard all pass with zero baseline regeneration.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full suite — prove Phase 43 broke nothing pre-existing (incl. the reject-guard specs that must stay green since loader behavior is untouched) + SAFE-03 against the rewired path</name>
  <read_first>
    - tests/runtime43/safe03-cross-runtime.spec.ts (the SAFE-03 seam from Plan 01 — now backed by both adapters; confirm it asserts attachmentKind-vs-loading-runtime + the handleRuntime cross-feed backstop)
    - .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-VALIDATION.md § "Sampling Rate" ("After every plan wave: npm run test" — the full suite proves the reject-guard specs stay green since D-02 leaves loader behavior untouched)
    - .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-RESEARCH.md § "Pitfall 6" (D-04 heavy-rig gate is Plan 05, NOT here — this task is the CI-subset full-suite proof)
  </read_first>
  <action>
Run the FULL vitest suite (`npm run test`, the per-wave sampling point per
`43-VALIDATION.md`). This proves the rewire (Task 1) broke nothing pre-existing
— critically the EXISTING reject-guard specs (the 4.3-schema / `<4.2` /
malformed-version throw assertions) MUST stay green because Phase 43 does NOT
touch loader reject/dispatch behavior (D-02; that is Phase 44/45). Also confirm
SAFE-03 (`tests/runtime43/safe03-cross-runtime.spec.ts`) now passes against the
real rewired path (both adapters exist after Plan 02/04; the 4.3 arm resolves
via `pickRuntime('4.3')`).

If ANY pre-existing spec regresses: the rewire altered behavior — DO NOT
mask it. Diagnose which leaf-call substitution drifted (the most likely
suspects, in order: a changed `computeWorldVertices` arg order, a reordered
LOCKED tick, a `setAnimation` vs `setAnimationWith` mismatch, a `.region`/`.uvs`
routing that changed a value, or the `(rt, sk)` ripple missing a call site).
Fix the substitution to be byte-faithful and re-run. SAFE-02 strict-`toEqual`
is the ground truth — the 4.2.111 runtime is unchanged, so any failure is a
plumbing bug in the rewire, never a "tolerance" issue (NEVER relax to epsilon —
RESEARCH Pitfall 3).

Do NOT run or rely on the D-04 heavy/proprietary-rig local gate here — that is
Plan 05's hard close-gate task (the heavy baselines are gitignored;
CI-subset-green is necessary but NOT sufficient — D-04). Do NOT modify any test
to make it pass; the only sanctioned change is fixing a non-byte-faithful leaf-
call substitution in Task 1's files.

Record in the SUMMARY: the full-suite pass count, explicit confirmation that the
reject-guard specs stayed green (loader behavior untouched — D-02), and SAFE-03
green.
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -25 | grep -Eq "Test Files.*passed" && ! npm run test 2>&1 | grep -Eq "[1-9][0-9]* failed" && npx vitest run tests/runtime43/safe03-cross-runtime.spec.ts 2>&1 | grep -q "passed" && echo "full suite green; reject-guard specs intact; SAFE-03 green"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0 with ZERO failed test files (the full pre-existing suite stays green — the rewire is behavior-neutral)
    - `npx vitest run tests/runtime43/safe03-cross-runtime.spec.ts` PASSES with both its tests run (NOT skipped — both adapters exist after Plan 02/04; the 4.3 arm resolves via `pickRuntime('4.3')`; cross-feed backstop asserted)
    - The existing loader reject-guard specs (whatever asserts the `<4.2` / `≥4.3`-schema / malformed-version typed throws) are GREEN — confirm by running `npx vitest run tests/` and observing no reject-guard regression (D-02: loader behavior untouched)
    - `git status --porcelain tests/` shows ONLY the new Plan-01 `tests/runtime43/*` files and (if Task 1 needed it) the arch.spec — NO modification to any `tests/safe01/baselines/*` (D-09)
    - SAFE-02 (`npx vitest run tests/safe01/safe01-baseline.spec.ts`) still PASSES on re-run (idempotent — strict byte-equal, not flaky)
  </acceptance_criteria>
  <done>The full vitest suite is green; the pre-existing reject-guard specs stayed green (loader reject/dispatch behavior untouched per D-02); SAFE-03 passes against the real rewired both-adapter path; no SAFE-01 baseline was regenerated; the only test-tree changes are the Plan-01 seams (+ arch.spec if Task 1 required).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JSON → loader | the existing `checkSpineVersion`/`checkSpine43Schema` guards are the input boundary — UNCHANGED this phase (still run before any adapter call) |
| loader/sampler/bounds ↔ runtime adapter | opaque branded handles cross here; `load.runtime` is the single seam; no spine-core type names in the three consumers |
| rewired path ↔ FROZEN SAFE-01 corpus | SAFE-02 re-runs the existing harness; baselines must NOT be regenerated (D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-10 | Tampering | a non-byte-faithful leaf-call substitution (changed arg order / reordered LOCKED tick / setAnimation vs setAnimationWith / changed .region routing) silently drifting 4.2 output | mitigate | SAFE-02 strict `toEqual` on the full canonicalized SamplerOutput across all 3 maps (NOT epsilon — Pitfall 3) catches a single drifted `${skin}/${slot}/${attachment}` record; the LOCKED-tick + Pass-1.5 invariants are preserved verbatim and grep-asserted |
| T-43-11 | Tampering | accidental regeneration of a frozen SAFE-01 baseline to "make SAFE-02 pass" | accept | The D-09 freeze guard (`safe01-freeze-guard.spec.ts`) fails loudly if the baseline commit is not a git-ancestor of the alias commit and asserts no env-regen path; acceptance asserts `git status --porcelain tests/safe01/baselines/` is empty; the only sanctioned fix is correcting the rewire, never the baseline |
| T-43-12 | Tampering | the SIMPLE_PROJECT_43 4.2-sibling being committed here, tripping the SAFE-01 enumeration (Pitfall 3 / Q2) | mitigate | This plan explicitly does NOT git-add `fixtures/SIMPLE_PROJECT_43/` (Plan 05 owns the 4.3-only commit); the enumeration spec is run as an acceptance gate and must stay green |
| T-43-13 | Spoofing | a 4.2 JSON silently loaded by the 4.3 runtime (zero constraints, no error) | accept | N/A this phase — loader hard-picks 4.2 UNCONDITIONALLY (D-02); the existing strict version-parse guard still rejects malformed input before any adapter call; routing/dispatch is Phase 44 (DISP-01/03) |
</threat_model>

<verification>
- `npx vitest run tests/arch.spec.ts -t "Phase 43 RT-02"` — GREEN (the Plan-01 anchor's offenders list is now empty).
- `npx vitest run tests/safe01/safe01-baseline.spec.ts` — GREEN (SAFE-02 hard exit gate: every git-tracked 4.2 fixture byte-identical to the FROZEN baseline).
- `npx vitest run tests/safe01/safe01-enumeration.spec.ts tests/safe01/safe01-freeze-guard.spec.ts` — GREEN (no dropout; ancestry intact; no regen path).
- `npm run test` — full suite GREEN incl. the pre-existing reject-guard specs (loader behavior untouched — D-02).
- `npx vitest run tests/runtime43/safe03-cross-runtime.spec.ts` — GREEN (cross-runtime instanceof invariant + handleRuntime cross-feed backstop).
- `git status --porcelain tests/safe01/baselines/` — EMPTY (D-09: zero baseline regeneration).
</verification>

<success_criteria>
- `sampler.ts`/`bounds.ts`/`loader.ts` import NO spine-core package; the RT-02 arch anchor is GREEN.
- `loader.ts` hard-picks `pickRuntime('4.2')` unconditionally (D-02 — no version detection); the parse seam + Phase-33 patch are relocated; `LoadResult.runtime` is populated.
- SAFE-02 is GREEN: every git-tracked 4.2 fixture sampled through the rewired adapter is byte-identical (strict `toEqual`) to the FROZEN Phase-42 SAFE-01 baseline — zero baseline regeneration (D-09).
- SAFE-01 enumeration + freeze-guard stay green; the SIMPLE_PROJECT_43 sibling is NOT committed here.
- The full suite (incl. pre-existing reject-guard specs) is green; SAFE-03 is green against the real both-adapter path.
</success_criteria>

<output>
After completion, create `.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-03-SUMMARY.md`
</output>
