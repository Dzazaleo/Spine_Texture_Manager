---
phase: 43-runtime-adapter-facade-verified-4-3-api-mapping
plan: 05
type: execute
wave: 3
depends_on: [43-03, 43-04]
files_modified:
  - tests/runtime43/runtime43-baseline.spec.ts
  - tests/runtime43/runtime43-d03.spec.ts
  - tests/runtime43/baselines/skeleton2.json
  - fixtures/SIMPLE_PROJECT_43/skeleton2.json
  - fixtures/SIMPLE_PROJECT_43/skeleton2.atlas
  - fixtures/SIMPLE_PROJECT_43/skeleton2.png
  - .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-VERIFICATION.md
autonomous: false
requirements: [PORT-01, PORT-02, PORT-03, SAFE-02]
user_setup: []

must_haves:
  truths:
    - "The 4.3 owner rig skeleton2.json samples through runtime-43 WITHOUT throw, exercising every API-mapped surface, and is byte-stable against its OWN freshly-captured 4.3 baseline (SEPARATE store, NOT golden-shared with SAFE-01 — D-01)"
    - "The PORT-03 4.3 rotated-region behavior is EMPIRICALLY validated against a known-good (4.2-sibling geometry or editor bounds) BEFORE the Approach-A no-op is trusted; Approach B is the documented falsification fallback (Assumptions Log A1 — highest risk)"
    - "The TransformConstraint-on-SQUARE rig reports SQUARE's peak as the POST-constraint value (D-03 empirical canary)"
    - "ONLY the 4.3 triplet (skeleton2.json + .atlas + .png) is committed; the 4.2 sibling skeleton2_42.* stays UNTRACKED (Phase-44 ORCL-01-reserved — D-05/Q2); SAFE-01 enumeration stays green automatically"
    - "The D-04 documented local heavy/proprietary-rig SAFE-02 byte-equal pass is recorded in 43-VERIFICATION.md (the hard close gate — CI-subset-green is necessary but NOT sufficient)"
  artifacts:
    - path: "tests/runtime43/baselines/skeleton2.json"
      provides: "the freshly-captured 4.3 own-baseline (SEPARATE store; canonicalized SamplerOutput frozenPart)"
      contains: "globalPeaks"
    - path: "tests/runtime43/runtime43-baseline.spec.ts"
      provides: "the wired 4.3 own-baseline sentinel + the A1 rotated-region empirical validation"
    - path: ".planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-VERIFICATION.md"
      provides: "the D-04 documented local heavy-rig SAFE-02 pass record (hard close gate) + the A1 validation result"
      contains: "heavy-rig SAFE-02"
  key_links:
    - from: "tests/runtime43/runtime43-baseline.spec.ts"
      to: "fixtures/SIMPLE_PROJECT_43/skeleton2.json via pickRuntime('4.3')"
      via: "tryLoad43() + sample-through-runtime-43 + canonicalize() vs the SEPARATE 4.3 baseline"
      pattern: "canonicalize"
    - from: ".planning/phases/.../43-VERIFICATION.md"
      to: "the gitignored heavy rigs (Girl/SKINS/CHJ/3Queens/Jokerman)"
      via: "documented local SAFE-02 byte-equal run (D-04 hard close gate)"
      pattern: "heavy-rig SAFE-02"
---

<objective>
Capture the 4.3 own-baseline for `skeleton2.json` (SEPARATE store, NOT
golden-shared with SAFE-01 — D-01), empirically validate the highest-risk
assumption A1 (the PORT-03 4.3 rotated-region "verify-then-no-op" — proven
against a known-good, NOT assumed), commit ONLY the 4.3 triplet (D-05/Q2),
and produce the D-04 hard close gate: a documented local heavy/proprietary-rig
SAFE-02 byte-equal pass.

Purpose: D-01 makes the 4.3 evidence real-rig sampled (not smoke-only); A1 is
flagged as the single highest-risk assumption that MUST be empirically validated
before the no-op is trusted; D-04 makes the documented local heavy-rig SAFE-02
pass a hard phase-close gate (CI-subset-green alone does not close Phase 43 —
"subtle drift hides exactly there"). This plan closes all three.

Output: the captured 4.3 baseline + the wired sentinel + the A1 validation; the
4.3-only fixture commit (git-narrated); `43-VERIFICATION.md` with the D-04
record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-CONTEXT.md
@.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-RESEARCH.md
@.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-PATTERNS.md
@.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-VALIDATION.md

<interfaces>
<!-- This plan WIRES the Plan-01 seams (which currently skip) by capturing the
     4.3 baseline + driving the sample through runtime-43, and produces the
     verification record. The 4.3 load path = pickRuntime('4.3') + runtime-43
     parse (NOT loadSkeleton — D-02 hard-picks 4.2). -->

From tests/runtime43/load43.ts (Plan 01 — the shared 4.3 loader; now resolves since runtime-43 exists):
```typescript
export const FIXTURE_43: string;            // fixtures/SIMPLE_PROJECT_43/skeleton2.json
export function tryLoad43(): { rt: SpineRuntime; skeletonData: OpaqueSkeletonData } | null;
```

From src/core/sampler.ts (post Plan 03 — sampleSkeleton reads load.runtime):
```typescript
export function sampleSkeleton(load: LoadResult, opts?: SamplerOptions): SamplerOutput;
// LoadResult.runtime must be the 4.3 adapter for the 4.3 sample — construct a
// LoadResult-shaped object with runtime = pickRuntime('4.3'), skeletonData =
// rt.parseSkeleton(...), sourceDims/editorFps as the 4.3 fixture requires.
```

From tests/safe01/canonical-json.ts (reuse VERBATIM — D-01; do NOT re-derive):
```typescript
export function canonicalize(output: unknown, meta: { fixture: string }): string;
```

From tests/safe01/safe01-baseline.spec.ts:83-98 (the D-04 presence-guarded heavy arm — already exists):
```typescript
for (const d of heavy) {
  const file = path.join(BASE_DIR, baselineFileName(d.fixture));
  it.skipIf(!existsSync(file))(`heavy/local-only: ${d.fixture} matches its local (gitignored) baseline`, () => {
    expect(frozenPart(live)).toEqual(frozenPart(committed));   // strict byte-equal
  });
}
```

The 4.2 sibling (Phase-44 ORCL-01-reserved — DO NOT touch/commit this plan; D-05):
- `fixtures/SIMPLE_PROJECT_43/skeleton2_42.json` (+ `_42.atlas` + `_42.png`) — same-session same-hash `mFDzgNETPHo`, the A1 known-good geometry source (read-only) and the Phase-44 cross-runtime oracle pair.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the 4.3 own-baseline, wire the sentinel, and EMPIRICALLY validate the A1 rotated-region behavior against the 4.2-sibling known-good</name>
  <read_first>
    - tests/runtime43/runtime43-baseline.spec.ts + tests/runtime43/runtime43-d03.spec.ts (the Plan-01 seams to wire — they currently skip via tryLoad43()==null||!existsSync(BASE_43))
    - tests/runtime43/load43.ts (the shared 4.3 loader — now resolves since Plan 04 landed runtime-43)
    - tests/safe01/safe01-baseline.spec.ts:38-78 (the frozenPart + strict toEqual + canonicalize pattern to mirror for the SEPARATE 4.3 store)
    - tests/safe01/canonical-json.ts (the canonicalize signature — reuse verbatim; D-01, NOT re-derived)
    - src/core/sampler.ts (post Plan 03 — sampleSkeleton(load) reads load.runtime; construct a 4.3 LoadResult)
    - fixtures/SIMPLE_PROJECT_43/skeleton2.json + skeleton2.atlas (the 4.3 owner rig — `"spine":"4.3.01"`, the `transform` constraint CHAIN_8 on SQUARE, the rotate:90 TRIANGLE/rect regions)
    - fixtures/SIMPLE_PROJECT_43/skeleton2_42.json + skeleton2_42.atlas (READ-ONLY — the same-session same-hash 4.2 sibling = the A1 known-good geometry; do NOT commit/modify — D-05)
    - .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-RESEARCH.md §§ "PORT-03" (Approach A vs B), "Assumptions Log" A1 (highest risk — empirically validate, NOT assume), "Pitfall 1" (the SQUARE post-constraint canary), "Open Questions" Q2 (the 4.3-only commit / enumeration interaction)
    - .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-PATTERNS.md §§ "runtime43-baseline.spec.ts", "runtime43-d03.spec.ts" (the wiring shapes + the SEPARATE-store + D-05 constraints)
  </read_first>
  <action>
**Step A — capture the 4.3 own-baseline (SEPARATE store; D-01, NOT
golden-shared):**
Create `tests/runtime43/baselines/` (NEW directory — physically separate from
`tests/safe01/baselines/`; D-01 / RESEARCH § Anti-Patterns: never share the 4.2
goldens as the 4.3 expected output). Add a one-shot capture path INSIDE
`runtime43-baseline.spec.ts` (or a tiny sibling driver it imports) that:
1. `const { rt, skeletonData } = tryLoad43()!;` (resolves now — runtime-43
   exists; the fixture is present locally even though untracked).
2. Build a `LoadResult`-shaped object: `{ runtime: rt, skeletonData,
   sourceDims: <built from the 4.3 atlas regions>, editorFps: <from
   skeletonData / default 30> }` mirroring how the loader assembles it (read
   the 4.3 atlas via `rt.makeAtlas` for the regions; the sourceDims map keys
   are region names — reuse the loader's region-name resolution shape).
3. `const output = sampleSkeleton(load);` — MUST NOT throw (PORT-01: the 4.3
   rig samples via the verified Pose API).
4. `const json = canonicalize(output, { fixture: 'SIMPLE_PROJECT_43/
   skeleton2.json' });` then write `frozenPart(JSON.parse(json))` (the
   `{globalPeaks, perAnimation, setupPosePeaks}` shape; `_meta` excluded as
   volatile, exactly like SAFE-01) to
   `tests/runtime43/baselines/skeleton2.json`.
   This is the FRESHLY-CAPTURED 4.3 baseline (D-01) — captured in Phase 43,
   stored SEPARATE, NOT shared with SAFE-01.
Then make the spec's steady-state assertion strict: `expect(frozenPart(live)).
toEqual(frozenPart(committed))` against that file. This is a regression
SENTINEL, NOT the phase-stop gate (D-01 — only SAFE-02 is the HARD exit gate).

**Step B — EMPIRICALLY validate A1 (the highest-risk assumption — PORT-03
rotated-region; NOT a comment-only assumption):**
`skeleton2.atlas` has `rotate:90` regions (TRIANGLE + rect — verified in
RESEARCH). Plan 04 implemented `runtime-43.applyRotatedRegionFix` as the
Approach-A candidate (verify-then-no-op). This task PROVES or FALSIFIES it
against a known-good — do NOT assume parity. In `runtime43-baseline.spec.ts`
add an explicit `it('A1: 4.3 rotated-region world geometry matches the
4.2-sibling known-good (PORT-03 Approach-A empirical proof)')`:
- Known-good source: the 4.2-sibling `skeleton2_42.json` + `skeleton2_42.atlas`
  (same-session, same-hash `mFDzgNETPHo`, full-precision, same geometry — the
  RESEARCH-blessed known-good; READ-ONLY, NOT committed — D-05). Load it via
  `pickRuntime('4.2')` + the runtime-42 parse (mirror `load43.ts` but for the
  4.2 sibling and the 4.2 adapter — a local helper in this spec, NOT a new
  committed fixture path).
- For every `RegionAttachment` whose region has `rotate:90` (TRIANGLE, rect):
  compute the world-quad AABB on the 4.3 path (`rt43.regionWorldVertices`) and
  on the 4.2-sibling path (`rt42.regionWorldVertices`) at setup pose, and
  assert they agree within a SMALL tolerance (1e-4 — same-hash same-geometry
  rigs; this is the A1 ground truth, NOT the strict 4.2 SAFE-02 gate). If they
  AGREE → Approach A (the Plan-04 no-op) is empirically validated; record
  "A1 VALIDATED" in the SUMMARY + `43-VERIFICATION.md`.
- **Falsification fallback (Approach B):** if the rotated-region AABBs DIVERGE
  beyond 1e-4, Approach A is FALSIFIED. Then: change
  `runtime-43.applyRotatedRegionFix` from the no-op to Approach B —
  re-express Phase-33's correction by recomputing into
  `attachment.sequence.offsets[sequence.resolveIndex(setupPose)]` (the array
  `getOffsets` reads): `update()` the sequence first so `offsets` is allocated,
  then overwrite the 8 floats with the SAME corrected-offset math as
  `loader.ts:566-611` (the SWAP form), targeting `sequence.offsets[i]` instead
  of `attachment.offset[]` (RESEARCH § PORT-03 Approach B — the math is
  identical, only the write target changes). Re-run this A1 test; it MUST then
  pass within 1e-4. Record "A1 FALSIFIED → Approach B applied + re-validated"
  in the SUMMARY + `43-VERIFICATION.md`. (This is why the plan is `autonomous:
  false` — if B is needed, the Task-2 checkpoint surfaces the runtime-43 change
  for human review before close.)

**Step C — wire the D-03 positive canary (`runtime43-d03.spec.ts`):**
Replace the Plan-01 placeholder assertion with the real positive canary: from
the captured 4.3 sample, assert `SQUARE`'s `globalPeaks` record's `peakScale`
reflects the `transform` constraint (CHAIN_8 on SQUARE) — i.e. it is the
POST-constraint value, NOT the pre-constraint setup value. Use the
4.2-sibling's SQUARE peak (same-hash, same constraint, computed via the
already-byte-trusted 4.2 path) as the expected reference within 1e-4 (the 4.2
path is SAFE-02-frozen-trusted; if 4.3's SQUARE matches it, the appliedPose
read is correct — this is the empirical proof D-03 works, co-located with the
Plan-04 negative dev-assertion unit). This is the Pitfall-1 existential-
undersize backstop.

Do NOT commit the 4.2 sibling or add it to git (it is read-only known-good
here, Phase-44 ORCL-01-reserved — D-05). Do NOT touch `tests/safe01/**` or any
SAFE-01 baseline (D-09). Do NOT widen the SAFE-02 gate (Plan 03 owns it; it
stays strict). The 4.3 baseline + the A1/canary tolerances are 1e-4 (same-hash
cross-runtime geometry), distinct from SAFE-02's strict byte-equal — keep them
clearly labeled so they are never confused.
  </action>
  <verify>
    <automated>npx vitest run tests/runtime43/ tests/safe01/safe01-baseline.spec.ts tests/safe01/safe01-enumeration.spec.ts 2>&1 | grep -Eq "passed" && ! npx vitest run tests/runtime43/ tests/safe01/safe01-baseline.spec.ts tests/safe01/safe01-enumeration.spec.ts 2>&1 | grep -Eq "[1-9][0-9]* failed" && test -f tests/runtime43/baselines/skeleton2.json && echo "4.3 own-baseline captured + sentinel green + A1 validated + SAFE-02/enumeration still green"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/runtime43/baselines/skeleton2.json && grep -c "globalPeaks" tests/runtime43/baselines/skeleton2.json` returns at least `1` (the 4.3 baseline is captured, frozenPart shape)
    - `git rev-parse --show-toplevel >/dev/null && test ! -d tests/safe01/baselines/SIMPLE_PROJECT_43 && ! grep -rq "SIMPLE_PROJECT_43" tests/safe01/baselines/_manifest.json` — the 4.3 baseline is in the SEPARATE `tests/runtime43/baselines/` store, NOT under `tests/safe01/baselines/`, and NOT in the SAFE-01 manifest (D-01, NOT golden-shared)
    - `npx vitest run tests/runtime43/runtime43-baseline.spec.ts` PASSES with the A1 rotated-region test RUN (not skipped) and GREEN — the 4.3 `rotate:90` world geometry agrees with the 4.2-sibling known-good within 1e-4 (A1 empirically validated; or Approach B was applied and re-validated — the SUMMARY records which)
    - `npx vitest run tests/runtime43/runtime43-d03.spec.ts` PASSES with the SQUARE post-constraint canary RUN and GREEN (SQUARE's 4.3 peak == the 4.2-sibling's SQUARE peak within 1e-4 — D-03 empirical proof; the existential-undersize failure mode is backstopped)
    - `grep -c "A1\|Approach A\|Approach B\|rotated-region\|known-good\|4.2-sibling" tests/runtime43/runtime43-baseline.spec.ts` returns at least `1` (the A1 empirical validation is a real test, NOT a comment)
    - `npx vitest run tests/safe01/safe01-baseline.spec.ts tests/safe01/safe01-enumeration.spec.ts tests/safe01/safe01-freeze-guard.spec.ts` STILL PASSES (capturing the 4.3 baseline + the A1 test touched no SAFE-01 artifact; D-09)
    - `git status --porcelain tests/safe01/` is EMPTY (SAFE-01 corpus untouched — D-09)
    - If Approach B was needed: `grep -c "sequence.offsets\|Approach B" src/core/runtime/runtime-43.ts` returns at least `1` AND the SUMMARY explicitly records "A1 FALSIFIED → Approach B applied + re-validated" (otherwise the SUMMARY records "A1 VALIDATED — Approach-A no-op confirmed against the 4.2-sibling known-good")
  </acceptance_criteria>
  <done>The 4.3 own-baseline is captured in the SEPARATE tests/runtime43/baselines/ store (NOT golden-shared with SAFE-01); the A1 rotated-region behavior is EMPIRICALLY validated against the 4.2-sibling known-good within 1e-4 (Approach A confirmed, or Approach B applied + re-validated and the runtime-43 change recorded for review); the SQUARE post-constraint D-03 canary is green; SAFE-01/SAFE-02/enumeration/freeze-guard all stay green untouched.</done>
</task>


<task type="auto">
  <name>Task 2: Commit ONLY the 4.3 triplet (scripted, git-narrated for a beginner) + draft 43-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-RESEARCH.md § "Open Questions" Q2 (commit ONLY the 4.3 triplet; the 4.2 sibling stays untracked → SAFE-01 enumeration stays green automatically because the 4.3 file lands in discover()'s `excluded` arm)
    - 43-VALIDATION.md § "Manual-Only Verifications" (the exact D-04 record contract the verification doc must contain)
    - .planning/phases/42-pre-v1-6-4-2-baseline-npm-alias-boundary-scaffolding/42-CONTEXT.md D-08-R (the two-tier heavy-rig presence-guard the D-04 record references)
  </read_first>
  <action>
The git commit is fully scriptable — automate it (automation-first rule: Claude
stages exactly the three files and commits; the human only verifies the genuinely
non-automatable D-04 heavy-rig local run in Task 3). The user is a git beginner
([[user_git_experience]] / [[feedback_explain_git]]) — narrate EACH git command
in the SUMMARY in plain English so they can follow what happened.

Run these EXACT commands in order, capturing output for the SUMMARY narration:

1. `git status --porcelain fixtures/SIMPLE_PROJECT_43/` — observe SIX untracked
   (`??`) files: the three 4.3 (`skeleton2.json/.atlas/.png`) + the three 4.2
   sibling (`skeleton2_42.json/.atlas/.png`). (SUMMARY narration: "git status
   shows files git is not yet tracking; `??` = new/untracked.")
2. Stage ONLY the 4.3 triplet by EXPLICIT path (NEVER `git add
   fixtures/SIMPLE_PROJECT_43/` — that would also stage the Phase-44-reserved
   4.2 sibling and trip the SAFE-01 enumeration; D-05/Q2):
   `git add fixtures/SIMPLE_PROJECT_43/skeleton2.json fixtures/SIMPLE_PROJECT_43/skeleton2.atlas fixtures/SIMPLE_PROJECT_43/skeleton2.png`
   (SUMMARY narration: "`git add <file>` includes exactly that file in the next
   commit; naming the three explicitly is the safety mechanism.")
3. `git status --porcelain fixtures/SIMPLE_PROJECT_43/` — CONFIRM exactly three
   `A ` (staged) lines for the `skeleton2.*` files AND three still-`??` lines for
   the `skeleton2_42.*` sibling. If any `_42` shows `A `, run `git restore
   --staged fixtures/SIMPLE_PROJECT_43/skeleton2_42.json
   fixtures/SIMPLE_PROJECT_43/skeleton2_42.atlas
   fixtures/SIMPLE_PROJECT_43/skeleton2_42.png` and re-confirm. (SUMMARY
   narration: "`A ` = staged; `git restore --staged <file>` un-stages without
   deleting.")
4. `git commit -m "test(43): commit 4.3 owner rig SIMPLE_PROJECT_43 triplet (D-01/D-05; 4.2 sibling Phase-44-reserved, untracked)"`
   (SUMMARY narration: "`git commit` permanently records the staged files as one
   snapshot with that message.")
5. `git ls-files fixtures/SIMPLE_PROJECT_43/` — MUST list EXACTLY three files
   (`skeleton2.atlas`, `skeleton2.json`, `skeleton2.png`) and NO `_42` file.
   (SUMMARY narration: "`git ls-files` lists everything git now tracks under
   that path; exactly three = correct scope.")
6. `npx vitest run tests/safe01/safe01-enumeration.spec.ts tests/safe01/safe01-baseline.spec.ts`
   — BOTH must be GREEN with NO manifest edit (Q2: the 4.3 file is
   `"spine":"4.3.01"` → the 4.2-hardpicked loadSkeleton throws → discover()
   puts it in `excluded`, never `discovered`, so the manifest is unchanged; the
   untracked 4.2 sibling is never discovered). If `safe01-enumeration.spec.ts`
   FAILS, the 4.2 sibling leaked into the commit — `git rm --cached
   fixtures/SIMPLE_PROJECT_43/skeleton2_42.*` and re-run.

Then DRAFT `.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-VERIFICATION.md`
with a `## D-04 Heavy-Rig SAFE-02 Close Gate` section containing a TEMPLATE the
maintainer fills in Task 3: a date line; a per-rig pass/fail table for
`fixtures/Girl/`, `fixtures/SKINS/`, `fixtures/CHJ/`, `fixtures/3Queens/`,
`fixtures/Jokerman/` (status column left as `PENDING — fill from local run`); a
verbatim sentence "CI-subset-green is necessary but NOT sufficient; this
documented local heavy-rig SAFE-02 byte-equal pass is the D-04 hard close gate
(Phase 42 D-08 — subtle drift hides in complex rigs)."; and an `## A1
Rotated-Region Resolution` section pre-filled with the Task-1 outcome ("A1
VALIDATED — Approach-A no-op confirmed against the 4.2-sibling known-good" OR
"A1 FALSIFIED → Approach B applied + re-validated", read from the Task-1
result). Leave the D-04 table status cells PENDING for Task 3.

Do NOT git-add the 4.2 sibling. Do NOT touch any `tests/safe01/baselines/*` or
the manifest (D-09). Do NOT run the heavy-rig gate here (Task 3 owns it — the
heavy baselines are gitignored, present only on the maintainer machine).
  </action>
  <verify>
    <automated>test $(git ls-files fixtures/SIMPLE_PROJECT_43/ | wc -l | tr -d ' ') -eq 3 && ! git ls-files fixtures/SIMPLE_PROJECT_43/ | grep -q "_42" && npx vitest run tests/safe01/safe01-enumeration.spec.ts tests/safe01/safe01-baseline.spec.ts 2>&1 | grep -Eq "passed" && ! npx vitest run tests/safe01/safe01-enumeration.spec.ts 2>&1 | grep -Eq "[1-9][0-9]* failed" && test -f .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-VERIFICATION.md && echo "4.3-only triplet committed; SAFE-01 enumeration+baseline green; verification doc drafted"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files fixtures/SIMPLE_PROJECT_43/ | wc -l` returns `3` AND `git ls-files fixtures/SIMPLE_PROJECT_43/ | grep -c "_42"` returns `0` (EXACTLY the 4.3 triplet committed; the 4.2 sibling untracked — D-05/Q2)
    - `git log -1 --name-only --pretty=format: -- fixtures/SIMPLE_PROJECT_43/ | grep -c "_42"` returns `0` (the commit itself did not include any `_42` file)
    - `npx vitest run tests/safe01/safe01-enumeration.spec.ts` PASSES with NO change to `tests/safe01/baselines/_manifest.json` (`git status --porcelain tests/safe01/baselines/_manifest.json` is empty — Q2: the 4.3 file is `excluded`, never `discovered`)
    - `npx vitest run tests/safe01/safe01-baseline.spec.ts` PASSES (SAFE-02 still green after the fixture commit)
    - `git status --porcelain tests/safe01/baselines/` is EMPTY (no baseline regenerated — D-09)
    - `test -f .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-VERIFICATION.md` AND `grep -c "D-04 Heavy-Rig SAFE-02 Close Gate" .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-VERIFICATION.md` returns at least `1` AND `grep -c "A1 VALIDATED\|A1 FALSIFIED" .planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-VERIFICATION.md` returns at least `1` (the doc is drafted with the D-04 template + the A1 outcome from Task 1)
  </acceptance_criteria>
  <done>Exactly the 4.3 triplet is committed by explicit path (no `_42` sibling — D-05/Q2); SAFE-01 enumeration + baseline stay green with no manifest/baseline change (D-09); 43-VERIFICATION.md is drafted with the D-04 heavy-rig template (status PENDING for Task 3) and the A1 resolution recorded; every git step is narrated in the SUMMARY in plain English for the git-beginner user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-04 hard close gate — maintainer runs the local heavy/proprietary-rig SAFE-02 byte-equal pass and fills 43-VERIFICATION.md</name>
  <what-built>
Task 1 captured the 4.3 own-baseline + empirically validated A1 + wired the D-03
SQUARE canary (all automated, green). Task 2 committed ONLY the 4.3 triplet
(scripted, narrated) and drafted `43-VERIFICATION.md` with the D-04 heavy-rig
template (status cells PENDING) + the A1 outcome. The full redistributable-subset
suite is green: SAFE-02 (Plan 03), the RT-02 anchor (Plan 03), the 4.3
own-baseline + A1 + D-03 canary (Task 1), the 4.3-only commit + SAFE-01
enumeration (Task 2). The ONLY remaining item is inherently non-automatable: the
D-04 heavy/proprietary-rig SAFE-02 byte-equal pass. Those baselines
(`fixtures/Girl/`, `fixtures/SKINS/`, `fixtures/CHJ/`, `fixtures/3Queens/`,
`fixtures/Jokerman/`) are gitignored and present ONLY on the maintainer machine —
CI structurally cannot run them, and D-04 makes the documented local pass a HARD
phase-close gate ("subtle drift hides exactly there"; CI-subset-green is
necessary but NOT sufficient — Phase-42 D-08).
  </what-built>
  <how-to-verify>
On the maintainer machine, with the gitignored heavy/proprietary baselines
present locally:

1. Run the full SAFE-02 gate locally (the heavy
   `it.skipIf(!existsSync(file))` arm at `tests/safe01/safe01-baseline.spec.ts:
   83-98` RUNS — not skips — for every heavy rig whose local gitignored baseline
   is present, asserting strict byte-equality through the rewired adapter):
   `npx vitest run tests/safe01/safe01-baseline.spec.ts`

2. Confirm in the run output that the heavy rigs are RUN (not all skipped). You
   should see per-heavy-rig test lines (`heavy/local-only: Girl ...`, `... SKINS
   ...`, etc.) PASSING — not `skipped`. If they all show `skipped`, the
   gitignored baselines are not present locally; the gate cannot be satisfied
   from this machine and the phase cannot close (D-04).

3. Edit `.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-VERIFICATION.md`:
   fill the `## D-04 Heavy-Rig SAFE-02 Close Gate` table's status cells with the
   ACTUAL per-rig result (e.g. `Girl: byte-equal ✓`, `SKINS: byte-equal ✓`, ...
   for each rig that ran), the date, and the count (e.g. "5/5 heavy rigs
   byte-equal through the rewired adapter"). The verbatim "CI-subset-green is
   necessary but NOT sufficient ..." sentence drafted by Task 2 stays.

If ANY heavy rig is byte-DIFFERENT: the rewire leaked on a constraint-heavy
proprietary rig — exactly the D-04 failure mode. DO NOT approve. Report the
drifted rig + the divergent `${skin}/${slot}/${attachment}` records. The fix is
a non-byte-faithful leaf substitution in the Plan-03 rewire — NEVER a baseline
regen and NEVER a tolerance relaxation (SAFE-02 is strict; the 4.2.111 runtime
is unchanged so any diff is a plumbing bug).
  </how-to-verify>
  <resume-signal>Type "approved" once `npx vitest run tests/safe01/safe01-baseline.spec.ts` is fully green LOCALLY with the heavy/proprietary rigs RUN (not skipped) and `43-VERIFICATION.md`'s D-04 table is filled with the per-rig byte-equal results + date. If any heavy rig drifted, describe the rig + the divergent records instead (do NOT approve — it blocks phase close per D-04).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 4.3 own-baseline ↔ FROZEN SAFE-01 corpus | the 4.3 baseline MUST be a SEPARATE store, never golden-shared with the 4.2 SAFE-01 goldens (D-01) |
| committed 4.3 triplet ↔ Phase-44-reserved 4.2 sibling | only the 4.3 triplet is committed; the 4.2 sibling stays untracked or the SAFE-01 enumeration trips (D-05/Q2) |
| CI redistributable subset ↔ gitignored heavy/proprietary rigs | CI cannot prove byte-equality on the heavy rigs; the documented local pass is the D-04 hard close gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-14 | Information disclosure | the A1 rotated-region no-op being TRUSTED without empirical proof → undersized rotate:90 textures ship (the highest-risk assumption) | mitigate | Task 1 Step B is a real automated test diffing the 4.3 rotated-region world geometry against the same-hash 4.2-sibling known-good within 1e-4; Approach B is the documented falsification fallback; the outcome is recorded in 43-VERIFICATION.md |
| T-43-15 | Tampering | the 4.2 sibling accidentally committed → SAFE-01 enumeration trips OR the Phase-44 oracle pair is contaminated (D-05/Q2) | mitigate | The git steps name the three 4.3 files EXPLICITLY (never `git add <dir>`); a mandatory `git ls-files` sanity check + the `safe01-enumeration.spec.ts` green gate before the resume-signal |
| T-43-16 | Information disclosure | a constraint-heavy proprietary-rig plumbing drift shipping undetected because CI only runs the small redistributable subset (the D-04 failure mode) | mitigate | D-04 makes the documented LOCAL heavy-rig SAFE-02 byte-equal pass a HARD close gate recorded in 43-VERIFICATION.md; CI-subset-green is explicitly stated insufficient; a heavy-rig diff blocks close |
| T-43-17 | Tampering | the 4.3 own-baseline confused with / written into the frozen SAFE-01 store | accept | The baseline is written to the physically separate `tests/runtime43/baselines/`; acceptance asserts it is absent from `tests/safe01/baselines/` and the SAFE-01 manifest; `git status --porcelain tests/safe01/` must be empty (D-09 freeze remains the enforcement) |
</threat_model>

<verification>
- `npx vitest run tests/runtime43/runtime43-baseline.spec.ts` — GREEN with the A1 rotated-region test RUN (4.3 vs 4.2-sibling within 1e-4; Approach A confirmed or B applied+re-validated).
- `npx vitest run tests/runtime43/runtime43-d03.spec.ts` — GREEN with the SQUARE post-constraint canary RUN (D-03 empirical proof).
- `git ls-files fixtures/SIMPLE_PROJECT_43/` — EXACTLY three files (`skeleton2.json/.atlas/.png`); NO `_42` sibling (D-05/Q2).
- `npx vitest run tests/safe01/safe01-enumeration.spec.ts tests/safe01/safe01-baseline.spec.ts tests/safe01/safe01-freeze-guard.spec.ts` — GREEN with NO manifest edit and NO baseline regen (D-09 / Q2).
- `43-VERIFICATION.md` — records the D-04 documented local heavy-rig SAFE-02 byte-equal pass (the hard close gate) + the A1 outcome.
- `tests/runtime43/baselines/skeleton2.json` exists and is absent from `tests/safe01/baselines/` + the SAFE-01 manifest (D-01, NOT golden-shared).
</verification>

<success_criteria>
- The 4.3 owner rig samples through `runtime-43` without throw and is byte-stable vs its OWN freshly-captured 4.3 baseline in the SEPARATE store (D-01 — NOT golden-shared, a sentinel not the phase-stop gate).
- A1 (the highest-risk assumption) is EMPIRICALLY validated against the 4.2-sibling known-good within 1e-4 — Approach-A no-op confirmed, or Approach B applied + re-validated, with the resolution recorded.
- The TransformConstraint-on-SQUARE D-03 canary proves the post-constraint (appliedPose) read empirically.
- ONLY the 4.3 triplet is committed (git-narrated for a beginner); the 4.2 sibling stays untracked; SAFE-01 enumeration + baseline + freeze-guard stay green automatically with no manifest/baseline change (D-05/Q2/D-09).
- The D-04 documented local heavy/proprietary-rig SAFE-02 byte-equal pass is recorded in `43-VERIFICATION.md` — the hard phase-close gate (CI-subset-green alone does not close).
</success_criteria>

<output>
After completion, create `.planning/phases/43-runtime-adapter-facade-verified-4-3-api-mapping/43-05-SUMMARY.md`
</output>
