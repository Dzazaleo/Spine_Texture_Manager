---
phase: 39
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md
autonomous: false
requirements: [WINUAT-01, WINUAT-02, WINUAT-03]
must_haves:
  truths:
    - "A single contract document at `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md` records the host-availability decision for this v1.5 cycle."
    - "The contract explicitly documents that `human_needed` is a first-class non-failure outcome for Phase 39 per ROADMAP Success Criterion 4 + v1.5 Locked Constraints in STATE.md."
    - "The contract documents that WINUAT-01 and WINUAT-02 outcomes are INDEPENDENT — one can pass while the other defers to v1.6+."
    - "The contract names the two pending todos and the two UAT files it gates, and records the exact path each is expected to live at (including the Phase 20 file's deleted state and the Phase 31 file's archived location)."
    - "The contract is committed as a single atomic commit `docs(39): host-availability decision + graceful-degradation contract`."
  artifacts:
    - path: ".planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md"
      provides: "Host-availability decision + graceful-degradation contract; gates plans 39-02 and 39-03 with a single recorded decision (host_available: yes | no | partial)."
      contains: "host_available:"
      min_lines: 40
  key_links:
    - from: ".planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md"
      to: ".planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md"
      via: "named-reference path in the contract body"
      pattern: "2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat\\.md"
    - from: ".planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md"
      to: ".planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md"
      via: "named-reference path in the contract body"
      pattern: "2026-05-08-phase-31-windows-admin-dnd-release-uat\\.md"
    - from: ".planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md"
      to: ".planning/STATE.md `v1.5 Locked Constraints` — WINUAT-01..03 graceful degradation"
      via: "verbatim reference paragraph quoting the locked constraint"
      pattern: "first-class non-failure"
---

<objective>
Produce `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md` — a single decision-gate document that records, for the current v1.5 cycle:

  (a) Whether a real Windows host with an installed v1.x release binary is available right now,
  (b) The graceful-degradation contract that governs Phase 39 closure (`human_needed` is first-class non-failure per ROADMAP Phase 39 Success Criterion 4 + STATE.md `v1.5 Locked Constraints`),
  (c) The two UAT files and two pending-todo files Phase 39 touches, including the fact that the Phase 20 UAT file was deleted in commit `0787fe1` (v1.2 cleanup) and the Phase 31 UAT file lives in the v1.3.1 archive, NOT at the path referenced in ROADMAP / REQUIREMENTS,
  (d) The independence invariant — WINUAT-01 (DocBuilder DnD) and WINUAT-02 (admin DnD) outcomes are independent; one can pass while the other defers to v1.6+.

Purpose: Plans 39-02 and 39-03 are dual-mode — live execution if a Windows host is available, structured deferral if not. Both modes need the same up-front decision and the same path-correctness facts. Capturing them once in 39-CONTRACT.md avoids duplicating the logic in two plans and gives the executor + reviewer a single audit-trail anchor.

Output: One new markdown file. No source code change. No dependency change. No new attack surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
@.planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
@.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md
@CLAUDE.md

<facts>
<!-- Pre-discovered file-system facts the executor needs but should NOT have to re-derive. -->

**Phase 20 HUMAN-UAT file status:** DELETED. The file lived at `.planning/phases/20-documentation-builder-feature/20-HUMAN-UAT.md` from 2026-05-01 (commit `1095afa`) through 2026-05-03, was last updated in commit `d21d26e` (test #3 set to `deferred`), and was DELETED in commit `0787fe1` (`chore(planning): cleanup v1.1.2/v1.2 plan artifacts + v1.3.1 housekeeping`). The v1.2 archive at `.planning/milestones/v1.2-phases/20-documentation-builder-feature/` contains only the four `20-NN-SUMMARY.md` files; it does NOT contain a HUMAN-UAT.md copy. The pending todo `2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md` is therefore the ONLY surviving audit-trail anchor for WINUAT-01 outcome recording. Plan 39-02 will operate against the todo file directly; the deleted UAT file does NOT need to be restored (it has no consumer beyond the todo, and the todo already carries the test-3 scenario verbatim).

**Phase 31 HUMAN-UAT file status:** EXISTS at the archived path `.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md` (NOT at `.planning/phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md` as referenced in ROADMAP Phase 39 Success Criterion 2 and REQUIREMENTS WINUAT-02 — Phase 31 was archived to `v1.3.1-phases/` during v1.3.1 milestone close). The file's frontmatter has `status: partial`; item 1 has `result: deferred — will exercise at v1.3.1 release time...`. Plan 39-03 must use the archived path for all edits.

**Pending todos:**
- `.planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md` (frontmatter `resolves_phase: 39` — already wired to this phase)
- `.planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md` (frontmatter `resolves_phase: 39` — already wired)

**Shipped binary candidates the user might run for the live UAT:**
- WINUAT-01 source todo mentions `release/Spine Texture Manager-1.1.3-x64.exe` (v1.1.3 NSIS installer, built 2026-05-01). Any later v1.x release that includes the same DocBuilder dialog code path is acceptable; the DocBuilder feature shipped in Phase 20 (v1.2.x) so v1.2+ is the floor. Latest tagged release at time of v1.5 planning is v1.3.x; v1.4.0 is `pending push` per STATE.md.
- WINUAT-02 source todo specifies a `v1.3.1` installer or later (PLATFORM-01 advisory shipped in Phase 31 / v1.3.1).
- A v1.3.x or v1.4.x installer satisfies BOTH advisories (DocBuilder dialog code unchanged since Phase 20; admin-DnD advisory unchanged since Phase 31). User can run one installer and exercise both UATs.
</facts>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Record host-availability decision (`host_available: yes | no | partial`)</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 39 section + the four success criteria — confirm the executor + user share the same understanding of the four success criteria before writing the contract)
    - .planning/REQUIREMENTS.md (lines 41-48 — WINUAT-01..03 verbatim wording)
    - .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md (the test scenario + what "executed" requires)
    - .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md (the test scenario + verbatim advisory copy)
    - .planning/STATE.md (lines 86-92 — `v1.5 Locked Constraints` block, specifically the WINUAT-01..03 graceful-degradation lock)
  </read_first>
  <decision>Is a real Windows host with an installed v1.x release binary (v1.3.x or v1.4.x recommended; v1.1.3 minimum for WINUAT-01, v1.3.1 minimum for WINUAT-02) available to the user RIGHT NOW for live UAT execution in this v1.5 cycle?</decision>
  <context>
This decision gates plans 39-02 and 39-03 — they each have two operating modes (LIVE-EXECUTION vs STRUCTURED-DEFERRAL) and need to know which mode applies before the executor proceeds. The decision can be partial (e.g. Win host available but no v1.3.1+ installer — only WINUAT-01 executable; WINUAT-02 must defer). The decision is RECORDED in the contract document Task 2 writes; this checkpoint just captures the user's answer.

Why this is `checkpoint:decision` not `checkpoint:human-verify`: the executor cannot infer host availability from the file system or git state — only the user knows what hardware is in front of them right now.

Why this gates the wave: 39-02 and 39-03 must each open the contract document and read `host_available:` before deciding which mode to execute. Without a recorded decision, 39-02 + 39-03 would have to ask the user again — duplicating the prompt and risking inconsistent answers across the two plans.
  </context>
  <options>
    <option id="yes">
      <name>yes — Windows host + v1.3.1+ installer available; both WINUAT-01 and WINUAT-02 can execute live this cycle</name>
      <pros>Closes WINUAT-01..03 outright; resolves both long-lived todos to `resolved/` with closing notes; phase ships clean.</pros>
      <cons>Requires hands-on time at a Windows machine; can't be done in this Claude session — user must report observed behavior back.</cons>
    </option>
    <option id="partial-winuat01-only">
      <name>partial — Windows host available with a v1.1.3+ installer but NO v1.3.1+ installer; only WINUAT-01 (DocBuilder DnD) executable, WINUAT-02 (admin DnD advisory) defers</name>
      <pros>Closes WINUAT-01 now; WINUAT-02 carry-forward note records the specific gap (need v1.3.1+ binary).</pros>
      <cons>WINUAT-02 stays pending; WINUAT-03 partial close (one todo to resolved/, one stays in pending/ with updated note).</cons>
    </option>
    <option id="partial-winuat02-only">
      <name>partial — Windows host available with v1.3.1+ installer AND admin session, but the DocBuilder UAT is impractical right now (e.g. no time for full project-load flow); only WINUAT-02 executable, WINUAT-01 defers</name>
      <pros>Closes WINUAT-02 + retires the older Phase 31 todo. The Phase 31 advisory is the higher-value UAT (structural elevation safety) — closing it first has higher signal value than the cosmetic DnD-thumbnail UAT.</pros>
      <cons>WINUAT-01 stays pending; WINUAT-03 partial close.</cons>
    </option>
    <option id="no">
      <name>no — no Windows host available this cycle; both UATs defer with audit-trail intact; phase outcome is `human_needed`</name>
      <pros>First-class non-failure per ROADMAP Success Criterion 4 + STATE.md v1.5 Locked Constraint. Phase closes cleanly with both pending todos updated to carry forward to v1.6+.</pros>
      <cons>Two long-lived todos extend further; closure depends on future cycle host availability.</cons>
    </option>
  </options>
  <resume-signal>Select: yes, partial-winuat01-only, partial-winuat02-only, or no</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`</name>
  <files>.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md</files>
  <read_first>
    - .planning/phases/39-windows-host-blocked-uat-burndown/39-01-PLAN.md (THIS plan, especially the `<facts>` block in `<context>` — re-read so the verbatim file-status facts make it into the contract document, not just the plan)
    - .planning/ROADMAP.md Phase 39 section (lines 80-90 — the four Success Criteria the contract reflects)
    - .planning/STATE.md lines 86-92 (the `v1.5 Locked Constraints` block — paste the WINUAT-01..03 graceful-degradation paragraph verbatim into the contract)
    - Task 1 decision outcome (recorded in this session) — drives the `host_available:` frontmatter value and the Live-Execution-Mode vs Structured-Deferral-Mode rules section
  </read_first>
  <action>
Create `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`. The file must include EXACTLY the four sections below in this order. All four sections are mandatory regardless of Task 1 outcome (the contract is the SAME document whether host_available is yes/partial/no; only the frontmatter value + the "what 39-02 + 39-03 do" paragraphs differ).

Frontmatter must include:

Use a standard YAML frontmatter block (triple-dash fence at top and bottom), with these exact keys:

| Key | Type | Value |
|-----|------|-------|
| `phase` | int | `39` |
| `title` | string | `Phase 39 host-availability decision + graceful-degradation contract` |
| `recorded_on` | date | `2026-05-13` |
| `host_available` | enum | one of `yes`, `partial-winuat01-only`, `partial-winuat02-only`, `no` (from Task 1 outcome) |
| `winuat_01_mode` | enum | `live` or `deferred` (derived from host_available per the matrix below) |
| `winuat_02_mode` | enum | `live` or `deferred` (derived from host_available per the matrix below) |
| `requirements` | inline-array | `[WINUAT-01, WINUAT-02, WINUAT-03]` |

(The YAML fence delimiter is the standard three-hyphen line that opens and closes any GSD frontmatter block; see for example the top of this PLAN file or any phase 38 PLAN/SUMMARY.)

Derivation rules for the two mode fields (record these exactly):
- `host_available: yes` → both modes `live`
- `host_available: partial-winuat01-only` → `winuat_01_mode: live`, `winuat_02_mode: deferred`
- `host_available: partial-winuat02-only` → `winuat_01_mode: deferred`, `winuat_02_mode: live`
- `host_available: no` → both modes `deferred`

**Section 1 — Decision recorded.**

Header: `## 1. Decision recorded`. Body restates the host-availability decision in 2-3 sentences, citing the Task 1 outcome value verbatim. Include the date `2026-05-13` and the v1.5 cycle reference.

If `host_available` is `yes` or `partial-winuat01-only` or `partial-winuat02-only`, the body MUST also record: (a) the specific Windows host description (Windows version + machine descriptor as reported by the user — if user did not volunteer specifics, write "Windows host details to be appended to outcome stanzas in 39-02 + 39-03 at live-execution time"), (b) the v1.x installer version available (e.g. "v1.3.1 NSIS installer, built YYYY-MM-DD" — placeholder if not yet specified).

If `host_available` is `no`, the body MUST record the carry-forward target: "WINUAT-01..03 carry forward to v1.6+; pending todos remain in `.planning/todos/pending/` with an updated carry-forward note dated 2026-05-13".

**Section 2 — Graceful-degradation contract (verbatim from STATE.md `v1.5 Locked Constraints`).**

Header: `## 2. Graceful-degradation contract`. Body MUST include this verbatim block (already locked in STATE.md lines 90-92):

```
WINUAT-01..03 host-blocked graceful degradation: Phase 39 human_needed outcome
is first-class non-failure; todos carry forward to v1.6+ if no Win host available
this cycle. Closure gates on programmatic verification + audit-trail integrity,
NOT on host availability.
```

Then add three explicit clarification bullets (NOT a paraphrase — these are operational rules 39-02 and 39-03 read directly):

- "**Independence invariant.** WINUAT-01 (Phase 20 DocBuilder DnD) and WINUAT-02 (Phase 31 admin DnD advisory) outcomes are INDEPENDENT. One can flip to `passed` / `failed` while the other defers as `human_needed`. The four-way partial-close matrix is reflected by `winuat_01_mode` + `winuat_02_mode` frontmatter fields."
- "**Outcome vocabulary.** Each UAT executes to exactly one of `passed`, `failed`, or `human_needed`. `human_needed` is NOT a failure — it records that the test cannot be exercised this cycle. The phase verifier MUST treat `human_needed` as a non-failure outcome per ROADMAP Phase 39 Success Criterion 4."
- "**Todo lifecycle.** A todo file moves from `pending/` to `resolved/` ONLY when its corresponding UAT executes to `passed` or `failed`. If the corresponding UAT is `human_needed`, the todo stays in `pending/` with an appended carry-forward note dated 2026-05-13 (or later) referencing Phase 39 and the carry-forward milestone (v1.6+)."

**Section 3 — File-correctness facts (pre-discovered).**

Header: `## 3. File-correctness facts (read before editing UAT or todo files)`. Body MUST include exactly these two subsections verbatim — they correct the path inaccuracies in ROADMAP Phase 39 Success Criterion 1 + REQUIREMENTS WINUAT-01..02:

```
### 3.1 Phase 20 HUMAN-UAT file: DELETED — todo is the audit-trail anchor

The file `.planning/phases/20-documentation-builder-feature/20-HUMAN-UAT.md` no
longer exists. It was created in commit 1095afa (2026-05-01), last updated in
commit d21d26e (test #3 set to `deferred`), and DELETED in commit 0787fe1
(`chore(planning): cleanup v1.1.2/v1.2 plan artifacts + v1.3.1 housekeeping`).

The v1.2 archive at `.planning/milestones/v1.2-phases/20-documentation-builder-feature/`
contains only `20-01-SUMMARY.md` .. `20-04-SUMMARY.md` — no UAT.

Implication for plan 39-02: WINUAT-01 outcome recording happens IN-PLACE in
`.planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
(or its `resolved/` counterpart after move), NOT in a UAT markdown file. The
todo body already carries the test-3 scenario verbatim. Do NOT restore the
deleted UAT file — it has no surviving consumer.

### 3.2 Phase 31 HUMAN-UAT file: ARCHIVED location (NOT the ROADMAP path)

ROADMAP Phase 39 Success Criterion 2 and REQUIREMENTS WINUAT-02 both reference
`.planning/phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`. That path
does NOT exist. The file lives at:

  .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md

Phase 31 was archived to `v1.3.1-phases/` during v1.3.1 milestone close. Plan
39-03 must edit the archived path for the test-1 result flip + summary table
update.
```

**Section 4 — Plan 39-02 / 39-03 routing matrix.**

Header: `## 4. Plan routing matrix`. Body MUST include this verbatim table (drives 39-02 + 39-03 task selection):

```
| host_available value         | winuat_01_mode | winuat_02_mode | 39-02 task | 39-03 task |
|------------------------------|----------------|----------------|------------|------------|
| yes                          | live           | live           | LIVE       | LIVE       |
| partial-winuat01-only        | live           | deferred       | LIVE       | DEFER      |
| partial-winuat02-only        | deferred       | live           | DEFER      | LIVE       |
| no                           | deferred       | deferred       | DEFER      | DEFER      |
```

Then add this rule paragraph verbatim:

```
Plan 39-02 reads `winuat_01_mode` from this contract's frontmatter and chooses
LIVE or DEFER. Plan 39-03 reads `winuat_02_mode` for WINUAT-02 routing and
ALSO reads `winuat_01_mode` (because WINUAT-03 — todo cleanup — must move
the Phase 20 todo to resolved/ only if WINUAT-01 closed live, otherwise carry
it forward in pending/). The 39-03 plan thus has FOUR concrete paths through
its task tree, one per row of the matrix.
```

Commit the new file in a single atomic commit:

```
git add .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md
git commit -m "docs(39): host-availability decision + graceful-degradation contract"
```

Plain-English note for the git-beginner workflow per memory `feedback_explain_git`: this is a single new file, no rename, no deletions, no source code change. `git add <path>` stages the new file; `git commit -m "..."` records it. Nothing destructive. `git status` after the commit should be clean.
  </action>
  <verify>
    <automated>test -s .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q '^host_available:' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q '^winuat_01_mode:' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q '^winuat_02_mode:' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q 'first-class non-failure' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q 'Independence invariant' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q 'DELETED' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q '0787fe1' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q 'milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q '2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat\.md' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q '2026-05-08-phase-31-windows-admin-dnd-release-uat\.md' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q 'Plan routing matrix' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && git log -1 --pretty=%s | grep -q '^docs(39): host-availability decision'</automated>
  </verify>
  <acceptance_criteria>
    - File exists and non-empty: `test -s .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Frontmatter `host_available:` present: `grep -q '^host_available:' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Frontmatter `host_available:` value is one of yes / partial-winuat01-only / partial-winuat02-only / no: `grep -qE '^host_available: (yes|partial-winuat01-only|partial-winuat02-only|no)$' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Frontmatter `winuat_01_mode:` value is live OR deferred: `grep -qE '^winuat_01_mode: (live|deferred)$' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Frontmatter `winuat_02_mode:` value is live OR deferred: `grep -qE '^winuat_02_mode: (live|deferred)$' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - All three requirement IDs appear in frontmatter: `grep -q 'requirements: \[WINUAT-01, WINUAT-02, WINUAT-03\]' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Graceful-degradation verbatim block present (key phrase): `grep -q 'first-class non-failure' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Independence invariant bullet present: `grep -q 'Independence invariant' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Outcome vocabulary bullet present: `grep -q 'Outcome vocabulary' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Todo lifecycle bullet present: `grep -q 'Todo lifecycle' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Phase 20 UAT deletion fact recorded: `grep -q 'DELETED' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q '0787fe1' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Phase 31 UAT archived path recorded: `grep -q 'milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Both pending todos named in body: `grep -q '2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat\.md' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md && grep -q '2026-05-08-phase-31-windows-admin-dnd-release-uat\.md' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Plan routing matrix section present: `grep -q '## 4. Plan routing matrix' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`
    - Routing matrix contains all 4 rows: `grep -c '^| ' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md | awk '{ if ($1 < 5) exit 1 }'` (header + separator + 4 data rows minimum)
    - Atomic commit message: `git log -1 --pretty=%s | grep -q '^docs(39): host-availability decision'`
    - No source files in `src/` or `tests/` modified: `git diff HEAD~1..HEAD -- src/ tests/ | wc -l | awk '{ if ($1 != 0) exit 1 }'`
  </acceptance_criteria>
  <done>
The contract file exists at `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md` with valid frontmatter (host_available + winuat_01_mode + winuat_02_mode + requirements), all four numbered sections present, graceful-degradation verbatim block intact, independence + outcome + todo-lifecycle bullets present, Phase 20 deletion fact + Phase 31 archived path both recorded, plan routing matrix complete with 4 data rows, atomic commit `docs(39): host-availability decision + graceful-degradation contract` in git log, no source files touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only a single new planning markdown file. No code, no IPC, no UI, no user-facing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | (n/a) | (n/a) | accept | No new attack surface — phase 39 modifies only planning markdown and existing UAT outcome records. No code, no dependencies, no user-facing UI built. Audit-trail integrity is the only relevant security property and is enforced by structured outcome recording in 39-CONTRACT.md (Section 2 — Graceful-degradation contract) plus per-plan grep gates in `<acceptance_criteria>` blocks. |
</threat_model>

<verification>
After Task 2 commit:

```bash
# Contract document exists with valid frontmatter
test -s .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md

# All four numbered sections present
for section in '## 1. Decision recorded' '## 2. Graceful-degradation contract' '## 3. File-correctness facts' '## 4. Plan routing matrix'; do
  grep -qF "$section" .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md || echo "MISSING: $section"
done

# Atomic commit
git log -1 --pretty=%s
```

No source files modified (sanity): `git diff HEAD~1..HEAD -- src/ tests/` should be empty.
</verification>

<success_criteria>
- The host-availability decision is RECORDED (not just remembered) at `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md` with a valid `host_available:` frontmatter value from the four-way matrix.
- The `winuat_01_mode` + `winuat_02_mode` frontmatter fields are derived consistently with the matrix rules in the body.
- The contract documents the graceful-degradation invariant (verbatim from STATE.md), the independence invariant (WINUAT-01 and WINUAT-02 outcomes independent), and the todo-lifecycle rule (move to resolved/ only on passed/failed; stay in pending/ with carry-forward note on human_needed).
- The contract corrects two path inaccuracies that exist in the ROADMAP + REQUIREMENTS (Phase 20 UAT deleted; Phase 31 UAT lives in v1.3.1 archive) — this saves 39-02 + 39-03 from rediscovering them.
- Atomic commit `docs(39): host-availability decision + graceful-degradation contract` in git log.
- WINUAT-01, WINUAT-02, WINUAT-03 all referenced in the contract body (the three REQs Phase 39 addresses are jointly gated by this decision).
- No source files in `src/` or `tests/` modified.
</success_criteria>

<output>
After completion, create `.planning/phases/39-windows-host-blocked-uat-burndown/39-01-SUMMARY.md`.
</output>
