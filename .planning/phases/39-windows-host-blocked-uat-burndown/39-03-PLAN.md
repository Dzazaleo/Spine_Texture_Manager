---
phase: 39
plan: 03
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md
  - .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
  - .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
autonomous: false
requirements: [WINUAT-02, WINUAT-03]
must_haves:
  truths:
    - "WINUAT-02 has a recorded outcome (`passed`, `failed`, or `human_needed`) — no longer in an unresolved `deferred` state."
    - "If WINUAT-02 outcome is `passed` or `failed`: item 1 in `.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md` has its `result:` line updated from `deferred — will exercise at v1.3.1 release time…` to `passed (2026-05-13) — Phase 39 WINUAT-02 …` or `failed (2026-05-13) — Phase 39 WINUAT-02 …`, AND the Summary table is updated to reflect the flip (`passed: 4` + `pending: 0`, or `failed: 1` + `passed: 3` + `pending: 0`)."
    - "If WINUAT-02 outcome is `human_needed`: the 31-HUMAN-UAT.md item-1 `result:` line is updated to `human_needed (2026-05-13) — Phase 39 carry-forward to v1.6+` AND the Summary table updates `pending: 1` → `pending: 0` + adds `human_needed: 1` (or equivalent — the existing summary block ends with `Gaps` and no `human_needed` field, so the executor adds a new line)."
    - "Phase 31 todo (`.planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md`) moves to `resolved/` with a `## Resolved` close-out (if WINUAT-02 closed live), OR stays in `pending/` with a `## Carry-forward note` (if WINUAT-02 deferred)."
    - "WINUAT-03 is satisfied: both pending todos (Phase 20 + Phase 31) are accounted for — either moved to resolved/ with close-out OR kept in pending/ with a dated 2026-05-13 carry-forward note. Plan 39-02 handles Phase 20; this plan handles Phase 31."
    - "A single atomic commit captures this plan's file changes with message `chore(39): WINUAT-02 — <outcome> — Phase 31 admin DnD advisory on Windows`."
  artifacts:
    - path: ".planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md"
      provides: "WINUAT-02 outcome recorded in the existing Phase 31 UAT scenario script (the archived path — see 39-CONTRACT.md §3.2)"
      contains: "Phase 39"
    - path: "(LIVE) .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md OR (DEFER) .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md"
      provides: "Phase 31 todo audit-trail anchor — closed or carried forward"
      contains: "Phase 39"
  key_links:
    - from: ".planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md item 1 result line"
      to: "Phase 39 outcome (`passed` / `failed` / `human_needed`)"
      via: "result: line update with date + WINUAT-02 reference"
      pattern: "Phase 39"
    - from: "Phase 31 todo (resolved or pending)"
      to: "39-CONTRACT.md"
      via: "outcome paragraph cites the contract decision"
      pattern: "39-CONTRACT\\.md"
    - from: "Phase 31 todo (resolved or pending)"
      to: "WINUAT-02 + WINUAT-03 requirements"
      via: "verbatim REQ-ID references"
      pattern: "WINUAT-02"
---

<objective>
Close out WINUAT-02 (Phase 31 Windows admin DnD release UAT — verbatim advisory rendering, drag-over ring suppression, File → Open still works, relaunch-normally restores DnD) AND finalize WINUAT-03 for the Phase 31 todo per the host-availability decision recorded in `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`.

Two execution modes — the executor chooses based on the contract's `winuat_02_mode` frontmatter value:

  - **LIVE mode** (`winuat_02_mode: live`): user right-clicks the v1.3.1+ installed binary → Run as administrator → confirms UAC → exercises the four observation points (verbatim advisory copy, drag-over ring suppression, File → Open path, normal-relaunch DnD restoration). Executor flips item 1's `result:` line in `31-HUMAN-UAT.md` (archived path) from `deferred…` to `passed (2026-05-13)…` or `failed (2026-05-13)…`, updates the Summary table, and moves the Phase 31 todo from `pending/` to `resolved/` with a `## Resolved` close-out.

  - **DEFER mode** (`winuat_02_mode: deferred`): UAT not exercised this cycle. Executor flips item 1's `result:` line to `human_needed (2026-05-13) — Phase 39 carry-forward to v1.6+`, updates the Summary table (subtracts 1 from `pending:`, adds `human_needed: 1`), and appends a `## Carry-forward note` to the pending todo IN-PLACE (file stays in `pending/`).

Both modes also satisfy WINUAT-03 for the Phase 31 side. (The Phase 20 side of WINUAT-03 is owned by plan 39-02.)

Purpose: WINUAT-02 has been deferred since 2026-05-08 ("release-time UAT" that never happened because v1.3.1 + later releases shipped without a Win-admin session at release time). Phase 39 either retires it or formally records the v1.5 carry-forward.

Output: One UAT file edit (`31-HUMAN-UAT.md` archived path) + one todo file change (move OR append). Single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md
@.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md
@.planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
@.planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<facts>
**WINUAT-02 verbatim scenario from REQUIREMENTS.md line 46 (the source of truth):**

> Phase 31 Windows admin DnD release UAT — on a real Windows host with the installed v1.x release binary, right-click → "Run as administrator" → confirm UAC → with idle DropZone, verbatim two-sentence advisory renders (`"Drag-and-drop is unavailable while running as administrator. Use File → Open instead, or relaunch the app without administrator privileges."`) → drag-over ring does NOT appear → File → Open still works → relaunch normally → DnD works as before. Flip item 1 in `31-HUMAN-UAT.md` from `deferred` to `passed`/`failed`.

**Path correction (from 39-CONTRACT.md Section 3.2):** `31-HUMAN-UAT.md` is at the ARCHIVED path `.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`, NOT `.planning/phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md` (which does NOT exist — Phase 31 was archived to v1.3.1-phases/ during v1.3.1 milestone close).

**Verbatim advisory copy the user must see at the idle DropZone (the WINUAT-02 acceptance test — copy-paste this exactly when prompting the user):**

> Drag-and-drop is unavailable while running as administrator. Use File → Open instead, or relaunch the app without administrator privileges.

Source: `src/main/elevation.ts` advisory string (PLATFORM-01 shipped in Phase 31 / v1.3.1). The advisory is rendered by the renderer's DropZone empty-state when `process.elevation === true` via the elevation probe IPC. Programmatic verification at v1.3.1 close confirmed the string is wired correctly in 5/5 must-haves; only the LIVE Windows admin session was untested.

**Current state of `31-HUMAN-UAT.md` item 1 (archived path):**

```
### 1. Windows-admin DnD advisory live UAT
expected: On a real Windows machine, launching the app via 'Run as administrator' produces an idle DropZone showing the verbatim two-sentence advisory. Dragging a `.json` file over the window does NOT toggle the drag-over ring and does NOT load anything. macOS + Linux DnD remain functional.
result: deferred — will exercise at v1.3.1 release time when an installed binary + admin session can be obtained. Tracked as a pending todo so it surfaces in `/gsd-progress` and `/gsd-audit-uat`.
```

Summary block (file lines 31-39):

```
## Summary

total: 4
passed: 3
issues: 0
pending: 1
skipped: 0
blocked: 0
```

After LIVE pass: `passed: 4`, `pending: 0`. After LIVE fail: `passed: 3`, `failed: 1`, `pending: 0` (executor adds `failed:` line). After DEFER: `passed: 3`, `pending: 0`, `human_needed: 1` (executor adds `human_needed:` line).

The four observation points the user must report back (LIVE mode):
1. **Advisory copy matches verbatim** — the two-sentence advisory at the idle DropZone reads exactly `"Drag-and-drop is unavailable while running as administrator. Use File → Open instead, or relaunch the app without administrator privileges."` (yes/no — typo, missing arrow character, different phrasing all count as no).
2. **Drag-over ring does NOT appear** — dragging a `.json` file over the window does not trigger the `ring-2 ring-accent bg-accent/5` visual feedback (yes/no — yes means suppression works).
3. **File → Open still works** — clicking File → Open and selecting a `.json` file loads the project normally (yes/no).
4. **Relaunch without admin restores DnD** — quit the app, relaunch from the Start menu normally (NOT "Run as administrator"); drag-drop a `.json` file; project loads via DnD (yes/no — confirms the elevation probe correctly short-circuits when not elevated).

**macOS + Linux sanity item (REQUIREMENTS line 46 mentions "macOS + Linux DnD remain functional" — Linux is dropped per memory `project_linux_deferred`; macOS sanity is OPTIONAL — not gating WINUAT-02 outcome, but if the user is on macOS during recording, a "macOS sanity: passed" line in the outcome notes is welcome).**

**WINUAT-03 scope reminder:** REQUIREMENTS line 47 — "Resolve both pending todos … move to .planning/todos/resolved/ with closing notes referencing the v1.5 UAT phase. If WINUAT-01 / WINUAT-02 outcomes are `human_needed` (host unavailable this cycle), the todos remain in `pending/` with an updated note and carry forward to v1.6+." Plan 39-02 owns the Phase 20 todo half; this plan owns the Phase 31 todo half.
</facts>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: WINUAT-02 live execution on Windows (LIVE mode only — skip if `winuat_02_mode: deferred`)</name>
  <read_first>
    - .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md (frontmatter `winuat_02_mode:` value — if `deferred`, skip this task entirely and proceed to Task 2 DEFER branch; if `live`, run this task)
    - .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md (the test scenario the user uses as a script while at the Windows machine)
    - .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md item 1 verbatim (for the verbatim advisory string the user must match)
  </read_first>
  <what-built>
The Windows admin DnD suppression code path that shipped in Phase 31 (v1.3.1). The main-process elevation probe is in `src/main/elevation.ts`; the renderer's DropZone empty-state renders the verbatim advisory when `process.elevation === true`. No code change in this phase — Phase 39 is procedural UAT against the already-shipped binary. The user is verifying that the SHIPPED v1.3.1+ installer's admin-DnD advisory + suppression actually works on a real Windows admin session.
  </what-built>
  <how-to-verify>
Hand this script verbatim to the user at the Windows machine. The executor cannot perform any of these steps — they require physical interaction with a Windows host + admin UAC flow.

**Pre-conditions:**
- Windows machine (Windows 10 or 11 x64).
- A v1.3.1+ release installer is installed. v1.3.0 and earlier predate the PLATFORM-01 advisory; aborting to DEFER is correct if only v1.3.0 or earlier is available. v1.3.1 / v1.3.x / v1.4.x are all acceptable.
- User account has admin privilege (UAC will pop up; user must be able to confirm it).

**Steps:**

1. **Locate the installed app's launcher** — Start menu shortcut, desktop shortcut, or the installed binary in `C:\Program Files\` (the exact path depends on install method).
2. **Right-click the launcher** → click **"Run as administrator"** in the context menu.
3. **Confirm the UAC prompt** (Windows asks for permission; click Yes / OK).
4. **The app launches in elevated mode.** With NO project loaded (idle DropZone state visible in the main window):
   - **Observation #1 — Verbatim advisory copy.** Look at the empty-state text in the DropZone area. Does it read **EXACTLY** this:
     > Drag-and-drop is unavailable while running as administrator. Use File → Open instead, or relaunch the app without administrator privileges.
     Pass/fail: any character difference (typo, missing arrow, different phrasing, different line break) is a `fail`. Read carefully — the arrow is `→` (Unicode RIGHTWARDS ARROW U+2192), not `->` or `=>`.
5. **Observation #2 — Drag-over ring NOT appearing.** Drag a `.json` file from File Explorer over the app window. Watch the DropZone area:
   - If the ring `ring-2 ring-accent` (a 2px accent-colored border + faint accent background) does NOT appear during the drag-over: **pass**.
   - If the ring DOES appear, OR dropping the file loads the project: **fail** (suppression broken).
6. **Observation #3 — File → Open still works.** Click **File → Open** in the menu bar. Pick the same `.json` file. Project loads normally (regions populate; Global Max Render Source panel shows attachment rows). Pass/fail.
7. **Quit the app.**
8. **Relaunch normally** — click the Start menu shortcut WITHOUT right-clicking "Run as administrator". The app launches as a normal user.
9. **Observation #4 — Normal-relaunch DnD restores.** With no project loaded (idle DropZone), drag the same `.json` file over the app window. The drag-over ring (`ring-2 ring-accent bg-accent/5`) should appear during drag-over, and dropping should load the project. Pass/fail.

**(Optional) macOS sanity** — if the user is on a Mac after the Windows session, they can also confirm: launching the macOS dev or release build, the elevation probe short-circuits to `false`, the verbatim advisory does NOT render, and DnD works normally. This is OPTIONAL and not gating.

**Report back to the executor in this exact structured form:**

```
host_details: Windows <version>, <machine descriptor>
installer_version: <v1.x.y from the app's About panel or installer filename>
observation_1_advisory_verbatim: <pass | fail>
observation_2_no_ring: <pass | fail>
observation_3_file_open: <pass | fail>
observation_4_relaunch_dnd: <pass | fail>
macos_sanity: <pass | not-tested>
overall: <passed | failed>
notes: <free-form; any character-level differences in advisory copy if observation 1 failed, any unexpected behavior>
```

`overall: passed` requires all four observation points `pass`. Any single `fail` → `overall: failed`.
  </how-to-verify>
  <resume-signal>Paste the structured outcome block above, OR type "deferred — host unavailable now" to fall through to Task 2 DEFER branch.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record WINUAT-02 outcome in 31-HUMAN-UAT.md AND move/update the Phase 31 todo (WINUAT-03 half)</name>
  <files>.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md, .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md, .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md</files>
  <read_first>
    - .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md (frontmatter `winuat_02_mode:` value — branch selector)
    - Task 1 outcome (recorded in this session — the structured observation block, or "deferred — host unavailable now")
    - .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md (full file — lines 15-17 for item 1 result line; lines 31-39 for the Summary block; the Edit tool will modify both regions)
    - .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md (full file — the problem statement that stays byte-identical above the new appended section)
    - .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md (lines 75-78 — canonical close-out shape reused here)
  </read_first>
  <action>
This task has two branches, selected by `winuat_02_mode` from 39-CONTRACT.md frontmatter. Execute ONLY the matching branch. Each branch makes TWO edits to TWO files (the UAT file + the todo file) and commits them in one atomic commit.

***

**Branch A — LIVE mode (`winuat_02_mode: live`, Task 1 returned a structured outcome block).**

**Step A.1 — Edit `31-HUMAN-UAT.md` item 1 result line + Summary block.**

Use the Edit tool. Two edits to the same file:

Edit 1 — flip item 1's `result:` line. `old_string`:

```
result: deferred — will exercise at v1.3.1 release time when an installed binary + admin session can be obtained. Tracked as a pending todo so it surfaces in `/gsd-progress` and `/gsd-audit-uat`.
```

`new_string` (substitute `<outcome>` with `passed` or `failed` from Task 1; substitute `<observation-summary>` with a one-line summary of the four observations):

```
result: <outcome> (2026-05-13) — Phase 39 WINUAT-02 on Windows <host_details brief>; <observation-summary, e.g. "all 4 observations passed: verbatim advisory match, no ring on drag-over, File → Open works, normal-relaunch restores DnD" or "failed at observation 1: advisory copy reads 'Drag-and-drop' with hyphen but spec is em-dash" — be specific about which observation failed if any>.
```

Edit 2 — update the Summary block (lines 31-39). `old_string`:

```
total: 4
passed: 3
issues: 0
pending: 1
skipped: 0
blocked: 0
```

`new_string` (if `outcome: passed`):

```
total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0
```

`new_string` (if `outcome: failed`):

```
total: 4
passed: 3
failed: 1
issues: 1
pending: 0
skipped: 0
blocked: 0
```

Also update the frontmatter `status: partial` → `status: passed` (if all passed) or `status: failed` (if any failed). Two more Edits:

Edit 3 — frontmatter status. `old_string`: `status: partial`. `new_string`: `status: passed` or `status: failed` per outcome.

Edit 4 — frontmatter `updated:`. `old_string`: `updated: 2026-05-08T18:05:00Z`. `new_string`: `updated: 2026-05-13`.

**Step A.2 — `git mv` the Phase 31 pending todo to resolved/.**

```bash
git mv .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
```

Plain-English: same rename mechanic as plan 39-02 Step A.1 — git tracks this as a single rename, content preserved. `git status` should show `renamed:` afterward.

**Step A.3 — Edit-append `## Resolved` close-out section to the END of the resolved file.**

Use the Edit tool with `old_string` being the last line of existing content (`- .planning/phases/31-loader-ux-small-fixes-batch/31-VERIFICATION.md (human_verification[0])`) and `new_string` being that line followed by the close-out block below.

Close-out template (fill placeholders from Task 1 outcome):

```markdown

***

## Resolved

2026-05-13 — Phase 39 (WINUAT-02 + WINUAT-03 Phase-31-half) — outcome: **<passed | failed>**.

Executed on a real Windows admin session per host-availability decision recorded at `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`.

**Host:** <host_details verbatim from Task 1 outcome>
**Installer:** <installer_version verbatim from Task 1 outcome>

**Observation matrix:**
- Verbatim advisory copy match: **<pass | fail>**
- Drag-over ring suppressed: **<pass | fail>**
- File → Open path works: **<pass | fail>**
- Normal-relaunch DnD restores: **<pass | fail>**
- macOS sanity (optional): **<pass | not-tested>**

**Notes:** <notes verbatim from Task 1 outcome; if user did not supply any, write "none">.

**UAT file updated:** Item 1 in `.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md` flipped from `deferred` to `<outcome>`; Summary table updated to reflect `passed: <3|4>`, `pending: 0`, `failed: <0|1>`; frontmatter `status:` updated to match.

**Related REQs:** WINUAT-02 (live UAT outcome), WINUAT-03 (Phase 31 todo retired to resolved/). Phase 20 side of WINUAT-03 owned by plan 39-02.
```

**Step A.4 — stage and commit atomically.**

```bash
git add .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md
git add .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
git commit -m "$(cat <<'EOF'
chore(39): WINUAT-02 — <passed|failed> — Phase 31 admin DnD advisory on Windows

Live UAT on an elevated Windows session against the v1.3.1+ installer:
- Verbatim advisory copy: <pass|fail>
- Drag-over ring suppression: <pass|fail>
- File → Open path: <pass|fail>
- Normal-relaunch DnD restoration: <pass|fail>

Files changed:
- .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md
  (item 1 result flipped from "deferred" to "<outcome> (2026-05-13) — Phase 39
   WINUAT-02"; Summary table updated; frontmatter status flipped)
- .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
  -> .planning/todos/resolved/ (rename + Resolved close-out section appended)

Closes: WINUAT-02, WINUAT-03 (Phase 31 half — Phase 20 half owned by 39-02)
EOF
)"
```

Plain-English: `git mv` from Step A.2 already staged the rename; `git add` registers both the UAT file edit and the resolved-todo content append; `git commit` records all three changes as one atomic commit. `git log -1 --name-status -M` shows: `M` for the UAT file, `R<NN>` for the todo rename.

***

**Branch B — DEFER mode (`winuat_02_mode: deferred`).**

**Step B.1 — Edit `31-HUMAN-UAT.md` item 1 result line + Summary block.**

Edit 1 — flip item 1's `result:` line. `old_string`:

```
result: deferred — will exercise at v1.3.1 release time when an installed binary + admin session can be obtained. Tracked as a pending todo so it surfaces in `/gsd-progress` and `/gsd-audit-uat`.
```

`new_string`:

```
result: human_needed (2026-05-13) — Phase 39 carry-forward to v1.6+; no Windows admin session available in the v1.5 cycle. See `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md` for the host-availability decision and `.planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md` for the carry-forward note.
```

Edit 2 — Summary block. `old_string`:

```
total: 4
passed: 3
issues: 0
pending: 1
skipped: 0
blocked: 0
```

`new_string`:

```
total: 4
passed: 3
human_needed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0
```

Edit 3 — frontmatter `status: partial` → `status: human_needed`.

Edit 4 — frontmatter `updated:` → `2026-05-13`.

**Step B.2 — Edit-append `## Carry-forward note` to the END of the PENDING todo file (do NOT move it).**

Append exactly:

```markdown

***

## Carry-forward note

2026-05-13 — Phase 39 (WINUAT-02 + WINUAT-03 Phase-31-half) — outcome: **human_needed**.

Per host-availability decision recorded at `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md` (`host_available: <no | partial-winuat01-only>`), no Windows admin session with the v1.3.1+ installer was available in the v1.5 cycle to exercise this UAT. WINUAT-02 carries forward to v1.6+ with audit-trail intact.

`human_needed` is a first-class non-failure outcome for Phase 39 per ROADMAP Phase 39 Success Criterion 4 + STATE.md `v1.5 Locked Constraints`. The deferral does NOT block phase closure.

**UAT file updated:** Item 1 in `.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md` flipped from `deferred` to `human_needed (2026-05-13) — Phase 39 carry-forward to v1.6+`; Summary table updated (`human_needed: 1`, `pending: 0`); frontmatter `status:` flipped from `partial` to `human_needed`.

**Related REQs:** WINUAT-02 (carry-forward), WINUAT-03 (this todo stays in pending/ until v1.6+ exercises it). Phase 20 side of WINUAT-03 owned by plan 39-02.
```

**Step B.3 — stage and commit.**

```bash
git add .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md
git add .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
git commit -m "$(cat <<'EOF'
chore(39): WINUAT-02 — human_needed — carry forward Phase 31 admin DnD UAT to v1.6+

No Windows admin session with v1.3.1+ installer available in v1.5; UAT defers
with audit-trail intact. Phase 39 graceful-degradation contract (39-CONTRACT.md):
human_needed is first-class non-failure.

Files changed:
- .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md
  (item 1 result flipped to "human_needed (2026-05-13) — Phase 39 carry-forward
   to v1.6+"; Summary table adds human_needed: 1; frontmatter status flipped)
- .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
  (Carry-forward note appended in-place; file stays in pending/)

Related: WINUAT-02, WINUAT-03 (Phase 31 half — Phase 20 half owned by 39-02)
EOF
)"
```

Plain-English: no file rename — todo stays in `pending/`. Two `git add` calls stage the UAT file edits + the todo content append; one commit captures both.
  </action>
  <verify>
    <automated>WINUAT_02_MODE=$(grep '^winuat_02_mode:' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md | awk '{print $2}'); UAT=.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md; if [ "$WINUAT_02_MODE" = "live" ]; then ! grep -q '^result: deferred — will exercise at v1.3.1 release time' "$UAT" && grep -qE '^result: (passed|failed) \(2026-05-13\) — Phase 39 WINUAT-02' "$UAT" && grep -qE '^(passed: 4|failed: 1)' "$UAT" && grep -q '^pending: 0' "$UAT" && grep -qE '^status: (passed|failed)$' "$UAT" && ! test -e .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && test -s .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -q '^## Resolved' .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -q 'Phase 39 (WINUAT-02 + WINUAT-03' .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -qE 'outcome: \*\*(passed|failed)\*\*' .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -q '39-CONTRACT.md' .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && git log -1 --pretty=%s | grep -qE '^chore\(39\): WINUAT-02 — (passed|failed)' && git log -1 --name-status -M | grep -qE 'R[0-9]+\s+\.planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat\.md\s+\.planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat\.md'; elif [ "$WINUAT_02_MODE" = "deferred" ]; then ! grep -q '^result: deferred — will exercise at v1.3.1 release time' "$UAT" && grep -q '^result: human_needed (2026-05-13) — Phase 39 carry-forward to v1.6+' "$UAT" && grep -q '^human_needed: 1' "$UAT" && grep -q '^pending: 0' "$UAT" && grep -q '^status: human_needed$' "$UAT" && test -s .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && ! test -e .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -q '^## Carry-forward note' .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -q 'Phase 39 (WINUAT-02 + WINUAT-03' .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -q 'outcome: \*\*human_needed\*\*' .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -q 'first-class non-failure' .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -q '39-CONTRACT.md' .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && git log -1 --pretty=%s | grep -q '^chore(39): WINUAT-02 — human_needed'; else echo "ERR: winuat_02_mode not live or deferred"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - **Common:** UAT file edited at the archived path (NOT created at the bogus ROADMAP path): `test -s .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md && ! test -e .planning/phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Common:** Item 1 result line no longer contains the original `deferred — will exercise at v1.3.1 release time` text: `! grep -q '^result: deferred — will exercise at v1.3.1 release time' .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Common:** Result line references Phase 39: `grep -q 'Phase 39' .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Common:** Summary `pending:` is 0: `grep -q '^pending: 0' .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Common:** Frontmatter `updated:` is 2026-05-13: `grep -q '^updated: 2026-05-13$' .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Common:** Outcome paragraph in todo references contract + WINUAT-02 + WINUAT-03: `grep -lqE '39-CONTRACT\.md' .planning/todos/{pending,resolved}/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -lqE 'WINUAT-02' .planning/todos/{pending,resolved}/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -lqE 'WINUAT-03' .planning/todos/{pending,resolved}/2026-05-08-phase-31-windows-admin-dnd-release-uat.md`
    - **Branch A (LIVE):** Item 1 result line shows passed/failed with date: `grep -qE '^result: (passed|failed) \(2026-05-13\) — Phase 39 WINUAT-02' .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Branch A (LIVE):** Summary `passed: 4` OR `failed: 1` line present: `grep -qE '^(passed: 4|failed: 1)' .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Branch A (LIVE):** Frontmatter `status:` is `passed` or `failed`: `grep -qE '^status: (passed|failed)$' .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Branch A (LIVE):** Phase 31 todo moved to resolved/: `! test -e .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && test -s .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md`
    - **Branch A (LIVE):** Close-out section heading + outcome present: `grep -q '^## Resolved' .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -qE 'outcome: \*\*(passed|failed)\*\*' .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md`
    - **Branch A (LIVE):** Observation matrix records all four bullets: `for obs in 'Verbatim advisory copy match' 'Drag-over ring suppressed' 'File → Open path works' 'Normal-relaunch DnD restores'; do grep -q "$obs" .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md || exit 1; done`
    - **Branch A (LIVE):** Pre-existing problem statement preserved in resolved file: `grep -q '## What to test' .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -q 'PLATFORM-01' .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md`
    - **Branch A (LIVE):** Commit captures rename + UAT-file modify: `git log -1 --name-status -M | grep -qE 'R[0-9]+\s+\.planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat\.md\s+\.planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat\.md' && git log -1 --name-status -M | grep -qE '^M\s+\.planning/milestones/v1\.3\.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT\.md'`
    - **Branch A (LIVE):** Commit message matches `chore(39): WINUAT-02 — passed|failed`: `git log -1 --pretty=%s | grep -qE '^chore\(39\): WINUAT-02 — (passed|failed)'`
    - **Branch B (DEFER):** Item 1 result line shows human_needed: `grep -q '^result: human_needed (2026-05-13) — Phase 39 carry-forward to v1.6+' .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Branch B (DEFER):** Summary `human_needed: 1` line present: `grep -q '^human_needed: 1' .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Branch B (DEFER):** Frontmatter `status: human_needed`: `grep -q '^status: human_needed$' .planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md`
    - **Branch B (DEFER):** Phase 31 todo stays in pending/: `test -s .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && ! test -e .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md`
    - **Branch B (DEFER):** Carry-forward heading + outcome present: `grep -q '^## Carry-forward note' .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md && grep -q 'outcome: \*\*human_needed\*\*' .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md`
    - **Branch B (DEFER):** Carry-forward note cites first-class non-failure: `grep -q 'first-class non-failure' .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md`
    - **Branch B (DEFER):** Commit message matches `chore(39): WINUAT-02 — human_needed`: `git log -1 --pretty=%s | grep -q '^chore(39): WINUAT-02 — human_needed'`
    - **Both branches:** No source files in `src/` or `tests/` modified: `git diff HEAD~1..HEAD -- src/ tests/ | wc -l | awk '{ if ($1 != 0) exit 1 }'`
  </acceptance_criteria>
  <done>
WINUAT-02 outcome is recorded in `31-HUMAN-UAT.md` (archived path, item 1 result line + Summary table + frontmatter status, all consistent) AND in the Phase 31 todo's appended section (`## Resolved` if live, `## Carry-forward note` if deferred). Phase 31 todo is at the resolved/ path (LIVE outcome) or remains at pending/ with a dated v1.5 carry-forward note (DEFER outcome). Pre-existing problem statement preserved byte-identical. One atomic commit `chore(39): WINUAT-02 — <outcome> — ...` in git log. No source files touched. WINUAT-03 satisfied for the Phase 31 half (Phase 20 half owned by plan 39-02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only two planning markdown files (one UAT outcome record in an archived path, one todo file). No code, no IPC, no UI, no user-facing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | (n/a) | (n/a) | accept | No new attack surface — phase modifies only existing planning markdown files (UAT outcome record + todo carry-forward note). No code, no dependencies, no user-facing UI built. Audit-trail integrity is the only relevant security property; it is enforced by (a) structured outcome recording (observation matrix + dated section + REQ-IDs + contract reference + verbatim-advisory-copy match check), (b) grep gates in `<acceptance_criteria>` confirming Summary table + frontmatter consistency, (c) pre-existing problem statement preserved (`grep -q '## What to test'` + `grep -q 'PLATFORM-01'` gates), (d) atomic commit message convention enabling `git log --grep 'WINUAT-02'` traceability. |
</threat_model>

<verification>
After Task 2 commit (whichever branch ran):

```bash
# Branch detection
WINUAT_02_MODE=$(grep '^winuat_02_mode:' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md | awk '{print $2}')
UAT=.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md

# Result line no longer says "deferred — will exercise at v1.3.1 release time"
! grep -q '^result: deferred — will exercise at v1.3.1 release time' "$UAT"

# pending: 0 in Summary
grep -q '^pending: 0' "$UAT"

if [ "$WINUAT_02_MODE" = "live" ]; then
  test -s .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
  ! test -e .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
  git log -1 --name-status -M  # should show M for UAT file + R<NN> for todo rename
elif [ "$WINUAT_02_MODE" = "deferred" ]; then
  test -s .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
  ! test -e .planning/todos/resolved/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
  grep -q '^## Carry-forward note' .planning/todos/pending/2026-05-08-phase-31-windows-admin-dnd-release-uat.md
fi

# Sanity: no source files modified
git diff HEAD~1..HEAD -- src/ tests/
```
</verification>

<success_criteria>
- WINUAT-02 has a recorded outcome (`passed`, `failed`, or `human_needed`) — no longer in unresolved `deferred` state.
- `31-HUMAN-UAT.md` item 1 result line, Summary table, and frontmatter `status:` are mutually consistent with the outcome.
- Phase 31 todo is in the correct location per branch (resolved/ for live; pending/ for deferred).
- WINUAT-03 satisfied for the Phase 31 half: todo lifecycle is correct (move + close-out OR stay + carry-forward note).
- All edits dated 2026-05-13 and cite both 39-CONTRACT.md and WINUAT-02/-03 REQ IDs.
- Pre-existing problem statement preserved byte-identical (no rewrite of historical record in either the UAT file's pre-edit content above item 1 or the todo's `## Problem` / `## What to test` block).
- Atomic commit `chore(39): WINUAT-02 — <outcome> — ...` in git log.
- No source files in `src/` or `tests/` modified.
</success_criteria>

<output>
After completion, create `.planning/phases/39-windows-host-blocked-uat-burndown/39-03-SUMMARY.md`.
</output>
