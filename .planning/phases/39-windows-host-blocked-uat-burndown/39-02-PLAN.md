---
phase: 39
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
  - .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
autonomous: false
requirements: [WINUAT-01, WINUAT-03]
must_haves:
  truths:
    - "WINUAT-01 has a recorded outcome (`passed`, `failed`, or `human_needed`) — no longer in an unresolved `deferred` state."
    - "If outcome is `passed` or `failed`: the todo file moved from `.planning/todos/pending/` to `.planning/todos/resolved/` with an appended `## Resolved` close-out section dated 2026-05-13 referencing Phase 39 + WINUAT-01."
    - "If outcome is `human_needed`: the todo file stays in `.planning/todos/pending/` with an appended `## Carry-forward note` section dated 2026-05-13 referencing Phase 39 and the v1.6+ carry-forward target."
    - "The close-out / carry-forward note records the exact observed behavior (drag image rendered? mixTime=0.25? loop=false? console errors?) as reported by the user, OR the specific deferral reason (no Windows host this cycle, etc)."
    - "WINUAT-03 Phase-20-half satisfied: todo moved to .planning/todos/resolved/ with close-out (LIVE branch) OR kept in .planning/todos/pending/ with carry-forward note dated >= 2026-05-13 (DEFER branch)."
    - "A single atomic commit captures the file change with message `chore(39): WINUAT-01 — <outcome> — <one-line summary>`."
  artifacts:
    - path: "(LIVE-MODE) .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md OR (DEFER-MODE) .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md"
      provides: "WINUAT-01 audit-trail anchor — outcome recorded in `## Resolved` (live) or `## Carry-forward note` (defer)"
      contains: "Phase 39"
  key_links:
    - from: "Phase 20 todo (resolved or pending)"
      to: ".planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md"
      via: "outcome paragraph cites the contract decision"
      pattern: "39-CONTRACT\\.md"
    - from: "Phase 20 todo (resolved or pending)"
      to: "WINUAT-01 requirement"
      via: "verbatim REQ-ID reference"
      pattern: "WINUAT-01"
---

<objective>
Close out WINUAT-01 (Phase 20 Documentation Builder cross-platform DnD UAT) per the host-availability decision recorded in `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`.

Two execution modes — the executor chooses based on the contract's `winuat_01_mode` frontmatter value:

  - **LIVE mode** (`winuat_01_mode: live`): user runs the v1.x installer on Windows, opens a project, opens DocBuilder modal, drags an animation onto Track 0, reports the four observation points back. Executor records outcome (`passed` or `failed`) in a `## Resolved` close-out + moves the todo from `pending/` to `resolved/`.

  - **DEFER mode** (`winuat_01_mode: deferred`): user did not / could not execute the UAT this cycle. Executor records outcome as `human_needed` in a `## Carry-forward note` appended IN-PLACE to the pending todo (file stays in `pending/`). Note is dated 2026-05-13 and references the v1.6+ carry-forward target.

Purpose: WINUAT-01 has been deferred since 2026-05-01 (over a milestone now). Phase 39 either retires the todo or formally re-acknowledges the carry-forward — but it does NOT leave the todo silently pending without a v1.5-dated note.

Output: Either one file move (`git mv` pending → resolved) + content append, OR one in-place content append. Single atomic commit either way.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md
@.planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
@.planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<facts>
**WINUAT-01 verbatim scenario from REQUIREMENTS.md line 45 (the source of truth — what the user is asked to observe on Windows):**

> Phase 20 Documentation Builder cross-platform DnD UAT — on a real Windows host with the installed v1.x release binary, repeat UAT scenario 3 from `20-HUMAN-UAT.md` (open project → DocBuilder modal → Animation Tracks pane → "+ Add Track" → drag an animation from the side list onto Track 0 → confirm drag image renders, drop succeeds with `mixTime=0.25` + `loop=false`, no console errors). Flip test #3 in `20-HUMAN-UAT.md` from `deferred` to `passed`/`failed` + update summary table. (Linux scope dropped — out per memory `project_linux_deferred`.)

**Path correction (from 39-CONTRACT.md Section 3.1):** `20-HUMAN-UAT.md` was DELETED in commit `0787fe1`. The pending todo `.planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md` is the surviving audit-trail anchor — outcome is recorded there, NOT in a UAT markdown file. Do NOT attempt to restore the deleted UAT file.

**Linux scope:** dropped permanently per memory `project_linux_deferred` (Linux removed from CI/release at v1.3 — no UAT). The close-out / carry-forward note should explicitly state "Linux scope dropped per memory `project_linux_deferred`; Windows-only outcome recorded".

**The four observation points the user must report back (LIVE mode):**
1. Drag image renders during drag (yes/no — Electron Chromium quirk D-06 says `effectAllowed='copy'` guards this; the failure mode is "no drag thumbnail").
2. Drop creates a new entry on Track 0 with default `mixTime=0.25` (yes/no — visible in the Animation Tracks pane).
3. Drop creates the entry with default `loop=false` (yes/no — visible in the Animation Tracks pane).
4. No console errors during the operation (DevTools console clean — open with Ctrl+Shift+I or F12 on Windows).

**Outcome decision rule (LIVE mode):**
- All four observation points pass → `passed`
- One or more observation points fail → `failed` (record which one(s) failed verbatim in the close-out)
- User runs the UAT but cannot complete (e.g. installer fails to launch, project won't open) → record as `failed` with the blocker described, NOT `human_needed`. `human_needed` is reserved for "couldn't try" not "tried and broke".
</facts>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: WINUAT-01 live execution on Windows (LIVE mode only — skip if `winuat_01_mode: deferred`)</name>
  <read_first>
    - .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md (the frontmatter `winuat_01_mode:` value — if `deferred`, skip this task entirely and proceed to Task 2 DEFER branch; if `live`, run this task)
    - .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md (the test scenario — the user uses this as the UAT script while at the Windows machine)
  </read_first>
  <what-built>
The DocBuilder DnD code path that shipped in Phase 20 (v1.2.0). The renderer-side handler is in `src/renderer/src/modals/DocumentationBuilderDialog.tsx` (line 410 sets `effectAllowed='copy'` per RESEARCH §Pattern 4). No code change in this phase — Phase 39 is procedural UAT against the already-shipped binary. The user is verifying that the SHIPPED v1.x installer's DocBuilder DnD works on real Windows hardware.
  </what-built>
  <how-to-verify>
Hand this script verbatim to the user at the Windows machine. The executor cannot perform any of these steps — they require physical interaction with a Windows host running the installer.

**Pre-conditions:**
- Windows machine (Windows 10 or 11; x64 is the only shipped target).
- A v1.x release installer is installed (v1.2.0+ minimum for DocBuilder; v1.3.x or v1.4.x recommended). If the only available installer is `release/Spine Texture Manager-1.1.3-x64.exe` (the one named in the original 2026-05-01 todo), DocBuilder did not yet ship in v1.1.3 — abort to Task 2 DEFER branch with reason "no DocBuilder in v1.1.3 binary".
- A Spine project fixture loadable from the Windows machine. The repo's `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` is sufficient if the repo is checked out on Windows; otherwise any `.stmproj` or `.json` skeleton the user has on hand works (the test does not depend on specific attachments — just that the skeleton has at least one animation).

**Steps:**

1. Launch the installed v1.x app from the Windows Start menu (NOT "Run as administrator" — that's a different UAT scenario WINUAT-02; this scenario is normal-user launch).
2. **File → Open** (or drag-drop) the test skeleton JSON / `.stmproj`. Confirm the project loads — Global Max Render Source panel populates with regions. If load fails, record the failure and abort: outcome is `failed` with reason "project load broken on Windows v1.x — distinct regression from WINUAT-01 scope, record in resolved note".
3. Open the **Documentation Builder** modal. The exact opening seam depends on the v1.x version: in v1.2.x+ there is a Documentation toolbar button (look for a document/page icon in the sticky toolbar). If no Documentation button is visible, the installed binary predates v1.2.0 — abort to DEFER with reason "binary lacks DocBuilder".
4. Switch to the **Animation Tracks** pane within the DocBuilder modal.
5. Click **"+ Add Track"** — Track 0 row appears.
6. Drag any animation from the side list onto Track 0. Observe:
   - **Observation #1 — Drag image rendered.** While dragging, does a visible thumbnail / element ghost follow the cursor? Yes / No.
   - **Observation #2 — mixTime=0.25.** After the drop, does a new entry appear on Track 0 with `mixTime` field showing `0.25` (or equivalent default)? Yes / No.
   - **Observation #3 — loop=false.** Does the same new entry show `loop` field as `false` (unchecked checkbox or `false` value)? Yes / No.
   - **Observation #4 — No console errors.** Open DevTools (Ctrl+Shift+I or F12 in the app window); switch to the Console tab. Are there any red error lines from the drag operation? Yes (errors) / No (clean).

**Report back to the executor in this exact structured form:**

```
host_details: Windows <version, e.g. "11 22H2 x64">, <machine descriptor — e.g. "Dell Latitude 7420">
installer_version: <v1.x.y from the app's About panel or installer filename>
observation_1_drag_image: <pass | fail>
observation_2_mixTime: <pass | fail>
observation_3_loop: <pass | fail>
observation_4_console_clean: <pass | fail>
overall: <passed | failed>
notes: <free-form — anything weird, any blocker that aborted the test, etc>
```

If `overall: passed` — all four observations are `pass`. If any single observation is `fail`, `overall` is `failed`.
  </how-to-verify>
  <resume-signal>Paste the structured outcome block above, OR type "deferred — host unavailable now" to fall through to Task 2 DEFER branch.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record WINUAT-01 outcome — move to resolved/ (LIVE) or carry forward in pending/ (DEFER)</name>
  <files>.planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md, .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md</files>
  <read_first>
    - .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md (frontmatter `winuat_01_mode:` value — branch selector for this task)
    - Task 1 outcome (recorded in this session — the structured observation block from the user, or "deferred — host unavailable now")
    - .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md (full file — the problem statement that stays byte-identical above the new appended section)
    - .planning/todos/resolved/2026-04-24-phase-4-code-review-follow-up.md (lines 75-78 — the canonical close-out shape from Phase 38: `---` separator + `## Resolved` h2 + dated paragraph; same pattern reused here)
  </read_first>
  <action>
This task has two branches, selected by `winuat_01_mode` from 39-CONTRACT.md frontmatter. Execute ONLY the matching branch.

***

**Branch A — LIVE mode (`winuat_01_mode: live`, Task 1 returned a structured outcome block).**

**Step A.1 — `git mv` the pending todo to resolved/.**

```bash
git mv .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
```

What this does (plain English per memory `feedback_explain_git`): tells git to record this as a rename, not a delete + new file. Content is preserved bit-for-bit. After this, `git status` should show:

```
renamed:    .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md -> .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
```

**Step A.2 — Use the Edit tool to append a `## Resolved` close-out section to the END of the resolved file.**

Do NOT rewrite the file. Use the Edit tool with `old_string` being the last line of existing content (likely `- 20-RESEARCH.md §Pattern 4 (drag image guard)`) and `new_string` being that same line followed by the close-out block below.

The close-out block template (fill in the placeholders from the Task 1 outcome block):

```markdown

***

## Resolved

2026-05-13 — Phase 39 (WINUAT-01) — outcome: **<passed | failed>**.

Executed on a real Windows host per host-availability decision recorded at `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md`.

**Host:** <host_details verbatim from Task 1 outcome>
**Installer:** <installer_version verbatim from Task 1 outcome>

**Observation matrix:**
- Drag image rendered: **<pass | fail>**
- New entry `mixTime=0.25`: **<pass | fail>**
- New entry `loop=false`: **<pass | fail>**
- DevTools console clean (no errors): **<pass | fail>**

**Notes:** <notes verbatim from Task 1 outcome; if user did not supply any, write "none">.

**Linux scope dropped** per memory `project_linux_deferred` (Linux removed from CI/release at v1.3 — never UAT'd, never built). Windows-only outcome recorded; the original todo's Linux clause (item 2, "Linux test") is obsolete.

**Related REQ:** WINUAT-01 (REQUIREMENTS.md). Phase 20 `20-HUMAN-UAT.md` is not updated because the file was deleted in commit `0787fe1` (v1.2 cleanup); this todo is the surviving audit-trail anchor per 39-CONTRACT.md §3.1.
```

**Step A.3 — stage and commit atomically.**

```bash
git add .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
git commit -m "$(cat <<'EOF'
chore(39): WINUAT-01 — <passed|failed> — Phase 20 DocBuilder DnD on Windows

Move .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
to .planning/todos/resolved/ and append a Resolved section recording the live
UAT outcome against the v1.x installer on a real Windows host.

Host: <host_details>
Installer: <installer_version>
Observation matrix: <one-line summary, e.g. "drag-image: pass, mixTime: pass, loop: pass, console: pass — all 4 PASS">
Linux scope dropped per project_linux_deferred memory.

Closes: 2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat
EOF
)"
```

Plain-English note: `git mv` already staged the rename in Step A.1; `git add` registers the content append; `git commit` records both as one atomic commit. `git log -1 --name-status -M` after the commit should show an `R` (rename) status with the old and new paths.

***

**Branch B — DEFER mode (`winuat_02_mode` is irrelevant here, but `winuat_01_mode: deferred`).**

**Step B.1 — Use the Edit tool to append a `## Carry-forward note` section to the END of the PENDING file (do NOT move it).**

Append exactly:

```markdown

***

## Carry-forward note

2026-05-13 — Phase 39 (WINUAT-01) — outcome: **human_needed**.

Per host-availability decision recorded at `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md` (`host_available: <no | partial-winuat02-only>`), no Windows host with the right installer was available in the v1.5 cycle to exercise this UAT. WINUAT-01 carries forward to v1.6+ with audit-trail intact.

`human_needed` is a first-class non-failure outcome for Phase 39 per ROADMAP Phase 39 Success Criterion 4 + STATE.md `v1.5 Locked Constraints`. The deferral does NOT block phase closure.

**Linux scope dropped** per memory `project_linux_deferred` — when WINUAT-01 does execute (v1.6+ or later), only the Windows arm needs to run.

**Related REQ:** WINUAT-01 (REQUIREMENTS.md). Phase 20 `20-HUMAN-UAT.md` is not updated because the file was deleted in commit `0787fe1` (v1.2 cleanup); this todo is the surviving audit-trail anchor per 39-CONTRACT.md §3.1.
```

**Step B.2 — stage and commit.**

```bash
git add .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
git commit -m "$(cat <<'EOF'
chore(39): WINUAT-01 — human_needed — carry forward to v1.6+

Append carry-forward note to the pending Phase 20 cross-platform DnD UAT todo
recording the v1.5 cycle's host-availability decision. No Windows host with
the required v1.x installer was available this cycle; UAT defers to v1.6+
with audit-trail intact.

Phase 39 graceful-degradation contract (39-CONTRACT.md): human_needed is a
first-class non-failure outcome; the todo stays in pending/ with this dated
note so future cycle planning surfaces it.

Linux scope dropped per project_linux_deferred memory.
EOF
)"
```

Plain-English: no file move — the file stays in `pending/`. `git add <path>` stages the appended content; `git commit` records the change. `git status` after the commit should be clean.
  </action>
  <verify>
    <automated>WINUAT_01_MODE=$(grep '^winuat_01_mode:' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md | awk '{print $2}'); if [ "$WINUAT_01_MODE" = "live" ]; then ! test -e .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && test -s .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q '^## Resolved' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q 'Phase 39 (WINUAT-01)' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -qE 'outcome: \*\*(passed|failed)\*\*' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q 'project_linux_deferred' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q '39-CONTRACT.md' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && git log -1 --pretty=%s | grep -qE '^chore\(39\): WINUAT-01 — (passed|failed)' && git log -1 --name-status -M | grep -qE 'R[0-9]+\s+\.planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat\.md\s+\.planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat\.md'; elif [ "$WINUAT_01_MODE" = "deferred" ]; then test -s .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && ! test -e .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q '^## Carry-forward note' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q 'Phase 39 (WINUAT-01)' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q 'outcome: \*\*human_needed\*\*' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q '39-CONTRACT.md' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q 'first-class non-failure' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && git log -1 --pretty=%s | grep -q '^chore(39): WINUAT-01 — human_needed'; else echo "ERR: winuat_01_mode not live or deferred"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - **Common (both branches):** Phase 20 UAT file is NOT touched (it doesn't exist anyway): `! test -e .planning/phases/20-documentation-builder-feature/20-HUMAN-UAT.md`
    - **Branch A (LIVE):** Outcome paragraph references the contract: `grep -q '39-CONTRACT.md' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch A (LIVE):** Outcome paragraph references WINUAT-01 REQ: `grep -q 'WINUAT-01' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch A (LIVE):** Outcome paragraph cites `project_linux_deferred` memory: `grep -q 'project_linux_deferred' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch A (LIVE):** Pending file gone: `! test -e .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch A (LIVE):** Resolved file present + non-empty: `test -s .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch A (LIVE):** Resolved section heading present: `grep -q '^## Resolved' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch A (LIVE):** Outcome value is `passed` or `failed`: `grep -qE 'outcome: \*\*(passed|failed)\*\*' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch A (LIVE):** Observation matrix records all four bullets: `for obs in 'Drag image rendered' 'mixTime=0.25' 'loop=false' 'DevTools console clean'; do grep -q "$obs" .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md || exit 1; done`
    - **Branch A (LIVE):** Pre-existing problem statement preserved: `grep -q '## Problem' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q 'NSIS installer' .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch A (LIVE):** Commit captures rename (R status): `git log -1 --name-status -M | grep -qE 'R[0-9]+\s+\.planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat\.md\s+\.planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat\.md'`
    - **Branch A (LIVE):** Commit message matches `chore(39): WINUAT-01 — passed|failed`: `git log -1 --pretty=%s | grep -qE '^chore\(39\): WINUAT-01 — (passed|failed)'`
    - **Branch B (DEFER):** Outcome paragraph references the contract: `grep -q '39-CONTRACT.md' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch B (DEFER):** Outcome paragraph references WINUAT-01 REQ: `grep -q 'WINUAT-01' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch B (DEFER):** Outcome paragraph cites `project_linux_deferred` memory: `grep -q 'project_linux_deferred' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch B (DEFER):** Pending file present + non-empty: `test -s .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch B (DEFER):** Resolved file does NOT exist: `! test -e .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch B (DEFER):** Carry-forward section heading present: `grep -q '^## Carry-forward note' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch B (DEFER):** Outcome value is `human_needed`: `grep -q 'outcome: \*\*human_needed\*\*' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch B (DEFER):** Carry-forward note cites first-class non-failure: `grep -q 'first-class non-failure' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch B (DEFER):** Pre-existing problem statement preserved: `grep -q '## Problem' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md && grep -q 'NSIS installer' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md`
    - **Branch B (DEFER):** Commit message matches `chore(39): WINUAT-01 — human_needed`: `git log -1 --pretty=%s | grep -q '^chore(39): WINUAT-01 — human_needed'`
    - **Both branches:** No source files in `src/` or `tests/` modified: `git diff HEAD~1..HEAD -- src/ tests/ | wc -l | awk '{ if ($1 != 0) exit 1 }'`
  </acceptance_criteria>
  <done>
WINUAT-01 outcome is recorded in the Phase 20 todo's appended section (`## Resolved` if `passed`/`failed`, `## Carry-forward note` if `human_needed`). The todo is either at the resolved/ path (LIVE outcome) or remains at the pending/ path with a dated v1.5 carry-forward note (DEFER outcome). Pre-existing problem statement is byte-identical. One atomic commit `chore(39): WINUAT-01 — <outcome> — ...` in git log. No source files touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only one planning markdown file (move + append, OR in-place append). No code, no IPC, no UI, no user-facing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02 | (n/a) | (n/a) | accept | No new attack surface — phase modifies only an existing pending-todo planning markdown file. No code, no dependencies, no user-facing UI built. Audit-trail integrity is the only relevant security property; it is enforced by (a) structured outcome recording (observation matrix + dated section + REQ-ID + contract reference), (b) grep gates in `<acceptance_criteria>` confirming the matrix is present, (c) the pre-existing problem statement preserved byte-identical (`grep -q '## Problem'` gate), (d) atomic commit message convention enabling `git log --grep 'WINUAT-01'` traceability. |
</threat_model>

<verification>
After Task 2 commit (whichever branch ran):

```bash
# Branch detection — reads from the contract
WINUAT_01_MODE=$(grep '^winuat_01_mode:' .planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md | awk '{print $2}')

if [ "$WINUAT_01_MODE" = "live" ]; then
  # LIVE branch — file in resolved/, NOT in pending/
  test -s .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
  ! test -e .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
  git log -1 --name-status -M  # should show R<NN> rename
elif [ "$WINUAT_01_MODE" = "deferred" ]; then
  # DEFER branch — file stays in pending/ with carry-forward note
  test -s .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
  ! test -e .planning/todos/resolved/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
  grep -q '^## Carry-forward note' .planning/todos/pending/2026-05-01-phase-20-windows-linux-dnd-cross-platform-uat.md
fi

# Sanity: no source files modified
git diff HEAD~1..HEAD -- src/ tests/
```
</verification>

<success_criteria>
- WINUAT-01 has a recorded outcome (`passed`, `failed`, or `human_needed`) — no longer in unresolved `deferred` state.
- Outcome is recorded in the correct file location per branch (resolved/ for live; pending/ for deferred).
- Appended section is dated 2026-05-13 and references both 39-CONTRACT.md and WINUAT-01 REQ.
- Linux scope dropped per memory `project_linux_deferred`.
- Pre-existing problem statement preserved byte-identical (no rewrite of historical record).
- Atomic commit `chore(39): WINUAT-01 — <outcome> — ...` in git log.
- No source files in `src/` or `tests/` modified.
</success_criteria>

<output>
After completion, create `.planning/phases/39-windows-host-blocked-uat-burndown/39-02-SUMMARY.md`.
</output>
</output>
