---
phase: 47-spine-player-4-3-0-bump-viewer-regression
plan: 04
type: execute
wave: 4
depends_on: ["47-03"]
gap_closure: true
files_modified:
  - tests/runtime/reg4701-buildsummary-handoff.spec.ts
  - tests/runtime/dual-viewer-routing.spec.ts
  - tests/runtime/dv1-42-parse-guard.spec.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-VALIDATION.md
autonomous: true
requirements: [PLAYER-02]
must_haves:
  truths:
    - "[PLAYER-02 / DV-NOTE T-A] A PERMANENT REG-47-01 cross-runtime-handoff regression test exists (the deleted _dbg- throwaway is now permanent): loadSkeleton→sampleSkeleton→buildSummary on a 4.3 fixture does NOT throw `reading 'r'`; the 4.2 control passes the same chain"
    - "[PLAYER-02 / DV-1a T-B] A dual-runtime ROUTING regression test exists: spine-player-42 resolves a spine-core whose version is 4.2.111 (lacks 4.3-only Slider/BonePose) while @esotericsoftware/spine-player resolves 4.3.0; AND a SkeletonSummary with runtimeTag:'4.2' selects AnimationPlayerModal42 while '4.3' selects the migrated modal (off the core tag, not re-detection)"
    - "[PLAYER-02 / DV-RISK-1 T-C] A 4.2-alias parse standing-guard exists: the SkeletonJson reached via spine-player-42's bare core parses each DV-3 4.2 constraint-mix fixture clean (SIMPLE_TEST + CHJWC_SYMBOLS transform + TQORW_SYMBOLS ik+transform+events + TEST_03 ik+transform+physics); the same fixtures through canonical 4.3.0 core THROW (the gap is real → the alias fixes it)"
    - "[PLAYER-02 / DV-2] ROADMAP.md Phase 47 SC#2 + REQUIREMENTS.md PLAYER-02 + the Traceability row are reworded from 'renders a 4.2 and a 4.3 fixture correctly through the 4.3 player' to the DV-2 dual-runtime wording — SAME requirement ID PLAYER-02, reworded NOT new (traceability stays clean)"
    - "[PLAYER-02 / DV-1 addendum] 47-VALIDATION.md folds in the RESEARCH §7 Validation Architecture addendum (the 4 owed T-A..T-D tests, fully instantiated — NOT a stub; plan-checker Check 8e)"
    - "[blast-radius] ZERO src/ change in this plan (tests + planning docs only); zero CSP/CORS; the DV-2 edit REWORDS the existing PLAYER-02 contract, it does NOT drop or descope it (feedback_replan_can_silently_descope_roadmap_contract)"
  artifacts:
    - path: "tests/runtime/reg4701-buildsummary-handoff.spec.ts"
      provides: "the permanent T-A REG-47-01 cross-runtime-handoff regression"
      contains: "buildSummary"
    - path: "tests/runtime/dual-viewer-routing.spec.ts"
      provides: "the T-B dual-runtime routing + alias-distinctness regression"
      contains: "runtimeTag"
    - path: "tests/runtime/dv1-42-parse-guard.spec.ts"
      provides: "the T-C DV-RISK-1 4.2-alias-parse standing guard (4 DV-3 fixtures)"
      contains: "spine-player-42"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 47 SC#2 reworded to the DV-2 dual-runtime wording (same PLAYER-02)"
      contains: "frozen spine-player@4.2.111"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PLAYER-02 + Traceability reworded to DV-2 (same ID, not new)"
      contains: "frozen spine-player@4.2.111"
    - path: ".planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-VALIDATION.md"
      provides: "the RESEARCH §7 addendum folded in (T-A..T-D fully instantiated)"
      contains: "dual-viewer-routing"
  key_links:
    - from: "tests/runtime/reg4701-buildsummary-handoff.spec.ts"
      to: "src/main/summary.ts buildSummary"
      via: "drives loadSkeleton→sampleSkeleton→buildSummary on a 4.3 fixture (the REG-47-01 chain)"
      pattern: "buildSummary"
    - from: "tests/runtime/dv1-42-parse-guard.spec.ts"
      to: "spine-player-42 bare spine-core SkeletonJson"
      via: "constructs SkeletonJson from require('spine-player-42')'s core export, parses the 4 DV-3 4.2 fixtures"
      pattern: "spine-player-42"
    - from: ".planning/REQUIREMENTS.md PLAYER-02"
      to: ".planning/ROADMAP.md Phase 47 SC#2"
      via: "the same DV-2 reworded dual-runtime wording (traceability-consistent)"
      pattern: "frozen spine-player@4\\.2\\.111"
---

<objective>
Land the Q4 owed regression tests (47-RESEARCH §4 T-A/T-B/T-C — all HEADLESS,
CLAUDE.md Fact #5) that machine-guard the DV-1 dual-runtime viewer landed by
47-03, and reword the DV-2 source artifacts (ROADMAP SC#2 + REQUIREMENTS
PLAYER-02 + Traceability) from the now-known-impossible "through the 4.3 player"
wording to the locked DV-2 dual-runtime wording — SAME requirement ID, reworded
not new. Fold the RESEARCH §7 Validation Architecture addendum into
47-VALIDATION.md so it is not stale (plan-checker Check 8e blocks on a stub).

Purpose: the dual-runtime routing decision and the cross-runtime handoff are
testable headlessly; the visual render is the 47-05 owner UAT. T-A makes the
deleted REG-47-01 `_dbg-` throwaway a permanent standing regression (the debug
file's explicit owed-item). T-B/T-C are the DV-RISK-1 standing guards: if a bad
npm hoist ever collapses the alias split-brain, they fail loudly. The DV-2
rewording keeps traceability clean and prevents a silent ROADMAP-contract
descope (memory feedback_replan_can_silently_descope_roadmap_contract).

Output: 3 new headless spec files (all with in-repo analogs, no new
framework/fixtures), the DV-2 rewording of ROADMAP/REQUIREMENTS, and the
updated 47-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-CONTEXT.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-RESEARCH.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-VALIDATION.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-03-SUMMARY.md
@.planning/debug/reg-47-01-43-load-reading-r.md

<interfaces>
<!-- Analog test files (all confirmed to exist in-repo) the new specs clone idioms from -->

T-A analog → `tests/runtime/d13-43-load-smoke.spec.ts` + helper `tests/runtime43/load43.ts`
  (the `pickRuntime('4.3')` + loader→sampler→summary chain idiom).
  REG-47-01 fixed: `src/main/summary.ts` `buildSummary` now uses
  `load.runtime.makeSkeleton` (commit 53e480c) instead of the hardcoded 4.2
  `new Skeleton(...)`. The deleted throwaway was
  `tests/runtime43/_dbg-reg4701-full-load-chain.spec.ts` (debug
  `reg-47-01-43-load-reading-r` Resolution `repro_spec_disposition` —
  "SHOULD be authored under the Phase 47 gap-fix flow"). It drove
  `loadSkeleton → sampleSkeleton → buildSummary` and asserted the 4.3 fixture
  does NOT throw `TypeError reading 'r'` (was: at `Color.setFromColor` ←
  `Slot.setToSetupPose` ← `new Slot` ← `new Skeleton` ← `buildSummary`).

T-B analog → `tests/runtime/runtime-distinctness.spec.ts` (Phase-42
  alias-resolution/distinctness, real-module no-mock) + the source-shape idiom
  in `tests/renderer/app-shell-animation-viewer.spec.tsx`.

T-C analog → `tests/runtime43/runtime43-d03.spec.ts`
  (parse-a-real-fixture-through-a-specific-runtime idiom).

DV-3 4.2-leg constraint-mix fixtures (all in-repo, all `spine:"4.2.43/43"`):
- `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` (GL straight-alpha canary; has a path constraint)
- `fixtures/CHJ/CHJWC_SYMBOLS.json` (transform-only)
- `fixtures/3Queens/TQORW_SYMBOLS.json` (ik + transform + events)
- `fixtures/MON_FILES/EXPORT/TEST_03/4.2/TEST_03.json` (ik + transform + physics — most divergent)
4.3 leg / 4.2 control: `fixtures/SIMPLE_PROJECT_43/skeleton2.json` (`spine:"4.3.01"`).

DV-2 exact rewording targets (verified line numbers):
- `.planning/ROADMAP.md` line 183 — Phase 47 Success Criteria item 2 `*(PLAYER-02)*`.
- `.planning/ROADMAP.md` line 186 — the `47-02-PLAN.md` plan-list bullet (re-sequence to the new plans — see Task 3).
- `.planning/REQUIREMENTS.md` line 74 — `- [ ] **PLAYER-02**: …`.
- `.planning/REQUIREMENTS.md` line 147 — Traceability row `| PLAYER-02 | Phase 47 | Pending |`.
The DV-2 locked replacement wording (47-CONTEXT.md DV-2): *"the viewer renders a
4.2 fixture correctly via the frozen spine-player@4.2.111 path AND a 4.3 fixture
via the migrated spine-player@4.3.0 path."*
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: T-A — the permanent REG-47-01 cross-runtime-handoff regression spec</name>
  <files>tests/runtime/reg4701-buildsummary-handoff.spec.ts</files>
  <read_first>
    - .planning/debug/reg-47-01-43-load-reading-r.md (FULL — the root cause, the deleted `_dbg-` spec's exact chain `loadSkeleton → sampleSkeleton → buildSummary`, the verified Node trace `TypeError reading 'r' at Color.setFromColor ← Slot.setToSetupPose ← new Slot ← new Skeleton ← buildSummary summary.ts:300`, the Resolution `repro_spec_disposition` mandating a PERMANENT test under the Phase 47 gap-fix flow)
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-RESEARCH.md §4 the T-A row (analog `tests/runtime/d13-43-load-smoke.spec.ts` + `tests/runtime43/load43.ts`; 4.3 fixture `fixtures/SIMPLE_PROJECT_43/skeleton2.json`; 4.2 control `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`)
    - tests/runtime/d13-43-load-smoke.spec.ts (the analog — copy its node-env loader→chain idiom, fixture-loading pattern, and pickRuntime usage)
    - tests/runtime43/load43.ts (the helper the analog uses for the 4.3 chain seam)
    - src/main/summary.ts lines 320-330 + 543-595 (the post-53e480c buildSummary — it now uses `load.runtime.makeSkeleton`; this test guards that it stays fixed)
  </read_first>
  <behavior>
    - 4.3 case: `loadSkeleton → sampleSkeleton → buildSummary` on `fixtures/SIMPLE_PROJECT_43/skeleton2.json` completes WITHOUT throwing `Cannot read properties of undefined (reading 'r')` (the exact REG-47-01 symptom) and returns a `SkeletonSummary`
    - 4.3 case: the returned summary's `runtimeTag === '4.3'` (the DV-1a field is populated by the same buildSummary path — a free, valuable assertion this test should make)
    - 4.2 control: the SAME chain on `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` completes and the summary's `runtimeTag === '4.2'`
  </behavior>
  <action>
Create `tests/runtime/reg4701-buildsummary-handoff.spec.ts` — the PERMANENT
regression for the REG-47-01 cross-runtime `buildSummary` handoff (debug
`reg-47-01-43-load-reading-r` Resolution explicitly owes this; the original
`_dbg-reg4701-full-load-chain.spec.ts` was a throwaway, never git-tracked).

Clone the node-env structure of `tests/runtime/d13-43-load-smoke.spec.ts`
(fixture path resolution, the `loadSkeleton`/`sampleSkeleton`/`buildSummary`
imports, the `tests/runtime43/load43.ts` helper if it factors the chain). Two
`it()` cases:
1. **"4.3 repro: full chain on skeleton2.json does not throw reading 'r'"** —
   drive `loadSkeleton → sampleSkeleton → buildSummary` on
   `fixtures/SIMPLE_PROJECT_43/skeleton2.json`. Assert it does NOT throw
   (`expect(() => …).not.toThrow()` or an `await` that resolves), AND assert
   the resulting `summary.runtimeTag === '4.3'`. (Pre-53e480c this threw the
   `reading 'r'` TypeError at `buildSummary`; the test is the standing guard
   that the `load.runtime.makeSkeleton` fix is never reverted.)
2. **"4.2 control: full chain on SIMPLE_TEST.json succeeds"** — same chain on
   `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`; assert it completes and
   `summary.runtimeTag === '4.2'`.

This is node-env (no DOM/WebGL — CLAUDE.md Fact #5; the chain is core/main).
Run `npx vitest run tests/runtime/reg4701-buildsummary-handoff.spec.ts` — both
cases GREEN (the REG-47-01 fix 53e480c is in main, so it passes now and stays
the guard).
  </action>
  <verify>
    <automated>test -f tests/runtime/reg4701-buildsummary-handoff.spec.ts && grep -q "buildSummary" tests/runtime/reg4701-buildsummary-handoff.spec.ts && grep -q "skeleton2" tests/runtime/reg4701-buildsummary-handoff.spec.ts && grep -qE "runtimeTag.*['\"]4\.3['\"]" tests/runtime/reg4701-buildsummary-handoff.spec.ts && npx vitest run tests/runtime/reg4701-buildsummary-handoff.spec.ts && echo TA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/runtime/reg4701-buildsummary-handoff.spec.ts` succeeds (the deleted `_dbg-` throwaway is now a permanent git-tracked spec)
    - The spec drives the full `loadSkeleton`/`sampleSkeleton`/`buildSummary` chain (all three symbols grep-present) — NOT just `loadSkeleton` (the Phase-44 D-11 test only asserted loadSkeleton; that is exactly why this escapee was invisible — per the debug file)
    - It asserts the 4.3 fixture does NOT throw the `reading 'r'` symptom AND `summary.runtimeTag === '4.3'`; the 4.2 control asserts `runtimeTag === '4.2'`
    - `npx vitest run tests/runtime/reg4701-buildsummary-handoff.spec.ts` exits 0, both cases pass
    - `git diff --name-only <pre-plan-ref>..HEAD -- src/` is EMPTY (test-only — zero src change)
  </acceptance_criteria>
  <done>tests/runtime/reg4701-buildsummary-handoff.spec.ts is a permanent git-tracked spec driving the full loadSkeleton→sampleSkeleton→buildSummary chain on the 4.3 fixture (no `reading 'r'` throw, runtimeTag 4.3) + the 4.2 control (runtimeTag 4.2), GREEN; zero src change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T-B + T-C — the dual-runtime routing/alias-distinctness regression + the DV-RISK-1 4.2-parse standing guard</name>
  <files>tests/runtime/dual-viewer-routing.spec.ts, tests/runtime/dv1-42-parse-guard.spec.ts</files>
  <read_first>
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-RESEARCH.md §4 the T-B row (analog `tests/runtime/runtime-distinctness.spec.ts` real-module no-mock + `tests/renderer/app-shell-animation-viewer.spec.tsx` source-shape idiom; the two assertions: (1) `spine-player-42` resolves a spine-core whose `version==='4.2.111'` AND lacks `Slider`/`BonePose` while `@esotericsoftware/spine-player` resolves 4.3.0; (2) a `SkeletonSummary` with `runtimeTag:'4.2'` selects the `AnimationPlayerModal42` path, `'4.3'` the migrated path — mock both modal siblings, assert WHICH constructed) AND the T-C row (analog `tests/runtime43/runtime43-d03.spec.ts`; construct `SkeletonJson` from `require('spine-player-42')`'s core export with a null/AtlasAttachmentLoader; the 4 DV-3 4.2 fixtures parse clean, the same via canonical 4.3.0 core THROW) AND §1d (the live end-to-end proof: SIMPLE_TEST parsed clean via aliased core, threw via canonical 4.3.0)
    - tests/runtime/runtime-distinctness.spec.ts (the T-B resolution-arm analog — the real-module-resolution + distinctness idiom)
    - tests/runtime43/runtime43-d03.spec.ts (the T-C analog — parse-a-real-fixture-through-a-specific-runtime)
    - src/renderer/src/modals/AnimationPlayerModalRouter.tsx (the dispatcher under test for the T-B routing arm — created by 47-03 Task 2; assert its branch off `summary.runtimeTag`)
  </read_first>
  <behavior>
    - T-B resolution arm: `require('spine-player-42')`'s transitive spine-core has `version === '4.2.111'` and does NOT export `Slider`/`BonePose` (4.3-only); `require('@esotericsoftware/spine-player')`'s spine-core is `4.3.0` — the alias split-brain is REAL and distinct
    - T-B routing arm: rendering `AnimationPlayerModalRouter` with `summary.runtimeTag:'4.2'` constructs the `AnimationPlayerModal42` mock (NOT the migrated one); with `'4.3'` constructs the migrated `AnimationPlayerModal` mock — the branch is keyed on the tag, never a JSON sniff
    - T-C: `SkeletonJson` reached via `spine-player-42`'s bare core parses SIMPLE_TEST + CHJWC_SYMBOLS + TQORW_SYMBOLS + TEST_03 CLEAN (constraints registered, no `<X> constraint not found`); the SAME 4 fixtures via canonical `@esotericsoftware/spine-core@4.3.0` THROW (the gap is real → the alias is the fix)
  </behavior>
  <action>
**T-B — `tests/runtime/dual-viewer-routing.spec.ts`.** Two describe blocks:
- *Resolution/distinctness arm* (node env, real modules, no mock — clone
  `tests/runtime/runtime-distinctness.spec.ts`): assert
  `require('spine-player-42')`'s reachable spine-core `version === '4.2.111'`
  and that it lacks the 4.3-only `Slider`/`BonePose` exports, while the
  canonical `@esotericsoftware/spine-player` reaches spine-core `4.3.0`. (Use
  `createRequire`/module resolution exactly as the analog + 47-RESEARCH §1c(b)
  shows — `createRequire(<spine-player-42 entry>).resolve('@esotericsoftware/spine-core')`
  → 4.2.111; same from an app file → 4.3.0.)
- *Dispatcher-branch arm* (jsdom env, `vi.mock` BOTH modal siblings —
  `vi.mock('../../src/renderer/src/modals/AnimationPlayerModal42', …)` and
  `vi.mock('../../src/renderer/src/modals/AnimationPlayerModal', …)` with the
  repo's spec import-path convention): render `AnimationPlayerModalRouter` with
  a minimal `summary` whose `runtimeTag:'4.2'` → assert the
  `AnimationPlayerModal42` mock was constructed and the migrated one was NOT;
  repeat with `runtimeTag:'4.3'` → assert the inverse. Assert WHICH mock
  constructed — NEVER a GL render (CLAUDE.md Fact #5). Add a guard assertion
  that the router source contains no JSON-sniff token (mirrors the 47-03
  acceptance — the routing is explicit-identity only).

**T-C — `tests/runtime/dv1-42-parse-guard.spec.ts`** (node env; clone
`tests/runtime43/runtime43-d03.spec.ts`'s parse-a-fixture-through-a-runtime
idiom). For EACH of the 4 DV-3 4.2-leg fixtures
(`fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`, `fixtures/CHJ/CHJWC_SYMBOLS.json`,
`fixtures/3Queens/TQORW_SYMBOLS.json`,
`fixtures/MON_FILES/EXPORT/TEST_03/4.2/TEST_03.json`):
- construct a `SkeletonJson` from `require('spine-player-42')`'s exported core
  (the bare-core reached via the alias) with a null / AtlasAttachmentLoader,
  read the raw fixture JSON → assert it parses CLEAN (constraints registered;
  NO `constraint not found` / `attachment not found` throw).
- construct a `SkeletonJson` from canonical `@esotericsoftware/spine-core@4.3.0`
  with the same loader + JSON → assert it THROWS (the categorical 4.2-vs-4.3
  schema gap — `root.constraints[]` absent; this proves the gap is real and the
  alias is what closes it; 47-RESEARCH §1d proved exactly this for SIMPLE_TEST).
Use `it.each`/a loop over the 4 fixtures so each is an independent assertion
(physics, ik, transform, events, path all covered — the constraint-mix variety
that exposed the gap).

Both specs are headless (no GL). Run
`npx vitest run tests/runtime/dual-viewer-routing.spec.ts tests/runtime/dv1-42-parse-guard.spec.ts`
— all GREEN (47-03 landed the alias + the dispatcher, so these pass and become
the DV-RISK-1 standing guard: a bad future npm hoist that collapses the
split-brain fails them loudly — GA-3).
  </action>
  <verify>
    <automated>test -f tests/runtime/dual-viewer-routing.spec.ts && test -f tests/runtime/dv1-42-parse-guard.spec.ts && grep -q "spine-player-42" tests/runtime/dual-viewer-routing.spec.ts && grep -q "runtimeTag" tests/runtime/dual-viewer-routing.spec.ts && grep -q "spine-player-42" tests/runtime/dv1-42-parse-guard.spec.ts && grep -qE "CHJWC_SYMBOLS|TQORW_SYMBOLS|TEST_03" tests/runtime/dv1-42-parse-guard.spec.ts && npx vitest run tests/runtime/dual-viewer-routing.spec.ts tests/runtime/dv1-42-parse-guard.spec.ts && echo TB_TC_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both spec files exist; `npx vitest run tests/runtime/dual-viewer-routing.spec.ts tests/runtime/dv1-42-parse-guard.spec.ts` exits 0, all cases pass
    - T-B resolution arm asserts the `spine-player-42` reachable spine-core `version === '4.2.111'` AND lacks `Slider`/`BonePose`, AND canonical reaches `4.3.0` (the split-brain is real & distinct)
    - T-B dispatcher arm mocks BOTH modal siblings and asserts the `runtimeTag:'4.2'` case constructs `AnimationPlayerModal42` (not the migrated) and `'4.3'` constructs the migrated (not 42) — i.e. routing is keyed on the tag
    - T-C asserts ALL 4 DV-3 fixtures parse CLEAN via `spine-player-42`'s bare core AND THROW via canonical `@esotericsoftware/spine-core@4.3.0` — `grep -c "TEST_03" tests/runtime/dv1-42-parse-guard.spec.ts` >= 1 AND `grep -c "TQORW_SYMBOLS" …` >= 1 AND `grep -c "CHJWC_SYMBOLS" …` >= 1 AND `grep -c "SIMPLE_TEST" …` >= 1 (the full physics/ik/transform/events constraint-mix)
    - `git diff --name-only <pre-plan-ref>..HEAD -- src/` is EMPTY (test-only — zero src change)
  </acceptance_criteria>
  <done>tests/runtime/dual-viewer-routing.spec.ts (alias-distinctness + dispatcher-branch) and tests/runtime/dv1-42-parse-guard.spec.ts (4 DV-3 fixtures clean via spine-player-42, throw via canonical 4.3) both GREEN — the DV-RISK-1 standing guards; zero src change.</done>
</task>

<task type="auto">
  <name>Task 3: DV-2 — reword PLAYER-02 SC in ROADMAP + REQUIREMENTS + Traceability (same ID, not new) + fold the RESEARCH §7 addendum into 47-VALIDATION.md + re-sequence the ROADMAP plan list</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-VALIDATION.md</files>
  <read_first>
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-CONTEXT.md §"DV-2" (the EXACT locked replacement wording; "this is a reworded PLAYER-02, NOT a new requirement — keep traceability clean"; "the ROADMAP/REQUIREMENTS/traceability text edits are gap-plan work")
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-RESEARCH.md §"7. Validation Architecture — ADDENDUM" (the 4 rows to ADD to 47-VALIDATION.md — instantiate FULLY, not a stub; the Wave-0 note; the §1c(a) dual-stack-bundle grep to promote to a /gsd-verify-work build gate)
    - .planning/ROADMAP.md lines 177-188 (Phase 47 block — line 183 SC#2, line 186 the 47-02-PLAN bullet, line 187 the UI hint)
    - .planning/REQUIREMENTS.md lines 71-79 (PLAYER section, line 74 PLAYER-02) AND lines 142-150 (Traceability, line 147 the PLAYER-02 row)
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-VALIDATION.md (FULL — the existing Per-Task Verification Map + Manual-Only table this addendum extends; do NOT delete the existing rows, ADD)
  </read_first>
  <action>
**Step A — DV-2 rewording (same requirement ID PLAYER-02; reworded, NOT new —
traceability MUST stay clean).** The locked replacement wording from 47-CONTEXT
DV-2 is: *"the viewer renders a 4.2 fixture correctly via the frozen
spine-player@4.2.111 path AND a 4.3 fixture via the migrated
spine-player@4.3.0 path"* (GL straight-alpha re-verified; the 5 carried Phase
41 HUMAN-UATs re-run). Apply to ALL THREE places, preserving the `*(PLAYER-02)*`
tag / `**PLAYER-02**` ID exactly (do NOT add a PLAYER-03; do NOT renumber):
1. `.planning/ROADMAP.md` Phase 47 Success Criteria item 2 (line ~183) —
   replace the "renders both a 4.2 and a 4.3 fixture correctly **through the
   4.3 player**" clause with the DV-2 dual-runtime wording; keep the GL
   straight-alpha + 5-UAT clauses + the trailing `*(PLAYER-02)*`.
2. `.planning/REQUIREMENTS.md` line ~74 `- [ ] **PLAYER-02**: …` — same
   rewording; keep the `**PLAYER-02**` ID and the `- [ ]` checkbox state.
3. `.planning/REQUIREMENTS.md` Traceability row (line ~147)
   `| PLAYER-02 | Phase 47 | Pending |` — leave as `Pending` (47-05's owner
   sign-off flips it to Complete; this plan does NOT complete PLAYER-02). If the
   row carries a description column, reword it to the DV-2 wording; do NOT
   change the ID or the Phase mapping.
Self-check (memory feedback_replan_can_silently_descope_roadmap_contract): after
editing, the GL-straight-alpha clause + the "5 carried Phase 41 HUMAN-UATs"
clause MUST still be present in the ROADMAP SC#2 — DV-2 REWORDS the player-path
half, it does NOT drop the visual-acceptance half. Grep-confirm both clauses
survive.

**Step B — re-sequence the ROADMAP Phase 47 plan list (47-02 disposition).**
The ROADMAP line ~186 currently lists `47-02-PLAN.md` as the PLAYER-02 plan.
47-02 is SUPERSEDED (its Task-1 47-HUMAN-UAT.md was authored for the
now-superseded D-09 matrix; its owner checkpoint never ran). Update the Phase 47
`Plans:` block + bullet list so it reads (keep 47-01 `[x]` unchanged):
- `**Plans**: 4 plans` (was 2): 47-01 (PLAYER-01, complete), then the 3
  gap-closure plans 47-03 / 47-04 / 47-05.
- Replace the `- [ ] 47-02-PLAN.md …` bullet with three bullets:
  - `- [ ] 47-03-PLAN.md — PLAYER-02 (DV-1/DV-1a/DV-NOTE): dual-runtime viewer — npm-alias trio + frozen v1.5.1 4.2-leg modal + runtimeTag routing seam + dispatcher + T-D spec *(PLAYER-02)*`
  - `- [ ] 47-04-PLAN.md — PLAYER-02 (DV-2/Q4): T-A/T-B/T-C headless guards + DV-2 ROADMAP/REQUIREMENTS rewording + 47-VALIDATION update *(PLAYER-02)*`
  - `- [ ] 47-05-PLAN.md — PLAYER-02 (DV-3/D-02/D-08): DV-3-matrix 47-HUMAN-UAT revision + the blocking owner checkpoint:human-action live-UAT (gates v1.6 close, D-01) + in-place flip of 41-HUMAN-UAT.md (subsumes 47-02 Tasks 2-3) *(PLAYER-02)*`
- Add a one-line note under the bullets: `> 47-02-PLAN.md is SUPERSEDED by the
  DV-1..DV-3 gap re-discussion (its D-09-matrix 47-HUMAN-UAT.md is re-authored
  to the DV-3 matrix in 47-05; its owner-checkpoint + 41-flip Tasks are subsumed
  by 47-05). 47-01 is unchanged (the 4.3 leg).`

**Step C — fold the RESEARCH §7 addendum into 47-VALIDATION.md (do NOT leave it
stale — plan-checker Check 8e).** ADD (do not delete the existing rows) the 4
RESEARCH §7 rows to the Per-Task Verification Map, instantiated FULLY with the
real new file names this gap plan creates (NOT placeholders):
- T-A → `tests/runtime/reg4701-buildsummary-handoff.spec.ts` (47-04 T1) —
  `npx vitest run tests/runtime/reg4701-buildsummary-handoff.spec.ts` — ✅ exists.
- T-B → `tests/runtime/dual-viewer-routing.spec.ts` (47-04 T2) — its command —
  ✅ exists.
- T-C → `tests/runtime/dv1-42-parse-guard.spec.ts` (47-04 T2) — its command —
  ✅ exists.
- T-D → `tests/renderer/animation-player-modal-42.spec.tsx` (47-03 T3) — its
  command — ✅ exists.
Also: update the Manual-Only Verifications table to reflect the DV-3 matrix
(the 4.2 leg now = SIMPLE_TEST + the 3 constraint-mix fixtures via the frozen
4.2.111 path; the 4.3 leg = skeleton2.json via the migrated path; the owner UAT
proves routing + alias-isolation-actually-loads + constraint-mix incl.
physics — NOT re-proving the byte-identical 4.2 renderer) and add the
RESEARCH §7 GA-2 note (promote the §1c(a) dual-stack-bundle grep — both
`root.constraints` AND `root.transform`/`root.ik` markers present in the
renderer bundle after `vite build` — to a `/gsd-verify-work` build gate). Keep
the file's frontmatter `nyquist_compliant`/`wave_0_complete` accurate (Wave 0
now has the 4 new owed files — all created by 47-03/47-04, so still satisfiable;
update `wave_0_complete` reasoning text accordingly). Bump `updated:` /
`Approval:` date.
  </action>
  <verify>
    <automated>grep -q 'frozen spine-player@4.2.111' .planning/ROADMAP.md && grep -q 'frozen spine-player@4.2.111' .planning/REQUIREMENTS.md && grep -qE 'straight.?alpha' .planning/ROADMAP.md && grep -c 'PLAYER-03' .planning/REQUIREMENTS.md | grep -qx 0 && grep -q '47-05-PLAN.md' .planning/ROADMAP.md && grep -q 'SUPERSEDED' .planning/ROADMAP.md && grep -q 'dual-viewer-routing' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-VALIDATION.md && grep -q 'reg4701-buildsummary-handoff' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-VALIDATION.md && [ -z "$(git diff --name-only -- src/ tests/)" ] && echo DV2_VALIDATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'frozen spine-player@4.2.111' .planning/ROADMAP.md` >= 1 AND `grep -c 'frozen spine-player@4.2.111' .planning/REQUIREMENTS.md` >= 1 (DV-2 wording applied in both)
    - `grep -c 'through the 4.3 player' .planning/ROADMAP.md` returns `0` AND the same for REQUIREMENTS.md PLAYER-02 (the now-known-impossible wording is GONE from the PLAYER-02 SC — not merely appended)
    - `grep -c 'PLAYER-02' .planning/REQUIREMENTS.md` >= 2 (the requirement line + the Traceability row both still PLAYER-02) AND `grep -c 'PLAYER-03' .planning/REQUIREMENTS.md` returns `0` (reworded, NOT a new requirement — traceability clean)
    - The ROADMAP SC#2 still contains a GL `straight.?alpha` clause AND a "5 carried Phase 41" clause (DV-2 reworded only the player-path half — the visual-acceptance contract was NOT silently descoped; feedback_replan_can_silently_descope_roadmap_contract)
    - `.planning/ROADMAP.md` Phase 47 lists `47-03-PLAN.md`, `47-04-PLAN.md`, `47-05-PLAN.md` AND contains the `SUPERSEDED` note for 47-02 AND `**Plans**: 4 plans` (re-sequenced; 47-01 bullet unchanged `[x]`)
    - `.planning/REQUIREMENTS.md` Traceability `PLAYER-02` row still maps to `Phase 47` and is still `Pending` (this plan does NOT complete PLAYER-02 — 47-05's owner sign-off does)
    - `47-VALIDATION.md` contains the real new filenames `reg4701-buildsummary-handoff`, `dual-viewer-routing`, `dv1-42-parse-guard`, `animation-player-modal-42` (fully instantiated — NOT a stub/placeholder; plan-checker Check 8e) AND the existing pre-existing rows are still present (ADDED, not replaced) AND the `updated:`/`Approval:` date is bumped
    - `git diff --name-only <pre-plan-ref>..HEAD -- src/ tests/` is EMPTY (this task is planning-docs-only — zero code/test change)
  </acceptance_criteria>
  <done>PLAYER-02 reworded to the DV-2 dual-runtime wording in ROADMAP SC#2 + REQUIREMENTS + Traceability (same ID, no PLAYER-03, GL-alpha + 5-UAT clauses preserved); the ROADMAP Phase 47 plan list re-sequenced to 47-01/03/04/05 with the explicit 47-02-SUPERSEDED note; 47-VALIDATION.md folds the RESEARCH §7 addendum with the real new filenames + the DV-3 Manual-Only matrix; zero code/test diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm dependency resolution → the alias split-brain | T-B/T-C are the standing machine guard on the trust boundary 47-03 established: `spine-player-42`'s transitive `@esotericsoftware/spine-core` MUST stay nested at 4.2.111 and never dedupe onto canonical 4.3.0. A future `npm ci`/hoist that collapses it is a silent correctness break; these specs make it loud. |
| ROADMAP/REQUIREMENTS contract ↔ the delivered scope | The DV-2 rewording edits the milestone-close success contract. The boundary risk: a rewording that silently DROPS the GL-alpha or 5-UAT half (descope) while appearing to only "reword the player path" (feedback_replan_can_silently_descope_roadmap_contract). |
| 47-VALIDATION.md ↔ plan-checker Check 8e | A stale/stub VALIDATION.md is a green-wash of the validation contract — the checker blocks on it; the addendum must be fully instantiated with real filenames. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-11 | Tampering / Repudiation (silent ROADMAP descope) | the DV-2 rewording of PLAYER-02 SC#2 | mitigate | The DV-2 edit is constrained to the player-path clause; Task 3 acceptance criteria machine-assert (a) the GL `straight.?alpha` clause AND the "5 carried Phase 41" clause SURVIVE in ROADMAP SC#2, (b) the requirement stays ID `PLAYER-02` with `grep -c PLAYER-03 == 0` (reworded, not a covert new/dropped requirement), (c) the Traceability row still maps PLAYER-02→Phase 47. This makes the "silently descoped a ROADMAP contract" failure class (the named memory lesson) a hard task-failing gate, not a reviewer's eyeball check. |
| T-47-12 | Tampering (alias split-brain regression) | the `spine-player-42` nested-core resolution | mitigate | T-B (resolution arm) + T-C (parse arm over all 4 DV-3 fixtures) are permanent CI-run guards: if a future hoist collapses the split-brain, T-B's `version==='4.2.111'`/`Slider`-absent assertion fails and T-C's clean-parse-vs-canonical-throw assertion fails — loudly, in `npm test`, before any release. The committed lockfile is the prevention (GA-3); these tests are the detection. |
| T-47-13 | Repudiation / false-green (stale validation contract) | `47-VALIDATION.md` | mitigate | Task 3 instantiates the RESEARCH §7 addendum with the LITERAL new filenames (`reg4701-buildsummary-handoff`, `dual-viewer-routing`, `dv1-42-parse-guard`, `animation-player-modal-42`) and acceptance criteria grep-assert each is present and the pre-existing rows are retained (added, not replaced) — defeating the stub/placeholder green-wash that plan-checker Check 8e blocks on (memory feedback_instantiate_validation_md_from_research). |
| T-47-14 | Information Disclosure | the headless parse specs reading fixture JSON | accept | T-A/T-B/T-C read only in-repo test fixtures (no PII; the app is a desktop analysis tool — RESEARCH Security Domain V5 N/A) in node/jsdom test env; no network, no new surface. Proportional residual. |
</threat_model>

<verification>
**Plan-level gates (machine, before 47-05):**

1. T-A `tests/runtime/reg4701-buildsummary-handoff.spec.ts` GREEN — full
   `loadSkeleton→sampleSkeleton→buildSummary` chain, 4.3 no `reading 'r'` throw
   + `runtimeTag` 4.3, 4.2 control `runtimeTag` 4.2 (Task 1).
2. T-B `tests/runtime/dual-viewer-routing.spec.ts` + T-C
   `tests/runtime/dv1-42-parse-guard.spec.ts` GREEN — alias distinctness +
   dispatcher branch + the 4 DV-3 fixtures clean-via-42/throw-via-canonical
   (Task 2).
3. DV-2: `grep 'frozen spine-player@4.2.111'` hits ROADMAP + REQUIREMENTS;
   `grep 'through the 4.3 player'` is 0 in the PLAYER-02 SC; `grep PLAYER-03`
   is 0; the GL-alpha + 5-UAT clauses survive in ROADMAP SC#2; the ROADMAP
   plan list shows 47-03/04/05 + the 47-02-SUPERSEDED note (Task 3).
4. 47-VALIDATION.md folds the §7 addendum with the real filenames, pre-existing
   rows retained, date bumped (Task 3).
5. **Blast-radius (binding):** `git diff --name-only <pre-plan-ref>..HEAD`
   touches ONLY the 3 new spec files + `.planning/ROADMAP.md` +
   `.planning/REQUIREMENTS.md` + `47-VALIDATION.md` + this plan's docs. ZERO
   `src/` change; the new specs do not modify any existing source; no CSP/CORS;
   no `vitest.config.ts`/`electron.vite.config.ts` change.
6. `npm test` → no NEW failures vs the 47-03 post-state (the 3 new specs add
   green cases); `typecheck:node` not worsened vs its documented
   pre-existing-RED baseline (memory `project_typecheck_node_preexisting_red`).

This plan is autonomous (no checkpoint) and worktree-safe; it may run in an
isolated worktree. It depends on 47-03 (T-B/T-C exercise the alias + the
dispatcher 47-03 created).
</verification>

<success_criteria>
- T-A permanent REG-47-01 cross-runtime-handoff regression GREEN (the deleted
  `_dbg-` throwaway is now a git-tracked standing guard).
- T-B dual-runtime routing + alias-distinctness regression GREEN; T-C the
  DV-RISK-1 4.2-parse standing guard GREEN over all 4 DV-3 constraint-mix
  fixtures (clean via spine-player-42, throw via canonical 4.3).
- DV-2: PLAYER-02 SC#2 reworded to the dual-runtime wording in ROADMAP +
  REQUIREMENTS + Traceability — SAME ID, no PLAYER-03, the GL-alpha + 5-UAT
  clauses preserved (no silent descope), the now-impossible "through the 4.3
  player" wording removed.
- The ROADMAP Phase 47 plan list re-sequenced (47-01/03/04/05) with the
  explicit 47-02-SUPERSEDED disposition note.
- 47-VALIDATION.md folds the RESEARCH §7 addendum, fully instantiated with the
  real new filenames + the DV-3 Manual-Only matrix (no stub — Check 8e).
- Blast-radius: zero `src/` diff; tests + planning docs only; no CSP/CORS.
- PLAYER-02 (reworded) machine-checkable half complete — the owner visual UAT
  is 47-05.
</success_criteria>

<output>
After completion, create
`.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-04-SUMMARY.md`
recording: the T-A/T-B/T-C spec commits (all GREEN, with the analog each
cloned); the DV-2 rewording commit (the exact before→after of PLAYER-02 SC#2 in
all 3 places + the explicit confirmation the GL-alpha + 5-UAT clauses survived
and no PLAYER-03 was introduced); the ROADMAP plan-list re-sequence + the
47-02-SUPERSEDED note; the 47-VALIDATION.md addendum commit; and confirmation
of zero `src/` diff.
</output>
