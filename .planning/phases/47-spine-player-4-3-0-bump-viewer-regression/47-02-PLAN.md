---
phase: 47-spine-player-4-3-0-bump-viewer-regression
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md
  - .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md
autonomous: false
requirements: [PLAYER-02]
must_haves:
  truths:
    - "[PLAYER-02] D-08: a new owner-signed 47-HUMAN-UAT.md exists covering all 5 carried Phase 41 UATs (tests 2-6) + a GL straight-alpha SIMPLE_TEST/skeleton2 check + the D-09 4.2/4.3 render-pair test"
    - "[PLAYER-02] D-02: an explicit checkpoint:human-action (gate=blocking) requires the owner to run the real Electron app and execute every 47-HUMAN-UAT.md item live, signing each off — the phase does not complete and v1.6 does not close until every item is signed passed"
    - "[PLAYER-02] D-09: the render pair is FIXED — fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json (4.2 GL-alpha canary) + fixtures/SIMPLE_PROJECT_43/skeleton2.json (4.3 ORCL-01 sibling); both render correctly through the 4.3 player"
    - "[PLAYER-02] GL straight-alpha is empirically re-verified on the 4.3 player: no white/dark halo on SIMPLE_TEST mesh-attachment edges vs #232732 bg; a screenshot is captured in 47-HUMAN-UAT.md so the observation is durable (sharp/libvips PMA reasoning does NOT transfer — the owner's eyes on the GL canvas is the only valid evidence)"
    - "[PLAYER-02] D-06: same-framing parity — the 4.2 rig looks identical through the 4.3 player (no auto-fit/zoom/position drift vs the v1.5.1 framing); drift counts as a regression the owner UAT catches"
    - "[PLAYER-02] D-04 must-not-regress: the Phase 41 content-less-STOP-animation graceful degradation (return null, not the fatal showError) is an explicit owner-checked acceptance criterion (UAT terminal-error path)"
    - "[PLAYER-02] D-08: the 5 pending items in 41-HUMAN-UAT.md (tests 2-6) are flipped IN PLACE to resolved with a Phase 47 pointer; the original expected prose + the test-1 gaps block are preserved (audit trail kept)"
    - "[informational] D-01: v1.6 milestone close is HELD until this owner checkpoint signs every item green — no revert fallback; the roadmap 'decoupled + revertible' framing is consciously overridden"
    - "[informational] D-07: --skip-ui — the visual acceptance contract is this 5-UAT + GL-alpha owner checkpoint, NOT a UI-SPEC; zero new UI is designed"
    - "[informational] Pitfall 4 / security: the owner UAT does NOT broaden CSP/CORS; the atlas-less-parity UAT re-confirms the asset path resolves under the unchanged origin scope"
  artifacts:
    - path: ".planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md"
      provides: "Owner-signed Phase 47 viewer-regression UAT record (5 Phase 41 UATs + GL-alpha + D-09 pair)"
      contains: "approved_by: user"
    - path: ".planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md"
      provides: "In-place flip of the 5 pending items to resolved with a Phase 47 pointer (audit trail preserved)"
      contains: "Phase 47"
  key_links:
    - from: ".planning/phases/47-.../47-HUMAN-UAT.md"
      to: "fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json + fixtures/SIMPLE_PROJECT_43/skeleton2.json"
      via: "the D-09 fixed render pair referenced in the UAT Setup + the GL-alpha test"
      pattern: "SIMPLE_TEST|skeleton2"
    - from: ".planning/phases/41-.../41-HUMAN-UAT.md"
      to: ".planning/phases/47-.../47-HUMAN-UAT.md"
      via: "the in-place resolved-pointer on tests 2-6"
      pattern: "Phase 47"
---

<objective>
Deliver the human half of Phase 47 (PLAYER-02): author the owner-signed
`47-HUMAN-UAT.md` (the 5 carried Phase 41 UATs + a GL straight-alpha re-verify +
the D-09 4.2/4.3 render-pair), then the **blocking owner
`checkpoint:human-action`** in which the owner runs the real Electron app,
executes every item live on the 4.3 player, and signs each off — and flip the 5
pending items in `41-HUMAN-UAT.md` in place to resolved with a Phase 47 pointer.

Purpose: jsdom has no WebGL — the GL-halo, same-framing parity (D-06), scrub
synchrony, GL-leak, real-fs error UI, atlas-less parity, and File-menu
suppression are **only** observable by the owner rendering the D-09 pair in a
real GL context. Per D-01 v1.6 milestone close is HELD on this checkpoint (no
revert fallback). Per D-02 it is an explicit in-phase `checkpoint:human-action`
gating phase completion (Phase 46-style: an auto task authors the spec, the
owner executes + signs it). This is the visual acceptance contract — there is no
UI-SPEC (D-07 `--skip-ui`).

Output: `47-HUMAN-UAT.md` (NEW, owner-signed) + the in-place flip of
`41-HUMAN-UAT.md`'s 5 pending items (audit trail preserved). This is the last
v1.6 deliverable — when the owner signs, the milestone can close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-CONTEXT.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-RESEARCH.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-PATTERNS.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-01-SUMMARY.md
@.planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md

<isolation-note>
This plan carries a `checkpoint:human-action` (Task 2). Per CONTEXT
"Established Patterns" + PATTERNS.md: owner-action checkpoints are
**incompatible with agent-worktree isolation** (fresh-worktree-per-Task
continuation breaks the owner's live session). Phase 46 ran sequential on the
main tree for exactly this reason. **This plan's wave MUST run sequentially on
the main tree, NOT in a worktree.** The orchestrator must disable worktree
isolation for Wave 2.
</isolation-note>

<analog-refs>
Structural analog for Task 1 (authoring 47-HUMAN-UAT.md):
- Content source: `.planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md`
  tests 2-6 (the 5 carried tests — copy `expected:` prose verbatim) + the
  resolved G-01/G-02/G-03 (the regressions the bump must not reintroduce).
- Signed-off shape: `.planning/phases/36-split-overrides-per-loader-mode/36-HUMAN-UAT.md`
  front-matter L1-9 (the `status: passed` / `approved_by: user` /
  `approved_at:` signed-off YAML front-matter shape) + the `## Setup` block
  shape L13-19 + the richer per-test `setup:`/`expected:`/`why_human:`/`result:`
  shape L23-31. (Phase 41's L1-7 is the UNSIGNED `partial` shape — do NOT copy
  that end-state.)
- GL-alpha test content: RESEARCH.md "GL Straight-Alpha" section "Concrete
  empirical re-verification method".

Structural analog for Task 2 (the owner checkpoint:human-action):
- `.planning/phases/46-slider-constraint-validation-4-3-performance-budget/46-01-PLAN.md`
  Task 2 (L164-194) — the most recent completed owner-action checkpoint; same
  XML structure / attributes / required sub-elements / two-task
  author-then-owner-execute shape.

Analog for Task 3 (in-place flip of 41-HUMAN-UAT.md):
- The SAME file's already-present resolved-marker pattern: test-1's `gaps:`
  block (41-HUMAN-UAT.md L18-33) shows the exact `status: resolved` +
  `fixed_in: <sha> <msg>` provenance convention to mirror for the Phase 47
  pointer.
</analog-refs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 47-HUMAN-UAT.md — the 5 carried Phase 41 UATs + GL straight-alpha re-verify + the D-09 render-pair, owner-signature-ready</name>
  <files>.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md (FULL FILE — the 5 carried tests are tests 2-6 there; copy each `expected:` prose VERBATIM; also read the resolved G-01/G-02/G-03 under test 1 — those are the regressions the 4.3 bump must not reintroduce, especially G-03 straight-alpha)
    - .planning/phases/36-split-overrides-per-loader-mode/36-HUMAN-UAT.md (L1-40 — the SIGNED-OFF front-matter shape status:passed / approved_by:user / approved_at:, the `## Setup` block shape, and the richer per-test setup:/expected:/why_human:/result: shape)
    - .planning/phases/47-.../47-RESEARCH.md section "GL Straight-Alpha" (lines 289-320 — the verified 4.3.0 mechanism + the "Concrete empirical re-verification method" bullet list) AND "Validation Architecture" -> "Phase Requirements -> Test Map" (lines 473-486 — the PLAYER-02 human rows) AND "Sampling Rate" (lines 487-491 — the visual-regression detectability note)
    - .planning/phases/47-.../47-PATTERNS.md section "47-HUMAN-UAT.md (NEW planning artifact)" (lines 283-344 — the exact front-matter to copy, the Setup block, the per-test shape choice, the "Content to port" list mapping each test to its 41-HUMAN-UAT line range, the +GL-alpha test, the +D-09 pair test, the `## Summary` counter shape)
    - .planning/phases/47-.../47-01-SUMMARY.md (the just-landed migration — so the UAT references the correct built state: spine-player@4.3.0, the migrated modal; confirm whether the D-05 audit pulled in any extra rig — RESEARCH D-05 verdict said it did not)
  </read_first>
  <action>
Create `.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md`
as the owner-signature-ready Phase 47 viewer-regression UAT record. Copy the
SHAPE from `36-HUMAN-UAT.md` (the signed-off end-state) and the CONTENT from
`41-HUMAN-UAT.md` tests 2-6 (verbatim `expected:` prose).

Front-matter: copy the `36-HUMAN-UAT.md` L1-9 signed-off YAML front-matter shape
(open and close it with the standard triple-dash YAML front-matter fences,
exactly as `36-HUMAN-UAT.md` does at its top). Leave the approval fields BLANK
for the owner/Task-2 to fill — do NOT pre-fill `approved_by` before the owner
signs. The YAML keys/values, in front-matter order:
  - status: pending   (the owner flips this to `passed` at Task 2 sign-off)
  - phase: 47-spine-player-4-3-0-bump-viewer-regression
  - source: [47-VERIFICATION.md]
  - started: 2026-05-18
  - updated: 2026-05-18
  - approved_by:   (empty — owner fills at sign-off in Task 2)
  - approved_at:   (empty — owner fills at sign-off in Task 2)

`## Setup` block (copy the `36-HUMAN-UAT.md` L13-19 bullet shape):
- App build: `npm run dev` (Electron dev app — the migration lives in renderer
  source; NOT the packaged installer).
- Fixtures (the D-09 FIXED pair — do not substitute):
  `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` (4.2 GL-alpha canary; Phase 41 G-03
  was reproduced on it) + `fixtures/SIMPLE_PROJECT_43/skeleton2.json`
  (`spine:"4.3.01"`, the 4.3 ORCL-01 SIMPLE_TEST-equivalent sibling). Note: the
  D-05 audit added NO extra rig (no alpha/render risk flagged — confirm from
  47-01-SUMMARY / RESEARCH D-05 verdict), so the pair is exactly these two.
- Reset between tests: close the viewer modal and reload the project between
  each test (mirror `36-HUMAN-UAT.md`'s reset bullet).

Per-test shape: use the richer `36-HUMAN-UAT.md` shape consistently —
`### N. <title>` then `setup:` / `expected:` / `why_human:` / `result:`
sub-blocks. Author these 7 tests:

1. GL straight-alpha re-verify on the 4.3 player (the hard floor — NEVER skip).
   `setup:` `npm run dev`, load `SIMPLE_TEST.json`, open the Animation Viewer.
   `expected:` (from RESEARCH GL section "Concrete empirical re-verification
   method") — mesh-attachment edges (CIRCLE/SQUARE/TRIANGLE region + mesh
   attachments with transparent border pixels) anti-alias cleanly into the
   `#232732` panel background with **no opaque-white/bright ring or fringe** and
   **no dark/double-darkened seam** on semi-transparent pixels; visually
   identical to the v1.5.1 (4.2.111) render of the same rig at the same
   pose/zoom (D-06 same-framing parity). Then load `skeleton2.json` (the 4.3
   leg) and confirm the same clean-edge result (rules out a 4.3-fixture-specific
   texture/atlas interaction). `why_human:` jsdom has no WebGL; the 4.3.0
   spine-webgl premultiply mechanism MOVED (`PolygonBatcher.setBlendMode`'s pma
   arg deleted -> decision now at `GLTexture` `UNPACK_PREMULTIPLY_ALPHA_WEBGL =
   !pma`); the Phase 41 G-03 note cites a now-deleted code path; the
   `sharp`/`libvips` `project_pma_no_op_in_current_stack` reasoning does NOT
   transfer to this GL path; `scripts/pma-probe.mjs` does NOT cover it — the
   owner's eyes on the rendered canvas, screenshotted, is the only valid
   evidence. Require a screenshot embedded/linked in this file so the
   observation is durable and re-checkable. `result:` `[pending]`.
2. D-09 render pair — both 4.2 and 4.3 render correctly through the 4.3 player
   (PLAYER-02 SC#1). `expected:` `SIMPLE_TEST.json` (4.2) AND `skeleton2.json`
   (4.3) each paint their skeleton's first animation looping continuously in the
   near-fullscreen card against `#232732`, no blank canvas, no 1x1px collapse,
   no DevTools error spam. `why_human:` cross-major GL render correctness on the
   two fixed fixtures; jsdom cannot render. `result:` `[pending]`.
3. VIEWER-05 + VIEWER-06 anim/skin switch + scrub-pose synchrony (forward AND
   backward) — the T6 acceptance gate. Copy `expected:` VERBATIM from
   `41-HUMAN-UAT.md` test 2 (L36 — includes the WR-05 backward-scrub caveat:
   backward scrub uses negative `animationState.update(delta)`). `why_human:`
   T6 reworked the scrub from the private `p.playTime` to a TrackEntry-driven
   seek (RESEARCH D-05 T6 / Pitfall 5 / Assumption A1) — behavioral equivalence
   is reasoned, not machine-tested; forward+backward scrub synchrony is the only
   valid acceptance gate. `result:` `[pending]`.
4. VIEWER-08 GL leak across 10 open/close cycles. Copy `expected:` VERBATIM from
   `41-HUMAN-UAT.md` test 3 (L40). `why_human:` GPU memory growth is only
   observable in a real GL context across real cycles. `result:` `[pending]`.
5. VIEWER-09 real-fs malformed/missing-asset terminal error UI (the D-04
   must-not-regress check). Copy `expected:` VERBATIM from `41-HUMAN-UAT.md`
   test 4 (L44). Add an explicit acceptance line: the content-less /
   STOP-state path still degrades gracefully (the viewer shows the terminal
   error overlay / returns cleanly — it does NOT fatal-crash via spine-player's
   native `showError`); the custom resilient `sampleAnimationBounds` `null`
   return is preserved (D-04 invariant; RESEARCH D-04 proved the apply()
   migration does not regress it). `why_human:` real-fs corruption + the
   resilient-path behavior under the migrated apply() is only verifiable live.
   `result:` `[pending]`.
6. VIEWER-04 atlas-less visual parity (also the S12 / Pitfall-4 asset-origin
   re-confirm). Copy `expected:` VERBATIM from `41-HUMAN-UAT.md` test 5 (L48).
   `why_human:` atlas-less `rawDataURIs` parent-relative resolution is an S12
   render-behavior touchpoint (API-stable but Pose-rewrite major) AND it
   re-confirms the asset path resolves under the UNCHANGED CSP/`app-image://`
   origin scope (no broadening — security guardrail). `result:` `[pending]`.
7. File-menu auto-suppression contract while the viewer is open. Copy
   `expected:` VERBATIM from `41-HUMAN-UAT.md` test 6 (L52). `why_human:`
   OS-native menu state is a host-integration behavior; jsdom has no native
   menu. `result:` `[pending]`.

`## Summary` block (copy the `41-HUMAN-UAT.md` L55-62 counter shape):
total: 7 / passed: 0 / issues: 0 / pending: 7 / skipped: 0 / blocked: 0.
(Task 2 — the owner — flips these to all-passed at sign-off.)

Add a short `## Provenance` note: "Tests 3-7 = the 5 carried Phase 41 UATs
(`41-HUMAN-UAT.md` tests 2-6) re-run on the 4.3 player; tests 1-2 are the
Phase 47 GL-alpha re-verify + D-09 render-pair additions. D-08: this file +
the in-place flip of `41-HUMAN-UAT.md` preserve both audit trails."

Do NOT pre-sign (`approved_by`/`approved_at` blank, `result: [pending]`,
`status: pending`) — the owner signs at Task 2. Do NOT broaden any CSP/CORS or
add a "verify/adjust CSP" item (RESEARCH Pitfall 4 — the atlas-less UAT only
*re-confirms* the unchanged scope).
  </action>
  <verify>
    <automated>test -f .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -Eqi 'SIMPLE_TEST' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -Eqi 'skeleton2' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -Eqi 'straight.?alpha|halo' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && echo UAT_AUTHORED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md` succeeds
    - `grep -c '^### ' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md` returns `7` (the 5 carried Phase 41 UATs + GL-alpha + D-09 render-pair)
    - The file contains the literal strings `SIMPLE_TEST` AND `skeleton2` (the D-09 fixed render pair) AND a GL straight-alpha / halo test
    - `grep -Eci 'result:\s*\[pending\]' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md` returns `7` (every test starts pending — the owner has not signed yet; Task 2 flips them)
    - The front-matter `approved_by:` is BLANK and `status:` is `pending` (NOT pre-signed)
    - The 5 carried `expected:` prose blocks are present (the test exercising "10 open/close cycles", the one mentioning "scrub" + "backward", the one mentioning "terminal error", the one mentioning "atlas-less", the one mentioning "File menu") — grep each phrase returns >= 1
    - The file contains zero new CSP/CORS-broadening instruction (no `connect-src` directive widening text; the atlas-less test only re-confirms the unchanged scope)
  </acceptance_criteria>
  <done>47-HUMAN-UAT.md authored with 7 tests (GL-alpha + D-09 pair + the 5 verbatim-ported Phase 41 UATs), the 36-HUMAN-UAT signed-off front-matter/Setup/per-test shape, all result:[pending], approved_by blank, the D-09 fixed pair + screenshot requirement + D-04 must-not-regress acceptance line + the Provenance note present.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: OWNER — run the real Electron app, execute all 7 47-HUMAN-UAT.md items live on the 4.3 player, sign each off</name>
  <files>.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md (authored by Task 1 — the 7 tests + Setup the owner executes)
    - .planning/phases/47-.../47-RESEARCH.md section "GL Straight-Alpha" (lines 289-320 — so the owner knows exactly what halo/fringe artifact to look for and that the Phase 41 G-03 note describes a now-deleted mechanism)
  </read_first>
  <action>
This is a `checkpoint:human-action` — it has NO CLI/API equivalent. It requires
a human running the real Electron app and visually inspecting a live WebGL
canvas; jsdom has no WebGL, so no machine test can produce this evidence. Per
D-01 this gate **holds v1.6 milestone close** until the owner signs every item
(there is NO revert fallback — D-03 proved the bump is mechanically
non-revertible). Claude cannot automate this — pause and present the steps below
to the owner, then resume on the owner's signal.

WHAT TASK 1 BUILT: `47-HUMAN-UAT.md` — 7 tests (the GL straight-alpha
re-verify, the D-09 4.2/4.3 render-pair, and the 5 carried Phase 41 UATs
re-targeted at the 4.3 player), each with `setup:`/`expected:`/`why_human:` and
a `result: [pending]` the owner fills.

WHAT PLAN 01 BUILT (already landed, machine-green): spine-player bumped to
4.3.0, `AnimationPlayerModal.tsx` migrated (8 touchpoints), the lockstep test
mock migrated; `typecheck:web` 0, the 11 renderer suites GREEN, full `npm test`
no new failures.

OWNER STEPS:
1. `npm run dev` (the Electron dev app — the migration lives in renderer
   source; do NOT use the packaged installer).
2. Load `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`, open the Animation Viewer.
3. Test 1 — GL straight-alpha (the hard floor): inspect the mesh-attachment
   edges (CIRCLE/SQUARE/TRIANGLE region + mesh attachments with transparent
   borders) against the `#232732` panel background. PASS = edges anti-alias
   cleanly with **no opaque-white/bright ring or fringe** and **no
   dark/double-darkened seam**, visually identical to the v1.5.1 (4.2.111)
   render at the same pose/zoom (no auto-fit/zoom/position drift — D-06). Then
   load `fixtures/SIMPLE_PROJECT_43/skeleton2.json` and confirm the same
   clean-edge result. **Capture a screenshot of the SIMPLE_TEST render and
   embed/link it in `47-HUMAN-UAT.md` test 1** so the observation is durable.
   If a halo/fringe appears: do NOT guess — the RESEARCH "GL Straight-Alpha"
   diagnostic map says the 4.3.0 lever is now `pma` ->
   `UNPACK_PREMULTIPLY_ALPHA_WEBGL` (NOT the deleted `setBlendMode` pma arg);
   record the artifact + a screenshot and report it as a regression (do not
   self-fix in this checkpoint).
4. Test 2 — D-09 render pair: confirm both `SIMPLE_TEST.json` (4.2) and
   `skeleton2.json` (4.3) paint the first animation looping in the
   near-fullscreen card, no blank canvas / 1x1 collapse / DevTools error spam.
5. Tests 3-7 — the 5 carried Phase 41 UATs per `47-HUMAN-UAT.md`:
   (3) anim/skin switch + scrub-pose synchrony **forward AND backward**
   (the T6-rework gate — drag the scrub bar both directions, confirm coherent
   poses + pause-on-scrub + no leftover-skin bleed);
   (4) GL leak — open/close the viewer 10x watching DevTools GPU Memory (stays
   flat) + switch projects while open (clean close, no GL warning);
   (5) real-fs malformed/missing-asset terminal error UI — truncate a `.json`
   or remove a referenced PNG, confirm the verbatim terminal error overlay with
   disabled controls and that it does NOT fatal-crash (the resilient
   `sampleAnimationBounds` `null` path is preserved — D-04);
   (6) atlas-less visual parity — load an atlas-less project (no `.atlas`), same
   character/poses as the atlas-source equivalent, no region misalignment / PMA
   glitch / missing slot;
   (7) File-menu auto-suppression — with the viewer open, Save/Save As/Reload
   greyed out, Cmd-S a no-op, restored on close.
6. In `47-HUMAN-UAT.md`, set each test's `result:` to `passed` (or describe the
   regression if any test fails). Then set the front-matter `status: passed`,
   `approved_by: user`, `approved_at: <today's date>`, and update the
   `## Summary` counters (`passed: 7`, `pending: 0`). Commit the file (and the
   screenshot).

RESUME SIGNAL: the owner types "done" once every test in `47-HUMAN-UAT.md` is
signed `passed`, the front-matter is signed (`approved_by: user`), and the
GL-alpha screenshot is captured — OR reports which test regressed (in which
case v1.6 close stays held per D-01 and the regression is triaged).
  </action>
  <verify>
    <automated>test -f .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -qi 'approved_by:[[:space:]]*user' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && ! grep -Eqi 'result:[[:space:]]*\[pending\]' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && echo OWNER_SIGNED_OK || echo NOT_YET_SIGNED</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md` succeeds
    - `grep -qi 'approved_by:[[:space:]]*user' 47-HUMAN-UAT.md` matches (the owner signed the front-matter)
    - `grep -Eci 'result:\s*\[pending\]' 47-HUMAN-UAT.md` returns `0` (every test resolved by the owner — no item left pending)
    - The front-matter `status:` is `passed` and `approved_at:` is non-empty
    - `grep -Eci 'result:\s*passed' 47-HUMAN-UAT.md` returns `7` (all 7 tests signed passed — the gate condition for v1.6 close per D-01; if any test regressed instead, this is < 7 and the phase does NOT complete)
    - The Test-1 GL-alpha entry references a captured screenshot (the durable visual evidence — RESEARCH GL section requirement)
  </acceptance_criteria>
  <done>The owner ran the real Electron app, executed all 7 47-HUMAN-UAT.md items on the 4.3 player (GL straight-alpha + D-09 pair + the 5 carried Phase 41 UATs), captured the GL-alpha screenshot, and signed every test passed + the front-matter (approved_by: user, status: passed). v1.6 milestone-close gate satisfied (D-01).</done>
</task>

<task type="auto">
  <name>Task 3: Flip the 5 pending items in 41-HUMAN-UAT.md in place to resolved with a Phase 47 pointer (audit trail preserved)</name>
  <files>.planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md (FULL FILE — the 5 pending items are tests 2-6 result:[pending] at L37,41,45,49,53; the `## Current Test` line L9-11; the `## Summary` counters L57-62; the test-1 gaps: block L18-33 is the resolved-marker provenance pattern to mirror and MUST be preserved)
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md (the owner-signed Task-2 artifact — the pointer target; note its approved_at date for the provenance line)
    - .planning/phases/47-.../47-PATTERNS.md section "41-HUMAN-UAT.md — in-place flip" (lines 347-369 — the exact edit pattern: which lines, the fixed_in:-style pointer shape, preserve the original expected: prose + test-1 gaps: block)
  </read_first>
  <action>
Flip the 5 pending items in `41-HUMAN-UAT.md` IN PLACE to resolved with a
Phase 47 pointer (D-08 — this preserves the Phase 41 audit trail AND records the
Phase 47 re-run; both records are the point of D-08). This runs AFTER Task 2 so
the pointer can cite the owner-signed `47-HUMAN-UAT.md` + its sign-off date.

For each of tests 2-6, change the `result: [pending]` line (L37, L41, L45, L49,
L53) to a resolved status WITH a Phase 47 provenance pointer, mirroring the
test-1 `gaps:` `fixed_in: <sha> <msg>` provenance shape, e.g.:
`result: resolved — re-run on the 4.3 player in Phase 47; see 47-HUMAN-UAT.md test N (owner-signed <approved_at date from 47-HUMAN-UAT.md>)`

Map each Phase 41 test to its Phase 47 counterpart: 41 test 2 (anim/skin +
scrub) -> 47 test 3; 41 test 3 (GL leak) -> 47 test 4; 41 test 4 (real-fs
error) -> 47 test 5; 41 test 5 (atlas-less parity) -> 47 test 6; 41 test 6
(File-menu) -> 47 test 7. (47 tests 1-2 are the new GL-alpha + D-09-pair
additions and have no Phase 41 counterpart — do not invent a pointer for them.)

Also update:
- The `## Current Test` line (L9-11, currently `[items 2, 3, 4, 5, 6 still
  pending — run /gsd-verify-work 41 to resume]`) -> a resolved statement, e.g.
  `[all items resolved — tests 2-6 re-run + owner-signed on the 4.3 player in
  Phase 47; see 47-HUMAN-UAT.md]`.
- The `## Summary` counters (L57-62): `passed: 1` -> `passed: 6`,
  `pending: 5` -> `pending: 0` (total stays 6; issues/skipped/blocked
  unchanged).

**PRESERVE the Phase 41 audit trail:** do NOT delete or alter any original
`expected:` prose, and do NOT touch the test-1 `gaps:` block (L18-33, the
resolved G-01/G-02/G-03 record). Only flip the 5 `result:` lines + the
`## Current Test` line + the 2 `## Summary` counters, and add the forward
pointer text. The whole point of D-08 is keeping BOTH records intact.
  </action>
  <verify>
    <automated>grep -Eci 'result:\s*\[pending\]' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md; grep -c 'Phase 47' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md; grep -c 'fixed_in: b40b338' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eci 'result:\s*\[pending\]' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md` returns `0` (all 5 formerly-pending items flipped)
    - `grep -c 'Phase 47' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md` returns >= `5` (each flipped item carries a Phase 47 pointer)
    - `grep -c '47-HUMAN-UAT.md' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md` returns >= `5` (the pointers reference the owner-signed Phase 47 artifact)
    - `grep -c 'fixed_in: b40b338' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md` returns `1` (the test-1 G-03 gaps: provenance block is PRESERVED — audit trail intact)
    - `grep -Ec 'fixed_in: 6600761|fixed_in: f772427' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md` returns >= `1` (the G-01/G-02 resolved-markers preserved)
    - `grep -Ec 'passed:\s*6' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md` returns `1` AND `grep -Ec 'pending:\s*0' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md` returns `1` (Summary counters updated)
    - The original `expected:` prose for tests 2-6 is still present (e.g. the "10 open/close cycles" + "atlas-less" + "File menu" phrases each still grep >= 1) — no audit-trail deletion
  </acceptance_criteria>
  <done>41-HUMAN-UAT.md tests 2-6 result: flipped to resolved with a Phase 47 / 47-HUMAN-UAT.md pointer; `## Current Test` + `## Summary` (passed 6 / pending 0) updated; the original expected: prose + the test-1 G-01/G-02/G-03 gaps: block preserved (both audit trails intact per D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer to `app-image://` protocol (main) | The viewer fetches fixture JSON + atlas page PNGs over the custom `app-image://` scheme; CSP `connect-src 'self' app-image:` + the main-process ACAO header are the Phase 41 G-01/G-02 origin scope. This plan only *observes* (UAT) — it changes no code; it must not propose widening this. |
| owner (human) to live Electron WebGL canvas | The owner's visual inspection of the rendered GL canvas is the only evidence channel for the visual contract (PLAYER-02). The trust boundary is "what the owner sees" — captured as a durable screenshot so the evidence is auditable, not ephemeral. |
| planning artifacts to milestone-close gate | `47-HUMAN-UAT.md`'s signed `approved_by: user` + all-passed state is the machine-checkable witness that the human gate (D-02) was satisfied; it gates v1.6 close (D-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04 | Repudiation / false-green | the milestone-close gate (D-01/D-02) | mitigate | The owner sign-off is recorded as a durable, machine-checkable artifact: Task 2 acceptance criteria assert `approved_by: user` + zero `result: [pending]` + 7x `result: passed` + a captured GL-alpha screenshot in `47-HUMAN-UAT.md`, and Task 3 cross-links it from `41-HUMAN-UAT.md`. A merely-asserted "owner approved" with no signed artifact does NOT satisfy the gate — the evidence is auditable and re-checkable, preventing a green-wash of the visual contract (the analogue of Pitfall 2's false-green, applied to the human track). Per D-01 a regression keeps v1.6 close held (no revert fallback). |
| T-47-05 | Elevation of Privilege / Information Disclosure | CSP / `app-image://` ACAO origin scope | mitigate | This plan changes no code and explicitly authors NO "verify/adjust CSP" item; the `47-HUMAN-UAT.md` atlas-less-parity test (UAT #6) *re-confirms* the asset path resolves under the **unchanged** Phase 41 origin scope (RESEARCH S12: spine-player@4.3.0 uses the same parent-relative `AssetManager`+`rawDataURIs` model — verified in Plan 01). Task 1 acceptance criteria assert the UAT contains zero CSP/CORS-broadening instruction (RESEARCH Pitfall 4 / ASVS V14). Any observed need to broaden the origin would be reported as a regression, not silently applied. |
| T-47-06 | Denial of Service | the bumped player rendering malformed/missing-asset fixtures (UAT #5) | accept | UAT #5 *re-verifies* (does not introduce) the pre-existing terminal-error overlay + the resilient `sampleAnimationBounds` `null`-return (D-04, preserved byte-for-byte in Plan 01) handle malformed/missing-asset input on the 4.3 player without a fatal crash. No new input surface (read-only desktop analysis app, no PII — RESEARCH Security Domain V5 N/A). Residual is the pre-existing viewer's, now confirmed under 4.3.0 rather than newly introduced. |
</threat_model>

<verification>
**Plan-level gate (this plan has no compile/test artifacts of its own — its
deliverables are the two UAT records + the owner sign-off):**

1. `test -f .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md`
   AND it has 7 `### ` test sections + the D-09 pair (`SIMPLE_TEST` +
   `skeleton2`) + a GL straight-alpha test (Task 1).
2. After the owner checkpoint: `grep -qi 'approved_by:[[:space:]]*user'
   47-HUMAN-UAT.md` AND zero `result: [pending]` AND 7x `result: passed` AND a
   GL-alpha screenshot reference (Task 2 — the D-01/D-02 milestone-close gate).
3. `grep -Eci 'result:\s*\[pending\]' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md`
   returns `0` AND `grep -c 'Phase 47' 41-HUMAN-UAT.md` >= 5 AND the test-1
   `gaps:` provenance block (`fixed_in: b40b338`) is preserved (Task 3 — D-08
   both records intact).
4. No code/CSP diff anywhere from this plan: `git diff --name-only` shows only
   the two `*-HUMAN-UAT.md` files (+ any committed GL-alpha screenshot asset) —
   zero `src/`, zero `package.json`, zero `src/renderer/index.html` (this plan
   is verification-only; the migration was Plan 01).

**Wave isolation:** this wave runs **sequentially on the main tree** (Task 2 is
a `checkpoint:human-action` — incompatible with worktree isolation per CONTEXT /
PATTERNS / the Phase 46 precedent). The orchestrator must NOT use a worktree for
Wave 2.
</verification>

<success_criteria>
- `47-HUMAN-UAT.md` authored (Task 1): 7 tests = the 5 carried Phase 41 UATs
  (verbatim `expected:`) + the GL straight-alpha re-verify + the D-09
  4.2/4.3 render-pair; signed-off `36-HUMAN-UAT.md` shape; all `result:
  [pending]`, `approved_by` blank.
- Owner checkpoint (Task 2, `gate="blocking"`): the owner ran the real Electron
  app, executed all 7 items on the 4.3 player, captured the GL-alpha screenshot,
  signed every test `passed` + the front-matter (`approved_by: user`). v1.6
  milestone-close gate satisfied (D-01/D-02). A regression keeps the phase
  incomplete (no revert fallback).
- `41-HUMAN-UAT.md` (Task 3): the 5 pending items flipped in place to resolved
  with a Phase 47 / `47-HUMAN-UAT.md` pointer; `## Current Test` + `## Summary`
  (passed 6 / pending 0) updated; the original `expected:` prose + the test-1
  G-01/G-02/G-03 `gaps:` block preserved (both audit trails intact — D-08).
- Scope/security: zero code/CSP diff from this plan; the UAT contains no
  CSP/CORS-broadening instruction (T-47-05); the owner sign-off is a durable,
  machine-checkable artifact (T-47-04).
- PLAYER-02 satisfied. With PLAYER-01 (Plan 01) this closes Phase 47 and the
  v1.6 milestone-close gate.
</success_criteria>

<output>
After completion, create
`.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-02-SUMMARY.md`
recording: the `47-HUMAN-UAT.md` authoring commit; the owner sign-off outcome
(every test `passed` + `approved_at` date, OR the regression detail if any item
failed — in which case note that v1.6 close stays held per D-01); the
`41-HUMAN-UAT.md` flip commit; explicit confirmation that the original Phase 41
`expected:` prose + the test-1 `gaps:` block were preserved; and confirmation of
zero code/CSP diff from this plan. This is the final v1.6 deliverable record —
note whether the milestone-close gate (D-01/D-02) is now satisfied.
</output>
