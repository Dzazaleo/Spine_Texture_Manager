---
phase: 47-spine-player-4-3-0-bump-viewer-regression
plan: 05
type: execute
wave: 5
depends_on: ["47-03", "47-04"]
gap_closure: true
files_modified:
  - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md
  - .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md
autonomous: false
requirements: [PLAYER-02]
must_haves:
  truths:
    - "[PLAYER-02 / DV-3] 47-HUMAN-UAT.md is REVISED from the superseded D-09 pair to the DV-3 matrix: 4.2 leg = SIMPLE_TEST (GL straight-alpha hard floor) + CHJWC_SYMBOLS (transform) + TQORW_SYMBOLS (ik+transform+events) + TEST_03 (ik+transform+physics) via the frozen spine-player@4.2.111 path; 4.3 leg = skeleton2.json via the migrated spine-player@4.3.0 path"
    - "[PLAYER-02 / DV-3] Each fixture carries a per-fixture EXPECTED-PLAYER assertion — the UAT's real job is proving (1) routing sends each version to the right player off the core tag, (2) the alias-isolated 4.2 player ACTUALLY LOADS (the DV-RISK-1 split-brain), (3) the constraint-mix incl. physics renders — NOT re-proving the byte-identical 4.2 renderer"
    - "[PLAYER-02 / D-02] An explicit blocking checkpoint:human-action requires the owner to run the real Electron app and execute every DV-3-matrix item live, signing each off — the phase does not complete and v1.6 does not close until every item is owner-signed (D-01, STRICT, no revert fallback)"
    - "[PLAYER-02 / GL straight-alpha] GL straight-alpha is empirically re-verified on the 4.3-leg path (skeleton2.json, the migrated player): no white/dark halo on mesh-attachment edges vs #232732; a screenshot is captured (sharp/libvips PMA reasoning does NOT transfer — the owner's eyes on the GL canvas is the only valid evidence)"
    - "[PLAYER-02 / D-06] Same-framing parity: each 4.2 rig looks identical through the frozen 4.2.111 path as the owner-accepted v1.5.1 viewer (no auto-fit/zoom/position drift); the 4.2 leg is a byte-stable no-op so the UAT verifies routing+load, not a re-derived renderer"
    - "[PLAYER-02 / D-04 must-not-regress] The Phase 41 content-less-STOP-animation graceful degradation (return null, not fatal showError) is an explicit owner-checked acceptance criterion on BOTH legs"
    - "[PLAYER-02 / D-08] The 5 pending items in 41-HUMAN-UAT.md (tests 2-6) are flipped IN PLACE to resolved with a Phase 47 pointer; the original expected prose + the test-1 G-01/G-02/G-03 gaps block are preserved (both audit trails intact)"
    - "[informational] 47-02 disposition: this plan SUPERSEDES 47-02 — it re-authors 47-02 Task 1's D-09-matrix 47-HUMAN-UAT.md to the DV-3 matrix and subsumes 47-02's never-run Tasks 2 (owner checkpoint) + 3 (41-flip); no 47-02-SUMMARY.md is owed (47-02 is closed by supersession, recorded here)"
  artifacts:
    - path: ".planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md"
      provides: "the DV-3-matrix owner-signed Phase 47 viewer-regression UAT (revised from the superseded D-09 version)"
      contains: "approved_by: user"
    - path: ".planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md"
      provides: "in-place flip of the 5 pending items to resolved with a Phase 47 pointer (audit trail preserved)"
      contains: "Phase 47"
  key_links:
    - from: ".planning/phases/47-.../47-HUMAN-UAT.md"
      to: "the DV-3 fixture matrix"
      via: "the Setup block + per-fixture expected-player assertions"
      pattern: "CHJWC_SYMBOLS|TQORW_SYMBOLS|TEST_03|SIMPLE_TEST|skeleton2"
    - from: ".planning/phases/41-.../41-HUMAN-UAT.md"
      to: ".planning/phases/47-.../47-HUMAN-UAT.md"
      via: "the in-place resolved-pointer on tests 2-6"
      pattern: "Phase 47"
---

<objective>
Deliver the human half of the Phase 47 gap closure (PLAYER-02, reworded per
DV-2): REVISE the existing `47-HUMAN-UAT.md` (authored by the now-SUPERSEDED
47-02 Task 1 against the D-09 pair) to the DV-3 dual-runtime fixture matrix,
then the BLOCKING owner `checkpoint:human-action` in which the owner runs the
real Electron app, executes every DV-3-matrix item live, and signs each off —
and flip the 5 pending items in `41-HUMAN-UAT.md` in place to resolved with a
Phase 47 pointer.

Purpose: 47-03 made the viewer dual-runtime and 47-04 machine-guarded the
routing/parse/handoff headlessly — but jsdom has no WebGL, so the GL halo,
same-framing parity (D-06), the alias-isolated-4.2-player-actually-renders
DV-RISK-1 check, the constraint-mix (incl. physics) render, scrub synchrony, GL
leak, real-fs error UI, atlas-less parity, and File-menu suppression are ONLY
observable by the owner rendering the DV-3 matrix in a real GL context. Per D-01
v1.6 milestone close is HELD on this checkpoint (no revert fallback). Per D-02
it is an explicit in-phase `checkpoint:human-action` gating phase completion.
This MUST run AFTER 47-03 lands DV-1 — running the owner UAT before the
dual-runtime fix would just re-fail on the 4.2 constraint fixtures (the exact
gap). This is the last v1.6 deliverable — when the owner signs, the milestone
can close.

Output: a DV-3-matrix `47-HUMAN-UAT.md` (owner-signed) + the in-place flip of
`41-HUMAN-UAT.md`'s 5 pending items. Closes 47-02 by supersession.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-CONTEXT.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-RESEARCH.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-VALIDATION.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-03-SUMMARY.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-04-SUMMARY.md
@.planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md

<isolation-note>
This plan carries a `checkpoint:human-action` (Task 2). Per CONTEXT
"Established Patterns" + 47-PATTERNS.md + the Phase 46 precedent + 47-02's own
`<isolation-note>`: owner-action checkpoints are **incompatible with
agent-worktree isolation** (fresh-worktree-per-Task continuation breaks the
owner's live Electron session). **This plan's wave MUST run sequentially on the
main tree, NOT in a worktree.** The orchestrator must disable worktree
isolation for this wave. (47-03 and 47-04 are worktree-safe and run earlier
waves; only THIS plan's wave is main-tree-sequential.)
</isolation-note>

<analog-refs>
- The existing `47-HUMAN-UAT.md` (authored by 47-02 Task 1, commit `fdcef30`)
  is the REVISION base — it already has the 36-HUMAN-UAT signed-off
  front-matter/Setup/per-test shape with the per-test sub-blocks UNBOLDED
  (the Rule-1 auto-fix the STATE.md notes — preserve the unbolded form: the
  bold `**result:**` shape false-greens the Task-2 owner-sign machine gate).
  This plan REWRITES its test list from the D-09 pair to the DV-3 matrix; it
  does NOT re-author the front-matter/Setup skeleton from scratch.
- Content source for tests 3-7 (the 5 carried Phase 41 UATs): `41-HUMAN-UAT.md`
  tests 2-6 (copy `expected:` prose verbatim) + the resolved G-01/G-02/G-03
  under test 1 (the regressions the bump must not reintroduce).
- GL-alpha test content: `47-RESEARCH.md` "GL Straight-Alpha" section
  "Concrete empirical re-verification method".
- DV-3 matrix definition: `47-CONTEXT.md` DV-3 (the exact fixture list + the
  "real job" of the UAT) + `47-RESEARCH.md` §1d (the alias-isolation
  end-to-end proof) + §4 (the constraint-mix coverage rationale).
- Owner-checkpoint task structure: `46-01-PLAN.md` Task 2 (L164-194) — the
  most recent completed owner-action checkpoint; the XML structure /
  attributes / required sub-elements / two-task author-then-owner-execute
  shape (also captured in 47-PATTERNS.md "Plan task: owner live-UAT").
- 41-flip pattern: the SAME file's test-1 `gaps:` `status: resolved` +
  `fixed_in: <sha> <msg>` provenance convention (41-HUMAN-UAT.md L18-33).
</analog-refs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revise 47-HUMAN-UAT.md from the superseded D-09 pair to the DV-3 dual-runtime fixture matrix (owner-signature-ready)</name>
  <files>.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md (FULL — the existing 47-02-Task-1 artifact; KEEP its 36-shape signed-off front-matter skeleton + Setup block shape + the UNBOLDED per-test sub-block form [STATE.md Rule-1 fix — bold `**result:**` false-greens the Task-2 machine gate]; this task REPLACES its test list, not its skeleton)
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-CONTEXT.md §"DV-3" (the EXACT fixture matrix + "the UAT's real job is NOT re-proving the 4.2 renderer — it is proving (1) routing (2) the alias-isolated 4.2 player actually loads (3) the constraint-mix incl. physics") AND §"DV-RISK-1" (the split-brain is the single most likely failure — the UAT must explicitly catch it)
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-RESEARCH.md §"GL Straight-Alpha" (the verified 4.3.0 mechanism + the "Concrete empirical re-verification method") AND §1d (the SIMPLE_TEST-parses-clean-via-aliased-core / throws-via-canonical proof — the per-fixture expected-player rationale) AND §4 (the constraint-mix coverage map)
    - .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md (tests 2-6 — copy each `expected:` prose VERBATIM for the 5 carried UATs; the resolved G-03 under test 1 = the straight-alpha regression the bump must not reintroduce)
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-03-SUMMARY.md + 47-04-SUMMARY.md (so the UAT references the correct landed state: dual-runtime router, frozen 4.2.111 modal, alias trio, the T-A..T-D guards green — the machine half is done; this UAT is the visual half)
  </read_first>
  <action>
REVISE `.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md`
(it already exists from 47-02 Task 1 / `fdcef30`). KEEP its existing signed-off
36-HUMAN-UAT-shape front-matter skeleton, the `## Setup` block shape, and the
UNBOLDED per-test sub-block form (STATE.md records this as a load-bearing Rule-1
fix — bold `**result:**` false-greens the Task-2 owner-sign machine gate
`! grep 'result:[[:space:]]*\[pending\]'` and breaks the Task-3 pointer map; the
unbolded form satisfies BOTH the richer-sub-block intent and every machine
gate). Leave the approval fields BLANK for the owner/Task-2 (`status: pending`,
`approved_by:`/`approved_at:` empty, every `result:` `[pending]`). Bump
`updated:`. REPLACE the test list (the old D-09-pair tests) with the DV-3
matrix.

**`## Setup` block** — rewrite the Fixtures bullet to the DV-3 matrix (do not
substitute):
- 4.2 leg (the FROZEN spine-player@4.2.111 path — `AnimationPlayerModal42`):
  `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` (GL straight-alpha hard-floor
  canary; has a path constraint) + `fixtures/CHJ/CHJWC_SYMBOLS.json`
  (transform-only) + `fixtures/3Queens/TQORW_SYMBOLS.json` (ik + transform +
  events) + `fixtures/MON_FILES/EXPORT/TEST_03/4.2/TEST_03.json` (ik +
  transform + physics — the most 4.2/4.3-divergent mix).
- 4.3 leg (the MIGRATED spine-player@4.3.0 path — `AnimationPlayerModal`):
  `fixtures/SIMPLE_PROJECT_43/skeleton2.json` (`spine:"4.3.01"`).
- App build: `npm run dev` (Electron dev app — the fix lives in renderer
  source; NOT the packaged installer). Reset between tests (close the viewer +
  reload the project).

**Per-test list** — author these tests in the kept per-test shape
(`### N. <title>` then unbolded `setup:` / `expected:` / `why_human:` /
`result:`). The DV-3 "real job" framing MUST be explicit in each fixture's
`expected:`: per-fixture **expected-player** assertion (which player path the
core tag routes it to) + **actually-loads** (the DV-RISK-1 split-brain is the
single most likely failure) + the render result.

1. **GL straight-alpha re-verify (the hard floor — NEVER skip).** `setup:`
   `npm run dev`, load `SIMPLE_TEST.json` (routes to the frozen 4.2.111 path),
   open the Viewer; then load `skeleton2.json` (routes to the migrated 4.3.0
   path). `expected:` (from RESEARCH GL section) mesh-attachment edges
   anti-alias cleanly into `#232732` with NO opaque-white/bright ring/fringe
   and NO dark/double-darkened seam, on BOTH legs; the 4.2-leg render is
   visually identical to the owner-accepted v1.5.1 viewer at the same
   pose/zoom (D-06 same-framing parity — the 4.2 leg is the byte-stable v1.5.1
   no-op, so any drift = a routing/alias-load defect, not a renderer change).
   `why_human:` jsdom has no WebGL; the 4.3.0 spine-webgl premultiply mechanism
   moved (`UNPACK_PREMULTIPLY_ALPHA_WEBGL = !pma`); the sharp/libvips
   `project_pma_no_op_in_current_stack` reasoning does NOT transfer;
   `scripts/pma-probe.mjs` does NOT cover this GL path — the owner's eyes on
   the rendered canvas, screenshotted, is the only valid evidence. REQUIRE a
   screenshot embedded/linked in this file. `result:` `[pending]`.
2. **DV-3 routing + alias-isolation: each fixture loads in the RIGHT player and
   renders.** This is the core DV-1 acceptance test. For EACH 4.2-leg fixture
   (SIMPLE_TEST, CHJWC_SYMBOLS, TQORW_SYMBOLS, TEST_03): `expected:` it routes
   to the frozen spine-player@4.2.111 path (`AnimationPlayerModal42`), the
   alias-isolated 4.2 player ACTUALLY LOADS (no split-brain — the DV-RISK-1
   single-most-likely failure: the viewer opens and renders, it does NOT show
   the "Could not load skeleton data / <X> constraint not found" terminal
   overlay that the gap produced), and the rig's first animation paints looping
   against `#232732` — including TEST_03's physics-constraint-bearing motion.
   For the 4.3-leg fixture (skeleton2.json): it routes to the migrated
   spine-player@4.3.0 path (`AnimationPlayerModal`) and renders correctly.
   `why_human:` the cross-version routing + the alias-isolated 4.2 player
   actually-rendering + the constraint-mix (incl. physics) are GL-context-only;
   jsdom cannot render; this is the exact gap (debug
   `viewer-43-42-constraint-parse`) the fix closes. `result:` `[pending]`.
3. VIEWER-05 + VIEWER-06 anim/skin switch + scrub-pose synchrony (forward AND
   backward). Copy `expected:` VERBATIM from `41-HUMAN-UAT.md` test 2 (incl.
   the backward-scrub caveat). Note: on the 4.2 leg the scrub is the byte-stable
   v1.5.1 private-playTime path (frozen modal); on the 4.3 leg it is the
   47-01-T6 TrackEntry-driven rework — both must be coherent. `why_human:`
   behavioral equivalence is reasoned not machine-tested; forward+backward
   scrub synchrony is the only valid acceptance gate. `result:` `[pending]`.
4. VIEWER-08 GL leak across 10 open/close cycles. Copy `expected:` VERBATIM
   from `41-HUMAN-UAT.md` test 3. Add: cycle on BOTH a 4.2 fixture AND
   skeleton2.json (dual-stack — two full spine-player/webgl/core graphs are now
   bundled; confirm no GPU growth across either path). `why_human:` GPU memory
   growth is only observable in a real GL context across real cycles.
   `result:` `[pending]`.
5. VIEWER-09 real-fs malformed/missing-asset terminal error UI (the D-04
   must-not-regress check). Copy `expected:` VERBATIM from `41-HUMAN-UAT.md`
   test 4. Add the explicit acceptance line: the content-less / STOP-state path
   still degrades gracefully on BOTH legs (the viewer shows the terminal error
   overlay / returns cleanly — it does NOT fatal-crash via spine-player's
   native `showError`; the resilient `sampleAnimationBounds` `null` return is
   preserved — D-04). `why_human:` real-fs corruption + the resilient-path
   behavior is only verifiable live. `result:` `[pending]`.
6. VIEWER-04 atlas-less visual parity (also the Pitfall-4 asset-origin
   re-confirm). Copy `expected:` VERBATIM from `41-HUMAN-UAT.md` test 5.
   `why_human:` atlas-less `rawDataURIs` parent-relative resolution is a
   render-behavior touchpoint AND it re-confirms the asset path resolves under
   the UNCHANGED CSP/`app-image://` origin scope (no broadening — security
   guardrail). `result:` `[pending]`.
7. File-menu auto-suppression contract while the viewer is open. Copy
   `expected:` VERBATIM from `41-HUMAN-UAT.md` test 6. `why_human:` OS-native
   menu state is a host-integration behavior; jsdom has no native menu.
   `result:` `[pending]`.

**`## Summary` block:** total: 7 / passed: 0 / issues: 0 / pending: 7 /
skipped: 0 / blocked: 0. (Task 2 — the owner — flips these at sign-off.)

Update the `## Provenance` note: "REVISED 2026-05-19 from the superseded D-09
render-pair (47-02 Task 1, `fdcef30`) to the DV-3 dual-runtime matrix per the
Gap Re-Discussion (DV-1..DV-3). Tests 3-7 = the 5 carried Phase 41 UATs
(`41-HUMAN-UAT.md` tests 2-6) re-run on the dual-runtime viewer; tests 1-2 are
the GL-alpha re-verify + the DV-3 routing/alias-isolation matrix. D-08: this
file + the in-place flip of `41-HUMAN-UAT.md` preserve both audit trails. This
supersedes 47-02 (its never-run owner-checkpoint + 41-flip Tasks are this
plan's Tasks 2-3)."

Do NOT pre-sign. Do NOT broaden any CSP/CORS or add a "verify/adjust CSP" item
(RESEARCH Pitfall 4 — the atlas-less test only RE-confirms the unchanged scope).
  </action>
  <verify>
    <automated>test -f .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -q 'CHJWC_SYMBOLS' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -q 'TQORW_SYMBOLS' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -q 'TEST_03' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -q 'skeleton2' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -qiE 'straight.?alpha|halo' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -ciE 'result:[[:space:]]*\[pending\]' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md | grep -qx 7 && echo DV3_UAT_REVISED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f` the file succeeds AND `grep -c '^### ' …/47-HUMAN-UAT.md` returns `7`
    - The file contains ALL of: `SIMPLE_TEST`, `CHJWC_SYMBOLS`, `TQORW_SYMBOLS`, `TEST_03`, `skeleton2` (the full DV-3 matrix — the superseded D-09 pair is replaced, not merely appended)
    - `grep -c 'frozen spine-player@4.2.111\|spine-player@4.2.111\|4.2.111' …` >= 1 AND a per-fixture expected-PLAYER assertion is present (the DV-3 "real job": routing + alias-isolation, not re-proving the 4.2 renderer) — grep for `route` / `frozen` / `migrated` player-path language >= 1
    - `grep -ciE 'result:\s*\[pending\]' …` returns `7` AND the front-matter `approved_by:` is BLANK and `status:` is `pending` (NOT pre-signed — the owner signs at Task 2)
    - The per-test sub-block labels are UNBOLDED (`grep -c '^\*\*result:\*\*' …` returns `0` — the STATE.md Rule-1 fix preserved; bold would false-green the Task-2 gate)
    - The 5 carried `expected:` prose blocks are present (the "10 open/close cycles", "scrub" + "backward", "terminal error", "atlas-less", "File menu" phrases each grep >= 1)
    - The DV-RISK-1 alias-isolation acceptance is explicit: test 2 references the gap's terminal-overlay symptom (`constraint not found` / `Could not load skeleton data`) as the FAIL condition the dual-runtime fix must eliminate on the 4.2 constraint fixtures
    - The file contains zero new CSP/CORS-broadening instruction (no `connect-src` widening text)
    - The Provenance note states this REVISES the superseded D-09 version and supersedes 47-02
  </acceptance_criteria>
  <done>47-HUMAN-UAT.md revised to the 7-test DV-3 matrix (GL-alpha + the routing/alias-isolation matrix over SIMPLE_TEST/CHJWC_SYMBOLS/TQORW_SYMBOLS/TEST_03 + skeleton2 + the 5 verbatim-ported Phase 41 UATs), kept 36-shape signed-off skeleton with UNBOLDED sub-blocks, all result:[pending], approved_by blank, per-fixture expected-player + DV-RISK-1 fail-condition + screenshot requirement + D-04 must-not-regress line + the revised Provenance note present.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: OWNER — run the real Electron app, execute all 7 DV-3-matrix 47-HUMAN-UAT.md items live on the dual-runtime viewer, sign each off</name>
  <files>.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md (revised by Task 1 — the 7 DV-3-matrix tests + Setup the owner executes)
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-RESEARCH.md §"GL Straight-Alpha" (so the owner knows exactly what halo/fringe artifact to look for and that the Phase 41 G-03 note describes a now-deleted mechanism)
    - .planning/debug/viewer-43-42-constraint-parse.md (the gap this UAT proves closed — so the owner recognizes the OLD failure symptom ("Could not load skeleton data / Transform constraint not found") as the exact thing that must NOT appear on the 4.2 constraint fixtures anymore)
  </read_first>
  <action>
This is a `checkpoint:human-action` — it has NO CLI/API equivalent. It requires
a human running the real Electron app and visually inspecting a live WebGL
canvas; jsdom has no WebGL, so no machine test can produce this evidence. Per
D-01 this gate **holds v1.6 milestone close** until the owner signs every item
(there is NO revert fallback — D-03 proved the bump is mechanically
non-revertible; DV-1 ADDED the alias-isolated 4.2 stack rather than reverting).
Claude cannot automate this — pause and present the steps below to the owner,
then resume on the owner's signal.

WHAT TASK 1 BUILT: `47-HUMAN-UAT.md` REVISED to the DV-3 dual-runtime matrix —
7 tests (GL straight-alpha re-verify, the DV-3 routing/alias-isolation matrix
over the 4 4.2-leg fixtures + skeleton2.json, and the 5 carried Phase 41 UATs),
each with unbolded `setup:`/`expected:`/`why_human:` and a `result: [pending]`
the owner fills.

WHAT 47-03 + 47-04 BUILT (already landed, machine-green): the viewer is now
DUAL-RUNTIME — a 4.2 project routes to the frozen v1.5.1 spine-player@4.2.111
modal (`AnimationPlayerModal42`), a 4.3 project to the migrated
spine-player@4.3.0 modal; the npm-alias trio isolates the 4.2.111 graph; the
T-A (REG-47-01 handoff) / T-B (routing+alias-distinctness) / T-C (4.2-parse
guard over all 4 DV-3 fixtures) / T-D (frozen-modal spec) headless guards are
GREEN. The machine half is done; this checkpoint is the visual half.

OWNER STEPS:
1. `npm run dev` (the Electron dev app — the fix lives in renderer source; do
   NOT use the packaged installer).
2. Test 1 — GL straight-alpha (the hard floor): load
   `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` (routes to the frozen 4.2.111
   path), open the Animation Viewer, inspect the mesh-attachment edges against
   `#232732`. PASS = edges anti-alias cleanly, NO opaque-white/bright
   ring/fringe, NO dark/double-darkened seam, visually identical to the
   owner-accepted v1.5.1 render at the same pose/zoom (no auto-fit/zoom/position
   drift — D-06). Then load `fixtures/SIMPLE_PROJECT_43/skeleton2.json` (routes
   to the migrated 4.3.0 path) and confirm the same clean-edge result.
   **Capture a screenshot of the SIMPLE_TEST render and embed/link it in
   `47-HUMAN-UAT.md` test 1.** If a halo appears: do NOT guess — record the
   artifact + screenshot and report it as a regression.
3. Test 2 — DV-3 routing + alias-isolation (the core gap-closure test): load
   EACH of `fixtures/CHJ/CHJWC_SYMBOLS.json`,
   `fixtures/3Queens/TQORW_SYMBOLS.json`,
   `fixtures/MON_FILES/EXPORT/TEST_03/4.2/TEST_03.json` and `SIMPLE_TEST.json`,
   open the Animation Viewer on each. PASS (per fixture) = the viewer OPENS AND
   RENDERS the looping first animation — it does NOT show the
   "Could not load skeleton data / Transform/IK/Path constraint not found"
   terminal overlay (that overlay is the exact gap; its absence on these 4.2
   constraint fixtures is the proof DV-1 closed it; TEST_03 must show its
   physics-constraint motion). Then load `skeleton2.json` and confirm the 4.3
   leg renders. (This is "opened AND rendered", not "opened" — the
   feedback_uat_opened_is_not_rendered lesson: a load/open proxy is NOT
   acceptance.)
4. Tests 3-7 — the 5 carried Phase 41 UATs per `47-HUMAN-UAT.md`:
   (3) anim/skin switch + scrub synchrony forward AND backward (drag the scrub
   bar both directions on both a 4.2 fixture and skeleton2.json — coherent
   poses + pause-on-scrub + no leftover-skin bleed);
   (4) GL leak — open/close the viewer 10× on a 4.2 fixture AND skeleton2.json,
   watching DevTools GPU Memory (stays flat) + switch projects while open;
   (5) real-fs malformed/missing-asset terminal error UI — truncate a `.json`
   or remove a referenced PNG, confirm the verbatim terminal error overlay with
   disabled controls and that it does NOT fatal-crash (the resilient null path
   is preserved — D-04) on both legs;
   (6) atlas-less visual parity — load an atlas-less project, no region
   misalignment / PMA glitch / missing slot;
   (7) File-menu auto-suppression — with the viewer open, Save/Save As/Reload
   greyed out, Cmd-S a no-op, restored on close.
5. In `47-HUMAN-UAT.md`, set each test's `result:` to `passed` (or describe the
   regression if any fails). Then set the front-matter `status: passed`,
   `approved_by: user`, `approved_at: <today's date>`, update the `## Summary`
   counters (`passed: 7`, `pending: 0`). Commit the file (and the screenshot)
   (plain English for the git-beginner user: "`git add` the UAT file and the
   screenshot, then a `docs(47)` commit to save your sign-off — this is the
   record that v1.6's last gate passed").

RESUME SIGNAL: the owner types "done" once every test in `47-HUMAN-UAT.md` is
signed `passed`, the front-matter is signed (`approved_by: user`), and the
GL-alpha screenshot is captured — OR reports which test regressed (in which
case v1.6 close stays HELD per D-01 and the regression is triaged; the
dual-runtime fix is NOT reverted).
  </action>
  <verify>
    <automated>test -f .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && grep -qi 'approved_by:[[:space:]]*user' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && ! grep -qiE 'result:[[:space:]]*\[pending\]' .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md && echo OWNER_SIGNED_OK || echo NOT_YET_SIGNED</automated>
  </verify>
  <acceptance_criteria>
    - `test -f …/47-HUMAN-UAT.md` succeeds
    - `grep -qi 'approved_by:[[:space:]]*user' …` matches (the owner signed the front-matter)
    - `grep -ciE 'result:\s*\[pending\]' …` returns `0` (every test resolved by the owner — no item left pending)
    - The front-matter `status:` is `passed` and `approved_at:` is non-empty
    - `grep -ciE 'result:\s*passed' …` returns `7` (all 7 DV-3-matrix tests signed passed — the v1.6-close gate per D-01; if any regressed instead this is < 7 and the phase does NOT complete and v1.6 stays HELD)
    - The Test-1 GL-alpha entry references a captured screenshot (the durable visual evidence — RESEARCH GL requirement)
    - Test 2 records a per-fixture rendered verdict for SIMPLE_TEST + CHJWC_SYMBOLS + TQORW_SYMBOLS + TEST_03 + skeleton2 (the DV-3 matrix proven rendered, not merely "opened")
  </acceptance_criteria>
  <done>The owner ran the real Electron app, executed all 7 DV-3-matrix 47-HUMAN-UAT.md items on the dual-runtime viewer (GL straight-alpha + the routing/alias-isolation matrix over the 4 4.2-leg fixtures + skeleton2 + the 5 carried Phase 41 UATs), captured the GL-alpha screenshot, and signed every test passed + the front-matter (approved_by: user, status: passed). v1.6 milestone-close gate satisfied (D-01/D-02).</done>
</task>

<task type="auto">
  <name>Task 3: Flip the 5 pending items in 41-HUMAN-UAT.md in place to resolved with a Phase 47 pointer (audit trail preserved)</name>
  <files>.planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md (FULL — the 5 pending items are tests 2-6 `result: [pending]`; the `## Current Test` line; the `## Summary` counters; the test-1 `gaps:` block is the resolved-marker provenance pattern to mirror AND MUST be preserved — it carries `fixed_in: b40b338` G-03 / `fixed_in: 6600761` G-01 / `fixed_in: f772427` G-02)
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-HUMAN-UAT.md (the owner-signed Task-2 artifact — the pointer target; note its `approved_at` date for the provenance line)
    - .planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-PATTERNS.md §"41-HUMAN-UAT.md — in-place flip" (the exact edit pattern: which lines, the fixed_in-style pointer shape, preserve the original expected: prose + the test-1 gaps: block)
  </read_first>
  <action>
Flip the 5 pending items in `41-HUMAN-UAT.md` IN PLACE to resolved with a
Phase 47 pointer (D-08 — preserves the Phase 41 audit trail AND records the
Phase 47 dual-runtime re-run). This runs AFTER the Task 2 owner sign-off so the
pointer can cite the owner-signed `47-HUMAN-UAT.md` + its sign-off date.

For each of tests 2-6, change the `result: [pending]` line to a resolved status
WITH a Phase 47 provenance pointer, mirroring the test-1 `gaps:`
`fixed_in: <sha> <msg>` provenance shape, e.g.:
`result: resolved — re-run on the dual-runtime viewer in Phase 47; see 47-HUMAN-UAT.md test N (owner-signed <approved_at date from 47-HUMAN-UAT.md>)`

Map each Phase 41 test to its Phase 47 counterpart: 41 test 2 (anim/skin +
scrub) → 47 test 3; 41 test 3 (GL leak) → 47 test 4; 41 test 4 (real-fs error)
→ 47 test 5; 41 test 5 (atlas-less parity) → 47 test 6; 41 test 6 (File-menu)
→ 47 test 7. (47 tests 1-2 are the GL-alpha + DV-3 routing/alias matrix and
have no Phase 41 counterpart — do not invent a pointer for them.)

Also update:
- The `## Current Test` line → a resolved statement, e.g. `[all items resolved
  — tests 2-6 re-run + owner-signed on the dual-runtime viewer in Phase 47; see
  47-HUMAN-UAT.md]`.
- The `## Summary` counters: `passed: 1` → `passed: 6`, `pending: 5` →
  `pending: 0` (total stays 6; issues/skipped/blocked unchanged).

**PRESERVE the Phase 41 audit trail:** do NOT delete or alter any original
`expected:` prose, and do NOT touch the test-1 `gaps:` block (the resolved
G-01/G-02/G-03 record — `fixed_in: b40b338`/`6600761`/`f772427`). Only flip the
5 `result:` lines + the `## Current Test` line + the 2 `## Summary` counters,
and add the forward pointer text. Keeping BOTH records intact is the whole
point of D-08.
  </action>
  <verify>
    <automated>grep -ciE 'result:[[:space:]]*\[pending\]' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md | grep -qx 0 && grep -c 'Phase 47' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md && grep -c 'fixed_in: b40b338' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md | grep -qx 1 && echo FLIP41_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ciE 'result:\s*\[pending\]' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md` returns `0` (all 5 formerly-pending items flipped)
    - `grep -c 'Phase 47' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md` returns `>= 5` (each flipped item carries a Phase 47 pointer)
    - `grep -c '47-HUMAN-UAT.md' .planning/phases/41-spine-animation-viewer/41-HUMAN-UAT.md` returns `>= 5` (the pointers reference the owner-signed Phase 47 artifact)
    - `grep -c 'fixed_in: b40b338' …41-HUMAN-UAT.md` returns `1` (the test-1 G-03 gaps: provenance is PRESERVED) AND `grep -Ec 'fixed_in: 6600761|fixed_in: f772427' …` returns `>= 1` (G-01/G-02 markers preserved — audit trail intact)
    - `grep -Ec 'passed:\s*6' …` returns `1` AND `grep -Ec 'pending:\s*0' …` returns `1` (Summary counters updated)
    - The original `expected:` prose for tests 2-6 is still present (the "10 open/close cycles" + "atlas-less" + "File menu" phrases each still grep >= 1) — no audit-trail deletion
  </acceptance_criteria>
  <done>41-HUMAN-UAT.md tests 2-6 result: flipped to resolved with a Phase 47 / 47-HUMAN-UAT.md pointer; `## Current Test` + `## Summary` (passed 6 / pending 0) updated; the original expected: prose + the test-1 G-01/G-02/G-03 gaps: block preserved (both audit trails intact per D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (human) → live Electron WebGL canvas | The owner's visual inspection of the rendered GL canvas is the only evidence channel for the visual contract (PLAYER-02). The boundary is "what the owner sees on the DV-3 matrix" — captured as a durable screenshot so the GL-alpha evidence is auditable, not ephemeral. |
| the DV-3 4.2-constraint fixtures → the frozen 4.2.111 player | The single most likely failure (DV-RISK-1) is the alias split-brain: the frozen 4.2 modal failing to load because its nested spine-core leaked onto canonical 4.3.0. Test 2 is the owner-visible boundary check that the alias isolation actually holds at runtime in the packaged-equivalent dev app. |
| planning artifacts → milestone-close gate | `47-HUMAN-UAT.md`'s signed `approved_by: user` + all-passed state is the machine-checkable witness that the D-02 human gate was satisfied; it gates v1.6 close (D-01, no revert fallback). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-15 | Repudiation / false-green (UAT proxy) | the D-02 milestone-close gate | mitigate | The "opened ≠ rendered" green-wash class (memory feedback_uat_opened_is_not_rendered — the exact Phase-47 lesson that the original D-09 SIMPLE_TEST "opened correctly" sign-off masked total non-function) is defeated structurally: Task 1 states the observable END-STATE per fixture ("OPENS AND RENDERS the looping first animation; does NOT show the constraint-not-found overlay"), and Task 2 acceptance criteria require a per-fixture RENDERED verdict for all 5 DV-3 fixtures + a captured GL-alpha screenshot — a load/open proxy does NOT satisfy the gate. The owner sign-off is a durable, machine-checkable artifact (`approved_by: user` + zero `[pending]` + 7× `passed`). |
| T-47-16 | Tampering / Repudiation (audit-trail loss) | the in-place 41-HUMAN-UAT.md flip | mitigate | Task 3 acceptance criteria machine-assert the test-1 `gaps:` provenance (`fixed_in: b40b338`/`6600761`/`f772427` — the resolved G-01/G-02/G-03 straight-alpha/CSP/CORS record the bump must not regress) is PRESERVED and the original `expected:` prose survives — the flip ADDS a forward pointer, it cannot silently erase the Phase 41 record (D-08 keeps BOTH trails). |
| T-47-17 | Elevation of Privilege / Information Disclosure | CSP / `app-image://` ACAO origin scope | mitigate | This plan changes no code and explicitly authors NO "verify/adjust CSP" item; the atlas-less-parity test (UAT #6) RE-confirms the asset path resolves under the UNCHANGED Phase 41 origin scope (the frozen 4.2 modal is the literal v1.5.1 source — already origin-scoped + owner-accepted; the migrated 4.3 modal's S12 path was verified unchanged in 47-01). Task 1 acceptance asserts zero CSP/CORS-broadening instruction (RESEARCH Pitfall 4 / ASVS V14). Any observed need to broaden would be reported as a regression, not silently applied. |
| T-47-18 | Denial of Service | the dual-runtime viewer on malformed/missing-asset fixtures (UAT #5) | accept | UAT #5 RE-verifies (does not introduce) the pre-existing terminal-error overlay + the resilient `sampleAnimationBounds` `null`-return (D-04 — byte-preserved on the 4.3 leg by 47-01, and the 4.2 leg IS the literal v1.5.1 source) handle malformed input on BOTH legs without a fatal crash. No new input surface (read-only desktop analysis app, no PII — RESEARCH Security Domain V5 N/A). Residual is the pre-existing viewer's, now confirmed dual-runtime. |
</threat_model>

<verification>
**Plan-level gate (this plan has no compile/test artifacts of its own — its
deliverables are the revised UAT, the owner sign-off, and the 41-flip):**

1. Task 1: `47-HUMAN-UAT.md` revised to the 7-test DV-3 matrix (all 5 fixture
   names + skeleton2 + GL-alpha; 7× `result: [pending]`; unbolded sub-blocks;
   approved_by blank; the superseded D-09 pair replaced not appended).
2. Task 2 (the D-01/D-02 milestone-close gate): `grep -qi 'approved_by:
   user'` AND zero `result: [pending]` AND 7× `result: passed` AND a GL-alpha
   screenshot reference AND a per-fixture rendered verdict for the 5 DV-3
   fixtures.
3. Task 3: zero `result: [pending]` in `41-HUMAN-UAT.md` AND `grep -c 'Phase
   47'` >= 5 AND the test-1 `gaps:` provenance (`fixed_in: b40b338` +
   `6600761|f772427`) preserved AND the Summary counters updated.
4. **Blast-radius (binding):** `git diff --name-only` shows ONLY the two
   `*-HUMAN-UAT.md` files (+ any committed GL-alpha screenshot asset) + this
   plan's docs — ZERO `src/`, ZERO `tests/`, ZERO `package.json`, ZERO CSP
   (`src/renderer/index.html`). This plan is verification-only; the
   implementation was 47-03/47-04.

**Wave isolation:** this wave runs **sequentially on the main tree** (Task 2 is
a `checkpoint:human-action` — incompatible with worktree isolation per CONTEXT /
PATTERNS / the Phase 46 precedent / 47-02's `<isolation-note>`). The
orchestrator must NOT use a worktree for this wave.

**47-02 disposition (recorded here):** 47-02 is SUPERSEDED by this plan. Its
Task 1 (47-HUMAN-UAT.md authored for the D-09 matrix, commit `fdcef30`) is
re-authored to the DV-3 matrix by this plan's Task 1; its never-run Task 2
(owner checkpoint) + Task 3 (41-flip) are this plan's Tasks 2-3 against the
DV-3 matrix. 47-02 has no SUMMARY and is owed none — it is closed by
supersession; the 47-05-SUMMARY records this. 47-01 is UNCHANGED (the 4.3 leg).
</verification>

<success_criteria>
- `47-HUMAN-UAT.md` revised (Task 1): 7 tests = GL straight-alpha re-verify +
  the DV-3 routing/alias-isolation matrix (SIMPLE_TEST + CHJWC_SYMBOLS +
  TQORW_SYMBOLS + TEST_03 via the frozen 4.2.111 path + skeleton2 via the
  migrated 4.3.0 path) + the 5 carried Phase 41 UATs (verbatim `expected:`);
  kept 36-shape signed-off skeleton with UNBOLDED sub-blocks; all
  `result:[pending]`, `approved_by` blank.
- Owner checkpoint (Task 2, `gate="blocking"`): the owner ran the real Electron
  app, executed all 7 DV-3-matrix items, captured the GL-alpha screenshot,
  recorded a per-fixture RENDERED verdict (not an "opened" proxy), signed every
  test `passed` + the front-matter. v1.6 milestone-close gate satisfied
  (D-01/D-02). A regression keeps the phase incomplete (no revert fallback).
- `41-HUMAN-UAT.md` (Task 3): the 5 pending items flipped in place to resolved
  with a Phase 47 / `47-HUMAN-UAT.md` pointer; counters updated; the original
  `expected:` prose + the test-1 G-01/G-02/G-03 `gaps:` block preserved (both
  audit trails intact — D-08).
- 47-02 closed by supersession (recorded in the SUMMARY); 47-01 unchanged.
- Scope/security: zero code/test/CSP diff from this plan; the UAT contains no
  CSP/CORS-broadening instruction (T-47-17); the owner sign-off is a durable,
  machine-checkable artifact with a per-fixture rendered verdict (T-47-15).
- PLAYER-02 (reworded per DV-2) fully satisfied. With 47-01 (PLAYER-01) +
  47-03/47-04 this closes Phase 47 and the v1.6 milestone-close gate (D-01).
</success_criteria>

<output>
After completion, create
`.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-05-SUMMARY.md`
recording: the `47-HUMAN-UAT.md` DV-3 revision commit (the before→after: D-09
pair → DV-3 matrix); the owner sign-off outcome (every test `passed` +
`approved_at` date, OR the regression detail if any item failed — in which case
note v1.6 close stays HELD per D-01 and the dual-runtime fix is NOT reverted);
the `41-HUMAN-UAT.md` flip commit; explicit confirmation the original Phase 41
`expected:` prose + the test-1 `gaps:` block were preserved; the explicit
47-02-SUPERSEDED disposition (no 47-02-SUMMARY owed); confirmation of zero
code/test/CSP diff from this plan; and whether the milestone-close gate
(D-01/D-02) is now satisfied (PLAYER-02 reworded → Complete, Phase 47 done,
v1.6 closeable).
</output>
