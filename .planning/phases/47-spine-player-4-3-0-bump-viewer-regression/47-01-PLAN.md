---
phase: 47-spine-player-4-3-0-bump-viewer-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/renderer/src/modals/AnimationPlayerModal.tsx
  - tests/renderer/animation-player-modal.spec.tsx
autonomous: true
requirements: [PLAYER-01]
must_haves:
  truths:
    - "[PLAYER-01] package.json declares @esotericsoftware/spine-player exactly 4.3.0 (no caret), sibling-aligned to the frozen-canonical spine-core 4.3.0; package-lock.json regenerated (spine-webgl@4.3.0 transitive)"
    - "[PLAYER-01] AnimationPlayerModal.tsx imports zero MixBlend/MixDirection symbols and all 8 D-05 touchpoints (T1-T8) are migrated to the verified 4.3.0 API; npm run typecheck:web reports 0 errors (down from 22, all formerly in this file)"
    - "[PLAYER-01] D-04: the line-255 Animation.apply() is migrated 1:1 to the verified 10-arg shape (fromSetup=true, add=false, out=false, appliedPose=false); the surrounding sampleAnimationBounds try/catch + null-return graceful-degradation guards are byte-unchanged (Phase 41 content-less-STOP must-not-regress)"
    - "[PLAYER-01] D-04: spine-player 4.3's native calculateAnimationViewport is NOT adopted; the custom sampleAnimationBounds/sampleSetupBounds resilient path is preserved (only the 8 touchpoints migrate, not the algorithm)"
    - "[PLAYER-01] D-05: every spine-player/spine-webgl modal touchpoint is migrated per the audit; the T6 p.playTime private read/write is reworked to a TrackEntry-driven seek (no (p as any) cast), preserving pause-on-scrub and the literal updateWorldTransform(2)"
    - "[PLAYER-01] Pattern 2 lockstep: tests/renderer/animation-player-modal.spec.tsx mock surface (both defaultSpinePlayerImpl AND errorSpinePlayerImpl impl blocks + the call-order assertion) is migrated in the SAME unit; no stale 4.2 method name survives in the spec"
    - "[PLAYER-01] The ~11 formerly-RED tests/renderer/* suites pass under npm test; full npm test reports no new failures vs the documented pre-47 baseline"
    - "[informational] D-03: the spine-player bump is mechanically non-revertible under frozen-canonical 4.3.0 (live split-brain reproduced) — no revert fallback is encoded in this plan (D-01 holds)"
    - "[informational] D-07: --skip-ui — this plan designs zero new UI; it is a dep bump + 1:1 import migration + regression of the existing viewer"
    - "Pitfall 4 / security: src/renderer/index.html CSP and the app-image:// ACAO header are byte-unchanged; git diff shows zero lines under src/core/"
  artifacts:
    - path: "package.json"
      provides: "spine-player exact-pinned 4.3.0 dependency line"
      contains: '"@esotericsoftware/spine-player": "4.3.0"'
    - path: "src/renderer/src/modals/AnimationPlayerModal.tsx"
      provides: "Migrated 4.3.0 Pose-API modal (imports + 8 touchpoints)"
      min_lines: 600
    - path: "tests/renderer/animation-player-modal.spec.tsx"
      provides: "Lockstep-migrated 4.3.0 mock surface (2 impl blocks + call-order assertion)"
      min_lines: 500
  key_links:
    - from: "src/renderer/src/modals/AnimationPlayerModal.tsx"
      to: "@esotericsoftware/spine-player@4.3.0"
      via: "import { Physics, Skeleton, SpinePlayer, Vector2, type SpinePlayerConfig }"
      pattern: "from '@esotericsoftware/spine-player'"
    - from: "tests/renderer/animation-player-modal.spec.tsx"
      to: "the migrated modal's 4.3 call surface"
      via: "vi.mock('@esotericsoftware/spine-player') hoisted factory + per-instance impl blocks"
      pattern: "vi\\.mock\\('@esotericsoftware/spine-player'"
---

<objective>
Land the spine-player 4.2.111 → 4.3.0 bump as a **single atomic migration unit**: the
`package.json` exact-pin bump + lockfile regen, the full 8-touchpoint
`AnimationPlayerModal.tsx` Pose-API migration (imports drop + T1–T8 from the
RESEARCH D-04/D-05 verified tables), and the lockstep `animation-player-modal.spec.tsx`
test-mock migration — all in one wave with **no green intermediate state**.

Purpose: This closes PLAYER-01 (the machine-checkable half of Phase 47). The
codebase is in a reproduced split-brain *right now* (spine-player@4.2.111
bare-resolves frozen-canonical spine-core@4.3.0 where `MixBlend`/`MixDirection`
are deleted), so 11 renderer suites are RED at import and `typecheck:web` has 22
errors — all in this one file. Per D-03 the bump is mechanically non-revertible;
per D-01 there is no revert fallback. This migration is the only coherent path.

Output: `package.json` + `package-lock.json` (spine-player 4.3.0),
`AnimationPlayerModal.tsx` (migrated), `animation-player-modal.spec.tsx`
(lockstep-migrated). Machine-track green: `typecheck:web` 0 errors, 11 suites
GREEN, full `npm test` no new failures, no `MixBlend`/`MixDirection` in `src/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-CONTEXT.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-RESEARCH.md
@.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-PATTERNS.md

<interfaces>
<!-- Verified 4.3.0 contracts the executor needs. Extracted from RESEARCH.md D-04/D-05 -->
<!-- (HIGH confidence, cited against installed node_modules .d.ts). Use these directly. -->

The 4.3.0 Animation.apply() signature (node_modules/@esotericsoftware/spine-core/dist/Animation.d.ts:93):
```ts
apply(skeleton: Skeleton, lastTime: number, time: number, loop: boolean,
      events: Array<Event> | null, alpha: number,
      fromSetup: boolean, add: boolean, out: boolean, appliedPose: boolean): void;
```
Cross-confirmed by spine-player@4.3.0's own Player.js:644:
`animation.apply(this.skeleton, time, time, false, [], 1, true, false, false, false);`

The 4.3.0 Skeleton API the modal uses (node_modules/@esotericsoftware/spine-core/dist/Skeleton.d.ts):
```ts
setSkin(skinName: string): void          // :131  (public; setSkinByName is now PRIVATE :142)
setupPose(): void                        // :115  (was setToSetupPose)
setupPoseSlots(): void                   // :119  (was setSlotsToSetupPose)
getBounds(offset, size, temp?, clipper?) // :189  (UNCHANGED — returns the applied-pose AABB)
update(delta): void                      // :199  (UNCHANGED)
updateWorldTransform(physics: Physics)   // :113  (UNCHANGED; Physics.update === 2)
constructor(data: SkeletonData)          // :102  (UNCHANGED)
skin: Skin | null                        // :68   (UNCHANGED; Skin.name unchanged)
```

The 4.3.0 AnimationState / TrackEntry API (node_modules/@esotericsoftware/spine-core/dist/):
```ts
AnimationState.getTrack(trackIndex: number): TrackEntry | null   // :169 (replaces getCurrent)
AnimationState.update(delta): void / .apply(skeleton): boolean    // :59 / :65 (UNCHANGED)
TrackEntry.animation: Animation | null                            // :186 (.name / .duration unchanged)
TrackEntry.trackTime: number                                      // :271 (closest analog to old p.playTime)
TrackEntry.getAnimationTime(): number                             // :357 (clamped/loop-folded)
```

The 4.3.0 SpinePlayer / SpinePlayerConfig surface (npm pack spine-player@4.3.0 Player.d.ts):
```ts
SpinePlayerConfig.preserveDrawingBuffer: boolean   // :72  NOW REQUIRED (false = pre-bump effective default)
SpinePlayer.skeleton: Skeleton | null              // :125 (stricter null typing)
SpinePlayer.animationState: AnimationState | null  // :126 (stricter null typing)
SpinePlayer.playTime                               // :120 PRIVATE (no public setter)
// still exported: Physics, Skeleton, SpinePlayer, Vector2, SpinePlayerConfig
// REMOVED entirely from spine-core@4.3.0: MixBlend, MixDirection (0 .d.ts occurrences)
```
</interfaces>

<dependency-state>
package.json (verified current state, L25-36):
  "@esotericsoftware/spine-core": "4.3.0",       // L26 — canonical, FROZEN Phase 42 — DO NOT TOUCH
  "@esotericsoftware/spine-player": "4.2.111",   // L27 — THE ONLY LINE THIS PLAN CHANGES
  "spine-core-42": "npm:@esotericsoftware/spine-core@4.2.111"  // L35 — frozen alias — DO NOT TOUCH

src/renderer/index.html:7 (verified current CSP — must be byte-unchanged):
  content="default-src 'self'; style-src 'self' 'unsafe-inline'; script-src 'self'; img-src 'self' data: app-image:; font-src 'self' data:; connect-src 'self' app-image:;"
</dependency-state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump spine-player to exact 4.3.0 + regenerate the lockfile</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (lines 24-37 — see the verified current state in &lt;dependency-state&gt; above; confirm L27 is still "4.2.111" before editing)
    - .planning/phases/47-.../47-PATTERNS.md section "`package.json` line 27 (config, transform)" (the exact-pin convention + the DO-NOT-TOUCH L26/L35 note)
    - .planning/phases/47-.../47-RESEARCH.md section "Standard Stack" + "Installation" (the verified 4.3.0 publish facts; spine-player has no nested spine-core → bare-resolves canonical 4.3.0; no 4.3.x patch exists)
  </read_first>
  <action>
Change ONLY line 27 of `package.json` (the `@esotericsoftware/spine-player`
dependency entry). Apply the established exact-pin spine-dependency convention
(copy the shape of L26 `"@esotericsoftware/spine-core": "4.3.0"` — exact, no
caret, no range, sibling-aligned to the canonical spine-core):

  BEFORE: `"@esotericsoftware/spine-player": "4.2.111",`
  AFTER:  `"@esotericsoftware/spine-player": "4.3.0",`

Then regenerate `package-lock.json`:
```
npm install @esotericsoftware/spine-player@4.3.0
```
This pulls `@esotericsoftware/spine-webgl@4.3.0` transitively (spine-player@4.3.0
declares an exact `spine-webgl@4.3.0` pin); the previously-nested
`node_modules/@esotericsoftware/spine-webgl/node_modules/@esotericsoftware/spine-core@4.2.111`
disappears (spine-webgl@4.3.0's exact `spine-core@4.3.0` pin is satisfied by the
canonical top-level copy).

DO NOT edit `package.json` L26 (`spine-core: "4.3.0"` — frozen-canonical from
Phase 42) or L35 (`spine-core-42` alias — frozen Phase 42). DO NOT use a caret
or range — CLAUDE.md release-tag convention + RESEARCH Pitfall 1 (Esoteric ships
API changes in patch-shaped releases). Per RESEARCH D-03 the bump is NOT
revertible (frozen-canonical 4.3.0 split-brain) — there is no revert fallback in
this plan (D-01); do not add any "revert to 4.2.111" escape hatch.

This task leaves the tree NON-COMPILING (the modal still imports the now-deleted
MixBlend/MixDirection) — that is expected and correct (Pattern 1: single atomic
unit, no green intermediate). Task 2 + Task 3 land in the same wave and restore
green; do not stop or "verify green" between tasks.
  </action>
  <verify>
    <automated>grep -c '"@esotericsoftware/spine-player": "4.3.0"' package.json && node -e "const l=require('./package-lock.json'); const sp=l.packages?.['node_modules/@esotericsoftware/spine-player']?.version; const sw=l.packages?.['node_modules/@esotericsoftware/spine-webgl']?.version; if(sp!=='4.3.0') throw new Error('lock spine-player='+sp); if(sw!=='4.3.0') throw new Error('lock spine-webgl='+sw); console.log('LOCK_OK spine-player='+sp+' spine-webgl='+sw)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@esotericsoftware/spine-player": "4.3.0"' package.json` returns `1`
    - `grep -c '"@esotericsoftware/spine-player": "4.2.111"' package.json` returns `0`
    - `package-lock.json` resolves `node_modules/@esotericsoftware/spine-player` to version `4.3.0` AND `node_modules/@esotericsoftware/spine-webgl` to version `4.3.0` (the node -e guard above prints `LOCK_OK ...` and exits 0)
    - `grep -c '"@esotericsoftware/spine-core": "4.3.0"' package.json` still returns `1` (L26 untouched) and `grep -c 'spine-core-42' package.json` still returns `1` (L35 untouched)
    - `git diff --stat package.json` shows exactly 1 changed line (the spine-player entry)
  </acceptance_criteria>
  <done>package.json line 27 = `"@esotericsoftware/spine-player": "4.3.0"` (exact pin); package-lock.json regenerated with spine-player@4.3.0 + spine-webgl@4.3.0 transitive; spine-core L26 + spine-core-42 L35 byte-unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate AnimationPlayerModal.tsx — drop MixBlend/MixDirection + all 8 D-05 touchpoints (T1–T8)</name>
  <files>src/renderer/src/modals/AnimationPlayerModal.tsx</files>
  <behavior>
    - typecheck: `npm run typecheck:web` goes from 22 errors → 0 errors (every error was in this file: TS2554@255, TS2341@220/563/645/673/680, TS2339@242/282/488/614/668/221/564/646, TS2741@457, TS18047@549/550/563/564/675/676/679, TS2345@675)
    - import surface: zero `MixBlend` / `MixDirection` tokens remain in the file
    - graceful degradation preserved: a content-less / STOP-state animation still makes `sampleAnimationBounds` return `null` (NOT a fatal showError) — the try/catch + `if (!any) return null` + `if (!(width>0)...) return null` guards are byte-unchanged
    - scrub (T6): `onScrub` still calls `p.pause()` + `setIsPaused(true)` (pause-on-scrub) and still ends with `p.skeleton.updateWorldTransform(2)` (the literal 2 — CLAUDE.md fact #3); no `(p as any).playTime` cast anywhere
  </behavior>
  <read_first>
    - src/renderer/src/modals/AnimationPlayerModal.tsx (FULL FILE — read current state; the migration touches imports L45-53 and 8 sites across L210-299, L455-573, L662-683; you must see the real surrounding code, not assume)
    - .planning/phases/47-.../47-RESEARCH.md section "D-04 Answer" (lines 124-168 — the exact verified 10-arg apply() line + the full semantic mapping table + the must-not-regress proof) AND section "D-05 Answer" (lines 170-198 — the 8-touchpoint audit table T1-T8 with exact .d.ts citations + tsc error coordinates + the S1-S13 DO-NOT-EDIT stable list) AND section "Code Examples" (lines 356-406 — the verbatim before/after for imports, line-255, the T2/T3/T4 renames, the T5 track access)
    - .planning/phases/47-.../47-RESEARCH.md section "GL Straight-Alpha" (lines 289-320 — confirms the modal's `premultipliedAlpha:false`/`alpha:false` config stays UNCHANGED; T7 ONLY adds `preserveDrawingBuffer:false`) AND "Pitfall 5" (lines 348-352 — the T6 scrub-rework hazard) AND "Pitfall 3 / 4" (the GL + CSP do-not-touch guardrails)
    - .planning/phases/47-.../47-PATTERNS.md section "`src/renderer/src/modals/AnimationPlayerModal.tsx`" (lines 60-198 — every touchpoint anchored to its surrounding modal context: makeProbe L215-223, sampleAnimationBounds L234-275, onScrub L663-683, the stable touchpoint list)
  </read_first>
  <action>
Migrate `AnimationPlayerModal.tsx` to the verified 4.3.0 Pose API. Apply ALL of
the following — the line-255 fix alone leaves 21 of 22 errors (RESEARCH Pitfall
1). Copy the migrated code from RESEARCH D-04/D-05/Code-Examples **verbatim** —
it is HIGH-confidence with `.d.ts`-line citations; do NOT re-derive or guess.

**Imports (drop the 2 removed symbols) — current L45-53:**
Replace the import block with (RESEARCH "Imports (drop the two removed symbols)"):
```ts
import {
  Physics,
  Skeleton,
  SpinePlayer,
  Vector2,
  type SpinePlayerConfig,
} from '@esotericsoftware/spine-player';
```
`Physics`/`Skeleton`/`SpinePlayer`/`Vector2`/`SpinePlayerConfig` are all still
exported by spine-player@4.3.0 (verified). `MixBlend`/`MixDirection` are GONE
(0 occurrences in spine-core@4.3.0).

**T1 — Animation.apply() 8-arg → 10-arg (the one true API break, L255 inside
sampleAnimationBounds).** Replace:
```ts
anim.apply(probe, t, t, false, [], 1, MixBlend.setup, MixDirection.mixIn);
```
with the verified D-04 line (cross-confirmed vs spine-player Player.js:644):
```ts
anim.apply(probe, t, t, false, [], 1, /*fromSetup*/ true, /*add*/ false, /*out*/ false, /*appliedPose*/ false);
```
`MixBlend.setup → fromSetup=true`; `MixDirection.mixIn → out=false`; new
`add=false`, `appliedPose=false`. **The surrounding `try/catch` + `if (!any)
return null` + `if (!(width>0)...) return null` graceful-degradation guards
(L238, L267, L270, L272-274) MUST stay byte-unchanged** — RESEARCH D-04 proves
the signature change does not regress the content-less-STOP-animation `null`
return (the Phase 41 must-not-regress invariant). Do NOT adopt spine-player
4.3's native `calculateAnimationViewport` (D-04 REJECTED — it calls a fatal
`showError` on content-less animations; the custom resilient path exists to
avoid exactly that). Keep `sampleAnimationBounds`/`sampleSetupBounds`'s
algorithm verbatim — migrate only the touchpoints, not the logic.

**T2 — `setSkinByName` → `setSkin` (now private → public, 3 sites: L220
makeProbe, L563 success cb, L645 onSkinChange):**
`probe.setSkinByName(skinName)` → `probe.setSkin(skinName)` (string overload,
Skeleton.d.ts:131). PRESERVE the skin-set-THEN-slots-reset call order at every
site (4.3 `setSkin` JSDoc mandates `setupPoseSlots` after `setSkin`; memory
`project_strict_loadermode_separation` — leftover-skin-bleed guard).

**T3 — `setToSetupPose` → `setupPose` (2 sites: L242 sampleAnimationBounds,
L283 sampleSetupBounds):** `probe.setToSetupPose()` → `probe.setupPose()`
(Skeleton.d.ts:115).

**T4 — `setSlotsToSetupPose` → `setupPoseSlots` (3 sites: L221 makeProbe,
L564 success, L646 onSkinChange):** `probe.setSlotsToSetupPose()` →
`probe.setupPoseSlots()` (Skeleton.d.ts:119).

**T5 — `getCurrent(0)` → `getTrack(0)` (3 sites: L488 update cb, L614
refreshBounds, L668 onScrub):** `p.animationState?.getCurrent(0)` →
`p.animationState?.getTrack(0)` (AnimationState.d.ts:169). `TrackEntry.animation`
→ `.name`/`.duration` are unchanged.

**T6 — `p.playTime` private read+write → TrackEntry-driven scrub (THE
design-sensitive item; NOT a rename — see Pitfall 5).** In `onScrub`
(L663-683): the line `const delta = targetTime - p.playTime;` (L673) and
`p.playTime = targetTime;` (L680) both access the PRIVATE `playTime`. Rework
per RESEARCH D-05 T6 + Pitfall 5:
  - Take `entry` from `p.animationState.getTrack(0)` (T5, already applied above).
  - Compute `targetTime = entry.animation.duration * percentage` (the modal's
    own `scrubPercent` React state remains the UI source of truth).
  - Take the delta base from `entry.trackTime` (TrackEntry.d.ts:271 — the
    closest analog to the old `p.playTime`): `const delta = targetTime -
    entry.trackTime;`
  - **DROP the `p.playTime = targetTime;` write-back entirely** (scrubPercent
    is the UI source of truth — RESEARCH D-05 T6).
  - KEEP unchanged: `p.pause()`, `setIsPaused(true)` (pause-on-scrub), the
    existing `p.animationState.update(delta)` + `p.animationState.apply(p.skeleton)`
    + `p.skeleton.update(delta)` + `p.skeleton.updateWorldTransform(2)` sequence,
    and the literal `2` (CLAUDE.md fact #3).
  - **NO `(p as any).playTime` cast** — it is a latent break + code-review
    reject (RESEARCH "Don't Hand-Roll"). Use only the public `TrackEntry`
    surface. (This is the item the D-02 owner UAT #2 forward-AND-backward scrub
    is the only valid acceptance gate for — Plan 02.)

**T7 — add `preserveDrawingBuffer: false`** to the `SpinePlayerConfig` object
literal (the straight-alpha config block, ~L457-473). It is now required
(Player.d.ts:72); `false` matches the pre-bump effective default
(`new ManagedWebGLRenderingContext(canvas, { alpha: config.alpha,
preserveDrawingBuffer: config.preserveDrawingBuffer })`). **Do NOT change
`premultipliedAlpha:false` or `alpha:false`** — RESEARCH GL section + Pitfall 3:
the 4.3.0 mechanism moved but the config still produces a correct straight-alpha
render; only the owner UAT (Plan 02) may change it if a halo is empirically
observed. The L469 modal comment cites the now-deleted "Player.js:13167"
mechanism — you may leave the comment or update it to reference the 4.3.0
behavior (texture-upload `UNPACK_PREMULTIPLY_ALPHA_WEBGL = !pma`), but do NOT
change the config values.

**T8 — `skeleton`/`animationState` null-guards** at L549,550,563,564,675,676,679.
`p.skeleton`/`p.animationState` are now `Skeleton|null`/`AnimationState|null`
(Player.d.ts:125-126). Add `if (!p.skeleton) return;` (or optional chaining) at
the flagged sites. The `success` callback runs post-load so `p.skeleton` is
non-null there; the type just got stricter — a guard or optional-chain that
preserves existing behavior is correct.

**DO NOT edit any S1–S13 stable touchpoint** (RESEARCH D-05 lines 184-196):
`new Skeleton(data)`, `getBounds`, `Skeleton.update`,
`updateWorldTransform(Physics)`, `skin?.name`, `SkeletonData.find*`,
`AnimationState.update/apply`, `setAnimation`, `play/pause/dispose`,
`sceneRenderer/camera/canvas`, the fixed `config.viewport` dummy, atlas
parent-dir resolution, the swallow-constructor-throw try/catch. These are
API-stable; their render-behavior correctness is verified by the Plan 02 owner
UAT, not by code edits here.

**DO NOT touch `src/renderer/index.html` CSP or any `src/core/*` file**
(RESEARCH Pitfall 4 + Anti-Patterns; out of scope per CONTEXT). No CSP/CORS
diff is allowed in this plan.

This task is part of the single atomic unit — the tree only compiles once Task 3
(the lockstep mock) also lands. Do not "verify green" between Task 2 and Task 3;
the wave-level verification covers it.
  </action>
  <verify>
    <automated>grep -c 'MixBlend\|MixDirection' src/renderer/src/modals/AnimationPlayerModal.tsx; npm run typecheck:web 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MixBlend\|MixDirection" src/renderer/src/modals/AnimationPlayerModal.tsx` returns `0`
    - `grep -c "setSkinByName\|setToSetupPose\|setSlotsToSetupPose\|getCurrent" src/renderer/src/modals/AnimationPlayerModal.tsx` returns `0` (all migrated to setSkin/setupPose/setupPoseSlots/getTrack)
    - `grep -c "\.apply(probe, t, t, false, \[\], 1, " src/renderer/src/modals/AnimationPlayerModal.tsx` returns `1` (the migrated 10-arg line-255 shape present)
    - `grep -c "(p as any).playTime\|(p as any)\['playTime'\]" src/renderer/src/modals/AnimationPlayerModal.tsx` returns `0` (no private-field cast — T6 done via TrackEntry)
    - `grep -c "p.playTime\s*=" src/renderer/src/modals/AnimationPlayerModal.tsx` returns `0` (the private write-back is dropped, T6)
    - `grep -c "updateWorldTransform(2)" src/renderer/src/modals/AnimationPlayerModal.tsx` returns ≥ `1` (the CLAUDE.md fact-#3 literal `2` in onScrub is preserved)
    - `grep -c "preserveDrawingBuffer: false" src/renderer/src/modals/AnimationPlayerModal.tsx` returns `1` (T7 added)
    - `grep -c "premultipliedAlpha: false" src/renderer/src/modals/AnimationPlayerModal.tsx` returns `1` AND `grep -c "alpha: false" src/renderer/src/modals/AnimationPlayerModal.tsx` returns ≥ `1` (the Phase 41 G-03 straight-alpha config UNCHANGED — Pitfall 3)
    - `git diff --stat src/renderer/index.html | grep -c index.html` returns `0` (CSP byte-unchanged — Pitfall 4 / security)
    - `git diff --name-only | grep -c '^src/core/'` returns `0` (zero src/core/ change — scope invariant)
    - `npm run typecheck:web` exits 0 with 0 errors (the 22 errors, all formerly in this file, are resolved). NOTE: this becomes fully green only AFTER Task 3 lands too (the modal imports compile but the lockstep mock is part of the same unit); the binding wave-level gate is the &lt;verification&gt; block.
  </acceptance_criteria>
  <done>AnimationPlayerModal.tsx imports drop MixBlend/MixDirection; T1 (line-255 1:1 to verified 10-arg), T2 (setSkin ×3, call-order preserved), T3 (setupPose ×2), T4 (setupPoseSlots ×3), T5 (getTrack ×3), T6 (TrackEntry-driven scrub, no cast, write-back dropped, pause-on-scrub + literal 2 kept), T7 (+preserveDrawingBuffer:false, alpha config unchanged), T8 (null-guards) all migrated; S1-S13 + CSP + src/core/ byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Lockstep-migrate the test mock (2 impl blocks + call-order assertion) so the spec cannot green-wash a broken modal</name>
  <files>tests/renderer/animation-player-modal.spec.tsx</files>
  <read_first>
    - tests/renderer/animation-player-modal.spec.tsx (FULL FILE — read current state; the stale-4.2 surface is in 3 distinct locations: the hoisted vi.mock factory ~L85-110, defaultSpinePlayerImpl ~L126-156, errorSpinePlayerImpl ~L305-325, and the call-order assertion ~L594-615)
    - .planning/phases/47-.../47-PATTERNS.md section "`tests/renderer/animation-player-modal.spec.tsx`" (lines 201-280 — names EVERY stale line: L91/92/93 mock Skeleton methods, L106/107 MixBlend/MixDirection exports, L131/143/144/149 defaultSpinePlayerImpl, L305-325 errorSpinePlayerImpl mirror, L594-615 call-order assertion + the lockstep grep gate)
    - .planning/phases/47-.../47-RESEARCH.md section "Pattern 2" (lines 264-268) + "Pitfall 2" (lines 330-334) — the false-green mechanism + the exact stale lines
    - src/renderer/src/modals/AnimationPlayerModal.tsx (the JUST-MIGRATED modal — the mock must mirror its new 4.3 call surface: setSkin/setupPose/setupPoseSlots/getTrack, no MixBlend/MixDirection, TrackEntry.trackTime)
  </read_first>
  <action>
Migrate the 4.2 mock surface to the migrated modal's 4.3 call surface — in the
**same change** as Task 2 (Pattern 2; a stale mock green-washes a broken modal —
the `feedback_verify_all_entrypoint_runtimes` class: testing against a fiction).
There are **3 distinct locations** (PATTERNS confirmed 2 impl blocks, not 1):

**1. The hoisted vi.mock factory mock `Skeleton` class + module exports
(~L85-110):**
  - `setSkinByName(): void {}` → `setSkin(): void {}`
  - `setSlotsToSetupPose(): void {}` → `setupPoseSlots(): void {}`
  - `setToSetupPose(): void {}` → `setupPose(): void {}`
  - In the returned module object: DROP `MixBlend: { setup: 0 }` and
    `MixDirection: { mixIn: 0 }` (removed in 4.3.0). KEEP
    `Physics: { update: 2 }` (still exported), `SpinePlayer`, `Skeleton`,
    `Vector2`.

**2. `defaultSpinePlayerImpl` mock player object (~L126-156):**
  - `playTime: 0` (L131) — the migrated modal no longer reads/writes
    `playTime`; drop it or leave it inert. Ensure the mocked
    `animationState.getTrack` returns an entry exposing the `trackTime` the
    migrated `onScrub` now reads.
  - `setSkinByName: vi.fn()` (L143) → `setSkin: vi.fn()`
  - `setSlotsToSetupPose: vi.fn()` (L144) → `setupPoseSlots: vi.fn()`
  - `getCurrent: vi.fn(() => ({ animation: { duration: 1, name: 'idle' },
    loop: true }))` (L149) → `getTrack: vi.fn(() => ({ animation: { duration: 1,
    name: 'idle' }, trackTime: 0 }))` (add `trackTime` so the migrated
    TrackEntry-driven scrub seek has a delta base; keep `animation.duration` +
    `animation.name`).

**3. `errorSpinePlayerImpl` mock player object (~L305-325):** the SAME renames
apply — `setSkinByName`→`setSkin` (L315), `setSlotsToSetupPose`→`setupPoseSlots`
(L316), `getCurrent`→`getTrack` (L321 → `getTrack: vi.fn(() => null)`),
`playTime` (L311) drop/inert. This second impl block mirrors block 2 and MUST
migrate identically.

**4. The call-order assertion (~L594-615)** asserts the OLD contract — rename
the test title + the 4 references:
  - the test title `'skin onChange calls setSkinByName THEN setSlotsToSetupPose
    in that order'` → `'... calls setSkin THEN setupPoseSlots ...'`
  - `inst.player.skeleton.setSkinByName` (L605) → `.setSkin`
  - `inst.player.skeleton.setSlotsToSetupPose` (L606) → `.setupPoseSlots`
  - `expect(setSkinByName).toHaveBeenCalledWith('red')` (L614) → `setSkin`
  - `expect(setSlotsToSetupPose).toHaveBeenCalledTimes(1)` (L615) →
    `setupPoseSlots`
The migrated modal calls `setSkin` THEN `setupPoseSlots`; asserting the old
names asserts a contract the modal no longer has (Pitfall 2).

Do NOT alter any other test logic — only the mock surface + the call-order
assertion's names. Do not weaken assertions to "make it pass"; the suite must
exercise the real 4.3 call shape.
  </action>
  <verify>
    <automated>grep -c "setSkinByName\|setToSetupPose\|setSlotsToSetupPose\|getCurrent\|MixBlend\|MixDirection" tests/renderer/animation-player-modal.spec.tsx; npx vitest run tests/renderer/animation-player-modal.spec.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setSkinByName\|setToSetupPose\|setSlotsToSetupPose\|getCurrent\|MixBlend\|MixDirection" tests/renderer/animation-player-modal.spec.tsx` returns `0` (the lockstep gate — RESEARCH Test Map line 479: zero stale-4.2 hits in the spec)
    - `grep -c "setSkin\b\|setupPose\b\|setupPoseSlots\b\|getTrack\b" tests/renderer/animation-player-modal.spec.tsx` returns ≥ `4` (the migrated 4.3 names are present in the mock + assertion)
    - `grep -c "trackTime" tests/renderer/animation-player-modal.spec.tsx` returns ≥ `1` (the mocked getTrack entry exposes trackTime for the migrated scrub)
    - `npx vitest run tests/renderer/animation-player-modal.spec.tsx` exits 0 (the spec's own suite passes against the migrated mock)
  </acceptance_criteria>
  <done>Both mock impl blocks (defaultSpinePlayerImpl + errorSpinePlayerImpl), the hoisted vi.mock factory Skeleton/exports, and the call-order assertion are migrated to the 4.3 surface (setSkin/setupPose/setupPoseSlots/getTrack/trackTime; no MixBlend/MixDirection); the spec passes; zero stale-4.2 token in the file (lockstep gate green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer ↔ `app-image://` protocol (main) | The viewer fetches fixture JSON + atlas page PNGs over the custom `app-image://` scheme; CSP `connect-src 'self' app-image:` + the main-process ACAO header are the Phase 41 G-01/G-02 hard-won origin scope. The dep bump must not widen this. |
| build graph ↔ npm registry | `npm install @esotericsoftware/spine-player@4.3.0` pulls `spine-webgl@4.3.0` transitively + bare-resolves the frozen-canonical `spine-core@4.3.0`. Supply-chain surface = the exact-pin + committed lockfile. |
| fixture JSON ↔ spine-player parser | The bumped player parses the same fixture JSON the viewer always did (no new input path). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Elevation of Privilege / Information Disclosure | `src/renderer/index.html` CSP + the main-process `app-image://` ACAO header | mitigate | The S12 RESEARCH audit verified spine-player@4.3.0 uses the **same** parent-relative `AssetManager`+`rawDataURIs` model as 4.2.111 — zero new origin/`connect-src`/ACAO is needed. Task 2 acceptance criteria assert `git diff --stat src/renderer/index.html` shows **zero** changed lines and there is no task proposing a widened CSP/CORS directive (RESEARCH Pitfall 4 / Anti-Pattern; ASVS V14). Plan 02's owner UAT #5 (atlas-less parity) empirically re-confirms the asset path resolves under the unchanged origin scope. |
| T-47-02 | Tampering (supply chain) | `package.json` / `package-lock.json` spine-player version | mitigate | Exact-pin `"4.3.0"` (no caret/range — CLAUDE.md release-tag convention + RESEARCH Pitfall 1: Esoteric ships API changes in patch-shaped releases) + committed regenerated `package-lock.json` + `npm ci` in CI (project convention). Task 1 acceptance criteria assert the lockfile resolves spine-player AND spine-webgl to exactly `4.3.0`. The currently-live D-03 split-brain is itself a tampering-class instance (a mismatched runtime) — this migration *resolves* it; the runtime-distinctness posture is owned by Phase 42's gates and is not re-litigated here. |
| T-47-03 | Denial of Service | the bumped spine-player parsing malformed/oversized fixture JSON in the renderer | accept | Pre-existing controls already contain this and are unchanged by the bump: the resilient `sampleAnimationBounds` (D-04 must-not-regress, explicitly preserved byte-for-byte in Task 2) returns `null` on content-less/STOP state instead of crashing, and the terminal-error overlay handles malformed input. No new input surface is introduced (V5 N/A — RESEARCH Security Domain). Residual risk is the pre-existing viewer's, re-verified (not newly introduced) by Plan 02 owner UAT #4. Low value target (read-only desktop analysis app, no PII). |
</threat_model>

<verification>
**Wave-level machine gate (binding — run after all 3 tasks land; this is the
single atomic unit's only meaningful green checkpoint):**

1. `npm run typecheck:web` → exits 0 with **0 errors** (from the documented 22,
   all formerly in `AnimationPlayerModal.tsx`).
2. `npx vitest run tests/renderer/` → **0 failed suites** (the ~11 formerly-RED
   import-failure suites are now GREEN — they import the modal transitively and
   go green once it compiles + the lockstep mock matches).
3. `npm test` (full vitest run) → **no new failures** vs the documented pre-47
   baseline (STATE 2026-05-18: 1280 passed / 0 actual failures; the 11
   `tests/renderer/*` MixBlend suites were RED-at-import and are now FIXED by
   this plan). Any residual must be the separately-tracked pre-existing set, not
   a new regression.
4. `! grep -rq "MixBlend\|MixDirection" src/` → no MixBlend/MixDirection anywhere
   in `src/`.
5. `! grep -q "setSkinByName\|setToSetupPose\|setSlotsToSetupPose\|getCurrent\|MixBlend\|MixDirection" tests/renderer/animation-player-modal.spec.tsx`
   → the lockstep mock has zero stale-4.2 surface.
6. `git diff --name-only | grep -E '^src/core/' | wc -l` → `0` (zero `src/core/`
   change — scope invariant).
7. `git diff --stat src/renderer/index.html` → empty (CSP byte-unchanged —
   security guardrail T-47-01).
8. `git diff --name-only` over the spine-player ACAO main-process handler → no
   diff (CSP/CORS non-broadening — T-47-01).

**typecheck:node baseline note (do NOT block on clean):** `npm run
typecheck:node` is **pre-existingly RED** (memory
`project_typecheck_node_preexisting_red` — v1.0-era probe scripts + Phase 43/44
test-family gap). Phase 47 must only prove via `git diff --name-only` that this
plan did **not worsen** it (this plan touches no `tests/` typecheck:node-scoped
file beyond the renderer spec and no `src/core/`/`src/main/` file). The
post-merge build gate uses `typecheck`/`typecheck:web`, **not** `npm run build`
(electron-builder). Do not require `typecheck:node` clean.
</verification>

<success_criteria>
- `package.json` declares `"@esotericsoftware/spine-player": "4.3.0"` (exact
  pin); `package-lock.json` regenerated with spine-player@4.3.0 +
  spine-webgl@4.3.0; spine-core L26 + spine-core-42 L35 byte-unchanged.
- `AnimationPlayerModal.tsx`: zero MixBlend/MixDirection; T1–T8 migrated per the
  RESEARCH-verified tables; the resilient `sampleAnimationBounds` null-return +
  pause-on-scrub + literal `2` preserved; `premultipliedAlpha:false`/`alpha:false`
  unchanged (only `preserveDrawingBuffer:false` added).
- `animation-player-modal.spec.tsx`: both mock impl blocks + the hoisted factory
  + the call-order assertion migrated in lockstep; zero stale-4.2 token.
- Machine-track green: `typecheck:web` 0, 11 renderer suites GREEN, full
  `npm test` no new failures, no MixBlend/MixDirection in `src/`.
- Scope/security invariants: zero `src/core/` diff; CSP + `app-image://` ACAO
  byte-unchanged; no revert fallback encoded (D-01/D-03).
- PLAYER-01 satisfied. (PLAYER-02 — the visual/owner half — is Plan 02.)
</success_criteria>

<output>
After completion, create
`.planning/phases/47-spine-player-4-3-0-bump-viewer-regression/47-01-SUMMARY.md`
recording: the bump commit, the modal-migration commit(s), the lockstep-mock
commit; the before→after typecheck:web error count (22→0); the formerly-RED →
GREEN renderer suite count; any deviation from the RESEARCH-verified migration
(there should be none — copy verbatim); explicit confirmation of zero `src/core/`
and zero CSP/CORS diff; and the documented `typecheck:node` pre-existing-RED
non-worsening check.
</output>
