---
phase: 48-core-scale-bake-module-regression-oracle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.json
  - fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.atlas
  - fixtures/SCALE_BAKE_4_2/TEST_01.json
  - fixtures/SCALE_BAKE_4_2/TEST_01.atlas
  - fixtures/SCALE_BAKE_4_2/TEST_03.json
  - fixtures/SCALE_BAKE_4_2/TEST_03.atlas
  - fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json
  - fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.atlas
autonomous: true
requirements: [BAKE-04]

must_haves:
  truths:
    - "DEMON .json+.atlas are committed (json+atlas ONLY, no PNGs) into fixtures/SCALE_BAKE_4_3/ — tracked and reachable on a fresh clone"
    - "TEST_01 .json+.atlas (deform-heavy 4.2 all-four-types) are committed into fixtures/SCALE_BAKE_4_2/ — tracked and reachable"
    - "TEST_03 .json+.atlas (4.2 ik-softness-curve) are committed into fixtures/SCALE_BAKE_4_2/ — tracked and reachable"
    - "A tiny synthetic 4.3 path-Fixed fixture (positionMode+spacingMode fixed, path timeline animating BOTH position+spacing, >=2 keys, >=1 curve) + minimal 1-region .atlas exist in fixtures/SCALE_BAKE_PATH_43/"
    - "git check-ignore prints nothing for every committed fixture; git ls-files --error-unmatch succeeds for each; git archive HEAD lists each .json AND each .atlas"
    - "NO PNG bytes are committed under any SCALE_BAKE_* dir (PNG-exclusion = IP protection, D-04)"
  artifacts:
    - path: "fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.json"
      provides: "4.3 all-constraint-types + physics + slider + mesh stress rig (DEMON copy)"
      contains: "4.3.02"
    - path: "fixtures/SCALE_BAKE_4_2/TEST_01.json"
      provides: "deform-heavy 4.2 all-four-types rig (transform+ik+path+physics, deform x18)"
      contains: "4.2.43"
    - path: "fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json"
      provides: "synthetic 4.3 path-Fixed fixture — the only on-disk coverage for the PATH length-mode timeline channel"
      contains: "fixed"
  key_links:
    - from: "fixtures/SCALE_BAKE_*"
      to: "the committed git tree (CI clone)"
      via: "COPY into new non-ignored dirs (NOT git add of an ignored/untracked source dir)"
      pattern: "SCALE_BAKE"
---

<objective>
Make every fixture the regression oracle depends on **actually committed and reachable on a fresh CI clone** — not just present on the author's disk. This is the explicit, separate D-06a fixture-commit-safety task the CONTEXT mandates, plus the one authored synthetic fixture (D-05) for the single channel no real rig exercises.

Purpose: a gitignored/untracked fixture passes locally then breaks CI on a clean clone — the exact v1.3.1 landmine ([[feedback_gitignore_fixtures_check_test_refs]]). DEMON is untracked-not-ignored (`git add` would stage 174MB of PNGs); TEST_01 lives under a dir-ignored path (`git add` silently no-ops). The root-cause fix is to COPY `.json`+`.atlas` (NEVER PNGs) into brand-new non-ignored dirs, then PROVE they are tracked with the authoritative "would CI see it?" git commands.
Output: three new `fixtures/SCALE_BAKE_*` dirs, json+atlas-only, proven tracked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-core-scale-bake-module-regression-oracle/48-CONTEXT.md
@.planning/phases/48-core-scale-bake-module-regression-oracle/48-RESEARCH.md
@.planning/phases/48-core-scale-bake-module-regression-oracle/48-PATTERNS.md

<interfaces>
<!-- VERIFIED git-status reality this session (matches RESEARCH section Fixture-Commit Safety exactly): -->
<!-- fixtures/DEMON/SKINS_SPINE_V02.json  - UNTRACKED, NOT ignored (check-ignore exit 1). git add fixtures/DEMON/ stages 63 PNGs / 174MB -> DO NOT. -->
<!-- fixtures/MON_FILES/EXPORT/TEST_01/4.2/TEST_01.json - DIR-IGNORED (.gitignore: fixtures/MON_FILES/). check-ignore MATCHES -> git add silently no-ops (v1.3.1 landmine). -->
<!-- fixtures/MON_FILES/EXPORT/TEST_03/4.2/TEST_03.{json,atlas} - same dir-ignored; both present on disk. -->
<!-- Tracked + reachable already (oracle reuses in place, NO action): spineboy_4.3, SLIDER_4_3, SIMPLE_PROJECT_43, SIMPLE_PROJECT. -->

Real .atlas text shape (text-only, NO pixel data - D-04 confirms committing it leaks nothing). From fixtures/SIMPLE_PROJECT_43/skeleton2.atlas, per-region: a regionName line, then bounds:x,y,w,h, optional rotate:90; header is page.png / size:W,H / filter:...

The synthetic path fixture animation timeline structure (4.3, VERIFIED vs SkeletonJson.js:985-999):
animations[animName].path[constraintName][timelineName] where timelineName is position | spacing | mix.
The bake scales the position channel keys iff positionMode===Fixed, spacing channel keys iff spacingMode===Length||Fixed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: COPY DEMON + TEST_01 + TEST_03 (.json+.atlas ONLY) into new non-ignored fixture dirs</name>
  <files>fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.json, fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.atlas, fixtures/SCALE_BAKE_4_2/TEST_01.json, fixtures/SCALE_BAKE_4_2/TEST_01.atlas, fixtures/SCALE_BAKE_4_2/TEST_03.json, fixtures/SCALE_BAKE_4_2/TEST_03.atlas</files>
  <read_first>
    - .planning/phases/48-core-scale-bake-module-regression-oracle/48-RESEARCH.md (section "Fixture-Commit Safety (D-06a) - concrete, verified recipe")
    - .planning/phases/48-core-scale-bake-module-regression-oracle/48-CONTEXT.md (D-06a - the four-step defense, in order)
    - .planning/phases/48-core-scale-bake-module-regression-oracle/48-PATTERNS.md ("The three dirs + their provenance" table)
  </read_first>
  <action>
COPY (do NOT move, do NOT `git add` the source dirs) exactly these six files into two NEW non-ignored dirs. A brand-new dir has no PNGs to accidentally stage and no ignore rule to fight (RESEARCH D-06a option 1). Use `cp` (these are existing on-disk files; copying fixtures via shell `cp` is correct — do NOT hand-retype 700KB JSON):

```bash
mkdir -p fixtures/SCALE_BAKE_4_3 fixtures/SCALE_BAKE_4_2
cp fixtures/DEMON/SKINS_SPINE_V02.json  fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.json
cp fixtures/DEMON/SKINS_SPINE_V02.atlas fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.atlas
cp "fixtures/MON_FILES/EXPORT/TEST_01/4.2/TEST_01.json"  fixtures/SCALE_BAKE_4_2/TEST_01.json
cp "fixtures/MON_FILES/EXPORT/TEST_01/4.2/TEST_01.atlas" fixtures/SCALE_BAKE_4_2/TEST_01.atlas
cp "fixtures/MON_FILES/EXPORT/TEST_03/4.2/TEST_03.json"  fixtures/SCALE_BAKE_4_2/TEST_03.json
cp "fixtures/MON_FILES/EXPORT/TEST_03/4.2/TEST_03.atlas" fixtures/SCALE_BAKE_4_2/TEST_03.atlas
```

CRITICAL - do NOT copy any `.png`. The PNG-exclusion IS the IP-protection mechanism (D-04): only the painted PNGs carry confidential art; the `.json` (geometry) and `.atlas` (region names + page dims + UV rects + a page filename, NO pixel data) leak nothing. Verify zero PNGs landed in the new dirs after copying.

Do NOT `git add fixtures/DEMON/` (would stage 174MB of PNGs) and do NOT try to un-ignore `fixtures/MON_FILES/` (git cannot re-include a child of a dir-excluded path without a fragile descend-then-reinclude rewrite - the COPY approach avoids this entirely).
  </action>
  <verify>
    <automated>test -f fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.json && test -f fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.atlas && test -f fixtures/SCALE_BAKE_4_2/TEST_01.json && test -f fixtures/SCALE_BAKE_4_2/TEST_03.json && test -z "$(find fixtures/SCALE_BAKE_4_3 fixtures/SCALE_BAKE_4_2 -name '*.png')" && echo OK_FIXTURES_NO_PNG</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist: `fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.{json,atlas}`, `fixtures/SCALE_BAKE_4_2/TEST_01.{json,atlas}`, `fixtures/SCALE_BAKE_4_2/TEST_03.{json,atlas}`
    - `find fixtures/SCALE_BAKE_4_3 fixtures/SCALE_BAKE_4_2 -name '*.png'` returns NOTHING (zero PNGs committed)
    - `grep -c "4.3.02" fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.json` returns >= 1 (correct DEMON rig copied)
    - `grep -c "4.2.43" fixtures/SCALE_BAKE_4_2/TEST_01.json` returns >= 1 (correct TEST_01 rig copied)
    - `head -1 fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.atlas` shows a `.png` page-filename line (atlas is text-only)
  </acceptance_criteria>
  <done>
    DEMON, TEST_01, and TEST_03 .json+.atlas are copied into the two new non-ignored dirs with zero PNGs. Source dirs untouched (not git-added).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the synthetic 4.3 path-Fixed fixture (D-05 residual gap) + minimal .atlas</name>
  <files>fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json, fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.atlas</files>
  <read_first>
    - .planning/phases/48-core-scale-bake-module-regression-oracle/48-RESEARCH.md (section "Residual gap decision (resolves D-05)" - the exact minimal shape)
    - .planning/phases/48-core-scale-bake-module-regression-oracle/48-PATTERNS.md ("Minimal synthetic .atlas for fixtures/SCALE_BAKE_PATH_43/" - the verbatim atlas; the synthetic-rig shape row)
    - node_modules/@esotericsoftware/spine-core/dist/SkeletonJson.js (lines 985-999 - the path position/spacing timeline the fixture must drive)
  </read_first>
  <action>
Author a TINY synthetic 4.3 skeleton (stable version, ~60-100 lines JSON) - the smallest fixture driving the PATH position/spacing length-mode timeline channel (SkeletonJson.js:994/999), the one channel NO real on-disk rig exercises (every real path rig uses positionMode=Percent, spacingMode=Length, zero spacing timelines).

Write `fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json` with:
- `skeleton`: `{ "spine": "4.3.0", "x": 0, "y": 0, "width": 64, "height": 64 }` (STABLE 4.3 version - beta versions are typed-rejected, RESEARCH Pitfall 5).
- `bones`: a `root` + at least 2 more bones (the path needs bones to follow).
- `slots`: one slot bound to a bone, carrying the path attachment.
- `skins`: a `default` skin with one `path`-type attachment carrying a few `vertices` and a `lengths` array (e.g. `"closed": false, "vertexCount": 4, "lengths": [10, 20], "vertices": [ ... ]`).
- `constraints`: one 4.3 unified path constraint with **`"type": "path"`, `"positionMode": "fixed"`, AND `"spacingMode": "fixed"`** (the two modes that trigger x-s on the timeline), plus `bones` + `target`.
- `animations`: one animation with a `path` timeline animating BOTH `position` AND `spacing` on that constraint, each with **>=2 keyframes** and **>=1 keyframe carrying a `curve` array** (so the cy-scale path is exercised). Use the verified 4.3 structure `animations.<name>.path.<constraintName>.{ "position": [ ... ], "spacing": [ ... ] }`. Example keys: `{ "time": 0, "value": 30, "curve": [0.25, 0, 0.75, 60] }` then `{ "time": 1, "value": 60 }`.

Keep all referenced names (bone/slot/target/constraint) wired correctly so `SkeletonJson.readSkeletonData` parses it without error (the oracle parses it on both sides). Keep it self-consistent and minimal.

Write `fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.atlas` VERBATIM (single 1-region page; oracle reads only region dims, never pixels):
```
PATH_FIXED.png
size:64,64
filter:Linear,Linear
region
bounds:0,0,32,32
```
Do NOT create a PNG (none needed - math phase reads zero PNG bytes).

The structural correctness of this fixture (parses on both oracle sides; drives the position+spacing timeline scale) is PROVEN by the oracle test in 48-04, which parses it through both spine-core sides and asserts field-identity. This task's job is to author it; the oracle is the proof.
  </action>
  <verify>
    <automated>test -f fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json && test -f fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.atlas && grep -qi '"positionmode"[^,]*"fixed"' fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json && grep -qi '"spacingmode"[^,]*"fixed"' fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json && grep -q '"spacing"' fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json && grep -q '"curve"' fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json && echo OK_PATH_FIXED_SHAPE</automated>
  </verify>
  <acceptance_criteria>
    - `fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json` and `fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.atlas` both exist
    - JSON contains a path constraint with `positionMode: fixed` AND `spacingMode: fixed` (case-insensitive grep both succeed)
    - JSON contains both `"position"` and `"spacing"` timeline channels AND at least one `"curve"` array
    - JSON declares stable spine `"4.3.0"` and NO beta token: `grep -ci "beta" fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json` returns 0
    - `fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.atlas` first line is `PATH_FIXED.png`; no `.png` byte-file exists in the dir
  </acceptance_criteria>
  <done>
    A tiny self-consistent synthetic 4.3 path-Fixed fixture exists with positionMode+spacingMode fixed and a path timeline animating both position+spacing (>=2 keys, >=1 curve), plus a 1-region .atlas. No PNG. The oracle (48-04) is its correctness proof.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove every new fixture is git-tracked and reachable on a fresh clone (the authoritative would-CI-see-it check)</name>
  <files>fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.json, fixtures/SCALE_BAKE_4_2/TEST_01.json, fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json</files>
  <read_first>
    - .planning/phases/48-core-scale-bake-module-regression-oracle/48-RESEARCH.md (section "Fixture-Commit Safety" -> "Verification commands (the authoritative would-CI-see-it)")
    - .planning/phases/48-core-scale-bake-module-regression-oracle/48-CONTEXT.md (D-06a step 2 - "prove tracked, not just on-disk")
  </read_first>
  <action>
This task proves the fixtures are TRACKED, not merely on disk — the defense against the v1.3.1 landmine where files passed locally but never committed.

First STAGE the eight new fixture files (six copied + two synthetic). Narrate the git steps in plain English for the user (memory: user is a git beginner — explain that `git add` stages files and `git commit` records them).

```bash
git add fixtures/SCALE_BAKE_4_3/ fixtures/SCALE_BAKE_4_2/ fixtures/SCALE_BAKE_PATH_43/
git status
```
Before committing, re-confirm zero PNGs are staged: `git diff --cached --name-only | grep -i '\.png$'` must return NOTHING.

Then commit so the proof commands have a committed tree to inspect (append the repo's Co-Authored-By trailer):
`git commit -m "test(48): commit SCALE_BAKE fixture matrix (json+atlas only, no PNGs)"`

Then run the THREE authoritative proofs for EVERY fixture .json AND .atlas (RESEARCH verification commands). All must pass:
```bash
# 1. NOT ignored (must print nothing):
git check-ignore fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.json fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.atlas fixtures/SCALE_BAKE_4_2/TEST_01.json fixtures/SCALE_BAKE_4_2/TEST_01.atlas fixtures/SCALE_BAKE_4_2/TEST_03.json fixtures/SCALE_BAKE_4_2/TEST_03.atlas fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.atlas

# 2. Tracked (each must succeed):
git ls-files --error-unmatch fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.json fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.atlas fixtures/SCALE_BAKE_4_2/TEST_01.json fixtures/SCALE_BAKE_4_2/TEST_01.atlas fixtures/SCALE_BAKE_4_2/TEST_03.json fixtures/SCALE_BAKE_4_2/TEST_03.atlas fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.atlas

# 3. In the committed tree (each .json AND .atlas listed; expect 8):
git archive HEAD | tar -t | grep SCALE_BAKE
```
`git archive` MUST list all 8 files. If any fixture fails any check, it is NOT safely committed — fix (likely landed under an ignored path; re-confirm it is under `fixtures/SCALE_BAKE_*` and re-add) before proceeding.

Record in the SUMMARY: local green != CI green; the binding signal is the watched per-OS run of BOTH `ci.yml` AND `release.yml` (they diverge — memory `feedback_release_yml_diverges_from_ci_yml`). Do NOT push tags or trigger release (memory: tags local-only).
  </action>
  <verify>
    <automated>test -z "$(git check-ignore fixtures/SCALE_BAKE_4_3/SKINS_SPINE_V02.json fixtures/SCALE_BAKE_4_2/TEST_01.json fixtures/SCALE_BAKE_PATH_43/PATH_FIXED.json)" && test "$(git archive HEAD | tar -t | grep -c SCALE_BAKE)" -ge 8 && echo OK_FIXTURES_TRACKED</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch <each of the 8 fixture files>` SUCCEEDS (every one tracked)
    - `git check-ignore <each fixture>` prints NOTHING (none ignored)
    - `git archive HEAD | tar -t | grep -c SCALE_BAKE` returns >= 8 (every .json AND .atlas in the committed tree)
    - `git diff --cached --name-only | grep -i '\.png$'` returns NOTHING during staging (no PNG ever staged)
    - The SUMMARY records the authoritative-signal caveat (binding signal = watched per-OS ci.yml AND release.yml)
  </acceptance_criteria>
  <done>
    All 8 new fixture files are committed and proven not-ignored + tracked + present in the committed tree via the three authoritative git commands. Zero PNGs staged. The v1.3.1 silent-no-commit landmine is defused.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author disk -> git committed tree | Fixtures must cross from on-disk to the committed tree; a silent no-op (ignored path) leaves CI without them. |
| committed .atlas/.json -> public repo | These files become public. Only PNGs carry confidential art (D-04); .json (geometry) + .atlas (text, no pixels) are non-sensitive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Information Disclosure | committing confidential pixel art | mitigate | D-04 PNG-exclusion: COPY only .json+.atlas into new dirs; Task 1+3 grep-assert zero `.png` staged. .atlas is verified pixel-data-free text. LOW severity (no PII; geometry only). |
| T-48-05 | Repudiation | fixture silently uncommitted -> CI false-green | mitigate | D-06a steps 2-3: git check-ignore / ls-files --error-unmatch / git archive prove tracked; the 48-04 oracle hard-fails (no skipIf) if a fixture is absent. Defuses the v1.3.1 landmine. |
| T-48-06 | Denial of Service | committing 174MB of PNGs (repo bloat) | mitigate | COPY-into-new-dir (no PNGs present to stage) instead of `git add fixtures/DEMON/`; Task 3 re-confirms no .png staged before commit. |

No network, auth, session, or crypto surface. Only V5 Input Validation (the fixtures feed the bake's validation guards) is tangentially relevant. LOW severity overall — does not block.
</threat_model>

<verification>
- All 8 fixture files exist json+atlas-only with zero PNGs
- The synthetic path-Fixed fixture has positionMode+spacingMode fixed + a path timeline animating position+spacing with a curve key
- All 8 files prove tracked via `git ls-files --error-unmatch` + `git check-ignore` (empty) + `git archive HEAD | tar -t | grep SCALE_BAKE`
- This plan touches NO `src/` and NO test code — it is fixtures-only (the oracle that consumes them lands in 48-04)
</verification>

<success_criteria>
- `fixtures/SCALE_BAKE_4_3/` (DEMON), `fixtures/SCALE_BAKE_4_2/` (TEST_01 + TEST_03), and `fixtures/SCALE_BAKE_PATH_43/` (synthetic) exist json+atlas-only.
- Every fixture is proven committed + reachable on a fresh clone (the authoritative "would CI see it?" checks pass).
- Zero PNG bytes committed (D-04 IP protection intact).
- BAKE-04's fixture-matrix prerequisite is satisfied (the oracle in 48-04 wires them in).
</success_criteria>

<output>
After completion, create `.planning/phases/48-core-scale-bake-module-regression-oracle/48-02-SUMMARY.md`
</output>
