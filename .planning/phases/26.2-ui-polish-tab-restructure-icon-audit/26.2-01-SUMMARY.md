---
phase: "26.2"
plan: "01"
subsystem: "renderer"
tags: [tab-restructure, ui-polish, unused-assets, AppShell, query-lifting]
dependency_graph:
  requires: []
  provides: [3-tab-system, unused-tab-gated, query-lifted-to-appshell, stroke-icon-standard]
  affects: [AppShell.tsx, UnusedAssetsPanel.tsx, unused-assets-panel.spec.tsx]
tech_stack:
  added: []
  patterns: [tab-gated-render, lifted-query-prop, stroke-icon-standard, conditional-tab-badge]
key_files:
  created: []
  modified:
    - src/renderer/src/components/AppShell.tsx
    - src/renderer/src/panels/UnusedAssetsPanel.tsx
    - tests/renderer/unused-assets-panel.spec.tsx
decisions:
  - "D-01: UnusedAssetsPanel moved from always-rendered to tab-gated (activeTab === 'unused')"
  - "D-02: Auto-redirect useEffect watches orphanedFiles.length primitive to avoid infinite re-render (T-26.2-03)"
  - "D-03: query/onQueryChange lifted to AppShell props; UnusedAssetsPanel no longer owns search state"
  - "D-04/D-06: TabButton gains optional icon and badge props; inline-flex layout"
  - "D-05: TabButton active branch changed from font-semibold to font-medium per UI-SPEC 2-weight limit"
  - "D-06a: GridIcon, WarningTriangleTabIcon, ChartBarsIcon added as stroke-style tab strip icons"
  - "D-07a: UnusedAssetsPanel header SVG icon converted from fill to stroke (viewBox 0 0 20 20)"
metrics:
  duration: "~20 minutes"
  completed: "2026-05-04"
  tasks_completed: 2
  files_changed: 3
---

# Phase 26.2 Plan 01: 3-Tab Restructure + UnusedAssetsPanel Query Lifting Summary

**One-liner:** 3-tab AppShell (Global / Unused / Animation) with conditional danger-badge Unused tab, lifted query props on UnusedAssetsPanel, and stroke icon standard applied to both tab strip and panel header.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 (RED) | Update tests for lifted query props | ed40ef5 | tests/renderer/unused-assets-panel.spec.tsx |
| 1 (GREEN) | Refactor UnusedAssetsPanel — lift query props, stroke icon | 9b80793 | src/renderer/src/panels/UnusedAssetsPanel.tsx |
| 2 | Restructure AppShell — 3-tab type + TabButton + Unused tab | 0f65fd9 | src/renderer/src/components/AppShell.tsx |

## What Was Built

### UnusedAssetsPanel (Task 1)

- Added `query: string` and `onQueryChange: (q: string) => void` required props (D-03 — matches GlobalMaxRenderPanel shape)
- Removed `const [query, setQuery] = useState('')` (lifted to AppShell)
- Removed `SearchBar` import and the `div.basis-full` inline SearchBar JSX block
- Converted header SVG from fill-style (16x16 viewBox) to stroke-style (20x20 viewBox, `fill="none"`, `stroke="currentColor"`, `strokeWidth="1.5"`)
- Retained the `if (orphanedFiles.length === 0) return null` defensive guard (D-06)
- `filteredOrphans` useMemo continues reading `query` from prop — no logic change

### AppShell (Task 2)

- `type ActiveTab` expanded: `'global' | 'unused' | 'animation'` (3 values)
- D-02 auto-redirect `useEffect` added: if `activeTab === 'unused'` and `orphanedFiles.length === 0`, redirects to `'global'`. Watches the primitive `.length` (not the array reference) per T-26.2-03 threat mitigation
- `TabButton` function updated: added optional `icon?: ReactNode` and `badge?: number` props; changed className to `inline-flex items-center gap-1`; active branch changed from `font-semibold` to `font-medium` per UI-SPEC 2-weight limit
- Three tab strip icon helpers added as zero-prop functions: `GridIcon`, `WarningTriangleTabIcon`, `ChartBarsIcon` (all stroke-style, `viewBox="0 0 20 20"`, `w-4 h-4`)
- Tab strip nav updated: all 3 tabs have icons; Unused tab is conditionally rendered only when `orphanedFiles.length > 0`; Unused tab shows danger pill badge with count
- `UnusedAssetsPanel` moved from always-rendered to `{activeTab === 'unused' && (...)}` gate with `query={query} onQueryChange={setQuery}` props threaded through

### Tests (Task 1)

- All 9 `render(...)` calls in `unused-assets-panel.spec.tsx` updated to pass `query=""` and `onQueryChange={() => {}}` as required props
- Test (i) rewritten to drive filtering via prop-driven `rerender` instead of `fireEvent` on internal textbox (which no longer exists)

## Deviations from Plan

None - plan executed exactly as written. All 6 changes in Task 2 applied verbatim.

## Verification Results

All plan verification checks pass:

1. `type ActiveTab = 'global' | 'unused' | 'animation'` — line 78
2. `badge={(effectiveSummary.orphanedFiles ?? []).length}` — line 1351
3. `activeTab === 'unused'` — 3 hits (useEffect L255 + tab button isActive L1348 + JSX gate L1581)
4. `font-semibold` absent from TabButton active branch — 0 hits (uses `font-medium`)
5. `bg-danger/20 text-danger rounded-full text-xs font-medium px-1.5 py-0.5 ml-1.5` — verbatim literal, line 1796
6. `query: string` in `UnusedAssetsPanelProps` — line 31
7. No `useState('')` for query in `UnusedAssetsPanel.tsx` — 0 hits
8. No `SearchBar` import in `UnusedAssetsPanel.tsx` — 0 hits
9. `viewBox="0 0 20 20"` in `UnusedAssetsPanel.tsx` — line 68

**npm test result:** 754 passed, 1 skipped, 2 todo. The 2 failures are pre-existing (package.json version mismatch + atlas-preview-modal dblclick test) — confirmed present on main branch before these changes.

## Known Stubs

None.

## Threat Flags

None. No new IPC channels, network endpoints, auth paths, or schema changes introduced. All changes are pure renderer JSX restructuring. T-26.2-03 threat mitigated: useEffect dependency array uses `(effectiveSummary.orphanedFiles ?? []).length` (primitive number), not the array reference — eliminates infinite re-render risk.

## Self-Check: PASSED

- `src/renderer/src/components/AppShell.tsx` — exists, modified
- `src/renderer/src/panels/UnusedAssetsPanel.tsx` — exists, modified
- `tests/renderer/unused-assets-panel.spec.tsx` — exists, modified
- Commits: ed40ef5, 9b80793, 0f65fd9 — all present in git log
