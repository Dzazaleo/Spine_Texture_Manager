---
phase: 26.2-ui-polish-tab-restructure-icon-audit
verified: 2026-05-04T00:00:00Z
status: human_needed
score: 18/18 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Plan 01 Task 3 — sketch-001 variant A visual match + AP-01 compliance"
    expected: "Two rows visible at the top of the panel zone: Row 1 (main toolbar — unchanged) and Row 2 (NEW sub-toolbar) with [⊞ Global] [▦ Animation Breakdown] full-width row, separated from row 1 by a thin border-border line. Active tab shows 2px orange underline; tab icons render at w-4 h-4 with gap-1 to label; icon color matches active text color. Sub-toolbar nav must be a SIBLING of <header> in DevTools, never a descendant."
    why_human: "Visual layout match against sketch-001 variant A reference rendering; jsdom cannot compute layout, hover/active color transitions, or DOM-rendered visual fidelity at runtime. Code-level AP-01 grep gate already PASSES (0 nav nodes inside <header>) — this UAT is the visual-aesthetic gate."
  - test: "Plan 02 Task 6 — cross-site stroke-triangle visual consistency + D-08/D-09 surface check"
    expected: "All 4 stroke triangles (MissingAttachmentsPanel header, UnusedAssetsPanel header, GlobalMaxRenderPanel missing-row cell, AnimationBreakdownPanel missing-row cell) render visually IDENTICAL — outline triangle with 1.5 stroke width, no filled shape, inner exclamation mark visible, no row-height shift, no layout shift. UnusedAssetsPanel headline reads 'N unused file(s)' (not orphaned), aria-label is 'Unused image files'. No filter input above the table when expanded; table immediately lists all unused files. Plan 01's tab icons (filmstrip/bar-chart) still render correctly."
    why_human: "Visual rendering parity at 16x16px in dark mode — element-local fill on the exclamation dot (D-06a) needs runtime confirmation that it renders cleanly. jsdom cannot evaluate visual identity across the 4 sites. Requires `npm run dev` against fixture project + a stray PNG to surface UnusedAssetsPanel."
---

# Phase 26.2: UI polish — tab restructure + icon audit Verification Report

**Phase Goal:** UI polish — move existing 2-tab strip from main toolbar `<header>` into a dedicated sub-toolbar `<nav role="tablist">` row (sketch-001 variant A; resolves AP-01); add filmstrip + bar-chart icons to 2 tabs (verbatim panel-header SVG reuse, w-4 h-4); rename "orphaned" → "unused" on user-visible strings in UnusedAssetsPanel; remove inline SearchBar from UnusedAssetsPanel; convert 4 fill warning-triangle icons to stroke style via shared WarningTriangleIcon component (D-06 single source of truth).

**Verified:** 2026-05-04
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                | Status     | Evidence                                                                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | AP-01 — Tabs render in a dedicated `<nav role="tablist">` SIBLING of main toolbar `<header>`, never inside it                                                        | VERIFIED   | `awk '/<header /,/<\/header>/' AppShell.tsx \| grep -c '<nav role="tablist"'` = **0**; `</header>` at L1413, `<nav role="tablist">` at L1421 (sibling) |
| 2   | D-01 — Existing 2-tab strip moved out of main toolbar `<header>` into a new sibling `<nav role="tablist">` separated by `border-t border-border`                     | VERIFIED   | AppShell.tsx:1421-1466 — nav after `</header>` with className `flex gap-1 items-center bg-panel border-t border-border px-6`                       |
| 3   | D-02 — ActiveTab type stays `'global' \| 'animation'` (no `'unused'` literal added). Default `'global'` unchanged                                                    | VERIFIED   | AppShell.tsx:78 — `type ActiveTab = 'global' \| 'animation';`; `grep -c "'unused'"` = 0                                                            |
| 4   | D-03 — Global tab renders verbatim filmstrip `<rect>+<path>` from GlobalMaxRenderPanel.tsx:836-839; Animation Breakdown tab renders verbatim bar-chart from AnimationBreakdownPanel.tsx:415 | VERIFIED   | AppShell.tsx:1439 `rect x="2" y="6" width="16" height="8" rx="1"`, L1440 filmstrip path, L1460 bar-chart path                                      |
| 5   | D-04 — Tab icons follow canonical stroke attributes (stroke=currentColor, strokeWidth=1.5, strokeLinecap/Join=round, fill=none, viewBox=0 0 20 20, aria-hidden)      | VERIFIED   | AppShell.tsx:1429-1437 + 1450-1458 — both `<svg>` blocks carry full canonical attribute set; both at `w-4 h-4`                                     |
| 6   | TabButton accepts optional `icon?: ReactNode` prop, rendered left of children with `gap-1`                                                                            | VERIFIED   | AppShell.tsx:1789 (`icon?: ReactNode`); L1798 (`'relative inline-flex items-center gap-1 ...'`); L1802 (`{icon}` then `{children}`)                |
| 7   | Active 2px orange underline indicator preserved verbatim                                                                                                              | VERIFIED   | AppShell.tsx:1804-1808 — `<span className="absolute left-0 right-0 -bottom-px h-[2px] bg-accent" />` inside `{isActive && (...)}`                   |
| 8   | Tailwind v4 literal-class discipline maintained — no template-string interpolation                                                                                    | VERIFIED   | All className values are literals or clsx conditional branches with literal values (REVIEW.md confirms)                                            |
| 9   | D-05 — All 4 fill warning-triangle icons (MissingAttachmentsPanel:65, UnusedAssetsPanel:59, GlobalMaxRenderPanel:461, AnimationBreakdownPanel:703) converted to stroke; all retain `w-4 h-4`         | VERIFIED   | All 4 panels: `import { WarningTriangleIcon }` + `<WarningTriangleIcon className="w-4 h-4" />`; old fill path `M8 1.5 L14.5 13.5 H1.5 Z` purged from all 4 |
| 10  | D-06 — All 4 conversion sites render the SAME canonical stroke-triangle path (no per-site divergence)                                                                 | VERIFIED   | `grep -rl 'M10 3 L17 16 H3 Z' src/renderer/src` returns **1** file (WarningTriangleIcon.tsx) — single-source-of-truth grep gate PASS                |
| 11  | D-07 — Canonical SVG extracted to `src/renderer/src/components/icons/WarningTriangleIcon.tsx`; accepts `className?: string` prop                                       | VERIFIED   | File exists; exports `WarningTriangleIcon` + `WarningTriangleIconProps { className?: string }`                                                     |
| 12  | D-06a — Exclamation dot rendered as `<circle cx="10" cy="14.5" r="0.5" fill="currentColor" />`                                                                        | VERIFIED   | WarningTriangleIcon.tsx:39 — exact element-local-fill circle present                                                                               |
| 13  | D-08 — UnusedAssetsPanel user-visible strings renamed: line 71 → `{count} unused file{plural}`; line 52 aria-label → `Unused image files`. Internal types/props NOT renamed | VERIFIED   | UnusedAssetsPanel.tsx:44 `aria-label="Unused image files"`; L59 `{count} unused file{plural}`; L25 prop `orphanedFiles: OrphanedFile[]` preserved   |
| 14  | D-09 — Inline SearchBar removed: deleted JSX block, query useState, filteredOrphans useMemo, (no matches) cell. Table renders orphanedFiles directly. SearchBar import removed | VERIFIED   | `grep -c '<SearchBar' UnusedAssetsPanel.tsx` = 0; `grep -c filteredOrphans` = 0; `grep -c useMemo` = 0; `grep -c 'orphanedFiles.map'` = 1; SearchBar import absent |
| 15  | User can see existing unused files alert bar with stroke-style warning triangle and headline 'N unused files' (not orphaned)                                          | VERIFIED   | UnusedAssetsPanel.tsx:51 (WarningTriangleIcon) + L59 ('unused file{plural}'); old 'orphaned file{plural}' absent                                    |
| 16  | User can no longer filter the unused files list inline (search box removed)                                                                                           | VERIFIED   | `<SearchBar>` JSX absent; `query` useState absent; tests/renderer/unused-assets-panel.spec.tsx:124 enforces "no inline filter input"                |
| 17  | Plan 01's tab-icon source paths preserved in panels (filmstrip in GlobalMaxRenderPanel, bar-chart in AnimationBreakdownPanel) — must NOT be touched by Plan 02       | VERIFIED   | GlobalMaxRenderPanel.tsx still contains `rect x="2" y="6" width="16" height="8" rx="1"` + `M5 6 v3 ...`; AnimationBreakdownPanel still contains `M3 17 V10 ...` |
| 18  | TypeScript typecheck passes after all edits                                                                                                                            | VERIFIED   | `npx tsc --noEmit` exit 0 (no output, no errors)                                                                                                  |

**Score:** 18/18 truths verified

### Required Artifacts

| Artifact                                                  | Expected                                                                                          | Status     | Details                                                                                                                                                                           |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/renderer/src/components/AppShell.tsx`                | Tab strip extracted from `<header>` into sibling `<nav role="tablist">`; TabButton optional icon prop; verbatim panel SVGs at w-4 h-4 | VERIFIED   | Modified (1813 lines); `<nav role="tablist">` at L1421 (sibling of `</header>` at L1413); TabButton at L1780 with `icon?: ReactNode`; verbatim filmstrip+bar-chart paths           |
| `src/renderer/src/components/icons/WarningTriangleIcon.tsx` | Canonical stroke-style warning triangle (D-06 single source of truth); exports `WarningTriangleIcon`; canonical path `M10 3 L17 16 H3 Z` | VERIFIED   | New file (43 lines); exports component + props interface; D-04 canonical attributes; D-06a element-local fill on dot circle                                                       |
| `src/renderer/src/panels/UnusedAssetsPanel.tsx`           | D-08 rename, D-09 SearchBar removal, D-05 stroke icon via component                                | VERIFIED   | Modified; aria-label `Unused image files`, headline `unused file{plural}`, no SearchBar/query/useMemo/(no matches), `<WarningTriangleIcon className="w-4 h-4" />`; internal `orphanedFiles` prop preserved |
| `src/renderer/src/panels/MissingAttachmentsPanel.tsx`     | D-05 fill→stroke icon swap via WarningTriangleIcon                                                 | VERIFIED   | L34 import; L66 `<WarningTriangleIcon className="w-4 h-4" />`; old fill path purged                                                                                                |
| `src/renderer/src/panels/GlobalMaxRenderPanel.tsx`        | D-05 fill→stroke icon swap (line 461) via WarningTriangleIcon; filmstrip section-header preserved   | VERIFIED   | L68 import; L462 component usage; old fill path purged; filmstrip rect+path at L836-839 still present (Plan 01 source preserved); aria-label `Missing PNG` preserved on parent span |
| `src/renderer/src/panels/AnimationBreakdownPanel.tsx`     | D-05 fill→stroke icon swap (line 703) via WarningTriangleIcon; bar-chart section-header preserved   | VERIFIED   | L86 import; L704 component usage; old fill path purged; bar-chart path at L415 still present (Plan 01 source preserved); aria-label `Missing PNG` preserved                       |

### Key Link Verification

| From                                                  | To                                                              | Via                                                          | Status | Details                                                                                                                                  |
| ----------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------ | ------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| AppShell main render                                  | `<nav role="tablist">` sub-toolbar                              | sibling DOM placement immediately below `<header>`, separated by `border-t border-border` | WIRED  | `</header>` at L1413, comment block L1414-1420, `<nav>` opens at L1421 with `border-t border-border` class — sibling at same nesting level |
| `<TabButton>` Global                                  | filmstrip SVG body verbatim from GlobalMaxRenderPanel.tsx:836-839 | `icon` prop                                                  | WIRED  | AppShell.tsx:1428-1442 — `<rect x="2" y="6" width="16" height="8" rx="1" />` + `<path d="M5 6 v3 ...">` exactly verbatim                |
| `<TabButton>` Animation Breakdown                     | bar-chart SVG body verbatim from AnimationBreakdownPanel.tsx:415 | `icon` prop                                                  | WIRED  | AppShell.tsx:1449-1462 — `<path d="M3 17 V10 M8 17 V6 M13 17 V12 M18 17 V4" />` exactly verbatim                                       |
| WarningTriangleIcon.tsx                               | all 4 warning-triangle render sites                              | named import, used as JSX with optional className prop       | WIRED  | All 4 panels import `{ WarningTriangleIcon }` from `'../components/icons/WarningTriangleIcon'` and render `<WarningTriangleIcon className="w-4 h-4" />` |
| UnusedAssetsPanel headline copy                       | user-visible 'unused' wording                                    | string literal at line 59 (post-rename)                      | WIRED  | UnusedAssetsPanel.tsx:59 `{count} unused file{plural}` (old `orphaned` purged)                                                          |
| UnusedAssetsPanel table                               | orphanedFiles prop directly                                      | removal of filter useMemo                                    | WIRED  | UnusedAssetsPanel.tsx:86 `orphanedFiles.map((f) => ...)`; no filter, no useMemo, no SearchBar                                            |

### Data-Flow Trace (Level 4)

| Artifact                                              | Data Variable | Source                                                                                            | Produces Real Data | Status   |
| ----------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------- | ------------------ | -------- |
| AppShell tab strip                                    | activeTab     | useState `'global' \| 'animation'` (default `'global'`); setActiveTab toggled by tab onClick handlers | Yes                | FLOWING  |
| UnusedAssetsPanel                                     | orphanedFiles | prop from AppShell, populated from existing IPC `loadProject` → `SkeletonSummary.orphanedFiles` (Phase 24 contract — unchanged) | Yes                | FLOWING  |
| WarningTriangleIcon                                    | (static SVG, no data flow) | n/a — pure presentation component with literal path data; only `className` prop forwarded     | n/a                | n/a      |

All dynamic data flows are intact (no upstream changes — Plan 02 only swapped icon markup; Plan 01 only restructured DOM nesting). State→render wiring for `activeTab` continues to drive panel switching as before.

### Behavioral Spot-Checks

| Behavior                                              | Command                                                                                                                                | Result                       | Status |
| ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ------ |
| TypeScript compile                                    | `npx tsc --noEmit`                                                                                                                     | Exit 0, no output            | PASS   |
| AP-01 grep gate                                       | `awk '/<header /,/<\/header>/' src/renderer/src/components/AppShell.tsx \| grep -c '<nav role="tablist"'`                              | 0                            | PASS   |
| Single tablist nav in AppShell                        | `grep -c '<nav role="tablist"' src/renderer/src/components/AppShell.tsx`                                                               | 1                            | PASS   |
| Single canonical triangle path (D-06 SOT)             | `grep -rl 'M10 3 L17 16 H3 Z' src/renderer/src \| wc -l`                                                                               | 1 (WarningTriangleIcon.tsx)  | PASS   |
| Old fill triangle purged from 4 panels                | `grep -l 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/*.tsx`                                                                     | (no matches)                 | PASS   |
| Plan 01 sources preserved in panels                   | `grep -c 'rect x="2" y="6"' GlobalMaxRenderPanel.tsx; grep -c 'M3 17 V10' AnimationBreakdownPanel.tsx`                                  | 1; 1                         | PASS   |
| Visual / hover / underline UAT                        | (requires `npm run dev` + sketch-001 variant A reference comparison)                                                                   | -                             | SKIP — routed to human |
| Cross-site triangle visual identity at 16x16px         | (requires runtime DOM rendering)                                                                                                       | -                             | SKIP — routed to human |

Test suite reported by orchestrator: **754 pass, 2 pre-existing unrelated failures** (build-scripts version assertion + atlas-preview-modal dblclick) verified to fail at base commit `400d1e6`, NOT introduced by Phase 26.2. Treated as out of scope.

### Requirements Coverage

| Requirement | Source Plan      | Description                                                                                                                                                         | Status     | Evidence                                                                                                                                                                                                |
| ----------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UI-08       | 26.2-01, 26.2-02 | Icon audit: existing icons updated to a consistent style; icons added where the UI currently shows text-only labels that would benefit from a visual affordance (e.g. panel section headers, toolbar actions, modal tabs). | SATISFIED  | (a) Icon audit — 4 fill warning triangles converted to canonical D-04 stroke style via shared `WarningTriangleIcon`; (b) Icons added — Global tab gets filmstrip, Animation Breakdown gets bar-chart, both verbatim panel-header reuse at `w-4 h-4`. Both halves of UI-08 (style consistency + add-icons-to-text-only-labels) satisfied. |

No orphaned requirements — Phase 26.2 declares only UI-08 in plan frontmatter and REQUIREMENTS.md traceability maps UI-08 to "Phase 26".

### Anti-Patterns Found

Anti-pattern scan against the modified files (AppShell.tsx, WarningTriangleIcon.tsx, 4 panel files, 1 test file):

| File                                                   | Line | Pattern                                | Severity | Impact                                                                                                                                  |
| ------------------------------------------------------ | ---- | -------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| (none)                                                 | -    | -                                      | -        | No TODO/FIXME/PLACEHOLDER comments introduced. No empty handlers. No raw hex colors. No template-string className interpolation. No emojis. |

Pre-existing review-flagged warnings (REVIEW.md WR-01, WR-02) — informational, NOT blockers for goal achievement:

| ID    | Severity | Concern                                                                                                                                                                          |
| ----- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WR-01 | warning  | Sub-toolbar `<nav>` is not sticky; tab strip scrolls out of view when content scrolls (parent `<header>` has sticky-top, the new sibling nav does not inherit it). UX consideration but does not violate D-01..D-09. |
| WR-02 | warning  | Pre-existing aria pattern not improved by this diff (advisory).                                                                                                                  |

These warnings do not invalidate the goal or any locked decision (D-01..D-09); they are advisory items for future polish. No critical findings; 0 blockers.

### Human Verification Required

Two deferred human-verify checkpoints from the worktree-mode execution were intentionally deferred to the orchestrator post-merge (executor cannot serve a `npm run dev` interactive session against an isolated worktree branch):

#### 1. Plan 01 Task 3 — sketch-001 variant A visual match + AP-01 compliance

**Test:** Run `npm run dev` and load `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`. Visually compare the tab strip layout against `.planning/sketches/001-tab-bar-placement/index.html` variant A.

**Expected:**
- Two rows visible at the top of the panel zone.
  - Row 1 (main toolbar — unchanged): `[file badge] [counts] [SearchBar] [☑ Source] [Atlas Preview] [Documentation] [Optimize Assets] [Save] [Open]`.
  - Row 2 (NEW sub-toolbar): `[⊞ Global] [▦ Animation Breakdown]` — full-width row with `bg-panel`, separated from row 1 by a thin `border-border` line.
- Active tab shows 2px orange underline; tab icons render at `w-4 h-4` with `gap-1` separation from label; icon color matches active text color (`text-accent` on active, `text-fg-muted` on inactive).
- DevTools DOM inspection: the `<nav role="tablist">` element is a SIBLING of the main toolbar `<header>` element (NOT a descendant).
- Resize narrow (~600px) → sub-toolbar stays full-width, tabs do not wrap or overlap.
- Alert-bar zone below the sub-toolbar is unchanged: MissingAttachmentsPanel and UnusedAssetsPanel render in their existing locations.

**Why human:** Visual layout match against sketch-001 variant A reference rendering; jsdom cannot compute layout, hover/active color transitions, or DOM-rendered visual fidelity at runtime. The code-level AP-01 grep gate already PASSES (0 nav nodes inside `<header>`); this UAT is the visual-aesthetic gate.

#### 2. Plan 02 Task 6 — D-05/D-06/D-08/D-09 + cross-site icon consistency

**Test:** Run `npm run dev`, load fixture. Surface the 4 stroke triangle sites (drop a stray .png into images folder if needed to surface UnusedAssetsPanel).

**Expected:**
- Site 1 — MissingAttachmentsPanel header: outline triangle (1.5 stroke-width), no filled shape, inner exclamation visible.
- Site 2 — UnusedAssetsPanel header: identical-shape outline triangle.
- Site 3 — GlobalMaxRenderPanel missing-row table cell: same outline triangle, sized identically (no row-height shift).
- Site 4 — AnimationBreakdownPanel missing-row table cell: same outline triangle.
- Cross-site invariant: 4 triangles visually IDENTICAL in shape, weight, and proportions (single source of truth — same component).
- D-08 verification: UnusedAssetsPanel headline reads "N unused file(s)" (NOT "orphaned"); aria-label is "Unused image files".
- D-09 verification: clicking "Show details" reveals NO filter input box above the table; table immediately lists all unused files.
- Plan 01's tab icons (filmstrip on Global, bar-chart on Animation Breakdown) still render correctly — Plan 02 must not have touched them.

**Why human:** Visual rendering parity at 16x16px in dark mode — element-local fill on the exclamation dot (D-06a) needs runtime confirmation that it renders cleanly. jsdom cannot evaluate visual identity across the 4 sites.

### Gaps Summary

No automated gaps. All 18 must-haves verified at the code level:

- **AP-01 critical structural invariant — PASS.** `awk '/<header /,/<\/header>/' AppShell.tsx | grep -c '<nav role="tablist"'` returns **0**. The new `<nav>` is structurally a sibling of `<header>` (line 1421 vs `</header>` at line 1413).
- **All D-01..D-09 locked decisions — VERIFIED** at the file/grep level.
- **Single-source-of-truth invariant for warning triangle — VERIFIED.** `grep -rl 'M10 3 L17 16 H3 Z' src/renderer/src` returns exactly one file (the new `WarningTriangleIcon.tsx`).
- **Plan 01 tab-icon source paths preserved in panels** (filmstrip rect+path in GlobalMaxRenderPanel, bar-chart path in AnimationBreakdownPanel) — Plan 02 did not touch them.
- **Internal identifier scope guard — RESPECTED.** `OrphanedFile`, `orphanedFiles` prop preserved per D-08 scope.
- **Tailwind v4 literal-class discipline + token-only colors — preserved** (REVIEW.md confirms).
- **TypeScript compile — clean.** Test suite reports 754 pass + 2 pre-existing unrelated failures (verified at base commit `400d1e6`, NOT introduced by Phase 26.2).

The phase is **functionally complete at the code level**. Status is `human_needed` exclusively because the two deferred visual-UAT checkpoints (Plan 01 Task 3 and Plan 02 Task 6) require interactive `npm run dev` verification against sketch-001 variant A and cross-site rendering parity at 16x16px — both intentionally deferred from worktree-mode execution and listed for the orchestrator to surface to the user.

REVIEW.md WR-01 (sub-toolbar not sticky) is an advisory UX consideration that does not violate the locked decisions D-01..D-09; it is not a goal-achievement gap.

---

_Verified: 2026-05-04_
_Verifier: Claude (gsd-verifier)_
