---
phase: 26.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/panels/MissingAttachmentsPanel.tsx
  - src/renderer/src/panels/GlobalMaxRenderPanel.tsx
  - src/renderer/src/panels/AnimationBreakdownPanel.tsx
autonomous: true
requirements: [UI-08]

must_haves:
  truths:
    - "MissingAttachmentsPanel header icon is stroke style (viewBox='0 0 20 20', fill='none', stroke='currentColor', strokeWidth='1.5')"
    - "GlobalMaxRenderPanel missing-row warning icon is stroke style"
    - "AnimationBreakdownPanel missing-row warning icon is stroke style"
    - "All three converted icons retain w-4 h-4 (no layout shift)"
    - "No fill='currentColor' remains on any of the four original fill triangle SVGs"
    - "npm test passes with 0 failures"
  artifacts:
    - path: "src/renderer/src/panels/MissingAttachmentsPanel.tsx"
      provides: "Stroke warning icon in panel header (line ~65)"
      contains: "stroke=\"currentColor\""
    - path: "src/renderer/src/panels/GlobalMaxRenderPanel.tsx"
      provides: "Stroke warning icon in missing-row cell (line ~461)"
      contains: "stroke=\"currentColor\""
    - path: "src/renderer/src/panels/AnimationBreakdownPanel.tsx"
      provides: "Stroke warning icon in missing-row cell (line ~703)"
      contains: "stroke=\"currentColor\""
  key_links:
    - from: "MissingAttachmentsPanel.tsx header span"
      to: "stroke SVG"
      via: "direct child swap — surrounding span unchanged"
      pattern: "viewBox=\"0 0 20 20\""
    - from: "GlobalMaxRenderPanel.tsx row isMissing span"
      to: "stroke SVG"
      via: "direct child swap — surrounding aria-label span unchanged"
      pattern: "viewBox=\"0 0 20 20\""
    - from: "AnimationBreakdownPanel.tsx row isMissing span"
      to: "stroke SVG"
      via: "direct child swap — surrounding aria-label span unchanged"
      pattern: "viewBox=\"0 0 20 20\""
---

<objective>
Convert the four fill/solid warning triangle SVG icons in three panel files from
the old fill style (viewBox="0 0 16 16" fill="currentColor") to the canonical
stroke/outline style (viewBox="0 0 20 20" fill="none" stroke="currentColor"
strokeWidth="1.5"). This is the icon audit portion of Phase 26.2 (D-07).

Note: UnusedAssetsPanel.tsx icon conversion is handled in Plan 26.2-01 (same
file as the tab restructure refactor). This plan covers the remaining 3 files
with 3 icon conversion sites (MissingAttachmentsPanel: 1 site,
GlobalMaxRenderPanel: 1 site, AnimationBreakdownPanel: 1 site).

Purpose: Establish visual consistency with the stroke icon standard (D-05)
introduced in Phase 26.1 for section-header icons, extending it to panel alert
banners and table row indicators.

Output: 3 panel files with stroke warning triangle icons replacing fill triangles.
No structural changes, no prop changes, no test changes (icons are aria-hidden;
existing tests assert on ARIA and text content, not SVG internals).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.2-ui-polish-tab-restructure-icon-audit/26.2-CONTEXT.md
@.planning/phases/26.2-ui-polish-tab-restructure-icon-audit/26.2-PATTERNS.md
@.planning/phases/26.2-ui-polish-tab-restructure-icon-audit/26.2-UI-SPEC.md

<interfaces>
<!-- Extracted from codebase for executor — no codebase exploration needed. -->

The canonical stroke warning triangle SVG to use at ALL three sites (D-07a: retain w-4 h-4):

```tsx
<svg
  viewBox="0 0 20 20"
  fill="none"
  stroke="currentColor"
  strokeWidth="1.5"
  strokeLinecap="round"
  strokeLinejoin="round"
  className="w-4 h-4"
  aria-hidden="true"
>
  <path d="M10 3.5 L17.5 16.5 H2.5 Z" />
  <line x1="10" y1="8" x2="10" y2="12" />
  <circle cx="10" cy="14.5" r="0.5" fill="currentColor" />
</svg>
```

The fill SVG being replaced at all three sites (byte-for-byte identical at each site):

```tsx
<svg viewBox="0 0 16 16" fill="currentColor" className="w-4 h-4" aria-hidden="true">
  <path d="M8 1.5 L14.5 13.5 H1.5 Z" />
  <rect x="7.25" y="5.5" width="1.5" height="4" fill="white" rx="0.5" />
  <rect x="7.25" y="11" width="1.5" height="1.5" fill="white" rx="0.5" />
</svg>
```

**Site 1 — MissingAttachmentsPanel.tsx:65**

Surrounding span (unchanged):
```tsx
<span
  className="inline-flex items-center justify-center w-4 h-4 text-danger flex-shrink-0"
  aria-hidden="true"
>
  {/* ← SVG goes here */}
</span>
```

**Site 2 — GlobalMaxRenderPanel.tsx:461**

Surrounding span (unchanged):
```tsx
<span
  className="inline-flex items-center justify-center w-4 h-4 text-danger mr-1 flex-shrink-0"
  aria-label="Missing PNG"
>
  {/* ← SVG goes here */}
</span>
```

**Site 3 — AnimationBreakdownPanel.tsx:703**

Surrounding span (unchanged — byte-for-byte identical to GlobalMaxRenderPanel site):
```tsx
<span
  className="inline-flex items-center justify-center w-4 h-4 text-danger mr-1 flex-shrink-0"
  aria-label="Missing PNG"
>
  {/* ← SVG goes here */}
</span>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert MissingAttachmentsPanel fill icon to stroke (line 65)</name>
  <files>src/renderer/src/panels/MissingAttachmentsPanel.tsx</files>
  <read_first>
    - src/renderer/src/panels/MissingAttachmentsPanel.tsx lines 55-80 (panel header with fill icon)
  </read_first>
  <action>
At line 65, replace the fill SVG element (the entire svg tag and its children):

REMOVE:
```tsx
<svg viewBox="0 0 16 16" fill="currentColor" className="w-4 h-4" aria-hidden="true">
  <path d="M8 1.5 L14.5 13.5 H1.5 Z" />
  <rect x="7.25" y="5.5" width="1.5" height="4" fill="white" rx="0.5" />
  <rect x="7.25" y="11" width="1.5" height="1.5" fill="white" rx="0.5" />
</svg>
```

REPLACE WITH:
```tsx
<svg
  viewBox="0 0 20 20"
  fill="none"
  stroke="currentColor"
  strokeWidth="1.5"
  strokeLinecap="round"
  strokeLinejoin="round"
  className="w-4 h-4"
  aria-hidden="true"
>
  <path d="M10 3.5 L17.5 16.5 H2.5 Z" />
  <line x1="10" y1="8" x2="10" y2="12" />
  <circle cx="10" cy="14.5" r="0.5" fill="currentColor" />
</svg>
```

The surrounding `<span className="inline-flex items-center justify-center w-4 h-4 text-danger flex-shrink-0" aria-hidden="true">` is unchanged. Do not touch anything else in the file.
  </action>
  <verify>
    <automated>grep -n 'viewBox="0 0 20 20"' /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager/src/renderer/src/panels/MissingAttachmentsPanel.tsx</automated>
  </verify>
  <done>
    - MissingAttachmentsPanel.tsx contains viewBox="0 0 20 20" at the icon site
    - MissingAttachmentsPanel.tsx does NOT contain viewBox="0 0 16 16"
    - MissingAttachmentsPanel.tsx contains stroke="currentColor" strokeWidth="1.5"
    - Surrounding span className is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert GlobalMaxRenderPanel and AnimationBreakdownPanel missing-row fill icons to stroke</name>
  <files>src/renderer/src/panels/GlobalMaxRenderPanel.tsx, src/renderer/src/panels/AnimationBreakdownPanel.tsx</files>
  <read_first>
    - src/renderer/src/panels/GlobalMaxRenderPanel.tsx lines 454-475 (missing-row warning icon site)
    - src/renderer/src/panels/AnimationBreakdownPanel.tsx lines 696-718 (missing-row warning icon site)
  </read_first>
  <action>
Apply the identical SVG swap at both files. Each file has exactly one fill warning triangle inside the `isMissing` conditional block.

**GlobalMaxRenderPanel.tsx (line 461):**

Inside the `{row.isMissing && (` block, the `<span aria-label="Missing PNG">` contains the fill SVG. Replace:

REMOVE:
```tsx
<svg
  viewBox="0 0 16 16"
  fill="currentColor"
  className="w-4 h-4"
  aria-hidden="true"
>
  <path d="M8 1.5 L14.5 13.5 H1.5 Z" />
  <rect x="7.25" y="5.5" width="1.5" height="4" fill="white" rx="0.5" />
  <rect x="7.25" y="11" width="1.5" height="1.5" fill="white" rx="0.5" />
</svg>
```

REPLACE WITH:
```tsx
<svg
  viewBox="0 0 20 20"
  fill="none"
  stroke="currentColor"
  strokeWidth="1.5"
  strokeLinecap="round"
  strokeLinejoin="round"
  className="w-4 h-4"
  aria-hidden="true"
>
  <path d="M10 3.5 L17.5 16.5 H2.5 Z" />
  <line x1="10" y1="8" x2="10" y2="12" />
  <circle cx="10" cy="14.5" r="0.5" fill="currentColor" />
</svg>
```

The surrounding `<span className="inline-flex items-center justify-center w-4 h-4 text-danger mr-1 flex-shrink-0" aria-label="Missing PNG">` is unchanged.

**AnimationBreakdownPanel.tsx (line 703):**

Apply the exact same SVG swap. The fill icon block at lines 703-712 is byte-for-byte identical to the GlobalMaxRenderPanel site. The surrounding span is also identical. Do not touch anything else in either file.
  </action>
  <verify>
    <automated>cd /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager && grep -c 'viewBox="0 0 16 16"' src/renderer/src/panels/GlobalMaxRenderPanel.tsx src/renderer/src/panels/AnimationBreakdownPanel.tsx src/renderer/src/panels/MissingAttachmentsPanel.tsx</automated>
  </verify>
  <done>
    - GlobalMaxRenderPanel.tsx: viewBox="0 0 20 20" present, viewBox="0 0 16 16" absent from isMissing icon block
    - AnimationBreakdownPanel.tsx: viewBox="0 0 20 20" present, viewBox="0 0 16 16" absent from isMissing icon block
    - Both files contain stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" on the converted icon
    - Both files retain w-4 h-4 on the converted SVG
    - grep -c 'viewBox="0 0 16 16"' across all three files returns 0 for each
    - npm test exits 0 (icon changes are aria-hidden; no test assertions on SVG internals)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer JSX → screen | SVG icon elements rendered directly; no dynamic content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.2-04 | Tampering | SVG icon paths | accept | Icon paths are hardcoded string literals in JSX. No user input or dynamic content enters SVG attribute values. No XSS vector. |
| T-26.2-05 | Information Disclosure | Missing-row indicator | accept | The ⚠ icon signals that a PNG is missing — this is the intended UX. No sensitive information exposed beyond what is already shown in the table row's attachment name. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c 'viewBox="0 0 16 16"' src/renderer/src/panels/MissingAttachmentsPanel.tsx` → 0
2. `grep -c 'viewBox="0 0 16 16"' src/renderer/src/panels/GlobalMaxRenderPanel.tsx` → 0
3. `grep -c 'viewBox="0 0 16 16"' src/renderer/src/panels/AnimationBreakdownPanel.tsx` → 0
4. `grep -c 'viewBox="0 0 20 20"' src/renderer/src/panels/GlobalMaxRenderPanel.tsx` → at least 1
5. `grep -c 'viewBox="0 0 20 20"' src/renderer/src/panels/AnimationBreakdownPanel.tsx` → at least 1
6. `grep -c 'viewBox="0 0 20 20"' src/renderer/src/panels/MissingAttachmentsPanel.tsx` → at least 1
7. `npm test` exits 0
</verification>

<success_criteria>
- All three panel files have stroke warning triangle icons (fill="none" stroke="currentColor" strokeWidth="1.5")
- No fill="currentColor" viewBox="0 0 16 16" pattern remains in any of the three files
- All converted icons retain w-4 h-4 (D-07a — no layout shift)
- Surrounding span elements (with aria-label or aria-hidden) are unchanged
- npm test exits 0 (icon changes do not affect ARIA or text content assertions)
</success_criteria>

<output>
After completion, create `.planning/phases/26.2-ui-polish-tab-restructure-icon-audit/26.2-02-SUMMARY.md`
</output>
