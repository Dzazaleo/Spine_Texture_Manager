---
phase: 26.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/icons/WarningTriangleIcon.tsx
  - src/renderer/src/panels/UnusedAssetsPanel.tsx
  - src/renderer/src/panels/MissingAttachmentsPanel.tsx
  - src/renderer/src/panels/GlobalMaxRenderPanel.tsx
  - src/renderer/src/panels/AnimationBreakdownPanel.tsx
autonomous: false
requirements:
  - UI-08
must_haves:
  truths:
    - "AP-01 acknowledged: this plan does NOT touch tab placement (Plan 01 owns that). It only modifies panel files. Reading .continue-here.md confirms the AP-01 anti-pattern boundary."
    - "D-05 — All 4 fill warning-triangle icons (MissingAttachmentsPanel:65, UnusedAssetsPanel:59, GlobalMaxRenderPanel:461, AnimationBreakdownPanel:703) are converted to the canonical stroke style. All 4 retain w-4 h-4 (no upsize — table-cell + alert-bar layout constraint)."
    - "D-06 — All 4 conversion sites render the SAME canonical stroke-triangle path. No per-site divergence. Single-source-of-truth invariant is structurally enforced by extraction to a shared component."
    - "D-07 — Implementation form: the canonical SVG is extracted to src/renderer/src/components/icons/WarningTriangleIcon.tsx (planner discretion: extract chosen over inline-at-4-sites because the new file structurally enforces single-source-of-truth across current AND future warning-triangle uses). The component accepts an optional className?: string prop."
    - "D-06a — Exclamation dot rendered as <circle cx=10 cy=14.5 r=0.5 fill=currentColor /> (planner discretion: explicit element-local fill renders cleanly on dark mode at 16x16px)."
    - "D-08 — Two user-visible strings in UnusedAssetsPanel.tsx are renamed: line 71 'orphaned file{plural}' becomes 'unused file{plural}'; line 52 aria-label 'Orphaned image files' becomes 'Unused image files'. Internal types (OrphanedFile), prop names (orphanedFiles), and variable names (filteredOrphans) are explicitly OUT of scope and unchanged."
    - "D-09 — Inline SearchBar removed from UnusedAssetsPanel: deleted JSX block (lines 87-93), query useState (line 30), filteredOrphans useMemo (lines 39-43), (no matches) empty-state cell (lines 106-114). Table renders orphanedFiles directly. SearchBar import removed (no other usage in this file)."
    - "D-04 — Canonical stroke icon attributes used: stroke=currentColor strokeWidth=1.5 strokeLinecap=round strokeLinejoin=round fill=none viewBox='0 0 20 20' aria-hidden=true."
    - "Tailwind v4 literal-class discipline maintained — no template-string interpolation in className."
    - "Token-only colors preserved (existing text-danger on wrapping spans is unchanged)."
    - "ROADMAP §Phase 26.2 is STALE (describes dropped 3-tab scope); CONTEXT.md is the source of truth for this plan."
    - "User can see existing unused files alert bar with stroke-style warning triangle and the headline text 'N unused files' (not 'orphaned')."
    - "User can no longer filter the unused files list inline (search box removed); list is small enough to display without filtering per CONTEXT D-09."
  artifacts:
    - path: "src/renderer/src/components/icons/WarningTriangleIcon.tsx"
      provides: "Canonical stroke-style warning triangle icon component (D-06 single source of truth)"
      exports: ["WarningTriangleIcon"]
      contains: "M10 3 L17 16 H3 Z"
    - path: "src/renderer/src/panels/UnusedAssetsPanel.tsx"
      provides: "User-visible orphaned-to-unused rename, inline SearchBar removed, fill-to-stroke icon via WarningTriangleIcon component"
      contains_after: "{count} unused file"
    - path: "src/renderer/src/panels/MissingAttachmentsPanel.tsx"
      provides: "Fill-to-stroke warning triangle via WarningTriangleIcon component (line 65 site)"
      contains_after: "<WarningTriangleIcon"
    - path: "src/renderer/src/panels/GlobalMaxRenderPanel.tsx"
      provides: "Fill-to-stroke warning triangle in missing-row cell via WarningTriangleIcon component (line 461 site)"
      contains_after: "<WarningTriangleIcon"
    - path: "src/renderer/src/panels/AnimationBreakdownPanel.tsx"
      provides: "Fill-to-stroke warning triangle in missing-row cell via WarningTriangleIcon component (line 703 site)"
      contains_after: "<WarningTriangleIcon"
  key_links:
    - from: "WarningTriangleIcon.tsx"
      to: "all 4 warning-triangle render sites"
      via: "named import, used as JSX element with optional className prop"
      pattern: "import \\{ WarningTriangleIcon \\}"
    - from: "UnusedAssetsPanel headline copy"
      to: "user-visible 'unused' wording"
      via: "string literal at line 71 (post-rename)"
      pattern: "unused file"
    - from: "UnusedAssetsPanel table"
      to: "orphanedFiles prop directly"
      via: "removal of filter useMemo"
      pattern: "orphanedFiles\\.map"
---

<objective>
Polish the 4 panel files: rename user-visible "orphaned" to "unused" copy in UnusedAssetsPanel (D-08), remove the inline SearchBar in UnusedAssetsPanel (D-09), and convert all 4 fill-style warning triangles to a single canonical stroke-style component (D-05/D-06/D-07).

Purpose: Closes the icon-style audit per UI-08. Single-source-of-truth invariant for the warning triangle is structurally enforced via extraction to a shared `WarningTriangleIcon` component. Two prior 3-tab attempts on this phase were reverted; this plan is intentionally narrow (panel files only) and runs in parallel with Plan 01 (AppShell only).

Output: One new file (`src/renderer/src/components/icons/WarningTriangleIcon.tsx`); four modified panel files. No changes to AppShell, no changes to internal type/variable names (`OrphanedFile`, `orphanedFiles`, `filteredOrphans`), no changes to the math layer.

> **NOTE:** ROADMAP.md §Phase 26.2 still describes the dropped 3-tab scope. CONTEXT.md (D-05..D-09) supersedes the ROADMAP entry for this plan; the ROADMAP update is downstream.
</objective>

<ap_acknowledgement>
**AP-01 acknowledgement** (per `.continue-here.md` mandatory template):

> AP-01 is the toolbar-inlined tab anti-pattern (two prior reverts placed
> the tab buttons inside the main toolbar `<header>`). This plan does NOT
> modify `AppShell.tsx` or any tab-strip placement; it only touches panel
> files (UnusedAssetsPanel, MissingAttachmentsPanel, GlobalMaxRenderPanel,
> AnimationBreakdownPanel) plus a new icon component file. The AP-01
> structural invariant (tab strip lives in a sibling `<nav>` row below
> the main toolbar `<header>`) is owned by Plan 01.
</ap_acknowledgement>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/phases/26.2-ui-polish-tab-restructure-icon-audit/.continue-here.md
@.planning/phases/26.2-ui-polish-tab-restructure-icon-audit/26.2-CONTEXT.md

<interfaces>
New WarningTriangleIcon component contract (Task 1 creates this file):

```tsx
// src/renderer/src/components/icons/WarningTriangleIcon.tsx
export interface WarningTriangleIconProps {
  className?: string;
}
export function WarningTriangleIcon(props: WarningTriangleIconProps): JSX.Element
```

Existing fill SVG block (byte-identical at all 4 sites) being replaced:

```tsx
<svg viewBox="0 0 16 16" fill="currentColor" className="w-4 h-4" aria-hidden="true">
  <path d="M8 1.5 L14.5 13.5 H1.5 Z" />
  <rect x="7.25" y="5.5" width="1.5" height="4" fill="white" rx="0.5" />
  <rect x="7.25" y="11" width="1.5" height="1.5" fill="white" rx="0.5" />
</svg>
```

Replacement: a single `<WarningTriangleIcon className="w-4 h-4" />` JSX element.

UnusedAssetsPanel.tsx existing prop interface (D-08/D-09 rename of internals is OUT of scope per CONTEXT — only user-visible strings change):

```tsx
export interface UnusedAssetsPanelProps {
  orphanedFiles: OrphanedFile[];
}
export function UnusedAssetsPanel({ orphanedFiles }: UnusedAssetsPanelProps)
```

The existing wrapping spans at lines 56-58 / 62-64 / 457-460 / 700-702 use
`inline-flex items-center justify-center w-4 h-4 text-danger flex-shrink-0` and are
NOT modified. Only the inner `<svg>` block changes. Color inherits via currentColor
from the wrapping span's `text-danger` token.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create WarningTriangleIcon component (single source of truth)</name>
  <files>src/renderer/src/components/icons/WarningTriangleIcon.tsx</files>
  <read_first>
    - .planning/phases/26.2-ui-polish-tab-restructure-icon-audit/26.2-CONTEXT.md (D-04, D-06, D-06a, D-07 — canonical attributes + single-path invariant + dot rendering choice)
    - src/renderer/src/panels/GlobalMaxRenderPanel.tsx:836-839 (reference: existing stroke-style icon attributes used by panel headers)
    - src/renderer/src/components/SearchBar.tsx (reference: neighboring component file to confirm React 17+ JSX transform — no `import React` shim required)
  </read_first>
  <action>
Create a new directory and file: `src/renderer/src/components/icons/WarningTriangleIcon.tsx`

Use this exact content:

```tsx
/**
 * Phase 26.2 D-06/D-07 — Canonical stroke-style warning triangle icon.
 *
 * Single source of truth for the warning-triangle SVG used in:
 *   - MissingAttachmentsPanel header (alert bar)
 *   - UnusedAssetsPanel header (alert bar)
 *   - GlobalMaxRenderPanel missing-row cell (table)
 *   - AnimationBreakdownPanel missing-row cell (table)
 *
 * Path data is locked by D-06 (no per-site divergence). The exclamation
 * dot is rendered as a small filled circle (D-06a) with element-local
 * fill="currentColor" to override the SVG-level fill="none" cleanly.
 *
 * Color: inherits from currentColor — caller's surrounding span (e.g.
 * text-danger) sets the color. The className prop is optional; callers
 * pass w-4 h-4 per Phase 26.2 D-05 (no upsize at any of the 4 sites —
 * layout-shift constraint inside table cells and alert-bar headers).
 *
 * Attributes follow the Phase 26.1 D-12 / Phase 26.2 D-04 stroke standard.
 */
export interface WarningTriangleIconProps {
  className?: string;
}

export function WarningTriangleIcon({ className }: WarningTriangleIconProps) {
  return (
    <svg
      viewBox="0 0 20 20"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
      fill="none"
      className={className}
      aria-hidden="true"
    >
      <path d="M10 3 L17 16 H3 Z" />
      <path d="M10 8 V12" />
      <circle cx="10" cy="14.5" r="0.5" fill="currentColor" />
    </svg>
  );
}
```

Notes on the path data choice:

- Outer triangle path `M10 3 L17 16 H3 Z` is the exact form locked by CONTEXT D-06 (top vertex at (10,3), bottom vertices at (3,16) and (17,16)).
- Exclamation stem `M10 8 V12` is the exact form locked by D-06.
- Exclamation dot uses `<circle r="0.5">` with element-local `fill="currentColor"` per D-06a planner discretion. The element-local fill overrides the SVG-level `fill="none"` for this single element only; combined with `strokeWidth="1.5"` and the small radius, this produces a visually consistent dot at 16x16px in dark mode.
- No raw hex colors. No template-interpolation classNames.
- The component does NOT import React explicitly — the project uses the React 17+ JSX transform (no `import React` shim required; verify by checking neighboring component files like `SearchBar.tsx` which follow the same pattern).
  </action>
  <verify>
    <automated>cd /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager && test -f src/renderer/src/components/icons/WarningTriangleIcon.tsx && grep -c 'M10 3 L17 16 H3 Z' src/renderer/src/components/icons/WarningTriangleIcon.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/renderer/src/components/icons/WarningTriangleIcon.tsx`
    - Single canonical outer triangle path: `grep -c 'M10 3 L17 16 H3 Z' src/renderer/src/components/icons/WarningTriangleIcon.tsx` outputs `1`
    - Single exclamation stem path: `grep -c 'M10 8 V12' src/renderer/src/components/icons/WarningTriangleIcon.tsx` outputs `1`
    - Element-local fill on the dot: `grep -q 'cx="10" cy="14.5" r="0.5" fill="currentColor"' src/renderer/src/components/icons/WarningTriangleIcon.tsx`
    - All canonical stroke attributes present: each of these greps must succeed: `grep -q 'strokeWidth="1.5"'`, `grep -q 'strokeLinecap="round"'`, `grep -q 'strokeLinejoin="round"'`, `grep -q 'viewBox="0 0 20 20"'`, `grep -q 'fill="none"'` against `src/renderer/src/components/icons/WarningTriangleIcon.tsx`
    - Named export: `grep -q 'export function WarningTriangleIcon' src/renderer/src/components/icons/WarningTriangleIcon.tsx`
    - Optional className prop: `grep -q 'className?: string' src/renderer/src/components/icons/WarningTriangleIcon.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    `src/renderer/src/components/icons/WarningTriangleIcon.tsx` exists with the canonical stroke-style warning triangle, exports `WarningTriangleIcon` and the props interface. tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: UnusedAssetsPanel — D-08 rename + D-09 inline-search removal + D-05 fill-to-stroke icon</name>
  <files>src/renderer/src/panels/UnusedAssetsPanel.tsx</files>
  <read_first>
    - src/renderer/src/panels/UnusedAssetsPanel.tsx (lines 1-133 — full file; this task touches lines 19, 22, 28-43, 52, 59-63, 71, 87-93, 106-126)
    - src/renderer/src/components/icons/WarningTriangleIcon.tsx (Task 1 output — the import target)
    - .planning/phases/26.2-ui-polish-tab-restructure-icon-audit/26.2-CONTEXT.md (D-05, D-06, D-08, D-09 — locked decisions for this file)
  </read_first>
  <action>
Five coordinated edits inside `src/renderer/src/panels/UnusedAssetsPanel.tsx`. Apply them all in one pass.

**Edit A — Import changes (lines 19, 22):**

- Line 19: change `import { useState, useMemo } from 'react';` to `import { useState } from 'react';` (useMemo no longer used after Edit C).
- Line 22: delete the line `import { SearchBar } from '../components/SearchBar';` entirely (no other usage in the file — verified: only 2 occurrences of "SearchBar" in the file, both in deleted lines).
- Add a new import line below where the SearchBar import was: `import { WarningTriangleIcon } from '../components/icons/WarningTriangleIcon';`

**Edit B — Remove `query` useState (line 30):**

Delete the line:

```tsx
const [query, setQuery] = useState('');
```

The `expanded` useState immediately above (line 29) is unchanged.

**Edit C — Remove `filteredOrphans` useMemo (lines 39-43):**

Delete the entire block:

```tsx
const filteredOrphans = useMemo(() => {
  const q = query.trim().toLowerCase();
  if (q === '') return orphanedFiles.slice();
  return orphanedFiles.filter((f) => f.filename.toLowerCase().includes(q));
}, [orphanedFiles, query]);
```

**Edit D — D-08 user-visible string renames (lines 52, 71):**

- Line 52: change `aria-label="Orphaned image files"` to `aria-label="Unused image files"`.
- Line 71: change `{count} orphaned file{plural}` to `{count} unused file{plural}`.

Internal identifier names (the `orphanedFiles` prop, `count` derived from `orphanedFiles.length`, `OrphanedFile` type) are NOT renamed — out of scope per CONTEXT D-08.

**Edit E — D-05 fill-to-stroke icon (lines 59-63):**

Replace the existing inline `<svg>` block:

```tsx
<svg viewBox="0 0 16 16" fill="currentColor" className="w-4 h-4" aria-hidden="true">
  <path d="M8 1.5 L14.5 13.5 H1.5 Z" />
  <rect x="7.25" y="5.5" width="1.5" height="4" fill="white" rx="0.5" />
  <rect x="7.25" y="11" width="1.5" height="1.5" fill="white" rx="0.5" />
</svg>
```

With:

```tsx
<WarningTriangleIcon className="w-4 h-4" />
```

The surrounding wrapping `<span className="inline-flex items-center justify-center w-4 h-4 text-danger flex-shrink-0" aria-hidden="true">` (lines 55-58) is UNCHANGED — color inherits via currentColor from `text-danger` on the parent span.

**Edit F — D-09 SearchBar JSX removal (lines 87-93) and empty-state cell removal (lines 106-126):**

1. Delete the `<div className="basis-full mt-2">` wrapping the `<SearchBar>` block (lines 87-93):

```tsx
<div className="basis-full mt-2">
  <SearchBar
    value={query}
    onChange={setQuery}
    placeholder="Filter by filename…"
  />
</div>
```

2. Replace the conditional ternary at lines 106-126 (the `<tbody>` body) with an unconditional map over `orphanedFiles`:

Before:

```tsx
<tbody>
  {filteredOrphans.length === 0 ? (
    <tr>
      <td
        colSpan={2}
        className="text-fg-muted font-mono text-sm text-center py-4"
      >
        (no matches)
      </td>
    </tr>
  ) : (
    filteredOrphans.map((f) => (
      <tr key={f.filename} className="border-b border-border">
        <td className="py-2 px-3 font-mono text-sm text-fg">
          {f.filename}
        </td>
        <td className="py-2 px-3 font-mono text-sm text-fg-muted">
          {formatBytes(f.bytesOnDisk)}
        </td>
      </tr>
    ))
  )}
</tbody>
```

After:

```tsx
<tbody>
  {orphanedFiles.map((f) => (
    <tr key={f.filename} className="border-b border-border">
      <td className="py-2 px-3 font-mono text-sm text-fg">
        {f.filename}
      </td>
      <td className="py-2 px-3 font-mono text-sm text-fg-muted">
        {formatBytes(f.bytesOnDisk)}
      </td>
    </tr>
  ))}
</tbody>
```

The `(no matches)` empty-state cell is no longer reachable: `orphanedFiles.length === 0` is already short-circuited by `if (orphanedFiles.length === 0) return null;` at line 47 (this guard is RETAINED — it now serves as the sole empty-state path).

**Constraints across all edits:**

- Do not rename `OrphanedFile`, `orphanedFiles`, or any other internal identifier — D-08 explicitly scopes the rename to the 2 user-visible strings.
- The `if (orphanedFiles.length === 0) return null;` guard at line 47 stays.
- The `expanded` toggle button (lines 77-84) is unchanged.
- The wrapping `<div role="alert" ...>` (lines 50-53) keeps all its existing classes; only the `aria-label` value changes.
- After edits, the file should still type-check (`tsc --noEmit`).
  </action>
  <verify>
    <automated>cd /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager && grep -q 'unused file' src/renderer/src/panels/UnusedAssetsPanel.tsx && ! grep -q 'orphaned file{plural}' src/renderer/src/panels/UnusedAssetsPanel.tsx && ! grep -q '<SearchBar' src/renderer/src/panels/UnusedAssetsPanel.tsx && grep -q 'WarningTriangleIcon' src/renderer/src/panels/UnusedAssetsPanel.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - D-08 headline rename: `grep -q '{count} unused file{plural}' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - D-08 aria-label rename: `grep -q 'aria-label="Unused image files"' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - Old strings absent: `! grep -q '{count} orphaned file{plural}' src/renderer/src/panels/UnusedAssetsPanel.tsx` AND `! grep -q 'aria-label="Orphaned image files"' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - D-09 SearchBar JSX gone: `! grep -q '<SearchBar' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - D-09 SearchBar import gone: `! grep -q "import { SearchBar }" src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - D-09 query useState gone: `! grep -q "const \[query, setQuery\]" src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - D-09 filteredOrphans removed: `! grep -q 'filteredOrphans' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - D-09 (no matches) cell removed: `! grep -q '(no matches)' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - D-09 unconditional render: `grep -q 'orphanedFiles.map' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - D-05 stroke icon via component: `grep -q 'WarningTriangleIcon' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - D-05 old fill SVG gone from this file: `! grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - useMemo no longer imported: `! grep -q 'useMemo' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - Internal identifiers preserved (D-08 scope guard): `grep -q 'orphanedFiles: OrphanedFile\[\]' src/renderer/src/panels/UnusedAssetsPanel.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    UnusedAssetsPanel.tsx renders "N unused files" headline (verified via grep), aria-label is "Unused image files", inline SearchBar fully removed (JSX + state + import + filter useMemo + empty-state cell), warning triangle uses `<WarningTriangleIcon className="w-4 h-4" />` from the new shared component, internal type/variable names unchanged. tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: MissingAttachmentsPanel — D-05 fill-to-stroke icon swap (line 65)</name>
  <files>src/renderer/src/panels/MissingAttachmentsPanel.tsx</files>
  <read_first>
    - src/renderer/src/panels/MissingAttachmentsPanel.tsx (lines 1-30 for existing imports; lines 60-70 for the icon site)
    - src/renderer/src/components/icons/WarningTriangleIcon.tsx (Task 1 output — the import target)
    - .planning/phases/26.2-ui-polish-tab-restructure-icon-audit/26.2-CONTEXT.md (D-05, D-06, D-07 — single-source-of-truth invariant)
  </read_first>
  <action>
Two edits inside `src/renderer/src/panels/MissingAttachmentsPanel.tsx`:

**Edit A — Add import.**

Add the import line near the top of the file (after the existing import block):

```tsx
import { WarningTriangleIcon } from '../components/icons/WarningTriangleIcon';
```

(Locate the existing import section — typically near the top of the file. Place the new import alongside or just below the other component imports, following the file's existing import ordering convention.)

**Edit B — Replace the inline `<svg>` at lines 65-69.**

Replace:

```tsx
<svg viewBox="0 0 16 16" fill="currentColor" className="w-4 h-4" aria-hidden="true">
  <path d="M8 1.5 L14.5 13.5 H1.5 Z" />
  <rect x="7.25" y="5.5" width="1.5" height="4" fill="white" rx="0.5" />
  <rect x="7.25" y="11" width="1.5" height="1.5" fill="white" rx="0.5" />
</svg>
```

With:

```tsx
<WarningTriangleIcon className="w-4 h-4" />
```

The surrounding wrapping `<span className="inline-flex items-center justify-center w-4 h-4 text-danger flex-shrink-0" aria-hidden="true">` (lines 62-64) is UNCHANGED — color inherits via currentColor from the wrapping span.

No other changes to this file. Headline copy ("N attachment{plural} missing PNG{plural}") is unchanged — D-08 rename is scoped to UnusedAssetsPanel only.
  </action>
  <verify>
    <automated>cd /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager && grep -q 'WarningTriangleIcon' src/renderer/src/panels/MissingAttachmentsPanel.tsx && ! grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/MissingAttachmentsPanel.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Import added: `grep -q "import { WarningTriangleIcon } from '../components/icons/WarningTriangleIcon';" src/renderer/src/panels/MissingAttachmentsPanel.tsx`
    - Component used: `grep -q '<WarningTriangleIcon className="w-4 h-4" />' src/renderer/src/panels/MissingAttachmentsPanel.tsx`
    - Old fill triangle path gone: `! grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/MissingAttachmentsPanel.tsx`
    - Old fill rect cutout gone: `! grep -q 'rect x="7.25" y="5.5" width="1.5" height="4" fill="white"' src/renderer/src/panels/MissingAttachmentsPanel.tsx`
    - Headline text unchanged: `grep -q 'attachment{plural} missing PNG{plural}' src/renderer/src/panels/MissingAttachmentsPanel.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    MissingAttachmentsPanel.tsx imports and uses `<WarningTriangleIcon className="w-4 h-4" />` in place of the old inline fill SVG. Old fill-style path data is no longer present in the file. Other content unchanged. tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: GlobalMaxRenderPanel — D-05 fill-to-stroke icon swap (line 461)</name>
  <files>src/renderer/src/panels/GlobalMaxRenderPanel.tsx</files>
  <read_first>
    - src/renderer/src/panels/GlobalMaxRenderPanel.tsx (lines 1-50 for existing imports; lines 455-475 for the missing-row icon site)
    - src/renderer/src/components/icons/WarningTriangleIcon.tsx (Task 1 output)
  </read_first>
  <action>
Two edits inside `src/renderer/src/panels/GlobalMaxRenderPanel.tsx`:

**Edit A — Add import.**

Add the import line near the top of the file alongside other component imports:

```tsx
import { WarningTriangleIcon } from '../components/icons/WarningTriangleIcon';
```

**Edit B — Replace the inline `<svg>` at lines 461-471 (inside the missing-row table cell).**

Replace:

```tsx
<svg
  viewBox="0 0 16 16"
  fill="currentColor"
  className="w-4 h-4"
  aria-hidden="true"
>
  <path d="M8 1.5 L14.5 13.5 H1.5 Z" />
  <rect x="7.25" y="5.5" width="1.5" height="4" fill="white" rx="0.5" />
  <rect x="7.25" y="11" width="1.5" height="1.5" fill="white" rx="0.5" />
</svg>
```

With:

```tsx
<WarningTriangleIcon className="w-4 h-4" />
```

The surrounding wrapping `<span className="inline-flex items-center justify-center w-4 h-4 text-danger mr-1 flex-shrink-0" aria-label="Missing PNG">` (lines 457-460) is UNCHANGED — color inherits via currentColor from the wrapping span; the `aria-label="Missing PNG"` on the span provides the accessible name (note: the icon component itself has `aria-hidden="true"`, the parent span carries the label).

The conditional `{row.isMissing && (...)}` wrapper (line 456) and the surrounding table cell are unchanged. The filmstrip section-header icon at lines 836-839 is NOT modified — it is already in stroke style (and is the source of truth referenced by Plan 01's Global tab icon).
  </action>
  <verify>
    <automated>cd /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager && grep -q 'WarningTriangleIcon' src/renderer/src/panels/GlobalMaxRenderPanel.tsx && grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/GlobalMaxRenderPanel.tsx && echo "ERROR: old path still present" || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Import added: `grep -q "import { WarningTriangleIcon } from '../components/icons/WarningTriangleIcon';" src/renderer/src/panels/GlobalMaxRenderPanel.tsx`
    - Component used: `grep -q '<WarningTriangleIcon className="w-4 h-4" />' src/renderer/src/panels/GlobalMaxRenderPanel.tsx`
    - Old fill triangle path gone (this file only had the one fill instance at line 461; the section-header at 836-839 was already stroke): `! grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/GlobalMaxRenderPanel.tsx`
    - Old fill rect cutout gone: `! grep -q 'rect x="7.25" y="5.5" width="1.5" height="4" fill="white"' src/renderer/src/panels/GlobalMaxRenderPanel.tsx`
    - Filmstrip section-header icon preserved (Plan 01's source of truth — must NOT be touched): `grep -q 'rect x="2" y="6" width="16" height="8" rx="1"' src/renderer/src/panels/GlobalMaxRenderPanel.tsx` AND `grep -q 'M5 6 v3 M8 6 v2 M11 6 v3 M14 6 v2 M17 6 v3' src/renderer/src/panels/GlobalMaxRenderPanel.tsx`
    - aria-label preserved on wrapper span: `grep -q 'aria-label="Missing PNG"' src/renderer/src/panels/GlobalMaxRenderPanel.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    GlobalMaxRenderPanel.tsx imports and uses `<WarningTriangleIcon className="w-4 h-4" />` for the missing-row warning. Old fill SVG path is removed. The filmstrip section-header icon at lines 836-839 is preserved (it is Plan 01's verbatim source for the Global tab icon). tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: AnimationBreakdownPanel — D-05 fill-to-stroke icon swap (line 703)</name>
  <files>src/renderer/src/panels/AnimationBreakdownPanel.tsx</files>
  <read_first>
    - src/renderer/src/panels/AnimationBreakdownPanel.tsx (lines 1-50 for existing imports; lines 697-715 for the missing-row icon site)
    - src/renderer/src/components/icons/WarningTriangleIcon.tsx (Task 1 output)
  </read_first>
  <action>
Two edits inside `src/renderer/src/panels/AnimationBreakdownPanel.tsx`:

**Edit A — Add import.**

Add the import line near the top of the file alongside other component imports:

```tsx
import { WarningTriangleIcon } from '../components/icons/WarningTriangleIcon';
```

**Edit B — Replace the inline `<svg>` at lines 703-712 (inside the missing-row table cell).**

Replace:

```tsx
<svg
  viewBox="0 0 16 16"
  fill="currentColor"
  className="w-4 h-4"
  aria-hidden="true"
>
  <path d="M8 1.5 L14.5 13.5 H1.5 Z" />
  <rect x="7.25" y="5.5" width="1.5" height="4" fill="white" rx="0.5" />
  <rect x="7.25" y="11" width="1.5" height="1.5" fill="white" rx="0.5" />
</svg>
```

With:

```tsx
<WarningTriangleIcon className="w-4 h-4" />
```

The surrounding wrapping `<span className="inline-flex items-center justify-center w-4 h-4 text-danger mr-1 flex-shrink-0" aria-label="Missing PNG">` (lines 700-702) is UNCHANGED — color inherits via currentColor; aria-label "Missing PNG" remains on the parent span.

The conditional `{row.isMissing && (...)}` wrapper (line 698) and the surrounding table cell are unchanged. The bar-chart section-header icon at line 415 is NOT modified — it is already in stroke style (and is the source of truth referenced by Plan 01's Animation Breakdown tab icon).
  </action>
  <verify>
    <automated>cd /Users/leo/Documents/WORK/CODING/Spine_Texture_Manager && grep -q 'WarningTriangleIcon' src/renderer/src/panels/AnimationBreakdownPanel.tsx && grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/AnimationBreakdownPanel.tsx && echo "ERROR: old path still present" || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Import added: `grep -q "import { WarningTriangleIcon } from '../components/icons/WarningTriangleIcon';" src/renderer/src/panels/AnimationBreakdownPanel.tsx`
    - Component used: `grep -q '<WarningTriangleIcon className="w-4 h-4" />' src/renderer/src/panels/AnimationBreakdownPanel.tsx`
    - Old fill triangle path gone: `! grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/AnimationBreakdownPanel.tsx`
    - Old fill rect cutout gone: `! grep -q 'rect x="7.25" y="5.5" width="1.5" height="4" fill="white"' src/renderer/src/panels/AnimationBreakdownPanel.tsx`
    - Bar-chart section-header icon preserved (Plan 01's source of truth — must NOT be touched): `grep -q 'M3 17 V10 M8 17 V6 M13 17 V12 M18 17 V4' src/renderer/src/panels/AnimationBreakdownPanel.tsx`
    - aria-label preserved on wrapper span: `grep -q 'aria-label="Missing PNG"' src/renderer/src/panels/AnimationBreakdownPanel.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    AnimationBreakdownPanel.tsx imports and uses `<WarningTriangleIcon className="w-4 h-4" />` for the missing-row warning. Old fill SVG path is removed. The bar-chart section-header icon at line 415 is preserved (Plan 01's verbatim source for the Animation Breakdown tab icon). tsc passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human visual verification — D-05/D-06/D-08/D-09 + cross-site icon consistency</name>
  <what-built>
    - New shared component `src/renderer/src/components/icons/WarningTriangleIcon.tsx` (D-06/D-07 single source of truth).
    - All 4 fill warning triangles converted to stroke style via the shared component (D-05): MissingAttachmentsPanel:65, UnusedAssetsPanel:59, GlobalMaxRenderPanel:461, AnimationBreakdownPanel:703.
    - UnusedAssetsPanel headline reads "N unused files" (was "orphaned files") (D-08).
    - UnusedAssetsPanel aria-label is "Unused image files" (was "Orphaned image files") (D-08).
    - UnusedAssetsPanel inline SearchBar removed (D-09): no filter input, no `(no matches)` cell, list renders unconditionally.
    - Internal type/variable names (`OrphanedFile`, `orphanedFiles`, `filteredOrphans`) intentionally NOT renamed (D-08 scope).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and load `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`. (If the fixture has no orphaned PNGs visible by default, drop a stray .png into the images folder to surface the UnusedAssets alert bar.)
    2. Verify D-08 rename (UnusedAssetsPanel):
       - Headline reads `N unused file(s)` (NOT "orphaned").
       - Hover the alert bar; the accessible name (DevTools accessibility inspector) shows `Unused image files`.
    3. Verify D-09 search removal (UnusedAssetsPanel):
       - Click "Show details" on the unused-files alert bar.
       - Confirm there is NO filter input box above the table.
       - Confirm the table immediately lists all unused files (no filtering UI).
       - With zero unused files, confirm the panel does not render (the existing `return null` guard fires).
    4. Verify D-05 / D-06 / D-07 single canonical stroke triangle at all 4 sites:
       - **Site 1 — MissingAttachmentsPanel header (alert bar)**: surface this by loading a project with at least one missing PNG. Confirm: outline triangle with thin (1.5 stroke-width) strokes, no filled shape, an inner exclamation mark visible.
       - **Site 2 — UnusedAssetsPanel header (alert bar)**: confirm identical-shape outline triangle.
       - **Site 3 — GlobalMaxRenderPanel missing-row table cell**: switch to Global tab, find a row marked missing — confirm the same outline triangle in the cell, sized identically (no row-height shift).
       - **Site 4 — AnimationBreakdownPanel missing-row table cell**: switch to Animation Breakdown tab, find a row marked missing — confirm the same outline triangle.
       - **Cross-site invariant:** the 4 triangles must be visually IDENTICAL in shape, weight, and proportions (single source of truth — same component). Color may differ if the parent span carries a different `text-*` token (currently all 4 use `text-danger`).
    5. Confirm absence of layout shift at any of the 4 sites (rows should have the same height as before; alert bar headers should align identically).
    6. Confirm Plan 01's tab icons (filmstrip on Global, bar-chart on Animation Breakdown) are still rendered correctly — Plan 02 must not have touched them.
    7. Open browser DevTools → Network/Sources, search for "M8 1.5" — confirm zero matches in any of the 4 modified panel files (old fill path is fully purged).
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 stroke triangles are visually identical, the rename is correct, the inline SearchBar is gone, and Plan 01's tab icons are intact. Otherwise describe the issue (e.g., "site 3 triangle is bolder than site 1", "headline still says orphaned", "search box still visible") and the executor will iterate.</resume-signal>
  <files>(none — verification only; no files written by this checkpoint)</files>
  <action>Pause execution. Present the &lt;what-built&gt; summary to the user, run `npm run dev`, and walk through the &lt;how-to-verify&gt; steps interactively. Wait for the user's resume-signal response before continuing. Do not proceed past this plan until approval.</action>
  <verify>
    <automated>echo "human-verify checkpoint — no automated verify; gated on user approval"</automated>
  </verify>
  <done>User typed "approved" (or equivalent) confirming the cross-site stroke-triangle visual consistency, D-08 rename, D-09 search removal, and Plan 01 tab-icon preservation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan is renderer-only UI polish (string + SVG + DOM-structure changes). No new IPC handlers, no new file I/O, no new network calls, no new shell commands, no untrusted input parsing or rendering paths.

| Boundary | Description |
|----------|-------------|
| (none new) | This plan introduces no new trust boundaries. The existing renderer↔main IPC surface and the existing `OrphanedFile` data flow (loaded via existing IPC, rendered as React text children — already mitigated for XSS by React auto-escaping per Phase 24 PANEL-02 mitigation T-24-03-01) are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.2-02-01 | I (Information disclosure) | WarningTriangleIcon component | accept | UI-only SVG component with hard-coded literal path data and attributes. No props affect SVG content; only `className` is forwarded to the root `<svg>` element. No user input flows into rendered SVG markup. |
| T-26.2-02-02 | T (Tampering) | UnusedAssetsPanel rename | accept | The rename swaps two literal strings ('orphaned' to 'unused' in copy and aria-label). No code path is added or removed; data flow into the table remains identical (the `orphanedFiles` prop is read unmodified). React continues to escape filename text children — XSS mitigation T-24-03-01 unchanged. |
| T-26.2-02-03 | T (Tampering) | UnusedAssetsPanel SearchBar removal | accept | Removing the inline `<SearchBar>` and its `query` state reduces (does not expand) the input surface. The toolbar SearchBar in `AppShell.tsx` is unaffected. No new event handlers or state are introduced. |
</threat_model>

<verification>
**Automated checks (run after Tasks 1-5, before the human checkpoint):**

```sh
# WarningTriangleIcon component exists and exports the canonical path
test -f src/renderer/src/components/icons/WarningTriangleIcon.tsx
grep -q 'M10 3 L17 16 H3 Z' src/renderer/src/components/icons/WarningTriangleIcon.tsx

# Single source of truth: the canonical outer-triangle path appears ONLY in WarningTriangleIcon.tsx
total=$(grep -rl 'M10 3 L17 16 H3 Z' src/renderer/src 2>/dev/null | wc -l | tr -d ' ')
[ "$total" = "1" ] && echo "PASS: single source of truth"

# Old fill triangle path is gone from all 4 panel files
! grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/UnusedAssetsPanel.tsx
! grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/MissingAttachmentsPanel.tsx
! grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/GlobalMaxRenderPanel.tsx
! grep -q 'M8 1.5 L14.5 13.5 H1.5 Z' src/renderer/src/panels/AnimationBreakdownPanel.tsx

# All 4 panel files import and use WarningTriangleIcon
for f in UnusedAssetsPanel MissingAttachmentsPanel GlobalMaxRenderPanel AnimationBreakdownPanel; do
  grep -q "import { WarningTriangleIcon }" "src/renderer/src/panels/${f}.tsx" || echo "MISSING import in ${f}"
  grep -q '<WarningTriangleIcon ' "src/renderer/src/panels/${f}.tsx" || echo "MISSING usage in ${f}"
done

# D-08 rename in UnusedAssetsPanel
grep -q '{count} unused file{plural}' src/renderer/src/panels/UnusedAssetsPanel.tsx
grep -q 'aria-label="Unused image files"' src/renderer/src/panels/UnusedAssetsPanel.tsx
! grep -q '{count} orphaned file{plural}' src/renderer/src/panels/UnusedAssetsPanel.tsx
! grep -q 'aria-label="Orphaned image files"' src/renderer/src/panels/UnusedAssetsPanel.tsx

# D-09 SearchBar fully removed from UnusedAssetsPanel
! grep -q 'SearchBar' src/renderer/src/panels/UnusedAssetsPanel.tsx
! grep -q 'filteredOrphans' src/renderer/src/panels/UnusedAssetsPanel.tsx
! grep -q 'useMemo' src/renderer/src/panels/UnusedAssetsPanel.tsx
! grep -q '(no matches)' src/renderer/src/panels/UnusedAssetsPanel.tsx
grep -q 'orphanedFiles.map' src/renderer/src/panels/UnusedAssetsPanel.tsx

# D-08 internal-identifier scope guard: prop name still uses 'orphanedFiles'
grep -q 'orphanedFiles: OrphanedFile' src/renderer/src/panels/UnusedAssetsPanel.tsx

# Plan 01's tab-icon source paths preserved (must NOT be touched by Plan 02)
grep -q 'rect x="2" y="6" width="16" height="8" rx="1"' src/renderer/src/panels/GlobalMaxRenderPanel.tsx
grep -q 'M3 17 V10 M8 17 V6 M13 17 V12 M18 17 V4' src/renderer/src/panels/AnimationBreakdownPanel.tsx

# Build + typecheck + tests
npx tsc --noEmit
npm run build
npm run test
```

**Human verification:** Task 6 checkpoint — visual cross-site triangle consistency + rename + search-removal sanity check.
</verification>

<success_criteria>
- `WarningTriangleIcon` component exists with the canonical D-06 path.
- Canonical outer triangle path `M10 3 L17 16 H3 Z` appears in exactly ONE file in `src/renderer/src` (single-source-of-truth grep gate).
- All 4 panel files import and use `<WarningTriangleIcon className="w-4 h-4" />`.
- Old fill-style path `M8 1.5 L14.5 13.5 H1.5 Z` is purged from all 4 panel files.
- UnusedAssetsPanel headline reads "{count} unused file{plural}"; aria-label is "Unused image files".
- UnusedAssetsPanel has no `<SearchBar>`, no `query` state, no `filteredOrphans` useMemo, no `(no matches)` cell.
- UnusedAssetsPanel internal identifiers (`orphanedFiles`, `OrphanedFile`) preserved.
- Plan 01's tab-icon source paths (filmstrip rect+path in GlobalMaxRenderPanel, bar-chart path in AnimationBreakdownPanel) are still present and unchanged.
- `tsc --noEmit`, `npm run build`, and `npm run test` all exit 0.
- Human checkpoint approved: stroke triangles visually identical at all 4 sites; rename and search-removal land cleanly; Plan 01's tab icons intact.
</success_criteria>

<output>
After completion, create `.planning/phases/26.2-ui-polish-tab-restructure-icon-audit/26.2-02-SUMMARY.md` covering:
- AP-01 acknowledgement (this plan does not touch tab placement; Plan 01 owns that).
- The 5 file edits (WarningTriangleIcon component creation + 4 panel call-site swaps + UnusedAssetsPanel rename and search removal).
- Single-source-of-truth grep evidence (one file contains the canonical path; 4 files import it).
- D-08 internal-identifier scope guard evidence (`orphanedFiles` prop name preserved).
- Human checkpoint outcome.
- Note that ROADMAP §Phase 26.2 remains stale; downstream task to update.
</output>
