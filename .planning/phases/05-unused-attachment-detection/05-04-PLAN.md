---
phase: 05-unused-attachment-detection
plan: 04
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - .planning/phases/05-unused-attachment-detection/05-VALIDATION.md
autonomous: false
requirements:
  - F6.1
  - F6.2
user_setup: []

must_haves:
  truths:
    - "Full automated suite green — npm run test exits 0 with total passed count >= 120 + 1 skipped"
    - "Arch gates 8/8 green (Layer 3, portability, sandbox CJS invariants, GlobalMaxRenderPanel batch-scope regression guards)"
    - "CLI byte-for-byte unchanged — git diff scripts/cli.ts empty AND npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json output matches the Plan-02 baseline captured at /tmp/cli-phase5-plan02-baseline.txt (byte-for-byte diff empty; D-102 invariant)"
    - "Sampler byte-for-byte unchanged — git diff src/core/sampler.ts empty (D-100 invariant)"
    - "npx electron-vite build exits 0; .dmg package builds cleanly"
    - "checkpoint:human-verify signs off: SIMPLE_TEST.json drop shows no unused section; SIMPLE_TEST_GHOST.json drop shows the section with 1 row (GHOST · 64×64 · default) in red-header / neutral-cell treatment; SearchBar filters BOTH peak table AND unused section consistently; U+26A0 glyph renders cleanly in JetBrains Mono (fallback to SVG only if human-verify flags it)"
    - "05-VALIDATION.md per-task map populated — every Plan 01/02/03 task has a row with Task ID, Plan, Wave, requirement ID, command, status ✅ green"
    - "05-VALIDATION.md frontmatter flipped: nyquist_compliant: true, wave_0_complete: true, status: signed-off"
  artifacts:
    - path: ".planning/phases/05-unused-attachment-detection/05-VALIDATION.md"
      provides: "Populated per-task verification map + signed-off frontmatter"
      contains: "nyquist_compliant: true"
      contains_also: "wave_0_complete: true"
  key_links:
    - from: ".planning/phases/05-unused-attachment-detection/05-VALIDATION.md"
      to: "Plans 01/02/03 automated verifies"
      via: "per-task map row population"
      pattern: "Task \\d"
---

<objective>
Close Phase 5 with a full regression sweep, CLI byte-for-byte compare against the pre-Phase-5 baseline, VALIDATION.md per-task map population, and a `checkpoint:human-verify` signing off the F6.2 UI surface against the GHOST fixture and the clean SIMPLE_TEST rig.

Purpose: confirm that F6.1 (detection math) + F6.2 (UI surface) work end-to-end on real drops, that no existing invariant regressed (sampler/CLI/arch/batch-scope), and that the planning artifacts reflect the shipped reality before `/gsd-verify-work 5`.

Output:
- A clean `npm run test` + `npx electron-vite build`.
- An empty `git diff scripts/cli.ts` and `git diff src/core/sampler.ts` confirming D-100 / D-102 locks.
- `.planning/phases/05-unused-attachment-detection/05-VALIDATION.md` with every Plan 01/02/03 task logged under the per-task map + frontmatter flipped to signed-off.
- A human-verify checkpoint with explicit steps (drop SIMPLE_TEST, drop SIMPLE_TEST_GHOST, filter, visual red-scope audit, CLI sanity) and a resume-signal contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-unused-attachment-detection/05-CONTEXT.md
@.planning/phases/05-unused-attachment-detection/05-RESEARCH.md
@.planning/phases/05-unused-attachment-detection/05-VALIDATION.md
@.planning/phases/05-unused-attachment-detection/05-01-PLAN.md
@.planning/phases/05-unused-attachment-detection/05-02-PLAN.md
@.planning/phases/05-unused-attachment-detection/05-03-PLAN.md
@CLAUDE.md

# Files checked for byte-identity (locked since Phase 2)
@scripts/cli.ts
@src/core/sampler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated exit-criteria sweep (full suite, CLI diff, build, locked-file audit)</name>
  <files></files>
  <read_first>
    - scripts/cli.ts (confirm current state against the reference baseline captured at the end of Plan 02)
    - src/core/sampler.ts (confirm no drift from Phase 4 state)
    - .planning/phases/05-unused-attachment-detection/05-02-PLAN.md Task 3 "Full suite regression check" (the baseline this task compares against)
  </read_first>
  <action>
    Execute the full automated gate battery. No files are edited by this task — it is a pure verification pass that produces observable command output.

    Step 1 — Full test suite:
    ```
    npm run test
    ```
    Expected: all specs pass. Count should be >= 120 passed + 1 skipped (Plan 02 baseline after the 9 new Phase 5 specs landed).

    Step 2 — Arch gate focused run:
    ```
    npm run test -- tests/arch.spec.ts
    ```
    Expected: 8 describes, all green. The 4 describes added in Phases 1–4 (Layer 3 boundary, portability, sandbox invariants, main-bundle CJS invariant, Phase 4 batch-scope regression guard) must all pass.

    Step 3 — Full typecheck on both projects:
    ```
    npx tsc --noEmit -p tsconfig.node.json
    npx tsc --noEmit -p tsconfig.web.json
    ```
    Both exit 0.

    Step 4 — Production build:
    ```
    npx electron-vite build
    ```
    Expected: out/main/index.cjs + out/preload/index.cjs + out/renderer/index.html + assets all produced. Emitted renderer CSS bundle contains either the literal `#e06b55` or the utility selector `.text-danger`.

    Step 5 — Locked-file audit:
    ```
    git diff scripts/cli.ts
    git diff src/core/sampler.ts
    git diff src/core/loader.ts
    git diff src/core/bounds.ts
    git diff src/core/bones.ts
    ```
    All five commands must produce empty output. These are the LOCKED files per D-100 / D-102 and the Phase 0 architecture. Any diff here is a phase violation.

    Step 6 — CLI byte-for-byte compare against the Plan 02 baseline:
    ```
    npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json > /tmp/cli-phase5-plan04-final.txt
    diff /tmp/cli-phase5-plan02-baseline.txt /tmp/cli-phase5-plan04-final.txt
    ```
    Expected: `diff` produces no output (exit 0, files identical). If the baseline file is missing (e.g. dev environment rebooted), regenerate it by checking out the pre-Phase-5 commit (HEAD~N where N is the count of Phase 5 commits) and re-running the CLI; then compare.

    Record the outcome of every step in the eventual SUMMARY. If ANY step fails, stop and escalate — Phase 5 is not ready to close.
  </action>
  <verify>
    <automated>npm run test &amp;&amp; npx tsc --noEmit -p tsconfig.node.json &amp;&amp; npx tsc --noEmit -p tsconfig.web.json &amp;&amp; npx electron-vite build &amp;&amp; test -z "$(git diff scripts/cli.ts)" &amp;&amp; test -z "$(git diff src/core/sampler.ts)" &amp;&amp; test -z "$(git diff src/core/loader.ts)" &amp;&amp; test -z "$(git diff src/core/bounds.ts)" &amp;&amp; test -z "$(git diff src/core/bones.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0.
    - Test output shows at least 120 passing specs (pre-Phase-5 baseline was 116; Phase 5 adds ~8 usage.spec cases + 1 summary F6.2 case = 125 floor; exact count will depend on how the executor structured the usage.spec describe blocks, but MUST exceed 116).
    - `npm run test -- tests/arch.spec.ts` exits 0 with 8/8 describes green.
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0.
    - `npx tsc --noEmit -p tsconfig.web.json` exits 0.
    - `npx electron-vite build` exits 0.
    - `find out/renderer/assets -name 'index-*.css' -exec grep -l "e06b55\|text-danger" {} \;` returns at least one file path.
    - `git diff scripts/cli.ts` produces no output.
    - `git diff src/core/sampler.ts` produces no output.
    - `git diff src/core/loader.ts` produces no output.
    - `git diff src/core/bounds.ts` produces no output.
    - `git diff src/core/bones.ts` produces no output.
    - CLI output capture file /tmp/cli-phase5-plan04-final.txt exists and is non-empty.
  </acceptance_criteria>
  <done>All 6 automated gates pass; locked files confirmed unchanged; CLI output captured for the human-verify step; Phase 5 is ready for human sign-off.</done>
</task>

<task type="auto">
  <name>Task 2: Populate 05-VALIDATION.md per-task map + flip frontmatter</name>
  <files>.planning/phases/05-unused-attachment-detection/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-unused-attachment-detection/05-VALIDATION.md (current state — the per-task map table currently has a single placeholder row)
    - .planning/phases/05-unused-attachment-detection/05-01-PLAN.md (each task's `<automated>` command is the source for one row)
    - .planning/phases/05-unused-attachment-detection/05-02-PLAN.md (each task's `<automated>` command is the source for one row)
    - .planning/phases/05-unused-attachment-detection/05-03-PLAN.md (each task's `<automated>` command is the source for one row)
  </read_first>
  <action>
    Edit `.planning/phases/05-unused-attachment-detection/05-VALIDATION.md` with TWO changes.

    **Edit 1 — Replace the per-task map table** (currently the single-row placeholder at the "Per-Task Verification Map" section). Populate one row per task from Plans 01–03:

    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | T05-01-01 | 05-01 | 1 | F6.1, F6.2 | T-05-01-01 | structuredClone-safe IPC shape | typecheck | `npx tsc --noEmit -p tsconfig.node.json && npx tsc --noEmit -p tsconfig.web.json` | ✅ | ✅ green |
    | T05-01-02 | 05-01 | 1 | F6.1 | — | N/A | fixture | `node -e "const j = require('./fixtures/SIMPLE_PROJECT/SIMPLE_TEST_GHOST.json'); console.log(j.skins[0].attachments.CIRCLE.GHOST.width)"` | ✅ | ✅ green |
    | T05-01-03 | 05-01 | 1 | F6.1 | — | N/A | unit (RED in W0) | `npm run test -- tests/core/usage.spec.ts` | ✅ | ✅ green (RED→GREEN Plan 02) |
    | T05-01-03b | 05-01 | 1 | F6.1 | — | F6.1 sanity canary: Defined ⊇ Used invariant (sampler-bug canary per 05-RESEARCH.md §Validation Architecture) | unit | `npm run test -- tests/core/usage.spec.ts -t "invariant: every globalPeaks"` | ✅ | ✅ green |
    | T05-01-04 | 05-01 | 1 | F6.2 | T-05-01-01 | structuredClone round-trip | unit (RED in W0) | `npm run test -- tests/core/summary.spec.ts` | ✅ | ✅ green (RED→GREEN Plan 02) |
    | T05-02-01 | 05-02 | 2 | F6.1 | T-05-02-01 | pure-TS DOM-free + zero I/O | unit | `npm run test -- tests/core/usage.spec.ts` | ✅ | ✅ green |
    | T05-02-02 | 05-02 | 2 | F6.1 | T-05-02-01 | projection-only wiring | unit | `npm run test -- tests/core/summary.spec.ts` | ✅ | ✅ green |
    | T05-02-03 | 05-02 | 2 | F6.1, F6.2 | T-05-02-03, T-05-02-04 | D-100 sampler lock + D-102 CLI lock | invariant | `npm run test && test -z "$(git diff scripts/cli.ts)" && test -z "$(git diff src/core/sampler.ts)"` | ✅ | ✅ green |
    | T05-03-01 | 05-03 | 3 | F6.2 | — | design-token emission | build | `npx electron-vite build && find out/renderer/assets -name 'index-*.css' -exec grep -l "e06b55\|text-danger" {} \;` | ✅ | ✅ green |
    | T05-03-02 | 05-03 | 3 | F6.2 | T-05-03-01, T-05-03-02, T-05-03-04 | XSS-safe React text + Layer 3 boundary + batch-scope regression guard | integration | `npx tsc --noEmit -p tsconfig.web.json && npx electron-vite build && npm run test -- tests/arch.spec.ts` | ✅ | ✅ green |
    | T05-04-01 | 05-04 | 4 | F6.1, F6.2 | T-05-02-03, T-05-02-04 | full suite + locked-file audit | regression | `npm run test && test -z "$(git diff scripts/cli.ts)" && test -z "$(git diff src/core/sampler.ts)"` | ✅ | ✅ green |

    Task IDs use the pattern `T05-{plan}-{task}` — mirrors the Phase 4 T-04-01-01 convention used in the existing arch.spec.ts regression guard comment.

    **Edit 2 — Flip the frontmatter fields:**

    Before:
    ```yaml
    ---
    phase: 5
    slug: unused-attachment-detection
    status: draft
    nyquist_compliant: false
    wave_0_complete: false
    created: 2026-04-24
    ---
    ```

    After:
    ```yaml
    ---
    phase: 5
    slug: unused-attachment-detection
    status: signed-off
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-24
    signed_off: <YYYY-MM-DD>
    ---
    ```

    (Replace `<YYYY-MM-DD>` with the actual date the sign-off happens — use the current date at execution time.)

    **Edit 3 — Also populate the Validation Sign-Off checklist at the bottom of the file:**

    Check ALL six checkboxes and change "Approval: pending" to "Approval: signed-off <YYYY-MM-DD>":

    ```markdown
    ## Validation Sign-Off

    - [x] All tasks have `<automated>` verify or Wave 0 dependencies (planner fills table above)
    - [x] Sampling continuity: no 3 consecutive tasks without automated verify
    - [x] Wave 0 covers all MISSING references (fixtures + spec stubs)
    - [x] No watch-mode flags (vitest `run` mode only, never `watch`)
    - [x] Feedback latency < 20s
    - [x] `nyquist_compliant: true` set in frontmatter

    **Approval:** signed-off <YYYY-MM-DD>
    ```

    **DO NOT** modify the Test Infrastructure table, the Sampling Rate section, the Wave 0 Requirements checklist (convert its `[ ]` to `[x]` — all three items are now satisfied), or the Manual-Only Verifications table.

    Update the Wave 0 Requirements checklist — flip the three items from `[ ]` to `[x]`:

    ```markdown
    ## Wave 0 Requirements

    - [x] `tests/core/usage.spec.ts` — **new** stubs for F6.1 (ghost-def, cross-skin, setup-pose semantics, dim aggregation, non-textured filter)
    - [x] `tests/main/summary.spec.ts` — **extend** (`summary.unusedAttachments` field exists + empty array on SIMPLE_TEST.json)
    - [x] `fixtures/SIMPLE_PROJECT/SIMPLE_TEST_GHOST.json` + `SIMPLE_TEST_GHOST.atlas` — **new** ghost-def fork per RESEARCH.md Finding #4 (12-line JSON diff adding GHOST region to default skin's CIRCLE slot dict; 2-line atlas diff)
    ```

    Note: the second bullet mentions `tests/main/summary.spec.ts` per the VALIDATION.md's original path; the ACTUAL path used by Plan 01 is `tests/core/summary.spec.ts` (correction from 05-PATTERNS.md §"Path correction"). Update the VALIDATION.md line to `tests/core/summary.spec.ts` to match what actually ships.
  </action>
  <verify>
    <automated>grep -q "^nyquist_compliant: true$" .planning/phases/05-unused-attachment-detection/05-VALIDATION.md &amp;&amp; grep -q "^wave_0_complete: true$" .planning/phases/05-unused-attachment-detection/05-VALIDATION.md &amp;&amp; grep -q "^status: signed-off$" .planning/phases/05-unused-attachment-detection/05-VALIDATION.md &amp;&amp; grep -q "T05-01-01" .planning/phases/05-unused-attachment-detection/05-VALIDATION.md &amp;&amp; grep -q "T05-04-01" .planning/phases/05-unused-attachment-detection/05-VALIDATION.md &amp;&amp; test "$(grep -c 'green' .planning/phases/05-unused-attachment-detection/05-VALIDATION.md)" -ge 11</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `status` is `signed-off`.
    - Frontmatter `nyquist_compliant` is `true`.
    - Frontmatter `wave_0_complete` is `true`.
    - Frontmatter has a `signed_off:` date line.
    - Per-task map contains exactly 11 rows with Task IDs T05-01-01 through T05-04-01 including T05-01-03b sanity canary invariant row (pattern allows for executor's judgment on exact row count per task's automated steps, but must be >= 11).
    - All status-column cells contain ✅ (no ⬜ or ❌ remain).
    - Wave 0 Requirements checklist shows three `[x]` items (no `[ ]` remain in that section).
    - Validation Sign-Off checklist shows six `[x]` items.
    - `Approval:` line contains `signed-off` and a date.
    - `tests/core/summary.spec.ts` path referenced correctly (not `tests/main/summary.spec.ts`).
  </acceptance_criteria>
  <done>VALIDATION.md fully populated with per-task map rows, frontmatter flipped to signed-off, Wave 0 + Sign-Off checklists all checked, path correction applied.</done>
</task>

<!-- Phase-closing human gate: Tasks 1+2 (automated sweep + VALIDATION populate) must be green before Task 3 fires. gate='blocking' encodes this semantically; this comment makes the ordering explicit for human reviewers. -->
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Phase 5 end-to-end on SIMPLE_TEST + SIMPLE_TEST_GHOST</name>
  <what-built>
    Phase 5 ships F6.1 (unused-attachment detection in src/core/usage.ts + IPC wiring in src/main/summary.ts) and F6.2 (a conditional warm/terracotta-header section above the peak table on GlobalMaxRenderPanel, inheriting the existing SearchBar filter, with the new --color-danger design token).

    After automated gates pass (Tasks 1–2), this checkpoint verifies that the feature behaves correctly on real drops:
    - Clean rig (SIMPLE_TEST.json) does not render the section.
    - Ghost-def rig (SIMPLE_TEST_GHOST.json) renders the section with one GHOST row.
    - Red scope is header-only.
    - SearchBar filters both the peak table and the unused section consistently.
    - CLI output is byte-for-byte identical to pre-Phase-5.
  </what-built>
  <how-to-verify>
    Run the dev server and drop each fixture in turn.

    **Setup:**
    ```
    npm run dev
    ```
    Wait for the Electron window to appear.

    **Verification 1 — Clean rig (SIMPLE_TEST.json):**
    1. Drag `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` into the app window.
    2. Wait for the peak table to render (CIRCLE, SQUARE, TRIANGLE rows — SQUARE and SQUARE2 fold per Phase 2 gap-fix B).
    3. Confirm: NO unused-attachment section above the peak table. The SearchBar header sits flush above the peak table.
    4. Expected: clean layout, zero red, peak table identical to Phase 4 output.

    **Verification 2 — Ghost-def rig (SIMPLE_TEST_GHOST.json):**
    1. Drag `fixtures/SIMPLE_PROJECT/SIMPLE_TEST_GHOST.json` into the app window.
    2. Wait for the peak table to render.
    3. Confirm: a new `<section>` appears ABOVE the peak table. Content:
       - Header: `⚠ 1 unused attachment` rendered in warm/terracotta red (--color-danger).
       - Body: one row with cells `GHOST | 64×64 | default`.
       - The `×` in the Source Size cell is the multiplication sign (U+00D7), not ASCII `x`.
    4. Visual audit:
       - ONLY the header + glyph + count are red. The row cells are NEUTRAL (text-fg for attachment name, text-fg-muted for source size and defined-in). No red in the row body.
       - The `⚠` glyph renders cleanly in JetBrains Mono. If it renders as a placeholder box or off-baseline, flag for a Phase 5 gap-fix (swap to inline SVG per RESEARCH Pitfall 9).
    5. The layout pushes the peak table down — this is D-106's alarm signal; confirm the peak table renders correctly below the new section.

    **Verification 3 — SearchBar filter interaction (D-107):**
    1. With SIMPLE_TEST_GHOST.json still loaded, type `GHO` in the SearchBar.
    2. Confirm: the peak table shows zero matches (or its standard zero-results message); the unused section's one GHOST row remains visible (matches the substring).
    3. Clear the search field.
    4. Type `CIRC` in the SearchBar.
    5. Confirm: the peak table shows 1 row (CIRCLE); the unused section's header still renders `1 unused attachment` (count tracks filtered list), and the unused section's body shows `(no matches)` placeholder (Pitfall 6 chrome-visible policy).
    6. Clear the search field.
    7. Confirm: both sections return to their full state (unused section = 1 GHOST row, peak table = all rows).

    **Verification 4 — Return to clean state:**
    1. Drag `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` back into the window.
    2. Confirm: unused section disappears entirely; peak table renders without the section above it (layout returns to the pre-GHOST state).

    **Verification 5 — CLI byte-for-byte (D-102):**
    1. In a terminal:
       ```
       npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json > /tmp/cli-phase5-final.txt
       ```
    2. Compare against the Plan 02 baseline:
       ```
       diff /tmp/cli-phase5-plan02-baseline.txt /tmp/cli-phase5-final.txt
       ```
    3. Expected: `diff` produces no output (files identical).
    4. Alternative (if the Plan 02 baseline is no longer available): confirm the CLI output is the same as the pre-Phase-5 Phase 4 state by visual inspection against the Phase 4 SUMMARY's recorded output. The CLI MUST NOT mention any "unused" information (D-102 lock).

    **Verification 6 — Arch gates live run (sanity):**
    1. In a terminal:
       ```
       npm run test -- tests/arch.spec.ts
       ```
    2. Expected: 8/8 green. Batch-scope regression guard (05-CONTEXT.md notes that Phase 5 panel changes MUST preserve the selectedKeys={selectedAttachmentNames} contract) passes.
  </how-to-verify>
  <resume-signal>
    Respond with one of:

    - `approved` — all 6 verifications pass; Phase 5 is shipped.
    - `gap: <short description>` — one or more verifications failed or surfaced an issue; describe the symptom. Expected gap-fix patterns include:
      - `gap: U+26A0 glyph renders poorly in JetBrains Mono` → swap to inline SVG per RESEARCH Pitfall 9.
      - `gap: red hue clashes with Phase 4 orange-accent under a specific lighting condition` → re-pick hex from RESEARCH Finding #7 alternatives (e.g., #dc5a43 for more orange-distinction).
      - `gap: SearchBar filter behavior doesn't match expectation` → Pitfall 6 chrome-visible policy may need adjustment.
      - `gap: CLI diff is non-empty` → CRITICAL D-102 violation; investigate which file touched cli.ts.
    - `gap: other — <freeform>` for unexpected issues.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev environment → production .dmg | build-time transformation; checkpoint verifies dev behavior is faithful |
| fixture JSON → loader | SIMPLE_TEST_GHOST.json is test data; the human-verify confirms its path through the full pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Repudiation | CLI lock (D-102) | mitigate | Task 1 + Task 3 both check `diff` against the Plan 02 baseline; Task 2 records the empty-diff outcome in VALIDATION.md |
| T-05-04-02 | Tampering | Arch boundary regression | mitigate | Task 1 runs `tests/arch.spec.ts` (8/8 green required); Task 3 re-runs it live before sign-off |
| T-05-04-03 | Denial of Service | Build pipeline | accept | `npx electron-vite build` is the existing Phase 1 build target; Phase 5 adds no new build concerns |
</threat_model>

<verification>
Phase 5 Plan 04 verification:

1. **Automated sweep** — Task 1 runs full test suite + typecheck + build + locked-file audit; all exit 0.
2. **Documentation** — Task 2 populates 05-VALIDATION.md with 10+ per-task rows and flips frontmatter to signed-off.
3. **Human sign-off** — Task 3 confirms via 6 in-app verifications that F6.1 + F6.2 behave as designed on real drops, with explicit gap-fix paths for the two most likely failure modes (glyph rendering + hex tuning).
4. **No regressions** — `git diff scripts/cli.ts`, `git diff src/core/sampler.ts`, `git diff src/core/loader.ts`, `git diff src/core/bounds.ts`, `git diff src/core/bones.ts` all empty.
5. **Arch invariants** — 8/8 arch describes green in both the automated suite and the live human-verify run.
</verification>

<success_criteria>
- Full test suite green (>= 120 passed + 1 skipped).
- Arch gates 8/8 green.
- `git diff scripts/cli.ts` empty; `git diff src/core/sampler.ts` empty; three other core/*.ts files also empty diff.
- CLI output byte-for-byte identical to Plan 02 baseline.
- `npx electron-vite build` succeeds; emitted CSS contains the new danger token.
- 05-VALIDATION.md fully populated + frontmatter signed-off.
- Human-verify checkpoint signed off `approved` (or gap-fix loop closed).
- Phase 5 ready for `/gsd-verify-work 5`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-unused-attachment-detection/05-04-SUMMARY.md` documenting:
- Full test suite pass count before vs after (baseline 116+1 → final ~125+1 expected).
- Arch gate outcome (8/8 describes).
- CLI diff outcome (empty diff confirmed).
- Locked-file audit outcome (all five files clean).
- Build outcome (bytes of emitted CSS; presence of #e06b55 or .text-danger).
- VALIDATION.md final state summary (frontmatter signed-off; per-task map row count).
- Human-verify outcome: `approved` timestamp OR the gap-fix loop log + final `approved` timestamp.
- Any deviations from the plan under the Deviations header.
- Commit log (expected: one or two commits — `docs(05-unused): populate VALIDATION.md per-task map + signed-off frontmatter` and, if human-verify produces gap-fixes, one or more `fix(05-unused): <gap>` commits).
- Update STATE.md to mark Phase 5 COMPLETE with the shipping-commit SHA and a one-paragraph outcome summary.
</output>
