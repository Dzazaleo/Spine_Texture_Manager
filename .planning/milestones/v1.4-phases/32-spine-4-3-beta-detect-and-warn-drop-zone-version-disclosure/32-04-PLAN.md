---
phase: 32
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/seeds/SEED-006-spine-4.3-runtime-port.md
  - .planning/seeds/SEED-003-spine-4.3-compatibility.md
autonomous: true
requirements: []
tags:
  - planning
  - seed
  - spine-4.3
  - docs
seed_plant: true

must_haves:
  truths:
    - "A new file `.planning/seeds/SEED-006-spine-4.3-runtime-port.md` exists with the SEED-003-mirroring frontmatter shape (id, status: planted, planted, planted_during, trigger_when, scope, proposed_phase) and the costed-inventory body (PORT-01..04 from REQUIREMENTS.md L33-36 verbatim)."
    - "The frontmatter `trigger_when:` clause references `npm view @esotericsoftware/spine-core@latest` returning 4.3.x AND/OR a paying user reporting they cannot re-export their rig as Version 4.2 (REQUIREMENTS.md L38 verbatim)."
    - "The body cross-links to SEED-003 (the schema-delta groundwork) and to this phase's CONTEXT.md (which closed Option A)."
    - "The existing `.planning/seeds/SEED-003-spine-4.3-compatibility.md` is updated with a one-line addendum noting 'Option A landed in v1.4 Phase 32; full port queued via SEED-006.' — the existing SEED-003 body, frontmatter, and source links stay byte-stable except for that single insertion."
  artifacts:
    - path: ".planning/seeds/SEED-006-spine-4.3-runtime-port.md"
      provides: "SEED for the full Spine 4.3 runtime port — costed inventory, trigger condition, scope, sources"
      contains: "SEED-006"
    - path: ".planning/seeds/SEED-003-spine-4.3-compatibility.md"
      provides: "Existing SEED-003 with a Phase 32 addendum noting Option A landed and pointing forward to SEED-006"
      contains: "SEED-006"
  key_links:
    - from: "SEED-006"
      to: "SEED-003"
      via: "explicit cross-link in body — 'See SEED-003 for the schema-delta groundwork that landed in v1.4 Phase 32 (Option A — detect-and-warn).'"
      pattern: "SEED-003"
    - from: "SEED-003 (existing)"
      to: "SEED-006"
      via: "one-line addendum near the bottom of the file"
      pattern: "SEED-006"
    - from: "SEED-006 trigger_when"
      to: "REQUIREMENTS.md L38"
      via: "verbatim copy of the trigger-condition wording"
      pattern: "npm view @esotericsoftware/spine-core@latest"
---

<objective>
Phase 32 close-of-phase deliverable (NOT a REQ). Plant `SEED-006: Full Spine 4.3 runtime port` under `.planning/seeds/` carrying the costed inventory from this milestone's investigation: 5 sampler renames + 2 bounds signature changes + `slot.pose` access pattern + slider-constraint validation strategy + vendoring strategy. The seed mirrors `SEED-003` shape verbatim (frontmatter fields + section headers + tone). Add a one-line addendum to SEED-003 noting that Option A landed in this phase and pointing forward to SEED-006.

Purpose: capture the post-stable port cost-inventory in a discoverable, structured artifact so the work is queueable behind a clear external trigger (`npm view @esotericsoftware/spine-core@latest` returning 4.3.x OR a paying user reporting they cannot re-export). SEED-001 -> Phase 21, SEED-002 -> Phase 22, SEED-003 -> Phase 32 Option A; the seed-file convention is well-established.

Output: one new file (SEED-006) + one minor addendum to SEED-003. No code changes. No test changes. Pure planning-doc work — `requirements: []` because this is a phase deliverable, not a REQ.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-CONTEXT.md
@.planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-PATTERNS.md
@.planning/seeds/SEED-001-atlas-less-mode.md
@.planning/seeds/SEED-002-dims-badge-override-cap.md
@.planning/seeds/SEED-003-spine-4.3-compatibility.md
@.planning/seeds/SEED-004-rotated-atlas-regions.md
@.planning/seeds/SEED-005-rgba2-inherittimeline-coverage.md

<source_excerpts>
Source-of-truth excerpts to copy verbatim into SEED-006:

From REQUIREMENTS.md lines 33-36 (PORT-01..04 — costed inventory; copy VERBATIM):
- PORT-01: Migrate `core/sampler.ts` from spine-core 4.2 to 4.3 (`setToSetupPose` -> `setupPose`; `setSlotsToSetupPose` -> `setupPoseSlots`; `state.setAnimationWith` -> `state.setAnimation`; `slot.getAttachment()` -> `slot.pose.attachment`).
- PORT-02: Migrate `core/bounds.ts` `computeWorldVertices` call sites to the 4.3 signatures (`RegionAttachment`: adds `vertexOffsets`; `VertexAttachment`: adds `skeleton` first arg).
- PORT-03: Validate `slider` constraint timelines sample correctly via the existing `updateWorldTransform` propagation path with a dedicated fixture.
- PORT-04: Decide vendoring strategy (git submodule + tsc, npm fork, or wait-for-publish) and publish a build pipeline change.

From REQUIREMENTS.md line 38 (trigger condition — copy VERBATIM): `npm view @esotericsoftware/spine-core@latest` returns 4.3.x (i.e. 4.3.0 stable shipped + npm publish landed) OR a paying user reports they cannot re-export their rig as Version 4.2.

From SEED-003 lines 70-79 (sources — re-cite VERBATIM into SEED-006 § Sources): Spine Changelog; Blog: 4.3 beta announcement; spine-runtimes 4.3-beta CHANGELOG.md; 4.3-beta SkeletonJson.ts (raw); 4.2 SkeletonJson.ts (raw); Spine-Unity 4.2 -> 4.3 Upgrade Guide; spine-editor#891; Spine JSON format docs.

From SEED-003 lines 1-9 (frontmatter shape — mirror, but populate values for SEED-006):
- id: SEED-003 -> SEED-006
- status: planted (preserve)
- planted: 2026-05-06 -> 2026-05-10
- planted_during: post-v1.2 exploration ... -> v1.4 Phase 32 (Spine 4.3-beta detect-and-warn close-out)
- trigger_when: ... -> verbatim from REQUIREMENTS.md L38 (see above)
- scope: A=Small / B=Medium / C=Large -> Large (full runtime port — sampler + bounds + new constraint type + vendoring decision)
- proposed_phase: 29 (if Option A only) ... -> TBD (post-4.3.0-stable npm publish)
</source_excerpts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .planning/seeds/SEED-006-spine-4.3-runtime-port.md mirroring SEED-003 shape</name>

  <files>
    - .planning/seeds/SEED-006-spine-4.3-runtime-port.md (NEW)
  </files>

  <read_first>
    - .planning/seeds/SEED-003-spine-4.3-compatibility.md (full file — frontmatter shape + section headers + tone; this is the verbatim mirror precedent per CONTEXT D-07)
    - .planning/REQUIREMENTS.md (lines 31-38 — PORT-01..04 inventory + trigger condition; copy VERBATIM)
    - .planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-CONTEXT.md (D-07 — content-source instructions; "Plant at phase close — last commit in the execute sequence, separate from REQ-implementing commits")
    - .planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-PATTERNS.md (section ".planning/seeds/SEED-006-spine-4.3-runtime-port.md" — frontmatter target shape + section-header pattern)
    - .planning/seeds/SEED-001-atlas-less-mode.md (additional shape reference — companion seed file format)
    - .planning/seeds/SEED-004-rotated-atlas-regions.md (additional shape reference — recent seed format)
  </read_first>

  <action>
Create `.planning/seeds/SEED-006-spine-4.3-runtime-port.md` using the Write tool. Use the exact content below verbatim, with ONE substitution: every line marked `%%% (executor: type three literal dashes here — ...)` must be replaced by a single line containing exactly three dashes (`---`) on its own. The sentinels exist only because this PLAN.md's own frontmatter parser would otherwise mis-detect the embedded YAML; the SEED-006 file MUST contain real `---` delimiters around its frontmatter so future tooling can parse it.

FILE START -- use Write tool, paste verbatim (with the two `%%%` substitutions applied):

```
%%% (executor: type three literal dashes here — these are the YAML frontmatter open delimiter)
id: SEED-006
status: planted
planted: 2026-05-10
planted_during: v1.4 Phase 32 (Spine 4.3-beta detect-and-warn close-out)
trigger_when: "`npm view @esotericsoftware/spine-core@latest` returns 4.3.x (i.e. 4.3.0 stable shipped + npm publish landed) OR a paying user reports they cannot re-export their rig as Version 4.2"
scope: Large (full runtime port — sampler + bounds + new constraint type + vendoring decision)
proposed_phase: TBD (post-4.3.0-stable npm publish)
%%% (executor: type three literal dashes here — these are the YAML frontmatter close delimiter)

# SEED-006: Full Spine 4.3 runtime port

## Why This Matters

Phase 32 (v1.4) shipped the detect-and-warn path (Option A from SEED-003): a 4.3-beta JSON now produces a clear "Re-export from your 4.3 editor as Version 4.2 (supported downgrade) and try again" message instead of the misleading `IK Constraint not found:` symptom. That unblocks the user-facing failure mode but does NOT add 4.3 support — the app still requires 4.2-shaped JSON. Once Spine 4.3.0 stable ships and `@esotericsoftware/spine-core@4.3.x` lands on npm (currently latest is 4.2.x — no 4.3 tag), the full port becomes worthwhile: the schema is frozen, the runtime API is stable, and porting effort isn't burned on mid-beta drift (the `uniform: bool` -> `scaleY: number` rename at 4.3.73-beta is the cautionary precedent).

This seed captures the costed inventory the v1.4 milestone investigation produced, so the future port phase has a queue-ready scoping document.

## Costed Inventory (verbatim from REQUIREMENTS.md -> Future Requirements)

- **PORT-01**: Migrate `core/sampler.ts` from spine-core 4.2 to 4.3 (`setToSetupPose` -> `setupPose`; `setSlotsToSetupPose` -> `setupPoseSlots`; `state.setAnimationWith` -> `state.setAnimation`; `slot.getAttachment()` -> `slot.pose.attachment`).
- **PORT-02**: Migrate `core/bounds.ts` `computeWorldVertices` call sites to the 4.3 signatures (`RegionAttachment`: adds `vertexOffsets`; `VertexAttachment`: adds `skeleton` first arg).
- **PORT-03**: Validate `slider` constraint timelines sample correctly via the existing `updateWorldTransform` propagation path with a dedicated fixture.
- **PORT-04**: Decide vendoring strategy (git submodule + tsc, npm fork, or wait-for-publish) and publish a build pipeline change.

## Schema + Runtime Deltas (full inventory from the v1.4 investigation)

### 5 sampler renames (PORT-01)

| 4.2 API | 4.3 API | Affected sites |
|---|---|---|
| `Skeleton.setToSetupPose()` | `Skeleton.setupPose()` | `core/sampler.ts` |
| `Skeleton.setSlotsToSetupPose()` | `Skeleton.setupPoseSlots()` | `core/sampler.ts` |
| `AnimationState.setAnimationWith(trackIndex, animation, loop)` | `AnimationState.setAnimation(trackIndex, animation, loop)` | `core/sampler.ts` |
| `slot.getAttachment()` | `slot.pose.attachment` (property access via Pose API) | `core/sampler.ts` (visibility-pass loops) |
| `state.update(dt)` / `state.apply(skeleton)` lifecycle | likely-unchanged shape but new `Pose`/`Posed`/`PosedActive`/`BonePose`/`IkConstraintPose` types thread through; verify against final 4.3.0 SkeletonJson.ts | `core/sampler.ts` |

### 2 bounds signature changes (PORT-02)

| 4.2 signature | 4.3 signature | Affected sites |
|---|---|---|
| `regionAttachment.computeWorldVertices(slot, worldVertices, offset, stride)` | `regionAttachment.computeWorldVertices(slot, worldVertices, offset, stride, vertexOffsets)` | `core/bounds.ts` |
| `vertexAttachment.computeWorldVertices(slot, start, count, worldVertices, offset, stride)` | `vertexAttachment.computeWorldVertices(skeleton, slot, start, count, worldVertices, offset, stride)` (adds `skeleton` first arg) | `core/bounds.ts` |

### slot.pose access pattern (PORT-01 detail)

4.2's `slot.getAttachment()` becomes a property read in 4.3: `slot.pose.attachment`. Visibility tests in `core/sampler.ts` (Pass 1 + the skin-manifest pass added in v1.3 per memory `project_sampler_visibility_invariant.md`) read `slot.getAttachment()` to determine whether an attachment is bound at sample-time. The 4.3 port must thread Pose access through every call site that reads attachment-binding state.

### Slider constraint timeline validation (PORT-03)

`slider` is a NEW constraint type in 4.3 with `SliderTimeline` and `SliderMixTimeline`. There is no 4.2 analog; the detect-and-warn path in v1.4 Phase 32 explicitly does NOT translate it (Out of Scope per REQUIREMENTS.md L51). The full-port phase needs a dedicated fixture exercising slider-driven bone movement and asserting that `updateWorldTransform` propagates the slider's effect into the world matrix the same way IK / Transform / Path / Physics do. If the propagation works for free (via the existing `Physics.update` pass), PORT-03 is a fixture-only task; if not, the sampler needs a slider-specific apply step.

### Vendoring strategy (PORT-04)

Three options, in cost order:

| Option | Effort | Pros | Cons |
|---|---|---|---|
| **a. Wait for `@esotericsoftware/spine-core@4.3.x` npm publish** | 0 LOC | One-line `package.json` bump | Time-bound on Esoteric's npm release cadence (4.3.0-stable -> npm tag could lag months) |
| **b. Git submodule on `spine-runtimes/spine-ts/spine-core` + tsc-via-build-script** | ~1 day setup | Pin to any commit, including post-stable-tag pre-npm | Build-time tsc dependency; submodule-update discipline |
| **c. Maintain a fork on npm** | ~2-3 days setup + ongoing maintenance | Easiest CI; works around upstream-publish gaps | Maintenance burden; fork drift |

Recommendation: default to (a) once the trigger fires; fall back to (b) only if the npm publish lags >3 months past the 4.3.0 stable tag.

## Trigger Condition

`npm view @esotericsoftware/spine-core@latest` returns 4.3.x (i.e. 4.3.0 stable shipped + npm publish landed) OR a paying user reports they cannot re-export their rig as Version 4.2.

## Cross-Links

- **SEED-003** (`.planning/seeds/SEED-003-spine-4.3-compatibility.md`) — the schema-delta groundwork. Documents WHY 4.2 + 4.3 JSON breaks (constraint section unification: `root.ik`/`root.transform`/`root.path`/`root.physics` -> `root.constraints[]`), provides the reproduction fixture inventory, costs Options A/B/C. Phase 32 closed Option A; this seed queues Option C.
- **Phase 32 CONTEXT.md** (`.planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-CONTEXT.md`) — the implementation closure for Option A. Documents the predicate-pair pattern, the byte-locked COMPAT-01 message, the SPINE_4_3_TEST fixture shape.
- **REQUIREMENTS.md -> Future Requirements** (PORT-01..04 + the trigger clause) — the formal carry-forward of the costed inventory.

## Recommendation

1. **Do nothing today.** The 4.3 schema is frozen but `@esotericsoftware/spine-core@4.3.x` has not landed on npm. Porting against the 4.3-beta tarball would burn engineering hours that get rewritten when stable + npm publish converge.
2. **Trigger the port phase** (a) the moment `npm view @esotericsoftware/spine-core@latest` reports 4.3.x, OR (b) the moment a paying user reports they cannot re-export as 4.2 (e.g. their pipeline has 4.3-only assets — `slider` constraints, mid-beta `scaleY` IK rigs — that the editor's "Version: 4.2" downgrade can't preserve).
3. **Plan as 4-task phase**: Task 1 PORT-04 (vendoring landed first), Task 2 PORT-01 (sampler renames), Task 3 PORT-02 (bounds signature changes), Task 4 PORT-03 (slider fixture + validation). Each task lands its own commit; CI matrix expands to test 4.3.x alongside 4.2.x for one milestone before deprecating 4.2.

## Sources (verified during the v1.4 milestone investigation; re-cited verbatim from SEED-003)

- [Spine Changelog](https://esotericsoftware.com/spine-changelog) — 4.3-beta release timeline and per-build notes
- [Blog: 4.3 beta announcement (April 4, 2025)](https://en.esotericsoftware.com/blog/The-4.3-beta-is-now-available)
- [spine-runtimes 4.3-beta CHANGELOG.md](https://raw.githubusercontent.com/EsotericSoftware/spine-runtimes/4.3-beta/CHANGELOG.md) — confirms unified `ConstraintData` list, `Slider`/`SliderData`/`SliderTimeline`/`SliderMixTimeline`, IK `scaleY` replaces `uniform`
- [4.3-beta SkeletonJson.ts (raw)](https://raw.githubusercontent.com/EsotericSoftware/spine-runtimes/4.3-beta/spine-ts/spine-core/src/SkeletonJson.ts) — verified unified `root.constraints` array with `type` discriminator
- [4.2 SkeletonJson.ts (raw)](https://raw.githubusercontent.com/EsotericSoftware/spine-runtimes/4.2/spine-ts/spine-core/src/SkeletonJson.ts) — verified four separate `root.ik`/`root.transform`/`root.path`/`root.physics` arrays
- [Spine-Unity 4.2 -> 4.3 Upgrade Guide (forum)](https://esotericsoftware.com/forum/d/29234-spine-unity-42-to-43-upgrade-guide)
- [spine-editor#891 — IK timeline scrambling on 4.3->4.2 downgrade](https://github.com/EsotericSoftware/spine-editor/issues/891)
- [Spine JSON format docs](https://en.esotericsoftware.com/spine-json-format)
```

FILE END.

Notes for the executor:
- Use the Write tool. Do NOT use heredoc (`cat << 'EOF'`).
- Pay attention to the YAML frontmatter — `trigger_when:` is wrapped in double quotes because the value contains backticks; YAML accepts this inline.
- Section headers mirror SEED-003 closely. Tone is technical, concrete, source-cited — same as SEED-003.
- The "5 sampler renames + 2 bounds signature changes + slot.pose access + slider validate + vendoring" inventory ROADMAP.md L794 mandated is fully captured.
  </action>

  <verify>
    <automated>test -f .planning/seeds/SEED-006-spine-4.3-runtime-port.md && grep -q "id: SEED-006" .planning/seeds/SEED-006-spine-4.3-runtime-port.md && grep -q "trigger_when:" .planning/seeds/SEED-006-spine-4.3-runtime-port.md && grep -q "PORT-01" .planning/seeds/SEED-006-spine-4.3-runtime-port.md && grep -q "PORT-04" .planning/seeds/SEED-006-spine-4.3-runtime-port.md && grep -q "supported downgrade" .planning/seeds/SEED-006-spine-4.3-runtime-port.md</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/seeds/SEED-006-spine-4.3-runtime-port.md` exits 0
    - `grep -c "^id: SEED-006$" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns `1` (frontmatter id)
    - `grep -c "^status: planted$" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns `1`
    - `grep -c "^planted: 2026-05-10$" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns `1`
    - `grep -c "^trigger_when:" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns `1`
    - `grep -c "^proposed_phase:" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns `1`
    - `grep -c "npm view @esotericsoftware/spine-core@latest" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `1`
    - `grep -c "PORT-01" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `1`
    - `grep -c "PORT-02" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `1`
    - `grep -c "PORT-03" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `1`
    - `grep -c "PORT-04" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `1`
    - `grep -c "setToSetupPose" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `1`
    - `grep -c "vertexOffsets" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `1`
    - `grep -c "slot.pose" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `1`
    - `grep -c "slider" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `2`
    - `grep -c "vendoring" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `2`
    - `grep -c "SEED-003" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns at least `2`
    - `grep -c "spine-runtimes/4.3-beta/CHANGELOG.md" .planning/seeds/SEED-006-spine-4.3-runtime-port.md` returns `1`
    - The frontmatter is valid YAML — verifiable by reading the first 9 lines and confirming the `---` delimiters and key-value structure.
  </acceptance_criteria>

  <done>
    `.planning/seeds/SEED-006-spine-4.3-runtime-port.md` exists with the full SEED-003-mirroring shape: frontmatter (id, status, planted, planted_during, trigger_when, scope, proposed_phase) + body (Why This Matters, Costed Inventory PORT-01..04, Schema+Runtime Deltas, Trigger Condition, Cross-Links, Recommendation, Sources). The "5 sampler renames + 2 bounds signature changes + slot.pose access + slider validate + vendoring strategy" inventory ROADMAP L794 mandated is fully captured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 32 close-out addendum to SEED-003</name>

  <files>
    - .planning/seeds/SEED-003-spine-4.3-compatibility.md
  </files>

  <read_first>
    - .planning/seeds/SEED-003-spine-4.3-compatibility.md (full file — frontmatter at lines 1-9, body at lines 11-79; the `## Recommendation` section at lines 60-64 is the natural anchor for the Phase 32 addendum per CONTEXT D-07)
    - .planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-CONTEXT.md (D-07 — "SEED-003 itself gets a one-line addendum noting Option A landed in v1.4 Phase 32; full port queued via SEED-006.")
  </read_first>

  <action>
Add a Phase 32 close-out addendum to `.planning/seeds/SEED-003-spine-4.3-compatibility.md` immediately AFTER the `## Recommendation` numbered list (currently ends at line 64 with the "3. Skip Option B …" item) and BEFORE the `## Open Question (parked for next conversation)` section header at line 66.

Use the Edit tool to perform the insertion. Find the exact anchor:

OLD STRING (find verbatim — this is the last line of the `## Recommendation` numbered list):
`3. **Skip Option B** unless a paying user explicitly requests "load 4.3 files now without re-exporting" — the maintenance burden against a moving beta target is a known trap.`

NEW STRING (replace with the same line + a blank line + the new section + a blank line):

```
3. **Skip Option B** unless a paying user explicitly requests "load 4.3 files now without re-exporting" — the maintenance burden against a moving beta target is a known trap.

## Status (2026-05-10) — Option A landed; Option C queued via SEED-006

- **Option A (detect-and-warn) landed in v1.4 Phase 32.** The loader now throws `SpineVersionUnsupportedError` with the actionable message *"This app currently supports Spine v4.2. Re-export from your 4.3 editor as Version 4.2 (supported downgrade) and try again."* on either of the two detection signals (`skeleton.spine` semver `>= 4.3` OR top-level `constraints` array present). The drop-zone advisory at `App.tsx:622` calls out the v4.2-only support before drop. Cryptic `IK Constraint not found:` is no longer reachable for 4.3-beta JSONs.
- **Option C (full 4.3 runtime port) queued via SEED-006.** See `.planning/seeds/SEED-006-spine-4.3-runtime-port.md` for the costed inventory: 5 sampler renames + 2 bounds signature changes + `slot.pose` access pattern + slider-constraint validation strategy + vendoring strategy. Trigger: `npm view @esotericsoftware/spine-core@latest` returns 4.3.x OR a paying user reports they cannot re-export as Version 4.2.
- **Option B (schema-shim) remains skipped** — Out of Scope in v1.4 per REQUIREMENTS.md L50 (HIGH trap risk; doesn't model `slider`; brittle against beta drift).
```

The rest of SEED-003 (frontmatter, body sections 1-7, Open Question at lines 66-68, Sources at lines 70-79) MUST stay byte-stable. Do NOT update the frontmatter `status: planted` field — Plan 04 is an addendum, not a status change; SEED-003 stays planted until Option C closes too.

Diff scope verification: the addendum should land as a new section between `## Recommendation` and `## Open Question (parked for next conversation)`. No other lines change.
  </action>

  <verify>
    <automated>grep -q "Status (2026-05-10) — Option A landed" .planning/seeds/SEED-003-spine-4.3-compatibility.md && grep -q "SEED-006" .planning/seeds/SEED-003-spine-4.3-compatibility.md && grep -q "## Open Question" .planning/seeds/SEED-003-spine-4.3-compatibility.md && grep -q "## Recommendation" .planning/seeds/SEED-003-spine-4.3-compatibility.md && grep -q "## Sources (verified during research)" .planning/seeds/SEED-003-spine-4.3-compatibility.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Status (2026-05-10) — Option A landed" .planning/seeds/SEED-003-spine-4.3-compatibility.md` returns `1` (the new section header)
    - `grep -c "SEED-006" .planning/seeds/SEED-003-spine-4.3-compatibility.md` returns at least `2` (cross-link in the new section header + body reference)
    - `grep -c "## Recommendation" .planning/seeds/SEED-003-spine-4.3-compatibility.md` returns `1` (existing section preserved byte-stable)
    - `grep -c "## Open Question (parked for next conversation)" .planning/seeds/SEED-003-spine-4.3-compatibility.md` returns `1` (existing section preserved byte-stable)
    - `grep -c "## Sources (verified during research)" .planning/seeds/SEED-003-spine-4.3-compatibility.md` returns `1` (existing section preserved byte-stable)
    - `grep -c "id: SEED-003" .planning/seeds/SEED-003-spine-4.3-compatibility.md` returns `1` (frontmatter unchanged)
    - `grep -c "status: planted" .planning/seeds/SEED-003-spine-4.3-compatibility.md` returns `1` (status NOT escalated)
    - `grep -c "supported downgrade" .planning/seeds/SEED-003-spine-4.3-compatibility.md` returns at least `1` (COMPAT-01 wording quoted in the new section)
    - `grep -c "App.tsx:622" .planning/seeds/SEED-003-spine-4.3-compatibility.md` returns at least `1` (cross-reference to COMPAT-02 site)
    - `git diff .planning/seeds/SEED-003-spine-4.3-compatibility.md | grep -c "^-" | head -1` returns at most `1` (just the diff `---` header — no removed lines from the existing body)
  </acceptance_criteria>

  <done>
    SEED-003 has a new `## Status (2026-05-10) — Option A landed; Option C queued via SEED-006` section landing between `## Recommendation` and `## Open Question`. The new section documents the Option A close-out (Phase 32) + Option C queue-via-SEED-006 + Option B still skipped. The rest of SEED-003 is byte-stable. Frontmatter `status: planted` unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure planning-doc work — no code, no tests, no runtime artifacts; SEED files are markdown documentation read by humans + future GSD planners |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-13 | Tampering | seed file content drift | accept | SEED files are version-controlled markdown; `git diff` surfaces any unintended drift. Future SEED audits compare against the v1.4 Phase 32 close-out commit. |
| T-32-14 | Information Disclosure | seed body referencing source URLs | accept | All cited URLs are public Esoteric documentation + GitHub issues. No credentials, no internal-only links. Same disposition as the existing SEED-003 sources block. |
| T-32-15 | Repudiation | uncited claim in seed body | mitigate | Every technical claim in the Schema + Runtime Deltas section is cross-referenced to a Sources URL (CHANGELOG.md, raw SkeletonJson.ts, blog post). Future readers can verify each claim independently. |

**Phase-32-specific note (security_enforcement gate):** purely documentation work. No code surface, no runtime path, no IPC, no user input. ASVS does not apply — there is no security-relevant change. The threat_model block exists per phase policy; the actual phase-level security mitigations live in Plan 01 (the predicate-input boundary) and Plan 02 (the static JSX edit).
</threat_model>

<verification>
- `test -f .planning/seeds/SEED-006-spine-4.3-runtime-port.md` exits 0
- `git status --porcelain .planning/seeds/SEED-006-spine-4.3-runtime-port.md` shows `??` (untracked, ready to commit)
- `git status --porcelain .planning/seeds/SEED-003-spine-4.3-compatibility.md` shows ` M` (modified, ready to commit)
- `head -9 .planning/seeds/SEED-006-spine-4.3-runtime-port.md` shows valid YAML frontmatter (`---` delimiters + 7 keys: id, status, planted, planted_during, trigger_when, scope, proposed_phase)
- `npm test --run` exits 0 (no code changes; no test impact — but run anyway as a smoke-test that the planning-doc diff didn't accidentally break a fixture-relative path test that scans .planning/)
- `git diff --stat .planning/seeds/SEED-003-spine-4.3-compatibility.md` shows a small additive diff (no large block deletions; the existing body is preserved byte-stable except for the addendum insertion)
</verification>

<success_criteria>
- `.planning/seeds/SEED-006-spine-4.3-runtime-port.md` exists with the SEED-003-mirroring frontmatter + body shape
- The frontmatter has `id: SEED-006`, `status: planted`, `planted: 2026-05-10`, `planted_during`, `trigger_when`, `scope`, `proposed_phase` keys
- The body has sections: Why This Matters; Costed Inventory; Schema + Runtime Deltas (with sub-sections for sampler renames, bounds signature changes, slot.pose access, slider validation, vendoring strategy); Trigger Condition; Cross-Links; Recommendation; Sources
- PORT-01 through PORT-04 are present verbatim from REQUIREMENTS.md
- The Sources block re-cites the same 8 URLs SEED-003 used
- `.planning/seeds/SEED-003-spine-4.3-compatibility.md` has a new `## Status (2026-05-10) — Option A landed; Option C queued via SEED-006` section between `## Recommendation` and `## Open Question`
- The rest of SEED-003 is byte-stable (frontmatter unchanged; existing body sections preserved)
- No code or test changes; `npm test --run` and `npm run build` continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-04-SUMMARY.md` documenting:
- The new SEED-006 file (path, byte count, frontmatter fields)
- The SEED-003 addendum (line range of the new section, byte-stability of the rest of the file)
- Confirmation that no code / tests / runtime artifacts changed
- Suggested commit message: `docs(seed): plant SEED-006 — full Spine 4.3 runtime port queued`
</output>
