---
phase: 32
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/App.tsx
autonomous: true
requirements:
  - COMPAT-02
tags:
  - renderer
  - ui
  - drop-zone
  - copy-edit
  - tailwind

must_haves:
  truths:
    - "User opens the app with no project loaded; the drop-zone idle copy reads 'Drop a Spine v4.2 .spine JSON file anywhere in this window' with the `v4.2` token rendered in font-bold + text-danger styling."
    - "The supported-version disclosure is visible BEFORE the user drops any file (i.e. it lives inside the `state.status === 'idle' && !isElevated` branch, not gated by any drop interaction)."
    - "The Windows-admin elevated-fallback advisory at App.tsx:603-619 is UNCHANGED — no v4.2 callout added there per UI-SPEC.md Audit Anchors and CONTEXT D-04."
    - "The error banner JSX block at App.tsx:660-697 is UNCHANGED — no className diffs, no JSX node additions/removals (the new COMPAT-01 error string surfaces via `state.error.message` through the existing template — Plan 01 + Plan 03 deliver the new string)."
    - "No new CSS files, no new Tailwind tokens, no new components — pure inline JSX diff at the single locked anchor."
  artifacts:
    - path: "src/renderer/src/App.tsx"
      provides: "Drop-zone idle copy with v4.2 inline `<span>` styled font-bold + text-danger"
      contains: "<span className=\"font-bold text-danger\">v4.2</span>"
  key_links:
    - from: "App.tsx idle drop-zone <p>"
      to: "v4.2 inline <span>"
      via: "single literal Tailwind className 'font-bold text-danger' (Tailwind v4 literal-class discipline; no template strings, no array .join(' '))"
      pattern: "font-bold text-danger"
---

<objective>
COMPAT-02 — restyle the existing `idle && !isElevated` drop-zone copy at `src/renderer/src/App.tsx:621-623` so the supported Spine version is called out explicitly with `v4.2` in `font-bold text-danger` styling. Pairs visually with the COMPAT-01 error message (same `text-danger` token; same "v4.2" wording) so a user reading the error sees the same supported-version contract they read before dropping the file.

Purpose: surface the v4.2-only support BEFORE drop, so users on a 4.3 editor know the constraint up-front and can re-export-as-Version-4.2 without ever triggering the COMPAT-01 error path.

Output: a single inline JSX edit at App.tsx:621-623. No new components. No new CSS files. No new Tailwind tokens. The error-banner block + the elevated-fallback advisory stay byte-stable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-CONTEXT.md
@.planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-PATTERNS.md
@.planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-UI-SPEC.md
@src/renderer/src/App.tsx

<interfaces>
<!-- Existing inline-span color-token precedents in App.tsx (DO NOT modify these — reference only). -->

App.tsx:675 (existing pattern — `font-semibold text-danger` for the "Skeleton not found:" prefix):
```tsx
<span className="font-semibold text-danger">Skeleton not found:</span>{' '}
{state.error.message}
```

Phase 32 introduces ONE inline span with a one-step weight bump (`font-bold` instead of `font-semibold`) per UI-SPEC.md typography contract. The `text-danger` color token is reused verbatim (5.33:1 AA contrast on `--color-surface`).

App.tsx:603-619 (UNCHANGED — Windows-admin elevated-fallback branch; UI-SPEC.md Audit Anchor explicitly marks this as "no v4.2 callout added"). Do not touch.

App.tsx:660-697 (UNCHANGED — error banner; UI-SPEC.md Audit Anchor explicitly marks this as "no className diffs, no JSX node additions/removals"). The new COMPAT-01 message surfaces through `state.error.message` at line 676 with zero renderer-side template changes; Plan 01 changes only the core/errors.ts class.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit drop-zone idle copy at App.tsx:621-623 to insert the v4.2 inline span</name>

  <files>
    - src/renderer/src/App.tsx
  </files>

  <read_first>
    - src/renderer/src/App.tsx (lines 596-700 — covers the entire idle-state JSX block at 600-697 plus the elevated branch at 603-619 and the error banner at 660-697; you must see all three to verify which is the modify-target and which two stay byte-stable)
    - .planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-UI-SPEC.md (full file — Audit Anchors table at lines 207-220 enumerates the byte-stable surfaces; Copywriting Contract at lines 119-141 byte-locks the new `<p>` shape)
    - .planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-CONTEXT.md (D-04 — "Minimal in-line insert at App.tsx:622"; Specific Ideas — "v4.2 token style is REQUIREMENTS-locked: font-bold text-danger")
    - .planning/REQUIREMENTS.md (line 14 — COMPAT-02 acceptance: `v4.2` is rendered with `text-danger` token, font-bold weight)
  </read_first>

  <action>
**Locate the exact target block in `src/renderer/src/App.tsx` at lines 620-624:**

The current code is:
```tsx
        ) : (
          <p className="text-fg-muted font-mono text-sm">
            Drop a <code>.spine</code> JSON file anywhere in this window
          </p>
        ))}
```

Replace JUST the `<p>` element (line 621-623, three lines) with:

```tsx
          <p className="text-fg-muted font-mono text-sm">
            Drop a Spine <span className="font-bold text-danger">v4.2</span> <code>.spine</code> JSON file anywhere in this window
          </p>
```

**Token-by-token diff (per UI-SPEC.md Copywriting Contract):**
- `Drop a` → `Drop a Spine` (insert literal ` Spine` before the `<code>` token)
- Insert `<span className="font-bold text-danger">v4.2</span> ` (with a single trailing ASCII space) BEFORE `<code>.spine</code>`
- `<code>.spine</code>` — UNCHANGED
- `JSON file anywhere in this window` — UNCHANGED
- The outer `<p className="text-fg-muted font-mono text-sm">` — UNCHANGED

**Whitespace semantics (UI-SPEC.md spacing contract):**
- Single ASCII space before the `<span>` (between "Spine" and the opening `<`)
- Single ASCII space after the `</span>` closing tag (between the closing `>` and `<code>`)
- Native HTML whitespace collapsing applies; no `mr-1` / `ml-1` utility added

**Tailwind v4 literal-class discipline (Pitfall 8):** the new `<span>`'s `className` is a single string literal `"font-bold text-danger"` — no template literals, no array `.join(' ')`, no conditional concatenation.

**DO NOT touch (UI-SPEC.md Audit Anchors at lines 207-220):**

1. The `idle && isElevated` branch at lines 603-619 (the Windows-admin advisory). It stays byte-stable; COMPAT-02 only locks the `idle && !isElevated` branch.

2. The `state.status === 'projectLoadFailed'` error banner block at lines 660-697. It stays byte-stable. The new COMPAT-01 error message that Plan 01 produces will surface at line 676 (`{state.error.message}`) with zero renderer changes.

3. `src/renderer/src/index.css` (no new `--color-*` tokens, no new utility @apply blocks).

4. `src/renderer/src/modals/HelpDialog.tsx:158` (different surface; explicitly out of scope per CONTEXT D-04).

**Layer-3 invariant note:** this is a renderer-side edit; the Layer-3 invariant (`core/` is pure TS, no DOM) is irrelevant here — App.tsx already imports React. No new imports added.
  </action>

  <verify>
    <automated>npm test -- src/renderer/ tests/renderer/ --run 2>/dev/null; (grep -F '<span className="font-bold text-danger">v4.2</span>' src/renderer/src/App.tsx && grep -F 'Drop a Spine ' src/renderer/src/App.tsx && grep -F '<code>.spine</code>' src/renderer/src/App.tsx) || (echo "FAIL: drop-zone copy not at expected shape" && exit 1)</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '<span className="font-bold text-danger">v4.2</span>' src/renderer/src/App.tsx` returns `1` (exact literal match — no template strings, no class-name array)
    - `grep -c "Drop a Spine " src/renderer/src/App.tsx` returns `1` (the new "Spine" word inserted)
    - `grep -c "<code>.spine</code>" src/renderer/src/App.tsx` returns `1` (preserved verbatim)
    - `grep -c "Drop a <code>.spine</code> JSON file" src/renderer/src/App.tsx` returns `0` (the OLD copy is GONE)
    - `grep -cF 'Drag-and-drop is unavailable while running as administrator. Use File → Open instead, or relaunch the app without administrator privileges.' src/renderer/src/App.tsx` returns `1` (Windows-admin advisory at App.tsx:603-619 is UNCHANGED — same exact full sentence preserved)
    - `grep -c '<span className="font-semibold text-danger">Skeleton not found:</span>' src/renderer/src/App.tsx` returns `1` (error banner prefix at App.tsx:675 UNCHANGED)
    - `grep -c "role=\"alert\"" src/renderer/src/App.tsx` returns at least `1` (error banner role attr UNCHANGED)
    - `grep -c "Locate skeleton…" src/renderer/src/App.tsx` returns `1` (error banner button UNCHANGED)
    - `git diff --stat src/renderer/src/index.css | wc -l` returns `0` (no CSS changes)
    - `git diff --stat src/renderer/src/modals/HelpDialog.tsx | wc -l` returns `0` (HelpDialog unchanged)
    - `npm run build` exits 0 (TypeScript + Vite renderer compile clean)
    - The diff in src/renderer/src/App.tsx is contained to the lines around the drop-zone idle copy — `git diff src/renderer/src/App.tsx | grep -E '^[+-]' | grep -v -E '^(\\+\\+\\+|---)' | wc -l` returns no more than `5` lines (1 line removed, up to 1 line added; the line count includes both diff markers)
  </acceptance_criteria>

  <done>
    Single-line JSX edit in App.tsx lands. The drop-zone idle copy reads "Drop a Spine **v4.2** `.spine` JSON file anywhere in this window" with v4.2 in font-bold + text-danger. Windows-admin elevated branch + error banner + HelpDialog + index.css are all byte-stable. `npm run build` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer JSX -> DOM | inline static text + an inline static span; no user-controlled content |
| n/a | no IPC, no fs, no network, no eval — purely static text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Tampering | static drop-zone copy | accept | Copy is hard-coded literal text. No user-input substitution, no `dangerouslySetInnerHTML`, no template-string interpolation of dynamic data. The `<span>` and `<code>` are React JSX nodes, not raw HTML strings. |
| T-32-07 | Information Disclosure | static "v4.2" text | accept | The string `v4.2` is product documentation, not sensitive data. Same disposition as the existing `<code>.spine</code>` token. |
| T-32-08 | Denial of Service | rendered DOM size | accept | One additional inline `<span>` per render. Negligible memory + paint cost. The drop-zone is the idle screen; not a hot path. |

**Phase-32-specific note (security_enforcement gate):** purely static JSX edit. No new user-input handling, no new IPC channels, no new event handlers, no new external dependencies. ASVS L1 V5.3 (output encoding): React's default JSX text-node escaping applies; no unsafe HTML primitives introduced. No new attack surface.
</threat_model>

<verification>
- `npm run build` exits 0 (TypeScript + Vite renderer build green)
- `npm test --run` exits 0 (no test regressions; Plan 02 doesn't touch any test files; existing renderer tests if any continue to pass)
- Manual visual verification (deferred to phase-close UAT): on `npm run dev`, the idle drop-zone shows "Drop a Spine **v4.2** `.spine` JSON file anywhere in this window" with `v4.2` rendered in bold terracotta (#e06b55). The Windows-admin advisory branch (when `isElevated === true`) shows the original copy unchanged. The error banner (when `state.status === 'projectLoadFailed'`) renders unchanged.
- UI-SPEC.md Audit Anchors table at lines 207-220: every UNCHANGED row remains UNCHANGED — verifiable with the grep checks in `<acceptance_criteria>`.
</verification>

<success_criteria>
- App.tsx:621-623 (the `idle && !isElevated` branch's `<p>`) reads exactly: `Drop a Spine <span className="font-bold text-danger">v4.2</span> <code>.spine</code> JSON file anywhere in this window`
- App.tsx:603-619 (the `idle && isElevated` Windows-admin branch) is byte-stable
- App.tsx:660-697 (the `state.status === 'projectLoadFailed'` error banner) is byte-stable
- src/renderer/src/index.css is unchanged (no new tokens)
- src/renderer/src/modals/HelpDialog.tsx is unchanged (different surface, out of scope)
- No new components in src/renderer/src/components/
- `npm run build` is green (TypeScript + Vite)
</success_criteria>

<output>
After completion, create `.planning/phases/32-spine-4-3-beta-detect-and-warn-drop-zone-version-disclosure/32-02-SUMMARY.md` documenting:
- The exact line range modified in App.tsx
- The before-and-after JSX shape (verbatim, with whitespace preserved)
- Confirmation that the three byte-stable anchors (Windows-admin branch, error banner, HelpDialog) are untouched
- Confirmation that index.css is unchanged
- `npm run build` output trail
</output>
