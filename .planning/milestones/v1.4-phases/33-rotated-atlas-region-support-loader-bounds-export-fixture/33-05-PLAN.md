---
phase: 33
plan: 05
type: execute
wave: 3
depends_on: [33-03]
files_modified:
  - src/main/image-worker.ts
  - tests/main/image-worker-rotation.spec.ts
  - tests/core/export-rotation-dims.spec.ts
autonomous: true
requirements:
  - ATLAS-03
must_haves:
  truths:
    - "src/main/image-worker.ts passthrough atlas-extract path inserts sharp.rotate(90) when row.atlasSource.rotated === true"
    - "src/main/image-worker.ts resize atlas-extract path inserts sharp.rotate(90) when row.atlasSource.rotated === true (in BOTH the SW two-pipeline branch and the non-SW single-pipeline branch)"
    - "Strip-Whitespace + rotation extend() args use post-rotation canonical orientation: sourceCanvasW = a.rotated ? a.packH : a.packW, sourceCanvasH = a.rotated ? a.packW : a.packH"
    - "buildExportPlan emits canonical outW/outH (NOT packed-swapped) for a synthetic rotated row with canonicalW=100, canonicalH=500, peakScale=1.0 → outW=100, outH=500"
    - "Image-worker passthrough produces an output PNG with metadata.width=canonicalW and metadata.height=canonicalH for a rotated input row"
    - "Image-worker resize produces an output PNG with metadata.width=canonicalW * effectiveScale and metadata.height=canonicalH * effectiveScale for a rotated input row"
    - "tests/core/export-rotation-dims.spec.ts is no longer describe.skip — active passing block"
    - "tests/main/image-worker-rotation.spec.ts is no longer describe.skip — passthrough + resize cases pass"
    - "npm test exits 0"
    - "npx tsc --noEmit exits 0"
    - "Sharp rotation direction is +90 (NOT -90) — direction baked into a load-bearing comment per RESEARCH §Sharp Rotation Direction (Empirical)"
  artifacts:
    - path: "src/main/image-worker.ts"
      provides: "atlas-extract pipelines with sharp.rotate(+90) for rotated regions"
      contains: ".rotate(90)"
    - path: "tests/main/image-worker-rotation.spec.ts"
      provides: "ATLAS-03 image-worker passthrough + resize coverage"
      contains: "runExport"
    - path: "tests/core/export-rotation-dims.spec.ts"
      provides: "ATLAS-03 buildExportPlan canonical-dim assertion"
      contains: "buildExportPlan"
  key_links:
    - from: "src/main/image-worker.ts passthrough path"
      to: "row.atlasSource.rotated"
      via: "branch on rotated flag, inject .rotate(90) between extract and extend"
      pattern: "rotate(90)"
    - from: "src/main/image-worker.ts resize path"
      to: "row.atlasSource.rotated"
      via: "branch on rotated flag, inject .rotate(90) on the pre-pipeline; preserve two-pipeline materialize-toBuffer SW pattern"
      pattern: "pre.rotate(90)"
    - from: "tests/main/image-worker-rotation.spec.ts"
      to: "fixtures/spine_rotated/EXPORT/skeleton.png"
      via: "ROTATED_PAGE constant, hand-built ExportPlan row with atlasSource.rotated=true"
      pattern: "ROTATED_PAGE"
---

<objective>
Add sharp.rotate(+90) to BOTH atlas-extract paths in src/main/image-worker.ts (passthrough at lines ~274-298, resize at lines ~380-470 + ~519-541), AND fill in the ATLAS-03 test bodies (un-skipping export-rotation-dims and image-worker-rotation specs from Wave 1). After this plan, ATLAS-03 (ExportPlan output dimensions reflect canonical W×H AND image-worker produces canonical-orientation PNG output) is met.

Sharp rotation direction is **+90** (NOT -90 as CONTEXT D-03 hypothesized). RESEARCH §"Sharp Rotation Direction (Empirical)" empirically falsified the -90 hypothesis using `probe-sharp-rotate.mjs`: `sharp.rotate(+90)` cancels Spine's CCW packing convention. The verified direction MUST be baked into a load-bearing comment so future readers do not re-derive (per memory `feedback_narrow_before_fixing`).

Strip-Whitespace + rotation interaction is non-trivial: the existing `.extend()` reconstitution at image-worker.ts:290-298 (passthrough) and :521-541 (resize two-pipeline) uses `a.packW`/`a.packH` directly. After `.rotate(+90)` the buffer dims are swapped (now `packH × packW`), so `.extend()` args MUST use post-rotation canonical orientation. Use derived locals `sourceCanvasW = a.rotated ? a.packH : a.packW` and `sourceCanvasH = a.rotated ? a.packW : a.packH` in BOTH paths.

Resize path uses the existing **two-pipeline** materialize-toBuffer pattern for SW (per RESEARCH §"Pitfall 4: Sharp pipeline order"). Rotation slots into the `pre` pipeline before `.toBuffer()`, leaving the libvips reordering workaround intact. For non-SW + rotated, single pipeline is fine.

Purpose: ATLAS-03 acceptance criteria. After this plan, exporting per-region PNGs from a rotated atlas project produces canonical-orientation output with canonical W×H.

Output: image-worker.ts updated; both ATLAS-03 test specs un-skipped + passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-CONTEXT.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-PATTERNS.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-01-SUMMARY.md
@.planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-03-SUMMARY.md
@CLAUDE.md
@src/main/image-worker.ts
@src/core/export.ts
@src/shared/types.ts
@tests/main/image-worker.strip-whitespace.spec.ts
@tests/main/image-worker.passthrough.spec.ts
@tests/core/export.spec.ts
@tests/main/image-worker-rotation.spec.ts
@tests/core/export-rotation-dims.spec.ts

<interfaces>
Image-worker passthrough path post-edit form (RESEARCH lines 514-541; replaces the existing block at ~lines 274-298):

```
const a = row.atlasSource!;
let pipeline = sharp(a.pagePath).extract({
  left: a.x, top: a.y, width: a.packW, height: a.packH,
});
if (a.rotated) {
  // Phase 33 D-03 - un-rotate Spine-CCW-packed region back to canonical orientation.
  // Direction VERIFIED EMPIRICALLY in 33-RESEARCH.md (CONTEXT D-03 hypothesis
  // of -90 was falsified): sharp.rotate(+90) cancels CCW packing per libgdx
  // atlas convention. Probe: probe-sharp-rotate.mjs.
  pipeline = pipeline.rotate(90);
}
// Strip-whitespace re-canvas. For rotated regions, the buffer is now packH x packW
// (swapped), so extend args use post-rotation canonical orientation.
const sourceCanvasW = a.rotated ? a.packH : a.packW;
const sourceCanvasH = a.rotated ? a.packW : a.packH;
if (sourceCanvasW !== a.w || sourceCanvasH !== a.h) {
  pipeline = pipeline.extend({
    top:    a.h - a.offsetY - sourceCanvasH,
    bottom: a.offsetY,
    left:   a.offsetX,
    right:  a.w - a.offsetX - sourceCanvasW,
    background: { r: 0, g: 0, b: 0, alpha: 0 },
  });
}
await pipeline.toFile(tmpPath);
```

Image-worker resize path post-edit form (RESEARCH lines 547-572; replaces the existing block at ~lines 519-541, with the typed-error block at ~422-438 already deleted by Plan 03):

```
const a = row.atlasSource!;
let pipeline: sharp.Sharp;
const sourceCanvasW = a.rotated ? a.packH : a.packW;
const sourceCanvasH = a.rotated ? a.packW : a.packH;
if (sourceCanvasW !== a.w || sourceCanvasH !== a.h) {
  // Two-pipeline (SW path): materialize extend output, then re-open.
  // Mirrors existing line 521-533 fix for libvips operation reordering
  // (RESEARCH §Pitfall 4: Sharp pipeline order). Rotation slots into the
  // pre-pipeline before .toBuffer().
  let pre = sharp(a.pagePath).extract({ left: a.x, top: a.y, width: a.packW, height: a.packH });
  if (a.rotated) {
    // Phase 33 D-03 - direction VERIFIED EMPIRICALLY (see passthrough comment).
    pre = pre.rotate(90);
  }
  const orig = await pre
    .extend({
      top:    a.h - a.offsetY - sourceCanvasH,
      bottom: a.offsetY,
      left:   a.offsetX,
      right:  a.w - a.offsetX - sourceCanvasW,
      background: { r: 0, g: 0, b: 0, alpha: 0 },
    })
    .png().toBuffer();
  pipeline = sharp(orig);
} else {
  // No SW - single pipeline.
  pipeline = sharp(a.pagePath).extract({ left: a.x, top: a.y, width: a.packW, height: a.packH });
  if (a.rotated) {
    pipeline = pipeline.rotate(90);
  }
}
await applyResizeAndSharpen(pipeline, row.outW, row.outH, row.effectiveScale, sharpenEnabled).toFile(tmpPath);
```

Test patterns (per 33-PATTERNS.md):
- tests/core/export-rotation-dims.spec.ts mirrors tests/core/export.spec.ts:147-220 (synthetic SkeletonSummary)
- tests/main/image-worker-rotation.spec.ts mirrors tests/main/image-worker.strip-whitespace.spec.ts (hand-built ExportPlan + sharp metadata + raw pixel compare)

ExportPlan canonical-dim verification (no math change required — already canonical-relative at src/core/export.ts:325):
- outW = ceil((canonicalW ?? sourceW) * effScale)
- outH = ceil((canonicalH ?? sourceH) * effScale)
- For rotated rows: canonicalW/H = unrotated source dims; the math flows through unchanged
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sharp.rotate(+90) to image-worker.ts passthrough path</name>
  <files>src/main/image-worker.ts</files>
  <read_first>
    src/main/image-worker.ts (read lines 270-310 — current passthrough atlas-extract path; Plan 03 confirmed line numbers may have shifted slightly due to deletions),
    src/main/image-worker.ts (read lines 25-50 — docblock; Plan 03 deleted the rotated-region bullet),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md (§"Sharp rotation in image-worker (passthrough path)" lines 514-541 — verbatim form to insert),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md (§"Sharp Rotation Direction (Empirical)" lines 203-223 — direction proof),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md (§"Common Pitfalls → Pitfall 3: extend() offset orientation post-rotation" — load-bearing for SW + rotation interaction),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-PATTERNS.md (§"src/main/image-worker.ts - passthrough path rotate" — adapt-vs-mirror notes)
  </read_first>
  <behavior>
    - Test 1 (covered by Task 3): passthrough export of a rotated atlas row produces output PNG with metadata.width=canonicalW (NOT packed-swapped)
    - Test 2 (covered by Task 3): passthrough output PNG has metadata.height=canonicalH (NOT packed-swapped)
    - Test 3 (regression): non-rotated passthrough rows still work (existing image-worker.passthrough tests + image-worker.strip-whitespace tests stay green)
  </behavior>
  <action>
Read src/main/image-worker.ts lines 270-310 to locate the current passthrough atlas-extract block. Plan 03 left this block intact (it only deleted the typed-error gate at ~422-438). The current block opens with something like `if (passthroughUseAtlasExtract && row.atlasSource) { ... await pipeline.toFile(tmpPath); }`.

Replace the block contents with the post-edit form from <interfaces> above. Specifically:

1. The existing extract call stays unchanged: `sharp(a.pagePath).extract({ left: a.x, top: a.y, width: a.packW, height: a.packH })`.

2. After the extract, INSERT the rotation block:
```
if (a.rotated) {
  // Phase 33 D-03 - un-rotate Spine-CCW-packed region back to canonical orientation.
  // Direction VERIFIED EMPIRICALLY in 33-RESEARCH.md (CONTEXT D-03 hypothesis
  // of -90 was falsified): sharp.rotate(+90) cancels CCW packing per libgdx
  // atlas convention. Probe: probe-sharp-rotate.mjs.
  pipeline = pipeline.rotate(90);
}
```

3. REPLACE the existing strip-whitespace condition `if (a.packW !== a.w || a.packH !== a.h)` with the post-rotation canonical-orientation form:
```
const sourceCanvasW = a.rotated ? a.packH : a.packW;
const sourceCanvasH = a.rotated ? a.packW : a.packH;
if (sourceCanvasW !== a.w || sourceCanvasH !== a.h) {
```

4. INSIDE the SW block, REPLACE `top: a.h - a.offsetY - a.packH` with `top: a.h - a.offsetY - sourceCanvasH`, AND REPLACE `right: a.w - a.offsetX - a.packW` with `right: a.w - a.offsetX - sourceCanvasW`. Other extend() args (`bottom: a.offsetY`, `left: a.offsetX`, `background: {...}`) are unchanged.

5. The final `await pipeline.toFile(tmpPath)` is unchanged.

After the edit, run:
- npx tsc --noEmit (must exit 0)
- npm test -- tests/main/image-worker.passthrough.spec.ts (regression: existing non-rotated passthrough tests must still pass)
- npm test -- tests/main/image-worker.strip-whitespace.spec.ts (regression: existing SW tests must still pass — sourceCanvasW/H derived locals make the existing non-rotated case mathematically equivalent)
- npm test (full suite must still exit 0; the new image-worker-rotation.spec.ts is still describe.skip from Plan 02)
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- tests/main/image-worker.passthrough.spec.ts tests/main/image-worker.strip-whitespace.spec.ts &amp;&amp; grep -q "Phase 33 D-03" src/main/image-worker.ts &amp;&amp; grep -c "pipeline.rotate(90)" src/main/image-worker.ts | awk '$1 &gt;= 1'</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Phase 33 D-03" src/main/image-worker.ts exits 0 (header comment landed)
    - grep -q "VERIFIED EMPIRICALLY" src/main/image-worker.ts exits 0 (direction-lock comment landed)
    - grep -q "pipeline.rotate(90)" src/main/image-worker.ts exits 0 (passthrough rotate insert)
    - grep -q "sourceCanvasW = a.rotated" src/main/image-worker.ts exits 0 (post-rotation canonical-orientation derivation)
    - grep -q "sourceCanvasH = a.rotated" src/main/image-worker.ts exits 0
    - grep -q "a.h - a.offsetY - sourceCanvasH" src/main/image-worker.ts exits 0 (extend top uses post-rotation H)
    - grep -q "a.w - a.offsetX - sourceCanvasW" src/main/image-worker.ts exits 0 (extend right uses post-rotation W)
    - npx tsc --noEmit exits 0
    - npm test -- tests/main/image-worker.passthrough.spec.ts exits 0 (regression: non-rotated passthrough still works)
    - npm test -- tests/main/image-worker.strip-whitespace.spec.ts exits 0 (regression: SW non-rotated still works)
    - npm test exits 0 (full suite still green)
  </acceptance_criteria>
  <done>Passthrough path rotates rotated regions; SW + rotation extend args use post-rotation canonical orientation; existing non-rotated tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sharp.rotate(+90) to image-worker.ts resize path (with two-pipeline SW handling)</name>
  <files>src/main/image-worker.ts</files>
  <read_first>
    src/main/image-worker.ts (read lines 380-470 — typed-error block at ~422-438 was deleted by Plan 03; the resize atlas-extract logic continues),
    src/main/image-worker.ts (read lines 510-545 — existing two-pipeline SW pattern; this is the load-bearing template for SW + rotation),
    src/main/image-worker.ts (read lines 545-555 — applyResizeAndSharpen call signature),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md (§"Sharp rotation in image-worker (resize path)" lines 547-572 — verbatim form to insert),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-RESEARCH.md (§"Pitfall 4: Sharp pipeline order with extract().rotate().extend().resize() chain" — load-bearing two-pipeline rationale),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-PATTERNS.md (§"src/main/image-worker.ts - resize path rotate" — pre-pipeline materialize-toBuffer pattern)
  </read_first>
  <behavior>
    - Test 1 (covered by Task 3): resize export of a rotated atlas row produces output PNG with metadata.width = canonicalW * effectiveScale
    - Test 2 (covered by Task 3): resize output PNG has metadata.height = canonicalH * effectiveScale
    - Test 3 (regression): non-rotated SW resize rows still work (existing tests stay green)
    - Test 4 (regression): non-rotated non-SW resize rows still work
  </behavior>
  <action>
Read src/main/image-worker.ts lines 380-555 to locate the resize atlas-extract block. The block sits inside the main per-row export loop and uses the two-pipeline materialize-toBuffer pattern for SW.

The current resize logic (post-Plan-03 — typed-error block deleted) likely looks like:

```
const a = row.atlasSource;
let pipeline: sharp.Sharp;
if (a.packW !== a.w || a.packH !== a.h) {
  // Two-pipeline (SW path)
  const orig = await sharp(a.pagePath)
    .extract({ left: a.x, top: a.y, width: a.packW, height: a.packH })
    .extend({
      top: a.h - a.offsetY - a.packH,
      bottom: a.offsetY,
      left: a.offsetX,
      right: a.w - a.offsetX - a.packW,
      background: { r: 0, g: 0, b: 0, alpha: 0 },
    })
    .png().toBuffer();
  pipeline = sharp(orig);
} else {
  pipeline = sharp(a.pagePath).extract({
    left: a.x, top: a.y, width: a.packW, height: a.packH,
  });
}
await applyResizeAndSharpen(pipeline, row.outW, row.outH, row.effectiveScale, sharpenEnabled).toFile(tmpPath);
```

Replace it with the post-edit form (verbatim from <interfaces> above):

1. INTRODUCE derived locals BEFORE the SW branch:
```
const sourceCanvasW = a.rotated ? a.packH : a.packW;
const sourceCanvasH = a.rotated ? a.packW : a.packH;
```

2. REPLACE the SW condition `if (a.packW !== a.w || a.packH !== a.h)` with `if (sourceCanvasW !== a.w || sourceCanvasH !== a.h)`. The new condition is mathematically equivalent for non-rotated rows (where sourceCanvasW=a.packW and sourceCanvasH=a.packH) so existing non-rotated SW tests remain green.

3. INSIDE the SW branch, RESTRUCTURE the existing single chained pipeline to allow inserting rotate(+90) BETWEEN extract and extend:
```
let pre = sharp(a.pagePath).extract({ left: a.x, top: a.y, width: a.packW, height: a.packH });
if (a.rotated) {
  // Phase 33 D-03 - direction VERIFIED EMPIRICALLY (see passthrough comment).
  pre = pre.rotate(90);
}
const orig = await pre
  .extend({
    top:    a.h - a.offsetY - sourceCanvasH,
    bottom: a.offsetY,
    left:   a.offsetX,
    right:  a.w - a.offsetX - sourceCanvasW,
    background: { r: 0, g: 0, b: 0, alpha: 0 },
  })
  .png().toBuffer();
pipeline = sharp(orig);
```

The two-pipeline materialize-toBuffer pattern is preserved — the rotate slots into the pre-pipeline. Per RESEARCH §Pitfall 4, this is load-bearing because libvips reorders operations within a single pipeline; materializing to Buffer between extract+rotate+extend and resize prevents the reorder bug.

4. INSIDE the non-SW branch, INSERT rotation after extract:
```
pipeline = sharp(a.pagePath).extract({ left: a.x, top: a.y, width: a.packW, height: a.packH });
if (a.rotated) {
  pipeline = pipeline.rotate(90);
}
```

5. The final `await applyResizeAndSharpen(pipeline, row.outW, row.outH, row.effectiveScale, sharpenEnabled).toFile(tmpPath)` is unchanged.

Run after the edit:
- npx tsc --noEmit (must exit 0)
- npm test -- tests/main/image-worker.strip-whitespace.spec.ts (regression — non-rotated SW resize)
- npm test -- tests/main/image-worker.atlas-extract.spec.ts (regression — non-rotated atlas-extract resize; Plan 03 deleted the rotated sub-test, this confirms the rest still pass)
- npm test (full suite still green)

Edge case: if the existing resize block uses `pipeline.toFile(tmpPath)` with a different chain shape (e.g., directly calls applyResizeAndSharpen without the SW branch), adapt the structure but preserve the rotate-insertion point and the post-rotation canonical-orientation derivation. The fundamental contract is: the buffer fed to applyResizeAndSharpen must be in canonical orientation with effective dims = canonicalW × canonicalH (or sourceCanvasW × sourceCanvasH for SW intermediate state).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- tests/main/image-worker.strip-whitespace.spec.ts tests/main/image-worker.atlas-extract.spec.ts &amp;&amp; grep -c "rotate(90)" src/main/image-worker.ts | awk '$1 &gt;= 3'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "rotate(90)" src/main/image-worker.ts outputs at least 3 (passthrough + resize SW + resize non-SW)
    - grep -q "sourceCanvasW = a.rotated" src/main/image-worker.ts exits 0 (resize path also uses derived locals)
    - grep -q "sourceCanvasH = a.rotated" src/main/image-worker.ts exits 0
    - grep -q "let pre = sharp" src/main/image-worker.ts exits 0 (resize SW two-pipeline restructured to support rotation)
    - grep -q "pre = pre.rotate(90)" src/main/image-worker.ts exits 0 (pre-pipeline rotation in SW branch)
    - npx tsc --noEmit exits 0
    - npm test -- tests/main/image-worker.strip-whitespace.spec.ts exits 0 (non-rotated SW regression green)
    - npm test -- tests/main/image-worker.atlas-extract.spec.ts exits 0 (non-rotated atlas-extract regression green)
    - npm test exits 0 (full suite still green; the new image-worker-rotation.spec.ts is still describe.skip from Plan 02)
  </acceptance_criteria>
  <done>Resize path rotates rotated regions in BOTH SW two-pipeline branch and non-SW single-pipeline branch; existing non-rotated tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Fill in tests/core/export-rotation-dims.spec.ts AND tests/main/image-worker-rotation.spec.ts</name>
  <files>
    tests/core/export-rotation-dims.spec.ts,
    tests/main/image-worker-rotation.spec.ts
  </files>
  <read_first>
    tests/core/export-rotation-dims.spec.ts (current Wave 1 RED scaffold from Plan 02),
    tests/main/image-worker-rotation.spec.ts (current Wave 1 RED scaffold),
    tests/core/export.spec.ts (analog — synthetic SkeletonSummary pattern around lines 147-220),
    tests/main/image-worker.strip-whitespace.spec.ts (analog — hand-built ExportPlan + tmpDir setup + sharp metadata),
    tests/main/image-worker.passthrough.spec.ts (analog — passthrough ExportPlan row shape),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-PATTERNS.md (§"tests/core/export-rotation-dims.spec.ts" + §"tests/main/image-worker-rotation.spec.ts" — verbatim adapt sketches),
    .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-01-SUMMARY.md (Fixture Shape table — exact packedW, packedH, canonicalW, canonicalH, offsetX, offsetY values to substitute into the hand-built ExportPlan rows),
    src/shared/types.ts (ExportPlan + ExportRow + AtlasSource type definitions),
    src/main/image-worker.ts (post-Tasks-1+2 with rotation; runExport export signature)
  </read_first>
  <action>
Replace both test files with active bodies. Use the analogs and the Fixture Shape table (33-01-SUMMARY.md) for exact values.

***

### File 1: tests/core/export-rotation-dims.spec.ts (ATLAS-03 buildExportPlan)

Replace contents with:

```
import { describe, expect, it } from 'vitest';
import { buildExportPlan } from '../../src/core/export.js';
import type { ExportPlan, SkeletonSummary } from '../../src/shared/types.js';

describe('buildExportPlan — rotated region canonical out dims (ATLAS-03)', () => {
  it('rotated row with canonicalW=100, canonicalH=500, peakScale=1.0 → outW=100, outH=500 (NOT 500x100 swapped)', () => {
    const summary = {
      peaks: [
        {
          attachmentKey: 'default/STRIP/STRIP',
          attachmentName: 'STRIP',
          skinName: 'default',
          slotName: 'STRIP',
          animationName: 'static',
          time: 0,
          frame: 0,
          peakScale: 1.0,
          peakScaleX: 1.0,
          peakScaleY: 1.0,
          worldW: 100,
          worldH: 500,
          sourceW: 100,
          sourceH: 500,
          canonicalW: 100,
          canonicalH: 500,
          sourcePath: '/fake/STRIP.png',
          atlasSource: {
            pagePath: '/fake/page.png',
            x: 0,
            y: 0,
            packW: 500,   // PACKED dims swapped (rotate:true)
            packH: 100,
            offsetX: 0,
            offsetY: 0,
            w: 100,       // canonical
            h: 500,
            rotated: true,
          },
        },
      ],
      orphanedFiles: [],
    } as unknown as SkeletonSummary;

    const plan: ExportPlan = buildExportPlan(summary, new Map());
    const allRows = [...plan.rows, ...plan.passthroughCopies];
    expect(allRows.length).toBe(1);

    const row = allRows[0];
    // Canonical (unrotated) WxH - NOT 500x100 packed-swapped.
    expect(row.outW, 'outW must be canonical (unrotated) W=100, NOT packed W=500').toBe(100);
    expect(row.outH, 'outH must be canonical (unrotated) H=500, NOT packed H=100').toBe(500);
  });

  it('rotated row preserves atlasSource.rotated=true through buildExportPlan', () => {
    const summary = {
      peaks: [
        {
          attachmentKey: 'default/STRIP/STRIP',
          attachmentName: 'STRIP',
          skinName: 'default',
          slotName: 'STRIP',
          animationName: 'static',
          time: 0,
          frame: 0,
          peakScale: 1.0,
          peakScaleX: 1.0,
          peakScaleY: 1.0,
          worldW: 100,
          worldH: 500,
          sourceW: 100,
          sourceH: 500,
          canonicalW: 100,
          canonicalH: 500,
          sourcePath: '/fake/STRIP.png',
          atlasSource: {
            pagePath: '/fake/page.png',
            x: 0, y: 0,
            packW: 500, packH: 100,
            offsetX: 0, offsetY: 0,
            w: 100, h: 500,
            rotated: true,
          },
        },
      ],
      orphanedFiles: [],
    } as unknown as SkeletonSummary;

    const plan: ExportPlan = buildExportPlan(summary, new Map());
    const allRows = [...plan.rows, ...plan.passthroughCopies];
    const row = allRows[0];
    expect(row.atlasSource).toBeDefined();
    expect(row.atlasSource!.rotated).toBe(true);
  });
});
```

Note on field shape: the synthetic SkeletonSummary requires the precise shape buildExportPlan expects. If the existing tests/core/export.spec.ts uses different field names (e.g., `effectiveScale` lookup keys, override-region semantics), align with those. Read tests/core/export.spec.ts:147-220 first to confirm the canonical synthetic-row shape; the cast `as unknown as SkeletonSummary` accommodates omitted fields per the existing pattern.

***

### File 2: tests/main/image-worker-rotation.spec.ts (passthrough + resize)

Replace contents with the active body. Use the Fixture Shape table from 33-01-SUMMARY.md to substitute the FIXTURE_REGION_NAME, PACKED_W, PACKED_H, CANONICAL_W, CANONICAL_H, OFFSET_X, OFFSET_Y, ATLAS_X, ATLAS_Y values:

```
import { describe, expect, it, beforeEach, afterEach } from 'vitest';
import * as os from 'node:os';
import * as fs from 'node:fs';
import * as path from 'node:path';
import sharp from 'sharp';
import { runExport } from '../../src/main/image-worker.js';
import type { ExportPlan } from '../../src/shared/types.js';

const ROTATED_DIR = path.resolve('fixtures/spine_rotated/EXPORT');
const ROTATED_PAGE = path.join(ROTATED_DIR, 'skeleton.png');

// Fixture-shape values — substitute from 33-01-SUMMARY.md "Fixture Shape" table.
// CANONICAL_W and CANONICAL_H are the unrotated source rectangle dims (e.g., 100, 500).
// PACKED_W and PACKED_H are the bounds: line dims in skeleton.atlas (canonical-H, canonical-W swapped).
// ATLAS_X / ATLAS_Y are the bounds: line origin within skeleton.png.
// OFFSET_X / OFFSET_Y come from any offsets: line in skeleton.atlas (default 0/0 if SW disabled).
const FIXTURE_REGION_NAME = '{REGION_NAME_FROM_SUMMARY}';
const CANONICAL_W = {CANONICAL_W_FROM_SUMMARY};
const CANONICAL_H = {CANONICAL_H_FROM_SUMMARY};
const PACKED_W    = {PACKED_W_FROM_SUMMARY};      // = CANONICAL_H
const PACKED_H    = {PACKED_H_FROM_SUMMARY};      // = CANONICAL_W
const ATLAS_X     = {ATLAS_X_FROM_SUMMARY};
const ATLAS_Y     = {ATLAS_Y_FROM_SUMMARY};
const OFFSET_X    = {OFFSET_X_FROM_SUMMARY};
const OFFSET_Y    = {OFFSET_Y_FROM_SUMMARY};

let tmpDir: string;
beforeEach(() => { tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'stm-export-rot-')); });
afterEach(() => { fs.rmSync(tmpDir, { recursive: true, force: true }); });

describe('runExport — rotated atlas region extract (Phase 33 D-03)', () => {
  it('passthrough: rotated region produces output PNG with canonical W x H (NOT packed-swapped)', async () => {
    const syntheticPerRegionPath = path.join(tmpDir, 'src', `${FIXTURE_REGION_NAME}.png`);
    // Note: the source PNG path may not exist on disk; image-worker uses
    // atlasSource.pagePath for atlas-extract path so the per-region path is
    // not actually read.
    const plan: ExportPlan = {
      rows: [],
      excludedUnused: [],
      passthroughCopies: [{
        sourcePath: syntheticPerRegionPath,
        outPath: `images/${FIXTURE_REGION_NAME}.png`,
        sourceW: CANONICAL_W,
        sourceH: CANONICAL_H,
        outW: CANONICAL_W,
        outH: CANONICAL_H,
        effectiveScale: 1,
        attachmentNames: [FIXTURE_REGION_NAME],
        atlasSource: {
          pagePath: ROTATED_PAGE,
          x: ATLAS_X,
          y: ATLAS_Y,
          packW: PACKED_W,
          packH: PACKED_H,
          offsetX: OFFSET_X,
          offsetY: OFFSET_Y,
          w: CANONICAL_W,
          h: CANONICAL_H,
          rotated: true,
        },
      }],
      totals: { count: 1 },
    } as unknown as ExportPlan;

    const summary = await runExport(plan, tmpDir, () => {}, () => false);
    expect(summary.errors, 'no export errors').toEqual([]);
    expect(summary.successes).toBe(1);

    const outPath = path.join(tmpDir, 'images', `${FIXTURE_REGION_NAME}.png`);
    const meta = await sharp(outPath).metadata();
    expect(meta.width, 'output W must be canonical W (NOT packed W)').toBe(CANONICAL_W);
    expect(meta.height, 'output H must be canonical H (NOT packed H)').toBe(CANONICAL_H);
  });

  it('resize: rotated region downscaled by 0.5x produces canonical W/2 x H/2 output', async () => {
    const syntheticPerRegionPath = path.join(tmpDir, 'src', `${FIXTURE_REGION_NAME}.png`);
    const plan: ExportPlan = {
      rows: [{
        sourcePath: syntheticPerRegionPath,
        outPath: `images/${FIXTURE_REGION_NAME}.png`,
        sourceW: CANONICAL_W,
        sourceH: CANONICAL_H,
        outW: Math.ceil(CANONICAL_W * 0.5),
        outH: Math.ceil(CANONICAL_H * 0.5),
        effectiveScale: 0.5,
        attachmentNames: [FIXTURE_REGION_NAME],
        atlasSource: {
          pagePath: ROTATED_PAGE,
          x: ATLAS_X,
          y: ATLAS_Y,
          packW: PACKED_W,
          packH: PACKED_H,
          offsetX: OFFSET_X,
          offsetY: OFFSET_Y,
          w: CANONICAL_W,
          h: CANONICAL_H,
          rotated: true,
        },
      }],
      excludedUnused: [],
      passthroughCopies: [],
      totals: { count: 1 },
    } as unknown as ExportPlan;

    const summary = await runExport(plan, tmpDir, () => {}, () => false);
    expect(summary.errors, 'no export errors').toEqual([]);
    expect(summary.successes).toBe(1);

    const outPath = path.join(tmpDir, 'images', `${FIXTURE_REGION_NAME}.png`);
    const meta = await sharp(outPath).metadata();
    expect(meta.width).toBe(Math.ceil(CANONICAL_W * 0.5));
    expect(meta.height).toBe(Math.ceil(CANONICAL_H * 0.5));
  });

  it('passthrough: rotated region produces visually-correct (not upside-down or mirrored) output — top-left pixel column non-empty', async () => {
    // Light pixel-content sanity check (per RESEARCH §Pixel-Compare Strategy).
    // A wrong rotation direction (e.g., -90 instead of +90) would put visible
    // pixels at a different edge of the canvas. We can't easily compare against
    // an unrotated reference unless the fixture provides one, so we assert that
    // SOME pixels in the canonical-orientation top-left quadrant are non-empty
    // (alpha > 0) — this fails decisively for a 180° wrong rotation.
    const syntheticPerRegionPath = path.join(tmpDir, 'src', `${FIXTURE_REGION_NAME}.png`);
    const plan: ExportPlan = {
      rows: [],
      excludedUnused: [],
      passthroughCopies: [{
        sourcePath: syntheticPerRegionPath,
        outPath: `images/${FIXTURE_REGION_NAME}.png`,
        sourceW: CANONICAL_W,
        sourceH: CANONICAL_H,
        outW: CANONICAL_W,
        outH: CANONICAL_H,
        effectiveScale: 1,
        attachmentNames: [FIXTURE_REGION_NAME],
        atlasSource: {
          pagePath: ROTATED_PAGE,
          x: ATLAS_X, y: ATLAS_Y,
          packW: PACKED_W, packH: PACKED_H,
          offsetX: OFFSET_X, offsetY: OFFSET_Y,
          w: CANONICAL_W, h: CANONICAL_H,
          rotated: true,
        },
      }],
      totals: { count: 1 },
    } as unknown as ExportPlan;

    await runExport(plan, tmpDir, () => {}, () => false);
    const outPath = path.join(tmpDir, 'images', `${FIXTURE_REGION_NAME}.png`);
    const { data, info } = await sharp(outPath).raw().toBuffer({ resolveWithObject: true });
    expect(info.width).toBe(CANONICAL_W);
    expect(info.height).toBe(CANONICAL_H);
    // Sum alpha over the entire image. A correctly-rotated, non-blank region
    // has SOME alpha > 0 somewhere; a 180° mirrored or empty buffer also has
    // alpha > 0 in different places. This is a smoke check, not a pixel-perfect
    // direction lock — the dim assertions above plus the empirical probe at
    // 33-RESEARCH.md §Sharp Rotation Direction (Empirical) carry the direction-correctness load.
    let totalAlpha = 0;
    for (let i = 3; i < data.length; i += info.channels) {
      totalAlpha += data[i];
    }
    expect(totalAlpha, 'output must have some non-transparent pixels').toBeGreaterThan(0);
  });
});
```

After both files are written:
- Substitute the {REGION_NAME_FROM_SUMMARY}, {CANONICAL_W_FROM_SUMMARY}, etc. placeholders with the actual values from 33-01-SUMMARY.md "Fixture Shape" table.
- Run npm test -- tests/core/export-rotation-dims.spec.ts (must exit 0)
- Run npm test -- tests/main/image-worker-rotation.spec.ts (must exit 0)
- Run npm test (full suite must still exit 0)

If any test fails:
- export-rotation-dims failure → check that the synthetic peak shape matches what buildExportPlan expects (read tests/core/export.spec.ts to compare) — adjust field names but DO NOT relax the canonical-dim assertion
- image-worker-rotation passthrough dim failure → check Task 1's passthrough rotation insertion is on the actual code path (some image-worker code paths bypass atlas-extract for atlas-less mode; the fixture is atlas-source so passthroughUseAtlasExtract should be true)
- image-worker-rotation resize dim failure → check Task 2's two-pipeline restructuring; the rotation must apply BEFORE the toBuffer
- pixel-content sanity failure → almost certainly indicates the rotation direction is WRONG; revisit Task 1/2 +90 vs -90

Note: the third test (pixel-content sanity) is intentionally lenient — a stronger pixel-by-pixel direction lock would require a sibling reference fixture (rotation:false re-export of the same source rig). That's out of scope for this plan; the dim assertions + the empirical probe direction-lock comment in image-worker.ts carry the load.
  </action>
  <verify>
    <automated>npm test -- tests/core/export-rotation-dims.spec.ts tests/main/image-worker-rotation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "describe.skip" tests/core/export-rotation-dims.spec.ts exits 1 (no longer skipped)
    - grep -q "describe.skip" tests/main/image-worker-rotation.spec.ts exits 1 (no longer skipped)
    - grep -q "describe('buildExportPlan . rotated region canonical out dims (ATLAS-03)'" tests/core/export-rotation-dims.spec.ts exits 0
    - grep -q "describe('runExport . rotated atlas region extract (Phase 33 D-03)'" tests/main/image-worker-rotation.spec.ts exits 0
    - grep -q "expect(row.outW.*).toBe(100)" tests/core/export-rotation-dims.spec.ts || grep -q "outW.*toBe(CANONICAL_W)" tests/core/export-rotation-dims.spec.ts (canonical-dim assertion present, value 100 matches the standard test case)
    - grep -q "rotated: true" tests/core/export-rotation-dims.spec.ts exits 0 (synthetic atlasSource has rotated flag)
    - grep -q "rotated: true" tests/main/image-worker-rotation.spec.ts exits 0
    - grep -q "ROTATED_PAGE" tests/main/image-worker-rotation.spec.ts exits 0 (fixture page path resolved)
    - grep -q "runExport(plan" tests/main/image-worker-rotation.spec.ts exits 0
    - grep -q "sharp(outPath).metadata()" tests/main/image-worker-rotation.spec.ts exits 0 (output dim assertion uses sharp metadata)
    - npm test -- tests/core/export-rotation-dims.spec.ts exits 0 (ATLAS-03 buildExportPlan met)
    - npm test -- tests/main/image-worker-rotation.spec.ts exits 0 (ATLAS-03 image-worker met)
    - npm test exits 0 (full suite green)
  </acceptance_criteria>
  <done>Both ATLAS-03 specs pass; describe.skip removed; canonical-dim assertions lock the contract.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit exits 0
- npm test exits 0
- npm test -- tests/core/export-rotation-dims.spec.ts exits 0 (ATLAS-03 buildExportPlan)
- npm test -- tests/main/image-worker-rotation.spec.ts exits 0 (ATLAS-03 image-worker passthrough + resize + sanity)
- src/main/image-worker.ts contains 3 .rotate(90) call sites (passthrough + resize SW pre-pipeline + resize non-SW)
- Direction-lock comment "VERIFIED EMPIRICALLY" is present
- Existing image-worker.{passthrough,strip-whitespace,atlas-extract}.spec.ts still pass (regression)
</verification>

<success_criteria>
1. sharp.rotate(+90) inserted in 3 sites (passthrough + resize SW pre + resize non-SW)
2. Post-rotation canonical-orientation derivation (sourceCanvasW/H) used in extend args
3. Direction-lock comment cross-references RESEARCH §Sharp Rotation Direction (Empirical)
4. Two-pipeline materialize-toBuffer pattern preserved for SW + rotation
5. Both ATLAS-03 specs un-skipped + passing
6. Full suite green (no regression in non-rotated paths)
7. ATLAS-03 acceptance text met: ExportPlan output dims reflect canonical W×H AND image-worker produces canonical-orientation PNG output
</success_criteria>

<output>
After completion, create .planning/phases/33-rotated-atlas-region-support-loader-bounds-export-fixture/33-05-SUMMARY.md documenting:
- The 3 sharp.rotate(90) call sites in image-worker.ts (line ranges)
- Output of npm test -- tests/core/export-rotation-dims.spec.ts and npm test -- tests/main/image-worker-rotation.spec.ts (both green)
- Confirmation regression tests (image-worker.passthrough.spec.ts, image-worker.strip-whitespace.spec.ts, image-worker.atlas-extract.spec.ts) all green
- Note for Plan 06: All 4 ATLAS REQs are now code-complete; Plan 06 verifies the export.ts canonical-flow is unchanged AND exercises the end-to-end UAT.
</output>
</content>
