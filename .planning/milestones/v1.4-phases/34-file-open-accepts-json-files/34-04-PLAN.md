---
phase: 34-file-open-accepts-json-files
plan: 04
type: execute
wave: 4
depends_on:
  - 34-01
  - 34-02
  - 34-03
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - OPEN-01
  - OPEN-02
  - OPEN-03
  - OPEN-04
  - OPEN-05
user_setup: []

must_haves:
  truths:
    - "REQUIREMENTS.md introduces the OPEN-0x namespace under v1.4 with five requirement entries."
    - "REQUIREMENTS.md traceability table maps OPEN-01..05 → Phase 34 → Pending (or Complete if Plans 01-03 already shipped)."
    - "REQUIREMENTS.md coverage counter increments from 6 (v1.4 pre-Phase-34) to 11 total v1.4 requirements."
    - "ROADMAP.md Phase 34's `**Requirements**: TBD` placeholder is replaced with the concrete `OPEN-01..05` list."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "OPEN-01..05 requirements + traceability rows"
      contains: "OPEN-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 34 Requirements field populated"
      contains: "OPEN-01"
  key_links:
    - from: ".planning/ROADMAP.md (Phase 34 Requirements line)"
      to: ".planning/REQUIREMENTS.md (OPEN-0x section)"
      via: "shared REQ identifier"
      pattern: "OPEN-0[1-5]"
---

<objective>
Close the documentation loop: introduce the `OPEN-0x` REQ namespace in REQUIREMENTS.md and update both the v1.4 coverage counter and the ROADMAP.md Phase 34 Requirements field to reference the five concrete REQ IDs.

Purpose: Phase 34 was added with `**Requirements**: TBD (to be locked during /gsd-discuss-phase 34)` in ROADMAP.md (line 888). The CONTEXT.md `<decisions>` "Claude's Discretion" section authorized the planner to lock the OPEN-0x namespace. This plan does that locking — REQUIREMENTS.md gains five new requirement entries + traceability rows; ROADMAP.md Phase 34 gets the concrete ID list.

Output: REQUIREMENTS.md grows by one new section ("File → Open Menu Acceptance") containing OPEN-01..05 + 5 traceability rows + an updated coverage summary. ROADMAP.md Phase 34's Requirements line is rewritten from `TBD` to `OPEN-01, OPEN-02, OPEN-03, OPEN-04, OPEN-05`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/34-file-open-accepts-json-files/34-CONTEXT.md

<interfaces>
<!-- REQ identifiers and descriptions, sourced from CONTEXT.md "Claude's Discretion". -->

OPEN-01 — File → Open dialog filter accepts both `.stmproj` and `.json` extensions in a single unified filter entry on macOS + Windows. Dialog title is "Open Spine Project or Skeleton". (Implements D-01.)

OPEN-02 — Picking a `.json` routes through the same drag-drop loader cascade (`src/core/loader.ts` D-05/D-07/D-08). Strict loaderMode separation preserved per memory `project_strict_loadermode_separation.md`. No user-mode toggle. (Implements D-04.)

OPEN-03 — Picking a `.stmproj` routes through `handleProjectOpenFromPath` unchanged. End-to-end parity with drag-drop `.stmproj`. (Verification-only requirement.)

OPEN-04 — Opening a JSON or .stmproj over an unsaved in-progress project triggers the Phase 08.1 dirty-guard confirmation. Cancelling the picker (kind === 'cancelled') never fires the guard. (Implements D-05 — amends Phase 08.2 D-183 for the menu path.)

OPEN-05 — Cmd+O (macOS) / Ctrl+O (Windows) accelerator behaves identically to the menu item. (Verification-only — inherited from Phase 08.2 D-173.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OPEN-0x section to .planning/REQUIREMENTS.md + update traceability table + coverage counter</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — 77 lines)
  </read_first>
  <action>
    1. Read .planning/REQUIREMENTS.md in full. Identify three insertion points:
       - The new requirements section (Phase 34 OPEN-0x) lands AFTER the existing `### Rotated Atlas Region Support` section (around lines 17-21) and BEFORE the `### Future Planning (no user-facing requirement)` section (around line 23). This places Phase 34 inside the active v1.4 requirements block, sequentially after ATLAS-* (Phase 33).
       - The traceability table lands AFTER the existing `| ATLAS-04 | Phase 33 | Complete |` row (around line 66) and BEFORE the `**Coverage:**` summary block.
       - The coverage summary numbers are updated.

    2. Insert the new requirements section (preserve the existing `- [ ]` checkbox style + the `**REQ-ID**: description` formatting used by COMPAT/ATLAS sections):

    ```markdown
    ### File → Open Menu Acceptance

    Added 2026-05-11 via `/gsd-plan-phase 34`. Closes the menu ↔ drag-drop asymmetry so keyboard-driven workflows reach full parity with the drag-drop surface (relevant to atlas-less users on Windows-admin where DnD is disabled per PLATFORM-01).

    - [ ] **OPEN-01**: File → Open dialog filter accepts both `.stmproj` and `.json` extensions in a single unified filter entry on macOS + Windows. Dialog title is "Open Spine Project or Skeleton". Single OS picker filter (no dropdown to operate); matches drag-drop's two-extension acceptance with no user toggle. Implements Phase 34 D-01.
    - [ ] **OPEN-02**: Picking a `.json` from the File → Open dialog routes through the same drag-drop loader cascade (`src/core/loader.ts` D-05/D-07/D-08 atlas-source vs atlas-less sibling detection). Strict loaderMode separation preserved per memory `project_strict_loadermode_separation.md` (locked 2026-05-06). No user-mode toggle pre-load, no main-side pre-flight probe. Implements Phase 34 D-04.
    - [ ] **OPEN-03**: Picking a `.stmproj` from the File → Open dialog routes through `handleProjectOpenFromPath` unchanged. End-to-end parity with drag-drop `.stmproj` (same loader + sampler + buildSummary chain; same recovery branch for `SkeletonNotFoundOnLoadError`). Verification-only — no implementation delta vs Phase 08.2 baseline; locks the regression-free contract.
    - [ ] **OPEN-04**: Opening a JSON or `.stmproj` from the File → Open dialog over an unsaved in-progress project triggers the Phase 08.1 dirty-guard SaveQuitDialog with the actual kind (`'json'` or `'stmproj'`) discriminator. Cancelling the picker (`{ kind: 'cancelled' }`) NEVER fires the guard (D-05 improvement over status-quo Phase 08.2 D-183 which fired the guard pre-picker). Implements Phase 34 D-05 (amends Phase 08.2 D-183 for the menu path).
    - [ ] **OPEN-05**: `Cmd+O` (macOS) / `Ctrl+O` (Windows) accelerator behaves identically to the menu item: same picker, same dispatch, same dirty-guard timing. Verification-only — accelerator-to-menu wiring inherited from Phase 08.2 D-173 native Electron menu surface; this REQ locks the regression-free contract after the D-06 two-IPC-step rewrite.

    ```

    3. Insert five new rows into the Traceability table (after the existing ATLAS-04 row, before the `**Coverage:**` summary):

    ```markdown
    | OPEN-01 | Phase 34 | Pending |
    | OPEN-02 | Phase 34 | Pending |
    | OPEN-03 | Phase 34 | Pending |
    | OPEN-04 | Phase 34 | Pending |
    | OPEN-05 | Phase 34 | Pending |
    ```

    (Use "Pending" verbatim — execute-phase + verify-phase will flip these to "Complete" at the appropriate gate. If Plans 01-03 have already shipped at the moment this plan runs, the executor MAY use "Complete" for the rows whose plans are committed — but the safe default is "Pending" since this plan's commit is the final wave gate.)

    4. Update the `**Coverage:**` summary block. The existing block reads:
    ```
    - v1.4 requirements: 6 total
    - Mapped to phases: 6 ✓
    - Unmapped: 0
    - Phases: 2 (Phase 32 carries 2 REQs; Phase 33 carries 4 REQs)
    ```
    Replace with:
    ```
    - v1.4 requirements: 11 total
    - Mapped to phases: 11 ✓
    - Unmapped: 0
    - Phases: 3 (Phase 32 carries 2 REQs; Phase 33 carries 4 REQs; Phase 34 carries 5 REQs)
    ```

    5. Update the footer line at the bottom of the file (around line 76-77). Append:
    ```
    *Last updated: 2026-05-11 — Phase 34 planning: OPEN-01..05 added (File → Open menu accepts .json).*
    ```
    (Preserve any existing footer lines; just add a new dated line.)
  </action>
  <verify>
    <automated>grep -c "^- \[ \] \*\*OPEN-0[1-5]\*\*" .planning/REQUIREMENTS.md | grep -q "^5$" && grep -c "^| OPEN-0[1-5] | Phase 34 |" .planning/REQUIREMENTS.md | grep -q "^5$" &amp;&amp; grep -c "v1.4 requirements: 11 total" .planning/REQUIREMENTS.md | grep -q "^1$" &amp;&amp; grep -c "### File → Open Menu Acceptance" .planning/REQUIREMENTS.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[ \] \*\*OPEN-0[1-5]\*\*" .planning/REQUIREMENTS.md` equals 5 (five requirement bullet points).
    - `grep -c "^| OPEN-0[1-5] | Phase 34 |" .planning/REQUIREMENTS.md` equals 5 (five traceability rows).
    - `grep -c "v1.4 requirements: 11 total" .planning/REQUIREMENTS.md` equals 1.
    - `grep -c "Mapped to phases: 11 ✓" .planning/REQUIREMENTS.md` equals 1.
    - `grep -c "### File → Open Menu Acceptance" .planning/REQUIREMENTS.md` equals 1.
    - `grep -c "Phase 34 carries 5 REQs" .planning/REQUIREMENTS.md` equals 1.
    - Pre-existing entries unchanged: `grep -c "^- \[ \] \*\*COMPAT-01\*\*" .planning/REQUIREMENTS.md` equals 1 (sanity check — file didn't get clobbered).
  </acceptance_criteria>
  <done>OPEN-01..05 are first-class v1.4 requirements with traceability rows + an updated coverage counter.</done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/ROADMAP.md Phase 34 Requirements field from "TBD" to "OPEN-01..05"</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 880-910 (Phase 34 section — block to edit)
    - .planning/ROADMAP.md lines 86-92 (one-line phase summary at the top of the v1.4 phase list — also has a "REQs TBD" note that may need updating)
  </read_first>
  <action>
    1. Locate the Phase 34 detail section at .planning/ROADMAP.md line 882+. Find the `**Requirements**: TBD (to be locked during \`/gsd-discuss-phase 34\`)` line at line 888.

    2. Replace that line with:

    ```markdown
    **Requirements**: OPEN-01, OPEN-02, OPEN-03, OPEN-04, OPEN-05 (locked 2026-05-11 by `/gsd-plan-phase 34`; see REQUIREMENTS.md `### File → Open Menu Acceptance`)
    ```

    3. Locate the one-line phase summary at line 89 (the bullet that introduces Phase 34 in the top-of-roadmap phase list — contains `(REQs TBD during /gsd-discuss-phase 34)` at the end). Replace `(REQs TBD during /gsd-discuss-phase 34)` with `(REQs: OPEN-01..05)`.

    4. Do NOT touch the rest of Phase 34's section (Goal, Depends on, Background, Constraints, Success Criteria, Plans, UI hint lines). Do NOT touch the Progress table at the bottom — that's updated by execute-phase / verify-phase as plans complete.

    5. Sanity check: the Plans line at around line 904 currently reads `**Plans:** 0 plans (run \`/gsd-plan-phase 34\` to break down)`. The planning workflow's `update_roadmap` step will rewrite this; if it hasn't yet, this plan does NOT touch it (it's updated by the planner orchestrator after this plan commits). If it has already been rewritten to `**Plans:** 4 plans` with the bullet list of 34-01..04, leave it alone.
  </action>
  <verify>
    <automated>grep -c "\*\*Requirements\*\*: OPEN-01, OPEN-02, OPEN-03, OPEN-04, OPEN-05" .planning/ROADMAP.md | grep -q "^1$" &amp;&amp; grep -cE "Requirements: TBD.*gsd-discuss-phase 34" .planning/ROADMAP.md | grep -q "^0$" &amp;&amp; grep -cE "REQs TBD during /gsd-discuss-phase 34" .planning/ROADMAP.md | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\*\*Requirements\*\*: OPEN-01, OPEN-02, OPEN-03, OPEN-04, OPEN-05" .planning/ROADMAP.md` equals 1.
    - `grep -cE "Requirements:\s*TBD.*gsd-discuss-phase 34" .planning/ROADMAP.md` equals 0 (placeholder physically replaced).
    - `grep -cE "REQs TBD during /gsd-discuss-phase 34" .planning/ROADMAP.md` equals 0 (top-of-roadmap one-liner also updated).
    - `grep -c "REQs: OPEN-01..05" .planning/ROADMAP.md` equals 1.
    - Pre-existing Phase 33 section unchanged: `grep -c "^### Phase 33:" .planning/ROADMAP.md` equals 1 (sanity check).
  </acceptance_criteria>
  <done>ROADMAP.md Phase 34 Requirements line carries the concrete REQ IDs; placeholder text removed; one-line summary at top of roadmap also updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files | No trust boundaries — `.planning/*.md` is project documentation, not runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | Tampering | REQUIREMENTS.md / ROADMAP.md edits | accept | Documentation-only plan; no code path executes against these files. Git history is the audit trail. |

(No code surface in this plan. Threat model is intentionally minimal.)
</threat_model>

<verification>
- `grep -n "OPEN-" .planning/REQUIREMENTS.md | wc -l` returns >= 10 (5 bullets + 5 traceability rows minimum).
- `grep -n "OPEN-" .planning/ROADMAP.md | wc -l` returns >= 2 (top-of-roadmap one-liner + Phase 34 detail line).
- No code tests required — this is a docs-only plan.
- Optional sanity: `npm test --run` (full suite) — should pass identically to post-Plan-03 state (this plan changes no code).
</verification>

<success_criteria>
1. OPEN-01..05 are documented as v1.4 requirements with rationale + D-XX references.
2. Traceability table maps all five IDs to Phase 34.
3. v1.4 coverage counter increments to 11 total / 11 mapped / 0 unmapped.
4. ROADMAP.md Phase 34 Requirements line is concrete (no TBD).
5. Pre-existing entries (COMPAT-*, ATLAS-*, PORT-*) are untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/34-file-open-accepts-json-files/34-04-SUMMARY.md` per the template.
</output>
