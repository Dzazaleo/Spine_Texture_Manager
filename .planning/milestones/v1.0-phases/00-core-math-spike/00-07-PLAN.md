---
phase: 00-core-math-spike
plan: 07
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06]
files_modified: []
autonomous: false
requirements: [N1.1, N2.1]
requirements_addressed: [N1.1, N2.1]

must_haves:
  truths:
    - "`npm test` run from a clean shell exits 0 and reports N test cases all passing."
    - "`npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` exits 0 and prints a plausible peak table."
    - "Peak table has at least 3 rows (CIRCLE, SQUARE, TRIANGLE) with finite scale values."
    - "Sampler elapsed time reported by the CLI footer is under 500 ms."
    - "User (human) eyeballs the table and confirms the scale values look plausible for the fixture."
  artifacts: []
  key_links: []
---

<objective>
Final verification of Phase 0's exit criteria. The previous 6 plans each had their own tests and smoke checks, but this plan runs the full exit criteria from `.planning/ROADMAP.md` → Phase 0 section in a clean shell, captures the output, and hands it to the user for a one-time visual confirmation that the table's scale values are plausible (per ROADMAP.md: "CLI prints a table listing CIRCLE, SQUARE, TRIANGLE peaks with plausible scale values").

"Plausible" is a human judgment — there's no golden assertion that proves a peak of `0.847` is "right" without re-deriving the math by hand. So we gate on human eyeballs, informed by the knowledge that every component test has already proven math correctness within its scope.
Output: (no new files) — a human-verified confirmation that the phase is ready to close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/00-core-math-spike/00-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full exit criteria sweep and capture output</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 0 "Exit criteria" section)
  </read_first>
  <files>(none — reads only)</files>
  <action>
    Run the following in order, each as a separate `Bash` tool call so the orchestrator captures each output distinctly:

    1. `npm test` — full vitest suite. Must exit 0. Save the final "Test Files N passed" / "Tests N passed" line.

    2. `npm run typecheck` (alias for `tsc --noEmit`). Must exit 0.

    3. `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json > /tmp/stm-phase0-cli-output.txt 2>&1; echo "exit=$?"` then `cat /tmp/stm-phase0-cli-output.txt`. Must print the table AND exit 0.

    4. Parse the footer line from the CLI output and confirm the elapsed ms is under 500. Use `grep "Sampled in" /tmp/stm-phase0-cli-output.txt` and read the number. If >= 500, Phase 0 has a perf regression even though the vitest N2.1 test passed (indicates cold-JIT flake vs warm test) — flag for user.

    5. Confirm the table has rows for CIRCLE, SQUARE, TRIANGLE:
       `grep -c "CIRCLE\\|SQUARE\\|TRIANGLE" /tmp/stm-phase0-cli-output.txt` should be >= 3.

    6. Confirm `git log --oneline` shows exactly one commit per preceding plan (plans 01–06 each produced commits) — purely informational, no gate:
       `git log --oneline` and count commits with `phase-00` in the message.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json | grep -qE "CIRCLE.*SQUARE|Sampled in" && npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json | grep -q "Sampled in"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with 0 failing tests
    - `npm run typecheck` exits 0
    - `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` exits 0
    - CLI stdout contains all three region names: `grep -qE "CIRCLE" /tmp/stm-phase0-cli-output.txt` AND `grep -qE "SQUARE" /tmp/stm-phase0-cli-output.txt` AND `grep -qE "TRIANGLE" /tmp/stm-phase0-cli-output.txt`
    - CLI stdout contains a line starting with "Sampled in " (footer present)
    - The elapsed-ms value in the footer, when parsed, is a positive finite number strictly less than 500
  </acceptance_criteria>
  <done>All automated exit criteria pass; CLI output captured for human review in the next task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review — do the CLI's scale values look plausible?</name>
  <what-built>
    Phase 0 is done except for one thing: confirming that the table the CLI just printed looks RIGHT to a human familiar with the fixture. Every automated test has passed, but "plausible scale values" is an explicit ROADMAP exit criterion that requires eyeballs, not asserts.

    Executor has run `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` and captured the output at `/tmp/stm-phase0-cli-output.txt`.
  </what-built>
  <how-to-verify>
    1. `cat /tmp/stm-phase0-cli-output.txt` (or re-run `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json`).
    2. Sanity-check each row of the table:
       - **CIRCLE** is a weighted mesh on the CHAIN_* chain. Source is 699×699. Does the Peak W×H look like it could have come from the rig's animations?
       - **SQUARE** (slot SQUARE) is a leaf region on bone `SQUARE` — which is the target of a TransformConstraint. Source is 1000×1000. Expect a peak scale reflecting any scaling the TRANSFORM animation applies.
       - **SQUARE2** (slot SQUARE2) is a second instance of the same SQUARE region on the `SQUARE2` bone (pre-scaled per CLAUDE.md). Its peak may differ from SQUARE's.
       - **TRIANGLE** is a leaf region. Source 833×759. Expect a modest peak if no animation specifically scales it.
    3. Verify the `Source Animation` column: attachments touched only by setup pose show "Setup Pose (Default)"; attachments moved by animations show one of `PATH`, `SIMPLE_ROTATION`, `SIMPLE_SCALE`, `TRANSFORM`.
    4. Verify `Frame` column shows integer frame numbers (at 60 fps) — these are informational.
    5. Verify the footer prints elapsed ms, sampling Hz (120), attachment count, skin count, animation count.

    Expected observations (from fixture inventory):
       - Exactly 1 skin (default), 4 animations.
       - At least 3 rows (CIRCLE, SQUARE, TRIANGLE); up to 5 (if SQUARE2 slot is separately reported and PATH attachment had a non-skip type — it shouldn't).
       - Scale values > 0 and finite.
       - Elapsed time well under 500 ms on a warm run.

    Red flags that should FAIL the checkpoint:
       - All scales are exactly 1.0 (suggests sampler isn't applying animation transforms).
       - All peaks are Setup Pose (Default) (suggests animations aren't touching slots as expected).
       - Any NaN / Infinity / negative values.
       - Elapsed time > 500 ms.
       - Stderr has warnings/errors mixed in with the table.
  </how-to-verify>
  <resume-signal>
    Type "approved" to close Phase 0 and advance STATE.md to Phase 1 readiness.
    Type "issues: …" describing what looked off — the orchestrator will route the concern back to the relevant plan (most likely plan 04 sampler or plan 03 bounds).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Advance STATE.md to Phase 0 complete</name>
  <read_first>
    - .planning/STATE.md
    - .planning/ROADMAP.md (Phase 0 exit criteria)
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
    After human approval in Task 2, update `.planning/STATE.md`:

    1. Change `## Current phase` from `**Phase 0 — Core-math spike** (not yet started)` to `**Phase 0 — Core-math spike** (COMPLETE — all exit criteria green)`.
    2. Under `## Last completed`, add:
       - `Phase 0 shipped: loader.ts + bounds.ts + sampler.ts + CLI + golden test suite.`
       - `All N1.1–N1.6 correctness tests green. N2.1 perf gate <500 ms. N2.3 FS-free hot loop verified by grep.`
       - `CLI renders CIRCLE/SQUARE/TRIANGLE peak table on SIMPLE_TEST.json — user-approved.`
    3. Change `## Next action` to `Run /gsd-plan-phase 1 to begin Electron + React scaffold phase (PHASE 1).`
    4. Leave `## Open questions` alone (the weighted-mesh + PhysicsConstraint questions from before are now RESOLVED — note them as resolved at the bottom of that section instead of deleting for audit trail).

    Commit:
    ```bash
    git add .planning/STATE.md
    git commit -m "docs(phase-00): mark Phase 0 complete in STATE.md

    - All exit criteria green (automated + human-verified table)
    - Ready for Phase 1"
    ```
  </action>
  <verify>
    <automated>grep -q "Phase 0 — Core-math spike" .planning/STATE.md && grep -q "COMPLETE" .planning/STATE.md && [ -z "$(git status --porcelain .planning/STATE.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains the substring `COMPLETE` in the Current phase section
    - `.planning/STATE.md` "Next action" references Phase 1
    - `git status --porcelain .planning/STATE.md` outputs empty (committed)
    - `git log --oneline -1` contains `phase-00`
  </acceptance_criteria>
  <done>STATE.md advanced; Phase 0 officially closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI stdout → user eyeballs | Human verification step. No technical trust boundary, but a process boundary: the human can reject the output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-07-01 | T (Tampering) | false-positive human approval | accept | The human is the user themselves. If they approve bad output, Phase 1 will surface the bug quickly because the UI renders the same values. Cheap to catch, cheap to fix. |
| T-00-07-02 | D (DoS) | cold-JIT perf flake | mitigate | Task 1 runs the CLI twice (once for exit-code capture, once for the grep check). The second run is warm. If still >500 ms, flag for user. |
</threat_model>

<verification>
- `npm test` green.
- `npm run typecheck` green.
- CLI exits 0 on fixture and prints all 3 regions.
- Elapsed time < 500 ms.
- Human approves the table's scale values.
- STATE.md updated and committed.
</verification>

<success_criteria>
- Phase 0 ROADMAP exit criteria all green (automated + human).
- STATE.md reflects completion.
- Project is ready for `/gsd-plan-phase 1`.
</success_criteria>

<output>
After completion, create `.planning/phases/00-core-math-spike/00-07-SUMMARY.md` documenting: full CLI output (captured), the user's approval verbatim, elapsed-ms timings across the automated exit sweep, the STATE.md commit SHA. This summary serves as the phase's canonical "done" artifact.
</output>
