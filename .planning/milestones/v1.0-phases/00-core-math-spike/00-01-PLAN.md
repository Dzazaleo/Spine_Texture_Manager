---
phase: 00-core-math-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - src/core/.gitkeep
  - tests/core/.gitkeep
  - scripts/.gitkeep
autonomous: true
requirements: [N2.3]
requirements_addressed: [N2.3]

must_haves:
  truths:
    - "Repo is initialized as a git repository with `.gitignore` excluding `temp/`, `node_modules/`, `dist/`, `coverage/`."
    - "`npm install` completes with zero errors; `node_modules/@esotericsoftware/spine-core/` exists."
    - "`npx tsc --noEmit` exits 0 on the empty-but-scaffolded project (strict mode)."
    - "`npm test` runs vitest and exits 0 (no tests discovered yet is OK — `passWithNoTests: true` set)."
    - "`.gitignore` blocks `temp/` from being staged."
  artifacts:
    - path: "package.json"
      provides: "Node project manifest with spine-core, vitest, typescript, tsx, @types/node in deps; scripts for test / test:watch / cli / typecheck."
      contains: "@esotericsoftware/spine-core"
    - path: "tsconfig.json"
      provides: "TypeScript strict mode, ES2022, ESNext modules, bundler moduleResolution."
      contains: "\"strict\": true"
    - path: "vitest.config.ts"
      provides: "Vitest Node environment, globs tests/**/*.spec.ts, passWithNoTests:true so bootstrap commit can proceed with an empty test dir."
      contains: "passWithNoTests: true"
    - path: ".gitignore"
      provides: "Excludes temp/, node_modules/, dist/, coverage/, .DS_Store."
      contains: "temp/"
    - path: "src/core/.gitkeep"
      provides: "Placeholder so git tracks the core/ directory structure."
    - path: "tests/core/.gitkeep"
      provides: "Placeholder so git tracks the tests/core/ directory."
    - path: "scripts/.gitkeep"
      provides: "Placeholder so git tracks the scripts/ directory."
  key_links:
    - from: "package.json scripts.test"
      to: "vitest.config.ts"
      via: "npm run test invokes vitest which reads vitest.config.ts"
      pattern: "\"test\":\\s*\"vitest run\""
    - from: "package.json scripts.cli"
      to: "tsx runtime"
      via: "npm run cli -- <path> executes scripts/cli.ts via tsx"
      pattern: "\"cli\":\\s*\"tsx scripts/cli.ts\""
---

<objective>
Bootstrap the Spine Texture Manager repo as a headless Node + TypeScript project. Initialize git with the correct `.gitignore` (so the user's `temp/` editor files never get staged), add the dependencies that the core math needs (`@esotericsoftware/spine-core`, `vitest`, `typescript`, `tsx`, `@types/node`), and verify the toolchain runs end-to-end on an empty codebase.

Purpose: nothing else in Phase 0 can proceed until `npm install`, `tsc --noEmit`, and `vitest` all work. This plan establishes that baseline atomically.
Output: `package.json`, `tsconfig.json`, `vitest.config.ts`, `.gitignore`, and a `src/core/` + `tests/core/` + `scripts/` folder skeleton tracked in git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-core-math-spike/00-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git init + .gitignore (lock temp/ out before the first stage)</name>
  <read_first>
    - CLAUDE.md (Folder conventions: `temp/` must be gitignored)
    - .planning/phases/00-core-math-spike/00-CONTEXT.md (Git Initialization section)
  </read_first>
  <files>.gitignore</files>
  <action>
    1. Verify repo is not yet initialized: `test -d .git && echo "ALREADY INITIALIZED" || echo "FRESH"`. If already initialized, skip `git init`.
    2. Create `.gitignore` at repo root with EXACTLY the following content (one item per line, with a blank line between groups and a trailing newline):

       ```
       # Dependencies
       node_modules/

       # Build output
       dist/
       coverage/

       # User's Spine editor source files — NOT part of the app
       temp/

       # OS cruft
       .DS_Store

       # Logs
       *.log
       npm-debug.log*
       ```

    3. Run `git init` (use default branch `main` if the user's git config sets it; do not override their init.defaultBranch). If git complains about default branch, run `git init --initial-branch=main`.
    4. Do NOT run `git add` yet — subsequent tasks finish the scaffolding first, then a single commit wraps this whole plan.
  </action>
  <verify>
    <automated>test -d .git && grep -qx "temp/" .gitignore && grep -qx "node_modules/" .gitignore && grep -qx "dist/" .gitignore && grep -qx "coverage/" .gitignore && grep -qx ".DS_Store" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `.git/` directory exists at repo root
    - `.gitignore` at repo root contains lines `temp/`, `node_modules/`, `dist/`, `coverage/`, `.DS_Store` (each on its own line, verified via `grep -qx`)
    - `git status --porcelain -- temp/` outputs nothing (temp/ is ignored)
  </acceptance_criteria>
  <done>Git repo exists; `.gitignore` excludes `temp/` and build/cruft dirs; nothing staged yet.</done>
</task>

<task type="auto">
  <name>Task 2: package.json + tsconfig.json + vitest.config.ts + folder scaffolding</name>
  <read_first>
    - .planning/phases/00-core-math-spike/00-CONTEXT.md (Runtime & Dependencies decisions)
    - CLAUDE.md (Commands section: npm run test, npm run cli)
  </read_first>
  <files>package.json, tsconfig.json, vitest.config.ts, src/core/.gitkeep, tests/core/.gitkeep, scripts/.gitkeep</files>
  <action>
    1. Create `package.json`:

       ```json
       {
         "name": "spine-texture-manager",
         "version": "0.0.0",
         "private": true,
         "type": "module",
         "description": "Desktop app that computes per-attachment peak render scale for Spine 4.2+ skeletons.",
         "scripts": {
           "test": "vitest run",
           "test:watch": "vitest",
           "typecheck": "tsc --noEmit",
           "cli": "tsx scripts/cli.ts"
         },
         "dependencies": {
           "@esotericsoftware/spine-core": "^4.2.0"
         },
         "devDependencies": {
           "@types/node": "^20.0.0",
           "tsx": "^4.0.0",
           "typescript": "^5.3.0",
           "vitest": "^1.0.0"
         },
         "engines": {
           "node": ">=18"
         }
       }
       ```

       Use the CURRENT stable release of each package — if `npm install` reports a newer major is out and the quoted `^` range is fine, proceed. If `@esotericsoftware/spine-core` has moved past 4.2.x, pin to the latest 4.2.x so we stay on the 4.2 runtime surface the plan was verified against.

    2. Create `tsconfig.json`:

       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "ESNext",
           "moduleResolution": "bundler",
           "lib": ["ES2022"],
           "strict": true,
           "esModuleInterop": true,
           "skipLibCheck": true,
           "forceConsistentCasingInFileNames": true,
           "resolveJsonModule": true,
           "allowSyntheticDefaultImports": true,
           "noEmit": true,
           "types": ["node"],
           "outDir": "./dist"
         },
         "include": ["src/**/*.ts", "tests/**/*.ts", "scripts/**/*.ts"],
         "exclude": ["node_modules", "dist", "temp", "coverage"]
       }
       ```

    3. Create `vitest.config.ts`:

       ```ts
       import { defineConfig } from 'vitest/config';

       export default defineConfig({
         test: {
           environment: 'node',
           include: ['tests/**/*.spec.ts'],
           globals: false,
           testTimeout: 10_000,
           passWithNoTests: true,
         },
       });
       ```

       **Critical:** `passWithNoTests: true` is required because Task 3 runs `npm test` on an empty `tests/core/` directory; vitest 1.x/2.x exits non-zero by default when no spec files match, which would block the bootstrap commit. The later TDD plan (05) adds the real specs.

    4. Create empty `.gitkeep` files at `src/core/.gitkeep`, `tests/core/.gitkeep`, `scripts/.gitkeep` (empty file contents are fine).

    Note on `moduleResolution: "bundler"`: it plays nicely with ESM + tsx + vitest and avoids the `.js` extension dance that `"node16"` forces on relative imports. Discretion call per CONTEXT.md.
  </action>
  <verify>
    <automated>test -f package.json && test -f tsconfig.json && test -f vitest.config.ts && test -f src/core/.gitkeep && test -f tests/core/.gitkeep && test -f scripts/.gitkeep && grep -q "@esotericsoftware/spine-core" package.json && grep -q "\"strict\": true" tsconfig.json && grep -q "vitest/config" vitest.config.ts && grep -q "passWithNoTests: true" vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists and `grep -q "@esotericsoftware/spine-core" package.json` succeeds
    - `package.json` `grep -q "\"test\":" package.json` and `grep -q "\"typecheck\":" package.json` and `grep -q "\"cli\":" package.json`
    - `package.json` declares `"type": "module"` (`grep -q "\"type\": \"module\"" package.json`)
    - `tsconfig.json` `grep -q "\"strict\": true" tsconfig.json`
    - `tsconfig.json` `grep -q "\"target\": \"ES2022\"" tsconfig.json`
    - `vitest.config.ts` imports from `vitest/config` (`grep -q "vitest/config" vitest.config.ts`)
    - `vitest.config.ts` sets `passWithNoTests: true` (`grep -q "passWithNoTests: true" vitest.config.ts`)
    - Directories `src/core/`, `tests/core/`, `scripts/` all exist (verified by `.gitkeep` files)
  </acceptance_criteria>
  <done>Manifest + TS config + vitest config written; directory skeleton tracked; `passWithNoTests` flag present so the bootstrap `npm test` passes on an empty suite.</done>
</task>

<task type="auto">
  <name>Task 3: npm install + verify toolchain runs on empty project</name>
  <read_first>
    - package.json (from Task 2)
    - tsconfig.json (from Task 2)
    - vitest.config.ts (from Task 2)
  </read_first>
  <files>package-lock.json</files>
  <action>
    1. Run `npm install`. This installs all deps and generates `package-lock.json`.
       - If install fails because a listed version no longer exists on npm, bump to the latest stable of that package and retry. Record any version deviations in the SUMMARY.
    2. Verify the critical transitive module is present: `test -d node_modules/@esotericsoftware/spine-core`.
    3. Run `npx tsc --noEmit` — must exit 0 on an empty `src/` (nothing to typecheck except the empty folders — which is fine).
    4. Run `npm test` — vitest must exit 0. With `passWithNoTests: true` in `vitest.config.ts`, an empty `tests/core/` directory is a green result.
    5. Stage everything and make the initial commit:

       ```bash
       git add .gitignore package.json package-lock.json tsconfig.json vitest.config.ts src/core/.gitkeep tests/core/.gitkeep scripts/.gitkeep
       git commit -m "chore(phase-00): bootstrap TypeScript + vitest project scaffolding

       - Add spine-core, vitest, tsx, typescript deps
       - Strict TS with ES2022 target
       - vitest passWithNoTests so empty bootstrap suite exits 0
       - .gitignore blocks temp/, node_modules/, dist/, coverage/
       - Folder skeleton: src/core/, tests/core/, scripts/"
       ```

    6. After commit: `git log --oneline -1` must show the commit. `git status --porcelain` must be empty (working tree clean, no ignored-but-staged files).
  </action>
  <verify>
    <automated>test -d node_modules/@esotericsoftware/spine-core && npx tsc --noEmit && npm test --silent && git log --oneline -1 | grep -q "phase-00" && [ -z "$(git status --porcelain)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/@esotericsoftware/spine-core/package.json` exists
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 (vitest found no tests, `passWithNoTests: true` allows this)
    - `git log --oneline -1` output contains the substring `phase-00`
    - `git status --porcelain` outputs empty string (clean tree)
    - `git ls-files temp/ 2>/dev/null` outputs nothing (temp/ NOT tracked)
  </acceptance_criteria>
  <done>Dependencies installed; toolchain verified green; scaffolding committed as the repo's first commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local FS → `package.json` | `npm install` fetches code from npm; any malicious transitive dep could execute during install. |
| local FS → `.gitignore` | If `temp/` not excluded, user's private Spine editor files leak into source control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01-01 | I (Information disclosure) | `.gitignore` | mitigate | `.gitignore` must exclude `temp/` on its own line (grep-verified in acceptance criteria) before any `git add` runs. Sequenced as Task 1 so it lands before any tracking happens. |
| T-00-01-02 | T (Tampering) | npm supply chain | accept | Using well-known packages (spine-core official, vitest, tsx, typescript). `package-lock.json` is committed so future installs are deterministic. Full supply-chain auditing (npm audit, sigstore) is out of scope for Phase 0. |
| T-00-01-03 | D (DoS) | `npm install` size | accept | Four top-level dev deps; transitive bloat is acceptable for a desktop dev toolchain. |
</threat_model>

<verification>
- `.gitignore` excludes `temp/`, `node_modules/`, `dist/`, `coverage/`.
- `npm install` succeeds, `package-lock.json` is created and committed.
- `@esotericsoftware/spine-core` is in `node_modules/` after install.
- `npx tsc --noEmit` exits 0 on an empty scaffolded project.
- `npm test` exits 0 (vitest finds no tests; `passWithNoTests: true` allows this for the bootstrap).
- Initial commit created; working tree is clean; `temp/` is not tracked.
</verification>

<success_criteria>
- Repo is a git repo with a single clean initial commit.
- `npm test`, `npm run typecheck`, and `npm run cli` (the latter will fail at runtime until scripts/cli.ts exists — only the script definition is required here) are all wired into `package.json`.
- Folder skeleton `src/core/`, `tests/core/`, `scripts/` tracked by git.
- User's `temp/` directory is untouched and untracked.
</success_criteria>

<output>
After completion, create `.planning/phases/00-core-math-spike/00-01-SUMMARY.md` documenting: final dep versions installed, any deviations from the proposed versions, toolchain verification output, commit SHA of the initial commit.
</output>
