---
phase: 08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea
plan: 02
subsystem: main
tags: [typescript, electron-ipc, discriminated-union, tdd-green, vr-02]

# Dependency graph
requires:
  - phase: 08.1
    plan: 01
    provides: SerializableError discriminated union with 7-field SkeletonNotFoundOnLoadError arm + RED 8.1-IPC-01 spec
provides:
  - handleProjectOpenFromPath populates 7 threaded recovery fields (projectPath, originalSkeletonPath, mergedOverrides, samplingHz, lastOutDir, sortColumn, sortDir) when SkeletonJsonNotFoundError fires
  - handleProjectReloadWithSkeleton parallel rescue branch threads same 7 fields when the user-picked replacement skeleton is itself missing
  - NonRecoveryKind type alias (Exclude<SerializableError['kind'], 'SkeletonNotFoundOnLoadError'>) — locks SpineLoaderError forwarders out of the recovery-payload arm at compile time
  - 8.1-IPC-01 GREEN — main-side wire for the VR-02 fix landed
affects: [08.1-03-appshell-onClickOpen, 08.1-04-app-tsx-projectLoadFailed-and-onBeforeDrop-ref-bridge]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Compile-time narrowing via Exclude<Union['kind'], RecoverySite> — forces SpineLoaderError forwarders to construct the flat (non-payload) arm of a discriminated union, so a future error subclass cannot accidentally produce the recovery arm without populating its threaded fields"

key-files:
  created: []
  modified:
    - "src/main/project-io.ts (+62/-5: SkeletonJsonNotFoundError rescue at handleProjectOpenFromPath threads 7 D-159 fields from the in-scope `materialized` object + `absolutePath`; parallel rescue at handleProjectReloadWithSkeleton threads the same 7 fields with `a.projectPath` / `a.newSkeletonPath` / cast `a.mergedOverrides` / lifted `samplingHz`/`lastOutDir`/`sortColumn`/`sortDir`; SpineLoaderError forwarders narrowed to NonRecoveryKind cast; new type alias NonRecoveryKind near imports with 14-line JSDoc)"
    - "src/main/ipc.ts (+8/-3: KnownErrorKind narrowed to Exclude<SerializableError['kind'], 'SkeletonNotFoundOnLoadError'> with parallel reasoning + 6-line JSDoc citing the project-io.ts NonRecoveryKind precedent)"

key-decisions:
  - "Cast narrowing via `Exclude<SerializableError['kind'], 'SkeletonNotFoundOnLoadError'>` chosen over per-site `if (err.name === 'SkeletonJsonNotFoundError')` switches: the Exclude approach gives compile-time type safety AND keeps the SpineLoaderError forwarder shape unchanged at runtime (still a single-line `kind: err.name as ...` cast). Per-site switches would have added 5 lines per call site × 3 call sites = 15 lines of runtime branching for zero behavior difference."
  - "Plan body owned only project-io.ts:338 (the primary D-159 wire). Per the orchestrator objective + Plan 08.1-01's Next-Phase-Readiness map, also fixed project-io.ts:349/505/511 + ipc.ts:257 in this single commit — they form an atomic unit (all 5 stem from the same Plan-01 type-contract change; splitting them would have left the typecheck broken between Plans 08.1-02 and a hypothetical 08.1-02b)."

patterns-established:
  - "When a discriminated-union arm carries kind-specific payload, narrow forwarder casts via Exclude<Union['kind'], PayloadArmKind> at every site that constructs the flat arm via `err.name as ...`. TypeScript then verifies the forwarder cannot accidentally construct the payload arm without populating its required fields."
  - "GREEN phase of multi-plan TDD: a Wave-N plan that flips a Wave-0 RED spec to GREEN should commit all type-contract resolutions in the same atomic commit so the inter-plan typecheck never breaks (downstream plans inherit a clean compile)."

requirements-completed: [VR-02 (main-side wire), F9.2, D-149]

# Metrics
duration: ~3 min
completed: 2026-04-26
---

# Phase 08.1 Plan 02: Thread recovery fields into SkeletonNotFoundOnLoadError envelope Summary

**handleProjectOpenFromPath now threads 7 recovery fields (projectPath, originalSkeletonPath, mergedOverrides, samplingHz, lastOutDir, sortColumn, sortDir) into the SerializableError envelope when SkeletonJsonNotFoundError fires (D-159) — closes 5 deliberate Plan-01 typecheck breaks and flips spec 8.1-IPC-01 GREEN.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-26T10:57:49Z
- **Completed:** 2026-04-26T11:00:29Z
- **Tasks:** 1/1
- **Files modified:** 2 (src/main/project-io.ts, src/main/ipc.ts)

## Accomplishments

- **handleProjectOpenFromPath SkeletonJsonNotFoundError rescue now threads the 7-field recovery payload (D-159).** The `materialized` object (line 323) and the `absolutePath` function arg are both already in scope at the rescue branch, so no re-fetching needed. The 7 fields populate verbatim from `materialized.skeletonPath` / `materialized.overrides` / `materialized.samplingHz` / `materialized.lastOutDir` / `materialized.sortColumn` / `materialized.sortDir` + `absolutePath`. AppShell.tsx:529-538 (the renderer's pre-Plan-08.1-03 best-effort placeholder) still typechecks because empty strings + null + 120 satisfy the new union arm — Plan 08.1-03 will replace those empty literals with field reads.
- **handleProjectReloadWithSkeleton parallel rescue branch threads same 7 fields.** The plan body did not require this — but Plan 08.1-01's Next-Phase-Readiness map and the orchestrator objective both flagged it as Plan-02 scope. The recovery semantics: if the user-picked replacement skeleton is ALSO missing (rare race condition: file deleted between picker close and `loadSkeleton` call), the renderer can re-prompt the locate-skeleton picker without losing the cached overrides/settings. `originalSkeletonPath` carries the just-picked (now-missing) path so the picker can default-locate near it.
- **SpineLoaderError forwarders narrowed via NonRecoveryKind.** Three cast sites (project-io.ts:362, 525 + ipc.ts:264) previously cast `err.name as SerializableError['kind']` — but post-Plan-01 that union includes `'SkeletonNotFoundOnLoadError'` whose arm requires 7 extra fields. New type alias `NonRecoveryKind = Exclude<SerializableError['kind'], 'SkeletonNotFoundOnLoadError'>` makes TypeScript verify these forwarders cannot accidentally produce the recovery-payload arm without populating its threaded fields.
- **8.1-IPC-01 RED → GREEN** (1 passed | 11 skipped | total 12 in `tests/main/project-io.spec.ts`). Existing T-08-MISS spec ("missing skeleton returns typed error") preserved at 12/12 GREEN. Full vitest baseline: 270 → 271 passed (+1), 5 → 4 failed (−1; the 4 RED renderer specs flip in Plans 08.1-03/04), 1 skipped, 1 todo unchanged.

## Verbatim diff (Task 1) — Primary D-159 wire at handleProjectOpenFromPath

**BEFORE** (src/main/project-io.ts:333-343):
```typescript
    if (err instanceof SkeletonJsonNotFoundError) {
      // D-149 — triggers locate-skeleton flow in the renderer (T-08-MISS).
      return {
        ok: false,
        error: {
          kind: 'SkeletonNotFoundOnLoadError',
          message: err.message,
        },
      };
    }
```

**AFTER** (per CONTEXT D-159 verbatim):
```typescript
    if (err instanceof SkeletonJsonNotFoundError) {
      // D-149 — triggers locate-skeleton flow in the renderer (T-08-MISS).
      // Phase 8.1 D-158/D-159: thread the cached recovery payload from the
      // already-materialized project (line 323) + the function arg
      // `absolutePath`. handleProjectReloadWithSkeleton (lines 459-555)
      // consumes these values verbatim when the user picks a replacement
      // skeleton. Pre-Phase-8.1 these fields were absent from the envelope,
      // forcing the renderer to populate empty literals — VR-02 was the
      // resulting bug (rejection at handleLocateSkeleton with
      // "projectPath must be a .stmproj path").
      return {
        ok: false,
        error: {
          kind: 'SkeletonNotFoundOnLoadError',
          message: err.message,
          projectPath: absolutePath,
          originalSkeletonPath: materialized.skeletonPath,
          mergedOverrides: materialized.overrides,
          samplingHz: materialized.samplingHz,
          lastOutDir: materialized.lastOutDir,
          sortColumn: materialized.sortColumn,
          sortDir: materialized.sortDir,
        },
      };
    }
```

## Spec status delta

| Spec ID | File | Before | After |
|---------|------|--------|-------|
| 8.1-IPC-01 | tests/main/project-io.spec.ts | RED (1 failed) | **GREEN (1 passed)** |
| missing skeleton returns typed error (D-149, T-08-MISS) | tests/main/project-io.spec.ts | GREEN | GREEN (preserved) |
| 8.1-VR-01 | tests/renderer/save-load.spec.tsx | RED | RED (Plan 08.1-04 turns GREEN) |
| 8.1-VR-02 | tests/renderer/save-load.spec.tsx | RED | RED (Plan 08.1-03 turns GREEN) |
| 8.1-VR-03a | tests/renderer/save-load.spec.tsx | RED | RED (Plan 08.1-04 turns GREEN) |
| 8.1-VR-03b | tests/renderer/save-load.spec.tsx | RED | RED (Plan 08.1-04 turns GREEN) |

## Test baseline impact

| | Before (Plan 08.1-01 close) | After (Plan 08.1-02 close) | Delta |
|--|--|--|--|
| Passed | 270 | **271** | **+1 (8.1-IPC-01 flipped GREEN)** |
| Failed | 5 | **4** | **−1 (8.1-IPC-01 GREEN)** |
| Skipped | 1 | 1 | 0 |
| Todo | 1 | 1 | 0 |

CONTEXT D-166 baseline expectation: "270 passed → ≥273 passed" — Plan 08.1-02 delivers +1 of the +3 (the remaining +2 land in Plans 08.1-03/04 when 8.1-VR-02 / VR-01 / VR-03a / VR-03b flip GREEN).

## Deliberate downstream typecheck breaks (all 5 cleared)

| Site | Pre-Plan-02 error | Post-Plan-02 |
|------|-------------------|--------------|
| `src/main/project-io.ts:338` | TS2322 — `{kind: 'SkeletonNotFoundOnLoadError', message}` missing 7 required fields | RESOLVED — 7 fields populated from `materialized.*` + `absolutePath` |
| `src/main/project-io.ts:349` | TS2322 — `kind: err.name as SerializableError['kind']` produces `'SkeletonNotFoundOnLoadError'` literal which the second arm forbids | RESOLVED — narrowed to `as NonRecoveryKind` |
| `src/main/project-io.ts:505` | TS2322 — same as :338 (parallel rescue in handleProjectReloadWithSkeleton) | RESOLVED — 7 fields populated from `a.projectPath`/`a.newSkeletonPath`/cast `a.mergedOverrides`/lifted local vars |
| `src/main/project-io.ts:511` | TS2322 — same as :349 (parallel cast in handleProjectReloadWithSkeleton) | RESOLVED — narrowed to `as NonRecoveryKind` |
| `src/main/ipc.ts:257` | TS2322 — `KnownErrorKind` aliased to `SerializableError['kind']` includes `'SkeletonNotFoundOnLoadError'` but the forwarder constructs the flat arm | RESOLVED — `KnownErrorKind` re-aliased to `Exclude<SerializableError['kind'], 'SkeletonNotFoundOnLoadError'>` |

`npx tsc -b --noEmit` post-Plan-02: **only the pre-existing deferred** `scripts/probe-per-anim.ts:14:31` TS2339 error remains (out of scope per `.planning/phases/04-scale-overrides/deferred-items.md` + `.planning/phases/07-atlas-preview-modal/deferred-items.md` since Phase 4).

## AppShell.tsx:529-538 still typechecks (Plan 08.1-03 will tighten)

The renderer's pre-Plan-08.1-03 best-effort placeholder at `src/renderer/src/components/AppShell.tsx:529-538` constructs:
```typescript
setSkeletonNotFoundError({
  message: resp.error.message,
  originalSkeletonPath: '',
  projectPath: '',
  mergedOverrides: {},
  cachedSamplingHz: 120,
  cachedLastOutDir: null,
  cachedSortColumn: null,
  cachedSortDir: null,
});
```

These empty/default literals satisfy the new union arm (`string`/`Record<string, number>`/`number`/`string|null`) so the typecheck passes — but the **values** still hit the VR-02 bug (renderer sends empty `projectPath` to `reloadProjectWithSkeleton` which rejects with "projectPath must be a .stmproj path"). Plan 08.1-03 replaces these with field reads from `resp.error.*`.

## Files Created/Modified

- **`src/main/project-io.ts`** — primary D-159 wire (rescue branch at handleProjectOpenFromPath threads 7 fields); parallel D-159 wire at handleProjectReloadWithSkeleton (cleared the 5th typecheck break, hardens the rare "user-picked-skeleton-also-missing" race condition); 2 SpineLoaderError forwarder casts narrowed to `as NonRecoveryKind`; new type alias `NonRecoveryKind = Exclude<SerializableError['kind'], 'SkeletonNotFoundOnLoadError'>` near the imports with 14-line JSDoc.
- **`src/main/ipc.ts`** — `KnownErrorKind` re-aliased to `Exclude<SerializableError['kind'], 'SkeletonNotFoundOnLoadError'>` with parallel-reasoning JSDoc citing the project-io.ts precedent. Same compile-time guarantee for `handleSkeletonLoad`'s SpineLoaderError forwarder.

## Decisions Made

- **D-158 `Exclude<>` cast narrowing pattern adopted at all 3 SpineLoaderError forwarder sites.** Chose compile-time `Exclude` over per-site `switch`/`if` runtime branching because: (a) zero runtime cost; (b) preserves the existing single-line cast shape; (c) catches future error-class additions at compile time (any new `SpineLoaderError` subclass that produces `'SkeletonNotFoundOnLoadError'` would now require its own threaded-payload construction site instead of silently flowing through the forwarder cast).
- **Scope expansion: cleared all 5 typecheck breaks in this commit (not just :338).** Plan body owned only the primary :338 wire. Per the orchestrator's explicit objective ("Your work clears all 5 of those typecheck breaks") + Plan 08.1-01's Next-Phase-Readiness map (lines 207-211 of 08.1-01-SUMMARY.md), Plan 02 is the canonical owner of all 5. Splitting them across separate commits would have left the inter-plan typecheck broken — violating the Wave-0 type-contract-first invariant from Plan 08.1-01's `patterns-established` block.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Cleared 4 additional typecheck breaks the plan body did not own**
- **Found during:** Task 1 (typecheck after the primary :338 wire)
- **Issue:** The plan's `<task>` block only specified the wire at project-io.ts:334-343. But the plan-level `<verification>` and the orchestrator's objective both required `npx tsc -b --noEmit` to pass with no errors in src/main/project-io.ts (and the orchestrator additionally required ipc.ts:257 to compile). After fixing only :338, the remaining 4 typecheck breaks at project-io.ts:349/505/511 + ipc.ts:257 would have left the file in a broken-typecheck state — making subsequent plans unable to merge cleanly.
- **Fix:** Threaded the 7 fields at handleProjectReloadWithSkeleton's parallel rescue branch (project-io.ts:505 → cleared); narrowed the 3 SpineLoaderError forwarder casts via the new `NonRecoveryKind = Exclude<SerializableError['kind'], 'SkeletonNotFoundOnLoadError'>` alias (project-io.ts:349/525 + ipc.ts:264 → cleared); added 14-line JSDoc + 6-line JSDoc citing the parallel reasoning.
- **Files modified:** src/main/project-io.ts, src/main/ipc.ts
- **Verification:** `npx tsc -b --noEmit` after the change emits ONLY the pre-existing deferred `scripts/probe-per-anim.ts:14:31` TS2339 error. All 5 deliberate Plan-01 breaks resolved.
- **Committed in:** `dbb3ffc` (single atomic commit per the Wave-0 type-contract-first invariant — splitting would have left the typecheck broken between two plans).
- **Pattern:** Same Plan-01 Next-Readiness handoff precedent (08.1-01-SUMMARY.md:207-211 explicitly assigns these 4 sites to Plan 02). The orchestrator objective also explicitly required clearing all 5 typecheck breaks.

---

**Total deviations:** 1 auto-fixed (Rule 3 Blocking — scope expansion to the 4 sibling typecheck breaks that share the same root cause).

**Impact on plan:** Zero scope creep beyond what Plan 08.1-01's Next-Readiness map already assigned to this plan. The orchestrator objective explicitly authorized this scope.

## Issues Encountered

None — task executed cleanly. The 8.1-IPC-01 RED spec flipped to GREEN on the first attempt (no debug iterations). The 5 typecheck breaks resolved as predicted by Plan 01's break-map.

## Locked-file invariants confirmed clean (vs Phase 5 baseline `552389e`)

| File | Diff (lines) | Invariant |
|------|--------------|-----------|
| `scripts/cli.ts` | 0 | Phase 5 D-102 — CLI byte-for-byte (CLAUDE.md rule #3) |
| `src/core/sampler.ts` | 0 | Phase 5 D-102 (CLAUDE.md rule #3) |
| `src/core/loader.ts` | 0 | Informal lock — no Phase 8.1 fix needs it |
| `src/core/project-file.ts` | 392 | Created in Plan 08-02; D-171 lock holds (schema unchanged in 8.1; this plan does not touch the file) |

`npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` exits 0 with **CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000** byte-for-byte unchanged. Sampling rate 120 Hz, elapsed ~23 ms.

`git diff dbb3ffc --diff-filter=D --name-only HEAD~1 HEAD` produces no output — no unintended file deletions.

## Plan-level acceptance gate audit

The plan's `<acceptance_criteria>` block specified 7 single-grep-count gates (`grep -c "X" src/main/project-io.ts` returns 1). Three of these resolved as predicted; **four returned counts > 1** because the same field substrings (`samplingHz: materialized.samplingHz`, `lastOutDir: materialized.lastOutDir`, `sortColumn: materialized.sortColumn`, `sortDir: materialized.sortDir`) ALSO appear at the existing `MaterializedProject` construction at lines 425-432 (success-path return — was already there pre-Plan-02) AND `samplingHz: materialized.samplingHz` appears at line 407 (`sampleSkeleton` call site).

| Grep | Expected | Actual | Cause |
|------|----------|--------|-------|
| `projectPath: absolutePath` | 1 | 1 | ✓ unique to new wire |
| `originalSkeletonPath: materialized.skeletonPath` | 1 | 1 | ✓ unique to new wire |
| `mergedOverrides: materialized.overrides` | 1 | 1 | ✓ unique to new wire |
| `samplingHz: materialized.samplingHz` | 1 | 3 | rescue + sampler call (line 407) + MaterializedProject success-path (line 429) |
| `lastOutDir: materialized.lastOutDir` | 1 | 2 | rescue + MaterializedProject success-path (line 430) |
| `sortColumn: materialized.sortColumn` | 1 | 2 | rescue + MaterializedProject success-path (line 431) |
| `sortDir: materialized.sortDir` | 1 | 2 | rescue + MaterializedProject success-path (line 432) |

The gate intent is "the new code is present." All 7 fields are present in the new rescue branch at lines 370-376. The over-count is a **gate semantics false-positive** introduced by the plan author overlooking the pre-existing healthy success-path code that uses the same field-assignment shape. **Gate intent satisfied.**

## TDD Gate Compliance

Plan 08.1-02 is the GREEN phase for spec **8.1-IPC-01** (RED landed in Plan 08.1-01 commit `a40fffc`). Per the executor TDD discipline:

- **RED gate:** `a40fffc` (Plan 08.1-01 Task 2) — exists in git log, FAILS with `Received: undefined` for the projectPath assertion.
- **GREEN gate:** `dbb3ffc` (Plan 08.1-02 Task 1) — exists in git log, the spec PASSES (1 passed | 11 skipped | total 12). Commit message includes verbatim vitest output as evidence.
- **REFACTOR gate:** Not needed — the wire is direct field-pass-through, no cleanup opportunity.

Per the standard TDD cycle, this commit is `feat(...)` (not `test(...)`) because it adds production behavior that flips a pre-existing RED spec — the spec itself was committed in the RED phase.

## Next Phase Readiness

**Plan 08.1-03 (Wave 3) is unblocked.** It owns:
- Replacing the 8 hardcoded empty/null literals at `src/renderer/src/components/AppShell.tsx:529-538` with field reads from the typed `resp.error` payload (per CONTEXT D-160).
- The remaining 4 RED renderer specs flip GREEN incrementally:
  - **8.1-VR-02** flips GREEN in Plan 08.1-03 (AppShell.onClickOpen wire).
  - **8.1-VR-01 + 8.1-VR-03a + 8.1-VR-03b** flip GREEN in Plan 08.1-04 (App.tsx projectLoadFailed branch + onBeforeDropRef bridge).

The renderer call site at AppShell.tsx:529-538 typechecks cleanly with the post-Plan-02 SerializableError shape (empty strings + null + 120 satisfy the new union arm) — no inter-plan typecheck blockers between 08.1-02 and 08.1-03.

## Self-Check: PASSED

**Files exist:**
- `src/main/project-io.ts` — FOUND (modified)
- `src/main/ipc.ts` — FOUND (modified)

**Commits exist:**
- `dbb3ffc` (Task 1: thread 7 recovery fields + clear 5 typecheck breaks) — FOUND

**Plan-level success criteria:**
- ✓ Single task executed and committed atomically (`feat(08.1-02): ...` at `dbb3ffc`)
- ✓ All 5 deliberate downstream typecheck breaks from Wave 1 resolved
- ✓ 8.1-IPC-01 RED → GREEN (vitest output `Tests 1 passed | 11 skipped (12)` captured in commit message)
- ✓ Both typecheck projects clean (`npx tsc -b --noEmit` emits only pre-existing deferred `scripts/probe-per-anim.ts:14:31` TS2339 — out of scope)
- ✓ No regressions in the prior 270/271 vitest baseline (271 passed | 4 failed | 1 skipped | 1 todo — the 4 remaining failures are the 4 Plan-01 RED specs that flip in Plans 08.1-03/04)
- ✓ SUMMARY.md created (this file)
- ✓ STATE.md and ROADMAP.md will be updated in the close-out commit

**Locked-file invariants vs Phase 5 baseline `552389e`:**
- ✓ `scripts/cli.ts`: 0 lines
- ✓ `src/core/sampler.ts`: 0 lines
- ✓ `src/core/loader.ts`: 0 lines
- ✓ `src/core/project-file.ts`: 392 lines (Plan 08-02 baseline; this plan added 0)
- ✓ `npm run cli` byte-for-byte unchanged

---
*Phase: 08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea*
*Completed: 2026-04-26*
