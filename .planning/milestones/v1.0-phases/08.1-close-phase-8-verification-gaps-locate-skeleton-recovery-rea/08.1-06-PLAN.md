---
phase: 08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea
plan: 06
type: execute
wave: 5
depends_on: [08.1-01, 08.1-02, 08.1-03, 08.1-04, 08.1-05]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-VALIDATION.md
autonomous: false
status: planned
requirements_addressed: [VR-01, VR-02, VR-03, F9.2, D-143, D-149]
nyquist_compliant: true
must_haves:
  truths:
    - "Full vitest suite green: ≥273 passed + 1 skipped + ≤1 todo (270 baseline + 4 new GREEN: 8.1-VR-01, VR-02, VR-03a, VR-03b — VR-03a/b counted separately, plus 8.1-IPC-01 = 5 net new GREEN)"
    - "Both typecheck projects clean (or only pre-existing deferred errors)"
    - "electron-vite build green"
    - "All 4 locked-file invariants intact (scripts/cli.ts, src/core/sampler.ts, src/core/loader.ts, src/core/project-file.ts)"
    - "CLI byte-for-byte: npm run cli on SIMPLE_TEST.json exits 0 with CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000"
    - "ROADMAP.md Phase 08.1 Goal field is rewritten per D-172"
    - "ROADMAP.md Phase 08.1 plan checkboxes flipped from [ ] to [x] for all 6 plans"
    - "STATE.md advanced to Phase 08.1 COMPLETE; Last completed §08.1-06 records the close-out"
    - "Manual UAT: 3 reproducer scripts (VR-01, VR-02, VR-03) all signed off by user"
  artifacts:
    - path: ".planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-VALIDATION.md"
      provides: "Per-plan validation map for 6 plans + per-spec GREEN evidence"
      contains: "8.1-VR-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 08.1 closed with Goal rewritten per D-172"
      contains: "**Goal:** Close the 3 reachability gaps from Phase 8 verification"
    - path: ".planning/STATE.md"
      provides: "Phase 08.1 COMPLETE marker + Last completed §08.1-06"
      contains: "Phase 08.1"
  key_links:
    - from: ".planning/STATE.md current position"
      to: "Phase 9 (next)"
      via: "Phase 08.1 close-out commit"
      pattern: "Phase 9"
---

<objective>
Close-out plan for Phase 08.1. Runs the automated exit-criteria sweep (full vitest, both typecheck projects, electron-vite build, locked-file diffs, npm run cli), produces `08.1-VALIDATION.md` (per-plan map), updates ROADMAP.md per D-172 (Goal rewrite + plan checkbox flips), advances STATE.md, and gates the phase on manual UAT of the 3 reproducer scripts from CONTEXT `<specifics>`.

Purpose: ensures VR-01/VR-02/VR-03 are closed end-to-end (automated specs GREEN + human-verified reproducers GREEN) before `/gsd-verify-work 8` re-runs and Phase 9 advances. Locks all phase invariants in a single audit pass.

Output: 1 new file (08.1-VALIDATION.md), 2 updated files (ROADMAP.md, STATE.md); a passed manual UAT with developer signoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-CONTEXT.md
@.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-01-SUMMARY.md
@.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-02-SUMMARY.md
@.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-03-SUMMARY.md
@.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-04-SUMMARY.md
@.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-05-SUMMARY.md
@.planning/phases/08-save-load-project-state/08-VERIFICATION.md
</context>

<threat_model>
## Trust Boundaries

This plan does not modify any application code. The only edits are documentation files (ROADMAP.md, STATE.md, 08.1-VALIDATION.md). No new attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-10 | (n/a) | Documentation-only changes | accept | No code changes. The threat model for the phase as a whole is fully covered by the per-plan threat blocks in Plans 01-05. The close-out plan reaffirms the locked-file invariants via grep checks but does not introduce new IPC, UI, or code surface. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Automated exit-criteria sweep — full vitest, typecheck, build, locked-file audit, CLI smoke</name>
  <files>.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-CONTEXT.md (D-166 test baseline expectation: 270 → ≥273; D-170 locked-file diffs sweep)
    - .planning/phases/08-save-load-project-state/08-VALIDATION.md (the Phase 8 close-out's VALIDATION.md is the formatting model — same per-plan-row table layout)
    - .planning/STATE.md §08-05 entry (formatting reference for the close-out commit message in Task 4)
  </read_first>
  <action>
Run the full automated exit-criteria sweep. Capture output for the validation document.

**Sweep step 1: Full vitest run**
```bash
npx vitest run
```
Expected: ≥273 passed + 1 skipped + ≤1 todo. Confirm specifically that:
- 8.1-VR-01 PASS
- 8.1-VR-02 PASS
- 8.1-VR-03a PASS
- 8.1-VR-03b PASS
- 8.1-IPC-01 PASS
- All Phase 8 baseline tests still PASS (project-file.spec.ts, project-io.spec.ts D-149/T-08-MISS, save-load.spec.tsx existing 5 baseline)
- arch.spec.ts (Layer 3 + portability + sandbox + main-CJS + Phase 4 batch-scope + Phase 6 sharp/fs + Phase 8 electron) all PASS

**Sweep step 2: Both typecheck projects clean**
```bash
npx tsc -b --noEmit
```
Expected: zero errors in src/main/project-io.ts, src/renderer/src/App.tsx, src/renderer/src/components/AppShell.tsx, src/shared/types.ts, tests/main/project-io.spec.ts, tests/renderer/save-load.spec.tsx. Pre-existing deferred error in `scripts/probe-per-anim.ts` (per Phase 8 close-out STATE.md §08-05) may persist — confirm it's the SAME error class and not a regression introduced by Phase 8.1.

**Sweep step 3: electron-vite build green**
```bash
npx electron-vite build
```
Expected: out/main/index.cjs (CJS, ~58 kB), out/preload/index.cjs (CJS, ~8 kB), out/renderer/index.html + assets. Sizes should be within ±5% of Phase 8 close-out reference (renderer JS 688 kB, CSS 24 kB). Confirm no NEW sandbox/CJS regressions.

**Sweep step 4: Locked-file diffs sweep (D-170)**
```bash
git diff 552389e..HEAD -- scripts/cli.ts | wc -l       # must be 0 (Phase 5 D-102)
git diff 552389e..HEAD -- src/core/sampler.ts | wc -l  # must be 0 (CLAUDE.md rule #3)
git diff 552389e..HEAD -- src/core/loader.ts | wc -l   # must be 0 (informal lock)
git diff 552389e..HEAD -- src/core/project-file.ts | wc -l  # must be 0 (D-171)
```
All four must return 0.

**Sweep step 5: CLI byte-for-byte smoke**
```bash
npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json
```
Expected: exit 0; output contains rows for CIRCLE (peak 2.018), SQUARE (peak 1.500), TRIANGLE (peak 2.000), SQUARE2. Capture full stdout for the validation doc.

**Sweep step 6: 8.1-specific validation greps**
Run these greps and confirm each matches expectation:
```bash
grep -c "kind: 'SkeletonNotFoundOnLoadError'" src/shared/types.ts        # 1
grep -c "export type SerializableError" src/shared/types.ts              # 1
grep -c "export interface SerializableError" src/shared/types.ts         # 0
grep -c "projectPath: absolutePath" src/main/project-io.ts               # 1
grep -c "originalSkeletonPath: materialized.skeletonPath" src/main/project-io.ts  # 1
grep -c "projectPath: resp.error.projectPath" src/renderer/src/components/AppShell.tsx  # 1
grep -c "projectPath: ''" src/renderer/src/components/AppShell.tsx       # 0
grep -c "AppShell's Open button is best-effort" src/renderer/src/components/AppShell.tsx  # 0
grep -c "status: 'projectLoadFailed'" src/renderer/src/App.tsx           # ≥3
grep -c "Extract<SerializableError" src/renderer/src/App.tsx             # ≥1
grep -c "onBeforeDropRef={beforeDropRef}" src/renderer/src/App.tsx       # 2
grep -c "onBeforeDrop={handleBeforeDrop}" src/renderer/src/App.tsx       # 1
grep -c "saveQuitDialogState.cancelAction?." src/renderer/src/components/AppShell.tsx  # 1
grep -c "'new-skeleton-drop'" src/renderer/src/components/AppShell.tsx   # 1
grep -c "'new-project-drop'" src/renderer/src/components/AppShell.tsx    # 1
git diff 552389e..HEAD -- src/renderer/src/components/DropZone.tsx | wc -l  # 0
git diff 552389e..HEAD -- src/renderer/src/modals/SaveQuitDialog.tsx | wc -l # 0
```

**Sweep step 7: Create 08.1-VALIDATION.md** with the per-plan map.

Write the file at `.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-VALIDATION.md` with this structure:

```markdown
---
phase: 08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea
status: signed-off
nyquist_compliant: true
generated: 2026-04-26
---

# Phase 08.1 Validation Report

Closes Phase 8 verification gaps VR-01, VR-02, VR-03 from `.planning/phases/08-save-load-project-state/08-VERIFICATION.md`.

## Per-plan validation

| Plan | Goal | Spec/Grep evidence | Status |
|------|------|--------------------|--------|
| 08.1-01 | SerializableError discriminated union + RED specs | grep `kind: 'SkeletonNotFoundOnLoadError'` returns 1; grep `8.1-VR-01\|VR-02\|VR-03a\|VR-03b` returns ≥4; grep `8.1-IPC-01` in project-io.spec.ts returns 1; grep `it.todo` in save-load.spec.tsx returns 0 | ✅ |
| 08.1-02 | Main-side wire — 7 threaded fields at SkeletonJsonNotFoundError rescue | grep `projectPath: absolutePath` returns 1; spec 8.1-IPC-01 PASS; existing T-08-MISS PASS | ✅ |
| 08.1-03 | Renderer-side wire — AppShell.onClickOpen reads threaded fields | grep `projectPath: resp.error.projectPath` returns 1; grep `projectPath: ''` returns 0; "best-effort" comment block deleted; spec 8.1-VR-02 PASS | ✅ |
| 08.1-04 | App.tsx projectLoadFailed variant + recovery banner | grep `status: 'projectLoadFailed'` returns ≥3; grep `Extract<SerializableError` returns ≥1; spec 8.1-VR-01 PASS | ✅ |
| 08.1-05 | onBeforeDropRef bridge + cancelAction extension | grep `onBeforeDropRef={beforeDropRef}` returns 2; grep `saveQuitDialogState.cancelAction?.` returns 1; grep `'new-skeleton-drop'` + `'new-project-drop'` each return 1; specs 8.1-VR-03a + 8.1-VR-03b PASS; DropZone.tsx + SaveQuitDialog.tsx untouched | ✅ |
| 08.1-06 | Close-out automated sweep + manual UAT | full vitest ≥273 passed; both typecheck clean; electron-vite build green; 4 locked-file diffs each return 0; CLI exit 0 with CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000; 3-reproducer manual UAT signed off | (filled at human-verify completion) |

## Per-spec GREEN evidence

| Spec ID | File | Status |
|---------|------|--------|
| 8.1-VR-01 | tests/renderer/save-load.spec.tsx | ✅ |
| 8.1-VR-02 | tests/renderer/save-load.spec.tsx | ✅ |
| 8.1-VR-03a | tests/renderer/save-load.spec.tsx | ✅ |
| 8.1-VR-03b | tests/renderer/save-load.spec.tsx | ✅ |
| 8.1-IPC-01 | tests/main/project-io.spec.ts | ✅ |

## Locked-file invariants (D-170)

| File | Lock | Diff vs base 552389e | Status |
|------|------|----------------------|--------|
| scripts/cli.ts | Phase 5 D-102 + CLAUDE.md rule #3 | 0 lines | ✅ |
| src/core/sampler.ts | Phase 5 D-102 + CLAUDE.md rule #3 | 0 lines | ✅ |
| src/core/loader.ts | Informal 8.1 lock | 0 lines | ✅ |
| src/core/project-file.ts | D-171 (.stmproj v1 schema unchanged) | 0 lines | ✅ |
| src/renderer/src/components/DropZone.tsx | 8.1 invariant (already exposed onBeforeDrop) | 0 lines | ✅ |
| src/renderer/src/modals/SaveQuitDialog.tsx | D-165 (already supports both reasons) | 0 lines | ✅ |

## Test baseline

- Phase 8 close: 270 passed + 1 skipped + 3 todo
- Phase 08.1 close: ≥273 passed + 1 skipped + ≤1 todo
- Net: +3 passed (≥) + 4 new GREEN renderer specs (8.1-VR-01, VR-02, VR-03a, VR-03b) + 1 new GREEN main spec (8.1-IPC-01) = +5 new specs all PASS; 2 it.todo placeholders deleted

## CLI byte-for-byte smoke

```
$ npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json
[paste actual stdout here including elapsed footer]
exit 0
```

## Build artifact sizes

| Artifact | Phase 8 close | Phase 08.1 close | Delta |
|----------|---------------|------------------|-------|
| out/main/index.cjs | 57.96 kB | (capture) | (compute) |
| out/preload/index.cjs | 8.03 kB | (capture) | (compute) |
| out/renderer/*.js | 688.02 kB | (capture) | (compute) |
| out/renderer/*.css | 23.93 kB | (capture) | (compute) |

Expected: deltas within ±2% (Phase 08.1 only adds ~30 lines of TypeScript across App.tsx + AppShell.tsx; renderer bundle should grow by <2 kB).

## Manual UAT (filled in at the human-verify gate — Task 2)

| Reproducer | Description | Result |
|------------|-------------|--------|
| VR-01 | .stmproj drag-drop with renamed skeleton | (pending) |
| VR-02 | Toolbar Open / Cmd+O with renamed skeleton | (pending) |
| VR-03 | Dirty + new-skeleton-drop / new-project-drop | (pending) |

---

*Generated by Plan 08.1-06 (close-out).*
```

Save the file and proceed to the next task.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run 2>&1 | grep -E 'Test Files|Tests' | head -2` shows ≥273 passed
    - `npx tsc -b --noEmit 2>&1 | grep -cE "error TS"` returns 0 OR matches ONLY the pre-existing scripts/probe-per-anim.ts deferred error class
    - `npx electron-vite build 2>&1 | grep -cE 'error|ERROR'` returns 0
    - `git diff 552389e..HEAD -- scripts/cli.ts | wc -l` returns 0
    - `git diff 552389e..HEAD -- src/core/sampler.ts | wc -l` returns 0
    - `git diff 552389e..HEAD -- src/core/loader.ts | wc -l` returns 0
    - `git diff 552389e..HEAD -- src/core/project-file.ts | wc -l` returns 0
    - `git diff 552389e..HEAD -- src/renderer/src/components/DropZone.tsx | wc -l` returns 0
    - `git diff 552389e..HEAD -- src/renderer/src/modals/SaveQuitDialog.tsx | wc -l` returns 0
    - `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json 2>&1 | grep -c "CIRCLE"` returns ≥1
    - `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json 2>&1 | grep -c "2.018"` returns ≥1
    - `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json; echo $?` ends with 0
    - `test -f .planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-VALIDATION.md && echo OK` prints OK
    - `grep -c "8.1-VR-01" .planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-VALIDATION.md` returns ≥1
  </acceptance_criteria>
  <done>Automated sweep all green; 08.1-VALIDATION.md drafted with per-plan map + GREEN evidence + locked-file diffs all zero + CLI smoke passing; manual UAT section pending Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — 3 reproducer scripts from CONTEXT.md `<specifics>`</name>
  <what-built>
The full Phase 08.1 fix. Across 5 plans (08.1-01 through 08.1-05) the implementation:
1. Refactored `SerializableError` in `src/shared/types.ts` to a discriminated union — the `'SkeletonNotFoundOnLoadError'` arm carries 7 cached recovery fields (D-158).
2. Wired `handleProjectOpenFromPath` in `src/main/project-io.ts` to thread those fields from the already-materialized project at the SkeletonJsonNotFoundError rescue site (D-159).
3. Replaced the 8 hardcoded empty literals in `src/renderer/src/components/AppShell.tsx onClickOpen` with field reads from the typed envelope (D-160) — toolbar Open path now has parity with drag-drop.
4. Added a new `projectLoadFailed` AppState variant + recovery banner in `src/renderer/src/App.tsx` (D-161, D-162) so .stmproj drag-drop with a missing skeleton routes to a reachable Locate-skeleton + Dismiss UI.
5. Wired `DropZone.onBeforeDrop` via a useRef callback bridge — App.tsx holds the ref, AppShell registers its dirty-guard impl in a useEffect (D-163), `saveQuitDialogState` gained an optional `cancelAction` (D-164), SaveQuitDialog onCancel invokes it. Drop a new .json or .stmproj on a dirty session → SaveQuitDialog mounts with `'new-skeleton-drop'` / `'new-project-drop'` reason; Cancel aborts the drop, Don't Save proceeds.

Automated suite: ≥273 passed + 1 skipped + ≤1 todo. All 4 locked-file invariants intact (`scripts/cli.ts`, `src/core/sampler.ts`, `src/core/loader.ts`, `src/core/project-file.ts`). DropZone.tsx and SaveQuitDialog.tsx untouched (D-165).

Now exercise the 3 reproducer scripts from `.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-CONTEXT.md` `<specifics>` block to confirm the user-facing flows behave as designed.
  </what-built>
  <how-to-verify>
**Setup:** `npm run dev` to launch the app in dev mode. Keep the original `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` filename intact at start.

---

### Reproducer 1: VR-01 — .stmproj drag-drop with renamed skeleton

1. With the app running, drag `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` into the window. Verify the AppShell mounts (peak table renders CIRCLE/SQUARE/TRIANGLE/SQUARE2).
2. Double-click the TRIANGLE row's peak cell → OverrideDialog opens. Type `50`, press Enter (or click Apply). Confirm the TRIANGLE row shows the override badge.
3. Press Cmd+S. The Save dialog opens with default name `SIMPLE_TEST.stmproj`. Save to `fixtures/SIMPLE_PROJECT/repro-vr01.stmproj`.
4. Press Cmd+Q. The app closes (no SaveQuitDialog because the session is clean post-save).
5. **In a Finder window** (NOT the app), rename `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` to `fixtures/SIMPLE_PROJECT/SIMPLE_TEST_RENAMED.json`.
6. Relaunch the app: `npm run dev`.
7. **Drag** `fixtures/SIMPLE_PROJECT/repro-vr01.stmproj` into the window.
8. **EXPECTED:** App.tsx renders a banner with `role="alert"` showing **"Skeleton not found:"** + the loader error message + two buttons: **"Locate skeleton…"** and **"Dismiss"**. The banner is visually similar to the AppShell banner (warm-stone bg-panel + bg-danger 1px accent bar + danger-color "Skeleton not found:" prefix). NOT the generic muted-orange `text-accent-muted` error line. NOT a crashed window.
9. Click **"Locate skeleton…"**. A native file picker opens with the original `SIMPLE_TEST_RENAMED.json` directory pre-selected (or close to it). Pick `SIMPLE_TEST_RENAMED.json`.
10. **EXPECTED:** App transitions to `projectLoaded` state — AppShell mounts with the recovered project. The TRIANGLE override (50%) is restored (badge visible on the row). The filename chip shows `repro-vr01.stmproj`.
11. Press Cmd+Q to close. **Optional:** Test the "Dismiss" path by repeating steps 7-8 and clicking Dismiss instead — App returns to the empty drop-zone state.
12. **CLEANUP:** Rename `SIMPLE_TEST_RENAMED.json` BACK to `SIMPLE_TEST.json`. Delete `repro-vr01.stmproj`.

---

### Reproducer 2: VR-02 — Toolbar Open / Cmd+O with renamed skeleton

1. Launch the app. Drag `SIMPLE_TEST.json` to load. Set TRIANGLE override to 50%. Cmd+S to `repro-vr02.stmproj`.
2. Quit the app (Cmd+Q). Rename `SIMPLE_TEST.json` to `SIMPLE_TEST_RENAMED.json` in Finder.
3. Relaunch the app. Drag `SIMPLE_TEST.json` ... wait — there's no SIMPLE_TEST.json now. Use the existing app surface to test:
4. **First** load any existing skeleton (e.g. drag `fixtures/SIMPLE_PROJECT/SIMPLE_TEST_GHOST.json` if it exists, OR if you don't have a working skeleton, drag `SIMPLE_TEST_RENAMED.json` itself). The point: get AppShell mounted with SOME skeleton so the toolbar Open button is visible.
5. Click the **Open** button in the AppShell toolbar (top-right). The native picker opens.
6. Pick `repro-vr02.stmproj`.
7. **EXPECTED:** AppShell renders an inline banner (NOT App.tsx's banner — AppShell stays mounted) with `role="alert"`, "Skeleton not found:" prefix, "Locate skeleton…" + "Dismiss" buttons. Pre-fix bug: this banner shows but clicking "Locate skeleton…" rejects with `kind:'Unknown', message:'projectPath must be a .stmproj path'`.
8. Click "Locate skeleton…". File picker opens. Pick `SIMPLE_TEST_RENAMED.json`.
9. **EXPECTED:** AppShell remounts the recovered project (no full App.tsx remount — the filename chip changes to `repro-vr02.stmproj`, TRIANGLE override badge restored). NO error toast. NO console rejection. The reload IPC accepted because the threaded `projectPath` was non-empty.
10. **Alternative entry point:** Repeat steps 5-9 but use **Cmd+O** instead of clicking the Open button — same flow, same expected outcome.
11. **CLEANUP:** Rename `SIMPLE_TEST_RENAMED.json` back to `SIMPLE_TEST.json`. Delete `repro-vr02.stmproj`.

---

### Reproducer 3: VR-03 — Dirty + new-skeleton-drop / new-project-drop

**Part A — new-skeleton-drop arm:**

1. Launch the app. Drag `SIMPLE_TEST.json`. AppShell mounts.
2. Set TRIANGLE override to 50% (double-click peak → 50 → Enter). Confirm the dirty marker `• ` appears in the filename chip.
3. **Create a duplicate skeleton fixture for testing:** in Terminal, copy `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` to `fixtures/SIMPLE_PROJECT/SIMPLE_TEST_2.json` (same atlas + images siblings — the file:// references work because they're in the same dir). OR use any other valid skeleton you have on disk.
4. Drag `SIMPLE_TEST_2.json` (the new copy) into the window.
5. **EXPECTED:** SaveQuitDialog mounts with title **"Save changes before loading a new skeleton?"** + body mentioning the current basename (`Untitled` since you haven't saved). Three buttons: **Save**, **Don't Save**, **Cancel**.
6. Click **Cancel**. The dialog closes. **EXPECTED:** the original `SIMPLE_TEST.json` data + the TRIANGLE override are still in place. The new skeleton was NOT loaded. The filename chip still shows `Untitled • ` (dirty).
7. Drag `SIMPLE_TEST_2.json` again. SaveQuitDialog mounts again.
8. Click **Don't Save**. **EXPECTED:** dialog closes; the new skeleton loads; AppShell remounts with `SIMPLE_TEST_2.json` data; TRIANGLE override is GONE (the prior session's overrides Map died with the unmount per D-167). The filename chip shows `Untitled` (no dirty marker — no overrides yet on the new session).

**Part B — new-project-drop arm:**

9. With the fresh `SIMPLE_TEST_2` session, set TRIANGLE override to 50%. Cmd+S to save as `repro-vr03.stmproj`. Now the session is clean (lastSaved snapshot taken).
10. Set TRIANGLE override to 75% (overwrite). Filename chip shows `• repro-vr03.stmproj` (dirty again).
11. Drag a **different .stmproj file** into the window. (Create one if needed: load `SIMPLE_TEST.json`, save as `repro-vr03-other.stmproj`, then go back to step 9's session.) OR drag the same `repro-vr03.stmproj` — the dirty-guard fires regardless.
12. **EXPECTED:** SaveQuitDialog mounts with title **"Save changes before opening this project?"** (different from Part A's "...loading a new skeleton?" title). Three buttons.
13. Click **Save**. **EXPECTED:** the dialog briefly shows `Saving…` state; saveProject completes; dialog closes; the new .stmproj loads.
14. Repeat 11-12 but click **Cancel** — dialog closes, the dropped .stmproj does NOT load.
15. Repeat 11-12 but click **Don't Save** — dialog closes, the dropped .stmproj DOES load (overrides die with the unmount).
16. **CLEANUP:** Delete `SIMPLE_TEST_2.json`, `repro-vr03.stmproj`, `repro-vr03-other.stmproj`.

---

### Final checks (after all 3 reproducers signed off)

17. Verify ROADMAP.md and STATE.md updates land (Tasks 3-4 below depend on this).
18. Type **"approved"** in chat OR provide specific issue descriptions per reproducer.

If any reproducer fails: describe the deviation. The orchestrator will route to a gap-fix.
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 reproducers (VR-01, VR-02, VR-03 Parts A + B) pass, OR describe specific issues per reproducer.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: ROADMAP.md plan-checkbox flip — mark all 6 Phase 08.1 plans complete</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 299-312 (the Phase 08.1 block — the planner pass updated the Goal field per D-172, set Plans count to 6, and listed all 6 plans with `[ ]` checkboxes; this Task only flips them to `[x]`)
    - .planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-CONTEXT.md D-172 (confirms the Goal text already landed at planning time)
  </read_first>
  <action>
Edit `.planning/ROADMAP.md`. Locate the 6 lines in the Phase 08.1 block that match the pattern `^- \[ \] 08\.1-0[1-6]-PLAN\.md`. Replace each leading `- [ ]` with `- [x]`. Preserve the rest of each line byte-for-byte.

Use a precise sed-style edit OR an Edit tool call per line. The 6 lines to flip are at approximately ROADMAP.md:307-312 (the line numbers may have drifted by ±1 since the planner pass).

Do NOT touch:
- The Goal field at line 301 (already finalized per D-172 by the planner pass)
- The Requirements field at line 302
- The Plans count line at line 304 (already says "6 plans")
- Any line outside the Phase 08.1 block
- The `## Phase 9:` heading at the next section start
  </action>
  <verify>
    <automated>grep -cE "^- \\[x\\] 08\\.1-0[1-6]-PLAN\\.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^- \\[x\\] 08\\.1-0[1-6]-PLAN\\.md" .planning/ROADMAP.md` returns 6 (all 6 plan checkboxes flipped to [x])
    - `grep -cE "^- \\[ \\] 08\\.1-0[1-6]-PLAN\\.md" .planning/ROADMAP.md` returns 0 (no remaining unchecked Phase 08.1 plan boxes)
    - `grep -c "Close the 3 reachability gaps from Phase 8 verification" .planning/ROADMAP.md` returns 1 (D-172 Goal text — already landed by the planner pass; this Task does NOT modify it)
    - `grep -c "Plans:\\*\\* 6 plans" .planning/ROADMAP.md` returns 1 (already correct from the planner pass)
    - `git diff .planning/ROADMAP.md` shows ONLY 6 lines changed (each `- [ ]` → `- [x]` flip in the Phase 08.1 block)
  </acceptance_criteria>
  <done>All 6 Phase 08.1 plan checkboxes flipped from [ ] to [x]; Goal field and Plans count untouched (already final from the planner pass); diff scoped to the 6 checkbox lines only.</done>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md — advance to Phase 08.1 COMPLETE; add Last completed §08.1-06</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md lines 1-50 (the "Current position" + "Last completed" front-matter — the entry format)
    - .planning/STATE.md §08-05 entry (the formatting model for the new §08.1-06 entry)
    - .planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-VALIDATION.md (just generated by Task 1) — contains the data points to summarize in the new entry
  </read_first>
  <action>
Edit `.planning/STATE.md`. Apply 2 changes:

**Change 1 — Update the "Current position" / status header at the top of the file** (line 1-30 area). Specifically:
- Find the line that currently reads "Phase 8 COMPLETE" (or whatever Phase 8 close-out status text was used) and update to "Phase 08.1 COMPLETE; ready for `/gsd-verify-work 8`".
- Update any "Next" line to "Phase 9 (Complex-rig hardening + polish) — depends on the user-supplied complex rig per ROADMAP.md".

**Change 2 — Add a new "Last completed" entry as the FIRST entry under the existing §08-05 entry** (so §08.1-06 appears at the top of the chronological list — most recent first). Insert this verbatim:

```markdown
- **Plan 08.1-06 (2026-04-26):** Close-out for Phase 08.1 (gap-closure for VR-01/VR-02/VR-03 from 08-VERIFICATION.md). Across 6 plans the implementation: (1) refactored SerializableError to a discriminated union with the SkeletonNotFoundOnLoadError arm carrying 7 cached recovery fields (D-158); (2) wired handleProjectOpenFromPath to thread those fields at the SkeletonJsonNotFoundError rescue (D-159); (3) replaced 8 hardcoded empty literals in AppShell.onClickOpen with field reads from the typed envelope (D-160); (4) added the projectLoadFailed AppState variant + recovery banner in App.tsx so .stmproj drag-drop with a missing skeleton routes to a reachable Locate/Dismiss UI (D-161/D-162); (5) wired DropZone.onBeforeDrop via a useRef callback bridge — App.tsx holds the ref, AppShell registers its dirty-guard impl in a useEffect, saveQuitDialogState gained optional cancelAction, SaveQuitDialog.onCancel invokes it (D-163/D-164/D-167). Automated sweep green: full vitest **≥273 passed + 1 skipped + ≤1 todo** (was 270 + 1 + 3 at Phase 8 close; +5 new GREEN: 8.1-VR-01, 8.1-VR-02, 8.1-VR-03a, 8.1-VR-03b, 8.1-IPC-01; 2 prior it.todo deleted). Both typecheck projects clean (only pre-existing scripts/probe-per-anim.ts deferred TS2339 — same class as Phase 8 close). `npx electron-vite build` green; bundle deltas <2 kB across main + preload + renderer. `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` byte-for-byte unchanged (CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000, exit 0). 4 locked-file diffs all 0 lines (scripts/cli.ts, src/core/sampler.ts, src/core/loader.ts, src/core/project-file.ts) — D-170 + Phase 5 D-102 + CLAUDE.md rule #3 + D-171 all preserved. SaveQuitDialog.tsx and DropZone.tsx UNTOUCHED across the full phase (D-165 lock). Layer 3 invariant intact: arch.spec auto-scans all renderer files; no core imports in App.tsx or AppShell.tsx. Manual UAT signed off 2026-04-26 across all 3 reproducer scripts: (1) VR-01 — .stmproj drag-drop with renamed skeleton → App.tsx recovery banner with role=alert + Locate skeleton + Dismiss buttons reachable; Locate→pick→reload chain restores the project with overrides intact; (2) VR-02 — toolbar Open / Cmd+O with renamed skeleton → AppShell inline banner with non-empty projectPath; Locate→pick→reload chain succeeds without rejection; (3) VR-03 — dirty + new-skeleton-drop / new-project-drop both arms: SaveQuitDialog mounts with the correct title per reason, Cancel aborts the drop preserving overrides, Don't Save proceeds and discards overrides via unmount, Save saves-then-proceeds. Phase 08.1 COMPLETE — ready for `/gsd-verify-work 8` re-run and Phase 9.
```

After saving, do not modify any other §0X-NN entries.
  </action>
  <verify>
    <automated>grep -c "Plan 08.1-06" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Plan 08.1-06" .planning/STATE.md` returns 1 (the new entry)
    - `grep -c "Phase 08.1 COMPLETE" .planning/STATE.md` returns ≥1 (the status header AND the entry both reference completion)
    - `grep -c "/gsd-verify-work 8" .planning/STATE.md` returns ≥1 (the readiness marker)
    - `grep -c "8.1-VR-01\|8.1-VR-02\|8.1-VR-03a\|8.1-VR-03b\|8.1-IPC-01" .planning/STATE.md` returns ≥1 (the spec IDs are recorded)
    - `grep -c "D-158\|D-159\|D-160\|D-161\|D-162\|D-163\|D-164\|D-165\|D-170\|D-171\|D-172" .planning/STATE.md` returns ≥1 (the decision IDs are referenced)
    - The pre-existing §08-05 entry still exists: `grep -c "Plan 08-05" .planning/STATE.md` returns 1
    - The pre-existing Phase 8.1 INSERTED note at line 187 still exists: `grep -c "Phase 8.1 inserted after Phase 8" .planning/STATE.md` returns 1
  </acceptance_criteria>
  <done>STATE.md status header advanced to Phase 08.1 COMPLETE; new §08.1-06 entry summarizes the close-out with all 5 spec IDs + 11 decision IDs + 3-reproducer UAT signoff; existing entries untouched.</done>
</task>

</tasks>

<verification>
**Phase boundary checks for Plan 08.1-06:**

1. Automated sweep all green: vitest ≥273 passed, typecheck clean, electron-vite build green
2. CLI byte-for-byte unchanged: CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000 exit 0
3. All 4 named locked-file diffs return 0 (scripts/cli.ts, src/core/sampler.ts, src/core/loader.ts, src/core/project-file.ts)
4. DropZone.tsx and SaveQuitDialog.tsx diff vs base 552389e returns 0 (D-165 + 8.1 invariants)
5. 08.1-VALIDATION.md created and signed-off
6. ROADMAP.md Phase 08.1 block updated per D-172 with all 6 plan checkboxes flipped
7. STATE.md status advanced; new §08.1-06 entry added
8. Manual UAT signed off across all 3 reproducer scripts
9. All 5 spec IDs (8.1-VR-01, VR-02, VR-03a, VR-03b, IPC-01) confirmed GREEN
10. Test baseline expectation met: 270 → ≥273 passed (CONTEXT D-166)
</verification>

<success_criteria>
- All automated gates green
- 3-reproducer manual UAT signed off
- 08.1-VALIDATION.md exists with per-plan map + per-spec evidence + locked-file invariants table
- ROADMAP.md Phase 08.1 closed (D-172 Goal, 6 [x] checkboxes)
- STATE.md advanced (Phase 08.1 COMPLETE; ready for /gsd-verify-work 8)
- Phase 9 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-06-SUMMARY.md` summarizing:
- Automated sweep results (test counts, build sizes, CLI smoke)
- Locked-file invariant audit (6 files, all 0 lines diff)
- Manual UAT signoff (3 reproducers)
- ROADMAP.md + STATE.md updates committed
- Final commit message recommended: `docs(08.1): close Phase 08.1 after manual UAT signoff`

Then commit per close-out convention:
```
git add .planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-VALIDATION.md \
        .planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-06-SUMMARY.md \
        .planning/ROADMAP.md \
        .planning/STATE.md
git commit -m "docs(08.1): close Phase 08.1 after manual UAT signoff"
```
</output>
