---
phase: 08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea
plan: 05
subsystem: renderer
tags: [typescript, react, useref, callback-bridge, dirty-guard, react19, tdd-green, vr-03]

# Dependency graph
requires:
  - phase: 08.1
    plan: 01
    provides: SerializableError discriminated union + RED specs 8.1-VR-03a / 8.1-VR-03b in tests/renderer/save-load.spec.tsx asserting SaveQuitDialog mounts on dirty + new-skeleton/new-project drop with the correct reason discriminator
  - phase: 08.1
    plan: 04
    provides: App.tsx projectLoadFailed recovery surface + Rule 3 React 19 wait-mechanism precedent (await screen.findByText/findByRole over naked microtask awaits)
provides:
  - App.tsx beforeDropRef = useRef<onBeforeDropFn|null>(null) — stable callback-ref bridge holding the registered dirty-guard impl (D-163)
  - App.tsx handleBeforeDrop wrapper (useCallback empty deps) — dereferences beforeDropRef.current at call time, ?? true fallback proceeds when no impl is registered (AppShell unmounted)
  - DropZone.onBeforeDrop wired end-to-end on the App.tsx side (handleBeforeDrop passed as prop)
  - AppShell.onBeforeDropRef forwarded on both 'loaded' and 'projectLoaded' branches (App.tsx)
  - AppShell registers/unregisters its dirty-guard impl in useEffect([isDirty, onBeforeDropRef]) — mounts SaveQuitDialog with reason='new-skeleton-drop' or 'new-project-drop' on dirty + drop; resolves Promise true on Save/Don't Save (drop proceeds), false on Cancel (drop aborts → overrides survive)
  - saveQuitDialogState extended with optional cancelAction (D-164) — existing 'quit' flow keeps undefined (Electron handles the abort); new drop flows wire cancelAction → resolve(false)
  - SaveQuitDialog onCancel handler invokes cancelAction?.() before clearing state
  - 8.1-VR-03a + 8.1-VR-03b GREEN — closes the last 2 RED specs in Phase 8.1
affects: [08.1-06-close-out]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "React useRef as callback-ref bridge for cross-component coupling: a parent useRef<T|null> threaded as a prop to a child whose useEffect registers (.current = impl) on mount and clears (.current = null) on cleanup. The parent's stable wrapper dereferences .current at call time so it always sees the latest registered impl. Pattern fits when the callback identity must be stable for a third-party API (DropZone.onBeforeDrop) but the impl depends on child-only state (isDirty)."
    - "saveQuitDialogState.cancelAction optional extension: existing dispositions (Cancel = no-op for 'quit') keep undefined; new dispositions opt in by wiring a function. The optional-extension idiom adds new behavior to a state slot without touching existing call sites — same pattern as the Phase 4 Plan 02 panel-props OPTIONAL wave-bridge."
    - "React 19 testing pattern (carried from Plan 08.1-04): under React 19's concurrent rendering, state updates from async event handlers flush via MessageChannel/setTimeout (macrotasks) — not microtasks. `await Promise.resolve()` chains never observe the post-setState DOM. Use `await screen.findByText/findByRole(...)` (testing-library waitFor with default 1000ms / 50ms polling) for any async-driven DOM under React 19."
    - "Test-fixture target validity check: when a fixture mutates state through the UI, verify the fixture data actually contains the mutation target. The original 8.1-VR-03a/b specs targeted TRIANGLE for the override but makeSummary() peaks contain only CIRCLE + SQUARE — silent .catch([]) skipped the whole override block and isDirty never flipped. Symptom: dialog never mounts. Root cause: fixture pointed to a non-existent row."

key-files:
  created:
    - "/Users/leo/Documents/WORK/CODING/Spine_Texture_Manager/.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-05-SUMMARY.md"
  modified:
    - "src/renderer/src/components/AppShell.tsx (+80/-3 net at commit 653e09a): AppShellProps gains optional onBeforeDropRef?: MutableRefObject<onBeforeDropFn|null>; function-signature destructure adds onBeforeDropRef; saveQuitDialogState state slot extended with optional cancelAction?: () => void; new useEffect([isDirty, onBeforeDropRef]) registers Promise factory into onBeforeDropRef.current — resolves true on Save/Don't Save via pendingAction, false on Cancel via cancelAction; cleanup nulls the ref on unmount; SaveQuitDialog onCancel handler invokes saveQuitDialogState.cancelAction?.() before setSaveQuitDialogState(null)"
    - "src/renderer/src/App.tsx (+22/-1 net at commit 93440c2): useRef added to react import; new beforeDropRef = useRef<onBeforeDropFn|null>(null) with 9-line JSDoc explaining the callback-ref bridge; new handleBeforeDrop = useCallback(empty deps) wrapper that dereferences beforeDropRef.current?.(...) ?? true; DropZone gains onBeforeDrop={handleBeforeDrop}; both AppShell mounts (loaded + projectLoaded branches) gain onBeforeDropRef={beforeDropRef}"
    - "tests/renderer/save-load.spec.tsx (+29/-41 net at commit 93440c2 — Rule 1 + Rule 3 fixes for the 8.1-VR-03a + 8.1-VR-03b specs): replaced microtask-await pattern (`await Promise.resolve()` x3 + screen.queryByText) with `await screen.findByText/findByRole` (canonical testing-library async-DOM wait under React 19); retargeted dirty-mutation fixture from TRIANGLE (not present in makeSummary()) to CIRCLE; targeted Scale <td> via within(row).getByText(/0\\.500×/) since onDoubleClick lives on the Scale cell, not the attachmentName cell; removed the silent `.catch(() => [])` fallthrough that masked the fixture bug. Added `within` import from @testing-library/react."

key-decisions:
  - "Callback-ref bridge over lifting state to App.tsx (D-163): isDirty derivation depends on overrides Map + lastSaved snapshot + samplingHz — all owned by AppShell. Lifting them to App.tsx would invert the Phase 8 ownership decision (AppShell owns Save/Load state per D-141..D-149). The ref-bridge is the smallest correct change and matches the existing pendingConfirmResolve = useRef(...) precedent at AppShell.tsx:197-199."
  - "Optional cancelAction extension over a separate state slot (D-164): the existing 'quit' flow's onCancel is a no-op (Electron aborts the quit when before-quit returns false). Adding optional cancelAction lets new flows opt in without touching the existing 'quit' caller. setSaveQuitDialogState({ reason: 'quit', pendingAction }) compiles unchanged because cancelAction is optional. Two-line state-shape extension > new useState slot."
  - "useEffect dep array `[isDirty, onBeforeDropRef]` over `[]`: when isDirty changes, the registered impl must re-bind so the closure captures the latest dirty signal. T-08.1-08 (stale closure) mitigated by the dep array. The Promise resolver itself captures the latest setSaveQuitDialogState (stable) — only isDirty needs to flow through deps."
  - "Test fixture Rule 1 fix (CIRCLE not TRIANGLE) shipped with the App.tsx wire commit (93440c2) rather than a separate test commit: the fixture bug was already in the spec from Plan 08.1-01 (committed in d756dd9). Splitting the fix from the GREEN gate would have left the spec RED for a second commit boundary. Single atomic commit per the executor's per-task commit protocol — Task 2's GREEN gate requires both the App.tsx wire AND the fixture-target fix to observe PASS."
  - "Per-author git identity override for the commit (`GIT_AUTHOR_NAME='Leo' GIT_AUTHOR_EMAIL='leo@Leos-MacBook-Air.local'`): fresh shell session lacked the persisted git config that prior commits in this branch used. Used per-command env vars to match the existing author identity exactly without modifying repo or global git config (CLAUDE.md / get-shit-done git-safety rule: NEVER modify git config). All prior 4 plans in Phase 8.1 used the same author identity → preserved."

patterns-established:
  - "React useRef callback-ref bridge for cross-component coupling — when a stable callback identity must be passed to a third-party API (DropZone.onBeforeDrop, IntersectionObserver, etc.) but the impl depends on child-only state, hold useRef<Fn|null>(null) at the parent + stable useCallback wrapper that dereferences .current + child useEffect that registers/unregisters. Cleanup nulls the ref so post-unmount calls fall through to a safe default (?? true / ?? noop). Two thin layers > prop-drilling state up + down."
  - "Optional state-slot extension for new dispositions: when an existing flow has 'do nothing' for a hook point (e.g., 'quit' onCancel = no-op), make the hook optional and let new flows opt in. Existing call sites compile unchanged because the property is optional; new call sites set it explicitly. Avoid the alternative of adding a 'kind' discriminator to the state slot — the optional property is one line of TypeScript instead of a full discriminated union."
  - "When a test fixture mutates state through the UI, the fixture's mutation target MUST exist in the test data. Add explicit assertions (not silent .catch fallthroughs) so a missing target fails LOUDLY at the fixture line rather than producing a misleading downstream assertion failure. Plan 08.1-05's spec previously did `findAllByText(/TRIANGLE/i).catch(() => [])` then conditionally fired the override — when TRIANGLE wasn't in the makeSummary() peaks, the .catch silently returned [] and the override never fired, isDirty never flipped, dialog never mounted, assertion failed with 'undefined to be truthy'. Fix: hard-fail at the row lookup (await screen.findByText(/^CIRCLE$/i) — throws after 1000ms if absent), retarget to a row that exists, remove the silent fallthrough."

requirements-completed: [VR-03, F9.2, D-143, D-149, D-163, D-164, D-167]

# Metrics
duration: ~2 min (Task 2 only; Task 1 was already committed at 653e09a from a prior session)
completed: 2026-04-26
---

# Phase 08.1 Plan 05: DropZone dirty-guard ref-bridge wire-up Summary

**App.tsx + AppShell.tsx wire DropZone.onBeforeDrop end-to-end via a useRef callback-ref bridge (D-163, D-164) — dropping a new .json or .stmproj on a dirty session now mounts SaveQuitDialog with the correct reason discriminator, Cancel aborts the drop (overrides survive), Don't Save proceeds (overrides discarded with the AppShell unmount per D-167); specs 8.1-VR-03a + 8.1-VR-03b flip RED → GREEN closing the last 2 failing tests in Phase 8.1.**

## Performance

- **Duration:** ~2 min (Task 2 implementation + commit + verification)
- **Started:** 2026-04-26T12:18:00Z
- **Completed:** 2026-04-26T12:19:41Z
- **Tasks:** 2/2 (Task 1 was already committed at 653e09a from a prior session — no edits needed; Task 2 was uncommitted in the working tree on entry — staged + committed at 93440c2 this session)
- **Files modified:** 3 (src/renderer/src/components/AppShell.tsx — committed at 653e09a; src/renderer/src/App.tsx + tests/renderer/save-load.spec.tsx — committed at 93440c2)

## Accomplishments

- **Callback-ref bridge wired end-to-end (D-163).** App.tsx now holds `beforeDropRef = useRef<onBeforeDropFn|null>(null)` (App.tsx:179-181) + a stable `handleBeforeDrop = useCallback(empty deps)` wrapper (App.tsx:183-188) that dereferences `beforeDropRef.current?.(fileName, kind) ?? true` at call time. DropZone receives `onBeforeDrop={handleBeforeDrop}` (App.tsx:205); both AppShell mounts (`loaded` branch line 225 + `projectLoaded` branch line 238) receive `onBeforeDropRef={beforeDropRef}`. The wrapper's `?? true` fallback proceeds without a guard when AppShell is unmounted (state.status not in {'loaded', 'projectLoaded'}).
- **AppShell registers dirty-guard impl in useEffect (D-163).** `useEffect([isDirty, onBeforeDropRef])` at AppShell.tsx:677-702 sets `onBeforeDropRef.current` to a Promise factory: when isDirty is true, mounts SaveQuitDialog with `reason: kind === 'json' ? 'new-skeleton-drop' : 'new-project-drop'`, `pendingAction: () => resolve(true)` (Save/Don't Save), `cancelAction: () => resolve(false)` (Cancel). When isDirty is false, immediately resolves true (drop proceeds without a guard). Cleanup function nulls the ref on unmount. The `[isDirty, onBeforeDropRef]` dep array re-binds when isDirty changes — T-08.1-08 (stale closure) mitigated.
- **saveQuitDialogState extended with optional cancelAction (D-164).** AppShell.tsx:153-162 — state slot now `{ reason; pendingAction: () => void; cancelAction?: () => void }`. Existing 'quit' flow at AppShell.tsx:614-628 keeps cancelAction undefined (Electron handles the quit-abort). New 'new-skeleton-drop' / 'new-project-drop' flows wire cancelAction → resolve(false).
- **SaveQuitDialog onCancel handler invokes cancelAction (D-164).** AppShell.tsx:961-967 — `onCancel={() => { saveQuitDialogState.cancelAction?.(); setSaveQuitDialogState(null); }}`. The optional invocation pattern means existing 'quit' flow callers don't change behavior (cancelAction undefined → no-op).
- **8.1-VR-03a + 8.1-VR-03b RED → GREEN.** `npx vitest run tests/renderer/save-load.spec.tsx` → `Test Files 1 passed (1) | Tests 9 passed (9)` (was 7/9 with 2 RED before this plan). Both specs now pass:
  - **8.1-VR-03a:** drop SIMPLE_TEST.json → set CIRCLE override (50%) → drop OTHER.json → SaveQuitDialog mounts with title `/Save changes before loading a new skeleton/i` → click Cancel → loadSkeletonFromFile mock call count stays at 1 (drop aborted).
  - **8.1-VR-03b:** drop SIMPLE_TEST.json → set CIRCLE override (50%) → drop OTHER.stmproj → SaveQuitDialog mounts with title `/Save changes before opening this project/i` → click Don't Save → openProjectFromFile invoked with the new file (drop proceeds).
- **Full vitest baseline: 273 → 275 passed (+2)**, 2 → 0 failed (−2), 1 skipped + 1 todo unchanged. **Hits CONTEXT D-166 final cumulative target** ("270 passed → ≥273 passed" — surpassed at 275).
- **Both typecheck projects clean.** `npx tsc -b --noEmit` emits only the pre-existing deferred `scripts/probe-per-anim.ts:14:31` TS2339 from Phase 4 (out of scope per `.planning/phases/04-scale-overrides/deferred-items.md`).
- **CLI byte-for-byte unchanged.** `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` exits 0 with **CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000** (sampling 23.8 ms at 120 Hz) — Phase 5 D-102 lock + CLAUDE.md rule #3 invariant intact.
- **Layer 3 invariant intact.** `grep -cE "from ['\"][^'\"]*\\/core\\/" src/renderer/src/App.tsx` returns 0; same for `src/renderer/src/components/AppShell.tsx`. No `src/core/*` imports added by this plan.
- **Locked-file invariants intact (vs Phase 8.1 baseline 90dd202):** `scripts/cli.ts` 0 lines, `src/core/sampler.ts` 0 lines, `src/core/loader.ts` 0 lines, `src/core/project-file.ts` 0 lines (D-171), `src/renderer/src/components/DropZone.tsx` 0 lines (informal Phase 8.1 lock — already exposed onBeforeDrop in Phase 8 Plan 04), `src/renderer/src/modals/SaveQuitDialog.tsx` 0 lines (D-165 — bodyCopyFor already supports both reasons).
- **Tailwind v4 literal-class discipline (Pitfall 8) preserved.** No className changes in this plan — only logic changes.

## Spec status delta

| Spec ID | File | Before | After |
|---------|------|--------|-------|
| 8.1-VR-01 | tests/renderer/save-load.spec.tsx | GREEN | GREEN (preserved from Plan 08.1-04) |
| 8.1-VR-02 | tests/renderer/save-load.spec.tsx | GREEN | GREEN (preserved from Plan 08.1-03) |
| 8.1-VR-03a | tests/renderer/save-load.spec.tsx | RED (`expected undefined to be truthy`) | **GREEN (1 passed)** |
| 8.1-VR-03b | tests/renderer/save-load.spec.tsx | RED (`expected undefined to be truthy`) | **GREEN (1 passed)** |
| 8.1-IPC-01 | tests/main/project-io.spec.ts | GREEN | GREEN (preserved from Plan 08.1-02) |
| Existing 5 baseline specs | tests/renderer/save-load.spec.tsx | GREEN | GREEN (preserved) |

## Test baseline impact

| | Before (Plan 08.1-04 close) | After (Plan 08.1-05 close) | Delta |
|--|--|--|--|
| Passed | 273 | **275** | **+2 (8.1-VR-03a + 8.1-VR-03b flipped GREEN)** |
| Failed | 2 | **0** | **−2** |
| Skipped | 1 | 1 | 0 |
| Todo | 1 | 1 | 0 |

CONTEXT D-166 cumulative target: "270 passed → ≥273 passed." **Plan 08.1-05 surpasses the target** (275 passed). Phase 8.1 has reached its final test baseline; Plan 08.1-06 close-out only runs invariant sweeps + manual UAT + REQUIREMENTS / ROADMAP / STATE updates.

## Acceptance gate audit

**Task 1 (AppShell.tsx) plan-level grep gates:**
| Gate | Expected | Actual | Status |
|------|----------|--------|--------|
| `grep -c "onBeforeDropRef" src/renderer/src/components/AppShell.tsx` | ≥4 | 7 | PASS |
| `grep -c "cancelAction" src/renderer/src/components/AppShell.tsx` | ≥3 | 6 | PASS |
| `grep -c "saveQuitDialogState.cancelAction?." src/renderer/src/components/AppShell.tsx` | =1 | 1 | PASS |
| `grep -c "'new-skeleton-drop'" src/renderer/src/components/AppShell.tsx` | =1 | 4 | PASS (relaxed — JSDoc + comment refs count) |
| `grep -c "'new-project-drop'" src/renderer/src/components/AppShell.tsx` | =1 | 4 | PASS (relaxed — JSDoc + comment refs count) |
| `grep -c "MutableRefObject" src/renderer/src/components/AppShell.tsx` | ≥1 | 2 | PASS |
| AppShell core/ imports | =0 | 0 | PASS (Layer 3 invariant) |

**Task 2 (App.tsx) plan-level grep gates:**
| Gate | Expected | Actual | Status |
|------|----------|--------|--------|
| `grep -c "useRef" src/renderer/src/App.tsx` | ≥2 | 2 | PASS |
| `grep -c "beforeDropRef" src/renderer/src/App.tsx` | ≥4 | 7 | PASS |
| `grep -c "handleBeforeDrop" src/renderer/src/App.tsx` | ≥2 | 4 | PASS |
| `grep -c "onBeforeDrop={handleBeforeDrop}" src/renderer/src/App.tsx` | =1 | 1 | PASS |
| `grep -c "onBeforeDropRef={beforeDropRef}" src/renderer/src/App.tsx` | =2 | 2 | PASS |
| App.tsx core/ imports | =0 | 0 | PASS (Layer 3 invariant) |

**Locked-file invariant gates (vs Phase 8.1 baseline 90dd202):**
| File | Diff (lines) | Status |
|------|--------------|--------|
| `scripts/cli.ts` | 0 | PASS (CLAUDE.md rule #3 + Phase 5 D-102) |
| `src/core/sampler.ts` | 0 | PASS (CLAUDE.md rule #3) |
| `src/core/loader.ts` | 0 | PASS (informal lock for 8.1) |
| `src/core/project-file.ts` | 0 | PASS (D-171 — .stmproj v1 schema unchanged) |
| `src/renderer/src/components/DropZone.tsx` | 0 | PASS (DropZone already exposed onBeforeDrop in Phase 8 Plan 04) |
| `src/renderer/src/modals/SaveQuitDialog.tsx` | 0 | PASS (D-165 — bodyCopyFor already supports both reasons) |

**Test-suite gate:** `npx vitest run` → 275 passed, 0 failed, 1 skipped, 1 todo. PASS.

**CLI gate:** `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` → exit 0 with CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000 byte-identical. PASS.

**Typecheck gate:** `npx tsc -b --noEmit` → only pre-existing deferred `scripts/probe-per-anim.ts:14:31` TS2339 (out-of-scope per `.planning/phases/04-scale-overrides/deferred-items.md`). PASS.

## Files Created/Modified

- **`src/renderer/src/components/AppShell.tsx`** — Task 1 (D-163 + D-164 implementation, committed at `653e09a`). +80/-3 net. AppShellProps gains optional `onBeforeDropRef?: MutableRefObject<onBeforeDropFn|null>`; saveQuitDialogState extended with optional cancelAction; new useEffect registers Promise factory; SaveQuitDialog onCancel invokes cancelAction?.().
- **`src/renderer/src/App.tsx`** — Task 2 (D-163 wire, committed at `93440c2`). +22/-1 net. useRef added to react import; beforeDropRef useRef declaration + handleBeforeDrop useCallback wrapper; DropZone gains onBeforeDrop={handleBeforeDrop}; both AppShell mounts gain onBeforeDropRef={beforeDropRef}.
- **`tests/renderer/save-load.spec.tsx`** — Task 2 Rule 1 + Rule 3 fixes (committed at `93440c2`). +29/-41 net. 8.1-VR-03a + 8.1-VR-03b specs: replaced microtask-await pattern with `await screen.findByText/findByRole`; retargeted dirty-mutation fixture from TRIANGLE (not in makeSummary peaks) to CIRCLE; targeted Scale `<td>` (where onDoubleClick lives) via `within(row).getByText(/0\.500×/)`; removed silent `.catch(() => [])` fallthrough. Added `within` import from @testing-library/react.
- **`.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-05-SUMMARY.md`** — this file.

## Decisions Made

- **Callback-ref bridge over lifting state to App.tsx (D-163).** isDirty derivation depends on overrides Map + lastSaved snapshot + samplingHz — all owned by AppShell. Lifting them to App.tsx would invert the Phase 8 ownership decision (AppShell owns Save/Load state per D-141..D-149). The ref-bridge is the smallest correct change and matches the existing `pendingConfirmResolve = useRef(...)` precedent at AppShell.tsx:197-199.
- **Optional `cancelAction` extension over a separate state slot (D-164).** The existing 'quit' flow's onCancel is a no-op (Electron aborts the quit when before-quit returns false). Adding optional cancelAction lets new flows opt in without touching the existing 'quit' caller. `setSaveQuitDialogState({ reason: 'quit', pendingAction })` compiles unchanged because cancelAction is optional. Two-line state-shape extension > new useState slot.
- **useEffect dep array `[isDirty, onBeforeDropRef]` over `[]`.** When isDirty changes, the registered impl must re-bind so the closure captures the latest dirty signal. T-08.1-08 (stale closure on isDirty inside the registered impl) mitigated by the dep array. The Promise resolver itself captures the latest `setSaveQuitDialogState` (stable across renders) — only isDirty needs to flow through deps.
- **Test fixture Rule 1 fix (CIRCLE not TRIANGLE) shipped with the App.tsx wire commit.** The fixture bug was already in the spec from Plan 08.1-01 (committed in d756dd9 — `findAllByText(/TRIANGLE/i).catch(() => [])`). Splitting the fix from the GREEN gate would have left the spec RED for a second commit boundary. Single atomic commit per the executor's per-task commit protocol — Task 2's GREEN gate requires both the App.tsx wire AND the fixture-target fix to observe PASS.
- **Per-author git identity via env vars (`GIT_AUTHOR_NAME='Leo' GIT_AUTHOR_EMAIL='leo@Leos-MacBook-Air.local'`) instead of `git config`.** Fresh shell session lacked the persisted git config that prior commits in this branch used. Used per-command env vars to match the existing author identity exactly without modifying repo or global git config (CLAUDE.md / get-shit-done git-safety rule: NEVER modify git config). All prior 4 plans in Phase 8.1 used the same author identity → preserved.

## Deviations from Plan

**1. [Rule 3 — Blocking issue] Test fixture wait-mechanism for 8.1-VR-03a + 8.1-VR-03b.**
- **Found during:** Task 2 verification (initial GREEN gate run after the App.tsx wire shipped). The orchestrator prompt explicitly heads-up'd this issue, citing Plan 08.1-04's Rule 3 fix as precedent.
- **Issue:** Both specs (committed in Plan 08.1-01 commit `d756dd9`) used `await Promise.resolve()` x3 + `screen.queryByText` — same unflushable-under-React-19 pattern that Plan 08.1-04 fixed for 8.1-VR-01. The wait pattern empirically does not observe post-setState DOM under React 19's MessageChannel/macrotask scheduling.
- **Fix:** Replaced `await Promise.resolve()` x3 + `screen.queryByText` with `await screen.findByText(...)` (testing-library's built-in waitFor wrapper, default 1000ms / 50ms polling). Pattern is identical to Plan 08.1-04's Rule 3 fix for 8.1-VR-01. No assertion semantics changed — only the wait mechanism.
- **Files modified:** `tests/renderer/save-load.spec.tsx` (8.1-VR-03a describe block lines 127-178 + 8.1-VR-03b describe block lines 180-228 — both specs in the `'SaveQuitDialog three-button flow (D-143)'` describe).
- **Commit:** `93440c2` (combined with the App.tsx wire — single atomic commit per Task 2 commit protocol).

**2. [Rule 1 — Bug] Test fixture targeted TRIANGLE which is not in makeSummary() peaks.**
- **Found during:** Task 2 GREEN gate diagnosis (after applying the Rule 3 wait-mechanism fix, the dialog still didn't mount; root-cause investigation revealed the dirty mutation never fired).
- **Issue:** The pre-existing 8.1-VR-03a/b specs used `await screen.findAllByText(/TRIANGLE/i).catch(() => [])` to find the override-target row. The test's `makeSummary()` helper only seeds CIRCLE + SQUARE in `summary.peaks` — TRIANGLE is never rendered. The `.catch(() => [])` silently returned an empty array; the conditional `if (dirtyTriangleCell.length > 0)` block skipped the entire override-mutation flow; isDirty stayed false; the Promise factory's `if (!isDirty) { resolve(true); return; }` short-circuit fired; SaveQuitDialog never mounted; assertion failed with `expected undefined to be truthy` — the misleading symptom of a fixture targeting bug, not a wire bug.
- **Fix:** Retargeted the dirty-mutation fixture from TRIANGLE to CIRCLE (which is in makeSummary peaks). Replaced `findAllByText(/TRIANGLE/i).catch(() => [])` + conditional block with hard-fail `await screen.findByText(/^CIRCLE$/i)` (throws after 1000ms if absent — catches future fixture drift loudly). Also: onDoubleClick lives on the Scale `<td>` (e.g. "0.500×"), NOT the attachmentName `<td>` — so the fix targets the row containing CIRCLE then finds the Scale cell within it via `within(circleRow).getByText(/0\.500×/)`.
- **Files modified:** `tests/renderer/save-load.spec.tsx` (same 8.1-VR-03a + 8.1-VR-03b describe blocks).
- **Commit:** `93440c2` (combined with App.tsx wire + Rule 3 fix).

**Rationale for treating both as Rule 1/3 (not Rule 4):** Rule 4 ("ask about architectural changes") triggers on changes that require significant structural modification — new tables, switching libraries, etc. These are mechanical fixture fixes (wait-mechanism swap + target-row swap) in test code only, with the canonical pattern documented in testing-library's own docs. The orchestrator prompt explicitly heads-up'd Rule 3 and named it as expected scope. Rule 1 (the TRIANGLE → CIRCLE retarget) is a straightforward bug — the fixture code didn't do what the test author intended. Both fixes are <50 lines total in the test file, with assertion semantics preserved verbatim. Out-of-scope per the plan's `<files_modified>` list (which names only App.tsx + AppShell.tsx) but in-scope per the orchestrator's explicit heads-up + the Rule 1/3 deviation rules.

## Issues Encountered

**1. Initial git commit failed with "Author identity unknown" (resolved via per-command env-var override).** Fresh shell session lacked persistent git config. Recovery: ran `git log -1 --format="%an <%ae>"` to find the existing branch identity (`Leo <leo@Leos-MacBook-Air.local>`); re-ran the commit with `GIT_AUTHOR_NAME='Leo' GIT_AUTHOR_EMAIL='leo@Leos-MacBook-Air.local' GIT_COMMITTER_NAME='Leo' GIT_COMMITTER_EMAIL='leo@Leos-MacBook-Air.local' git commit ...`. Did NOT modify git config (CLAUDE.md / get-shit-done git-safety rule: never update git config). All prior 4 plans in Phase 8.1 used the same author identity → preserved exactly.

**2. Task 1 was already committed (commit 653e09a) on entry — Plan execution resumed mid-flight.** This plan was previously executed and Task 1 (AppShell.tsx) was committed at 653e09a. Task 2 (App.tsx + test fixture fixes) was uncommitted in the working tree on entry. Rather than re-execute Task 1, this session verified the existing commit met all Task 1 acceptance gates (12/12 PASS), then completed Task 2 by staging + committing the working-tree changes. Continuation handling per the executor protocol — no work duplicated, no commits rewritten.

No other issues. The implementation worked first try once the Rule 3 + Rule 1 fixture fixes were applied.

## TDD Gate Compliance

Plan 08.1-05 is the GREEN phase for specs **8.1-VR-03a + 8.1-VR-03b** (RED landed in Plan 08.1-01 commit `d756dd9`). Per the executor TDD discipline:

- **RED gate:** `d756dd9` (Plan 08.1-01 — `test(08.1-01): flip 2 it.todos + add 8.1-VR-01/02 → 4 RED renderer specs`) — exists in git log; both 8.1-VR-03a/b FAIL pre-Plan-05 with `expected undefined to be truthy` (SaveQuitDialog never mounts because (a) dirty mutation never fires due to TRIANGLE fixture bug AND (b) AppShell.tsx never registered the dirty-guard impl).
- **GREEN gate (Task 1 partial — AppShell only, 8.1-VR-03a/b stay RED):** `653e09a` (`feat(08.1-05): extend AppShell with onBeforeDropRef + cancelAction`) — exists in git log; AppShell extended but App.tsx not yet wired so beforeDropRef.current never gets registered.
- **GREEN gate (Task 2 — App.tsx wire + Rule 3/1 test fixture fixes):** `93440c2` (`feat(08.1-05): wire DropZone.onBeforeDrop via beforeDropRef bridge (D-163)`) — exists in git log; both specs PASS (`Test Files 1 passed (1) | Tests 9 passed (9)`).
- **REFACTOR gate:** Not needed — the dirty-guard implementation is direct (no premature abstraction over the existing 'quit' flow; intentional code re-use via the optional cancelAction extension).

Per the standard TDD cycle, this commit is `feat(...)` (not `test(...)`) because it adds production behavior that flips a pre-existing RED spec — the spec itself was committed in the RED phase (commit d756dd9). The Rule 1/3 test fixture fixes shipped in the same commit because they're required for the GREEN gate to be observable; commit message documents both the implementation AND the fixture fixes separately.

## Self-Check: PASSED

**Files exist:**
- `/Users/leo/Documents/WORK/CODING/Spine_Texture_Manager/src/renderer/src/components/AppShell.tsx` — FOUND (modified, 1010 lines, +80/-3 net at 653e09a)
- `/Users/leo/Documents/WORK/CODING/Spine_Texture_Manager/src/renderer/src/App.tsx` — FOUND (modified, 313 lines after this plan, +22/-1 net at 93440c2)
- `/Users/leo/Documents/WORK/CODING/Spine_Texture_Manager/tests/renderer/save-load.spec.tsx` — FOUND (modified, +29/-41 net in 8.1-VR-03a + 8.1-VR-03b describe blocks at 93440c2)
- `/Users/leo/Documents/WORK/CODING/Spine_Texture_Manager/.planning/phases/08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea/08.1-05-SUMMARY.md` — FOUND (this file)

**Commits exist:**
- `653e09a` (Task 1: feat AppShell extend onBeforeDropRef + cancelAction) — FOUND in `git log --oneline`
- `93440c2` (Task 2: feat App.tsx wire DropZone.onBeforeDrop + test fixture Rule 1/3 fixes) — FOUND in `git log --oneline`

**Plan-level success criteria:**
- [x] Both 2 tasks executed and committed individually (653e09a + 93440c2)
- [x] App.tsx holds onBeforeDropRef ref + stable handleBeforeDrop wrapper passed to DropZone
- [x] AppShell.tsx registers/unregisters its dirty-guard impl via the ref in a useEffect
- [x] saveQuitDialogState extended with optional cancelAction (D-164)
- [x] SaveQuitDialog Cancel button resolves the dirty-guard Promise to false
- [x] 8.1-VR-03a RED → GREEN
- [x] 8.1-VR-03b RED → GREEN
- [x] Both typecheck projects clean (only pre-existing deferred scripts/probe-per-anim.ts TS2339)
- [x] No regressions (full vitest 275 passed, 0 failed)
- [x] SUMMARY.md created (this file)
- [ ] STATE.md and ROADMAP.md updated (next step in this session — final commit)

**Locked-file invariants vs Phase 8.1 baseline `90dd202`:**
- [x] `scripts/cli.ts`: 0 lines diff
- [x] `src/core/sampler.ts`: 0 lines diff
- [x] `src/core/loader.ts`: 0 lines diff
- [x] `src/core/project-file.ts`: 0 lines diff (D-171)
- [x] `src/renderer/src/components/DropZone.tsx`: 0 lines diff (informal Phase 8.1 lock)
- [x] `src/renderer/src/modals/SaveQuitDialog.tsx`: 0 lines diff (D-165)
- [x] `npm run cli` byte-for-byte unchanged (CIRCLE 2.018 / SQUARE 1.500 / TRIANGLE 2.000; sampling 23.8 ms at 120 Hz)

**Layer 3 invariant:** `grep -cE "from ['\"][^'\"]*\\/core\\/" src/renderer/src/App.tsx` returns 0; same for AppShell.tsx — no `src/core/*` imports.

## Next Phase Readiness

**Plan 08.1-06 (Wave 5 close-out) is unblocked.** It owns:
- Locked-file diff sweep (vs Phase 8.1 baseline 90dd202).
- 3-reproducer manual UAT per CONTEXT `<specifics>` (VR-01 .stmproj-drop recovery; VR-02 toolbar Open recovery; VR-03 dirty-guard SaveQuitDialog flow).
- REQUIREMENTS.md / ROADMAP.md / STATE.md updates marking F9.2 / D-143 / D-149 / VR-01 / VR-02 / VR-03 complete.
- Final close-out commit signalling Phase 8.1 done → `/gsd-verify-work 8` re-runs cleanly → `/gsd-plan-phase 9`.

**No remaining work in 8.1 implementation scope.** All 5 specs (8.1-VR-01, 8.1-VR-02, 8.1-VR-03a, 8.1-VR-03b, 8.1-IPC-01) are GREEN. Cumulative test baseline 270 → **275 passed | 0 failed | 1 skipped | 1 todo** — surpasses CONTEXT D-166 target of ≥273 passed.

---
*Phase: 08.1-close-phase-8-verification-gaps-locate-skeleton-recovery-rea*
*Completed: 2026-04-26*
