---
phase: 08-save-load-project-state
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/08-save-load-project-state/08-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [F9, F9.1, F9.2]
must_haves:
  truths:
    - "Automated exit-criteria sweep passes: full vitest green; both typecheck projects clean; electron-vite build green; locked-file diffs empty"
    - "08-VALIDATION.md per-task map updated with green ✅ for every row 8-XX-01 through 8-XX-21 plus the 5 manual UAT rows"
    - "Manual UAT signed off: round-trip, atomic-write crash safety, .stmproj drag-drop, locate-skeleton recovery, before-quit dirty-guard 3-button flow"
    - "Phase 7 locked files unchanged: scripts/cli.ts + src/core/sampler.ts byte-identical from phase start"
    - "STATE.md advanced to 'Phase 8 COMPLETE'"
    - "ROADMAP.md Phase 8 plan checkboxes flipped to [x]"
  artifacts:
    - path: ".planning/phases/08-save-load-project-state/08-VALIDATION.md"
      provides: "Per-task map flipped to ✅ + frontmatter status:signed-off + nyquist_compliant:true + wave_0_complete:true"
      contains: "status: signed-off"
    - path: ".planning/STATE.md"
      provides: "Phase 8 COMPLETE state advance + Plan 05 summary entry"
      contains: "Phase 8"
    - path: ".planning/ROADMAP.md"
      provides: "Plans block under §Phase 8 with [x] checkboxes for all 5 plans"
      contains: "08-05-PLAN"
  key_links:
    - from: "automated sweep"
      to: "all 21 spec rows in 08-VALIDATION.md"
      via: "vitest exit code + grep verification"
      pattern: "✅"
    - from: "manual UAT"
      to: "5 manual gate rows"
      via: "human-verify checkpoint task with explicit step-by-step instructions"
      pattern: "UAT|human-verify"
---

<objective>
Phase 8 Plan 05 — Close-out. Runs the automated exit-criteria sweep (full test suite green, both typecheck projects clean, electron-vite build green, locked-file diffs empty), populates `08-VALIDATION.md`'s per-task map with green checkmarks, runs the 5 manual UAT gates from VALIDATION.md, and advances STATE.md + ROADMAP.md to reflect Phase 8 COMPLETE.

Purpose: Phase 8 ships F9.1 + F9.2 — the ROADMAP exit criterion is the round-trip (drop SIMPLE_TEST → set override → Save → close app → Load → overrides restored). Plans 01-04 build the round-trip; Plan 05 verifies it works end-to-end against real Electron + real filesystem. The 5 manual UAT gates are LOAD-BEARING (matching Phase 1 D-load-bearing-human-verify lesson) — every Electron lifecycle behavior (drag-drop with OS event, atomic-write crash safety, before-quit + Cmd+Q timing, OS file picker, real disk skeleton relocation) needs a real interaction to verify. Automated tests cannot reach the OS event layer.
Output: 1 modified VALIDATION (frontmatter + table flipped) + 1 modified STATE + 1 modified ROADMAP + commit closing Phase 8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-save-load-project-state/08-CONTEXT.md
@.planning/phases/08-save-load-project-state/08-VALIDATION.md
@.planning/phases/08-save-load-project-state/08-01-PLAN.md
@.planning/phases/08-save-load-project-state/08-02-PLAN.md
@.planning/phases/08-save-load-project-state/08-03-PLAN.md
@.planning/phases/08-save-load-project-state/08-04-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated exit-criteria sweep + populate 08-VALIDATION.md per-task map</name>
  <files>.planning/phases/08-save-load-project-state/08-VALIDATION.md</files>
  <read_first>
- .planning/phases/08-save-load-project-state/08-VALIDATION.md (full file — per-task map at lines 45-68; manual gates at 83-93)
- .planning/phases/08-save-load-project-state/08-01-PLAN.md, 08-02, 08-03, 08-04 (verify which test names actually landed)
- .planning/phases/06-optimize-assets-image-export/06-VALIDATION.md (closest precedent — Phase 6 close-out frontmatter shape)
- .planning/STATE.md (Phase 7 close-out commit shape — `984d01c docs(07-06): close Phase 7 after gap-fix UAT signoff`)
  </read_first>
  <action>
**Run the full automated sweep first; record results; then update VALIDATION.md.**

**Step 1 — Run sweep (record raw output for the SUMMARY):**

```bash
npm run test 2>&1 | tail -10
npm run typecheck 2>&1 | tail -10  # OR npx tsc --noEmit on both projects if no aggregate script
npx electron-vite build 2>&1 | tail -20
git diff scripts/cli.ts | wc -l    # MUST be 0
git diff src/core/sampler.ts | wc -l  # MUST be 0
```

Required outcomes:
- `npm run test` — full suite GREEN (Phase 7 baseline 240+1+1 + Phase 8 deltas: roughly +25 to +35 new tests across project-file.spec, project-io.spec, save-load.spec.tsx, arch.spec extension; expect 265-285 passed + 1 skipped + 1 todo).
- `npm run typecheck` (or both `npx tsc --noEmit -p tsconfig.web.json` and `npx tsc --noEmit -p tsconfig.node.json`) — exit 0 on both. Pre-existing `scripts/probe-per-anim.ts` TS2339 noted in Phase 4's deferred-items.md is out-of-scope.
- `npx electron-vite build` — exits 0; emits `out/main/index.cjs` + `out/preload/index.cjs` + `out/renderer/...` (preserves Phase 2 + Phase 6 CJS lock).
- `git diff scripts/cli.ts` — empty (Phase 5 D-102 lock).
- `git diff src/core/sampler.ts` — empty (CLAUDE.md rule #3 lock).
- `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` — exits 0 (CLI byte-for-byte unchanged sanity).

**Step 2 — Update 08-VALIDATION.md frontmatter:**

Change:
```yaml
status: draft
nyquist_compliant: false
wave_0_complete: false
```
To:
```yaml
status: signed-off
nyquist_compliant: true
wave_0_complete: true
signed_off: 2026-04-26  # or actual date
```

**Step 3 — Update the Per-Task Verification Map.** For each row 8-XX-01 through 8-XX-21:
- `File Exists` column: flip from `❌ W0` to `✅`
- `Status` column: flip from `⬜ pending` to `✅ green`

**Step 4 — Append a Validation Sign-Off section completion:**

```markdown
## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (the three new spec files + arch.spec block)
- [x] No watch-mode flags in any planned command
- [x] Feedback latency < 10s per run
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** signed-off 2026-04-26 — full automated sweep green; manual UAT pending Plan 05 Task 2.
```

**Step 5 — Add an "Automated Sweep Results" section** below the Manual-Only Verifications table:

```markdown
## Automated Sweep Results (2026-04-26)

| Gate | Command | Result |
|------|---------|--------|
| Full vitest | `npm run test` | ✅ {N} passed + 1 skipped + 1 todo (was 240+1+1 at Phase 7 close) |
| Web typecheck | `npx tsc --noEmit -p tsconfig.web.json` | ✅ exit 0 |
| Node typecheck | `npx tsc --noEmit -p tsconfig.node.json` | ✅ exit 0 (pre-existing scripts/probe-per-anim.ts TS2339 out-of-scope) |
| electron-vite build | `npx electron-vite build` | ✅ exit 0 — out/main/index.cjs + out/preload/index.cjs + out/renderer emitted |
| CLI sanity | `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` | ✅ exit 0; byte-for-byte unchanged from Phase 7 |
| `scripts/cli.ts` lock | `git diff scripts/cli.ts` | ✅ empty (Phase 5 D-102) |
| `src/core/sampler.ts` lock | `git diff src/core/sampler.ts` | ✅ empty (CLAUDE.md rule #3) |
| arch.spec | `npm run test -- tests/arch.spec.ts` | ✅ 8 baseline blocks + Phase 8 hygiene block green |

Replace `{N}` with the actual passed count from the sweep output.
```

**Run after writing:**

- `npm run test` once more to confirm no regression after VALIDATION edits (markdown doesn't affect tests but courtesy check).
- The actual numeric count in `{N}` must reflect the real `npm run test` output.

**Implementation note:** if any automated gate FAILS during sweep, abort Plan 05, surface the failure as a blocker, and either (a) gap-fix in a follow-up commit OR (b) escalate to a new plan 08-06 if the gap is structural. Do NOT continue to Task 2 (manual UAT) with a red automated gate — the human-verify cannot rescue an automated regression.
  </action>
  <verify>
    <automated>npm run test && npx tsc --noEmit -p tsconfig.web.json && npx tsc --noEmit -p tsconfig.node.json && npx electron-vite build && [ -z "$(git diff scripts/cli.ts)" ] && [ -z "$(git diff src/core/sampler.ts)" ]</automated>
  </verify>
  <acceptance_criteria>
- `npm run test` exits 0 (full suite green); passing count >= 240 + (Plan 01-04 specs added)
- `npx tsc --noEmit -p tsconfig.web.json` exits 0
- `npx tsc --noEmit -p tsconfig.node.json` exits 0
- `npx electron-vite build` exits 0
- `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` exits 0
- `git diff scripts/cli.ts | wc -l` outputs `0`
- `git diff src/core/sampler.ts | wc -l` outputs `0`
- `grep -c "status: signed-off" .planning/phases/08-save-load-project-state/08-VALIDATION.md` returns `1`
- `grep -c "nyquist_compliant: true" .planning/phases/08-save-load-project-state/08-VALIDATION.md` returns `1`
- `grep -c "wave_0_complete: true" .planning/phases/08-save-load-project-state/08-VALIDATION.md` returns `1`
- `grep -c "✅ green" .planning/phases/08-save-load-project-state/08-VALIDATION.md` returns at least `21` (one per row in per-task map)
- `grep -c "Automated Sweep Results" .planning/phases/08-save-load-project-state/08-VALIDATION.md` returns `1`
  </acceptance_criteria>
  <done>Full automated sweep green; VALIDATION.md frontmatter + per-task map + sign-off section all updated; locked files byte-identical from phase start.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify — 5 manual UAT gates (round-trip + atomic-write crash + .stmproj drag-drop + locate-skeleton + before-quit 3-button)</name>
  <what-built>
- `src/core/project-file.ts` (Plan 02): pure-TS schema + validator + migration ladder + cross-volume-aware path helpers
- `src/main/project-io.ts` (Plan 03): 5 async handlers wrapping dialog + fs + loader/sampler chain with typed-error envelopes
- `src/main/ipc.ts` (Plan 03): 5 new ipcMain.handle channels (project:save / save-as / open / open-from-path / locate-skeleton)
- `src/main/index.ts` (Plan 03): before-quit dirty-guard with Pitfall-1 setTimeout deferral + ipcMain.on confirm-quit-proceed + macOS open-file scaffold
- `src/preload/index.ts` (Plan 03): 7 new contextBridge methods preserving sandbox + contextIsolation
- `src/renderer/src/modals/SaveQuitDialog.tsx` (Plan 04): hand-rolled ARIA modal cloning OverrideDialog with 3-button footer
- `src/renderer/src/components/AppShell.tsx` (Plan 04): Save/Open buttons + state slots + dirty derive + Cmd/Ctrl+S+O listener + dirty-marker chip + SaveQuitDialog mount + locate-skeleton flow + stale-override banner
- `src/renderer/src/components/DropZone.tsx` (Plan 04): .json | .stmproj | reject extension branch with optional onBeforeDrop dirty-guard hook
  </what-built>
  <how-to-verify>
**5 manual gates per VALIDATION.md §"Manual-Only Verifications". Run each in dev mode (`npm run dev`) unless otherwise noted. Pass all 5 to advance to close-out.**

---

**Gate 1 — Round-trip exit criterion (F9, F9.1, F9.2 — primary ROADMAP gate):**

1. Start dev mode: `npm run dev`.
2. Drop `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` onto the DropZone.
3. Verify: skeleton renders, Global Max Render Source panel populates with CIRCLE/SQUARE/TRIANGLE rows.
4. Set TRIANGLE override to 50% (double-click TRIANGLE peak; type 50; Apply).
5. Verify: dirty marker `•` appears in filename chip (chip reads `• Untitled` since no save yet).
6. Press **Cmd+S** (macOS) or **Ctrl+S** (Linux).
7. Verify: Save dialog opens with `defaultPath` ending in `SIMPLE_TEST.stmproj` in the same directory as the skeleton.
8. Save as `~/Desktop/simple.stmproj` (or any persistent location).
9. Verify: dialog closes; dirty marker `•` disappears; chip reads `simple.stmproj` (no leading bullet).
10. Press **Cmd+Q** (macOS) — app quits cleanly (no SaveQuitDialog because session is clean).
11. Relaunch dev: `npm run dev`.
12. Open `~/Desktop/simple.stmproj` (drag onto DropZone OR Cmd+O picker).
13. Verify: skeleton renders; chip reads `simple.stmproj` (no bullet); TRIANGLE override badge shows 50%; sampler ran (you can confirm by the elapsed-ms in any debug surface or by the rendered worldW/H halving on the TRIANGLE row).
14. Verify: no stale-override banner (clean rig matches the saved overrides).

**Pass criteria:** override round-trips identical, sampler re-ran on Open, no errors, no spurious banners.

---

**Gate 2 — `.stmproj` drag-drop entry point (D-142):**

1. Start fresh dev session.
2. From Finder/Explorer, drag the `~/Desktop/simple.stmproj` file (created in Gate 1) directly onto the DropZone.
3. Verify: project loads (NOT the "Not a .json" rejection envelope from Phase 1); same final state as Gate 1 step 13.

**Pass criteria:** `.stmproj` drop dispatches to the project-load path; identical end state to picker-load.

---

**Gate 3 — Locate-skeleton recovery flow (D-149, T-08-MISS):**

1. Start fresh dev session, drop SIMPLE_TEST.json, set CIRCLE override to 75%, save as `~/Desktop/locate-test.stmproj`.
2. Quit app cleanly (Cmd+Q).
3. In Finder/Explorer, RENAME `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` to `SIMPLE_TEST.json.moved` (so the saved relative path won't resolve).
4. Relaunch dev mode.
5. Open `~/Desktop/locate-test.stmproj`.
6. Verify: inline error banner appears: `Skeleton not found at <path>` with a `Locate skeleton…` button.
7. Click `Locate skeleton…`.
8. In the file picker, select `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json.moved` (or rename it back first to SIMPLE_TEST.json — either is fine for the gate).
9. Verify: skeleton loads with the new path; CIRCLE override of 75% restored; dirty marker `•` appears (because the substituted path will be re-saved on next Save).
10. Cleanup: rename the file back to `SIMPLE_TEST.json` for subsequent gates.

**Pass criteria:** locate-skeleton picker opens; user-picked path replaces the missing one; overrides apply to the new sample.

---

**Gate 4 — `before-quit` dirty-guard 3-button flow (D-143):**

For each of {Save, Don't Save, Cancel}, run a fresh test:

**Sub-gate 4a — Cancel:**
1. Drop SIMPLE_TEST.json; set TRIANGLE override to 30% (dirty).
2. Press Cmd+Q.
3. Verify: SaveQuitDialog mounts with title "Save changes before quitting?"
4. Click **Cancel**.
5. Verify: dialog closes; app stays running; dirty marker still `•`; no file written.

**Sub-gate 4b — Don't Save:**
1. With the dirty session from 4a still running, press Cmd+Q again.
2. Click **Don't Save** in the dialog.
3. Verify: dialog closes; app quits without writing.

**Sub-gate 4c — Save:**
1. Relaunch; drop SIMPLE_TEST.json; set SQUARE override to 80%.
2. Press Cmd+Q. Click **Save** in the dialog.
3. Verify: dialog button transitions to "Saving…" briefly; save dialog opens (since this is an untitled session); pick a save location.
4. Verify: dialog closes; app quits; on disk a `.stmproj` exists with the SQUARE override saved.

**Pass criteria:** all three buttons honor their semantics; Save button shows "Saving…" during save; Cancel does not quit; Don't Save quits without writing.

---

**Gate 5 — Atomic-write crash safety (T-08-IO):**

1. Drop SIMPLE_TEST.json; set CIRCLE override to 25% in dev mode.
2. Save as `~/Desktop/atomic-test.stmproj`. Verify file exists. (This establishes a known-good baseline file.)
3. Modify CIRCLE override to 60% (now dirty against the saved baseline).
4. Open a second terminal window. Run `pgrep -f electron` to find the electron PIDs.
5. In the dev app, press Cmd+S to initiate a save.
6. IMMEDIATELY (within ~50 ms) in the second terminal: `kill -9 <pid>` to terminate Electron mid-write. Timing is racy — repeat steps 3-6 if you miss the window. The point is to kill SOME of the saves mid-flight.
7. Inspect `~/Desktop/`. Two outcomes are valid:
   a. `atomic-test.stmproj` exists with the OLD 25% value (overwrite did not complete; original intact).
   b. `atomic-test.stmproj` exists with the NEW 60% value (overwrite completed before SIGKILL).
8. Verify: there is NO file named `atomic-test.stmproj.tmp` orphaned (or if there is, it does NOT have invalid contents — re-saving in the next launch should clean it up; for Phase 8 a remaining `.tmp` is acceptable per Pitfall 2).
9. Verify: relaunch dev; open `atomic-test.stmproj`; it parses cleanly (no `ProjectFileParseError`).

**Pass criteria:** no malformed `.stmproj` on disk; previous-saved file (if Save was killed mid-write) loads cleanly. Tmp orphan acceptable per Pitfall 2.

---

**Failure handling:**
- If ANY gate fails, surface the failure clearly. The failure may be (a) implementation bug → gap-fix commit (Rule 1), (b) architectural change-of-intent (Rule 4), (c) environment issue (Node/OS quirk).
- Phase 4/5/7 precedent: human-verify gap-fixes are committed atomically with descriptive messages and added to the SUMMARY.

**Cleanup after all gates pass:**
- Rename SIMPLE_TEST.json.moved back to SIMPLE_TEST.json if Gate 3 cleanup was skipped.
- Delete `~/Desktop/simple.stmproj`, `~/Desktop/locate-test.stmproj`, `~/Desktop/atomic-test.stmproj` test artifacts (or keep for future regression runs).
  </how-to-verify>
  <resume-signal>
Type:
- "all 5 pass" → Plan 05 Task 3 (close-out)
- "gate {N} fail: {description}" → surface gap; either gap-fix in this plan or escalate to 08-06
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Advance STATE.md + ROADMAP.md to reflect Phase 8 COMPLETE; commit close-out</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
- .planning/STATE.md (full file — header frontmatter; "Current phase" + "Last completed" + "Next action" sections)
- .planning/ROADMAP.md (lines 274-287 — Phase 8 deliverables + exit criteria + Requirement coverage; current state has NO `Plans:` block under Phase 8)
- .planning/phases/06-optimize-assets-image-export/06-VALIDATION.md or 07/07-VALIDATION.md as a close-out frontmatter precedent
- The 4 SUMMARY files Plans 01-04 produced (`.planning/phases/08-save-load-project-state/08-0{1,2,3,4}-SUMMARY.md`)
  </read_first>
  <action>
**Three small edits + one commit. All append-only or surgical.**

---

**Edit 1 — Append a Plans block to ROADMAP.md under §"Phase 8":**

Find the "Phase 8: Save/Load project state" section (lines 274-287). After the `**Requirement coverage:** F9.` line, ADD:

```markdown
**Plans:** 5 plans

Plans:
- [x] 08-01-PLAN.md — Wave 0 scaffolding: src/shared/types.ts extension (7 new types + 4 new SerializableError kinds + 8 new Api members) + 3 RED spec stubs (project-file.spec.ts, project-io.spec.ts, save-load.spec.tsx) + arch.spec.ts Phase 8 electron-import block. Wave 1, autonomous, depends_on [].
- [x] 08-02-PLAN.md — Pure-TS schema module (src/core/project-file.ts): validateProjectFile + migrate + serializeProjectFile + materializeProjectFile + relativizePath + absolutizePath. Drives tests/core/project-file.spec.ts GREEN. Layer 3 strict (only node:path import). Wave 2, autonomous, depends_on [01].
- [x] 08-03-PLAN.md — Main + preload glue: src/main/project-io.ts (5 async handlers, atomic-write idiom, loader/sampler chain, typed-error envelope) + src/main/ipc.ts (5 new IPC channels) + src/main/index.ts (before-quit dirty-guard with Pitfall-1 setTimeout deferral + macOS open-file scaffold) + src/preload/index.ts (7 new contextBridge methods). Drives tests/main/project-io.spec.ts GREEN. Wave 3, autonomous, depends_on [01, 02].
- [x] 08-04-PLAN.md — Renderer wiring: src/renderer/src/modals/SaveQuitDialog.tsx (hand-rolled ARIA 3-button modal cloning OverrideDialog) + src/renderer/src/components/AppShell.tsx (Save/Open toolbar buttons + state slots + dirty derive + Cmd/Ctrl+S+O listener + dirty-marker chip + SaveQuitDialog mount + locate-skeleton flow + stale-override banner) + src/renderer/src/components/DropZone.tsx (.json | .stmproj | reject extension branch). Drives tests/renderer/save-load.spec.tsx GREEN. Wave 4, autonomous, depends_on [01, 02, 03].
- [x] 08-05-PLAN.md — Close-out: automated exit-criteria sweep + populate 08-VALIDATION.md per-task map + 5 manual UAT gates (round-trip, .stmproj drag-drop, locate-skeleton, before-quit 3-button, atomic-write crash). Advance STATE.md + ROADMAP.md. Wave 5, has checkpoint, depends_on [01, 02, 03, 04].
```

If a checkbox here was un-checked because a plan partially failed, replace `[x]` with `[ ]` for that row and add a note. The all-pass case is `[x]` across all 5.

---

**Edit 2 — Update STATE.md frontmatter + sections:**

Update the YAML frontmatter:
```yaml
last_updated: "2026-04-26T..."  # actual ISO timestamp
stopped_at: Phase 8 COMPLETE  # was "Phase 8 context gathered"
```

Update `## Current phase`:
```markdown
## Current phase

**Phase 8 — Save/Load project state COMPLETE** (5/5 plans executed; manual UAT signed off 2026-04-26 with all 5 gates passing; ready for /gsd-verify-work 8 then /gsd-plan-phase 9)
```

Update `## Current plan`:
```markdown
## Current plan

**Plan 08-05 closed.** Phase 8 COMPLETE. Next: `/gsd-verify-work 8` (formal Phase 8 verification) then `/gsd-plan-phase 9` (complex-rig hardening + polish).
```

PREPEND a new entry to the top of `## Last completed` (before the existing Plan 07-06 entry):

```markdown
- **Plan 08-05 (2026-04-26):** Close-out. Commits {commit-shas}. Automated sweep green: full vitest {N} passed + 1 skipped + 1 todo (was 240+1+1); both typecheck projects clean; electron-vite build green (main + preload CJS preserved per Phase 2 + Phase 6 lock); npm run cli green (CLI byte-for-byte unchanged); locked-file diffs empty (scripts/cli.ts + src/core/sampler.ts byte-identical from phase start). Manual UAT signed off 2026-04-26 across all 5 gates: (1) round-trip — drop SIMPLE_TEST → set TRIANGLE 50% → Cmd+S → Cmd+Q → relaunch → open .stmproj → override restored; (2) .stmproj drag-drop entry point — file from Finder dispatches to project-load path; (3) locate-skeleton recovery — rename underlying skeleton mid-test, picker prompts to substitute; (4) before-quit 3-button (Save/Don't Save/Cancel) — all three semantics correct, "Saving…" state visible during save promise; (5) atomic-write crash safety — kill -9 mid-write, original .stmproj intact, no malformed file. {Deviations recorded if any.} `npm run test` {N} passed. **Phase 8 COMPLETE — ready for `/gsd-verify-work 8`.** Key lesson: {add 1-2 sentences from actual learnings}. SUMMARY at `.planning/phases/08-save-load-project-state/08-05-SUMMARY.md`.
- **Plan 08-04 (2026-04-26):** Renderer wiring — SaveQuitDialog + AppShell major touch + DropZone branch. Commits {sha}. {1-2 sentence summary}. SUMMARY at `08-04-SUMMARY.md`.
- **Plan 08-03 (2026-04-26):** Main + preload glue. Commits {sha}. {1-2 sentence summary}. SUMMARY at `08-03-SUMMARY.md`.
- **Plan 08-02 (2026-04-26):** Pure-TS schema module. Commits {sha}. {1-2 sentence summary}. SUMMARY at `08-02-SUMMARY.md`.
- **Plan 08-01 (2026-04-26):** Wave 0 types + RED spec stubs + arch.spec extension. Commits {sha}. {1-2 sentence summary}. SUMMARY at `08-01-SUMMARY.md`.
```

Replace `{commit-shas}` and `{N}` with actual values from the sweep + commits list. Replace `{1-2 sentence summary}` with the actual one-liner from each plan's SUMMARY.md "Key outcome" or "What landed" section.

Update `## Next action`:
```markdown
## Next action

**Phase 8 COMPLETE.** Next: `/gsd-verify-work 8` to formally verify F9 / F9.1 / F9.2 coverage and close the phase. After verification, `/gsd-plan-phase 9` to start Phase 9 (complex-rig hardening + polish — UI virtualization, sampler worker thread if profiling shows main-thread jank, Settings modal, recent projects menu, OS file association registration drop-in).

Phase 7 verify (`/gsd-verify-work 7`) and Phase 4 verify (`/gsd-verify-work 4`) remain outstanding but are orthogonal — planner advances serially per the existing precedent.

Environment notes preserved from prior phases:
- Node 24 + Electron 41 require CJS output on preload AND main (4 arch.spec guards lock this).
- `ELECTRON_RUN_AS_NODE=1` global env breaks `npm run dev`.
- Phase 8 `.stmproj` files are forward-compat by construction: `documentation: {}` reserved slot per D-148; v2 migration ladder rung is one `case 2:` away.
```

Update `## Decisions` by APPENDING the Phase 8 D-140..D-156 decisions (these were already locked in 08-CONTEXT.md but STATE.md is the project-wide log):

```markdown
- Plan 08-01: src/shared/types.ts extension append-only — 7 new types (ProjectFileV1, ProjectFile, AppSessionState, MaterializedProject, SaveResponse, OpenResponse, LocateSkeletonResponse), 4 new SerializableError kinds (ProjectFileNotFoundError, ProjectFileParseError, ProjectFileVersionTooNewError, SkeletonNotFoundOnLoadError), 8 new Api members (5 invoke + 1 listener + 1 sender + openProjectFromFile File-handling wrapper). Existing Api / SerializableError / LoadResponse identifiers preserved verbatim.
- Plan 08-02: src/core/project-file.ts is Layer 3 strict — only node:path + type imports. node:path permitted because it has zero I/O (existing arch.spec grep at lines 116-134 explicitly exempts it). Phase 8 added an additional electron-import grep block at the bottom of arch.spec specifically targeting project-file.ts by name (graceful-skip pattern when the file does not exist — activates Plan 02 onward).
- Plan 08-02: validateProjectFile returns a discriminated `{ok, project} | {ok: false, error: {kind: 'invalid-shape' | 'unknown-version' | 'newer-version', message}}` envelope — RICHER than validateExportPlan's `string | null` because schema-version errors need a different recovery path than shape errors at the UX layer. ipc.ts's project-io chain (Plan 03) translates the inner kind to SerializableError.kind.
- Plan 08-02: relativizePath cross-volume detection rule (Pitfall 4) — checks `path.isAbsolute(rel)` (Windows cross-drive returns absolute string) AND `path.parse(...).root` differs (POSIX cross-mount produces '..' chain to root). Both detected; both fall back to absolute storage. absolutizePath is the inverse — stores both absolute and relative inputs round-trip cleanly.
- Plan 08-03: handleProjectOpenFromPath chains parse → validate → migrate → materialize → loadSkeleton → sampleSkeleton → buildSummary → assemble MaterializedProject. SkeletonJsonNotFoundError is caught and translated to 'SkeletonNotFoundOnLoadError' kind (D-149 trigger); JSON SyntaxError → 'ProjectFileParseError'; validate 'newer-version' → 'ProjectFileVersionTooNewError' (D-151). Stale-override keys are computed server-side: intersect saved overrides with summary.peaks names; dropped keys travel back in MaterializedProject.staleOverrideKeys for the renderer to surface.
- Plan 08-03: src/main/index.ts before-quit handler uses the Pitfall-1 pattern — module-level isQuitting flag + setTimeout(() => app.quit(), 0) on second invocation. Renderer responds via dual-one-way wiring (project:check-dirty-before-quit one-way send + project:confirm-quit-proceed one-way send back) — never invoke (synchronous before-quit listener cannot await). macOS open-file listener is wired now as a Phase 9 polish drop-in slot; the electron-builder .stmproj registration config is still Phase 9.
- Plan 08-04: SaveQuitDialog clones OverrideDialog ARIA scaffold verbatim — useFocusTrap on dialogRef, role=dialog + aria-modal=true + aria-labelledby, outer overlay onClick=onCancel, inner panel stopPropagation. Three buttons in DOM-order Save / Don't Save / Cancel; Save first so useFocusTrap auto-focus lands on it. Escape maps to onCancel (NOT Don't Save — Cancel is the safe default per Photoshop/AE convention).
- Plan 08-04: AppShell isDirty derivation is field-by-field equality on overrides Record + samplingHz against lastSaved snapshot (CONTEXT discretion §2). sortColumn/sortDir/active tab/scroll/search are NOT in the dirty signal (D-147 — ephemeral UI state defers to Phase 9). For Phase 8 v1 schema, sortColumn defaults to 'attachmentName' and sortDir to 'asc' (D-91) — Phase 9 hoists actual panel sort state into AppShell so it can be tracked.
- Plan 08-04: Cmd/Ctrl+S+O suppression uses document.querySelector('[role="dialog"]') sentinel (Pitfall 6). Every project modal uses role=dialog (OverrideDialog, OptimizeDialog, ConflictDialog, AtlasPreviewModal, SaveQuitDialog) — verified across the modal directory. Robust against future modal additions because the project's hand-rolled discipline (D-81) requires role=dialog on every new modal.
- Plan 08-04: stale-override banner state lives in AppShell's useState (Pitfall 8) — AppShell re-mounts on every drop because App.tsx transitions through idle/loading. Banner state auto-resets to null on remount; no cross-load state leak.
- Plan 08-04: DropZone gains optional onProjectDrop / onProjectDropStart / onBeforeDrop callbacks. Existing onLoad / onLoadStart preserved verbatim — DropZone consumers that don't wire the new pair simply reject .stmproj as before. AppShell wires both pairs + the dirty-guard pre-check.
```

If specific Plan 04 deviations surfaced during human-verify in Task 2, append them to STATE.md decisions as well.

---

**Edit 3 — Commit Phase 8 close-out:**

After Plans 01-04 already commit-individually (each plan has its own commits per execute-phase convention), Plan 05 produces a single docs-flavor commit:

```bash
git add .planning/STATE.md .planning/ROADMAP.md .planning/phases/08-save-load-project-state/08-VALIDATION.md
git commit -m "docs(08): close Phase 8 after manual UAT signoff

5/5 plans complete; manual UAT signed off 2026-04-26.

Gates passing:
- Round-trip exit criterion (F9, F9.1, F9.2)
- .stmproj drag-drop entry point (D-142)
- Locate-skeleton recovery flow (D-149)
- before-quit 3-button dirty-guard (D-143)
- Atomic-write crash safety (T-08-IO)

Phase 8 COMPLETE — ready for /gsd-verify-work 8."
```

(Use HEREDOC if a multi-line commit message is preferred per the GSD commit conventions in the planner role file.)

**If Plan 04 task 2 produced gap-fix commits during human-verify, the close-out commit message should reference them.**

**Run after writing:**

- `git status` — confirm only the 3 docs files staged.
- `git log -1` — confirm the commit landed cleanly.
- `git diff HEAD~5..HEAD --stat` — see the 5-plan footprint.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:"%s" | grep -c "Phase 8\|08" && grep -c "Phase 8" .planning/STATE.md && grep -c "08-05-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "Phase 8 — Save/Load project state COMPLETE" .planning/STATE.md` returns at least `1`
- `grep -c "Plan 08-05" .planning/STATE.md` returns at least `1`
- `grep -c "08-01-PLAN" .planning/ROADMAP.md` returns `1`
- `grep -c "08-02-PLAN" .planning/ROADMAP.md` returns `1`
- `grep -c "08-03-PLAN" .planning/ROADMAP.md` returns `1`
- `grep -c "08-04-PLAN" .planning/ROADMAP.md` returns `1`
- `grep -c "08-05-PLAN" .planning/ROADMAP.md` returns `1`
- All 5 plan checkboxes in ROADMAP.md show `[x]`: `grep -E "\[x\] 08-0[1-5]-PLAN" .planning/ROADMAP.md | wc -l` returns `5` (assuming all gates passed)
- Commit landed with subject containing "Phase 8" or "08": `git log -1 --pretty=format:"%s"` outputs a string containing `Phase 8` or `(08)` or similar
- `git status` shows clean working tree (no untracked or modified files post-commit, except possibly summary files Plans 01-04 didn't commit)
- `git diff scripts/cli.ts | wc -l` outputs `0`
- `git diff src/core/sampler.ts | wc -l` outputs `0`
  </acceptance_criteria>
  <done>STATE.md advanced to Phase 8 COMPLETE with full Plan 08-05 entry and 4 prior-plan summary entries. ROADMAP.md gains the Plans block under §Phase 8 with all 5 [x] checkboxes. Close-out commit lands. Locked files byte-identical from phase start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual UAT — OS event boundary | The 5 gates exercise OS-level drag-drop, file dialogs, app lifecycle (Cmd+Q), filesystem rename atomicity — automated tests cannot reach this layer |
| Documentation drift | STATE.md + ROADMAP.md changes can lag behind code; Plan 05 closes the loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-UAT | Tampering | manual UAT bypass | mitigate | All 5 gates blocking — none can be silently skipped. Each gate has explicit pass criteria + step-by-step instructions. Failure handling explicit (gap-fix commit OR escalate to 08-06). |
| T-08-DRIFT | Repudiation | STATE.md / ROADMAP.md drift | accept | Plan 05 explicitly updates both; the Phase 8 close-out commit is the audit trail. The single-developer + Claude workflow has no need for additional drift detection. |
| T-08-LOCK | Tampering | locked-file regression | mitigate | Acceptance criteria includes `git diff scripts/cli.ts | wc -l == 0` AND `git diff src/core/sampler.ts | wc -l == 0`. The Phase 5 D-102 + CLAUDE.md rule #3 locks are verified at three points: each plan's task verification, this plan's automated sweep, and the close-out commit. |
</threat_model>

<verification>
1. Automated sweep all green (Task 1 sub-criteria)
2. All 5 manual UAT gates pass (Task 2 explicit pass criteria)
3. STATE.md + ROADMAP.md + VALIDATION.md edits committed (Task 3)
4. Phase 5 D-102 + CLAUDE.md rule #3 locks preserved across all 5 plans
</verification>

<success_criteria>
- All 21 automated test rows in 08-VALIDATION.md per-task map are ✅ green
- All 5 manual UAT gates pass and are signed off in VALIDATION.md
- STATE.md reflects "Phase 8 COMPLETE" with all 5 plan summaries
- ROADMAP.md has the Plans block under §Phase 8 with [x] checkboxes
- Close-out commit lands cleanly
- `scripts/cli.ts` + `src/core/sampler.ts` byte-identical from phase start (locked-file invariants)
- Phase 8 ready for `/gsd-verify-work 8` formal verification
</success_criteria>

<output>
After completion, create `.planning/phases/08-save-load-project-state/08-05-SUMMARY.md` recording:
- Automated sweep raw outputs (test count, typecheck, build sizes)
- Manual UAT results: per-gate pass/fail with notes on any wrinkles
- Total commits across Phase 8 (sum of plan-internal + close-out)
- Total LOC delta across Phase 8 (rough — `git diff $PHASE_BASE..HEAD --stat` summary)
- Any deviations across all 5 plans aggregated (Rule 1/2/3/4 with 1-line each)
- Confirmation of locked-file invariants
- One-paragraph "Key lessons" — what surprised, what worked smoother than expected, what to remember for Phase 9
- Pointer to next action (`/gsd-verify-work 8` then `/gsd-plan-phase 9`)
</output>
