---
phase: 12.1
plan: 07
subsystem: docs
tags: [windows, electron-builder, winCodeSign, symlink, readme, contributor-onboarding]

requires:
  - phase: 12.1
    provides: Phase 12.1 RESEARCH §Code Examples §README.md "Building on Windows" — verbatim source content
provides:
  - "## Building on Windows" section in README.md with 3 ranked workarounds (Developer Mode / `--dir` quick path / run-as-Administrator)
  - Verbatim winCodeSign symlink-extract error block so contributors can pattern-match the failure
  - Pending todo lifecycle closed — pending → resolved with `## Resolved` section
affects: [contributor-onboarding, windows-developer-experience]

tech-stack:
  added: []
  patterns:
    - "README.md additive section pattern — new contributor-facing material lands adjacent to ## Commands (developer), not under tester-facing ## Installing (per PATTERNS.md §README.md (M))"
    - "Todo lifecycle: pending/ → resolved/ via git mv (preserves blame), append ## Resolved section linking back to closing plan"

key-files:
  created:
    - .planning/phases/12.1-installer-auto-update-live-verification/12.1-07-SUMMARY.md
  modified:
    - README.md (+38 lines, new ## Building on Windows section between ## Commands (developer) and ## Reporting issues)
    - .planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md (renamed from pending/, appended ## Resolved section)

key-decisions:
  - "README.md (not CONTRIBUTING.md) is the right home — repo has no CONTRIBUTING.md and the existing README.md already houses ## Commands (developer); adjacency keeps the discoverability radius small (per PATTERNS.md anti-pattern guard)"
  - "Documented all 3 workarounds rather than picking one — IT-locked Windows hosts can't toggle Developer Mode, so the run-as-Administrator alternative + the --dir quick path each cover a distinct user constraint"

patterns-established:
  - "Verbatim error excerpt pattern: copy the actual electron-builder failure text into the README so contributors can pattern-match by web search"
  - "Todo lifecycle: pending → resolved via git mv preserves blame history; appended ## Resolved section documents the closing plan"

requirements-completed:
  - SC-7

duration: 8 min
completed: 2026-04-28
---

# Phase 12.1 Plan 07: Building on Windows README docs Summary

**Documented the local Windows `npm run build` papercut where electron-builder cannot extract `winCodeSign-2.6.0.7z` macOS dylib symlinks without symlink-creation rights, and closed the pending contributor-onboarding todo.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-28T16:08Z
- **Completed:** 2026-04-28T16:16Z
- **Tasks:** 2
- **Files modified:** 2 (README.md, todo file rename)

## Accomplishments

- README.md gained a new `## Building on Windows` section (~38 lines) sitting between `## Commands (developer)` and `## Reporting issues`. Three ranked workarounds documented: Developer Mode one-time per-user toggle (recommended), `npx electron-builder --win --x64 --dir` quick path for unsigned local test builds, run-as-Administrator alternative for IT-locked machines. Verbatim error excerpt included so contributors can pattern-match the failure on web search.
- Pending todo `.planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md` moved to `.planning/todos/resolved/` via `git mv` (preserves blame history; git tracked the move with 88% similarity rename detection). Appended `## Resolved` section linking back to Plan 12.1-07.
- ROADMAP SC-7 closed: contributor-onboarding wall on default Windows installs is now documented.

## Task Commits

Both tasks landed in ONE atomic commit per plan instruction:

1. **Task 1 + Task 2 combined** — `ad6d9bf` (docs(12.1-07): document Windows winCodeSign symlink papercut in README)

## Files Created/Modified

- `README.md` — Added `## Building on Windows` section (~38 lines) between line 30 (last `## Commands (developer)` bullet) and the existing `## Reporting issues` heading. Section structure: intro paragraph → `### One-time setup (recommended)` (Developer Mode steps) → `### Quick path: skip signing entirely` (`--dir` shortcut) → `### Alternative: run as Administrator` → `### What the error looks like` (verbatim error block).
- `.planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md` — Renamed from `pending/` via `git mv`. Appended `## Resolved` section documenting the README change.

## Verification

- `grep -c "^## Building on Windows$" README.md` → 1
- `grep -c "Developer Mode" README.md` → 3
- `grep -c "winCodeSign" README.md` → 4
- `grep -c "Cannot create symbolic link" README.md` → 1
- `grep -c -- "--dir" README.md` → 1
- `grep -c "run as Administrator" README.md` → 1
- Section ordering preserved: `## Installing` → `## What it does` → `## Requirements` → `## Commands (developer)` → `## Building on Windows` → `## Reporting issues` ✓
- `git status --short .planning/todos/` shows `R` rename indicator ✓
- `npm run test` → 438 passed | 1 skipped | 1 todo (39 test files, no regressions)

## Deviations from Plan

**[Rule 1 — Documentation drift] Plan acceptance criterion stated "vitest 434/434" but actual baseline is 438** — Found during: Task 1 verification | Issue: Plan 12.1-07 acceptance criterion read "npm run test reports 434 passing (no test surfaces touched)". The plan author calculated 434 = 433 baseline + 1 new test from Plan 12.1-06 (the install-md.spec.ts size soft-gate). But Plan 12.1-06 is wave 4 and runs AFTER 12.1-07 (wave 1) per frontmatter `depends_on`. Actual current baseline at 12.1-07 start was 438 passed (the codebase has accreted +5 tests since the plan was authored). | Fix: None needed — 12.1-07 doesn't touch test surfaces. The actual count of 438 passed satisfies the spirit of the criterion ("no test surfaces touched"). | Files modified: none | Verification: `npm run test` post-Task-1 reports 438 passed, same as pre-edit baseline. | Commit hash: n/a (no code change required).

**Total deviations:** 1 documentation drift (Rule 1, auto-noted, no code impact). **Impact:** None — the plan's stated test count was a stale projection; actual baseline is higher and stable.

## Files NOT Touched

Per anti-pattern guards in plan:
- No new CONTRIBUTING.md created (would split discoverability)
- No other README.md sections touched (`## Installing`, `## What it does`, `## Requirements`, `## Commands (developer)`, `## Reporting issues` byte-for-byte unchanged)
- `package.json` untouched (workaround is documentation-only)
- `electron-builder.yml` untouched (the workaround does NOT involve config changes — `winCodeSign` cache path is electron-builder default)

## Issues Encountered

None.

## Next Phase Readiness

Ready for Plan 12.1-01 (D-10 publish-race synthesizer rework). 12.1-07 had no file overlap with 12.1-01 — README.md and `scripts/emit-latest-yml.mjs` are independent surfaces.

## Self-Check: PASSED

- [x] All tasks executed (2/2)
- [x] Single atomic commit per plan instruction (`ad6d9bf`)
- [x] All acceptance criteria verified via grep
- [x] No regressions: 438/438 tests passing
- [x] No untracked side effects: `git status` clean for plan-owned paths
- [x] Pending todo moved with `git mv` (rename detected at 88% similarity)
- [x] `## Resolved` section appended in resolved todo
