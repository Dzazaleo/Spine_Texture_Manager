---
phase: 12.1
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md
  - .planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md
autonomous: true
requirements:
  - SC-7  # ROADMAP success criterion 7: Windows local-build papercut documented
must_haves:
  truths:
    - "README.md gains a `## Building on Windows` section between the existing `## Commands (developer)` and `## Reporting issues` sections"
    - "The new section documents the winCodeSign-2.6.0.7z symlink extract failure verbatim error text + 3 ranked workarounds (Developer Mode / Run as Administrator / `--dir` shortcut for unsigned local test builds)"
    - "The pending todo file at `.planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md` is moved to `.planning/todos/resolved/` (per project todo-lifecycle convention)"
    - "vitest 434/434 still green (no test surfaces touched)"
  artifacts:
    - path: "README.md"
      provides: "New ## Building on Windows section with Developer Mode workaround + admin alternative + --dir shortcut + verbatim error excerpt"
      contains: "## Building on Windows"
      min_lines: 30  # the new section adds ~30 lines
    - path: ".planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md"
      provides: "The pending todo, moved to resolved/ once README documents the workaround"
      contains: "winCodeSign"
  key_links:
    - from: "README.md ## Building on Windows section"
      to: "Developer Mode / admin / --dir workarounds"
      via: "Verbatim copy from .planning/todos/pending/<wincodesign-todo>.md §Solution"
      pattern: "Developer Mode"
    - from: "README.md ## Building on Windows section"
      to: "Verbatim error excerpt"
      via: "Copy from todo file §Verbatim error excerpt block"
      pattern: "Cannot create symbolic link"
---

<objective>
Document the local Windows `npm run build` papercut where electron-builder fails to extract `winCodeSign-2.6.0.7z` because the archive contains macOS dylib symlinks and Windows requires Developer Mode (or Admin) to create symlinks. Add a new `## Building on Windows` section to README.md per RESEARCH §Code Examples + PATTERNS.md §README.md analog #2 + #3. Move the pending todo to resolved/ once the section ships.

Purpose: ROADMAP SC-7 — eliminate the contributor-onboarding wall on default Windows installs. Source content is fully enumerated in `.planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md` §Solution.

Output: 1 README.md edit (new section, ~30 lines), 1 git mv (pending → resolved), 1 atomic commit. Plan is fully autonomous and runnable in parallel with Plan 12.1-06 (no file overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-RESEARCH.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-PATTERNS.md
@.planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md
@README.md

<interfaces>
<!-- Exact section content + insertion point. -->

README.md current structure (lines 1-34, verified by Read):
- Line 1: `# Spine Texture Manager` (title)
- Line 5: `## Installing` (per Plan 12-06 D-16.2)
- Line 11: `## What it does`
- Line 19: `## Requirements`
- Line 24: `## Commands (developer)` (last developer-facing section)
- Line 32: `## Reporting issues` (last section)

Insertion point per PATTERNS.md §README.md (M) — append `## Building on Windows` section: between `## Commands (developer)` (lines 24-30) and `## Reporting issues` (line 32). New section sits adjacent to the developer-facing material, NOT the tester-facing `## Installing` section.

Section content (verbatim from RESEARCH.md §Code Examples §README.md "Building on Windows" lines 354-389):

```markdown
## Building on Windows

`npm run build` packages installers via `electron-builder`, which downloads `winCodeSign-2.6.0.7z` to its cache. This archive contains macOS dylib symlinks; extracting them on Windows requires either Administrator privileges or **Developer Mode** enabled.

### One-time setup (recommended)

1. Open **Settings → Privacy & Security → For developers**.
2. Toggle **Developer Mode** ON. (Grants the current user permission to create symbolic links without elevation.)
3. Restart the terminal.

After this, `npm run build:win` runs as your normal user.

### Quick path: skip signing entirely

For unsigned local test builds (no installer, just a runnable directory):

```powershell
npx electron-builder --win --x64 --dir
```

This sidesteps the `winCodeSign` cache extraction. Output is at `release/win-unpacked/` — run `Spine Texture Manager.exe` directly.

### Alternative: run as Administrator

If Developer Mode is disabled by your IT policy, run the terminal as Administrator instead. `winCodeSign` extracts under elevated privileges and the build proceeds.

### What the error looks like

```
ERROR: Cannot create symbolic link : A required privilege is not held by the client. :
  C:\Users\<you>\AppData\Local\electron-builder\Cache\winCodeSign\<id>\darwin\10.12\lib\libcrypto.dylib
```

`npm run dev` is unaffected — only packaged builds need this.
```

Note on nested code fences: the markdown above contains a nested ```powershell block. When pasting verbatim into README.md, the OUTER markdown code fence (the one wrapping the entire section example above in this PLAN context) is NOT part of the README content — only the markdown text inside it. The README.md file gets the section starting with `## Building on Windows` and ending with the line `npm run dev is unaffected — only packaged builds need this.`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append "## Building on Windows" section to README.md</name>
  <read_first>
    - README.md (FULL file — confirm current structure and pinpoint insertion location between `## Commands (developer)` block and `## Reporting issues` heading)
    - .planning/phases/12.1-installer-auto-update-live-verification/12.1-RESEARCH.md (§Code Examples §README.md "Building on Windows" — the verbatim source content lines 354-389)
    - .planning/phases/12.1-installer-auto-update-live-verification/12.1-PATTERNS.md (§README.md (M) — Insertion point + Source attribution + Anti-pattern note)
    - .planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md (§Solution — confirms the 3 workarounds match the README content)
  </read_first>
  <files>README.md</files>
  <action>
Open README.md. Locate the boundary between the `## Commands (developer)` section (which ends at the line `- `npm run cli -- fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` — CLI table dump`) and the `## Reporting issues` heading.

Insert the following section content INBETWEEN those two sections. The section must START with a blank line (separating from the preceding bullet list), then the new content, then a blank line before the existing `## Reporting issues` heading.

Exact content to insert (paste verbatim — including the nested PowerShell code fence and the verbatim error block):

```markdown

## Building on Windows

`npm run build` packages installers via `electron-builder`, which downloads `winCodeSign-2.6.0.7z` to its cache. This archive contains macOS dylib symlinks; extracting them on Windows requires either Administrator privileges or **Developer Mode** enabled.

### One-time setup (recommended)

1. Open **Settings → Privacy & Security → For developers**.
2. Toggle **Developer Mode** ON. (Grants the current user permission to create symbolic links without elevation.)
3. Restart the terminal.

After this, `npm run build:win` runs as your normal user.

### Quick path: skip signing entirely

For unsigned local test builds (no installer, just a runnable directory):

\`\`\`powershell
npx electron-builder --win --x64 --dir
\`\`\`

This sidesteps the `winCodeSign` cache extraction. Output is at `release/win-unpacked/` — run `Spine Texture Manager.exe` directly.

### Alternative: run as Administrator

If Developer Mode is disabled by your IT policy, run the terminal as Administrator instead. `winCodeSign` extracts under elevated privileges and the build proceeds.

### What the error looks like

\`\`\`
ERROR: Cannot create symbolic link : A required privilege is not held by the client. :
  C:\\Users\\<you>\\AppData\\Local\\electron-builder\\Cache\\winCodeSign\\<id>\\darwin\\10.12\\lib\\libcrypto.dylib
\`\`\`

`npm run dev` is unaffected — only packaged builds need this.
```

When writing this to the file, REPLACE the escaped backticks `\`\`\`` with actual three-backtick fences (the escapes are only present in this PLAN to make the nested fences readable; the file content gets real backticks).

REPLACE the escaped backslashes in the verbatim error path (`C:\\Users\\<you>\\...`) with single backslashes (`C:\Users\<you>\...`). The escapes are PLAN-only; the README content uses literal Windows path separators (single backslashes).

Anti-pattern guards (per PATTERNS.md):
- DO NOT introduce a new CONTRIBUTING.md just for this section — README.md already has developer-facing material; one more section keeps the discoverability radius small.
- DO NOT touch any other README.md section (## Installing, ## What it does, ## Requirements, ## Commands (developer), ## Reporting issues all stay byte-for-byte unchanged).
- DO NOT modify package.json — the workaround is documentation-only; no script changes.
- DO NOT modify electron-builder.yml — the workaround does NOT involve config changes (the `winCodeSign` cache path is electron-builder default; we document around it, not into it).
  </action>
  <verify>
    <automated>grep -q "^## Building on Windows$" README.md && grep -q "Developer Mode" README.md && grep -q "winCodeSign" README.md && grep -q "Cannot create symbolic link" README.md && grep -q -- "--dir" README.md && grep -q "run as Administrator" README.md && grep -q "^## Installing$" README.md && grep -q "^## Commands (developer)$" README.md && grep -q "^## Reporting issues$" README.md && npm run test 2>&1 | grep -E "Tests\s+[0-9]+ passed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Building on Windows$" README.md` returns `1` (new section heading exists)
    - `grep -c "Developer Mode" README.md` returns at least `1` (workaround mentioned)
    - `grep -c "winCodeSign" README.md` returns at least `2` (mentioned in intro paragraph + error block)
    - `grep -c "Cannot create symbolic link" README.md` returns `1` (verbatim error block present)
    - `grep -c -- "--dir" README.md` returns at least `1` (`--dir` quick-path documented)
    - `grep -c "run as Administrator" README.md` returns at least `1` (alternative documented)
    - All 5 pre-existing sections still present (`## Installing`, `## What it does`, `## Requirements`, `## Commands (developer)`, `## Reporting issues`)
    - Section ordering preserved: `## Commands (developer)` precedes `## Building on Windows` precedes `## Reporting issues` (verify with `grep -n "^## " README.md`)
    - `npm run test` reports 434 passing (no test surfaces touched)
    - `npm run typecheck:web` clean
  </acceptance_criteria>
  <done>README.md ## Building on Windows section landed at the correct insertion point with verbatim content from RESEARCH §Code Examples; vitest still 434/434.</done>
</task>

<task type="auto">
  <name>Task 2: Move pending todo file to resolved/ (todo-lifecycle convention)</name>
  <read_first>
    - .planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md (FULL file — confirm content matches the README section that just landed; the todo's §Solution maps 1:1 to the README content)
    - .planning/todos/ directory listing (confirm `resolved/` subdirectory exists; if not, create it)
  </read_first>
  <files>
    - .planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md (deleted)
    - .planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md (created — same content)
  </files>
  <action>
This is a file move (rename). Use `git mv` so git tracks the move as a rename rather than delete+add (preserves blame history).

1. Verify the resolved/ subdirectory exists:
   ```bash
   ls -d .planning/todos/resolved/ 2>/dev/null || mkdir -p .planning/todos/resolved/
   ```

2. Move the pending todo to resolved:
   ```bash
   git mv .planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md \
          .planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md
   ```

3. Optionally append a "Resolved" section to the moved file documenting the resolution (the task is closed by Plan 12.1-07 / README.md ## Building on Windows section). Keep this concise — the lifecycle convention is "move to resolved/ once user-facing fix lands."

Append at the bottom of the moved file (before any existing trailing whitespace):

```markdown

---

## Resolved

2026-04-28 — Phase 12.1 Plan 07: README.md gained a `## Building on Windows` section between `## Commands (developer)` and `## Reporting issues` covering the 3 documented workarounds (Developer Mode / Run as Administrator / `--dir` shortcut). Verbatim error excerpt + Solution-section content moved into README.md. `npm run dev` remains unaffected — only packaged builds need the workaround.
```

Anti-pattern guards:
- DO NOT modify the original todo content (Problem section + verbatim error excerpt + Solution section) — the historical record stays intact.
- DO NOT delete the file outright — the move-to-resolved/ pattern preserves the todo's identity for cross-references (e.g. STATE.md "Deferred Items" table will be updated by Plan 12.1-08 to reflect the resolved status).
- DO NOT create a duplicate todo file in pending/ — `git mv` ensures atomicity.
  </action>
  <verify>
    <automated>! test -f .planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md && test -f .planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md && grep -q "^## Resolved$" .planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md && grep -q "winCodeSign" .planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md && git status --short .planning/todos/ | grep -E "^R" </automated>
  </verify>
  <acceptance_criteria>
    - The pending file no longer exists at `.planning/todos/pending/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md`
    - The resolved file exists at `.planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md`
    - The resolved file contains the original Problem + Solution sections (unchanged) PLUS a new `## Resolved` section documenting Plan 12.1-07
    - `git status --short .planning/todos/` shows the move as a rename (`R` prefix), not separate D + A
    - The resolved file still contains the verbatim error block (`Cannot create symbolic link`)
  </acceptance_criteria>
  <done>Todo moved from pending/ to resolved/ via `git mv` (preserves rename history); appended `## Resolved` section documenting Plan 12.1-07's README change. Task 1 + Task 2 land in ONE atomic commit (the README change + the lifecycle move are coupled).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer Mode toggle (Win10/11) | OS-level setting; grants per-user symlink creation rights without elevation; recommended workaround per Microsoft documentation |
| Administrator terminal session | Elevated process; required only if Developer Mode is policy-disabled |
| `--dir` build (electron-builder unpacked output) | No installer; runnable directory at release/win-unpacked/; sidesteps cache extraction entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-07-01 | Elevation | "run as Administrator" workaround | mitigate | Documented as ALTERNATIVE only; Developer Mode (per-user, non-elevated) is the recommended path; admin only suggested if IT policy blocks Developer Mode |
| T-12.1-07-02 | Elevation | Developer Mode itself | accept | Developer Mode grants symlink-creation only (not arbitrary code execution); per Microsoft documentation it is the intended path for developer machines; per-user setting |
| T-12.1-07-03 | Information Disclosure | README.md path example contains `<you>` placeholder | accept | Path example uses `<you>` placeholder (not a real username); no PII risk |
| T-12.1-07-04 | Tampering | symlink contents (winCodeSign cache) | accept | The macOS dylib symlinks have NO effect on Windows runtime behavior — they are macOS-only files inside the cache; extraction is purely a packaging step; risk surface is contained to the build environment, not the shipped installer |
</threat_model>

<verification>
- README.md has new `## Building on Windows` section at the correct insertion point (between `## Commands (developer)` and `## Reporting issues`).
- The section contains all 3 workarounds (Developer Mode / Run as Administrator / `--dir`) + verbatim error excerpt.
- The pending todo file is moved (via `git mv`) to resolved/ with a `## Resolved` section appended.
- vitest 434/434 still passing (no test surfaces touched).
- typecheck:web clean.
- One atomic commit covering both the README edit and the todo move.
</verification>

<success_criteria>
- ROADMAP SC-7 closed: Windows local-build papercut documented in README.md with 3 ranked workarounds + verbatim error excerpt + `--dir` shortcut for unsigned local test builds.
- Contributor onboarding wall on default Windows installs is documented; future contributors will not waste time debugging `winCodeSign-2.6.0.7z` extraction failures.
- Project todo lifecycle preserved: pending → resolved/ on user-facing fix landing.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-installer-auto-update-live-verification/12.1-07-SUMMARY.md` documenting:
- The README.md insertion point + line count delta (~30 new lines)
- Confirmation that the 3 workarounds + verbatim error block landed verbatim from RESEARCH §Code Examples
- The `git mv` operation (pending → resolved) confirmed via `git status --short` rename indicator
- The commit SHA (single atomic commit)
- vitest 434/434 still green
</output>
</output>
