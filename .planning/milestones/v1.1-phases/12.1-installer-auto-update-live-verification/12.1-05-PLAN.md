---
phase: 12.1
plan: 05
type: execute
wave: 3
depends_on: [12.1-02]
files_modified:
  - .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md
  - docs/install-images/windows-smartscreen-more-info.png
  - docs/install-images/windows-smartscreen-run-anyway.png
autonomous: false
requirements:
  - UPD-06
  - REL-03
  - SC-4  # ROADMAP success criterion 4: Windows manual-fallback verified live
  - SC-5  # ROADMAP success criterion 5: SmartScreen + offline + "up to date" eyeballed
must_haves:
  truths:
    - "Tester downloaded fresh rc2 .exe from GitHub Releases on Windows test host (NOT a local build per RESEARCH §Pitfall 2)"
    - "Tester captured SmartScreen 'Microsoft Defender SmartScreen prevented...' dialog → docs/install-images/windows-smartscreen-more-info.png (≥ 5KB)"
    - "Tester captured SmartScreen 'Run anyway' expanded dialog → docs/install-images/windows-smartscreen-run-anyway.png (≥ 5KB)"
    - "Tester completed install via Run anyway → NSIS installer → app launched; Help → About reads `1.1.0-rc2`"
    - "After Plan 12.1-02 Task 3 publishes rc3, tester relaunched rc2 on Windows; UpdateDialog opens in `windows-fallback` variant (per src/main/auto-update.ts:92 SPIKE_PASSED=false on win32)"
    - "Tester clicked 'Open Release Page' button; system browser opened https://github.com/Dzazaleo/Spine_Texture_Manager/releases (UPD-06 manual-fallback verified live)"
    - "Tester confirmed: NO nag loop, NO modal interruption — non-blocking notice per CONTEXT D-04"
    - "Tester verified offline graceful (no error dialog, no crash) on Windows (UPD-05)"
    - "Tester verified Help → Check for Updates on rc2 with no newer version shows 'You're up to date' (UPD-02)"
    - "Tester verified Help → Installation Guide opens INSTALL.md externally (D-16 surface 3 Windows slice)"
    - "12.1-HUMAN-UAT.md Tests 3 (Windows slice), 4 (Windows slice), 5, 7, 9 (surface 3 Windows slice) flipped from [pending] to passed"
  artifacts:
    - path: "docs/install-images/windows-smartscreen-more-info.png"
      provides: "Real SmartScreen 'prevented an unrecognized app' dialog screenshot from fresh rc2 download"
      min_lines: 0
    - path: "docs/install-images/windows-smartscreen-run-anyway.png"
      provides: "Real SmartScreen expanded 'Run anyway' button screenshot"
      min_lines: 0
    - path: ".planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md"
      provides: "Updated runbook transcript with Windows evidence appended"
      contains: "passed"
  key_links:
    - from: "Windows install (SPIKE_PASSED=false)"
      to: "UpdateDialog 'windows-fallback' variant"
      via: "src/main/auto-update.ts:372-375 variant routing → IPC update:available payload includes variant: 'windows-fallback'"
      pattern: "variant === 'windows-fallback'"
    - from: "'Open Release Page' button click"
      to: "system browser → GitHub Releases page"
      via: "window.api.openExternalUrl('https://github.com/Dzazaleo/Spine_Texture_Manager/releases') → ipc.ts:174 allow-list check → shell.openExternal"
      pattern: "openExternalUrl GITHUB_RELEASES_INDEX_URL"
---

<objective>
Execute the Windows HUMAN-UAT runbook: install rc2 from GitHub Releases (capturing TWO SmartScreen screenshots during first launch), confirm Help → About reads rc2, observe rc2 detect rc3 in `windows-fallback` variant (NOT auto-download — per CONTEXT D-04 SPIKE_PASSED=false), click "Open Release Page" → confirm external browser opens, verify NO nag loop / NO modal interruption, verify offline + "up to date" + Help → Installation Guide. Append transcript evidence to 12.1-HUMAN-UAT.md.

Purpose: Satisfy ROADMAP SC-4 (Windows manual-fallback live) + SC-5 (SmartScreen dialogs + Windows offline/"up to date" eyeballed) + REL-03 captures #2 + #3 (SmartScreen PNGs).

Output: 2 binary PNGs + transcript edits flipping Tests 3 (Windows), 4 (Windows), 5, 7, 9 (surface 3 Windows). Per RESEARCH §Pitfall 7 + CONTEXT D-04: this verification is FALLBACK-ONLY — DO NOT attempt to flip SPIKE_PASSED to true; that is out of 12.1 scope (manual-fallback ships LIVE on Windows by default and IS the contracted UPD-06 path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-RESEARCH.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-PATTERNS.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md
@.planning/phases/12.1-installer-auto-update-live-verification/12.1-02-SUMMARY.md
@INSTALL.md
@src/main/auto-update.ts

<interfaces>
<!-- Windows-specific wiring + capture targets. -->

src/main/auto-update.ts (DO NOT MODIFY in this plan):
- line 92: `const SPIKE_PASSED = process.platform !== 'win32';`  (Windows = FALSE → manual-fallback variant)
- line 372-375: variant routing
  ```ts
  const variant: 'auto-update' | 'windows-fallback' =
    process.platform === 'win32' && !SPIKE_PASSED && !spikeRuntimePass
      ? 'windows-fallback'
      : 'auto-update';
  ```

UpdateDialog.tsx (DO NOT MODIFY):
- variant === 'windows-fallback' branch renders:
  - Version label
  - "Open Release Page" button (per UpdateDialog.tsx:217 button label)
  - NO download button
  - NO restart button
  - Dismissible

ipc.ts SHELL_OPEN_EXTERNAL_ALLOWED (DO NOT MODIFY):
- includes `https://github.com/Dzazaleo/Spine_Texture_Manager/releases` (GITHUB_RELEASES_INDEX_URL constant)

INSTALL.md Windows section (lines 45-71) — wording the captured screenshots must match (per RESEARCH §Pitfall 5):
- line 52: italicized `_"Microsoft Defender SmartScreen prevented an unrecognized app from starting."_`
  - Win10 vs Win11 wording differs subtly per RESEARCH §Pitfall 5 (title may be "Windows protected your PC" on Win10)
  - If captured wording differs, Plan 12.1-06 edits INSTALL.md:52 to match in same commit as binary swap

Screenshot paths (binary-only swap; markdown image references at INSTALL.md:54 + :60 unchanged):
- docs/install-images/windows-smartscreen-more-info.png
- docs/install-images/windows-smartscreen-run-anyway.png

Per Phase 12 winCodeSign todo, the Windows test host is `C:\Users\LeonardoCunha\Desktop\WORKTEMP\Spine_Texture_Manager` Win10 build 22000. Document the exact OS version inline with the captured PNGs (per RESEARCH §Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Install rc2 .exe from GitHub Releases on Windows test host; capture both SmartScreen screenshots</name>
  <what-built>
    - Plan 12.1-02 Task 2 published rc2 with .exe
  </what-built>
  <how-to-verify>
**CRITICAL (per RESEARCH §Pitfall 2):** Download fresh from GitHub Releases. Do NOT use a local `release/*.exe`.

Sequence (BLOCKING):

1. STOP — confirm rc2 published:
   ```powershell
   gh release view v1.1.0-rc2 --json isDraft -q .isDraft
   ```
   Expected: `false`.

2. On the Windows test host, download `Spine-Texture-Manager-1.1.0-rc2-x64.exe` from:
   `https://github.com/Dzazaleo/Spine_Texture_Manager/releases/tag/v1.1.0-rc2`

3. Verify the file is fully downloaded:
   ```powershell
   Get-Item .\Spine-Texture-Manager-1.1.0-rc2-x64.exe | Select-Object Length
   ```
   Compare against the GitHub release asset size.

4. Double-click the .exe in Explorer. Windows SmartScreen dialog appears.

5. CAPTURE PNG #1 (the initial "Microsoft Defender SmartScreen prevented..." dialog):
   - Use Win+Shift+S → Window snip → click the SmartScreen dialog
   - Save to clipboard, then paste into MS Paint OR use Snipping Tool to save directly as PNG
   - Save as: `C:\Users\<you>\Documents\windows-smartscreen-more-info.png` (or wherever convenient)
   - Then transfer to the dev box (over network share, USB, or `gh repo clone` + commit + push from Windows directly):
     ```powershell
     # If on Windows test host with the repo cloned:
     Copy-Item .\windows-smartscreen-more-info.png .\docs\install-images\windows-smartscreen-more-info.png -Force
     ```
   - Verify size:
     ```powershell
     (Get-Item .\docs\install-images\windows-smartscreen-more-info.png).Length
     ```
     Expected: ≥ 5000.

6. STOP — paste below: (a) verbatim transcription of the SmartScreen dialog title + body text (so 12.1-06 can verify INSTALL.md:52 wording match per RESEARCH §Pitfall 5); (b) Windows OS version (`winver`); (c) PNG #1 file size.

7. Click **More info** in the SmartScreen dialog. The dialog expands to show "Publisher: Unknown publisher" + a **Run anyway** button.

8. CAPTURE PNG #2 (the expanded "Run anyway" view) using same method as step 5. Save as `docs/install-images/windows-smartscreen-run-anyway.png`. Verify ≥ 5KB.

9. STOP — paste below: PNG #2 file size + verbatim transcription of the expanded dialog (publisher line + button labels).

10. Click **Run anyway**. NSIS installer launches. Follow installer prompts (default install location is `%LOCALAPPDATA%\Programs\Spine Texture Manager` — per-user, no admin rights).

11. After install, launch from Start Menu (search "Spine Texture Manager"). Verify Help → About reads `1.1.0-rc2`.

12. STOP — paste below: confirmation "Help → About reads 1.1.0-rc2 on Windows".
  </how-to-verify>
  <resume-signal>Type "captured — rc2 installed on Windows; 2 PNGs ready; Help→About reads rc2" with all evidence pasted. Type "wording-mismatch — see paste" if step 6(a) or step 9 reveals INSTALL.md needs text edits (Plan 12.1-06 will pick up). Type "rework — <reason>" for any failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: After rc3 publishes, observe rc2 on Windows opens UpdateDialog in windows-fallback variant + click 'Open Release Page'</name>
  <what-built>
    - Task 1: rc2 installed on Windows, Help → About reads rc2
    - Plan 12.1-02 Task 3 published rc3 (PRE-CONDITION before this task)
  </what-built>
  <how-to-verify>
Pre-condition: rc3 published.

This task verifies the FALLBACK behavior — NOT auto-update — per CONTEXT D-04. The expected behavior is:
- UpdateDialog opens in `'windows-fallback'` variant (NOT `'auto-update'`)
- "Open Release Page" button (NOT "Download + Restart")
- Clicking the button opens the GitHub Releases index page in the system browser
- NO automatic download
- NO restart prompt
- NO nag loop (dismiss closes dialog; no re-prompt for same version on next startup)

Sequence:

1. STOP — confirm rc3 published:
   ```powershell
   gh release view v1.1.0-rc3 --json isDraft -q .isDraft
   ```
   Expected: `false`.

2. Quit rc2 if running. Relaunch rc2 from Start Menu.

3. Wait ~5-10s. Observe: UpdateDialog appears with `windows-fallback` variant content:
   - Version label showing `1.1.0-rc3` (the available version)
   - "Open Release Page" button (per UpdateDialog.tsx:217 — exact label transcribed in Plan 12.1-06 if differs)
   - NO "Download + Restart" button
   - NO "Restart" button

4. STOP — paste below: (a) screenshot OR transcribe the dialog content; (b) confirm: button label is "Open Release Page" (or note the exact wording if different per RESEARCH §Open Question 4).

5. Click **Open Release Page**. Default browser opens with URL:
   `https://github.com/Dzazaleo/Spine_Texture_Manager/releases`

6. STOP — paste browser URL bar contents (must byte-match the GITHUB_RELEASES_INDEX_URL constant at src/main/auto-update.ts:73 + ipc.ts:174 allow-list).

7. Close the browser tab. Return to the Windows app. Confirm:
   - The app remains responsive (NO modal interruption blocking the app)
   - NO automatic download was triggered (check `%LOCALAPPDATA%\Programs\Spine Texture Manager\` for any new files — should be unchanged)
   - The dialog can be dismissed (close button or click outside the dialog overlay per UpdateDialog ARIA modal pattern)

8. Click "Later" or dismiss the UpdateDialog. Quit rc2 (`File → Exit` or close window).

9. **Nag-loop test:** Relaunch rc2 from Start Menu. Wait ~10s.

10. Expected: UpdateDialog does NOT re-appear for rc3 (per UPD-04 D-08 dismissedUpdateVersion semantics — Later persists "rc3 dismissed" to update-state.json).

11. STOP — paste below: (a) confirmation "rc3 dialog did NOT re-appear on second launch"; (b) confirm app continues to work normally (load a fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json file; do a basic action).
  </how-to-verify>
  <resume-signal>Type "windows-fallback verified — no nag loop, browser opens external" with paste-back evidence. Type "auto-update fired — SPIKE_PASSED appears flipped" if Download + Restart appeared (this would be a regression — STOP and investigate src/main/auto-update.ts:92). Type "rework — <reason>" for any other failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify offline graceful + "up to date" + Help → Installation Guide on Windows + flip 12.1-HUMAN-UAT tests</name>
  <what-built>
    - Task 1: SmartScreen PNGs captured, rc2 installed
    - Task 2: windows-fallback verified, no nag loop
  </what-built>
  <how-to-verify>
**Sub-test A — "Up to date" on Windows (UPD-02):**

1. With rc2 still installed (Windows doesn't auto-update past Task 2 — it stays on rc2 by design per D-04 manual-fallback), trigger Help → Check for Updates manually.

2. Expected behavior on Windows when a newer version EXISTS in feed: UpdateDialog opens in `windows-fallback` variant (same as Task 2). The "up to date" state on Windows is therefore only observable when rc2 IS the latest published version — which is NOT the current state (rc3 exists).

3. Workaround for verification: temporarily test against a state where rc2 IS latest:
   - Option A: ask user to test BEFORE rc3 is tagged (chronologically — but rc3 was tagged in Plan 12.1-02 Task 3, so this requires a separate rc4 + manual edit)
   - Option B: edit `%APPDATA%\spine-texture-manager-updater\update-state.json` to set `dismissedUpdateVersion: "1.1.0-rc999"` then trigger Help → Check for Updates; the prompt should be suppressed AND if the manual-check fires the "up to date" path is NOT shown on Windows fallback (windows-fallback only routes update-available; up-to-date message on Windows would route through the auto-update path).
   - Option C: SIMPLEST — verify via the macOS slice (already passed in Plan 12.1-03) and Linux slice (Plan 12.1-04). Mark Test 3 (Windows slice) as `[passed-by-parity — windows-fallback variant does not surface "up to date" by design; behavior verified on macOS + Linux instead]`.

4. STOP — paste below: which option used + brief evidence/rationale.

**Sub-test B — Offline graceful on Windows (UPD-05):**

5. Disable network: PowerShell `Disable-NetAdapter -Name "*" -Confirm:$false` OR System Settings → Network → Wi-Fi → Off.

6. Quit rc2. Relaunch from Start Menu.

7. Confirm: app launches normally to load screen, NO error dialog, NO crash.

8. STOP — paste confirmation.

9. Re-enable network: `Enable-NetAdapter -Name "*" -Confirm:$false` OR Settings → Wi-Fi → On.

**Sub-test C — Help → Installation Guide on Windows (D-16 surface 3):**

10. With network restored, click Help → Installation Guide…

11. Default browser opens with URL `https://github.com/Dzazaleo/Spine_Texture_Manager/blob/main/INSTALL.md`.

12. STOP — paste browser URL contents.

**Sub-test D — Flip 12.1-HUMAN-UAT.md tests:**

13. Edit `.planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md` to flip:
    - Test 3 (Help → Check for Updates "up to date") — Windows slice → result: passed (with the rationale from sub-test A); if both macOS + Linux + Windows slices passed/passed-by-parity, Test 3 graduates to passed
    - Test 4 (Offline graceful) — Windows slice → result: passed; if macOS + Linux + Windows all passed, Test 4 graduates to passed
    - Test 5 (Windows manual-fallback notice) → result: passed; append transcript from Task 2 step 4 + step 6 + step 11
    - Test 7 (SmartScreen PNGs) → result: passed; append transcript from Task 1 steps 6 + 9 + reference both PNG paths
    - Test 9 (INSTALL.md 4 link surfaces) — surface 3 Windows slice → append transcript from sub-test C; if all 3 OS slices passed, surface 3 graduates to passed

14. Update Summary tally; bump `updated:` ISO-8601.

15. Commit:
    ```bash
    git add .planning/phases/12.1-installer-auto-update-live-verification/12.1-HUMAN-UAT.md docs/install-images/windows-smartscreen-more-info.png docs/install-images/windows-smartscreen-run-anyway.png
    git commit -m "test(12.1-05): Windows HUMAN-UAT — Tests 3,4,5,7,9 (surface 3 Windows) passed; SmartScreen PNGs captured"
    ```

Paste below: confirmation + commit SHA.
  </how-to-verify>
  <resume-signal>Type "Windows runbook complete — Tests 3,4,5,7,9.3 flipped" with the commit SHA. Type "partial — <details>" for any sub-test failure. Windows slice of Phase 12.1 complete; 4 of 4 INSTALL.md PNGs captured across Plans 03 + 04 + 05 (gating Plan 12.1-06 binary swap).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Releases → Windows test host | TLS download of .exe; SmartScreen displays mark-of-the-web warning because installer is unsigned |
| rc2 installed app → GitHub Releases feed | electron-updater HTTPS fetch to /latest.yml; variant routing decides whether to download (auto-update path) or just notify (windows-fallback path) |
| 'Open Release Page' click | window.api.openExternalUrl(GITHUB_RELEASES_INDEX_URL) → ipc.ts:174 allow-list → shell.openExternal → user's default browser |
| update-state.json read/write | App-side persistence at %APPDATA%\spine-texture-manager-updater\update-state.json; atomic .tmp + rename pattern; JSON validate-version-FIRST + silent-swallow on read failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-05-01 | Spoofing | unsigned .exe execution | mitigate | SmartScreen "Run anyway" + INSTALL.md walkthrough — explicit user consent to run unsigned binary; per v1.1 milestone scope (no EV cert); contracted DIST-05 acceptance |
| T-12.1-05-02 | Tampering | docs/install-images/windows-smartscreen-*.png | mitigate | PNG magic-bytes + ≥5KB size both gated by tests/integration/install-md.spec.ts |
| T-12.1-05-03 | Information Disclosure | SmartScreen screenshot may capture filename / username / publisher info | mitigate | Tester reviews PNG before commit; usernames are tester's own; "Publisher: Unknown publisher" is the expected content; no third-party PII |
| T-12.1-05-04 | Elevation | NSIS installer prompts for elevation | mitigate | electron-builder.yml nsis.perMachine: false → per-user install at %LOCALAPPDATA%; no admin rights required; no privilege escalation |
| T-12.1-05-05 | Spoofing | external browser URL drift | mitigate | URL is hardcoded constant `GITHUB_RELEASES_INDEX_URL` at src/main/auto-update.ts:73; allow-listed at ipc.ts:174 (Set.has byte-equivalence); no runtime construction |
| T-12.1-05-06 | Denial of Service | nag-loop regression | mitigate | UPD-04 D-08 dismissedUpdateVersion persistence; compareSemver strict-`>` suppression; Task 2 step 9-11 verifies no re-prompt for same version |
</threat_model>

<verification>
- Tests 3 (Windows slice), 4 (Windows slice), 5, 7 in 12.1-HUMAN-UAT.md flipped to passed.
- Test 9 surface 3 Windows slice flipped — if all 3 OS slices pass, surface 3 graduates to passed.
- docs/install-images/windows-smartscreen-more-info.png ≥ 5KB.
- docs/install-images/windows-smartscreen-run-anyway.png ≥ 5KB.
- One atomic commit with all 3 file changes.
- DO NOT modify src/main/auto-update.ts (SPIKE_PASSED constant unchanged — manual-fallback ships LIVE on Windows by design per CONTEXT D-04).
</verification>

<success_criteria>
- ROADMAP SC-4 verified live on Windows: windows-fallback variant + Open Release Page → external browser; no nag loop, no modal interruption.
- ROADMAP SC-5 partial: SmartScreen PNGs captured + match INSTALL.md (or wording-mismatches flagged for 12.1-06); offline + (Windows slice of) "up to date" verified.
- REL-03 captures #2 + #3 of 4 land. Together with Plans 03 (PNG #1 macOS) + 04 (PNG #4 Linux), all 4 INSTALL.md PNGs are captured and ready for Plan 12.1-06 binary swap.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-installer-auto-update-live-verification/12.1-05-SUMMARY.md` documenting:
- Both SmartScreen PNG captures (sizes + Win OS version + provenance)
- The verbatim wording transcripts (for 12.1-06 wording-match check)
- The windows-fallback variant verification (button label + URL + no-nag-loop evidence)
- The "up to date" + offline + Help→Installation Guide sub-test results
- Any wording mismatches flagged for Plan 12.1-06
- Confirmation that 4 of 4 INSTALL.md PNGs are now captured (Plans 03 + 04 + 05)
- The commit SHA(s)
</output>
