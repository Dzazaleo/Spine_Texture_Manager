---
phase: 12.1
slug: installer-auto-update-live-verification
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-28
---

# Phase 12.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **Phase character note:** 12.1 is a *live-verification + docs* phase, not a feature dev phase. Most rows below are intentionally `manual / HUMAN-UAT` because the goal is to flip the 9 `human_needed` items in `12-VERIFICATION.md` to `passed` via real installer + real GitHub feed + real OS first-launch dialog observation. Where the planner can synthesize an automated check (e.g. file-size assertion that PNGs are no longer 1×1 placeholders), it does — but the load-bearing verification surface for this phase is human-attested transcripts.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.0.x (433/433 passing as of Phase 12 close) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npm run test` |
| **Full suite command** | `npm run test` (no quick-vs-full split today) |
| **Estimated runtime** | ~3 seconds (vitest run, 38 files) |

---

## Sampling Rate

- **After every task commit:** Run `npm run test`
- **After every plan wave:** Run `npm run test` (same — no split)
- **Before `/gsd-verify-work`:** Full suite must be green AND all 9 `human_needed` rows in 12-VERIFICATION.md flipped (or carried over to v1.2 with rationale)
- **Max feedback latency:** ~5s (vitest)
- **Phase gate add-on:** `gh release view v1.1.0-rc2 --json assets -q '.assets | length'` returns 6 (3 installers + 3 feed files) — proves publish-race fix landed.

---

## Per-Task Verification Map

> Populated by gsd-planner once plans exist. Anchor rows below show the *kinds* of checks that will land. Manual rows reference `12.1-HUMAN-UAT.md` runbook steps (greenfield surface for this phase).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 12.1-01-* | 01 (publish-race fix) | 1 | UPD-06 (live), success-criterion 1 | T-12.1-01 (config drift) | electron-builder.yml `publish: null`; static `build/app-update.yml` injected via extraResources | smoke (grep) | `grep -E '^publish:\s*null' electron-builder.yml && test -f build/app-update.yml` | ❌ W0 (build/app-update.yml greenfield) | ⬜ pending |
| 12.1-02-* | 02 (CI tag-push supervision rc2 + rc3) | 2 | UPD-06 (live), success-criterion 1 | — | tag push produces 6-asset GitHub Release atomically | observability (gh CLI) | `gh release view v1.1.0-rc2 --json assets -q '.assets \| length'` returns 6 | N/A — runbook | ⬜ pending |
| 12.1-03-* | 03 (macOS HUMAN-UAT runbook) | 3 | success-criteria 2, 5 | — | rc2 detects rc3, downloads, relaunches; "Later" suppresses re-prompt; offline graceful; "up to date" message | manual / HUMAN-UAT | N/A | manual-only | ⬜ pending |
| 12.1-04-* | 04 (Linux AppImage HUMAN-UAT) | 3 | success-criteria 3, 5 | — | Same as macOS via AppImage; libfuse2t64 dlopen error captured on Ubuntu 24.04 | manual / HUMAN-UAT | N/A | manual-only | ⬜ pending |
| 12.1-05-* | 05 (Windows HUMAN-UAT — manual-fallback variant) | 3 | success-criteria 4, 5 | — | UpdateDialog opens in `windows-fallback` variant; "Open Release Page" opens GitHub Releases; SmartScreen "Run anyway" walkthrough matches INSTALL.md | manual / HUMAN-UAT | N/A | manual-only | ⬜ pending |
| 12.1-06-* | 06 (binary PNG swap to docs/install-images/) | 4 | REL-03 (screenshots), success-criterion 6 | T-12.1-06 (PNG integrity) | 4 captured PNGs replace the 1×1 placeholders; PNG magic bytes preserved; size > 5 KB each | smoke (file-size + magic) | `for f in docs/install-images/*.png; do test $(stat -f%z "$f") -gt 5000; done && npm run test -- tests/integration/install-md.spec.ts` | extend existing | ⬜ pending |
| 12.1-07-* | 07 (winCodeSign Windows-build doc) | 4 | success-criterion 7 | — | "Building on Windows" section exists at agreed location with Developer Mode + admin + `--dir` workarounds | smoke (grep) | `grep -lE 'Developer Mode\|winCodeSign' README.md CONTRIBUTING.md 2>/dev/null` returns ≥ 1 path | greenfield section | ⬜ pending |
| 12.1-08-* | 08 (12-VERIFICATION flip + 12.1-VERIFICATION authoring) | 5 | success-criterion 8 | — | All 9 `human_needed` rows in 12-VERIFICATION.md flipped to `passed` (or `deferred_v1.2` with rationale) | smoke (grep + count) | `grep -c "human_needed" .planning/phases/12-auto-update-tester-install-docs/12-VERIFICATION.md` returns 0 | edit existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `build/app-update.yml` — greenfield static `electron-updater` feed config (provider: github, owner: Dzazaleo, repo: Spine_Texture_Manager, updaterCacheDirName).
- [ ] `12.1-HUMAN-UAT.md` — greenfield 9-step runbook surface in this phase directory; each step has expected-evidence + transcript-line slots.
- [ ] **OPTIONAL** `tests/integration/install-md.spec.ts` extension — 1 new `test()` block asserting each `docs/install-images/*.png` is > 5 KB (placeholders are 67 bytes; real captures are 50–200 KB). Soft gate against accidentally re-committing placeholders.

*No new vitest framework install or fixture additions needed. All auto-update code surfaces are already covered by `tests/main/auto-update.spec.ts` (20 tests), `tests/main/update-state.spec.ts` (10), `tests/renderer/update-dialog.spec.tsx` (16) — Phase 12 deliverables.*

---

## Manual-Only Verifications

The 9 `human_needed` rows from `12-VERIFICATION.md` map 1:1 to runbook steps in this phase. Each completes by appending a transcript line (date + tester + observed evidence + screenshot reference if applicable) to `12.1-HUMAN-UAT.md` AND back-folding the row in `12-VERIFICATION.md` from `human_needed` → `passed`.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| macOS auto-update happy path (UPD-01..UPD-04) | UPD-06 (live) | Real installer + real GitHub feed + real macOS first-launch dialog | Plan 12.1-PLAN-03 runbook step 3 |
| Linux AppImage auto-update happy path | UPD-06 (live) | Same as macOS via AppImage | Plan 12.1-PLAN-04 runbook step 3 |
| Help → Check for Updates ("up to date") | UPD-02 | Real-time GitHub feed query from packaged install | Plan 12.1-PLAN-03/04/05 runbook step 4 |
| Offline graceful (no error dialog, no crash) | UPD-05 | Network manipulation outside vitest scope | Plan 12.1-PLAN-03/04/05 runbook step 5 |
| Windows manual-fallback notice (windows-fallback variant) | UPD-06 | Packaged .exe + GitHub feed; CONTRACTED Windows behavior per Phase 12 D-04 | Plan 12.1-PLAN-05 runbook step 3 |
| Gatekeeper "Open Anyway" first-launch on macOS | REL-03 | Tester papercut surface; needs fresh install | Plan 12.1-PLAN-03 runbook step 1 + screenshot capture |
| SmartScreen "More info → Run anyway" on Windows | REL-03 | Tester papercut surface; needs fresh install | Plan 12.1-PLAN-05 runbook step 1 + screenshot capture |
| libfuse2t64 error on Ubuntu 24.04 | REL-03 | OS-version-specific dlopen failure; INSTALL.md text-byte-match | Plan 12.1-PLAN-04 runbook step 6 + screenshot capture |
| INSTALL.md 4 link surfaces | REL-03 / D-16 | Surfaces 1+2 are GitHub render-time; surfaces 3+4 are in-app menu/dialog clicks | Plan 12.1-PLAN-03 runbook step 6 (in-app menu); Plan 12.1-PLAN-02 runbook step 7 (release-template render after rc2 tag-push) |

*See `.planning/phases/12.1-installer-auto-update-live-verification/12.1-RESEARCH.md` § "Validation Architecture" for the long-form rationale and § "Recommended Plan Structure" for the runbook decomposition.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify, are explicitly classified `manual / HUMAN-UAT`, or declare a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive automated tasks without a `npm run test` invocation
- [ ] Wave 0 covers all MISSING references (`build/app-update.yml`, `12.1-HUMAN-UAT.md`)
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s (vitest is ~3s; gh CLI release-inspection is ~2s)
- [ ] `nyquist_compliant: true` set in frontmatter once planner populates the per-task map and authors `12.1-HUMAN-UAT.md`

**Approval:** pending
