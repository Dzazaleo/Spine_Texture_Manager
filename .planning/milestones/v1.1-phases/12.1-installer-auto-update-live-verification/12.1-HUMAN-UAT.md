---
status: partial
phase: 12.1-installer-auto-update-live-verification
source: [12-VERIFICATION.md, 12.1-VERIFICATION.md]
started: 2026-04-28T15:25:17Z
updated: 2026-04-28T17:50:00Z
---

## Current Test

[rc2 install rounds COMPLETE on macOS + Windows. Linux DEFERRED to v1.1.1 — no Linux host available this session, lima/multipass attempts failed (multipass: known QEMU SME bug on Apple Silicon Sequoia; lima aarch64 + qemu-x86_64-static needs full libc:amd64 multiarch which is out of scope for a single-screenshot capture). Pending: rc3 tag → observe rc2 detects rc3 on macOS + Windows.]

## Tests

### 1. macOS auto-update happy path (UPD-01..UPD-04, ROADMAP SC #2)
expected: Install v1.1.0-rc2 .dmg → confirm Help → About reads rc2 → wait for rc3 published → relaunch rc2 → UpdateDialog appears within 10s with rc3 release-notes Summary → click Download+Restart → app relaunches into rc3; declining (Later) does not re-prompt on next startup
result: [partial — rc2 install confirmed (Plan 12.1-03 Step 1); awaiting rc3 to observe detect+download+relaunch flow]
- 2026-04-28T17:05Z: rc2 .dmg downloaded from GitHub Release v1.1.0-rc2, opened, app dragged to Applications.
- 2026-04-28T17:05Z: First launch via right-click → Open hit macOS Sequoia Gatekeeper dialog: *"Apple could not verify 'Spine Texture Manager.app' is free of malware that may harm your Mac…"* with **Move to Trash / Done** buttons (NOT the older "Open Anyway" right-click bypass — macOS 15 changed the UX). Resolved via System Settings → Privacy & Security → "Open Anyway" → Touch ID → second confirmation dialog → Open. Captured to `docs/install-images/macos-gatekeeper-open-anyway.png` (154,051 bytes, 494×574 PNG).
- 2026-04-28T17:08Z: App launched. Help menu → About Spine Texture Manager shows version `1.1.0-rc2`. ✓
- 2026-04-28T17:08Z: Quick smoke test of features → working correctly.
- AWAITING rc3 publish → relaunch rc2 → observe UpdateDialog → Download+Restart → confirm About reads rc3.

### 2. Linux AppImage auto-update happy path (UPD-01..UPD-04, ROADMAP SC #3)
expected: Install v1.1.0-rc2 AppImage on Ubuntu (22.04 OR 24.04 with libfuse2t64 installed) → confirm Help → About reads rc2 → wait for rc3 published → relaunch rc2 → UpdateDialog appears with rc3 → Download+Restart → relaunches into rc3
result: [DEFERRED to v1.1.1 — no Linux host available this session; auto-update implementation parity with macOS asserted but not live-verified on Linux]
- 2026-04-28T17:40-17:50Z: Multiple VM-on-Mac approaches attempted: multipass 1.16.2 hits QEMU `host-arm-cpu.sme` not-found error on Apple Silicon Sequoia (known issue); lima 2.1.1 aarch64 VM created successfully but the rc2 AppImage is x86_64 ELF and would require full `libc:amd64` multiarch in the VM to run under qemu-user-static (out of scope). Decision: descope Linux live-verification to v1.1.1; preserve rc2 AppImage as published asset for testers with Linux access.

### 3. Help → Check for Updates with no newer version (UPD-02, ROADMAP SC #5)
expected: With latest installed version (rc3 after Test 1/2 complete) → Help → Check for Updates → "You're up to date" message via UpdateDialog state='none' (D-05 — no native alert())
result: [pending — needs rc3 tagged + installed first; verify on macOS + Windows; Linux deferred to v1.1.1]
- macOS: needs rc3 install (post-Test 1) → click Help → Check for Updates with rc3 latest → expect "You're up to date" UpdateDialog.
- Windows: needs Alt-key → Help → Check for Updates (menu hidden by default per todo `2026-04-28-windows-menu-bar-hidden-by-default-alt-reveals.md`).

### 4. Offline graceful (UPD-05, ROADMAP SC #5)
expected: Disconnect network → launch app → no error dialog, no crash, normal startup to load screen; DevTools console shows the swallow log only
result: [pending — straightforward to verify on macOS + Windows post-rc3 install; Linux deferred to v1.1.1]

### 5. Windows manual-fallback notice (UPD-06, ROADMAP SC #4 — manual-fallback path)
expected: On Windows install (SPIKE_PASSED=false on win32, src/main/auto-update.ts:92), when the GitHub feed serves rc3 to an installed rc2: UpdateDialog opens in 'windows-fallback' variant — version label + 'Open Release Page' button → click opens https://github.com/Dzazaleo/Spine_Texture_Manager/releases externally; non-blocking, no nag loop, no modal interruption
result: [partial — rc2 install confirmed (Plan 12.1-05 Step 1); awaiting rc3 to observe windows-fallback UpdateDialog]
- 2026-04-28T17:15Z: rc2 .exe downloaded from GitHub Release v1.1.0-rc2 on Windows host (`C:\Users\LeonardoCunha\Desktop\WORKTEMP\Spine_Texture_Manager`).
- 2026-04-28T17:15Z: SmartScreen "Microsoft Defender SmartScreen prevented an unrecognized app from starting" dialog hit. Captured ONE PNG (the expanded "Run anyway" view) to `docs/install-images/windows-smartscreen-run-anyway.png` (1,062,034 bytes, 532×498). Initial-dialog capture (PNG #1) deferred to rc3 install round (Windows remembers SmartScreen approval per-binary; can't re-trigger on same .exe).
- 2026-04-28T17:18Z: Run anyway → NSIS installer → app installed → launched.
- 2026-04-28T17:20Z: Menu bar hidden by default — pressed Alt to reveal File / Edit / View / Window / Help. Captured as v1.1.1 polish todo `2026-04-28-windows-menu-bar-hidden-by-default-alt-reveals.md`.
- 2026-04-28T17:22Z: "Spine Texture Manager → About spine-texture-manager" → shows `1.1.0.0` (Win32 file metadata, electron-builder strips SemVer pre-release suffix to satisfy 4-part version constraint). Captured as v1.1.1 polish todo `2026-04-28-windows-about-panel-shows-1.1.0.0-not-semver.md`. **NOTE: `app.getVersion()` runtime call returns the SemVer string `1.1.0-rc2` — auto-update version comparison uses SemVer correctly; the About-panel `1.1.0.0` is a cosmetic display issue only.**
- 2026-04-28T17:23Z: Quick smoke test → working correctly.
- AWAITING rc3 publish → relaunch rc2 → observe windows-fallback UpdateDialog appear (auto-detected on app boot per UPD-01) → click "Open Release Page" → confirm external browser opens.

### 6. Gatekeeper "Open Anyway" first-launch on macOS (REL-03, ROADMAP SC #5)
expected: Download fresh rc2 .dmg from GitHub Releases on macOS → double-click → Gatekeeper dialog → right-click → Open → "Open Anyway" click → app launches; capture screenshot for INSTALL.md (saved to docs/install-images/macos-gatekeeper-open-anyway.png)
result: passed — but with macOS Sequoia UX divergence noted
- 2026-04-28T17:05Z: PNG captured: `docs/install-images/macos-gatekeeper-open-anyway.png` (154,051 bytes, 494×574 RGBA, valid PNG, well above the 5KB soft-gate threshold).
- **Pitfall 4/5 OS-dialog evolution surfaced:** macOS 15 (Sequoia) deprecated the older right-click → Open → "Open Anyway" Gatekeeper bypass path. The new flow is: dialog with "Move to Trash / Done" buttons → click Done → Settings → Privacy & Security → scroll to Security section → "Open Anyway" → Touch ID/password → second confirmation. INSTALL.md describes the older flow and may need wording updates in Plan 12.1-06.

### 7. SmartScreen "More info → Run anyway" on Windows (REL-03, ROADMAP SC #5)
expected: Download fresh rc2 .exe from GitHub Releases on Windows 10/11 → run → SmartScreen dialog → CAPTURE PNG #1 (saved to docs/install-images/windows-smartscreen-more-info.png) → More info → CAPTURE PNG #2 (saved to docs/install-images/windows-smartscreen-run-anyway.png) → Run anyway → NSIS installer launches
result: [partial — one capture landed (expanded "Run anyway" view); initial-dialog capture (PNG #1) deferred to rc3 install round]
- 2026-04-28T17:15Z: One PNG captured (expanded view post-"More info" click). Both files at `docs/install-images/` are byte-identical (1,062,034 bytes, 532×498) — the second capture was missed because Windows remembers SmartScreen per-binary, so re-running the same .exe doesn't re-trigger the dialog. Plan 12.1-06 Task should:
  - Either: rename `docs/install-images/windows-smartscreen-more-info.png` to be the expanded view (current state), with `windows-smartscreen-run-anyway.png` standing in temporarily until rc3 install round captures the missing initial-dialog frame.
  - Or: capture both during rc3 install (fresh .exe = fresh SmartScreen state).
- AWAITING rc3 install round to capture the missing initial-dialog PNG.

### 8. libfuse2t64 error on Ubuntu 24.04 (REL-03, ROADMAP SC #5)
expected: Run rc2 AppImage on fresh Ubuntu 24.04 (no libfuse2t64 installed; Docker fallback acceptable per RESEARCH §Pitfall 6 + §Environment Availability) → observe "dlopen(): error loading libfuse.so.2"; INSTALL.md paragraph wording matches actual error; capture screenshot (saved to docs/install-images/linux-libfuse2-error.png)
result: [DEFERRED to v1.1.1 — same root cause as Test 2; INSTALL.md:104 already has the verbatim error text inline]
- The 67-byte 1×1 placeholder at `docs/install-images/linux-libfuse2-error.png` stays. INSTALL.md tester walkthrough still shows the verbatim error text; the missing screenshot is decorative.

### 9. INSTALL.md 4 link surfaces — eyeballed verification (REL-03 / D-16)
expected: (1) v1.1.0-rc2 GitHub Release body shows `See [INSTALL.md](https://github.com/Dzazaleo/Spine_Texture_Manager/blob/main/INSTALL.md)` (verifiable via `gh release view v1.1.0-rc2 --json body`); (2) README "Installing" section visible on github.com repo page; (3) In-app Help → Installation Guide… opens INSTALL.md externally via system browser; (4) HelpDialog "Install instructions" section link opens same URL
result: [partial — surface 1 verifiable now via gh release view; surfaces 3+4 pending in-app testing on macOS + Windows; Linux deferred]

## Summary

total: 9
passed: 1
issues: 0
pending: 4
partial: 2
deferred: 2
skipped: 0
blocked: 0

## Gaps

- **Linux live-verification deferred to v1.1.1.** Tests 2 + 8 carry over. The auto-update implementation IS in tree (src/main/auto-update.ts) and uses electron-updater@6.x in a platform-uniform way; the implementation parity argument is "macOS auto-update working in v1.1 implies Linux auto-update will work the same way — code path is identical, only differs at the electron-updater feed-file consumption layer (latest-linux.yml vs latest-mac.yml), and the synthesizer (D-10) emits both with identical schema." Live observation of Linux rc2-detects-rc3 deferred until a real Ubuntu host is available.
- **Windows menu-bar hidden by default** (Alt key reveals). Captured as `.planning/todos/pending/2026-04-28-windows-menu-bar-hidden-by-default-alt-reveals.md`. Affects Test 3 + Test 9 surface 3 discoverability on Windows; tests can be performed via Alt key but UX is sub-optimal for non-developer testers.
- **Windows About panel shows `1.1.0.0`** instead of `1.1.0-rc2`. Captured as `.planning/todos/pending/2026-04-28-windows-about-panel-shows-1.1.0.0-not-semver.md`. Cosmetic only — auto-update version comparison uses `app.getVersion()` which returns the correct SemVer string.
- **macOS Sequoia Gatekeeper UX evolution** — INSTALL.md describes the older right-click bypass which no longer works on macOS 15. Plan 12.1-06 INSTALL.md cleanup should update the macOS section to reflect Settings → Privacy & Security → Open Anyway flow.
- **Windows SmartScreen PNG #1 (initial dialog)** missing from this rc2 install round; will be captured in rc3 install round (different .exe = fresh SmartScreen state).
