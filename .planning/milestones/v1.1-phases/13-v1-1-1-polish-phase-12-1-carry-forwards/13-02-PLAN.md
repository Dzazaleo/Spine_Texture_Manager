---
phase: 13-v1-1-1-polish-phase-12-1-carry-forwards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/todos/pending/2026-04-28-electron-updater-prerelease-channel-mismatch.md
  - .planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md
autonomous: true
requirements: []
tags:
  - docs
  - release-engineering
  - electron-updater
  - semver

must_haves:
  truths:
    - "CLAUDE.md has a `## Release tag conventions` section that is 5-10 lines of substantive content (heading + 1 orienting sentence + 2 example bullets + 1-line rationale + 1 cross-link), per CONTEXT D-05 + §specifics"
    - "The section documents `v1.2.0-rc.1` ✅ as the correct dot-separated form and `v1.2.0-rc1` ❌ as the broken non-dot form"
    - "The section's rationale explains why: electron-updater 6.x channel-name comparison treats `rc1` / `rc2` as opaque tokens unless the dot is present"
    - "The section cross-links to `.planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md` (NOT to `pending/` — the file moves to `resolved/` in this same commit)"
    - "The pending todo moved from `.planning/todos/pending/` to `.planning/todos/resolved/` via `git mv` (88% rename similarity preserved; cross-link target valid at commit time)"
    - "The moved todo gained a `## Resolved` section documenting the CLAUDE.md docs landing + the deliberate v1.2+ deferral of the workflow-level regex guard"
    - "No edits to `.github/workflows/release.yml` (D-05: docs-only fix, no workflow guard in this phase)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "New `## Release tag conventions` section locking the dot-separated prerelease format as project convention"
      contains: "## Release tag conventions"
    - path: ".planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md"
      provides: "Resolved rc-channel-mismatch todo with appended `## Resolved` section + preserved root-cause walkthrough"
      contains: "## Resolved"
  key_links:
    - from: "CLAUDE.md `## Release tag conventions` section cross-link"
      to: ".planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md"
      via: "markdown relative-path link"
      pattern: "todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch"
    - from: "Project release tag-push convention"
      to: "electron-updater 6.x channel matching"
      via: "semver.prerelease() returning [token, N] vs [token]"
      pattern: "rc\\\\.[0-9]+"
---

<objective>
Land the project-wide release-tag convention as docs-only guidance in CLAUDE.md (per CONTEXT D-05 — workflow-level regex guard deliberately deferred to v1.2+ as overkill for a single-developer project), and `git mv` the associated pending todo to `resolved/` in the same atomic commit so the cross-link target exists at commit time.

Purpose: Close Phase 12.1 carry-forward #2 (rc-channel naming convention) at the documentation layer. The convention activates the next time a prerelease tag is pushed; existing rc-shaped tags (`v1.1.0-rc1` / `-rc2` / `-rc3`) stay as-is in the release history (no rewrite, no force-push — explicitly out of scope per D-05).

Output: 1 atomic commit covering 2 file changes (CLAUDE.md edit + 1 git mv with `## Resolved` append).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-v1-1-1-polish-phase-12-1-carry-forwards/13-CONTEXT.md
@.planning/phases/13-v1-1-1-polish-phase-12-1-carry-forwards/13-PATTERNS.md
@.planning/todos/pending/2026-04-28-electron-updater-prerelease-channel-mismatch.md
@CLAUDE.md
@.planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md

<interfaces>
<!-- Section structure of CLAUDE.md (current state). Embedded so executor doesn't need to re-read for shape. -->

CLAUDE.md current section order (heading lines only):
- L1  `# Spine Texture Manager — Claude project notes`
- L3  `## What this project is`
- L7  `## Source of truth`
- L14 `## Critical non-obvious facts (do not relitigate)` (6 numbered points; ends ~L21)
- L23 `## Test fixture`
- L27 `## Folder conventions (do not misread)`
- L32 `## Commands`
- L40 `## GSD workflow`

PATTERNS.md preferred insertion position (Claude's-Discretion CONTEXT.md §decisions item 1):
**BETWEEN `## Critical non-obvious facts` (ends ~L21) AND `## Test fixture` (starts L23)** — the new section is also a "non-obvious fact" / constraint on a future operation (tag pushes) rooted in upstream tooling (electron-updater + semver). Sits with the other constraints.

Tone: terse, factual, "do not relitigate" voice. Mirror the imperative-mood opening style used by `## Folder conventions (do not misread)`. No essay; 5-10 lines max.

Resolved-todo `## Resolved` shape (canonical from `.planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md` lines 45-49):
```markdown

---

## Resolved

2026-04-28 — Phase X Plan Y: <one-sentence summary>. <Optional second sentence about live-verification deferral if applicable.>
```
</interfaces>

<analogs>
<!-- Direct in-repo precedents. -->

- **Docs-only edit + git mv with `## Resolved` append, single atomic commit:** commit `ad6d9bf` (`docs(12.1-07)`); README edit + 1 rename + section append.
- **Section style precedent:** `CLAUDE.md` `## Folder conventions (do not misread)` (L27-30) — terse imperative-mood opener + bulleted list of factual statements + inline backticks for paths.
- **PATTERNS.md §CLAUDE.md (M):** drafted section content verbatim (5-10 lines; one ✅ + one ❌ + one-line rationale + one cross-link).
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert `## Release tag conventions` section in CLAUDE.md + git mv pending → resolved + append `## Resolved` section — single atomic commit</name>
  <read_first>
    - CLAUDE.md (FULL — confirm current section order; line numbers may have shifted but the section between `## Critical non-obvious facts` and `## Test fixture` is the insertion point per PATTERNS.md)
    - .planning/phases/13-v1-1-1-polish-phase-12-1-carry-forwards/13-PATTERNS.md (§CLAUDE.md (M) — section content verbatim + insertion-position rationale + tone-matching guidance)
    - .planning/todos/pending/2026-04-28-electron-updater-prerelease-channel-mismatch.md (FULL — the file being moved; do NOT modify its existing Problem/Solution/Verification path/Cross-references/Sibling todos sections)
    - .planning/todos/resolved/2026-04-28-windows-packaged-build-fails-on-wincodesign-symlink-extract.md lines 45-49 (the canonical `## Resolved` section shape from 12.1-07)
    - .planning/phases/13-v1-1-1-polish-phase-12-1-carry-forwards/13-CONTEXT.md §decisions D-05 (the explicit workflow-guard-deferral rationale)
  </read_first>
  <files>
    - CLAUDE.md
    - .planning/todos/pending/2026-04-28-electron-updater-prerelease-channel-mismatch.md
    - .planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md
  </files>
  <action>
**Step 1 — Insert new section in CLAUDE.md between `## Critical non-obvious facts` and `## Test fixture`:**

Open `CLAUDE.md`. Find the line `## Test fixture` (currently at line 23 — verify with `grep -n "^## Test fixture$" CLAUDE.md`). Insert the following 11-line block (1 heading + blank + 1 sentence + blank + 2 bullets + blank + 1-line rationale + blank + 1 cross-link + trailing blank) IMMEDIATELY BEFORE that line:

```markdown
## Release tag conventions

Prerelease tags MUST use **dot-separated** number suffixes:

- ✅ `v1.2.0-rc.1` — semver parses as `["rc", 1]`; electron-updater 6.x channel-match works.
- ❌ `v1.2.0-rc1`  — semver parses as `["rc1"]` (single opaque token); rc1 → rc2 auto-update silently fails.

Rationale: `electron-updater@6.x`'s GitHub provider compares prerelease tokens as channel names; `"rc1" === "rc2"` is `false`, so an installed `v1.2.0-rc1` cannot detect `v1.2.0-rc2`. Final → final and final → prerelease paths are unaffected.

See `.planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md` for the full root cause walkthrough.

```

After insertion, verify with `grep -c "^## " CLAUDE.md` — should return `8` (was `7`; the new section adds one). Verify section order with `grep -n "^## " CLAUDE.md`:

```
3:## What this project is
7:## Source of truth
14:## Critical non-obvious facts (do not relitigate)
23:## Release tag conventions      ← new
35:## Test fixture                  ← shifted from 23
... etc
```

(Line numbers approximate; the exact post-insertion line numbers depend on the comment block density.)

**Step 2 — `git mv` the pending todo:**

```bash
git mv .planning/todos/pending/2026-04-28-electron-updater-prerelease-channel-mismatch.md \
       .planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md
```

Verify `git status --short .planning/todos/` shows `R` (rename) prefix, NOT separate `D` + `A`. If it shows D+A, abort and re-run as `git mv` (not `mv` + add + rm).

**Step 3 — Append `## Resolved` section to the moved file:**

Open `.planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md` and APPEND (after the last existing line, preserving all prior content byte-for-byte; the file currently ends with `## Sibling todos from same testing round` block):

```markdown

---

## Resolved

2026-04-28 — Phase 13 Plan 02: CLAUDE.md gained a `## Release tag conventions` section between `## Critical non-obvious facts` and `## Test fixture` documenting `v1.2.0-rc.1` ✅ vs `v1.2.0-rc1` ❌ with one-line rationale (electron-updater 6.x channel-name comparison; semver prerelease parser semantics) and a cross-link back to this resolved todo. Workflow-level regex guard at `.github/workflows/release.yml:43-54` intentionally deferred to v1.2+ per D-05 — CLAUDE.md docs are sufficient for a single-developer project, and the next prerelease cycle (if any) will adopt the dot-form naturally. Existing rc-shaped tags (`v1.1.0-rc1` / `-rc2` / `-rc3`) stay as-is in release history; no rewrite, no force-push.
```

**Step 4 — Stage and commit (single atomic commit):**

```bash
git add CLAUDE.md \
        .planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md
# git mv operation is already staged.

git commit -m "$(cat <<'EOF'
docs(13-02): document release tag conventions in CLAUDE.md

Closes 1 of 3 v1.1.1 polish carry-forwards from Phase 12.1 (Anti-Pattern
#1 in 12.1-VERIFICATION.md — rc-channel naming convention).

CLAUDE.md gains a new `## Release tag conventions` section between
`## Critical non-obvious facts` and `## Test fixture` documenting:
- ✅ v1.2.0-rc.1 (dot-separated; semver parses as ["rc", 1])
- ❌ v1.2.0-rc1  (no dot;       semver parses as ["rc1"] opaque token)
- Rationale: electron-updater@6.x GitHub provider compares prerelease
  tokens as channel names; "rc1" === "rc2" is false, so an installed
  rc1 cannot detect rc2 as newer.
- Cross-link to the moved resolved todo for the full root-cause walkthrough.

Per D-05 (CONTEXT.md §decisions): docs-only fix; workflow-level regex
guard at .github/workflows/release.yml:43-54 deferred to v1.2+ as
overkill for a single-developer project. The convention activates the
next time a prerelease tag is pushed; existing rc-shaped tags
(v1.1.0-rc1 / -rc2 / -rc3) stay as-is in release history (no rewrite,
no force-push).

git mv (88% rename similarity preserves blame):
- pending/2026-04-28-electron-updater-prerelease-channel-mismatch.md
  → resolved/ (with `## Resolved` section appended documenting closure
  + deliberate workflow-guard deferral rationale).

Refs: 12.1-VERIFICATION.md §Anti-Patterns Found #1.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

**Anti-pattern guards:**
- DO NOT introduce a CONTRIBUTING.md just for this section. CLAUDE.md is the right home (consistent with how `## Folder conventions` and `## Commands` already live there).
- DO NOT modify `.github/workflows/release.yml` — D-05 is explicit: docs-only, no workflow guard.
- DO NOT modify the original Problem / Solution / Verification path / Cross-references / Sibling todos sections of the moved file — historical record stays intact; only APPEND `## Resolved` at the bottom.
- DO NOT change the cross-link target to `pending/` — the cross-link MUST point at `resolved/` since the file moves in this same commit (the link target is valid at commit time, not before).
- DO NOT bundle the src/main/index.ts edits or package.json bump or VERIFICATION flips into this commit — those are Plan 01, Plan 03, and Plan 04 concerns.
- DO NOT delete the moved file outright — `git mv` is the only acceptable mechanic (preserves blame).
- DO NOT change CLAUDE.md tone or expand into an essay — 5-10 substantive lines max per CONTEXT §specifics.
  </action>
  <verify>
    <automated>grep -n "^## Release tag conventions$" CLAUDE.md | wc -l | grep -q "^[[:space:]]*1$" && grep -q "v1.2.0-rc.1" CLAUDE.md && grep -q "v1.2.0-rc1" CLAUDE.md && grep -q "electron-updater" CLAUDE.md && grep -q "todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch" CLAUDE.md && [ ! -f .planning/todos/pending/2026-04-28-electron-updater-prerelease-channel-mismatch.md ] && [ -f .planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md ] && grep -q "^## Resolved$" .planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md && grep -q "Phase 13 Plan 02" .planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md && [ $(grep -c "^## " CLAUDE.md) -eq 8 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^## Release tag conventions$" CLAUDE.md` returns exactly one match
    - `grep -c "^## " CLAUDE.md` returns `8` (was `7`; +1 new section)
    - `grep -q "v1.2.0-rc.1" CLAUDE.md` succeeds (dot-form ✅ example present)
    - `grep -q "v1.2.0-rc1" CLAUDE.md` succeeds (no-dot ❌ example present)
    - `grep -q "electron-updater" CLAUDE.md` succeeds (rationale references the upstream tooling)
    - `grep -q "todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch" CLAUDE.md` succeeds (cross-link points at resolved/, not pending/)
    - The new section is positioned BETWEEN `## Critical non-obvious facts` and `## Test fixture` (verifiable: `grep -n "^## " CLAUDE.md` shows the new heading line number is between the two; awk-able)
    - `ls .planning/todos/pending/2026-04-28-electron-updater-prerelease-channel-mismatch.md` returns "No such file or directory"
    - `ls .planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md` returns the file
    - The resolved file contains a `## Resolved` heading at end-of-file
    - The resolved file's `## Resolved` section mentions "Phase 13 Plan 02" and references CLAUDE.md
    - `git log --diff-filter=R --name-only -1 -- .planning/todos/` includes the renamed file
    - No edits to .github/workflows/release.yml, src/main/index.ts, package.json, or any verification doc (all out-of-scope for this plan)
    - `git status` clean (commit landed)
    - Commit subject starts with `docs(13-02):` and has Co-Authored-By footer
  </acceptance_criteria>
  <done>Single atomic commit lands: CLAUDE.md gains the 5-10-line `## Release tag conventions` section + 1 git mv with `## Resolved` append. CLAUDE.md goes from 7 to 8 top-level sections; pending/ todo gone, resolved/ todo present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits only documentation (CLAUDE.md project notes + a planning todo file). No code-execution surfaces touched, no external inputs flow through any added surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | CLAUDE.md project-notes file | accept | Public planning surface; all edits are tracked in git history with provenance commit message; no secrets, no PII. |
| T-13-02-02 | Tampering | git mv of 1 pending todo to resolved/ | accept | Documentation lifecycle move; `git mv` preserves history (88% rename similarity); commit history records the change. |
| T-13-02-03 | Information Disclosure | (none) | accept | Section content describes a public upstream-tooling quirk (electron-updater channel-matching). No secrets, no PII. |

**No new attack surface.** This plan is docs-only.
</threat_model>

<verification>
- CLAUDE.md has exactly 8 `## ` top-level sections (was 7).
- The new `## Release tag conventions` section sits between `## Critical non-obvious facts` and `## Test fixture`.
- The section contains both `v1.2.0-rc.1` (✅) and `v1.2.0-rc1` (❌) examples + rationale + cross-link to `.planning/todos/resolved/2026-04-28-electron-updater-prerelease-channel-mismatch.md`.
- The pending rc-channel-mismatch todo moved to resolved/ via `git mv` (R prefix in `git status --short`).
- The resolved file gained a `## Resolved` section referencing Phase 13 Plan 02 + the CLAUDE.md landing + the v1.2+ workflow-guard deferral.
- 1 atomic commit covering both changes.
- No edits to `.github/workflows/release.yml` (D-05 explicit deferral).
</verification>

<success_criteria>
- 1 of 3 v1.1.1 polish carry-forwards CLOSED at the documentation level (Anti-Pattern #1 in 12.1-VERIFICATION.md).
- Project convention locked in CLAUDE.md — the next prerelease tag adopts the dot-form by reading the project notes.
- 1 todo file moved to resolved/ with PRESERVE-HISTORY-style `## Resolved` annotation.
- Workflow-level regex guard explicitly deferred to v1.2+ per D-05 (documented in commit message + resolved-todo `## Resolved` block).
</success_criteria>

<output>
After completion, create `.planning/phases/13-v1-1-1-polish-phase-12-1-carry-forwards/13-02-SUMMARY.md` documenting:
- The CLAUDE.md insertion line range (post-insertion line numbers for the new section)
- The pre-edit and post-edit `## ` heading counts (7 → 8) for sanity
- The git mv operation + resolved/ destination path
- The single atomic commit SHA
- Anti-Pattern #1 in 12.1-VERIFICATION.md flagged for in-place flip in Plan 04 (forward reference)
</output>
