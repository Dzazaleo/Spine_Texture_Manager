---
phase: 26.1
plan: 03
type: execute
wave: 2
depends_on: [26.1-01]
files_modified:
  - src/renderer/src/components/AppShell.tsx
autonomous: true
requirements: [UI-06, UI-10]

must_haves:
  truths:
    - "All 5 toolbar buttons (Atlas Preview, Documentation, Optimize Assets, Save, Open) are exactly h-8 (32px) tall"
    - "In atlas-less mode, the load-summary chip shows '{n} images' instead of '1 atlases'"
    - "In atlas mode, the chip continues to show '1 atlases' unchanged"
    - "The N selected / N total count cell has min-w-[6rem] to prevent layout shift"
  artifacts:
    - path: "src/renderer/src/components/AppShell.tsx"
      provides: "Unified toolbar height + atlas-less chip conditional + count cell min-width"
      contains: "loaderMode === 'atlas-less'"
  key_links:
    - from: "AppShell.tsx atlas counter chip (line ~1314)"
      to: "loaderMode state variable"
      via: "conditional ternary"
      pattern: "loaderMode === 'atlas-less'"
---

<objective>
Harmonize toolbar button heights to h-8 (32px) across all 5 action buttons, add the atlas-less mode conditional to the load-summary chip so it shows image count instead of atlas count, and add min-w-[6rem] to the count cell to prevent layout shift. All changes are in AppShell.tsx only (plus the SearchBar already handled in Plan 01).

Purpose: Unified toolbar height (D-09 / UI-06 / UI-10) eliminates the visual inconsistency between buttons of different natural heights. The atlas-less chip label (D-08) gives users accurate feedback when operating in images-folder mode.
Output: AppShell.tsx updated with h-8 on 5 buttons, conditional chip label, and count cell min-width.
</objective>

<execution_context>
@/Users/leo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26.1-ui-polish-visual-wins/26.1-CONTEXT.md
@.planning/phases/26.1-ui-polish-visual-wins/26.1-UI-SPEC.md
@.planning/phases/26.1-ui-polish-visual-wins/26.1-PATTERNS.md

<interfaces>
<!-- Exact current state of each change site, extracted from PATTERNS.md -->

Atlas counter chip (AppShell.tsx lines 1307–1316):
```tsx
<div
  className="inline-flex items-center gap-3 border border-border rounded-md px-3 py-1 text-xs font-mono text-fg-muted"
  aria-label="Load summary"
>
  <span><span className="text-fg font-semibold">1</span> skeletons</span>
  <span aria-hidden="true" className="text-border">|</span>
  <span><span className="text-fg font-semibold">1</span> atlases</span>
  <span aria-hidden="true" className="text-border">|</span>
  <span><span className="text-fg font-semibold">{effectiveSummary.attachments.count}</span> regions</span>
</div>
```
Change: replace the `<span>...<span>1</span> atlases</span>` with a conditional based on `loaderMode`.

Border-style toolbar button (AppShell.tsx line ~1372, repeated at ~1384, ~1403, ~1413):
```
border border-border rounded-md px-3 py-1 text-xs font-semibold transition-colors cursor-pointer hover:border-accent hover:text-accent active:bg-accent/10 focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:border-border disabled:hover:text-fg disabled:active:bg-transparent flex-shrink-0
```
Change: replace `py-1` with `h-8`.

Filled accent button (AppShell.tsx line ~1392, Optimize Assets):
```
bg-accent text-panel rounded-md px-3 py-1 text-xs font-semibold transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex-shrink-0
```
Change: replace `py-1` with `h-8`.

Count cell (GlobalMaxRenderPanel.tsx line 837 — handled in Plan 02 file scope; count cell is actually in GlobalMaxRenderPanel, NOT AppShell):
```tsx
<span className="text-fg-muted font-mono text-sm font-normal ml-auto">
  {selected.size} selected / {sorted.length} total
</span>
```
NOTE: The count cell is in GlobalMaxRenderPanel.tsx, not AppShell.tsx. Plan 02 owns GlobalMaxRenderPanel.tsx already. To avoid file overlap between Plan 02 and Plan 03, the count cell min-w-[6rem] change is moved to Plan 04 (which also touches GlobalMaxRenderPanel for C-09).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atlas counter chip — atlas-less conditional (C-05)</name>
  <files>src/renderer/src/components/AppShell.tsx</files>
  <read_first>
    - src/renderer/src/components/AppShell.tsx (read lines 1300–1325 to see the full load-summary chip div and confirm: (1) the exact current inner span for atlases, (2) that `loaderMode` is in scope at this point, (3) that `effectiveSummary.skippedAttachments` is accessible)
  </read_first>
  <action>
Find the load-summary chip div (aria-label="Load summary"). Inside it, find the span that renders `<span className="text-fg font-semibold">1</span> atlases`.

Replace that single span with a conditional:
```tsx
{loaderMode === 'atlas-less' ? (
  <span>
    <span className="text-fg font-semibold">
      {effectiveSummary.attachments.count - (effectiveSummary.skippedAttachments?.length ?? 0)}
    </span>
    {' images'}
  </span>
) : (
  <span><span className="text-fg font-semibold">1</span> atlases</span>
)}
```

Requirements:
- The `loaderMode` variable is already in scope at this location (confirmed via CONTEXT.md canonical refs line 274). Do NOT add a new variable or prop.
- `effectiveSummary.skippedAttachments` uses optional chaining `?.length` with `?? 0` fallback — do not assume it is always defined.
- The fallback branch `<span><span className="text-fg font-semibold">1</span> atlases</span>` is verbatim unchanged from the current implementation (it is not modified, just wrapped in the else branch).
- All class strings are literals. The computation `attachments.count - (skippedAttachments?.length ?? 0)` is a JS expression, not a class string — this is permitted.
- Do NOT change any other part of the load-summary chip (the skeletons span, the separator spans, the regions span, the outer div).
  </action>
  <verify>
    <automated>grep -c "loaderMode === 'atlas-less'" src/renderer/src/components/AppShell.tsx && grep -c "skippedAttachments?.length ?? 0" src/renderer/src/components/AppShell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "loaderMode === 'atlas-less'" src/renderer/src/components/AppShell.tsx` returns at least 1 (the chip conditional)
    - `grep -c "skippedAttachments?.length ?? 0" src/renderer/src/components/AppShell.tsx` returns 1
    - `grep -c "images'" src/renderer/src/components/AppShell.tsx` returns at least 1 (the ' images' text node)
    - The fallback branch still contains `1</span> atlases` for the non-atlas-less case
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>Load-summary chip shows {n} images in atlas-less mode; shows '1 atlases' in auto mode; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Toolbar button height harmonization — all 5 buttons to h-8 (C-06)</name>
  <files>src/renderer/src/components/AppShell.tsx</files>
  <read_first>
    - src/renderer/src/components/AppShell.tsx (read lines 1337–1420 to see all toolbar buttons; identify every button element and its current py-1 occurrence; count them to confirm there are exactly 5 buttons total: Atlas Preview, Documentation, Optimize Assets, Save, Open)
  </read_first>
  <action>
In the toolbar button cluster (lines ~1337–1416 in AppShell.tsx), find every occurrence of `py-1` in button className strings and replace with `h-8`.

There are two button class patterns to update:

**Pattern A — Border-style buttons (Atlas Preview, Documentation, Save, Open — 4 buttons):**
Current:
```
border border-border rounded-md px-3 py-1 text-xs font-semibold transition-colors cursor-pointer hover:border-accent hover:text-accent active:bg-accent/10 focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:border-border disabled:hover:text-fg disabled:active:bg-transparent flex-shrink-0
```
After — replace `py-1` with `h-8` (everything else stays verbatim):
```
border border-border rounded-md px-3 h-8 text-xs font-semibold transition-colors cursor-pointer hover:border-accent hover:text-accent active:bg-accent/10 focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:border-border disabled:hover:text-fg disabled:active:bg-transparent flex-shrink-0
```

**Pattern B — Filled accent button (Optimize Assets — 1 button):**
Current:
```
bg-accent text-panel rounded-md px-3 py-1 text-xs font-semibold transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex-shrink-0
```
After — replace `py-1` with `h-8`:
```
bg-accent text-panel rounded-md px-3 h-8 text-xs font-semibold transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex-shrink-0
```

Execution steps:
1. Read lines 1337–1420 to find every button and confirm their className strings
2. Replace `py-1` with `h-8` in each button — do NOT touch any other tokens in the string
3. Confirm the checkbox label line (~1349) already has `flex items-center` — if it does, no change needed there (it centers correctly at any height without a fixed h- token)
4. Do NOT change the py-1 in the load-summary chip div (that chip is not a button and should not be changed — it is at line ~1307, outside the button cluster scope)

Tailwind v4 literal-class discipline: the new class strings with `h-8` are static string literals. No template interpolation.
  </action>
  <verify>
    <automated>grep -n 'py-1[^.5]' src/renderer/src/components/AppShell.tsx | grep -v '^\s*//' | grep 'rounded-md px-3' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 5 toolbar button className strings contain `h-8` (not `py-1`) — verified by reading the file after edit
    - `grep 'rounded-md px-3 py-1 ' src/renderer/src/components/AppShell.tsx` returns 0 results (no button still uses py-1)
    - `grep -c 'rounded-md px-3 h-8' src/renderer/src/components/AppShell.tsx` returns 5 (all 5 buttons)
    - The load-summary chip div (aria-label="Load summary") class string is NOT changed (it uses `py-1` or `py-1.5` as part of the chip sizing — that is outside scope; verify it was not accidentally modified)
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>All 5 toolbar buttons use h-8; no button retains py-1 in its class string; load-summary chip unchanged; tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "loaderMode === 'atlas-less'" src/renderer/src/components/AppShell.tsx` → ≥ 1
2. `grep -c "rounded-md px-3 h-8" src/renderer/src/components/AppShell.tsx` → 5
3. `grep 'rounded-md px-3 py-1 ' src/renderer/src/components/AppShell.tsx` → 0 results
4. `npm run test` exits 0
</verification>

<success_criteria>
- Atlas counter chip: `loaderMode === 'atlas-less'` conditional renders `{n} images`; else `1 atlases`
- All 5 toolbar buttons: h-8 replaces py-1
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-ui-polish-visual-wins/26.1-03-SUMMARY.md`
</output>
