---
phase: 26.1
plan: "01"
subsystem: renderer-ui
tags: [css-tokens, color-scheme, layout, tailwind]
dependency_graph:
  requires: []
  provides: [color-token-foundation, full-width-panels, harmonized-search-bar]
  affects: [all-renderer-surfaces-via-cascade, GlobalMaxRenderPanel, AnimationBreakdownPanel, SearchBar]
tech_stack:
  added: []
  patterns: [css-custom-property-cascade, tailwind-v4-literal-class]
key_files:
  created: []
  modified:
    - src/renderer/src/index.css
    - src/renderer/src/panels/GlobalMaxRenderPanel.tsx
    - src/renderer/src/panels/AnimationBreakdownPanel.tsx
    - src/renderer/src/components/SearchBar.tsx
decisions:
  - "D-01/D-02 hex values locked: surface=#232732, panel=#2c3347, border=#384158 (no change)"
  - "Full-width panels: max-w-6xl mx-auto removed from both panel root divs"
  - "SearchBar height: py-1.5 replaced with h-8 for 32px toolbar alignment"
  - "Pre-existing test failures (3 tests) confirmed unrelated to this plan's changes"
metrics:
  duration: "~5 minutes"
  completed_date: "2026-05-04"
  tasks_completed: 2
  tasks_total: 2
---

# Phase 26.1 Plan 01: Color Token Foundation + Full-Width Panels Summary

**One-liner:** Cool blue-dark color token reset (#232732 surface, #2c3347 panel, #384158 border) cascades to every surface via CSS custom property inheritance; both data panels stretch edge-to-edge with max-width constraints removed; SearchBar input harmonized to 32px height.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Replace color tokens in index.css (C-01) | 3ba692e | src/renderer/src/index.css |
| 2 | Remove max-width + harmonize SearchBar height (C-02, C-06 partial) | 720574d | GlobalMaxRenderPanel.tsx, AnimationBreakdownPanel.tsx, SearchBar.tsx |

## Changes Made

### Task 1: Color Token Update (3ba692e)

Replaced warm-stone Tailwind variable references with literal hex values in `src/renderer/src/index.css` `@theme inline` block:

- `--color-surface`: `var(--color-stone-950)` → `#232732`
- `--color-panel`: `var(--color-stone-900)` → `#2c3347`
- `--color-border`: `var(--color-stone-800)` → `#384158`

All other tokens (fg, fg-muted, accent, accent-muted, danger, success, warning, font stacks) unchanged. The `@theme inline` block's cascade behavior means every modal, panel, and toolbar component inherits the new blue-dark scheme without any per-file JSX changes.

### Task 2: Full-Width Panels + SearchBar Height (720574d)

- **GlobalMaxRenderPanel.tsx** root div: `"w-full max-w-6xl mx-auto p-8"` → `"w-full p-8"`
- **AnimationBreakdownPanel.tsx** root div: `"w-full max-w-6xl mx-auto p-8"` → `"w-full p-8"`
- **SearchBar.tsx** input: `py-1.5` → `h-8` (remaining class tokens unchanged)

## Verification

```
grep -c '#232732' src/renderer/src/index.css              → 1 ✓
grep -c 'max-w-6xl' GlobalMaxRenderPanel.tsx              → 0 ✓
grep -c 'max-w-6xl' AnimationBreakdownPanel.tsx           → 0 ✓
grep 'h-8' SearchBar.tsx                                  → present ✓
grep -c 'var(--color-stone-95' index.css                  → 0 ✓
```

Test suite: 752 passing, 3 failing (pre-existing failures confirmed by running with stashed changes — same 3 failures exist on base HEAD). No regressions introduced by this plan.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None. Pure CSS token + className string changes. No new data flows, no new IPC channels, no user input surfaces added.

## Self-Check: PASSED

- `src/renderer/src/index.css` exists and contains `#232732`: FOUND
- `src/renderer/src/panels/GlobalMaxRenderPanel.tsx` contains `"w-full p-8"` without `max-w-6xl`: FOUND
- `src/renderer/src/panels/AnimationBreakdownPanel.tsx` contains `"w-full p-8"` without `max-w-6xl`: FOUND
- `src/renderer/src/components/SearchBar.tsx` contains `h-8` without `py-1.5`: FOUND
- Commit `3ba692e` exists: FOUND
- Commit `720574d` exists: FOUND
