---
phase: 26.1
plan: 04
type: execute
wave: 3
depends_on: [26.1-01, 26.1-02, 26.1-03]
files_modified:
  - src/renderer/src/panels/MissingAttachmentsPanel.tsx
  - src/renderer/src/panels/UnusedAssetsPanel.tsx
  - src/renderer/src/panels/AnimationBreakdownPanel.tsx
  - src/renderer/src/panels/GlobalMaxRenderPanel.tsx
autonomous: true
requirements: [UI-06, UI-07, UI-10]

must_haves:
  truths:
    - "MissingAttachmentsPanel header has bg-danger/10 background and border-danger/40 border (not bg-panel / border-border)"
    - "UnusedAssetsPanel header has the same danger styling as MissingAttachmentsPanel"
    - "Both danger panel headers have a 16px SVG triangle icon before the left-accent bar"
    - "AnimationBreakdownPanel header has an inline SVG bar-chart icon, 'Animation Breakdown' title in text-sm font-semibold text-fg, and a right-aligned animation count chip"
    - "GlobalMaxRenderPanel count cell has min-w-[6rem] text-right to prevent layout shift"
  artifacts:
    - path: "src/renderer/src/panels/MissingAttachmentsPanel.tsx"
      provides: "Danger-themed header"
      contains: "bg-danger/10 px-6 py-2 text-xs text-fg flex flex-wrap items-center gap-2"
    - path: "src/renderer/src/panels/UnusedAssetsPanel.tsx"
      provides: "Danger-themed header"
      contains: "bg-danger/10 px-6 py-2 text-xs text-fg flex flex-wrap items-center gap-2"
    - path: "src/renderer/src/panels/AnimationBreakdownPanel.tsx"
      provides: "Stronger section header with icon + count chip"
      contains: "filteredCards.length} animations"
    - path: "src/renderer/src/panels/GlobalMaxRenderPanel.tsx"
      provides: "Count cell with min-w-[6rem]"
      contains: "min-w-[6rem] text-right"
  key_links:
    - from: "MissingAttachmentsPanel.tsx header div"
      to: "bg-danger/10 token"
      via: "CSS custom property cascade from index.css"
      pattern: "border-danger/40 bg-danger/10"
    - from: "AnimationBreakdownPanel.tsx header"
      to: "filteredCards state"
      via: "filteredCards.length expression"
      pattern: "filteredCards.length"
---

<objective>
Upgrade the two "problem zone" panel headers (MissingAttachmentsPanel and UnusedAssetsPanel) to danger-themed styling with a warning icon, add an icon + count chip to the AnimationBreakdownPanel section header, and add min-w-[6rem] to the GlobalMaxRenderPanel count cell. These are the final visual polish items from the phase.

Purpose: Danger-themed headers (D-11) make it immediately clear — even before the user expands the section — that MissingAttachments and UnusedAssets panels contain problems. Stronger section headers (D-12) give AnimationBreakdownPanel visual parity with the already-strong GlobalMaxRenderPanel header. The count cell min-width (D-09) prevents layout jitter.
Output: 4 files updated; all changes are className string replacements and minor JSX additions.
</objective>

<execution_context>
@/Users/leo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26.1-ui-polish-visual-wins/26.1-CONTEXT.md
@.planning/phases/26.1-ui-polish-visual-wins/26.1-UI-SPEC.md
@.planning/phases/26.1-ui-polish-visual-wins/26.1-PATTERNS.md

<interfaces>
<!-- Exact current state of each change site, extracted from PATTERNS.md -->

From MissingAttachmentsPanel.tsx lines 56–59 (header div):
```tsx
<div
  role="alert"
  aria-label="Missing attachment PNGs"
  className="border-b border-border bg-panel px-6 py-2 text-xs text-fg flex flex-wrap items-center gap-2"
>
  <span
    className="inline-block w-1 h-4 bg-danger flex-shrink-0"
    aria-hidden="true"
  />
```

From UnusedAssetsPanel.tsx lines 49–58 (header div):
```tsx
<div
  role="alert"
  aria-label="Orphaned image files"
  className="border-b border-border bg-panel px-6 py-2 text-xs text-fg flex flex-wrap items-center gap-2"
>
  <span
    className="inline-block w-1 h-4 bg-danger flex-shrink-0"
    aria-hidden="true"
  />
```

From AnimationBreakdownPanel.tsx lines 400–402 (section header):
```tsx
<header className="mb-4 flex items-center gap-4">
  <h2 className="text-lg font-semibold">Animation Breakdown</h2>
</header>
```

From GlobalMaxRenderPanel.tsx line 837 (count chip span):
```tsx
<span className="text-fg-muted font-mono text-sm font-normal ml-auto">
  {selected.size} selected / {sorted.length} total
</span>
```

From GlobalMaxRenderPanel.tsx lines 820–840 (full header block, already satisfies D-12 except count chip min-width):
```tsx
<header className="mb-4 flex items-center gap-2 text-sm font-semibold text-fg">
  <span aria-hidden="true" className="inline-flex items-center justify-center w-5 h-5 text-fg">
    <svg viewBox="0 0 20 20" stroke="currentColor" strokeWidth="1.5" ... >
      ...
    </svg>
  </span>
  <span>Global Max Render Scale</span>
  {savingsPct ... (savings chip)}
  <span className="text-fg-muted font-mono text-sm font-normal ml-auto">
    {selected.size} selected / {sorted.length} total
  </span>
</header>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Danger-themed headers for MissingAttachmentsPanel + UnusedAssetsPanel (C-08)</name>
  <files>
    src/renderer/src/panels/MissingAttachmentsPanel.tsx
    src/renderer/src/panels/UnusedAssetsPanel.tsx
  </files>
  <read_first>
    - src/renderer/src/panels/MissingAttachmentsPanel.tsx (read lines 50–80 to see the full header div, the left-accent bar span, and any count text or content that follows — confirm the exact current className before editing)
    - src/renderer/src/panels/UnusedAssetsPanel.tsx (read lines 45–75 — identical header structure to confirm line numbers and exact class strings)
  </read_first>
  <action>
Apply the same change to both files. The change is two parts per file: (1) update the outer header div className, (2) insert a danger warning icon immediately before the existing left-accent bar span.

**For MissingAttachmentsPanel.tsx:**

Change the outer header div className from:
```
border-b border-border bg-panel px-6 py-2 text-xs text-fg flex flex-wrap items-center gap-2
```
To:
```
border-b border-danger/40 bg-danger/10 px-6 py-2 text-xs text-fg flex flex-wrap items-center gap-2
```
(Only `border-border` → `border-danger/40` and `bg-panel` → `bg-danger/10` change. All other tokens stay verbatim.)

Then, immediately before the `<span className="inline-block w-1 h-4 bg-danger flex-shrink-0" aria-hidden="true" />` left-accent bar, insert:
```tsx
<span
  className="inline-flex items-center justify-center w-4 h-4 text-danger flex-shrink-0"
  aria-hidden="true"
>
  <svg viewBox="0 0 16 16" fill="currentColor" className="w-4 h-4" aria-hidden="true">
    <path d="M8 1.5 L14.5 13.5 H1.5 Z" />
  </svg>
</span>
```

The left-accent bar span (`inline-block w-1 h-4 bg-danger`) stays unchanged — it follows the warning icon.

**For UnusedAssetsPanel.tsx:**

Identical transformation:
1. Change `border-border bg-panel` → `border-danger/40 bg-danger/10` in the header div className
2. Insert the same warning icon span immediately before the left-accent bar span

The `role="alert"` and `aria-label` attributes on the outer div stay unchanged in both files. The header height is unchanged (py-2 stays).

Tailwind v4 literal-class discipline: `border-danger/40` and `bg-danger/10` are string literals. No template interpolation.
  </action>
  <verify>
    <automated>grep -c "border-danger/40 bg-danger/10" src/renderer/src/panels/MissingAttachmentsPanel.tsx src/renderer/src/panels/UnusedAssetsPanel.tsx && grep -c "viewBox=\"0 0 16 16\"" src/renderer/src/panels/MissingAttachmentsPanel.tsx src/renderer/src/panels/UnusedAssetsPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "border-danger/40 bg-danger/10" src/renderer/src/panels/MissingAttachmentsPanel.tsx` returns 1
    - `grep -c "border-danger/40 bg-danger/10" src/renderer/src/panels/UnusedAssetsPanel.tsx` returns 1
    - `grep -c "border-border bg-panel" src/renderer/src/panels/MissingAttachmentsPanel.tsx` returns 0 (old class combo gone from header)
    - `grep -c "border-border bg-panel" src/renderer/src/panels/UnusedAssetsPanel.tsx` returns 0 (old class combo gone from header)
    - `grep -c "viewBox=\"0 0 16 16\"" src/renderer/src/panels/MissingAttachmentsPanel.tsx` returns 1 (the warning icon SVG)
    - `grep -c "viewBox=\"0 0 16 16\"" src/renderer/src/panels/UnusedAssetsPanel.tsx` returns 1
    - Both files still contain `inline-block w-1 h-4 bg-danger flex-shrink-0` (the left-accent bar is preserved)
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>Both danger panel headers have bg-danger/10 background, border-danger/40 border, and a 16px SVG triangle warning icon; left-accent bars preserved; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Stronger AnimationBreakdownPanel header + GlobalMaxRenderPanel count cell min-width (C-09, C-06 partial)</name>
  <files>
    src/renderer/src/panels/AnimationBreakdownPanel.tsx
    src/renderer/src/panels/GlobalMaxRenderPanel.tsx
  </files>
  <read_first>
    - src/renderer/src/panels/AnimationBreakdownPanel.tsx (read lines 395–415 to see the exact current header element — h2 text, gap, className; also confirm `filteredCards` variable name is in scope at this location in the component)
    - src/renderer/src/panels/GlobalMaxRenderPanel.tsx (read lines 830–845 to see the exact current count chip span className at line ~837; confirm `selected.size` and `sorted.length` are used there)
  </read_first>
  <action>
**AnimationBreakdownPanel.tsx — replace section header (C-09):**

Find the header element:
```tsx
<header className="mb-4 flex items-center gap-4">
  <h2 className="text-lg font-semibold">Animation Breakdown</h2>
</header>
```

Replace the entire header element with:
```tsx
<header className="mb-4 flex items-center gap-2">
  <span
    className="inline-flex items-center justify-center w-5 h-5 text-fg flex-shrink-0"
    aria-hidden="true"
  >
    <svg
      viewBox="0 0 20 20"
      stroke="currentColor"
      strokeWidth="1.5"
      strokeLinecap="round"
      strokeLinejoin="round"
      fill="none"
      className="w-5 h-5"
      aria-hidden="true"
    >
      <path d="M3 17 V10 M8 17 V6 M13 17 V12 M18 17 V4" />
    </svg>
  </span>
  <h2 className="text-sm font-semibold text-fg">Animation Breakdown</h2>
  <span className="text-fg-muted font-mono text-xs font-normal ml-auto">
    {filteredCards.length} animations
  </span>
</header>
```

Changes from current:
- `gap-4` → `gap-2` on the header element
- Prepend an inline SVG bar-chart icon (vertical bars, `w-5 h-5`) — same inline SVG pattern as GlobalMaxRenderPanel lines 821–826
- `h2` class changes from `text-lg font-semibold` → `text-sm font-semibold text-fg` (reduces title from 18px to 14px for visual parity with GlobalMaxRenderPanel)
- Add right-aligned count chip: `<span className="text-fg-muted font-mono text-xs font-normal ml-auto">{filteredCards.length} animations</span>`

Before writing: confirm the name of the filtered cards array in this component. If it's not `filteredCards`, use whatever the correct variable name is. Read the file first.

Icon SVG path explanation: `M3 17 V10 M8 17 V6 M13 17 V12 M18 17 V4` draws 4 vertical bars of increasing height (bar chart / breakdown icon). This is the same pattern referenced in UI-SPEC.md C-09.

Tailwind v4 literal-class discipline: all className values are string literals.

---

**GlobalMaxRenderPanel.tsx — count cell min-width (C-06 partial):**

Find the count chip span (should be near line 837):
```tsx
<span className="text-fg-muted font-mono text-sm font-normal ml-auto">
  {selected.size} selected / {sorted.length} total
</span>
```

Add `min-w-[6rem] text-right` to the className:
```tsx
<span className="text-fg-muted font-mono text-sm font-normal ml-auto min-w-[6rem] text-right">
  {selected.size} selected / {sorted.length} total
</span>
```

Only `min-w-[6rem] text-right` is added at the end of the existing class string. No other change to this span or its surrounding elements. Confirm the ml-auto is still present (it was already there).
  </action>
  <verify>
    <automated>grep -c "animations" src/renderer/src/panels/AnimationBreakdownPanel.tsx | head -1 && grep -c "min-w-\[6rem\]" src/renderer/src/panels/GlobalMaxRenderPanel.tsx && npm run test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AnimationBreakdownPanel.tsx header contains `class="text-sm font-semibold text-fg"` on the h2 element (text-lg is gone)
    - `grep -c "filteredCards.length} animations" src/renderer/src/panels/AnimationBreakdownPanel.tsx` returns 1 (or the correct array variable name)
    - `grep -c "ml-auto" src/renderer/src/panels/AnimationBreakdownPanel.tsx` returns at least 1 (the count chip has ml-auto)
    - `grep -c "viewBox=\"0 0 20 20\"" src/renderer/src/panels/AnimationBreakdownPanel.tsx` returns 1 (the new header icon)
    - `grep -c "min-w-\[6rem\]" src/renderer/src/panels/GlobalMaxRenderPanel.tsx` returns 1 (the count cell)
    - `grep -c "text-right" src/renderer/src/panels/GlobalMaxRenderPanel.tsx` returns at least 1 (added alongside min-w)
    - `grep 'text-lg font-semibold' src/renderer/src/panels/AnimationBreakdownPanel.tsx` returns 0 (old h2 class gone)
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>AnimationBreakdownPanel header has bar-chart icon + text-sm title + animation count chip; GlobalMaxRenderPanel count cell has min-w-[6rem]; all tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "border-danger/40 bg-danger/10" src/renderer/src/panels/MissingAttachmentsPanel.tsx` → 1
2. `grep -c "border-danger/40 bg-danger/10" src/renderer/src/panels/UnusedAssetsPanel.tsx` → 1
3. `grep -c "animations" src/renderer/src/panels/AnimationBreakdownPanel.tsx` → ≥ 1 (count chip)
4. `grep -c "viewBox=\"0 0 20 20\"" src/renderer/src/panels/AnimationBreakdownPanel.tsx` → 1 (header icon)
5. `grep -c "min-w-\[6rem\]" src/renderer/src/panels/GlobalMaxRenderPanel.tsx` → 1
6. `npm run test` exits 0
</verification>

<success_criteria>
- MissingAttachmentsPanel and UnusedAssetsPanel: header div uses bg-danger/10 + border-danger/40 + SVG warning icon
- AnimationBreakdownPanel: header has inline bar-chart SVG icon + text-sm title + {n} animations count chip
- GlobalMaxRenderPanel count cell: min-w-[6rem] text-right added
- All 9 component contracts (C-01 through C-09) are complete across all four plans
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-ui-polish-visual-wins/26.1-04-SUMMARY.md`
</output>
