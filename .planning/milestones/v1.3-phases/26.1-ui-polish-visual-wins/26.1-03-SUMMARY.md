---
phase: 26.1-ui-polish-visual-wins
plan: "03"
subsystem: ui
tags: [react, tailwind, appshell, toolbar, chip, atlas-less]

# Dependency graph
requires:
  - phase: 26.1-01
    provides: color token foundation and full-width panel layout that this wave builds on
provides:
  - Load-summary chip shows image count in atlas-less mode (C-05)
  - All 5 toolbar buttons unified at h-8 (32px) height (C-06)
affects: [26.1-04, visual verification of toolbar]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Tailwind h-8 replaces py-1 on toolbar buttons — enforces consistent 32px tap-target height"
    - "loaderMode conditional in chip label — minimal ternary at the render site, no new state"

key-files:
  created: []
  modified:
    - src/renderer/src/components/AppShell.tsx

key-decisions:
  - "load-summary chip py-1 intentionally preserved — it is a status chip, not a button; h-8 is button-only"
  - "skippedAttachments?.length optional chaining used — field is non-required on SkeletonSummary"
  - "attachments.count - skippedAttachments.length gives image count in atlas-less mode (skipped = missing PNGs not in sampler)"

patterns-established:
  - "Toolbar button canonical class strings: border buttons use rounded-md px-3 h-8; accent button uses bg-accent rounded-md px-3 h-8"

requirements-completed: [UI-06, UI-10]

# Metrics
duration: 10min
completed: 2026-05-04
---

# Phase 26.1 Plan 03: Toolbar Height Harmonization + Atlas-less Chip Summary

**All 5 toolbar buttons unified to h-8 (32px) and atlas-less chip now shows image count via loaderMode conditional in AppShell.tsx**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-05-04T16:52:00Z
- **Completed:** 2026-05-04T16:54:00Z
- **Tasks:** 2 completed
- **Files modified:** 1

## Accomplishments

- Atlas-less load-summary chip: `loaderMode === 'atlas-less'` branch renders `{attachments.count - (skippedAttachments?.length ?? 0)} images`; fallback branch preserves `1 atlases` verbatim
- 4 border-style toolbar buttons (Atlas Preview, Documentation, Save, Open) updated from `py-1` to `h-8`
- 1 filled accent button (Optimize Assets) updated from `py-1` to `h-8`
- Load-summary chip `py-1` intentionally untouched — chip sizing is correct as-is

## Task Commits

Each task was committed atomically:

1. **Task 1: Atlas counter chip — atlas-less conditional (C-05)** - `fc62b9a` (feat)
2. **Task 2: Toolbar button height harmonization — all 5 buttons to h-8 (C-06)** - `9b1f535` (feat)

**Plan metadata:** (see final-commit hash below)

## Files Created/Modified

- `src/renderer/src/components/AppShell.tsx` — chip conditional + h-8 on 5 buttons

## Decisions Made

- `loaderMode === 'atlas-less'` at the chip render site: loaderMode is a `useState` at line 274, in scope at the chip location (line 1309). No new state or prop needed.
- `attachments.count - (skippedAttachments?.length ?? 0)` for image count: `skippedAttachments` is populated from `LoadResult.skippedAttachments?` (optional field, Phase 21). The `?.length ?? 0` handles the `undefined` case safely.
- Open button's class string (no `disabled:` tokens) handled as a separate edit from the 3 disabled-capable border buttons — both become `h-8`.

## Deviations from Plan

None — plan executed exactly as written. The note in the plan's interfaces section about count cell min-width being moved to Plan 04 was honored: no changes were made to GlobalMaxRenderPanel.tsx.

## Issues Encountered

3 pre-existing test failures exist unrelated to this plan:
- `tests/integration/build-scripts.spec.ts` — package.json version check (version mismatch for v1.1.3)
- `tests/main/sampler-worker-girl.spec.ts` — wall-time gate (flaky on CI/worktree)
- `tests/renderer/atlas-preview-modal.spec.tsx` — dblclick canvas hit test

These failures were verified to pre-exist before any changes in this plan (confirmed via `git stash` baseline run). No regressions introduced.

## Known Stubs

None — no placeholder or hardcoded empty values introduced.

## Threat Flags

None — changes are purely presentational class string and conditional text rendering in the renderer. No network endpoints, auth paths, or schema changes.

## Self-Check

- [x] `fc62b9a` exists: `git log --oneline | grep fc62b9a` → found
- [x] `9b1f535` exists: `git log --oneline | grep 9b1f535` → found
- [x] `src/renderer/src/components/AppShell.tsx` modified: confirmed
- [x] `grep -c "loaderMode === 'atlas-less'" AppShell.tsx` → 2 (chip + checkbox)
- [x] `grep -c "rounded-md px-3 h-8" AppShell.tsx` → 5
- [x] No `rounded-md px-3 py-1` in any button className (only in chip font-mono line)

## Self-Check: PASSED

## Next Phase Readiness

- Plan 04 (GlobalMaxRenderPanel polish: count cell min-w, C-09) can proceed — no file overlap with this plan
- AppShell.tsx changes are self-contained; no cascading type changes required

---
*Phase: 26.1-ui-polish-visual-wins*
*Completed: 2026-05-04*
