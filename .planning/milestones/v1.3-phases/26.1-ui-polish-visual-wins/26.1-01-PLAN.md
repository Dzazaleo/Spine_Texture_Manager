---
phase: 26.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/index.css
  - src/renderer/src/panels/GlobalMaxRenderPanel.tsx
  - src/renderer/src/panels/AnimationBreakdownPanel.tsx
  - src/renderer/src/components/SearchBar.tsx
autonomous: true
requirements: [UI-06, UI-07, UI-10]

must_haves:
  truths:
    - "App background is cool blue-dark (#232732) instead of warm stone"
    - "Panel cards are visibly lighter than the surface (#2c3347)"
    - "Both panels stretch edge-to-edge with no max-width cap"
    - "SearchBar input height matches the toolbar button target (32px, h-8)"
  artifacts:
    - path: "src/renderer/src/index.css"
      provides: "Color token definitions"
      contains: "--color-surface: #232732"
    - path: "src/renderer/src/panels/GlobalMaxRenderPanel.tsx"
      provides: "Full-width panel root div"
      contains: "w-full p-8"
    - path: "src/renderer/src/panels/AnimationBreakdownPanel.tsx"
      provides: "Full-width panel root div"
      contains: "w-full p-8"
    - path: "src/renderer/src/components/SearchBar.tsx"
      provides: "Harmonized SearchBar height"
      contains: "h-8"
  key_links:
    - from: "src/renderer/src/index.css"
      to: "all JSX files"
      via: "CSS custom property token cascade"
      pattern: "--color-surface: #232732"
---

<objective>
Update the color token foundation and remove panel width constraints. This plan delivers the visual reset that all subsequent plans build on — the cool blue-dark color scheme cascades automatically to every surface via token inheritance, and both data panels stretch to the full window width.

Purpose: Token changes in index.css propagate to every modal, panel, and toolbar component without per-file JSX changes (D-04 confirmed). Full-width panels (D-05) eliminate the cramped feel on wide displays.
Output: Updated index.css (3 token lines changed), both panel root divs stripped of max-w-6xl mx-auto, SearchBar input at h-8.
</objective>

<execution_context>
@/Users/leo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-ui-polish-visual-wins/26.1-CONTEXT.md
@.planning/phases/26.1-ui-polish-visual-wins/26.1-UI-SPEC.md
@.planning/phases/26.1-ui-polish-visual-wins/26.1-PATTERNS.md

<interfaces>
<!-- Exact current state of each change site, extracted from PATTERNS.md -->

From src/renderer/src/index.css (lines 44–51 of the @theme inline block):
```css
@theme inline {
  /* Neutrals — warm stone base, two layers (D-12, D-13) */
  --color-surface:   var(--color-stone-950);
  --color-panel:     var(--color-stone-900);
  --color-border:    var(--color-stone-800);
  --color-fg:        var(--color-stone-100);
  --color-fg-muted:  var(--color-stone-400);
  ...
}
```

From GlobalMaxRenderPanel.tsx line 801 (root div):
```tsx
<div className="w-full max-w-6xl mx-auto p-8">
```

From AnimationBreakdownPanel.tsx line 399 (root div):
```tsx
<div className="w-full max-w-6xl mx-auto p-8">
```

From SearchBar.tsx line 55 (input):
```
w-full bg-panel border border-border rounded-md px-3 py-1.5 text-sm font-mono focus:outline-none focus:ring-2 focus:ring-accent placeholder:text-fg-muted
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace color tokens in index.css (C-01)</name>
  <files>src/renderer/src/index.css</files>
  <read_first>
    - src/renderer/src/index.css (read the full @theme inline block to see current token values and surrounding lines; do NOT guess the line numbers — read the file first)
  </read_first>
  <action>
In the `@theme inline` block inside `src/renderer/src/index.css`, replace the three warm-stone neutral token lines with literal hex values. The ONLY lines that change are the three listed below. All other token lines (fg, fg-muted, accent, accent-muted, danger, success, warning, font tokens) stay verbatim.

Replace:
```css
--color-surface:   var(--color-stone-950);
--color-panel:     var(--color-stone-900);
--color-border:    var(--color-stone-800);
```

With (exact hex values, locked by D-01/D-02 — do not alter):
```css
--color-surface:   #232732;
--color-panel:     #2c3347;
--color-border:    #384158;
```

Do NOT change any other lines in the file. No JSX changes are needed — modal dialogs and all other surfaces inherit the new values automatically through CSS custom property cascade (D-04).

Tailwind discipline: these are CSS token definitions in index.css, not className strings. The rule against template interpolation applies to JSX only; CSS custom property definitions use literal hex directly.
  </action>
  <verify>
    <automated>grep -c '#232732' src/renderer/src/index.css && grep -c '#2c3347' src/renderer/src/index.css && grep -c '#384158' src/renderer/src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/src/index.css` contains the line `--color-surface:   #232732;` (or with different whitespace spacing)
    - `src/renderer/src/index.css` contains `--color-panel:     #2c3347;`
    - `src/renderer/src/index.css` contains `--color-border:    #384158;`
    - `src/renderer/src/index.css` does NOT contain `var(--color-stone-950)`, `var(--color-stone-900)`, or `var(--color-stone-800)` (the old token references are gone)
    - `grep -c 'var(--color-stone-95' src/renderer/src/index.css` returns 0
  </acceptance_criteria>
  <done>Three color token lines updated; all other lines in index.css unchanged; old var(--color-stone-*) references for surface/panel/border removed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove max-width constraint from both panel root divs + harmonize SearchBar height (C-02, C-06 partial)</name>
  <files>
    src/renderer/src/panels/GlobalMaxRenderPanel.tsx
    src/renderer/src/panels/AnimationBreakdownPanel.tsx
    src/renderer/src/components/SearchBar.tsx
  </files>
  <read_first>
    - src/renderer/src/panels/GlobalMaxRenderPanel.tsx (read around line 801 — find the root div className; read the surrounding 10 lines to confirm context before editing)
    - src/renderer/src/panels/AnimationBreakdownPanel.tsx (read around line 399 — same root div pattern)
    - src/renderer/src/components/SearchBar.tsx (read line 55 for the input class string; confirm py-1.5 is present before replacing)
  </read_first>
  <action>
**GlobalMaxRenderPanel.tsx — root div (line ~801):**

Find the root div with class `"w-full max-w-6xl mx-auto p-8"`. Remove only `max-w-6xl mx-auto` from the className. The result must be exactly:
```tsx
<div className="w-full p-8">
```
Do NOT remove `p-8`, do NOT add any new class, do NOT change any other attribute on this div.

**AnimationBreakdownPanel.tsx — root div (line ~399):**

Same change: find `className="w-full max-w-6xl mx-auto p-8"` and change to:
```tsx
<div className="w-full p-8">
```
Identical transformation — remove `max-w-6xl mx-auto`, retain `p-8`.

**SearchBar.tsx — input height (line ~55):**

Find the input element. Its current class string contains `py-1.5`. Replace `py-1.5` with `h-8` in the class string. The result must be:
```
w-full bg-panel border border-border rounded-md px-3 h-8 text-sm font-mono focus:outline-none focus:ring-2 focus:ring-accent placeholder:text-fg-muted
```
Only `py-1.5` → `h-8` changes. All other tokens in the string stay verbatim.

Tailwind v4 literal-class discipline: every string written must be a string literal. These are static className strings so no special handling required.
  </action>
  <verify>
    <automated>grep -c 'max-w-6xl' src/renderer/src/panels/GlobalMaxRenderPanel.tsx src/renderer/src/panels/AnimationBreakdownPanel.tsx && grep -c '"w-full p-8"' src/renderer/src/panels/GlobalMaxRenderPanel.tsx src/renderer/src/panels/AnimationBreakdownPanel.tsx && grep 'h-8' src/renderer/src/components/SearchBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'max-w-6xl' src/renderer/src/panels/GlobalMaxRenderPanel.tsx` returns 0
    - `grep -c 'max-w-6xl' src/renderer/src/panels/AnimationBreakdownPanel.tsx` returns 0
    - `grep -c '"w-full p-8"' src/renderer/src/panels/GlobalMaxRenderPanel.tsx` returns 1 (the root div)
    - `grep -c '"w-full p-8"' src/renderer/src/panels/AnimationBreakdownPanel.tsx` returns 1 (the root div)
    - `grep 'h-8' src/renderer/src/components/SearchBar.tsx` returns a line containing the input element
    - `grep 'py-1.5' src/renderer/src/components/SearchBar.tsx` returns 0 (old padding token gone)
    - `npm run test` passes (no regressions in existing vitest suite)
  </acceptance_criteria>
  <done>Both panel root divs no longer have max-width constraint; SearchBar input uses h-8 instead of py-1.5; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token file | index.css is a build-time file; changes apply at compile time, not runtime |
| JSX className strings | Static string literals; no user input flows into className values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.1-01 | Spoofing | n/a | accept | Pure CSS/token pass — no authentication surfaces touched |
| T-26.1-02 | Tampering | index.css token values | accept | Build-time static asset; no runtime mutation path; dev build requires local filesystem access |
| T-26.1-03 | Information Disclosure | n/a | accept | No user data displayed or modified in this phase; color tokens convey no PII |
| T-26.1-04 | Elevation of Privilege | n/a | accept | No permission model changes; Electron IPC channels unchanged |
| T-26.1-05 | XSS/Injection | className strings | accept | All className values are static string literals in source; no user input is interpolated into class strings (Tailwind v4 literal-class discipline enforced); CSS custom property names are locked constants |

**Summary:** This phase is a pure CSS-token + className string polish pass. No new data flows, no new IPC channels, no user input surfaces added. The STRIDE threat surface is unchanged from the previous phase. XSS/injection via CSS token changes cannot occur — CSS custom properties in index.css are build-time constants, not runtime user inputs.
</threat_model>

<verification>
After both tasks complete:
1. `grep -c '#232732' src/renderer/src/index.css` → 1
2. `grep -c 'max-w-6xl' src/renderer/src/panels/GlobalMaxRenderPanel.tsx` → 0
3. `grep -c 'max-w-6xl' src/renderer/src/panels/AnimationBreakdownPanel.tsx` → 0
4. `grep 'h-8' src/renderer/src/components/SearchBar.tsx` shows the input element
5. `npm run test` exits 0
</verification>

<success_criteria>
- index.css: surface=#232732, panel=#2c3347, border=#384158; no var(--color-stone-95x) references for these tokens
- GlobalMaxRenderPanel.tsx root div: className="w-full p-8" (no max-w-6xl)
- AnimationBreakdownPanel.tsx root div: className="w-full p-8" (no max-w-6xl)
- SearchBar.tsx input: py-1.5 replaced with h-8
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-ui-polish-visual-wins/26.1-01-SUMMARY.md`
</output>
