# Phase 26.2: UI polish — tab restructure + icon audit - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-04
**Phase:** 26.2-ui-polish-tab-restructure-icon-audit
**Areas discussed:** Existing-context handling, sub-toolbar anti-pattern enforcement, warning-triangle SVG strategy, broader UI concerns, scope-redirect (3-tab → keep alert bars), tab-icon scope, rename clarification, plan structure
**Notable:** This is the THIRD attempt at Phase 26.2 (prior 2 attempts reverted via c13ca9c + earlier). The user reversed the 3-tab decision mid-discussion after viewing the running app via screenshot.

---

## Existing-context handling

| Option | Description | Selected |
|--------|-------------|----------|
| Update it | Re-discuss and refresh CONTEXT.md given the 2 reverts and need for explicit sketch-001 citation | ✓ |
| View it | Show current CONTEXT contents and decide after | |
| Skip | Keep existing CONTEXT.md as-is and proceed to /gsd-plan-phase 26.2 | |

**User's choice:** Update it.

---

## Original deltas to lock (initial framing — superseded mid-discussion)

| Option | Description | Selected |
|--------|-------------|----------|
| Sub-toolbar placement anti-pattern | Add explicit anti-pattern lock citing sketch-001 + the 2 prior reverts | ✓ |
| Tab icons = verbatim panel-header SVG reuse | Tighten D-06 to 'reuse exact SVG path data' rather than 'matches style' | ✓ |
| Add sketch-001 to canonical_refs | Mechanical — add sketch-001 README + index.html | ✓ |
| Capture broader 'UI doesn't look good' concerns as deferred seeds | Open-ended capture for a future polish phase | ✓ |

**User's choice:** All four selected.
**Notes:** This selection set assumed the 3-tab system from prior CONTEXT was still valid. It was later partially superseded by the user's redesign (3-tab dropped). The sub-toolbar anti-pattern remains in force — it now applies to the existing 2-tab strip's relocation rather than to a 3-tab system.

---

## Sub-toolbar anti-pattern enforcement level

| Option | Description | Selected |
|--------|-------------|----------|
| Both: blocking anti-pattern + CONTEXT D-08 | .continue-here.md severity:blocking + CONTEXT decision. Maximum protection given 2 prior reverts. | ✓ |
| CONTEXT D-08 only | Just a HARD-LOCK CONTEXT decision; no .continue-here.md | |
| Mention in canonical_refs and let sketch-001 do the talking | Lightest touch — just point planner at the sketch | |

**User's choice:** Both.
**Notes:** Wrote `.continue-here.md` with AP-01 (severity: blocking) + acknowledgement template. Planner/executor must acknowledge AP-01 in their own words before proceeding. This is the third attempt; maximum protection is warranted.

---

## Warning-triangle SVG strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Define canonical path in CONTEXT.md | Pin the exact stroke-triangle path in CONTEXT; all 4 conversion sites copy verbatim | ✓ |
| Constraint: 'all 5 must share a single path' — planner picks | Trust planner to converge on one shape during research | |
| Extract to a shared component | Add WarningTriangleIcon component, reuse via import | |

**User's choice:** Define canonical path.
**Notes:** Locked in CONTEXT as D-06. Planner may still extract to a shared component if they prefer (D-07 — Claude's discretion); the canonical path is the invariant, the inline-vs-component form is open.

---

## Broader 'UI doesn't look good' concerns

| Option | Description | Selected |
|--------|-------------|----------|
| Color palette / dark-theme balance | | |
| Density / spacing / breathing room | | |
| Visual hierarchy / typography | | |
| Iconography overall (beyond 26.2 stroke audit) | | |
| Other (free text) | The Global render scale header has round corners and looks nice, but the Unused and Animation Breakdown fail to follow the same layout. The panel design must be consistent. | ✓ |

**User's choice:** Other (free text — specific layout-consistency concern).
**Notes:** Mapped to two specific code locations: (1) AnimationBreakdownPanel.tsx:399 lacks the `<section>` wrapper that GlobalMaxRenderPanel.tsx:833 has — captured as deferred seed (out of 26.2 scope; carry-forward from Phase 26.1). (2) UnusedAssetsPanel.tsx:53 is a danger-alert-bar shape, not a section card — kept as-is per the user's later redesign decision (alert-bar layout is the locked target).

---

## SCOPE REDIRECT — 3-tab → keep alert bars

| Option | Description | Selected |
|--------|-------------|----------|
| Same shape as Global, danger-tinted | Wrap UnusedAssetsPanel in section card with border-danger/40 when moved to tab pane | |
| Same shape as Global, plain border | Drop danger theming on outer container | |
| No — keep current alert-bar look, defer card refactor | Leave UnusedAssetsPanel structure as-is for 26.2 | |
| Other (chat mode + screenshot) | "I like how the missing and orphaned files are being shown in the screenshot. Let's keep it that way. We don't need to create a Unused tab. Change the wording from Orphaned to Unused. Remove the dedicated search filter." | ✓ |

**User's choice:** Other — full reversal of the 3-tab decision after viewing the running app.
**Notes:** This is the major redesign moment. All 3-tab decisions (D-01 unused tab hidden, D-02 auto-redirect, D-03 query lift, D-04 pill badge) become OBSOLETE. New scope is: (a) move existing 2 tabs to sub-toolbar, (b) add icons to the 2 existing tabs, (c) rename orphaned→unused on user-visible strings, (d) remove inline search input, (e) icon audit (fill→stroke at 4 sites).

---

## Tab-icon scope under new design

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — add filmstrip + bar-chart icons to existing tabs | Verbatim reuse from panel section headers, w-4 h-4 | ✓ (with addendum) |
| No — leave existing tabs text-only | Drop tab icons; icon audit reduces to fill→stroke at 4 sites only | |

**User's choice:** Yes — and they still get moved to the second row in the toolbar (sub-toolbar).
**Notes:** Confirmed sketch-001 variant A is back in force for the 2 existing tabs. AP-01 anti-pattern still applies.

---

## Rename clarification

| Option | Description | Selected |
|--------|-------------|----------|
| Just the alert headline + aria-label | Rename UnusedAssetsPanel.tsx:71 + :52; no internal renames | ✓ |
| Also rename internal types/vars | OrphanedFile → UnusedFile + orphanedFiles → unusedFiles + comments + types/IPC | |
| I meant something else | (free text) | |

**User's choice:** Just the alert headline + aria-label.
**Notes:** User had originally selected "Inner table column 'Orphaned File' → 'Unused File'" but no such column exists (columns are Filename + Size on Disk). Cleared up in this clarifying question. Internal type/variable renames captured as deferred seed.

---

## Plan structure

| Option | Description | Selected |
|--------|-------------|----------|
| Single plan | One PLAN.md covering all 5 items in one wave | ✓ |
| Two plans | 26.2-01 = sub-toolbar + tab icons + rename + remove search; 26.2-02 = fill→stroke at 4 sites | |

**User's choice:** Single plan.
**Notes:** Scope is small enough to fit in one wave with atomic per-item commits.

---

## Claude's Discretion

- **D-06a:** Exclamation-dot rendering inside the warning triangle (`<circle r="0.5">` vs zero-length stroke path) — planner picks whichever renders cleanly at 16px in dark-mode.
- **D-07:** Inline `<svg>` at 4 sites vs extract `WarningTriangleIcon` component — planner's call. Single-source-of-truth invariant is fixed.

## Deferred Ideas

- AnimationBreakdownPanel section-card wrapper consistency vs GlobalMaxRenderPanel (Phase 26.1 carry-forward; user-flagged via screenshot).
- UnusedAssetsPanel container shape unification (would conflict with locked alert-bar layout — captured for future consideration).
- Toolbar action button icons (Atlas Preview / Documentation / Optimize Assets / Save / Open) — future iconography pass.
- DocumentationBuilderDialog tab strip icons — future iconography pass.
- Internal type/variable rename `OrphanedFile`/`orphanedFiles` → `UnusedFile`/`unusedFiles` — future code-quality phase.
