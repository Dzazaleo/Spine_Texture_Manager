# Spine Texture Manager

## What This Is

A desktop app (Electron + React + TypeScript) that reads Spine 4.2+ skeleton JSON and computes the **peak world-space render scale for every individual attachment** across every animation and skin. The animator exports a per-attachment-optimized `images/` folder via sharp Lanczos3 — and, as of v1.5, can additionally repack the optimized regions into a libgdx-format `.atlas` + composite page PNG(s), shipping packed atlases directly without round-tripping through the Spine editor. v1.2 added atlas-less mode (json + images folder, no .atlas), dims-badge round-trip safety, per-skeleton Documentation Builder with HTML export, a full UI redesign, and two macOS UX regressions closed. v1.3 closed correctness/semantic gaps, refined the Optimize workflow UX, polished the UI, and added optional output sharpening on downscale. v1.3.1 closed the per-region dedup correctness gap, added a user-configurable safety-buffer %, and refined small UX surfaces. v1.4 added Spine 4.3-beta detect-and-warn, rotated atlas region support, File→Open menu acceptance for `.json` skeletons, and a region-keyed export plan that fixes a multi-skin atlas-source undercount. v1.5 split overrides per loaderMode (atlas-source / atlas-less buckets), source-audited and fixture-locked the last two unverified Spine 4.2 timeline coverage gaps (RGBA2 + InheritTimeline), retired three long-lived tech-debt todos, and shipped the additive `loose | atlas | both` atlas-repack output mode. v1.5.1 added an in-app Spine Animation Viewer (Esoteric's official `spine-player`) for exported-atlas validation. v1.6 ported the skeleton/animation math to a **dual-runtime** architecture that loads and correctly samples both Spine 4.2 and Spine 4.3 skeleton JSON, routed by detected version (shipped as v1.6.1). v1.7 added the **Multi-Scale Per-Resolution Variant Exporter**: from one full-size export, the app now produces faithful genuinely-smaller rig variants (scaled skeleton JSON + resized textures + scaled atlas, each in its own folder), single or batch, sized to each smaller rig's own peak render demand — the first feature to ever make the app *write* a skeleton JSON.

## Core Value

Animators ship atlases that are as small as they mathematically can be without visible quality loss — driven by the actual world-space transforms the runtime computes, not guesswork.

## Current State (Phase 55 COMPLETE 2026-05-26 — variant effScale clamp lifted to 1/vs; v1.7 base CLOSED 2026-05-25)

**v1.7 FEATURE-COMPLETE, NOT shipped — milestone deliberately kept OPEN then finished (2026-05-24):** the original 4 phases (48–51) plus two added phases (52, 53) are ALL complete + verified. **Phase 52 — Batch Export Robustness + Variant-Dialog Cleanup COMPLETE + verified 2026-05-24** (EXPORT-06; 4/4 plans across 2 waves, 10/10 must-haves; code review 0 blocker / 2 warning / 3 info; full suite 153 files / 1546 passed / 0 failed, both typechecks exit 0). **Phase 53 — Persist Variant State in `.stmproj` COMPLETE + verified 2026-05-24** (SCALEUI-03; 2/2 plans across 2 waves, verified 10/10 must-haves / all 3 SC; code review 0 blocker / 3 warning / 4 info — all warnings align with locked-plan decisions; full suite 1553 passed / 0 failed, both typechecks exit 0). Phase 53 added an additive-optional `variantRows: { scale: number }[]` field persisted end-to-end (shared types → pure-core validator/serialize/materialize → main Open assembly → AppShell restore-on-both-load-paths + dirty-by-scale-projection), with NO `.stmproj` version bump (`V_LATEST` stays 1) and NO new fs check on `lastOutDir` (the saved output location reuses the already-persisted `lastOutDir` as a picker hint — D-01/D-02/SC#3). The Export Variant dialog's scale rows + chosen output location now round-trip across sessions on save. **v1.7 milestone CLOSED 2026-05-25:** all 6 phases security-audited (`48-SECURITY.md`..`53-SECURITY.md`), the Phase-49 native folder-picker UAT live-confirmed on both 4.2 and 4.3 legs, requirements + roadmap archived to `milestones/v1.7-*`, `REQUIREMENTS.md` reset for the next milestone, and tag `v1.7` created locally (annotated). **Nothing pushed/published — that is a separate owner ship decision** (per `feedback_dont_push_release_tags`). Next: `/gsd-new-milestone` when ready to scope v1.8.

**v1.7 progress (Multi-Scale Per-Resolution Variant Exporter) — Phases 48–51 COMPLETE (4/6); 52–53 PLANNED:** Phase 48 (core scale-bake module + regression oracle), Phase 49 (single-scale variant export — first feature to WRITE a skeleton JSON), Phase 50 (rig-bounds + two-way scale↔dimension input), and **Phase 51 COMPLETE 2026-05-23** (batch variant export, EXPORT-04, the milestone finale; 2/2 plans, verified 2/2 must-haves + user live-UAT approved). Phase 51 added a main-side `handleExportVariantBatch` engine (behavior-preserving extraction of the single-scale body into `exportOneVariant`, then loop it once per scale — SC#2 byte-identity to the single-scale path holds BY CONSTRUCTION; continue-on-error per-variant rollback D-07, between-variants cancel D-09, `variant:exportBatch`/`variant:cancelBatch` IPC + `BatchVariantResult`) and a renderer multi-row `VariantDialog` (rows[] list with per-row two-way control, +Add scale / per-row remove, duplicate-token + invalid Export gates D-10/D-11, per-folder result list + aggregate D-08, "variant N of M" progress + Cancel, one `exportVariantBatch` call after one parent pick D-12, one override bucket D-13; single-pane, NO tabs D-06). Faithfulness matrix 12/12 (4.2/4.3 × atlas-source/atlas-less); full suite 1530 passed / 0 failed; both typechecks clean; Layer-3 intact (`tokenFor` renderer-local). Live UAT approved by user (batch fan-out + one picker; continue-on-error ✗-row + Cancel). Code review 0 blocker / 5 warning / 4 info (`51-REVIEW.md`). **Post-verification UX polish — three user-requested rounds, all live-confirmed (commits `1c68cb8` → `167d6e0` → `fc92a54`; final full suite 152 files / 1536 passed / 0 failed):** (1) `1c68cb8` — VariantDialog footer overflow fixed (modal body is a `flex-1 overflow-y-auto min-h-0` scroll region + pinned `shrink-0` footer); first-cut progress bar; WR-05 (Cancel latches "Cancelling after current…" + disables on 1-scale/last/pre-first-event); WR-01 (reject a valid 0<s<1 scale whose 4dp token collapses to a degenerate `@0x`/`@1x` folder — main guard + renderer mirror + regression test) + IN-02 (factor `max` 0.99→0.9999). (2) `167d6e0` — the progress bar reworked to track PER-IMAGE progress within the current variant (0→100, resets each variant; driven by the per-file `export:progress` stream), and scale rows + summary rows now color LIVE green/amber/red as each variant finishes (new `variant:result` main→renderer event + `onVariantResult` subscription; `--color-success/warning/danger` tokens). (3) `fc92a54` — **variant-export overwrite now offered via a pre-flight ConflictDialog** (new `probeVariantBatchConflicts` over the `{NAME}@{s}x/` fan-out → "N folders will be overwritten / Overwrite all / Cancel"; Overwrite all runs with overwrite=true). **This SUPERSEDES the 51 D-12 "pre-existing folders fail per-folder" stance** — the main side always supported overwrite; the renderer just never offered it. The Optimize Assets conflict flow is unchanged (ConflictDialog gained an optional `onPickDifferent` + `noun` prop). **Remaining work scheduled as v1.7 Phases 52–53 (NOT v1.8):** the `51-REVIEW.md` items (WR-02/03/04, IN-01/03/04) → Phase 52 (EXPORT-06); persist the variant scale rows + output location in `.stmproj` → Phase 53 (SCALEUI-03; `.planning/todos/pending/2026-05-23-persist-variant-rows-and-output-location.md`). ⚠ workflow.security_enforcement=true but no 48/49/50/51-SECURITY.md (the v1.7 export phases DO add I/O surface — variant skeleton-JSON writes + per-folder fan-out + the `variant:exportBatch` IPC boundary; run /gsd-secure-phase 49 and /gsd-secure-phase 51 for the paper trail before milestone close).

**v1.6 in progress:** Spine 4.3 Runtime Port (Dual-Runtime) — see `## Current Milestone` below. SEED-006 trigger fired 2026-05-16 (`npm view @esotericsoftware/spine-core@latest` → `4.3.0`; `@esotericsoftware/spine-player@latest` → `4.3.0`). **Phase 42 COMPLETE 2026-05-16** (pre-v1.6 4.2 baseline + npm alias + boundary scaffolding; 5/5 plans, re-verified passed 5/5 — SAFE-01/RT-01/RT-03/RT-04/CI-01); dual-install live (`@esotericsoftware/spine-core@4.3.0` canonical + `spine-core-42`=`4.2.111` exact-pinned alias). **Phase 43 COMPLETE 2026-05-17** (runtime-adapter facade + verified 4.3 API mapping; 6/6 plans incl. gap-closure 43-06; verified passed 5/5 ROADMAP criteria — RT-02/SAFE-02/SAFE-03/PORT-01/PORT-02/PORT-03). `SpineRuntime` facade live: `sampler.ts`/`bounds.ts` call through `load.runtime.*` (no direct spine-core import, arch.spec.ts-anchored); `runtime-42`/`runtime-43` adapters exist; SAFE-02 32/32 byte-equal vs the frozen Phase-42 baseline (D-04 heavy-rig 32/32 close-gate passed in 43-05); GAP-43-PROD-SEAM CLOSED (prod worker resolves `out/main/runtime-4x.cjs`; build-required RED→GREEN falsifier). **Maintainer-adjudicated bounded exception (2026-05-17):** ARCHITECTURE §4 "lazy single-copy" is scoped to the spine-core runtime/animation graph (cleanly split, 4.2 path strictly single-copy); the pre-existing 43-04 parse-time `AtlasAttachmentLoader` edge (`runtime-43 → synthetic-atlas → spine-core-42`, 1× bare require in `runtime-43.cjs`) is an accepted, documented bounded exception — decouple deferred as a tracked non-blocking follow-up. Renderer still imports the 4.2-only `MixBlend` (11 `tests/renderer/*` suites red; spine-player `Player.js` build abort after main emits) — an intended, ROADMAP-Phase-47-owned state, not a regression. **Phase 44 COMPLETE 2026-05-18** (loader dispatch + equivalence oracle + 4.3 fixture authoring; 4 plans + 1 orchestrator-directed core-fix, verified passed 5/5 ROADMAP criteria — DISP-01/02/03, ORCL-01/02/03, XTRA-01/02). The loader is now a version dispatcher (`resolveRuntimeTag` is the sole gate at `loader.ts`; 4.2→4.2 runtime, 4.3→4.3 runtime; `<4.2` + new `≥4.4` typed-reject arms preserved); owner-exported 4.3 rigs (SLIDER_4_3, XTRA01_4_3, XTRA02_4_3) + the ORCL-02 4.2-sibling committed in-repo. **The ORCL-02 cross-runtime equivalence oracle did its job on first run — it caught a real, ship-blocking Phase-43-escaped defect: `runtime-43.ts attachmentUVs` returned region-space `MeshAttachment.regionUVs` instead of page-space, undersizing every 4.3 weighted/region mesh by exactly 2.251× (1.5²). Fixed under D-14 (commit `2d0246c`: route mesh UVs through `MeshAttachment.computeUVs`); the stale Phase-43-era 4.3 own-baselines were deliberately re-captured against corrected math (only CIRCLE mesh records moved, within 1e-4 of the byte-trusted runtime-42 leg); SAFE-02 4.2 byte-gate stayed 24/0.** ORCL-03 = v1.6 NO-OP (IK absent in both rig JSONs → spine-editor#891-immune; no human gate). 44-REVIEW.md advisory: 0 blocker / 5 warning / 6 info (test-hardening surfaces; core changes independently traced correct — not blocking). ⚠ workflow.security_enforcement=true but no 44-SECURITY.md (run /gsd-secure-phase 44 before milestone close). **Phase 45 COMPLETE 2026-05-18** (dispatcher user-facing flip + copy/docs sweep; 2 plans, 1 wave, parallel-safe, + 1 orchestrator-directed D-12 typecheck fix; verified passed 3/3 ROADMAP criteria — UX-01, UX-02). Pure user-facing copy/docs sweep with ZERO runtime/loader/dispatch/errors behavior change (`src/core/*` + `src/main/doc-export.ts` byte-identical to phase base `c5235837`): App.tsx drop-zone flipped to "Drop a Spine v4.2 or v4.3 JSON file…" (D-01/02/03, both tokens font-bold text-danger per locked D-02); HelpDialog:128 + README:3/15 swept to the affirmative "Spine 4.2 and 4.3" band + README:29 to the exact `<4.2`/`≥4.4` typed-reject band (D-04/05); net-new repo-root CHANGELOG.md announcing "Spine 4.3 skeleton support (dual-runtime)" (D-06); D-09 doc-export confirm-and-no-op dispositioned with grep evidence (version-agnostic by design, not edited); the renderer/docs UX-02 grep is clean under the D-08 allowlist. UX-02 test half: 10-file Phase-44-D-11 disposition audit (verify-only — zero surviving 4.3-reject false-greens, all `<4.2`/`≥4.4` throws preserved verbatim) + a permanent in-suite D-12 anti-false-green standing guard (`tests/runtime/d12-43-fixtures-route-standing-guard.spec.ts`; 4 in-repo 4.3 fixtures route via the load-bearing handleRuntime dispatch-target triplet, 4/4 green). 45-REVIEW.md advisory: 0 blocker / 2 warning / 2 info (README:5 stale `v1.3.6` release pointer [explicitly D-07-deferred — external GitHub surface, out of sweep scope], D-12 triple-invoke; quality nits, non-blocking). Pre-existing out-of-scope finding surfaced: `npm run typecheck:node` is red from ~100+ v1.0-era tracked `scripts/probe-*`/`diagnose-*` Spine-API-drift errors + a Phase-44 test-family `SkeletonData`/`OpaqueHandle` brand gap (predates Phase 45, contradicts the STATE.md/ci.yml "clean on fresh clone" note — a separate non-Phase-45 concern); Phase 45's only net-new typecheck error was fixed at user direction via the codebase's single sanctioned boundary cast (`as unknown as OpaqueSkeletonData`, commit `7c66505`) so the new permanent guard is typecheck-clean. Full vitest 1277 passed / 0 actual failures (11 pre-existing Phase-47-owned MixBlend import suites). ⚠ workflow.security_enforcement=true but no 45-SECURITY.md (run /gsd-secure-phase 45 before milestone close). **Phase 46 COMPLETE 2026-05-18** (slider constraint validation + 4.3 performance budget; 2 plans, 1 wave, sequential main-tree because 46-01 carried a `checkpoint:human-action`; verified PASSED 3/3 ROADMAP criteria — SLIDER-01, SLIDER-02, PERF-01). **The 4.3-only `SliderConstraint` is proven to propagate correctly through the UNCHANGED `updateWorldTransform(Physics.update)` path with ZERO slider-specific `src/core/` code — the proven absence of slider code IS the deliverable (machine-asserted: 0 `src/core/`+`src/main/` diff over the phase).** SLIDER-02 is a genuine closed-form oracle, not "it runs": `slider43-closedform.spec.ts` asserts the sampled `square` peakScale == the hand-derived analytical `4.0` (`1 + 3·clamp(p.time,0,1)` at `p.time = 0.005·200 = 1.0 s`, source-cited from vendored `spine-core@4.3.0 Slider.update()`), independently re-derived by the verifier. **D-05 three-way triangulation closed: hand-math 4.0 ≡ owner Spine-4.3-editor read 4.0 (`fixtures/SLIDER_4_3/NOTES.txt` — the independent third leg; owner read Esoteric's own reference runtime: `slide` end frame t=1.0 s → `slider_bone` world Scale X=Y=4.0) ≡ sampled runtime peak 4.0.** PERF-01: a CI-ENABLED (no `skipIf`) wall-time regression gate `tests/main/sampler-worker-spineboy43.spec.ts` measures warmed `runSamplerJob` on the committed redistributable complex 4.3 rig `fixtures/spineboy_4.3/spineboy-pro.*` (spine 4.3.01, 67/52/11/14) against a hardcoded `BUDGET = ⌈measured × 3⌉ = 1479 ms` — strictly stronger than the 4.2 Girl gate (which `skipIf(CI)` because Girl is gitignored); 4.2 parity explicitly NOT assumed (the 606 ms N2.2 contract is a logged descriptive reference, not a 4.3 gate). 46-REVIEW.md advisory: 0 critical / 2 warning / 3 info (WR-01 NOTES.txt-regex newline-span fragility + WR-02 SC#2 committed-range-only — both non-blocking robustness refinements to plan-specified test code; current committed content parses correctly and the invariant holds). ⚠ workflow.security_enforcement=true but no 46-SECURITY.md (run /gsd-secure-phase 46 before milestone close). Next: Phase 47 (spine-player 4.3.0 bump + viewer regression — the only open v1.6 phase; owns the pre-existing renderer `MixBlend`/`MixDirection` import failures; decoupled + revertible + sequenced last by roadmap design).

**v1.5.1 shipped (functionally complete):** Spine Animation Viewer — Phase 41 complete (3 plans, all green; basic-render VIEWER-04 live-confirmed on SIMPLE_TEST + JOKERMAN_SPINE_ROT after 3 gap fixes landed during UAT: CSP `connect-src 'app-image:'`, CORS ACAO on protocol handler, straight-alpha SpinePlayer config). 5 HUMAN-UAT items remain pending (anim/skin switch + scrub synchrony, GL leak cycles, real-fs error UX, atlas-less parity, File menu suppression), tracked in `41-HUMAN-UAT.md` — carried forward as v1.6 Deferred Items (visual/host-blocked, not code-blocking).

**Shipped:** v1.5 Override Routing + Coverage Hardening + Atlas Repack — 2026-05-15 (5 phases, 23 plans, 18 documented REQs + 10 REPACK REQs). Tag: `v1.5.0` (pending push). Full record in `.planning/MILESTONES.md`. Prior: v1.4 (2026-05-12), v1.3.1 (2026-05-09), v1.3.0 (2026-05-07), v1.2.0 (2026-05-03), v1.1.3 hotfix (2026-04-29), v1.1.1 (2026-04-29), v1.1.0 (2026-04-28), v1.0 (2026-04-26).

**v1.5 highlights:** Split overrides per loaderMode (Phase 36 — `overridesAtlasLess` field added additively to `ProjectFileV1` / `AppSessionState` / `MaterializedProject`; legacy v1.3.x/v1.4.x `.stmproj` routes by saved `loaderMode` at the Open seam per Decision 2-A, gated on on-disk key presence; per-bucket `migrateOverrides` at all 3 IPC seams with summed `migratedKeyCount` + unioned stale keys; AppShell `activeOverrides` memo flows to all 4 `buildExportPlan` sites, `OverrideDialog` writes target the active bucket only, `Save` serializes both buckets, dirty-detection covers both; one-shot mode-toggle toast with verbatim D-04 copy + localStorage suppression; CR-01 resample-handler routing bug + WR-01 drag-drop recovery field-loss bug fixed in code-review-fix cycle; SEED-007 closed) + Spine 4.2 timeline coverage hardened (Phase 37 — RGBA2Timeline + InheritTimeline source-audited with citations to `Animation.js:755` + `Bone.js:144`; `fixtures/INHERIT_TIMELINE/` JSON+atlas+PNG; TIMELINE-03 strict `peak(detached) > peak(baseline)` per TRIGGERED escalation clause — observed 2.5× ratio; TIMELINE-04 byte-equal RGBA2 invariance via synthetic injection; SEED-005 closed) + Phase 4 polish swept (Phase 38 — `38-POLISH-AUDIT.md` enumerates IN-01..IN-06 + WR-03 with verdicts: 1 applies (IN-02 drag-to-cancel `onMouseDown` + `e.target === e.currentTarget` guard in `OverrideDialog.tsx`), 5 no-op swept by intervening phases, 1 skip; v1.0-era todo retired) + Windows host-blocked UATs cleared (Phase 39 — `host_available: yes`; both Phase 20 DocBuilder DnD + Phase 31 admin DnD UATs executed live and flipped `passed`; both pending todos retired to `resolved/`) + Atlas Repack Output (Phase 40 — additive `loose | atlas | both` output mode in OptimizeDialog emits libgdx `.atlas` + composite page PNG(s) via `maxrects-packer` + `sharp` per-region trim/rotate/composition; `core/repack.ts` pure-TS pack planner; `main/atlas-writer.ts` libgdx text serializer; `main/repack-worker.ts` sharp orchestration with REPACK-10 atomic-or-fail rollback via shared writtenPaths Set; both atlas-source + atlas-less input loaderModes produce SHA256-identical output; 4 additive `.stmproj` fields with no schema bump; rotation default off (`sharp.rotate(-90)` for WRITE direction inverse of v1.4 +90 READ direction); 3 rounds of human UAT against `JOKERMAN_SPINE.json` caught + fixed 8 bugs; SEED-008 closed). Adjacent regression caught + fixed during Phase 36 UAT: window X-button / Cmd+W path skipped the dirty-save prompt (pre-existing since Phase 8/18) — fixed in `ef38cd3 fix(36-followup)` by mirroring the `before-quit` IPC dirty-check on `mainWindow.on('close')`.

**v1.4 highlights:** Spine 4.3-beta detect-and-warn (Phase 32 — `checkSpine43Schema` predicate sniffs `root.constraints` OR `skeleton.spine ≥ 4.3` before atlas resolution; structured `SpineVersionUnsupportedError` with "re-export as Version 4.2" wording replaces today's misleading `IK Constraint not found: <name>` symptom from spine-core 4.2; drop-zone copy at `App.tsx:622` surfaces `v4.2` in `font-bold text-danger`; SEED-006 planted for the full 4.3 runtime port queued for post-`spine-core@4.3-stable` npm publish) + rotated atlas region support (Phase 33 — `RotatedRegionUnsupportedError` removed atomically alongside loader D-01 attachment-walk for canonical-corner offset override + AABB W↔H swap in `bounds.ts` + `sharp.rotate(+90)` materialization in image-worker; libgdx atlas convention nuance documented: `bounds:x,y,W,H` is canonical, page-pixel rect is `(H × W)` for rotated regions) + File → Open accepts `.json` skeletons (Phase 34 — two-IPC-step `'project:open-dialog'` + `openProjectPicker`/`loadSkeletonFromPath` with three-arm discriminated `OpenDialogResponse` envelope; D-05 dirty-guard-after-picker amends Phase 08.2 D-183 for the menu path; old `'project:open'` channel physically deleted) + region-keyed export plan (Phase 35 — `buildExportPlan` migrated from `summary.peaks` to `summary.regions` in both `src/core/export.ts` and renderer parity `src/renderer/src/lib/export-view.ts`; closes the multi-skin atlas-source undercount where 160 atlas regions collapsed to 23 ExportRows on `fixtures/SKINS/JOKERMAN_SPINE.json`).

**v1.3.1 highlights:** Per-region dedup + override-region semantics (Phase 29 — analyzer.ts attachmentName→regionName key flip + new RegionRow IPC type + AtlasPreview re-key onto regionName + attachmentNames[]; Chicken path-indirection 13-vs-14 page-count drift fixed) + safety-buffer % in Optimize dialog (Phase 30 — multiplicative on effective scale + overrides; NARROW bufferCapped predicate per CONTEXT D-06; persisted as additive optional field in `.stmproj` v1 mirroring `sharpenOnExport` precedent — no schema-version bump) + small-fixes batch (Phase 31 — source-toggle disabling on missing artifacts; Animation Breakdown collapse defaults + bulk Expand/Collapse all; Windows admin DnD fallback advisory; ExtrapolationIcon tooltip primitive). Late tester-regression fixes pre-tag: 1b5414c Strip-Whitespace export pipeline + 834c975 auto-expand failed Optimize rows + d86e7b3 per-frame canonical dims for sequence attachments.

**v1.3 highlights:** Optimize workflow UX (defer folder picker; OptimizeDialog opens immediately) + Unused Assets semantics fixed (images-folder-vs-JSON orphaned PNGs; collapsible sibling panel) + missing-attachment in-context display (red left-border + danger triangle in Global + Animation Breakdown) + UI polish pass (`#232732` surface tokens, full-width panels, zebra rows, unified `h-8` toolbar buttons, danger-themed problem-zone headers, `WarningTriangleIcon` SVG component, 2-tab strip in dedicated sub-toolbar) + Phase 4 code-quality carry-forwards closed (functional `setSelected`, OverrideDialog input guard, dead prop removed) + optional output sharpening on downscale (`sharpen({ sigma: 0.5 })`, persisted in `.stmproj`). Linux AppImage build dropped from CI (untested target; re-enable when UAT lands).

**Working:** Drop Spine `.json` + `.atlas` (or `.json` + images folder, no atlas — atlas-less mode) or `.stmproj` → Global + Animation Breakdown panels populate (rows with missing source PNGs stay visible with red accent + danger triangle) → dims-badge surfaces when actual source PNG dims drift from canonical → set per-attachment overrides (now bucketed per loaderMode — atlas-source and atlas-less buckets are independent and round-trip losslessly) → preview the resulting atlas pack → click Optimize → OptimizeDialog opens immediately, folder picker fires on Start → choose output mode `loose | atlas | both` → export. Loose mode emits the optimized `images/` folder (cap prevents upscaling beyond actual source dims; optional sharpen-on-downscale; passthrough byte-copies for already-optimized rows). Atlas mode emits a libgdx `.atlas` + composite page PNG(s) packed via `maxrects-packer`; both mode runs both pipelines with a shared rollback set so a mid-write failure rolls back EVERY artifact. Documentation Builder accessible from sticky header. Atlas-savings metric replaces the older MB unused-attachment callout. `worker_threads` sampler offload + TanStack Virtual at N≥100 keep complex rigs interactive — `fixtures/Girl/TOPSCREEN_ANIMATION_JOKER.json` samples in 606 ms (~17× under N2.2 contract). macOS Cmd+Q + AppleScript quit + window X-button / Cmd+W all guard the dirty-save prompt correctly. Update dialog routes macOS to manual-download (GitHub Releases page).

**Tech stack (locked, validated through v1.5):**
- Electron 41 + electron-vite 5 + electron-builder 26
- TypeScript (strict) + React 19 + Tailwind v4 (`@theme inline`)
- `@esotericsoftware/spine-core` 4.2.111 — skeleton/animation/constraint/physics math
- `sharp` 0.34.5 — image resize (libvips Lanczos3) + atlas page composition (Phase 40)
- `maxrects-packer` 2.7.3 — atlas pack projection (preview) + atlas pack output (Phase 40)
- `@tanstack/react-virtual` — Phase 9 row virtualization
- `vitest` 4 — testing

**Known deferred (carried into v1.6+):**
- Phase 34 HUMAN-UAT 1 open scenario (host-blocked: macOS/Windows picker rendering parity verification)
- Phase 34 VERIFICATION human_needed (same root as the above)
- Phase 13.1: macOS/Windows v1.1.0→v1.1.1 auto-update lifecycle observation (host-blocked)
- Apple Developer ID code-signing + notarization ($99/yr; revisit when business case lands)
- Crash + error reporting (Sentry / equivalent; revisit when business case lands)
- SEED-006: Full Spine 4.3 runtime port (planted 2026-05-10 at Phase 32 close; trigger condition: `npm view @esotericsoftware/spine-core@latest` returns 4.3.x OR a paying user reports they cannot re-export their rig as Version 4.2)
- Phase 40 user-deferred polish: 4 WARNs (WR-03 envelope drops atlas fields on locate-skeleton-twice recovery; WR-04 atlas fields not threaded into ResampleArgs payload — main-side coerce is dead code on happy path; WR-05 writtenPaths rollback lacks outDir-containment defense-in-depth; WR-07 `regionBuffers.get()!` non-null assertion in composite layers) + 4 INFOs (IN-01 duplicate `pageFilename` helper; IN-02 `regionBuffers` Map never `.clear()`'d — memory pressure on large atlases; IN-03 generic error message for `:` in projectName; IN-04 dedup branch emits `status:'success'` for dropped row). All explicitly deferred per user decision in `40-REVIEW.md`. Backlog candidates for v1.6+.
- `.skel` binary loader (still deferred)
- Phase-0 scale-overshoot debug session (`investigating`; long-lived tech debt — no regression observed across 8 milestones)
- Audit-acknowledged carry-forwards from v1.0–v1.4 — see STATE.md → Deferred Items for the full table

**v1.5 closures (resolved this milestone):**
- ✓ SEED-005 (RGBA2 + InheritTimeline coverage gap) — closed Phase 37 (TIMELINE-01..05)
- ✓ SEED-007 (Split overrides per loaderMode) — closed Phase 36 (OVR-01..07)
- ✓ SEED-008 (Atlas Repack Output) — closed Phase 40 (REPACK-01..10)
- ✓ Phase 4 code-review follow-up todo (v1.0-era) — closed Phase 38
- ✓ Phase 20 Windows/Linux DnD UAT todo — closed Phase 39 (Linux scope dropped)
- ✓ Phase 31 Windows admin DnD release UAT todo — closed Phase 39
- ✓ Window X-button / Cmd+W dirty-save guard — closed `ef38cd3` during Phase 36 UAT (pre-existing since Phase 8/18, surfaced + fixed in same cycle)

## Last Completed Milestone: v1.7 Multi-Scale Per-Resolution Variant Exporter — COMPLETE (closed 2026-05-25)

**Goal:** From one full-size Spine export, produce faithful scaled-down rig variants — each a complete drop-in package (scaled skeleton JSON + scaled atlas + resized textures, in its own folder) sized to the peak render demand of that smaller rig. Activates SEED-010 (explored 2026-05-21; core de-risked by spikes 001–003).

**Target features:**
- Core scale-bake: a JSON→JSON transform mirroring spine-core `SkeletonJson.scale`, dual-runtime (4.2 + 4.3) / dual-schema, with the field-identity oracle wired as a CI test (incl. a deform-heavy fixture); finishes the remaining constraint-timeline curve channels.
- Variant sizing: `variant_peak = s × master_peak` (arithmetic; the bake is a proven true similarity — never re-sample a variant).
- Single-scale variant export: one scale → one folder (scaled JSON + resized textures + scaled atlas), respecting the existing `loose | atlas | both` output mode; reuses the existing export + atlas-writer pipeline.
- Two-way scale↔dimension input on the setup-pose rig bounds (enter a factor or a target px, see the other).
- Batch: N scales → N folders + folder-naming UX.

**Locked design facts (from SEED-010 + spikes 001–003, do not relitigate):**
1. **Don't scale a bone** (root explodes +316..619% on DEMON R_ARM; pivot leaves ±20–50% constraint residual) — produce variants via a **full Spine-style similarity bake** (= `SkeletonJson.scale` as a JSON→JSON transform). PROVEN field-identical to Spine's own scaling on 4.2 + 4.3.
2. **variant_peak = s × master_peak** (exact). The sampler's `peakScale` is invariant under the bake (measurement blind spot) — NEVER size a variant by sampling it.
3. **Faithfulness bar:** scaled variant behaves identically to the master (proven achievable end-to-end — DEMON world-AABB exactly s× incl. constraint-driven R_ARM).
4. **Regression oracle:** `parse(bake(orig,s),1)` ≡ `parse(orig, SkeletonJson.scale=s)`, run across a fixture matrix incl. a deform-heavy rig (DEMON 4.3 has no deform → false confidence).
5. Dual-runtime (4.2 + 4.3) + both loader modes (atlas-source + atlas-less) are hard requirements; `core/` stays pure-TS (Layer-3 invariant). First feature to ever make the app **write** a skeleton JSON; source JSON never modified.

**Open product decisions (deferred to discuss-phase, NOT technical unknowns):** output-folder naming convention; whether per-attachment overrides are shared or independent per scale; exact two-way-input UX.

**Phase numbering:** Continues from v1.6. Starts at **Phase 48** (no `--reset-phase-numbers`). Prior phase directories retained in `.planning/phases/`.

**Progress:** 6/6 phases (100%) — COMPLETE. All phases 48–53 done + verified + security-audited; 13/13 requirements satisfied; milestone archived to `milestones/v1.7-*` + tagged `v1.7` (local, not pushed) on 2026-05-25. **Phase 53 COMPLETE 2026-05-24** (Persist Variant State in `.stmproj`, SCALEUI-03; additive-optional `variantRows` round-trip, no version bump). **Phase 52 COMPLETE 2026-05-24** (Batch Export Robustness + Variant-Dialog Cleanup, EXPORT-06; 4/4 plans, verified 10/10 must-haves). **Phase 51 COMPLETE 2026-05-23** (Batch Variant Export, EXPORT-04). **Phase 50 COMPLETE 2026-05-23** (Rig-Bounds + Two-Way Scale↔Dimension Input, SCALEUI-01/02). **Phase 49 COMPLETE 2026-05-22** (Single-Scale Variant Export; 3/3 plans, verified 12/12 must-haves — EXPORT-01/02/03/05). First end-user value of v1.7 + the first feature to ever make the app WRITE a skeleton JSON: an "Export Variant…" action bakes a scaled copy of the skeleton JSON (`bake(json,s)` from Phase 48, source never mutated — sha256-proven), sizes textures arithmetically (`variant_peak = s × master_peak` via `scaleSummaryPeaks`, never re-sampling), and reuses the existing `buildExportPlan` + atlas-writer pipeline UNCHANGED to emit a drop-in `{NAME}@{s}x/` package across `loose | atlas | both` × dual-runtime (4.2+4.3) × dual loader-mode. Faithfulness oracle loads the *written* package and confirms world-AABB exactly s× on 4.2 + 4.3. Code review (1 blocker CR-01 + 6 warnings) fully fixed at owner request (re-export no longer silently corrupts; errors surfaced; re-entrancy/boundary hardening) and re-verified. One human item remains (native-dialog + visual half-size UAT — structurally manual, tracked in 49-HUMAN-UAT.md). **Phase 48 COMPLETE 2026-05-22** (Core Scale-Bake Module + Regression Oracle; 4/4 plans, 4/4 must-haves — BAKE-01..04): `src/core/scale-bake.ts` Layer-3-pure JSON→JSON similarity bake field-identical to spine-core `SkeletonJson.scale`, oracle in CI across 8 rigs × 3 scales. Next: Phase 53 (Persist Variant State in `.stmproj`, SCALEUI-03 — the last v1.7 phase).

## Previous Milestone: v1.6 Spine 4.3 Runtime Port (Dual-Runtime) — COMPLETE (shipped as v1.6.1)

**Goal:** Port the skeleton/animation math from a single spine-core 4.2 runtime to a dual-runtime architecture that loads and correctly samples both Spine 4.2 and Spine 4.3 skeleton JSON, routed by detected skeleton version. Activates SEED-006 (trigger fired 2026-05-16: `npm view @esotericsoftware/spine-core@latest` → `4.3.0`).

**Target features:**
- Add `@esotericsoftware/spine-core@4.3.0` alongside the existing `@4.2.111` via npm package aliasing (same package name, two pinned versions)
- Version-detection router: repurpose the existing `checkSpine43Schema` predicate (v1.4 Phase 32) from a **rejecter** into a **dispatcher** — 4.3 JSON routes to the 4.3 runtime instead of throwing `SpineVersionUnsupportedError`
- Port the diverging call sites behind a runtime-abstraction layer: `core/sampler.ts` (5 API renames + Pose API threading), `core/bounds.ts` (2 `computeWorldVertices` signature changes) — `core/` Layer-3 purity preserved
- New 4.3-only `slider` constraint type — validate timeline propagation via a dedicated fixture
- Bump `@esotericsoftware/spine-player` 4.2.111 → 4.3.0 (sibling-aligned with spine-core) + viewer regression pass
- Update the v1.4 drop-zone / error copy that tells users to "re-export as Version 4.2" — that advisory is wrong for the now-supported 4.3 path
- CI matrix tests 4.2.x and 4.3.x rigs side-by-side

**Locked design facts (from SEED-006 + 2026-05-16 milestone decisions, do not relitigate):**
1. **Dual-runtime, not single-runtime.** Keep spine-core@4.2.111; add @4.3.0 side-by-side; route by detected version. (User decision 2026-05-16 — overrides SEED-006's single-port assumption.)
2. **4.2 support is retained and regression-gated.** The in-repo `fixtures/SIMPLE_PROJECT/SIMPLE_TEST.json` 4.2 fixture stays green; existing 4.2-rig users are protected.
3. **PORT-04 (vendoring) collapses to a `package.json` alias bump** — npm 4.3.0 published, so SEED-006 Option (a) is satisfied; no submodule/fork.
4. **`core/` stays pure-TS (Layer-3 invariant).** The runtime-dispatch abstraction must remain DOM/Electron/sharp-free (locked by `tests/arch.spec.ts`).

**Scope flags:**
- ⚠ Dual-runtime expands SEED-006's single-port costed inventory (PORT-01..04): adds npm aliasing + a runtime-dispatch abstraction + a doubled test matrix. SEED-006's "~2-3 weeks / 4-task" estimate is low for this shape.
- ⚠ SEED-006's inventory was built against 4.3-**beta**; stable 4.3.0 had mid-beta churn (`uniform`→`scaleY` @ 4.3.73-beta). Research re-verifies the stable surface before planning.

**Phase numbering:** Continues from v1.5.1. Starts at **Phase 42** (no `--reset-phase-numbers`). Prior phase directories retained in `.planning/phases/`.

## Next Milestone: v1.8 (TBD)

**Status:** unscoped — v1.7 is COMPLETE + closed (2026-05-25); no milestone is currently active. Start with `/gsd-new-milestone` to scope v1.8. Candidate scope draws from the deferred / backlog list — Phase 40 polish carry-forward (8 items: WR-03/04/05/07 + IN-01..04 from `40-REVIEW.md`), the 5 open Phase 41 viewer HUMAN-UATs, atlas-repack maturity pass on real-user rigs, a 4.2 deprecation decision (now that v1.6 dual-runtime has shipped + matured), documentation refresh, plus v1.7 Future items (per-scale override behavior across scales, variant export presets / saved scale-sets, a what-if peak preview). Re-validate at `/gsd-new-milestone` time.

**Out of scope (continued exclusions):**
- Linux build/UAT (dropped at v1.3 ship; per `project_linux_deferred` memory)
- `.skel` binary loader (carried since v1.0)
- Cross-mode override copy/sync (deliberately rejected at SEED-007 capture per `project_strict_loadermode_separation`)
- Spine 4.3 schema-shim (SEED-003 Option B — HIGH trap risk; superseded by this milestone's real dual-runtime port)

## Primary user

Spine animators exporting rigs for performance-sensitive runtimes (mobile games, web games). Assumes fluency in Spine concepts (bones, slots, attachments, skins, constraints) but not in runtime math.

## Key Decisions (v1.0 outcomes)

| Decision | Outcome | Notes |
|----------|---------|-------|
| `core/` is pure TS, zero DOM/Electron deps | ✓ Good | Layer 3 arch.spec invariant held across all 12 phases — no `src/core/*` import of `sharp`, `node:fs`, or DOM. Made every math change unit-testable headless. |
| No PNG decoding during the math phase (stub `TextureLoader`) | ✓ Good | Sampler hot loop is pure CPU + spine-core. Held through Phase 9 worker offload. |
| 120 Hz default sampling rate (configurable) | ✓ Good | Above 60 Hz catches sub-frame easing peaks. Phase 9 made it configurable per-project via `samplingHz` slot. |
| Phase 0 derisks core math before any UI | ✓ Good | Saved 2-3 days of rework — UI phases shipped on a stable contract. |
| Override semantics: 100% = source dimensions, never surpassed (D-91) | ✓ Good | Supersedes original D-78/D-79 percent-of-peak semantics. Caught in Phase 4 human-verify; clearer mental model for animators. |
| Phase 6 export sizing: uniform-only on both axes | ✓ Good | Anisotropic export breaks Spine UV sampling. Locked in memory. |
| F7.2 reinterpreted: dims + page count + per-page efficiency, no bytes (D-127) | ✓ Good | File-size delta would require encoding every region — contradicts the Lanczos quality preservation contract. |
| `.stmproj` v1 forward-compat: reserved `documentation: object` slot (D-148) | ✓ Good | Documentation Builder (Phase 20) filled this slot without a schema-version bump — D-148 forward-compat design proved out. |
| macOS auto-update → manual-download UX (Phase 16) | ✓ Good | Squirrel.Mac code-sig strict-validation on ad-hoc builds made in-process swap impossible without Apple Developer ID. Manual-download is simpler, honest, and avoids $99/yr enrollment. |
| Atlas-less mode via synthetic atlas (Phase 21) | ✓ Good | IHDR byte-parsing (no decode) + synthetic TextureAtlas keeps Layer 3 invariant intact. AtlasNotFoundError preserved verbatim for malformed-project path. |
| Override-aware passthrough partition evaluated POST-override (Phase 22.1) | ✓ Good | Evaluating passthrough BEFORE override silently dropped animator overrides on capped rows (G-07 BLOCKER). POST-override re-partition was the only correct fix. |
| `worker_threads` Worker with path-based protocol + terminate-cancel (D-190/D-193/D-194) | ✓ Good | Greenfield in Phase 9. 606 ms wall-time on complex rig. |
| Discriminated-union typed-error envelope (D-158/D-171) | ✓ Good | 8 kinds including 7-field `SkeletonNotFoundOnLoadError` recovery payload. Caught misuse at compile time during Phase 8.1. |
| Atomic Pattern-B write (`.tmp` + `fs.rename`) | ✓ Good | Reused across Phase 6 (sharp export) and Phase 8 (.stmproj save) — load-bearing across two subsystems. |
| Hand-rolled ARIA modals (no library) | ✓ Good | OverrideDialog scaffold cloned by OptimizeDialog → AtlasPreviewModal → SaveQuitDialog → SettingsDialog → HelpDialog. Five dialogs, one pattern. |
| Optimize folder picker deferred to Start/Export click (Phase 23) | ✓ Good | Dialog opens immediately on toolbar click — user sees the export plan before the native folder picker stalls. Eliminates up-front modal-on-modal sequencing. |
| Missing-attachment rows synthesized post-sampler in `buildSummary` (Phase 25) | ✓ Good | Synthetic-atlas excludes stub regions from sampler output; `.map()+mark` had nothing to mark. Synthesizing stub `DisplayRow` entries from `skippedAttachments` is the only way to keep missing rows visible in panels. |
| `WarningTriangleIcon` shared component (Phase 26.2 D-06) | ✓ Good | Single source of truth for the 4 fill→stroke conversions. Replaces ad-hoc Unicode ⚠ glyphs that couldn't size precisely. |
| Tab strip in dedicated sub-toolbar row (Phase 26.2 sketch-001 variant A) | ✓ Good | Two prior reverts surfaced AP-01: tabs-in-main-toolbar caused vertical-space contention. Lifting to a separate row resolved AP-01 cleanly. The 3-tab restructure was DROPPED same milestone in favor of alert-bar layout. |
| Functional `setSelected((prev) => ...)` updater (Phase 27) | ✓ Good | Closes a latent stale-closure race in `handleToggleRow` + `handleRangeToggle`. Durable regression spec compares closure-form vs functional-form side-by-side. |
| Optional output sharpening on downscale, default OFF (Phase 28) | ✓ Good | `sharpen({ sigma: 0.5 })` mirrors Photoshop's "Bicubic Sharper (reduction)" preset. Downscale-only gate (`effectiveScale < 1.0`); passthrough rows + 1.0× rows unaffected. Persists per-project in `.stmproj` (additive optional `sharpenOnExport: boolean`). |
| PMA preservation falsified — sharp 0.34 + libvips 8.17 auto-handle PMA (Phase 28 pivot) | ✓ Good | Backlog 999.9 ("PMA preservation in Optimize Assets export") closed `falsified` 2026-05-06. `scripts/pma-probe.mjs` retained as regression sentinel. Phase 28 pivoted same day to optional sharpening — the actual user-visible quality lever. |
| Linux AppImage dropped from v1.3 release CI (untested target) | ✓ Good | Linux build path never verified live against a real Linux host. `electron-builder.yml linux:` block retained as no-op for re-enable after Linux UAT lands. |
| Split overrides per loaderMode (atlas-source / atlas-less buckets, SEED-007 D-1/D-2-A/D-3-A, Phase 36) | ✓ Good | Math is mode-invariant by construction (verified at SEED-007 capture); bug was UX intent-routing only. Schema-additive `overridesAtlasLess` field follows `loaderMode`/`sharpenOnExport`/`safetyBufferPercent` precedent — no version bump. Legacy file routing keyed on saved `loaderMode` at the Open seam (Decision 2-A); mode-switch leaves inactive bucket untouched (Decision 3-A); aligns with `project_strict_loadermode_separation`. CR-01 resample-handler bucket-routing bug surfaced + fixed in code-review-fix cycle. |
| TIMELINE-02 conditional escalation TRIGGERED — InheritTimeline mutates `Bone.inherit` read by `updateWorldTransform` (Phase 37) | ✓ Good | Source audit at `Animation.js:755` (writer) + `Bone.js:144` (reader) proved world-transform effect; TIMELINE-03 became load-bearing rather than precautionary. Strict `peak(detached) > peak(baseline)` test asserts the contract — observed 2.5× ratio (BASELINE=0.4 vs INHERIT_DETACH=1.0). RGBA2Timeline confirmed geometry-invariant (writes only `slot.color` / `slot.darkColor`). |
| Atlas Repack output mode is additive (default `loose`); JSON invariant under repack (SEED-008, Phase 40) | ✓ Good | Default `loose` preserves byte-equivalent pre-Phase-40 behavior (SHA256-baseline gate). `atlas` and `both` modes emit libgdx `.atlas` + composite page PNG(s); skeleton JSON is NOT modified (source-confirmed against spine-ts 4.2.111 — runtime reads region dims from `.atlas`, references are by name). 4 additive `.stmproj` fields with no schema bump. `core/` purity preserved (pack math); `sharp` + `.atlas` writing live in `main/`. |
| Sharp-emits-truth: packer reads back actual emitted dims via `metadata()` (Phase 40) | ✓ Good | Plan-target dims (`outW/outH` from `buildExportPlan`) and sharp-emitted dims can drift on edge cases (1px rounding); the packer must consume sharp's reality not the plan's intent. `repack-worker.ts:292-294` reads `meta.width/height` post-resize and feeds the packer. Eliminates pack-vs-emit dimension mismatches that would surface as visual cropping. |
| Shared `writtenPaths: Set<string>` rollback across `runExport` + `runRepack` (Phase 40, REPACK-10 D-04a) | ✓ Good | `both` mode dispatches loose+atlas pipelines sequentially under one shared rollback set so a mid-write failure rolls back EVERY artifact (loose PNGs AND `.atlas` + page PNGs). Inner-catch `fs.rm` sweep at `ipc.ts:974-976`. WR-05 noted as defense-in-depth opportunity (no outDir-containment check) — deferred per user. |
| Window X-button / Cmd+W dirty-save guard (close-path) — `mainWindow.on('close')` mirrors `before-quit` (`ef38cd3` during Phase 36 UAT) | ✓ Good | Pre-existing gap since Phase 8/18 — only `app.on('before-quit')` had the IPC dirty-check; window-close path skipped it entirely, silently discarding unsaved work on X / Cmd+W. Surfaced during Phase 36 Test 8; fix mirrors the `before-quit` pattern (re-entry guard + `event.preventDefault()` + IPC roundtrip). All 4 close-path scenarios (X-dirty, Cmd+W-dirty, X-clean, Cmd+W-clean) human-re-verified. |
| Dual-runtime (4.2 + 4.3 side-by-side, routed by detected version), not single-port (v1.6) | ✓ Good | npm-alias dual install (`@esotericsoftware/spine-core@4.3.0` canonical + `spine-core-42`=`4.2.111`); a `SpineRuntime` facade keeps `core/` calling through `load.runtime.*` (Layer-3 purity preserved, arch-anchored). 4.2 byte-equal regression-gated throughout. Shipped as v1.6.1. |
| Variants via a full Spine-style similarity bake, NOT bone-scaling (v1.7, SEED-010) | ✓ Good | Scaling a bone explodes (root) or leaves constraint residual (pivot) — falsified in SEED-010, proven in spikes 001–003. `src/core/scale-bake.ts` is a Layer-3-pure JSON→JSON transform field-identical to spine-core `SkeletonJson.scale`, gated by a sampling-free round-trip oracle in CI. |
| `variant_peak = s × master_peak` arithmetic; never re-sample the baked variant (v1.7) | ✓ Good | The sampler's `peakScale` is invariant under the bake (a measurement blind spot) — re-sampling a variant would silently undersize it. Variant texture demand is pure arithmetic on the master peak; the variant reuses `buildExportPlan` + the atlas-writer UNCHANGED. |
| Variant `.stmproj` persistence is additive-optional, no version bump (v1.7, Phase 53) | ✓ Good | `variantRows: { scale: number }[]` added end-to-end following the `sharpenOnExport`/`safetyBufferPercent`/`overridesAtlasLess` precedent; `V_LATEST` stays 1; old files open to defaults; the output half reuses the already-persisted `lastOutDir` as a picker hint (no new fs check). |

## Constraints (still valid)

- Spine 4.2 **and 4.3** are both supported (dual-runtime, shipped in v1.6 / v1.6.1; routed by detected skeleton version). Pre-release in-band 4.2.x/4.3.x export tokens (e.g. `4.3.91-beta`) are hard-rejected at `resolveRuntimeTag`; `<4.2` and `≥4.4` are typed-rejected. v1.7's variant exporter works across both runtimes.
- JSON skeletons only (`.skel` binary deferred to next milestone).
- Per-individual-skin sampling (combined-skin compositing out of scope).
- `core/` cannot import DOM, Electron, or `sharp` (Layer 3 invariant — locked by `tests/arch.spec.ts`).
- CLI byte-for-byte unchanged across phases (D-102; CLAUDE.md fact #3 / #5).

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

## Project root

`/Users/leo/Documents/WORK/CODING/Spine_Texture_Manager/`

## Key references

- Approved plan: `~/.claude/plans/i-need-to-create-zesty-eich.md`
- Simple test rig: `fixtures/SIMPLE_PROJECT/` (CIRCLE, SQUARE, TRIANGLE + CHAIN_2..8 + TransformConstraint)
- Complex test rig: `fixtures/Girl/` (used for Phase 9 N2.2 wall-time gate)
- Spine 4.2 API: <http://esotericsoftware.com/spine-api-reference>
- Spine JSON format spec: <https://en.esotericsoftware.com/spine-json-format>
- Sharp resize API: <https://sharp.pixelplumbing.com/api-resize/>

---

*Last updated: 2026-05-26 — **Phase 55 COMPLETE** (variant effScale clamp lifted to `1/vs`; `computeExportDims` CR-01 fixed; 1584 tests pass). Prior: 2026-05-25 — **v1.7 milestone CLOSED via `/gsd-complete-milestone`.** All 6 phases (48–53) complete, verified, and security-audited (`48-SECURITY.md`..`53-SECURITY.md`); 13/13 requirements satisfied; the one genuinely-new open item (Phase 49 native folder-picker visual UAT) was live-confirmed on both 4.2 and 4.3 legs on 2026-05-25; the remaining 11 open-artifact-audit items are documented prior-milestone carry-forwards, acknowledged + deferred (recorded in STATE.md → Deferred Items). Archived `ROADMAP.md` → `milestones/v1.7-ROADMAP.md` + `REQUIREMENTS.md` → `milestones/v1.7-REQUIREMENTS.md`; ROADMAP.md reorganized (v1.7 collapsed to a one-line milestone entry); `REQUIREMENTS.md` removed via `git rm` (fresh for next milestone); MILESTONES.md + RETROSPECTIVE.md updated; annotated tag `v1.7` created locally. **Nothing pushed/published — release is a separate owner ship decision** (per `feedback_dont_push_release_tags`; v1.4.0/v1.5.0 are likewise local-only). Next: `/gsd-new-milestone` to scope v1.8. Prior footer: 2026-05-24 — **Phase 53 COMPLETE → v1.7 FEATURE-COMPLETE (Persist Variant State in `.stmproj`, SCALEUI-03; 2/2 plans across 2 worktree-isolated waves, all autonomous; verified 10/10 must-haves / all 3 ROADMAP SC).** Adds an additive-optional `variantRows: { scale: number }[]` field persisted end-to-end — Wave 1 (53-01, data tier): declared on `ProjectFileV1`/`AppSessionState`/`MaterializedProject` (types.ts) + `PartialMaterialized` (project-file.ts); pure-core validator pre-massage (missing → `[{scale:0.5}]`, rejects non-array/non-finite-scale, accepts empty `[]` — mitigates tampering T-53-01/02), serialize (scales only) + materialize back-fill, threaded into main's Open response; **NO `.stmproj` version bump (`V_LATEST` stays 1, D-05)** and **NO new fs check on `lastOutDir`** (the saved output location reuses the already-persisted `lastOutDir` as a picker hint — D-01/D-02/SC#3); core (8 cases) + main (5 cases) test contract. Wave 2 (53-02, renderer): `AppShell.tsx` restores rows on BOTH load paths (first-open `useState` initializer + `mountOpenResponse`, each re-keyed with fresh `crypto.randomUUID()`); `buildSessionState` emits the scales; dirty-by-**scale-projection** machinery (order-sensitive `number[]` compare in both `isDirty` arms + `lastSaved.variantScales` + both `onClickSave`/`onClickSaveAs` snapshots) so editing rows marks dirty while a freshly-opened project is NOT dirty (regenerated ids never enter the compare — D-03) and `lastOutDir` stays non-dirty (D-03 asymmetry); 2 jsdom tests lock restore-into-UI + the dirty asymmetry. The Export Variant dialog's scale rows + chosen output location now round-trip across sessions on save. Post-merge gate GREEN: `typecheck:node` + `typecheck:web` exit 0; full vitest **1553 passed / 0 failed** (20 skipped, 2 todo); arch.spec.ts 20/20 (Layer-3 purity preserved — `src/core/project-file.ts` gained no DOM/Electron/sharp/node:fs import). **Code review (standard, 8 files): 0 blocker / 3 warning / 4 info (`53-REVIEW.md`)** — all 3 warnings align with locked-plan decisions, NOT correctness/data-integrity: WR-01 (locate-skeleton recovery hop resets rows to default) = the explicit 53-01 Task-1 recovery-envelope deferral; WR-02 (validator accepts any finite scale, not strictly `(0,1)`) = threat T-53-03 dispositioned "accept" (value clamp lives in the renderer + export-time `VariantScaleError`); WR-03/IN-* = quality polish (shared `DEFAULT_VARIANT_ROWS` constant). **53-VERIFICATION.md PASSED 10/10** (verifier goal-backward against source + ran the real UI-restore test, did not trust SUMMARYs). One pending todo auto-closed (`2026-05-23-persist-variant-rows-and-output-location.md`, resolves_phase 53). **v1.7 is now feature-complete across all 6 phases (48–53). Next = milestone-close housekeeping: `/gsd-secure-phase` for 48–53 (no SECURITY.md yet — milestone-CLOSE gate, run before the v1.7 tag), then `/gsd-complete-milestone`.** Nothing pushed (standing preference). Prior footer: 2026-05-24 — **Phase 52 COMPLETE + verified (Batch Export Robustness + Variant-Dialog Cleanup, EXPORT-06; 4/4 plans across 2 worktree-isolated waves, all autonomous).** Wave 1 (parallel, disjoint files): 52-01 = per-row duplicate-token continue-on-error in `handleExportVariantBatch` (WR-02 — replaced the whole-batch abort with a `dupTokens` Set; colliding rows `pushResult({status:'failed'})`+`continue` before `exportOneVariant`, non-colliding scales still export) + no orphan empty `{NAME}@{s}x/` dir on rollback (WR-03 — `readdir(outDir).length===0` only-if-empty removal in the rollback catch); 52-02 = unified variant-channel `safetyBufferPercent` coercion at BOTH `ipc.ts` variant handlers (WR-04 — dropped the redundant `Number(safetyBufferPercent) || 0`, now bare `Number(...)` relying on the single canonical `Number.isFinite` clamp in `exportOneVariant`; documenting comment recording the deliberate coerce-and-clamp policy, distinct from `export:start`'s validate-and-reject); 52-03 = removed the dead `plan` prop + its `buildExportPlan` call in the variant path (IN-03; kept `buildExportPlan`/`ExportPlan` live in the untouched Optimize flow) + converted the misleading `onStart` `useCallback(…,[props,…])` to a plain async fn (IN-04). Wave 2: 52-04 = regression locks — new `tests/main/variant-token-equivalence.spec.ts` asserting `tokenFor` ≡ `formatScaleToken` across IEEE-754 + near-collision samples (IN-01) + a dup partial-failure test + an orphan-folder-GONE tightening. **D-09 (no happy-path behavior change) held — changes confined to error/edge/rollback paths + dead-code removal.** One config widening: `tsconfig.node.json` `include` += `src/renderer/src/modals/*.ts` (single-level glob → matches exactly the one zero-import pure helper `variant-scale-derive.ts`; `.tsx` DOM modals excluded; mirrors the pre-existing `src/renderer/src/lib/**/*.ts` include) so the tests/main spec can typecheck its import. One recovered slip: a 52-01 early Edit briefly landed in the main tree, reverted via file-specific `git checkout` before any commit (main verified clean). Post-merge gate GREEN: `typecheck:node` + `typecheck:web` exit 0; full vitest **153 files / 1546 passed / 12 skipped / 2 todo / 0 failed**. **Code review (standard, 9 files): 0 blocker / 2 warning / 3 info (`52-REVIEW.md`)** — the dropped `|| 0` coercion adversarially proven safe (every undefined/null/""/"abc"/negative/over-range path resolves identically through the canonical clamp); WR-01 (degenerate `@0x`/`@1x` token reported as "duplicate" not "degenerate" — diagnostic misdirection only) + WR-02 (the `variant:batch-progress` event fires before the dup-skip so the renderer counter advances for a row that never runs) are cosmetic/diagnostic, NOT correctness or data-integrity — optional `/gsd-code-review-fix 52`. **52-VERIFICATION.md PASSED 10/10** (verifier goal-backward against source, not SUMMARYs; confirmed a single-occurrence degenerate scale is never swept into `dupTokens` so SC#1 holds). Nothing pushed (standing preference). Next: `/gsd-discuss-phase 53` (Persist Variant State in `.stmproj`, SCALEUI-03 — the last v1.7 phase), then v1.7 milestone-close housekeeping (`/gsd-secure-phase` for 48–52, then `/gsd-complete-milestone`). Prior footer: 2026-05-23 — **Phase 51 COMPLETE → v1.7 (phases 48–51) ALL PHASES COMPLETE (Batch Variant Export, EXPORT-04 — the Multi-Scale Variant Exporter milestone finale; 2/2 plans across 2 worktree-isolated waves, all autonomous, verified 2/2 must-haves + user live-UAT approved).** Wave 1 (51-01): a behavior-preserving extraction of the single-scale `handleExportVariant` body into a private un-guarded `exportOneVariant` (so all 34 Phase-49 variant tests stay green) + a thin guard wrapper + `handleExportVariantBatch` that claims the re-entrancy slot ONCE then loops `exportOneVariant` per scale — **SC#2 (each batch variant byte-identical to the single-scale path for that scale) holds BY CONSTRUCTION** because the batch runs the same body verbatim. D-07 continue-on-error (each variant mints its OWN `written` rollback Set — no batch-wide Set; a failed variant rolls back only its own folder, the loop continues), D-09 between-variants cancel (`variantBatchCancelRequested` reset at batch start [Pitfall 5], checked at the top of each iteration, in-flight variant never interrupted), D-10 token-collision defense; new `variant:exportBatch` (scales[] finite-coercion, Security V5) + `variant:cancelBatch` IPC channels + `BatchVariantResult` type + preload bindings. Proof: `tests/main/variant-batch-faithful.spec.ts` byte-identity matrix across 4.2/4.3 × atlas-source/atlas-less (honoring the 4.3-atlas-less = atlas-mode-only deviation) + continue-on-error + cancel, 12/12. Wave 2 (51-02): the renderer multi-row `VariantDialog` — `tokenFor` renderer-local helper (== main's `formatScaleToken`, Layer-3, no core/ import) + `rows[]`/`onRowsChange` (lifted to AppShell, opens with one row at 0.5) replacing the single `scale` prop; per-row Phase-50 two-way factor↔W↔H control keyed by `row.id`; `+ Add scale` (no presets) / per-row remove (disabled at 1 row); duplicate-token highlight + Export-disable (D-10) + invalid-row Export-disable (D-11); the complete state is a per-folder `BatchVariantResult` result list + "X of N exported" aggregate (D-08); in-progress "variant N of M — {token}" prefix + Cancel via a new `onVariantBatchProgress` preload subscription (D-09); ONE `exportVariantBatch(rows.map(r=>r.scale),…)` call after ONE parent pick (D-12, `onConfirmStartVariant` reused verbatim) with ONE override bucket (D-13); single-pane, NO tabs (D-06 overturns the old tabs-at-51 expectation). The existing Phase-49/50 variant specs were migrated to the rows prop and stay green; the `.ts`-renderer-glob landmine was avoided (new spec is `.spec.tsx`; `variant-twoway.spec.ts` stays in the tsconfig.node exclude). Post-merge gate GREEN across the integrated phase: typecheck:node + typecheck:web exit 0; full vitest **152 files / 1530 passed / 0 failures**; arch.spec.ts 20/20 (Layer-3 holds). **Code review (standard, 12 files): 0 blocker / 5 warning / 4 info (`51-REVIEW.md`)** — the headline WR-01/WR-02 (token rounds to 4 decimals while the validity guard accepts any 0<s<1, so a degenerate-extreme scale lands a misleadingly-named `@0x`/`@1x` folder / false-positive dedup) was probed by the verifier: byte-identity to the single-scale path still holds at those scales so SC#2 is NOT violated, and the extremes (s<0.0001 / s>0.9999) are far outside any real resize workflow (input step 0.05) — correctly WARNINGs, not blockers. **51-VERIFICATION.md PASSED 2/2** (verifier re-ran every gate, did not trust SUMMARYs). **User ran the live Electron UAT (`npm run dev`) and APPROVED both items 2026-05-23:** (1) a 3-scale batch (0.5/0.46/0.57) on CHJWC_SYMBOLS wrote sibling `@{s}x/` folders from ONE parent pick; (2) continue-on-error confirmed live — `@0.5x` showed `✗ Refusing to overwrite existing file` while `@0.46x` (346 files) + `@0.57x` (346 files) landed, "2 of 3 exported" aggregate. **User flagged two non-blocking UX follow-ups (v1.8 candidates, recorded in 51-HUMAN-UAT.md + the Next Milestone section):** (a) add a batch progress bar (Optimize-dialog style — the `onVariantBatchProgress` data already flows; addresses WR-05 = Cancel has no in-flight feedback); (b) the VariantDialog footer buttons overflow the modal when content grows (many rows / error reasons) — needs an internal scroll region with a pinned footer. ⚠ workflow.security_enforcement=true and the v1.7 export phases DO add I/O surface (variant skeleton-JSON writes + per-folder fan-out + the `variant:exportBatch` IPC boundary) but no SECURITY.md exists for 48/49/50/51 — run /gsd-secure-phase 49 + /gsd-secure-phase 51 before tagging/shipping v1.7. Nothing pushed to remote (standing preference — v1.7 tag/push is the owner's ship decision). Post-UAT polish (3 user-requested rounds, all live-confirmed, NOT pushed): `1c68cb8` (footer-overflow scroll fix + progress bar + WR-05 Cancel feedback + WR-01/IN-02), `167d6e0` (per-image progress bar + live green/amber/red scale-row & summary-row coloring via a new `variant:result` event), `fc92a54` (variant-export overwrite via a pre-flight ConflictDialog — SUPERSEDES 51 D-12 "fail per-folder"). Persisting variant rows + output location to `.stmproj` deferred to v1.8 (todo captured). Final full suite 152 files / 1536 passed / 0 failed. Next: v1.7 milestone-close housekeeping (`/gsd-secure-phase 51`, then `/gsd-complete-milestone`). Prior footer: 2026-05-23 — **Phase 50 COMPLETE (v1.7 Rig-Bounds + Two-Way Scale↔Dimension Input; 2/2 plans across 2 waves, all autonomous, verified 2/2 must-haves — SCALEUI-01/SCALEUI-02; user UAT approved).** Wave 1 (50-01, SCALEUI-02): `src/core/setup-bounds.ts` `computeSetupPoseBounds(load)` — a Layer-3-pure, dual-runtime ALL-SKINS setup-pose world-AABB union (generalizes the proven `aggregateWorldAABB`, swapping slot-bindings for the sampler Pass-1.5 skin manifest) with the mandatory `measured===0 → null` degenerate guard so no non-finite ever crosses IPC; surfaced as additive `SkeletonSummary.bbox: {w,h}|null` computed once in `summary.ts` via the already-bound runtime (REG-47-01-safe). Wave 2 (50-02, SCALEUI-01): enriched the `VariantDialog` Scale card IN PLACE (D-09, no tabs) — bbox W×H reference line + three coupled, aspect-locked inputs (Factor / target-W px / target-H px, uniform-only) backed by pure renderer-local `pxFromScale`/`scaleFromPx`/`displayFactor` helpers; typed px honored EXACTLY (no snap, D-03), `s` stays the single source of truth the export consumes (D-02), over-range `s≥1` allowed-but-Export-disabled with the inline hint (D-04), degenerate rig degrades gracefully; reads `summary.bbox`, ZERO new IPC/props. Post-merge gate caught a real cross-plan typecheck:node break — the new pure-helper test `tests/renderer/variant-twoway.spec.ts` (a .ts renderer test caught by the node program's `tests/**/*.ts` glob) imported a renderer-src module not in that program; fixed by excluding it (`2f979d7`, same class as the existing dual-viewer-routing.spec.ts exclusion — renderer tests are vitest-only). **Code review (standard, 13 files): 0 blocker / 4 warning / 4 info — WR-02 (VariantDialog reads raw `summary` not `effectiveSummary`) was FALSIFIED: that is the deliberate documented export-pipeline contract (AppShell.tsx:1239-1242, `effectiveSummary` is render-tree-only), so the bbox display matches exactly the shape the export bakes; switching would be a regression.** User ran the dual-runtime UAT (`npm run dev`, 4.2 + 4.3) and approved the rendered control; found ONE interaction defect — typing a multi-digit Width/Height was impossible because focus jumped to the Factor field after the first digit. **Root cause was the shared `useFocusTrap` hook (NOT VariantDialog): it listed `onEscape` in its effect deps; callers derive onEscape from an inline `props.onClose`, so every onScaleChange-driven parent re-render handed it a fresh identity → the effect tore down + re-ran → its mount-time auto-focus yanked focus to the first tabbable (the Factor input). Latent since Phase 6, invisible in Phase 49 (Factor was the only field), exposed by Phase 50 adding Width/Height after it. Fixed `7fe4528` — read onEscape from a ref + drop it from the effect deps so auto-focus is genuinely mount-only (matches the hook's documented invariant); benefits all 13 modals using the hook; covered by V13 regression test (controlled harness reproduces AppShell's fresh-per-render onClose, asserts focus stays on Width across keystrokes).** Gates: `npm run typecheck` (node+web) exit 0; full vitest **150 files / 1510 passed / 0 failures**. ⚠ workflow.security_enforcement=true but no 50-SECURITY.md (renderer UI + pure core/ bbox reader, no new I/O surface — run /gsd-secure-phase 50 before milestone close for the paper trail). Nothing pushed to remote (standing preference). Next: Phase 51 (Batch Variant Export). Prior footer: 2026-05-22 — **Phase 49 COMPLETE (v1.7 Single-Scale Variant Export; 3/3 plans across 2 waves, all autonomous, verified 12/12 must-haves — EXPORT-01/02/03/05).** The first end-user value of v1.7 and the first feature to ever make the app WRITE a skeleton JSON. Wave 1 (49-01) built the engine + IPC seam: `scaleSummaryPeaks` (Layer-3-pure peak-only A1 — multiplies only `peakScale/X/Y` on a `structuredClone`, dims stay at master size so `buildExportPlan` is reused UNCHANGED), `VariantScaleError` (D-08 edge guard, core `bake()` stays direction-agnostic), the first-ever atomic skeleton-JSON writer `writeSkeletonJsonAtomic` (`.tmp`+rename, registers both paths before write), `handleExportVariant` (guard→read→`bake(json,s)`→`{NAME}@{s}x/`→write-JSON-first→`buildExportPlan(scaled)`→dispatch under one rollback Set; runtime-agnostic, never re-samples), and the `variant:export` IPC channel + `Api.exportVariant` type + preload binding (shipped Optimize flow byte-untouched, D-04). Wave 2 (parallel worktrees, disjoint files): 49-02 the renderer "Export Variant…" toolbar action + single-pane `VariantDialog` (no tabs D-06; reuses the Optimize output-mode/atlas/safety/sharpen controls + one numeric scale field D-05; dedicated picker-only `onConfirmStartVariant`); 49-03 the proof matrix — package-layout/rollback (V3/V4/V7), the dual-runtime drop-in faithfulness oracle (V6 — loads the WRITTEN package via `loadSkeleton`, asserts per-attachment + aggregate world-AABB == exactly s× master on 4.2 + 4.3), and a Layer-3 arch anchor (V8). Source-never-modified proven by sha256 byte-identity before/after a live export (V2). **Code review (standard depth, 16 files) found 1 BLOCKER (CR-01: VariantDialog judged success solely on a thrown error, ignoring `summary.errors[]` → a re-export silently overwrote the JSON while images were refused and reported success) + 6 warnings + 3 info; the owner chose "fix now"** → CR-01 + WR-01..06 all fixed (writer honors an overwrite gate; dialog surfaces per-row worker errors mirroring OptimizeDialog; try/catch→error-envelope around buildExportPlan; renderer rejection reaches the error state; `formatScaleToken` strips float-artifact folder names; collision sentinel covers all-passthrough; `variantExportInFlight` re-entrancy guard; IPC re-validates `safetyBufferPercent`) + 2 new regression tests; re-verification confirmed 12/12 still hold, CR-01 closed, ZERO `src/core/` diff from the fixes, Optimize flow still byte-untouched. Gates: `npm run typecheck` exit 0; full vitest **147 files / 1495 passed / 0 failures**. ONE human item remains: the native-dialog + visual half-size EXPORT-01 UAT (Electron picker + WebGL — structurally manual, not jsdom-exercisable; tracked in 49-HUMAN-UAT.md, non-blocking). ⚠ workflow.security_enforcement=true but no 49-SECURITY.md (NEW disk-write surface — run /gsd-secure-phase 49 before milestone close). Nothing pushed to remote (standing preference). Next: Phase 50 (Rig-Bounds + Two-Way Scale↔Dimension Input). Prior footer: 2026-05-22 — **Phase 48 COMPLETE (v1.7 Core Scale-Bake Module + Regression Oracle; 4/4 plans across 3 waves, verified 4/4 must-haves — BAKE-01..04 Complete).** Delivered `src/core/scale-bake.ts`: a Layer-3-pure (zero imports), clone-first non-mutating JSON→JSON similarity bake `bake(json,s)` + `ScaleBakeError`, field-identical to spine-core `SkeletonJson.scale` across both schemas (4.2 split `transform/ik/path/physics[]` + 4.3 unified `constraints[]`), with all three BAKE-03 constraint-timeline curve channels (IK softness `cy` idx 5/7 only / paired mix unscaled; PATH position/spacing length-mode setup+timeline; slider remap) + scaled-default injection (`physics.limit`→5000×s, `referenceScale`→100×s when absent; physics x/y NOT length-scaled) + D-09 degenerate-`s` / D-10 assert-known guards. The decisive sampling-free regression oracle `tests/scale-bake.spec.ts` (`parse(bake(orig,s),1)` ≡ `parse(orig, SkeletonJson.scale=s)`, reference generated LIVE from spine-core — NO golden numbers) runs in CI across 8 rigs × 3 scales [0.5, 0.26, 2.0] = 24 field-identity assertions + an 8-rig hard-fail fixture-existence guard — GREEN on first run (the bake was already field-identical). 8 oracle fixtures committed json+atlas-only (zero PNGs per D-04; incl. a synthetic 4.3 PathConstraint-Fixed rig D-05 + deform-heavy 4.2 TEST_01/TEST_03), proven git-tracked/fresh-clone-reachable (D-06a). **2 orchestrator post-merge gate fixes** (both caught by the post-merge build/test gate — its exact purpose): (1) extended the locked SAFE-01 `SAFE01_EXCLUDED_PREFIXES` denylist in `tests/safe01/discover-fixtures.ts` with the SCALE_BAKE_* dirs (newly git-tracked oracle fixtures, no frozen golden → would leak into the SAFE-01 enumeration/baseline gates; same class as Phase 44 skeleton2_42 / Phase 46 spineboy_4.3 — **a planning blind spot, no Phase-48 plan co-required it**); (2) localized `any` cast in the oracle's `parseAt()` for a dual-runtime union-of-modules typecheck error (vitest-green but `tsc`-red; the executor self-check ran vitest only, not typecheck:node). 48-REVIEW.md advisory: 0 blocker / 2 warning / 3 info — bake math independently verified field-by-field against installed spine-core sources; WR-01 (oracle deep-compare narrower than its "two-sided" header) FALSIFIED by the verifier under symmetric+length-strict hardening (only divergence = the id-keyed `timelineIds` StringSet, which SC#1 explicitly excludes — not a material weakness). 48-VERIFICATION.md PASSED 4/4 (verifier probed fixtures directly: TEST_01 has 18 deform timelines + all four 4.2 constraint types; DEMON has 178 unified 4.3 constraints — confirmed, not just claimed). Final: typecheck:node + typecheck:web exit 0; full vitest 141 files / 1450 passed / 0 failures. ⚠ workflow.security_enforcement=true but no 48-SECURITY.md (pure-`core/` math, no I/O surface — run /gsd-secure-phase 48 before milestone close for the paper trail). Nothing pushed to remote (standing preference). Next: Phase 49 (Single-Scale Variant Export) — consumes the bake module directly. Prior footer: 2026-05-19 — **Phase 47 COMPLETE → v1.6 milestone COMPLETE (Spine 4.3 Runtime Port — Dual-Runtime; 6 phases 42–47, 24 plans)**. Phase 47 (spine-player 4.3.0 bump + viewer regression): 4 active plans across 3 executed waves (47-01 PLAYER-01 prior; 47-03/04/05 the DV-1..DV-3 gap-closure set; 47-02 SUPERSEDED-by-47-05, closed by supersession). DV-1 dual-runtime viewer landed: a PURE `package.json` npm-alias trio (`spine-player-42`/`spine-webgl-42`→@esotericsoftware/spine-player@4.2.111 nesting spine-core@4.2.111; canonical top-level 4.3.0; no Vite/vitest alias, no shim) nests the frozen v1.5.1 4.2 stack alongside the migrated 4.3.0 path; `AnimationPlayerModalRouter` dispatches SOLELY on the REQUIRED core-resolved `SkeletonSummary.runtimeTag` (`'4.2'|'4.3'`, populated from `load.runtime.tag` in summary.ts — explicit identity, zero JSON/version sniffing; locks `feedback_explicit_identity_over_inference`, same bug-class as REG-47-01). `AnimationPlayerModal42.tsx` is the byte-verbatim `git show 9f967d2:` literal v1.5.1 modal + 2 DV-NOTE seds. **GA-1 falsification + owner decision (locked):** the literal v1.5.1 modal carries 11 strict-TS errors INTRINSIC to its own genuine 4.2.111 surface (v1.5.1 only ever gated tests+runtime, never strict `typecheck:web`; NOT 4.3 type-bleed — alias isolation verified, 0 errors outside the file) → escalated; OWNER chose (AskUserQuestion 2026-05-19) a sanctioned `// @ts-nocheck` sentinel as the 3rd transform; DV-NOTE re-scoped LOCKED to "byte-verbatim body + 2 seds + 1 @ts-nocheck sentinel"; the 47-05 owner UAT is the binding visual gate (CONTEXT D-02), not tsc; no "green v1.5.1 ref" exists — do not relitigate (memory `project_phase47_ga1_falsified_tsnocheck_amendment`). T-A/T-B/T-C/T-D headless guards 38/38 GREEN. DV-2 reworded PLAYER-02 in ROADMAP/REQUIREMENTS/Traceability — SAME ID (no PLAYER-03), GL-alpha + 5-UAT clauses preserved, 47-VALIDATION coherently rebuilt (`47-02-0`==0 / `through the 4.3 player`==0), NOT a descope. Owner ran the real `npm run dev` Electron app against the full DV-3 dual-runtime matrix (4.2 leg: SIMPLE_TEST + CHJWC_SYMBOLS + TQORW_SYMBOLS + TEST_03 via frozen 4.2.111; 4.3 leg: skeleton2 via migrated 4.3.0) and signed all 7 tests passed ("done, all seems good" — transcribed blanket sign-off per Phase 46 precedent; GL-alpha per-leg screenshots NOT captured, honestly documented as a non-blocking residual since the owner is the D-02 authority and approved). 41-HUMAN-UAT.md tests 2-6 flipped in-place to resolved with a Phase 47 pointer (audit trail preserved). **D-01 STRICT no-revert hold RELEASED** — no revert was needed (DV-1 ADDED the alias-isolated 4.2 stack; D-03: the bump is mechanically non-revertible). Regression gate: full vitest **138 files / 1371 passed / 0 failures** @ `66d809a` (the formerly-pre-existing MixBlend renderer import suites are now GREEN). 47-VERIFICATION.md PASSED 2/2 (verifier independently re-derived against the codebase: alias nesting, the `runtimeTag` thread end-to-end with no undefined-tag dispatch path, the byte-verbatim-body diff == only the sentinel, zero `src/core/` diff, the 4.3 leg byte-untouched). 47-REVIEW.md advisory: 0 critical / 1 warning / 3 info (WR-01: `LoadResult.runtime` typed optional but treated as required in summary.ts — non-blocking type-expressiveness nit, the guard works + T-A empirically proves the chain). ⚠ workflow.security_enforcement=true but no 47-SECURITY.md (run /gsd-secure-phase 47 before tagging/shipping v1.6). Resolved debug sessions still in `.planning/debug/` (untracked): `viewer-43-42-constraint-parse` (closed by DV-1) + `reg-47-01-43-load-reading-r` (fixed `53e480c`) — move to `.planning/debug/resolved/` at milestone-close housekeeping. Nothing pushed to remote (standing preference — milestone tags/pushes stay local until ship-decision). Next: v1.6 milestone-close housekeeping (`/gsd-secure-phase 47`, then `/gsd-complete-milestone`); ship decision (tag `v1.6.0`) is the owner's. Prior footer: 2026-05-18 — Phase 46 COMPLETE (slider constraint validation + 4.3 performance budget; 2 plans, 1 wave, sequential main-tree — 46-01 carried a `checkpoint:human-action` incompatible with worktree isolation, 46-02 ran autonomously during the owner's editor-read dead time; verified PASSED 3/3 — SLIDER-01/SLIDER-02/PERF-01). The 4.3 `SliderConstraint` propagates through the UNCHANGED `updateWorldTransform(Physics.update)` path with ZERO slider-specific `src/core/` code (machine-asserted 0 `src/core/`+`src/main/` diff — the proven absence of slider code IS the deliverable). SLIDER-02 closed-form oracle: sampled `square` peakScale == hand-derived `4.0` (`1+3·clamp(p.time,0,1)` at `p.time=0.005·200=1.0 s`, source-cited from vendored `spine-core@4.3.0 Slider.update()`, verifier-re-derived). D-05 triangulation closed: hand-math 4.0 ≡ owner Spine-4.3-editor read 4.0 (`fixtures/SLIDER_4_3/NOTES.txt`) ≡ sampled runtime 4.0. PERF-01 CI-ENABLED (no `skipIf`) `sampler-worker-spineboy43.spec.ts` gates warmed `runSamplerJob` on the committed redistributable spineboy-pro 4.3 rig (spine 4.3.01, 67/52/11/14) `< BUDGET 1479 ms` (`⌈measured×3⌉`, BUDGET re-anchored 378→1479 for contended-env calibration; D-04 denylist co-extended with `fixtures/spineboy_4.3/`); 4.2 parity NOT assumed (606 ms N2.2 = logged descriptive reference only). Post-merge full vitest 1280 passed / 0 actual failures (11 pre-existing Phase-47-owned MixBlend import suites). 46-REVIEW.md advisory 0 critical / 2 warning / 3 info (non-blocking robustness nits to plan-specified test code). ⚠ workflow.security_enforcement=true but no 46-SECURITY.md (run /gsd-secure-phase 46 before milestone close). Next: Phase 47 (spine-player 4.3.0 bump + viewer regression — only open v1.6 phase; decoupled + revertible + sequenced last). Prior footer: 2026-05-18 — Phase 45 COMPLETE (dispatcher user-facing flip + copy/docs sweep; 2 plans, 1 wave, parallel-safe, + 1 orchestrator-directed D-12 typecheck fix; verified passed 3/3 ROADMAP criteria — UX-01, UX-02). Pure user-facing copy/docs sweep, ZERO runtime/loader/dispatch/errors behavior change (`src/core/*` + `src/main/doc-export.ts` byte-identical to phase base): App.tsx drop-zone → "Drop a Spine v4.2 or v4.3 JSON file…" (D-01/02/03, locked D-02 dual red tokens); HelpDialog:128 + README:3/15 → affirmative "Spine 4.2 and 4.3" band, README:29 → exact `<4.2`/`≥4.4` typed-reject band (D-04/05); net-new repo-root CHANGELOG.md ("Spine 4.3 skeleton support (dual-runtime)", D-06); D-09 doc-export confirm-and-no-op with grep evidence; renderer/docs UX-02 grep clean under D-08 allowlist. Test half: 10-file Phase-44-D-11 disposition audit (verify-only, zero false-greens, `<4.2`/`≥4.4` throws preserved verbatim) + permanent in-suite D-12 anti-false-green standing guard (4 in-repo 4.3 fixtures, handleRuntime dispatch-target triplet, 4/4 green). 45-REVIEW.md advisory: 0 blocker / 2 warning / 2 info (README:5 stale `v1.3.6` pointer [D-07-deferred external surface], D-12 triple-invoke; non-blocking nits). Pre-existing out-of-scope: `typecheck:node` red from ~100+ v1.0-era tracked `scripts/probe-*`/`diagnose-*` Spine-API-drift + a Phase-44 test-family `SkeletonData`/`OpaqueHandle` brand gap (predates Phase 45, contradicts the STATE.md/ci.yml "clean on fresh clone" note — separate non-Phase-45 concern); Phase 45's lone net-new typecheck error fixed via the sanctioned boundary cast (commit `7c66505`), new guard typecheck-clean. Full vitest 1277 passed / 0 actual failures (11 pre-existing Phase-47-owned MixBlend import suites). ⚠ workflow.security_enforcement=true but no 45-SECURITY.md (run /gsd-secure-phase 45 before milestone close). Next: Phase 46 (slider constraint validation + 4.3 performance budget). Prior footer: 2026-05-18 — Phase 44 COMPLETE (loader dispatch + equivalence oracle + 4.3 fixture authoring; 4 plans across 3 waves + 1 orchestrator-directed 44-CORE-FIX; verified passed 5/5 ROADMAP criteria — DISP-01/02/03, ORCL-01/02/03, XTRA-01/02). Loader flipped rejecter→dispatcher: `resolveRuntimeTag` is the sole loader gate (44-03 Rule-3 fix removed the dead pre-flip `checkSpineVersion`/`checkSpine43Schema` call sites that made the 44-02 flip dead code for 4.3); 4.2→4.2 runtime, 4.3→4.3 runtime, `<4.2` + new split-out `≥4.4` typed-reject arms preserved; dispatch verified across all 3 entrypoints (vitest + built CJS worker + tsx/ESM CLI). Owner-exported 4.3 rigs + ORCL-02 4.2-sibling committed in-repo byte-untouched. ORCL-02 HARD gate (D-12 all-3-maps / D-13 hybrid comparator / D-14 no-waiver) caught a genuine Phase-43-escaped runtime-43 mesh-UV undersize defect (region-space `regionUVs` vs required page-space; exactly 2.251× too small on every 4.3 weighted/region mesh) → fixed `2d0246c` via `MeshAttachment.computeUVs`; cross-runtime CIRCLE mesh now rel-divergence = 0 vs runtime-42; stale 4.3 own-baselines (incl. pre-existing `tests/runtime43/baselines/skeleton2.json`) deliberately re-captured under D-14 — proven only CIRCLE records moved by the documented ratio, within 1e-4. SAFE-01/SAFE-02 4.2 byte-gate not regressed (53/53); full vitest 1273 passed / 0 actual failures (11 pre-existing Phase-47-owned MixBlend import suites excluded). ORCL-03 = v1.6 NO-OP (IK absent in both rig JSONs). 44-REVIEW.md advisory: 0 blocker / 5 warning / 6 info (oracle/sentinel test-hardening; core fix independently traced correct). ⚠ workflow.security_enforcement=true but no 44-SECURITY.md (run /gsd-secure-phase 44 before milestone close). Next: Phase 45 (dispatcher user-facing flip + copy/docs sweep). Prior footer: 2026-05-17 — Phase 43 COMPLETE (runtime-adapter facade + verified 4.3 API mapping; 6/6 plans incl. gap-closure 43-06; verified passed 5/5 ROADMAP criteria — RT-02/SAFE-02/SAFE-03/PORT-01/PORT-02/PORT-03). `SpineRuntime` facade live (sampler.ts/bounds.ts call through `load.runtime.*`, arch.spec.ts-anchored; runtime-42/runtime-43 adapters); SAFE-02 32/32 byte-equal vs frozen Phase-42 baseline; D-04 heavy-rig 32/32 close-gate passed (43-05). GAP-43-PROD-SEAM CLOSED (43-06): electron.vite.config.ts emits `out/main/runtime-4x.cjs` resolvable entries + pickRuntime prod literal `./runtime-4x.js`→`../runtime-4x.cjs`; build-required spawn-smoke falsifier RED→GREEN proven; LOCKED Option-A constraints (a)-(d) byte-preserved. Maintainer-adjudicated §4 bounded exception 2026-05-17 (Option ii): §4 single-copy scoped to the spine-core runtime/animation graph (4.2 path strictly clean); the pre-existing 43-04 parse-time `AtlasAttachmentLoader` edge (runtime-43→synthetic-atlas→spine-core-42, 1× bare require in runtime-43.cjs) accepted as documented bounded exception, decouple deferred (tracked non-blocking). 43-REVIEW.md advisory: 0 critical / 4 warning / 3 info — all 4 warnings are spawn-smoke test-harness quality (worker leak on fail paths, falsifier blind-spot, build-as-side-effect, mtime guard), core fix independently confirmed correct; not blocking. ⚠ workflow.security_enforcement=true but no 43-SECURITY.md (run /gsd-secure-phase 43 before milestone close). Renderer 4.2-only MixBlend (11 tests/renderer/* red + spine-player build abort after main emits) remains ROADMAP-Phase-47-owned, not a regression. Next: Phase 44 (loader dispatch + equivalence oracle + 4.3 fixture authoring). Prior footer: 2026-05-16 — Phase 42 COMPLETE (pre-v1.6 4.2 baseline + npm alias + boundary scaffolding; 5/5 plans incl. gap-closure 42-05; re-verified passed 5/5 — SAFE-01/RT-01/RT-03/RT-04/CI-01). Dual-install live (`@esotericsoftware/spine-core@4.3.0` canonical + `spine-core-42`=`4.2.111` alias); frozen ancestry A `1b5327d`→B `cc5783f`→repoint `1a8c18b`→C `b6f3177`→D `2360c51` is the SAFE-01 acceptance test. 42-REVIEW.md advisory: 0 blockers / 2 warnings (asymmetric CR-01 baseline-side hardening — low-priority Phase-44/45 follow-up). 11 `tests/renderer/*` MixBlend failures deferred to ROADMAP Phase 47 (renderer 4.3 port), not a regression. Next: Phase 43. Prior footer: 2026-05-16 — v1.6 milestone STARTED (Spine 4.3 Runtime Port — Dual-Runtime). SEED-006 trigger fired (`npm view @esotericsoftware/spine-core@latest` → `4.3.0`; spine-player@4.3.0 also published). User decisions 2026-05-16: v1.6 (not v2.0); dual-runtime (4.2 + 4.3 side-by-side, not single-port); bump spine-player → 4.3.0. Continues phase numbering at Phase 42 (no --reset-phase-numbers). Research → requirements → roadmap cycle pending. Prior footer: 2026-05-15 — Phase 41 COMPLETE (Spine Animation Viewer; 3 plans landed; basic-render UAT confirmed live after 3 follow-up fixes for CSP/CORS/PMA). 5 HUMAN-UAT items remain partial. Prior footer: 2026-05-15 — v1.5.1 milestone STARTED (Spine Animation Viewer; viewer-only scope per user, polish items deferred). Continues phase numbering at Phase 41. Research skipped (scope locked by SEED-009 design facts 1–4). v1.5 phase directories 36–40 retained in `.planning/phases/`. Prior footer: 2026-05-15 — v1.5 SHIPPED (Override Routing + Coverage Hardening + Atlas Repack). 5 phases (36, 37, 38, 39, 40), 23 plans, 18 documented REQs (OVR-01..07, TIMELINE-01..05, POLISH-01..03, WINUAT-01..03) + 10 REPACK REQs (archived). Tag `v1.5.0` pending push. Test suite at close: 1181 passed / 2 skipped / 2 todo / 0 failures (108 files); `tsc --noEmit` clean. SEED-005 + SEED-007 + SEED-008 closed. 8 Phase 40 polish items (4 WARN + 4 INFO) explicitly deferred per user — backlog for v1.5.2 / v1.6. Milestone audit at `milestones/v1.5-MILESTONE-AUDIT.md` (passed; 18/18 + 10/10).*

<details>
<summary>Prior phase footer (v1.4 + v1.5 mid-flight detail)</summary>

*2026-05-13 — Phase 39 (Windows-Host-Blocked UAT Burndown, WINUAT-01..03) COMPLETE. 3 plans, 17/17 must-haves verified. Host-availability decision `host_available: yes` recorded in `.planning/phases/39-windows-host-blocked-uat-burndown/39-CONTRACT.md` — user confirmed Windows host + v1.3.1+ installer available, both UATs executed LIVE. WINUAT-01 (Phase 20 DocBuilder DnD on Windows) `passed` (all 4 observations: drag image rendered, mixTime=0.25, loop=false, console clean); pending Phase 20 cross-platform DnD todo retired to `.planning/todos/resolved/` with `## Resolved` close-out. WINUAT-02 (Phase 31 admin DnD release UAT) `passed` (all 4 observations: verbatim advisory copy, drag-over ring suppressed, File→Open functional, normal-relaunch DnD restored); `.planning/milestones/v1.3.1-phases/31-loader-ux-small-fixes-batch/31-HUMAN-UAT.md` item 1 flipped `deferred` → `passed (2026-05-13)`, Summary `passed: 3→4` + `pending: 1→0`, frontmatter `status: partial→passed`; pending Phase 31 admin DnD todo retired to `resolved/`. WINUAT-03 satisfied — both long-lived UAT todos (2026-05-01 Phase 20 + 2026-05-08 Phase 31) cleaned from `pending/`. Zero `src/` or `tests/` files touched (purely procedural UAT close-out + audit-trail). Full vitest suite 1081 passing post-completion. Code review: `skipped` (no source files in scope). Two path inaccuracies corrected in 39-CONTRACT.md §3 (Phase 20 `20-HUMAN-UAT.md` was deleted in commit `0787fe1` so the surviving anchor is the todo; Phase 31 `31-HUMAN-UAT.md` lives at the archived v1.3.1-phases path, not the ROADMAP-referenced path). Prior phase: Phase 38 (Phase 4 Code-Review Polish Pass, POLISH-01..03) COMPLETE. 3 plans, 4/4 must-haves verified. POLISH-AUDIT enumerated 7 deferred Phase 4 findings (IN-01..IN-06 + WR-03) against current source — 1 applies (IN-02 drag-to-cancel overlay guard: `onMouseDown` + `e.target === e.currentTarget` replaces `onClick={props.onCancel}` in `src/renderer/src/modals/OverrideDialog.tsx`, regression spec at `tests/renderer/override-dialog-drag-to-cancel.spec.tsx`, two atomic commits per Phase 27 TDD precedent), 5 no-ops (IN-01 swept by Phase 6 Gap-Fix R6 commit `5551073`; IN-03 by Phase 27 QA-02 `fb3fedc`; IN-05 by Phase 27 QA-03 `01468e4`; IN-06 by Phase 27 QA-04 `cf098e0`; WR-03 by Phase 27 QA-01 `f7668c4`), 1 skip (IN-04 `highlightMatch` duplication between `GlobalMaxRenderPanel.tsx` and `AnimationBreakdownPanel.tsx` intentional per Phase 2/3 self-contained-panel pattern; roadmap "SearchBar.tsx" wording was a slip, corrected in audit). v1.0-era `2026-04-24-phase-4-code-review-follow-up.md` retired from `pending/` to `resolved/` with audit-trail-intact close-out. Full vitest suite 1081 passing post-merge. Prior phase: Phase 36 (Split Overrides Per Loader Mode, OVR-01..07) COMPLETE. 5 plans, 14/14 must-haves verified. Two-bucket override model: `overridesAtlasLess` field added to `ProjectFileV1` + `AppSessionState` (schema-additive, no version bump). Open seam routes legacy single-bucket overrides by saved `loaderMode` (Decision 2-A, gated on on-disk key presence per WR-03 fix). `migrateOverrides` invoked per-bucket at all three seams (Open, recovery, resample) with summed `migratedKeyCount` + unioned `staleOverrideKeys`. AppShell exposes `activeOverrides` memo for reads, OverrideDialog writes target active bucket only, mode-switch leaves inactive bucket untouched (Decision 3-A). Mode-toggle toast (D-01..D-04) with `stm.overrideModeToast.suppressed` localStorage flag. Code review surfaced CR-01: `handleProjectResample` originally ignored `loaderMode` for bucket routing — fixed by extending `ResampleArgs` with `overridesAtlasLess?` and routing by bucket-name in both renderer (send both buckets) and main (route by name). 3 new main-side regression tests in `tests/main/project-io.spec.ts`. SEED-007 closed.

Milestone v1.5 (Override Routing + Coverage Hardening) STARTED 2026-05-13. Scope: SEED-007 (split overrides per loaderMode — decisions 1, 2-A, 3-A locked), SEED-005 Level B (RGBA2 + InheritTimeline coverage hardening via audit + fixture), 3 long-lived pending todos (Phase 4 code review + Phase 20 Win DnD UAT + Phase 31 Win admin DnD UAT). SEED-006 (full 4.3 runtime port) explicitly deferred — gated on `@esotericsoftware/spine-core@4.3.x` npm publish. Continues phase numbering from v1.4 → Phase 36+. Prior milestone: v1.4 (Spine 4.3 Forward-Compat + Rotated Atlases) SHIPPED 2026-05-12. 4 phases, 18 plans, 14 REQs (COMPAT-01..02 + ATLAS-01..04 + OPEN-01..05 + DEDUP-04..06). Phase 35 (Region-keyed export plan, DEDUP-04..06) closes the multi-skin atlas-source undercount on `fixtures/SKINS/JOKERMAN_SPINE.json`: `buildExportPlan` in both core (`src/core/export.ts`) and renderer parity (`src/renderer/src/lib/export-view.ts`) iterates `summary.regions` (RegionRow[]) instead of attachment-name-deduped peaks, so 160 atlas regions now produce 160 ExportRows. Phase 34 (File→Open accepts `.json`, OPEN-01..05) — two-IPC-step `'project:open-dialog'` flow with dirty-guard-after-picker (amends Phase 08.2 D-183 for the menu path); HUMAN-UAT 1 scenario host-blocked. Phase 33 (rotated atlas region support, ATLAS-01..04) — `RotatedRegionUnsupportedError` removed atomically; loader D-01 attachment-walk for canonical-corner offset override + AABB W↔H swap in `bounds.ts` + `sharp.rotate(+90)` materialization; libgdx atlas convention nuance documented (bounds W/H is canonical, page-pixel rect is `(H × W)`). Phase 32 (4.3-beta detect-and-warn + drop-zone v4.2 disclosure + SEED-006 plant, COMPAT-01/02) — `checkSpine43Schema` sniffs `root.constraints` OR `skeleton.spine ≥ 4.3` before atlas resolution; structured `SpineVersionUnsupportedError` replaces the misleading `IK Constraint not found: <name>` symptom; SEED-006 planted for full 4.3 runtime port. Prior milestone shipped: v1.3.1 (2026-05-09) — Phase 29/30/31. Tag `v1.4.0` pending push.*

</details>
